# AGENTS.md — 작업 에이전트용 운영 노트

> **권한 연대기(문서 범위 상태 구분):** 아래 변경 로그의 Cedar/PBAC "완료/시행"은 `.dc.html` 목업 또는 작성·시뮬레이션 계약을 뜻한다. 별도 공존 맵 승격 증거가 없는 한 이 문서가 전제하는 상태는 레거시 서버 권한/미들웨어와 PostgreSQL RLS 집행, Cedar target/shadow다. 이 상태 구분은 배포·런타임 검증 증거가 아니며, 실제 집행 상태는 별도 운영 증거로 검증해야 한다.
> CLAUDE.md(불변 헌장 포인터) · DESIGN.md(설계 헌장) · TODO.md(로드맵) · HANDOFF.md(백엔드 인수인계)와 함께 읽는다.
> 이 파일은 **세션 간 작업 컨텍스트**(아키텍처 지도·관례·변경 로그)를 담는다. 매 작업 후 갱신한다.

## 1. 산출물 · 아키텍처
- 산출물 2종: **`Oyatie Console.dc.html`** (데스크톱/웹 콘솔 — 전 화면·상태·로직 단일 파일) + **`Oyatie Mobile.dc.html`** (모바일 직원 앱 뷰 — iOS 프레임 + 콘솔 iframe 390px; 콘솔이 <768에서 7종 모듈·하단 탭 바·모바일 시트로 자체 전환. ios-frame.jsx 스타터).
  - **템플릿**(`<x-dc>` 내부, 상단~약 절반): 인라인 스타일만. 화면은 `<section grid-area:dashArea>`의 형제 블록들 + 좌 사이드바 + 우 커뮤니케이션 rail + 모달들(개인수신함 passkey·직원-일자·대근 인력풀·기록물 등재·토큰 드롭다운 등).
  - **로직 클래스**(`class Component extends DCLogic`, 하단 절반~끝): 생성자에 정적 데이터·`state`, 그 아래 메서드, 맨 끝 `renderVals()`가 템플릿에 값 주입. (줄번호는 자주 바뀜 — grep으로 앵커를 찾아 편집)
- 편집은 `dc_html_str_replace`(템플릿·스트리밍) / `dc_js_str_replace`(로직·핫리로드). 대량 기계적 치환만 `run_script`.
- 스타일: **인라인만**. `var(--*)` 토큰은 `tokens/*.css`(및 `.console` 테마 블록)에 정의 — 새 토큰 추측 금지. 비주얼은 바인딩된 디자인 시스템(`/projects/019dc2a0-992f-7697-b778-ef3dee4670bd/`)을 따른다 — 새 토큰·컴포넌트가 필요하면 거기서 복사.

## 2. 화면(state.screen) · 좌측 nav
overview · hr · recruit(지원자 카드 양방향) · org · review · att · pay · appr(계약 가드레일·유사 건 점검) · leave · benefit · docs · policy(**Cedar no-code 캔버스**) · inbox · audit · auto(블록 빌더) · ingest · explore(작용 자동화) · mail(개체 첨부·egress) · **msgr(메신저 풀뷰 — Slack/Teams 패리티)** · notif(알림 풀뷰) · map(전술 지도) · dashboard · **모듈 서피스 10종**(finance·purchase·inventory·asset·maintenance·field·compliance·laborcost·board·directory).
**nav 스텁 = 0** — 전 nav 항목이 실화면. 메일·메신저·알림은 rail(요약)↔풀뷰(main) 이중 서피스 — 동일 state 공유.

## 3. 핵심 시스템(재사용할 문법)
- **핀/창 모델(카드·패널 공통)**: 헤더 드래그=팝아웃(자유 플로트) · 더블클릭=핀(분할 패널, 본문 `padding-right`로 실제 공간 확보) · 트레이 드래그/버튼=최소화. hover 3버튼 컨트롤=핀·최소화·X. behavior는 `cardVal`/`cardToolVals`/`cardGrab`/`cardPinRight`/`cardRestoreDefault`(1곳), 상세 패널 chrome은 단일 `<sc-for panels>` 헤더(1곳). 참조 핀=`cardFloat`(fixed+visible, 화면 이동 유지). 쿼드런트 핀=`panels`+`snapTo`.
- **목록**: J/K/Enter, 다속성 검색, 열폭 드래그, 공유 트랙 정렬, 끝 여백+페이드.
- **개인 수신함/passkey**: `inboxDocs`(state) · `pkModal`/`pkPhase` · `pkStart→pkAuth→inboxConfirm`(열람=수령 증빙 기록). 법적 문서 `legal && !confirmed` = 잠김.
- **연차촉진/노무수령거부**: `lvPromotePush`(1·2차 AP-)·`lvRefusalPush` → `apprMySubs`(상신함) + 대상자 개인 수신함 전달(수령확인 대기).
- **감사 백본**: `logEvent(partial)` — 모든 상태 전이가 호출(who/what/when/where/how/on-what/decision/integrity + 데이터분류 + `deviceCtx` + seq 해시체인). `screen:"audit"` 라이브 피드. 상태 전이는 반드시 이벤트/역참조 남김. 토스트에 결과+경로 명시.
- **토큰 문법(앱 전역)**: `tokenParse/tokenRender`(표시) + `tokenType/tokenKey/tokenPick`(컴포저 자동완성) + `tokenCatalog/tokenVisible`(PBAC 후보 게이트 — covert·등급초과 제외). @멘션·#개체·!코드·바코드·날짜. 새 입력창에 이 컴포저를 붙여 확장.
- **근태 타임라인**: `buildAttDayTL`(오늘 = 계획/실적 2트랙 시간축, 상태=개체). **월간(`buildAttMonth`)도 계획 vs 실적**: 상태 어휘 `lv`(유급휴가)·`pa`(승인 부재=무급/신청 승인, 계획 부재·차분)·`ab`(무단결근=실적 이탈)·`abc`(결근·대근 대체=결원 커버). `pa`/`lv`는 출근율·결근 집계 제외(승인), `abc`는 본인 결근 계상 유지. `abc`+대근 배지(person `subBadge`·노드 `coveredIn`)는 `s.subAssignments`(coveredWho) 라이브 반영 — renderVals `mData`에서 계산(캐시 무효화 불요). **대근/인력풀**: `WORKFORCE_POOL`·`subOpen/subAssign`(결원→인력풀 배정→AP-+타임라인 fill-in). **직원-일자 개체**: `openEmpDay`(계획·실적+그날 감사+급여영향+커뮤니케이션, 분류별 게이트). **기록물 등재**: `docRegOpen/docRegSubmit`(IN- 등재대기→승인).
- **자동화**: `workflows`/`schedules` + `wfRun/wfSimulate/schRun`(실행 시 logEvent·실제 AP- 생성). `screen:"auto"`.

## 4. 관례
- **폐루프 페이지 리뷰 프로토콜 (DESIGN §4-25 — 매 세션 상시)**: 사용자 가시 페이지를 폐루프로 순회 — ① 최선 패턴인가(직관·인체공학·구성 가능·유지보수) ② 모듈+전사 워크플로·엣지 케이스 커버 ③ 마찰 예측·시선 흐름 ④ 도메인 source-cited 벤치마크(§4-21 3문) ⑤ 타 모듈 통합·재사용 컴포넌트 완전 활용(§4-18) ⑥ **목업 독립성**: 모든 데이터 = state 파생 or UI 생성 경로 보유 — 스텁·하드코딩-전용은 갭, 발견 즉시 등재·마감. 리뷰 없는 "완료" 선언 금지.
- 한국어 우선, 명사구 라벨, 상태=칩, 수치·코드=모노. **설명문·안내 배지 금지**(DESIGN §4.12) — 디자인이 스스로 말한다.
- 개체 코드: `AP-`(결재) `WO-`(정비) `AT-`(근태) `C-`(계약) `JL-`(업무일지) `PS-`(급여명세) `IN-`(접수).
- 새 기능 = 반드시 상·하류 개체 링크 + 감사/역참조 + 한 번의 클릭 이동(DESIGN §5 통합 원칙).

## 5. 변경 로그
- **2026-07-04**: (1) **핀 모델 통일** — 카드 hover 5버튼 툴바 제거 → 드래그=참조 핀 분리·더블클릭=우측 고정(토글); 상세와 동일 문법. (2) **개인 수신함 슬라이스 완료·검증** — 데이터모델+화면(2-pane 볼트)+passkey 열람 게이트 모달(스캔 애니)+연차촉진/노무수령거부 AP-화. 급여 셀프서비스→수신함 연결. 검증: 근로계약서 passkey→수령확인 스탬프, AP-3126 이종호 촉진 1차 상신. 아이콘 추가(mailbox·fingerprint·lock·scroll·gavel). DESIGN §4.7 카탈로그 + TODO 2·3·4 완료 갱신, HANDOFF.md 신설.
- **2026-07-04 (2)**: **감사 로그 완료·검증** — `logEvent` 이벤트 백본 + 8개 액션 연동, 라이브 피드(일자 그룹·최신 상단)·필터·전문검색·확장 상세(Cedar deny·before→after·세션/IP)·상관 drill·이상/정책거부 색상·통계바·내보내기. covert=deny-by-omission. 검증: 박지훈 급여 열람 forbid 상세, AP-3126 촉진 상신이 피드 최상단 실시간 반영. 아이콘 eye·ban·activity·download·alert 추가. TODO #22 완료.
- **다음 (2026-07-04 시점 · 이후 완료 — 참조용 히스토리)**: 워크플로 스튜디오+예약작업, 객체 탐색(관계 그래프), in-screen 스텁, 토큰 문법 @#! → **전부 완료(로그 하단 참조). 현재 「다음」은 파일 최하단 단일 블록 참조.**
- **2026-07-04 (3)**: 대량 세션 — (a) **감사 텔레메트리 표준화**(who/what/when/where/how/on-what/decision/integrity + 데이터분류 + 기기/위치/브라우저 `deviceCtx` + Cedar 결정이 로깅 구동 + seq 해시체인; NIST 800-53 AU·ISO 27001·CADF/OCSF). (b) **창(window) 모델**: 헤더 드래그=팝아웃·더블클릭=핀(분할, 본문 padding 확보)·트레이 최소화, 트레이 복원=자유 오버레이, 그립 삭제. (c) **모바일 오프캔버스 드로어**(<768: 사이드바·rail fixed transform, 햄버거·커뮤니케이션 헤더 버튼·백드롭). (d) **자동화 화면**(워크플로 트리거→조건→실행·예약 cron). (e) **근태 오늘 = 계획/실적 2트랙 시간축 타임라인**(`buildAttDayTL`; 상태=개체, 일용직 대근/승인휴가 반영). (f) **직원-일자 개체**(`openEmpDay`; 타임라인+그날 감사+급여영향+커뮤니케이션, **분류별 정책 게이트**, 건강=비밀 locked). (g) **기록물 등재**(`docRegSubmit`; 파일 드롭+메타→IN- 등재대기→기록관리자 승인). (h) **앱 전역 토큰 문법**(`tokenParse/tokenRender/tokenType`; @#!·바코드·날짜, PBAC 후보, 컴포저 자동완성). PDF 4종 `pii/`. 한국어 통일.
- **2026-07-04 (4)**: **월간 근태 = 계획 vs 실적** 완료·검증. `attStatus`/`buildAttMonth`에 `pa`(승인 부재) 어휘 + 집계 제외, renderVals `mData`에 `abc`(결근·대근 대체, red+surface 커버 dot)·`pa`(purple-tint)·`ab`(무단결근, red) 색 구분 + 툴팁(대체자 명시). `subAssignments` 라이브 반영: `coveredWhoSet`→피대체자 셀 `ab`→`abc`, `collectNames`/`coveredIn`→대근 배지(개인 "대근 대체"·노드 "대근 N")가 팀→사업장→법인 상향 전파. 셀 템플릿에 내부 커버 dot(`c.dotOn`/`c.dot`), 이름열 배지, 범례(승인 부재·무단결근·대근 대체) 갱신. OV 데모: 조이슨 7/3 무단결근→이대체 대근→`abc`, 이종호 `pa`, 6/16 조이슨 `pa`. 검증: 계산색(danger-solid/purple-bg+bd/커버 dot=surface)·배지 상향 전파 DOM 확인. TODO 인력·근무 스케줄링 「월간 뷰 계획 vs 실적」 완료.
- **2026-07-04 (5)**: **문서·데이터 통합 에픽 착수·문서화**. 아이콘 scan/database/table/transform/fileText/upload/xCircle/plug/edit 추가. **데이터 인제스트 화면**(`screen:"ingest"`) 착수 — 소스(파일 11종+사진·영상·ZIP·임의+외부 API 커넥터)→**결정적(no-AI) 파이프라인**(파싱/OCR→정제→분류·템플릿→매핑→검증→온톨로지 적재)·필드 매핑 검토(신뢰도·출처)·DX- 코드. **워크플로 외부 API 인제스트**(씨드 워크플로 2개+데이터 파이프라인 액션). 벤치마크 Palantir Foundry(Pipeline·Data Connection·Ontology·Lineage)·Rossum/ABBYY/Textract(결정적)·Airbyte/Fivetran·Rust. **문서화(다음 슬라이스)**: 증거능력 보존 아카이빙+미디어/ZIP(WORM·해시·TSA·chain-of-custody·인콘솔 뷰어·ZIP readonly), 인콘솔 오피스 편집기(Euro-Office=ONLYOFFICE DocumentServer AGPL **헤비 포크**, 감사·PBAC·컴플라이언스·비밀보호 내부 통합, 호스트가 버전·롤백·승인·협업 소유). DESIGN §6·HANDOFF §10·11·12·TODO 신규 4섹션. **(6) DLP·화면보호 문서화**: 정직한 위협 모델 — 웹앱=억제(user-select·앱내부 클립보드·canvas·워터마크·blur-on-blur·print 인터셉트)+추적(감사·워터마크)+게이트(PBAC 관리기기·passkey), **완전방지=엔터프라이즈 브라우저(Edge for Business/Purview·Netskope/Island)·VDI/RBI·엔드포인트 DLP 배포요건**(WASM도 동일 샌드박스, 완전차단 불가·보안극장 금지). HANDOFF §13·DESIGN §4.5·TODO 신규 섹션.
- **2026-07-04 (6)**: **ROADMAP.md 신설**(마스터 빌드 블루프린트 — 품질기준·온톨로지 그래프·교차시스템·모듈 매트릭스[벤치마크]·시그니처 상관 데모·실행 페이즈). **P1 데이터 인제스트 화면 완료·검증**(`screen:"ingest"`): 소스(파일11종+사진·영상·ZIP+API 커넥터)·큐(필터·검색·JK)·7단계 결정적 파이프라인·출처 프리뷰(스캔 OCR영역·표·JSON·미디어·ZIP·실패)·필드 매핑 검토(신뢰도·PII·검증/수정)·온톨로지 적재. `ingest*` 로직(advance/upload/connPoll/fieldEdit/verify/commit/autoToggle/JK). 워크플로 wf5·wf6(외부 API 인제스트). 검증: 계약서→C-208, 나라장터→Bid-633 적재+감사 이벤트.
- **2026-07-04 (7)**: **P1 객체 탐색(Object Explorer) 완료·검증** — `screen:"explore"`, `ONTOLOGY_GRAPH()`(20노드 typed 개체+엣지)·`exploreGo/exploreBack`(재중심·트레일). 방사형 그래프(SVG 엣지+절대배치 노드칩)+중심 개체 카드+상류/하류 링크 패널+범례. 계약→인력→근태→급여→수익성 전 체인 추적. nav 「객체 탐색」 배선. 검증: c207→att_cho→pay_cho 트래버스, 20노드.
- **다음(우선)**: 자동화 no-code 캔버스 → 대시보드·인건비/수익성(P2) → 컴플라이언스 → 증거 아카이빙(WORM·미디어/ZIP) → 오피스 편집기 셸 → 커뮤니케이션(메일=**mox** 백엔드+자체 UI) → ERP·현장운영 (ROADMAP 페이즈 순).
- **2026-07-04 (8)**: **메일 백엔드 = mox 결정 문서화**. mox(github.com/mjl-/mox · Go · **MIT**) = all-in-one 보안 메일 서버(IMAP4rev2·SMTP·SPF/DKIM/DMARC·MTA-STS·DANE·암호화 저장·webapi/webhooks). 자체 프런트 UI(커뮤니케이션 모듈 rail↔main) + mox 개조(감사·Cedar PBAC·컴플라이언스[법적보존·저널링·e-discovery]·DLP·온톨로지 Mail=CommObject·첨부→인제스트/증거·↔AP-). MIT라 포크 자유(오피스 AGPL 대비 유리). HANDOFF §14·DESIGN §6-10·TODO 커뮤니케이션 섹션·ROADMAP 매트릭스.
- **2026-07-04 (9)**: **커뮤니케이션 > 메일 풀뷰 완료·검증**. `screen:"mail"` — **mox 백엔드(자체 프런트)** 3-pane(폴더 7종·리스트·리딩), 13메일 시드(inbox/sent/draft/archive/spam/trash), 발신자 인증(SPF/DKIM/DMARC)·TLS·저장암호화 보안 패널, 분류(대외비·민감·격리)·PBAC·보존·litigation hold 거버넌스, 첨부→인제스트(DX-)/증거 등재, 연결 개체 이동, 컴포저(분류·DLP 외부발송 경고·mox SMTP·DKIM 서명), 스팸=DMARC fail 피싱 예시, 열람·발송 전량 감사. 나머지: 메신저·공지 풀뷰. HANDOFF §14·ROADMAP.
- **2026-07-04 (10)**: **메일 UX 정리** — 3-pane를 **단일 서피스**(폴더·리스트·리딩 = 한 카드 + 얇은 구분선, 부유 카드 갭 제거)로 통합 + **설명 캡션 제거**(mox 칩·보안 산문·거버넌스 산문·"이 열람은 감사됨" 메타·빈상태 부제·컴포저 캡션 — 상태 칩·피싱 경고만 유지). 규칙 강화: CLAUDE.md·**DESIGN §4-12**(부제·기술/프로토콜 캡션·메타 안내·칩 반복 산문 금지). **다음: 설명 캡션 스윕 전 화면 적용.**
- **2026-07-04 (11)**: **개체 생애주기 표준(Draft→Archive) 헌장화** — 정책형 파이프라인(초안→상신·검토→승인·게시[민감 passkey]→활성→개정[버전]→보관→폐기)을 대부분 업무 개체 공통 불변식으로. 전이=감사·버전+롤백·PBAC·상태 칩/스텝퍼. 워크플로 외부 API 인제스트(wf5 나라장터·wf6 오픈뱅킹, 파이프라인 액션) 렌더 확인 = todo 완료. **DESIGN §3.9·CLAUDE 원칙·TODO 스윕.**.
- **2026-07-04 (12)**: **생애주기 거버넌스 정교화(연구 반영)** — DESIGN §3.9.1–3 + HANDOFF §15: 미래 발효일(effective-dating)·영향분석 사전점검·maker-checker/SoD(SOX)·전결(DoA)·참조무결성 정산 게이트·변경 동결창·**보관=숨김(하드삭제 금지)**. 워크드 예시=조직 변경(조직도): 초안→사전점검→승인→발효(버전 N+1)→정산(전보/전적·법정 정산)→폐지 사전점검→보관. 벤치마크 Workday·SAP SuccessFactors·Oracle HCM·SOX·ISO 15489. 참조 구현=조직도(#18).
- **2026-07-04 (13)**: **조직 변경 생애주기(draft→archive) 참조 구현 완료·검증**. `orgChange` 상태기계 + 모달(entCard 「조직 개편 결재」 진입): 초안(신설/개편/폐지·발효일)→사전점검(영향 인원·의존 개체·동결창)→결재(HR·재무·법무·임원 maker-checker/SoD)→발효(effective-date·버전 N+1)→[폐지]정산(전보/전적·포지션·코스트센터·공고·자산·급여/4대보험/퇴직 — 참조무결성 게이트)→보관(archived·숨김·이력보존, 하드삭제 아님). 전이 전량 logEvent. 검증: ㈜코스 폐지 전 사이클 통과·무오류. DESIGN §3.9 실증. **잔여(#18)**: 계약·문서·인제스트 등 소급 + 트리 숨김 필터. **다음**: #18 소급 → 자동화 no-code 캔버스 → P2 분석.
- **2026-07-04 (14)**: **내부통제·가드레일 프레임워크 헌장화(연구 반영)** — DESIGN §3.10 + HANDOFF §16: 예방>탐지>시정, fail-closed. 6계층 = 권한·자격 게이트(least privilege)·셀프 체크리스트·피어 검토(four-eyes)·결재(SoD)·**egress 게이트**(승인/게시+권한일 때만 외부 발송/내보내기)·탐지(감사·알림). 참조 사고=미검토 계약 외부 발송(§3.10.1). 벤치마크 COSO·SOX ITGC·three-lines·NIST AC·four-eyes. 전 액션 공통(메일·문서·import/export·결재·워크플로·자동화·크론·정책·인제스트·복리후생·채용). **다음**: 참조 구현(계약 기안 가드레일 + 메일 egress 게이트).

- **2026-07-08**: (1) **가드레일 참조 구현 완료(§3.10)** — 계약 기안 템플릿(`contract`·권한 preflight 감사·셀프 체크 4항 attestation·four-eyes 검토자[기안자 제외]·SoD 결재선, 미완료=fail-closed 차단) + 메일 컴포저 egress 게이트(`egressDocs` 레지스트리·「개체 첨부」생애주기 칩·외부×미승인/민감=차단 패널+anomaly 감사+컴플라이언스 알림·단일 CTA). 메서드: `GUARD_SELF_CK/apprCkToggle/apprPeerPick/mailAttAdd/mailEgressEval`. (2) **자동화 no-code 블록 빌더** — `wfBuilder*`: 트리거/조건/액션 블록 팔레트(`WF_BLOCKS` 전 블록=개체 타입), 자동 규칙명·시뮬·저장=활성+감사. (3) **자동화↔온톨로지 양방향**(Palantir 역학) — wf 상세 「개체 체인」칩→explore 재중심(`wfChainOf/WF_TYPE_NODE`), explore 패널 「작용 자동화」→규칙 선택. (4) **대시보드 v1**(`screen:"dashboard"`) — 스탯바·계약 수익성·인건비 6M 추이·사업장 커버리지·내 지표 — 전 수치=개체 drill, 비클릭 숫자 없음. explore 푸터 캐프션 산문 삭제(§4-12). 신규 directive 등재: 완전 추적성(언급=링크·기안 구조화·중복 점검)·페르소나 워크플로 audit·분석 심화 (TODO 신규 섹션·ROADMAP §8).
- **2026-07-08 (2)**: **완전 추적성 1차 + 데드엔드 스윗**. (a) `msgParts` 업그레이드 — @멘션+**개체 코드(AP/WO/AT/CS/JL/PS/IN/DX/Bid/C/R-)** 자동 링크→`objectLinkGo`(items→패널, 그래프→explore, 폴백→docs 검색). (b) **결재 패널 구조화 블록** — 대상자(인사카드 칩·경비원 4명 PEOPLE 등재)·**중복 청구 점검**(clear=ok 칩 / similar=유사 건 링크 AP-3068·AP-3055)·증빙(열람=감사)·연결(`linkChipGo`) — AP-3114 특수검진비 워크드 예시 구현. (c) 데드엔드 ~20건 재배선(TODO 「데드엔드 제거 스윗」 참조): 팔레트 사람/화면, 새 메일/대화/공지, 인사 액션 6종→기안 프리필, 공고 초안 생성, 팀 명부/채널, egress 내보내기 등. 잔여: 게시판 풀뷰·Cedar 캔버스·파일 업로드·메신저 풀뷰·ERP군.

- **2026-07-08 (3)**: **모듈 서피스 10화면** — `MOD_SCREENS()` 단일 설정 + 제네릭 템플릿 1섹션(헤더 스탯바·검색·주액션 / 목록 공유트랙 / 상세 kv+링크+액션): finance(VC- 전표·wf6/급여/AP- 연동)·purchase(PO-)·inventory(IV-)·asset(FL-/GPU/BUS)·maintenance(WO- 재사용·처리 패널)·field(현장×계약×SLA)·compliance(CP- 의무×근거×개체)·laborcost(계약별 breakdown·예측)·board(NT- 수령확인)·directory(PEOPLE 동적). 모든 링크=`modLinkGo`(코드/노드/사람/스레드/메일/기안/패널/등재/화면). nav ERP·현장·컴플·분석·커뮤 전부 배선, 팔레트 맵 10종 추가, rail 공지 더보기→board, linkChipGo 폴백→docs 검색(「연동 예정」 문구 제거). 화면 목록에 +finance/purchase/inventory/asset/maintenance/field/compliance/laborcost/board/directory.

- **2026-07-08 (4)**: **Cedar 정책 no-code 캔버스 완료** — `policies`(state)·`polBuilder`·`POL_BLOCKS/polRuleText/polSim/polSave`: 누가→무엇을→액션 블록 + 허용/금지 + 조건 토글 + 자연어 규칙 자동생성 + 시뮬(principal 샘플) + 초안/개정(v+1) 저장·감사. 규칙 편집·시뮬 토스트 실계산으로 교체 — 정책 화면 데드함수 0. **컴포저 유사 건 점검**(전 유형) — 제목 토큰↔기존 AP- 대조 경고 칩(클릭=열람). **모듈 서피스 J/K** 키보드 탐색. 모듈 primary 액션 도메인화(실사=인제스트·자산=취득 기안·정비=요청 기안·현장=이슈 접수).

- **2026-07-08 (5)**: **채용 양방향 워크플로 완성 (Greenhouse 벤치마크 · 페르소나 e2e)** — 지원자 카드 모달(`rcCand*`): 지원서/이력서(열람=감사) · 단계 스템퍼 · 면접 평가 스코어카드(적합/보통/부적합·평가자·시각) · **오퍼 제안/조정/회수**(회수=면접 복귀·이력 보존) · 불합격 사유 enum 5종→인재풀(talentPool)·탈락 철회(재검토) · **입사 확정 = 직원 개체 생성**(EMPLOYEES+PEOPLE·포지션 프리셋 상속·근로계약 수신함 발송 감사). 가드레일: 평가 없이 오퍼 불가(fail-closed). 면접 행 「오퍼로 이동」=카드 경유. 채용 푸터 캐프션 2줄 삭제(§4-12). 전 전이 logEvent.

- **2026-07-08 (6)**: **「파일=경계 포맷, 개체=1급」 헌장화(DESIGN §4-13)** — xlsx/pdf/docx는 유입(인제스트→구조화+원본 provenance)·유출(export·egress)만. 지원자 카드 참조 적용: 「프로필」(구조화)=primary, 원본 PDF=보조 provenance 칩으로 강등. TODO 「파일-1급 동선 스윗」 등재(결재 증빙·메일 첨부·기록물·오피스 문서).

- **2026-07-08 (7)**: **메신저 풀뷰 완료** — `screen:"msgr"`·`msgrSel/msgrSend`: rail과 동일 threads 상태 공유(§4.8 rail↔main), 2-pane·버블·전송·드래그 참조 드롭·msgParts 실링크. 검증: 배차 관제 스레드에 「WO-2643 배차 확인 부탁 @김성호」 전송 → 코드·멘션 링크 렌더. nav 메신저→풀뷰.

- **2026-07-08 (8)**: **메신저 폴리시(Slack/Teams)** — 그루핑(headOn)·미독 구분선(msgrDiv)·대화/내용 검색(msgrQ)·@멘션=알림+감사(msgrSend)·메시지→할 일(msgrTodo·addTodoToDay). 검증: 김성아 스레드 그루핑+AP-3121 링크+hover 할일 버튼.

- **2026-07-08 (8b)**: **메신저 Slack/Teams 패리티 2차** — 사이드바 채널(#)/다이렉트 섹션+프레젠스(PRESENCE)·확인 ack(msgrAck 토글·카운트 칩)·답장 인용(msgrQuote→버블 인용 블록)·헤더 개체 코드 칩(자동 추출→objectLinkGo)·컴포저 자동완성(msgrKnownCodes: items+egressDocs+그래프 · @PEOPLE · 방향키/Enter/Tab/Esc). ⚠ 교훈: 인터럽된 턴의 잔재 코드(회의 MT-·전술지도 map·알림 풀뷰 notif도 그 턴 산출물) — 항상 현재 소스 grep 후 편집할 것.

- **2026-07-08 (9)**: **크래시 픽스 + 스크롤 랜딩 — 검증 사이클 완료**. (a) 알림 풀뷰 `notifRows.segs`의 `tokenRender(…).map` 예외(tokenRender=React element 반환)가 매 렌더 renderVals 전체를 죽이던 것을 배열 반환 `msgParts`로 교체 — 앱 전체 복구(검증기가 발견). (b) **메신저 스크롤 랜딩**: `_msgrScrollSync`(componentDidUpdate 선두·`_msgrLast` 메모·scrollTop 산술, scrollIntoView 금지 준수) — 스레드 진입=디바이더(`[data-msgr-div]`)·없으면 맨 아래, 전송=맨 아래. 템플릿 메시지 패인 `ref={{ msgrPaneRef }}`. 검증기 확인: 섹션·프레젠스·디바이더·자동완성·ack·인용·개체 칩 전부 실동작. (c) **디자인 시스템 바인딩** 명시(§1) — 새 비주얼은 `/projects/019dc2a0-992f-7697-b778-ef3dee4670bd/` 토큰·컴포넌트에서 복사.

- **2026-07-08 (10)**: **#18 생애주기 소급 완료 (계약·기록물·인제스트 — §3.9)** — 조직 변경 문법을 일반화한 **제네릭 생애주기 엔진**: `lcSeed(code)`(egressDocs·DOCS·docRegistered·ingestJobs에서 파생 — 저장은 mutation만 `lcReg`)·`lcOf/lcSet/lcSyncRegistries`(egressDocs 상태 양방향 동기 → 메일 egress 게이트 연동)·전이 메서드(`lcSubmit/lcApprove/lcEffectuate/lcRevise/lcRollback/lcArchive/lcDep/lcGate/lcDispose`, 전량 logEvent). **단일 모달**(orgChange 문법 재사용): 스텝퍼 5단계·발효일(초안만 편집)·버전 이력+롤백(비파괴 v+1)·결재선 SoD·보관 게이트(참조무결성 3항)·폐기 게이트(보존기한·legal hold·기록관리자)·연결 칩(원본 provenance §4-13·인제스트 DX-·그래프). **개정=sandbox**: v+1 초안 생성, 발효 전까지 이전 버전 「vN 활성 유지」 칩 + egress 게시 상태 유지. 진입 3곳: 문서·기록물 행(toast 데드엔드 제거, 처분 대기/폐기됨 칩·폐기 행 dim)·인제스트(적재=검토 진입, committed 클릭=생애주기 카드)·explore 중심 개체 「생애주기 · 단계·vN」 칩. 데모: C-207 v3 개정→상신→발효 v4 / IN-0301 불합격자 지원서류(신규 시드) 보존기한 만료→폐기. nav 「전술 지도」→「운영 지도」 개명.

- **2026-07-08 (11)**: **모듈 = 개체 서피스 + 시리즈 + 규제 원장 (DESIGN §4-14·15·16 신설)**. (a) **§4-13 파일-1급 스윕 완료** — 결재 증빙=구조화 라인(`APPR_EVID` 금액·항목·거래처, 패널·모달 「증빙」 rows + 「원본」 보조 칩·열람 감사), 메일 첨부=구조화·인제스트 primary CTA(`ingestUpload` 실 DX- 생성)+증거 등재=기록물 등재 프리필, 기안 증빙 첨부 실동작(`apprEvidAdd/Remove` 구조화 라인·경비 fail-closed 필수). (b) **온톨로지 그래프 머지**(`_ogBuild`) — 모듈 행 전부(VC/PO/IV/FL/WO/ST/CP/RG/NT/FC) typed 노드 합류, kv·링크 코드=자동 엣지, 시리즈·인사이트 노드 포함, `objLinks` 오버레이. (c) **개체 카드 3계층 완성** — 속성(모듈 kv)·시리즈 미니 타임라인(바·현재/예정 점선·추세)·작용 자동화/예약/정책 칩(`lcDynOf`)·연결 칩+**관계 그리기**(코드 입력 Enter·행 드래그 드롭→`objLinkAdd` 엣지+감사, × 해제). (d) **시리즈 SR- 6종**(`OBJ_SERIES/seriesOf`) — 임대료·서버비·정비 이력·검진·급여 회차·대근비; 모듈 상세·AP 패널 「시리즈」 칩. (e) **규제 개체 RG-101/102**(최저임금 2027 영향·시뮬·조정 기안 프리필·주52h 감시). (f) **대시보드 인사이트 5건**(AN- 파생 개체 — 근거 체인·처방 액션·그래프 합류). (g) 모듈 상세 「개체 카드」 칩·exTypeC 신규 타입 10종.

- **2026-07-08 (12)**: **온톨로지 스키마 = 개체 (§4-17 신설)** — 타입 레지스트리 `ontTypes` 13종(OT-01~13): 활성 11 + 초안 「거래처」(제안) + 보관 「배차」(WO- 통합·마이그레이션 완료 예시). **그래프 범례 = 타입 카드 진입점**(칩 클릭 → lcOpen(OT-): 정의·관리 주체·도입·활성 개체 수(그래프 실계산) + 생애주기 스텝퍼 + 데이터 거버넌스·임원 SoD + **보관 게이트** = 인스턴스 마이그레이션·자동화/정책 재바인딩), 초안/보관 배지, 「+ 타입 제안」 인라인 입력(Enter → 초안+감사+카드). 전이 → `lcSyncRegistries`가 ontTypes 동기(범례 실시간). 검증기 4건 수정: 시리즈별 `sch` 키워드(역학 오바인딩 제거)·닫기 CTA 중복 억제(ctaOn)·증빙 행 flex-wrap+min-width(§4-4)·RG 발효일 파생(`mod.row.c3` 날짜 패턴)+빈 deps 보관 라벨.

- **2026-07-08 (13)**: **그래프 저작 (§4-17 저작 절 추가)** — ① 새 개체: 탐색 사이드패널 「+ 새 개체」(활성 타입 select + 이름 Enter) → `nodeCreate` OB- 초안 생성(감사)+카드 오픈, `userNodes` 오버레이로 그래프 합류(비파괴·memo 유지). ② 새 부모: 개체 카드 관계 행 「시리즈 승격」(AP/WO/VC/PO/IV/PS/CS/IN/JL/NT/CP/OB 인스턴스형·시리즈 미소속일 때) → `seriesCreate` SR-21x 사용자 시리즈(첫 인스턴스 편입·`_ogBase` 재빌드) → 시리즈 카드에서 코드 입력·드롭 = `seriesAttach` 인스턴스 편입(cur 이동·추세 재계산·감사). ③ 새 관계: 탐색 중심 개체(사이드패널) 드롭 존 = `objLinkAdd`, 그래프 노드 칩 = 드래그 소스(`[코드]` 참조 토큰 payload 공유 — 컴포저·할일에도 드롭 가능). onLink 핸들러가 시리즈 카드에서는 편입, 그 외 카드에서는 엣지로 라우팅.

- **2026-07-08 (14)**: **배차·내 업무 화면 + 자동화 CRUD + 노드 에디터 폴리시** — `screen:"dispatch"`(WO- 큐×후보 기사×SLA·처리 패널 연동, nav 배차 스텁 해소)·`screen:"mywork"`(라이브 state 집계: 내 차례 결재/배차·진행 상신·수령확인, nav 「개인」 그룹) · 예약: `schNew/schArchive`+개정 v+1 감사(`schEditSave`)·보관 필터 · 워크플로: `wfEditOpen`(캔버스 로드)→저장=개정 v+1·`wfArchive` · 탐색 **개체 검색**(전 그래프 이름·코드·타입, §4-12 캡션 자리) · UX: Esc 카드 닫기·드롭 하이라이트(`lcDropHot/exDropHot`)·시리즈 placeholder 문맥화·v1 단일 이력 숨김 · §4-14 모듈 3계층 audit 조항. 잔여: 보관 wf 목록 dim·기사(드라이버) 개체·mywork 할 일 행.

- **2026-07-08 (15)**: **운영 지도 신설·배차 연동·경영 분석 확장** — map 화면은 vals만 있고 템플릿이 없던 잔재(인터럽션 산출물)였음 → 템플릿 신설: 스탯 바·오버레이 4종·현장 마커(펄스·선택 링)·유닛 레이어(FL-/기사/BUS)·오버레이별 큐 패널(배차=처리 CTA). 배차 행 「지도에서 보기」(mapOv/mapSel) 왕복. 알림 풀뷰 행=notifClick(코드 폴백 objectLinkGo). 내 업무: 미확인 알림 행+스탯 drill. 예약 편집=이름+주기 v+1. **경영 분석**(nav·타이틀 개명): 계약 인건비+사업장 수익성(ST-)+가동률(UT-) — AN-/FC- 근거 체인. modLinkGo `notif` 브랜치 추가.

- **2026-07-08 (16)**: **액션 생애주기 헌장(§3.9 확장 + §3.9.0 저장·적용 audit)** — 파이프라인에 셀프/피어 체크리스트·실효 대기·예고 단계 명문화. 직행 save/apply 금지(화이트리스트: 개인 설정·이벤트성·초안 내 편집·권한 토글). **개정 스테이징(pendingRev)** 참조 구현 = 워크플로·예약: 활성 개체 편집 저장 → 「개정 대기 v+1 · 현행 유지」 warn 배너 → 「적용 승인」(four-eyes 감사)=발효 / 「철회」=파기. 신규 규칙 = 초안(비활성)→활성화=게시. 예약 초안 편집=직행 허용(③). TODO에 직행 저장 전수 audit 스윕 등재.

- **2026-07-08 (17)**: **검증 픽스 + 문서 동기** — nav 「내 업무」 중복 제거(개요 그룹 항목을 mywork로 재배선·「개인」 그룹 삭제), 개체 카드 오픈=프로브 정상(오탐), 운영 지도 프로브(마커 7·큐 패널·wo 오버레이 유닛·큐 3행 정상). 참고: html-to-image 캡처가 「svg load failed」로 간헐 실패 — 앱 정상, 프로브는 console.log로 대체. `{{ to.l }}` 경고 = 스트리밍 일시 placeholder(최종 DOM 정상).

- **2026-07-08 (18)**: **직행 저장 전수 audit 완료(§3.9.0 스윕)** — 판정 부록 DESIGN §3.9.0 등재(결재 행위=적합·분석 구조물=화이트리스트·토글=게시 행위·조직 인라인=제안 sandbox). `orgTouched` 제안 카운터+감사 → 조직도 「제안 변경 N건 · 개편 결재」 warn 배너(상신 시 리셋)·§4-12 캡션 제거·schToggle 게시 감사.

- **2026-07-08 (19)**: **엔터프라이즈 표준 인코퍼레이션** — HANDOFF §17 신설(SaaS 신뢰·데이터 취급·보안/IP·위협 완화·관측성 백엔드 계약: SOC2·ISO 27001/17/18·SSO/SCIM·테넌시 격리·KMS·STRIDE·IR 800-61·OTel·SLO·SIEM). 컴플라이언스 모듈에 **FW-01~04 표준 개체** 추가(통제→콘솔 실기능=증거 매핑·감사/정책/inbox 링크·이상 필터 drill) + 표준 프레임워크 스탯. 원칙: 문서가 아니라 작동 기능이 증거(Vanta/Drata/Audit Manager 벤치마크). — 판정 부록을 DESIGN §3.9.0에 등재(결재 행위=적합·분석 구조물=화이트리스트·토글=게시 행위·조직 인라인=제안 sandbox). 구현: `orgTouched`(제안 카운터+감사) → 조직도 헤더 「제안 변경 N건 · 개편 결재」 warn 배너(상신 시 리셋), 조직도 §4-12 캡션 제거, schToggle 게시 감사. polSave·인제스트·메일·연차/결재 승인 = 적합 확인.

- **2026-07-08 (20)**: **지도 저작 + 우클릭 빠른 액션** — 「편집」 토글(마커 드래그 배치 `mapPos`·「+ 현장」 제안 추가 draft dashed·캔버스 우클릭 추가), 우클릭 컨텍스트 메뉴(마커: 요약/이동/근태/배차/이슈 기안/[draft]개편 결재 · 캔버스: 현장 추가 — Esc·바깥 클릭 닫힘), 마커 클릭=요약 선택 + 큐 패널 **현장 요약 카드**, 좌표 declutter. AGENTS (18) 항목 복원(편집 사고로 유실됐던 것).

- **2026-07-08 (21)**: **검증 픽스** — 빌더 저장 라벨 문맥화(`wfbSaveLabel`: 신규=「초안 저장」·편집=「개정 저장 — 승인 대기」, §3.9.0 정합) · 블록 type/short 보존(wfBuilderSave 신규+pendingRev 저장에 type·short 포함, `wfEditOpen` WF_BLOCKS label 재매칭 rehydrate — 온톨로지 칩 공백·{{ bt.type }} 경고 해소).

- **2026-07-08 (22)**: **Next Wave 1 — 역할 전환(view-as)** — `VIEWERS` 5종·`viewerNow/viewerSwitch`(전환=감사·auth), 사이드바 상단 역할 카드+메뉴(clearance 칩·Esc 닫힘), navGroups 페르소나 **deny-by-omission 필터**(NAV_SCR 라벨→화면 매핑), `logEvent` actor=viewer(전 행위 페르소나 귀속). ⚠ TODO.md 편집 시 헤더-라인 replace로 항목 글루 사고 3회 — 반드시 전체 라인 단위로 치환할 것.

- **2026-07-08 (23)**: **페르소나 게이트 확장(1b)** — 팔레트 화면 결과 viewer 필터·내 업무 viewer 재계산(반장=본인 상신+배차, 「내 상신」 분기).

- **2026-07-08 (24)**: **거래처 CL- 지속 개체 + OT-12 활성** — CLIENTS 4종 그래프 합류(거래 체인+현장 엣지)·개체 카드 속성·field 모듈 「거래처」 링크 4건(run_script 일괄)·OT-12 draft→active.

- **2026-07-08 (25)**: **내 업무 데이터 스코프 누수 수정(검증기)** — 비관리자 페르소나에서 전성진 소유 상신함(apprMySubs)·수신함(inboxDocs)·알림 행이 노출되던 것을 v1 소유자 한정으로 게이트(deny-by-omission 정합). 스탯도 자동 0 반영.

- **2026-07-08 (26)**: **표시 형식 심화 1차** — 재고=수량 바 매트릭스(현재고/안전재고 틱/월 소요·부족 danger), 자산=수명주기 타임라인(이벤트 dot·예정 점선·코드 행 drill). 제네릭 상세 `stock`/`tl` 확장 필드.

- **2026-07-08 (27)**: **모바일 앱 스코프** — <768 = 직원 앱 7종(메신저·메일·알림·주소록·게시판·수신함·결재): `MOBILE_SCR/mobileGuard`(mount+resize 리다이렉트), 하단 탭 바(5탭·배지·48px·safe-area·더보기=드로어 토글), nav 드로어 모바일 7종 필터(페르소나 필터 합성), 본문 padding-bottom 64px.

- **2026-07-08 (28)**: **설명 캡션 스윕 완료 판정 + 벤치마크 갭 레지스터** — h1 서브라인=운영 데이터 적합 판정, 실위반 2건 제거(인사 카드 stale 「연동 예정」 → Cedar 정책 칩·포지션 suffix 삭제). TODO에 벤치마크 갭 레지스터 신설(Palantir as-of·Slack 스레드 무음·n8n 실행 타임라인·Asana 완료 토글·Gmail 스레딩·Vanta 증거 뷰) — 이후 심화 슬라이스 선정 기준.

- **2026-07-08 (29)**: **모바일/데스크톱 분리** — `Oyatie Mobile.dc.html` 신설(iOS 프레임+콘솔 iframe 390px=자체 window 메트릭으로 모바일 자연 진입), 더보기=모바일 시트(수신함·주소록·게시판, 백드롭·Esc)로 데스크톱 드로어 의존 제거. AGENTS §1 산출물 2종으로 갱신.

- **2026-07-08 (30)**: **게시판 수령확인 진행 바(표시 형식)** — board 행 `prog` 필드(수령확인 done/total) → 모듈 상세 제네릭 진행 바(100%=ok·미만=warn), kv 중복 산문 제거. 제네릭 `prog` 필드 = 어느 모듈이든 사용 가능.

- **2026-07-08 (31)**: **문서 전면 동기** — 모바일 분리 산출물(Oyatie Mobile.dc.html·ios-frame.jsx)·게시판 진행 바·캡션 스윕 완료·벤치마크 갭 레지스터를 ROADMAP 진행 로그/모듈 매트릭스에 반영. TODO 완료 항목 체크 정리. 파일 구성: 콘솔·모바일·ios-frame·support.js + 마크다운 5종.

- **2026-07-09 (32)**: **자율 백로그 드라이브** — ① 내 업무 알림 행=실개체 내용(코드 해석→건명·요청자·마감·증빙 kv·처리 패널 직행; MOD_SCREENS 내 lcKnown 재귀 크래시 수정 — 빌드 중 modObjOf 호출 금지 교훈) ② n8n 실행 로그 타임라인(runLog·생성 개체 칩·오류 재시도) ③ 정비 SLA 큐(모듈 제네릭 lanes 칸반) ④ 모바일 2-pane 스택(메신저 목록↔채팅·메일 폴더 칩+목록↔읽기·뒤로 버튼) ⑤ 대시보드 스코프×기간(PBAC 세그먼트·as-of·KNL C-311·본사 빈 상태).

- **2026-07-09 (33)**: **자율 백로그 드라이브 2** — ⑥ Slack 스레드 무음(헤더 벨 토글·목록 벨-오프 아이콘·배지/탭 합계 억제 — 개인 설정 화이트리스트) ⑦ Gmail 대화 스레딩(제목 정규화 그룹·목록 대화 수 칩·읽기 pane 접힌 이전 메시지 행) ⑧ Vanta 통제→증거 매트릭스(FW- `ctl` 제네릭 필드·증거=실기능 drill) ⑨ 급여 「회차 시리즈」 칩=SR-205 카드. 벤치마크 갭 레지스터 소진: as-of 재구성·타입 속성 스키마 편집·Asana 완료 토글만 잔여(백엔드성·후순위).

- **2026-07-09 (34)**: **증거 아카이빙 + 오피스 편집기 셸 (P3–P4 에픽 UI 계약)** — ① EV-101~103 증거 개체(기록물 「증거」 필터·개체 카드: SHA-256+nullable TSA 상태 메타데이터·WORM 원본/파생본 분리·연계보관 이력·적격 칩·legal hold 게이트; non-null RFC-3161 토큰 실행 증거는 없음) ② 오피스 셸(C-209 DOCX): 편집 모달 — 문서 캔버스(contentEditable·dirty 감사)·버전 rail(불변 신버전·비파괴 복원)·PBAC 매트릭스·DLP 칩·협업자, 진입=개체 카드 「문서 편집」. Esc 체인 최상위 등록.

- **2026-07-09 (35)**: **표준화 스윕(§4-18 신설) + 운영 지도 지형** — DESIGN §4-18 컴포넌트 재사용 헌장(모듈 cfg 문법·lcOpen 단일 카드·TONE 팔레트·공용 칩/세그먼트/드래그 문법, "같은 모양 두 번 그리면 위반"). `TONE(t)` 단일 톤 헬퍼로 modV toneC·map toneC2·마커 C 삼항 중복 3곳 통합. 운영 지도에 **한반도 스키매틱 지형**(간이 해안선 path+제주+DMZ 점선+서해안 강조 — 외부 타일 없이 토큰 색) 삽입, 마커=실좌표감. TODO 증거 항목 복구 등재.

- **2026-07-09 (36)**: **잔여 갭 소진 + 지원자 페르소나** — ① **as-of 재구성**(생애주기 카드 버전 이력 「시점 보기」→`lcAsOf` 읽기 전용 뷰: info 배너·전이/게이트/CTA 전부 비활성·「현재 버전으로」·조회 감사) ② **타입 속성 스키마 편집**(OT- 카드 「속성 스키마·no-code」 — `ONT_SCHEMA_DEF/ontSchemaOf/ontAttrAdd/ontSchemaApprove/ontSchemaDiscard`: 활성=개정 스테이징(warn 배너·적용 승인=스키마 v+1·철회)·초안=직행 ③) ③ **mywork 할 일 행+완료 토글**(modLinkGo `todo`·`onRun` 제네릭 분기) ④ **인력풀 서피스**(`screen:"workforce"` — WP- 행·대근 편성=subOpen·미편성 결원 스탯 라이브·nav 인사 그룹·MOD_TYPEOF 그래프 합류) ⑤ **메일 본문 msgParts 실링크**(rail·풀뷰) ⑥ **시리즈 자동 탐지**(컴포저 유사 건 ≥2 → 「시리즈 승격」 칩) ⑦ **지원자 view-as v6 서준호**(directive) — scr=[candidate,inbox] deny-by-omission · 「내 지원」 모듈 서피스(rcData 본인 건만·내부 평가 비노출) · 오퍼=수신함 legal 문서(OFR-2607·passkey 열람=수령 증빙) · **수신함 owner 스코프 신설**(전 페르소나 본인 문서만 — 기존 타인 문서 누수 해소) · inboxConfirm 스탬프 by=viewer · 외부 페르소나 rail 미렌더(railExt) · viewerSwitch 랜딩=첫 인가 화면.

- **2026-07-09 (37)**: **전 모듈 라이브 audit (페르소나 e2e DOM 프로브) + 폴리시 픽스** — 검증: v6 지원자(nav 2종만·타 데이터 비가시·passkey 수령 e2e 서준호 스탬프)·인력풀(행·kv·대근 모달)·내 업무 할 일 토글·as-of(배너·CTA/결재선 비활성·복귀)·OT 스키마(개정 대기→v3 발효). 픽스 6건: ① 탑바 사용자 칩 = viewer 반영(하드코딩 전성진 → topUser* holes) ② 외부 페르소나 스코프 셀렉터 미렌더(scopeSelOn) ③ 시리즈 제안 임계 완화(단일 매칭이 시리즈 소속이면 「시리즈 · …」 칩 — 시드로 도달 가능) ④ rail 할 일 링크 폴백 toast→objectLinkGo(데드엔드 제거) ⑤ **Esc 체인 보강**(subModal·pkModal·rcCand·empDay·docReg — 대근 모달 Esc 미닫힘 버그) ⑥ **OT-14 지원자 타입 활성**(범례 칩·스키마 시드). 프로브 교훈: 스크린샷 파이프라인 svg 실패 → innerText/offsetParent 기반 DOM 프로브로 대체 · 핫리로드는 state 시드 미반영(범례 확인은 새로고침 후).

- **2026-07-09 (38)**: **UI 픽스 + 3개 directive 인코퍼레이션** — ① 인라인 코멘트 픽스: 목록 가로 스크롤 커버리지(모듈 리스트 헤더를 스크롤 컨테이너 내부 sticky로 이동+min-width:590 래퍼, hr 720/docs 670 래퍼 보정), 스탯 값 nowrap(줄바꿈 클리핑), 명부 이름 셀 간격, 탑바 사용자=viewer ② **§4-19 필드 타입 규율** 헌장화 + 공고 req[]/emp enum 참조 구현 ③ **§4-20 모든 개체 표면=드래그 소스** — objDrag 단일 헬퍼(lc 헤더 코드·연결/관계 칩·nav) ④ **모듈=개체 MD-01~37** — nav 드래그→컴포저 참조, msgParts MD- 링크→모듈 이동(PBAC 게이트) ⑤ **지원자 셀프서비스 완결** — 모집 공고 열람+지원하기(candApply=rcData 실합류·감사), 채용 담당 스코프드 DM(t9·thVisible viewer 게이트·msgr v6 인가·viewerSwitch activeThreadId 정합), 모바일 가드 viewer-aware, msgrSend/startThread 발신자=viewer ⑥ **데모/개발 스캐폴드 인벤토리 문서화(HANDOFF §0)** — view-as·pkAuth 시뮬·시드·deviceCtx 고정·클라이언트 해시체인·simulateReplies = 프로덕션 반입 금지 목록.

- **2026-07-09 (38)**: **공고/내 지원 분리 + Cedar 단일 chokepoint + 재사용·레이아웃 프리셋 (directive)** — ① `postings` 서피스 분리(JP- 코드·지원하기·지원함 상태, candidate는 본인 지원·오퍼·서류만) ② **Cedar `can(action,resource)` + `cedarScreenGuard`**(componentDidUpdate — 전 진입경로 fail-closed 재라우팅+deny 감사) ③ **@ 자동완성 단일 출처** `mentionPeople`/`mentionScopes`(quickAdd·rail 컴포저·msgr 풀뷰 AC 전부 재배선 — 지원자=김성아만, 조직범위 미노출, tokenCatalog·팔레트 동일 정책) ④ **인물 가시성 정책** `personVisible`(v6=채용 접점만·차단=deny 감사+토스트) ⑤ **내 업무 주간 캘린더 탭**(목록/주간 — schedByDay 7일 그리드·할일 클릭=완료 토글·개체 링크=objectLinkGo·오늘 칩) ⑥ **레이아웃 프리셋**: 모듈 테이블 `modNarrow`(실가용폭 계산) 풀/컴팩트 자동 + **고정 테이블 7종 유동화**(명부·채용·근태·급여·연차·복리후생·기록물 — 래퍼 min-width 제거·트랙 minmax 강하 = 수평 스크롤 소거). DESIGN §4-19 신설(재사용 chokepoint·레이아웃 프리셋 헌장).

- **2026-07-09 (39)**: **공고=전사 내부 공모 (directive) + 오버플로 소거 마감** — ① `postings`를 v6 전용에서 해제: nav는 `can("screen:view","postings")` 게이트(개요 그룹), v2·v3·v4 scr에 postings+candidate 추가, 내부 타이틀 「공고 · 내부 공모」(Workday internal career 벤치마크) · 지원하기=candApply e2e(파이프라인 합류·감사) ② 잔여 수평 오버플로 소거: 명부 grid 트랙 강하(96px~)·근태 타임라인 min-width 제거(% 축 압축)·주52시간 위젯 행 유동화(76/60/56px·gap 8) — 개요·인사·근태·재무 오버플로 0.

- **2026-07-09 (40)**: **내 업무 전체 달력 통합 (directive) + 마감 픽스** — ① date-keyed 달력 엔진(`dParse/dShift/weekStart/schedItemsOf/calCellItems` — 시드 주간 WEEK_DATES=schedByDay + 임의 날짜 `schedExtra` 병합, `addTodoToDate/toggleTodoAt`) ② **목록/주간/월간 3탭 + ◀▶·오늘 내비**(주간=주 단위 이동, 월간=6주 그리드·인접월 dim·+N=주간 drill) ③ **파생 이벤트 = 온톨로지 뷰**(매월 10일 급여 지급일→pay drill·대기 건 마감 M/D 파싱→개체 링크) ④ 할 일 추가 = 선택 날짜 타깃(placeholder에 날짜 표기·@이름 토큰) ⑤ 채용공고 인사 그룹 이동·중복 nav 제거·megaphone 아이콘(missing icon 픽스). 주의: 오늘 일정 위젯의 「월간」 버튼(→근태)과 mywork 탭 「월간」은 별개 — 프로브 시 parent로 구분.

- **2026-07-09 (41)**: **폴리시 스윕 — §4-19 enum 소급 + 모바일 마감 + 지원자 뷰 완결**. ① 모듈 행 제네릭 `en` 필드(44행 소급: 재무 계정·방향 / 구매 구분·품목군 / 재고 위치 / 자산 보유·분류 / 정비·배차 유형 / 현장 서비스 / 컴플 구분 / 게시판 유형 / 경영분석 단위 / 인력풀 유형·보안등급) — 상세 enum 칩 클릭=`modQ` 필터(목록 검색·J/K 필터에 en 값 포함). 템플릿: kv ↓ enum 칩 행. ② **모바일 탭 바 viewer 게이트** — v6=내 지원·공고·수신함(inbUnconfN 배지)·메시지 4탭, 그 외 페르소나 scr 필터 + mMoreItems 동일 게이트. ③ **스와이프 액션** `swipe(action)` 단일 헬퍼(touchstart/move/end — currentTarget transform 직접 조작·재렌더 없음·임계 -64px·수직 34px 가드): 메신저 목록(채널/DM)=무음 토글(`msgrMuteTo`), 메일 목록=보관 토글(`mailSwipeArchive`·감사). ④ **컴포저 키보드 세이프** — visualViewport resize → `kbInset` state: 본문 padding-bottom 가산 + 탭 바 숨김(mTabsOn). ⑤ 데드엔드·캡션 grep 재점검 = 잔재 0 (fail-closed 토스트만 존재—적합). TODO 스테일 데드엔드 섹션 정리.

- **2026-07-09 (42)**: **페르소나 워크플로 audit + 모바일 베스트 프랙티스** — ① ROADMAP §8 전 행 audit 판정 기록(✓) — 갭 2건 해소: **v7 정비 기사 김성호**(scr 13종 — mywork 랜딩·att·appr·docs·maintenance·map 등) · 내 업무 = 배정 WO- 행(`dpRows` 호이스팅 — 배차 화면과 단일 정의 §4-18, c3=viewer 필터·「일지 등재」=docs+docRegOpen) · 본인 명세 PS-2618 시드(owner 김성호) · **mywork myInbox owner 스코프 일반화**(v1 특수분기 제거 — 전 페르소나 본인 문서). ② 모바일 베스트 프랙티스: **엣지 스와이프 백** `swipeBack`(좌측 32px 엣지→우 64px+ = 뒤로 — 메신저 채팅·메일 읽기 pane) · **시트 드래그 닫기** `sheetDrag`(더보기 시트 — 그랩 핸들·임계 72px) · **iOS 줌 방지**(<768 input/textarea/select 16px !important) · tap-highlight 투명·`touch-action:manipulation`(더블탭 줌 제거)·`button:active` 피드백. 모바일 탭 바·시트는 (41) viewer 게이트 유지.

- **2026-07-09 (43)**: **인력풀 ↔ 채용·인사·Cedar 체인 (directive)** — ① 인력풀 공고 r6(pool 플래그) + rcAdvance pool 분기(확정=인력풀 등재 · EMPLOYEES 미생성 · `wpJoined` state · 감사) · 카드/행 CTA 라벨 pool 분기 · `wpAll()` 단일 조회(워크포스 화면·대근 모달·subAssign·polSimWho) · 워크포스 행 「출처」=공고 provenance kv + en 「고용 상태 · 비상근」 + 스탯 채용 연동 신규/인력풀 공고 ② 명부 empSt enum + `hrMatch` 단일 필터(비활성=집계 비합산·dim·「비활성·기간제」 필터) · 휴직(한서윤)·기간제(구본철) 시드 · 근태 로스터 빌더 empSt 가드 · 명부 헤더 인력풀 비합산 칩 · neutral 톤 칩 매핑 ③ Cedar p9·p10·p11 + POL_BLOCKS 고용 상태 principal ④ 데드엔드 스탯 5건 실배선(미확인 업무일지→docs JL 검색·rail 공지→board·재직 KPI→경영분석·인사카드 업로드→인제스트·복리 조건 편집→정책 캔버스). ⚠ 교훈 재확인: 로그 편집은 반드시 전체 라인 단위 치환(부분 문자열 치환 = 항목 글루 사고).

- **2026-07-09 (44)**: **어고노믹·통합 슬라이스 (Palantir/Teams/Slack 벤치마크)** — ① **모듈 테이블 열 정렬**(재사용 §4-18): 헤더 클릭=오름/내림/해제(`modSort` per-screen · 숫자·₩·% 인식 정렬 · ↑↓ 인디케이터) — 전 모듈 서피스 공통 ② **메신저 개체 카드 unfurl**(Teams 링크 프리뷰 문법): 메시지 첫 개체 코드 → 버블 아래 미니 카드(코드·제목·유형/상태 · 클릭=objectLinkGo) — `objPreviewOf` 단일 리졸버(items→egressDocs→modObjOf) ③ **대근 배정 = 건별 근로계약 자동 발행**(subAssign → 대상자 수신함 legal 문서 C-D·passkey 수령=계약 확인 — 자동·통합) ④ **출근 체크인/아웃**(`attCheckIn` — v3·v7 내 업무 행: 미체크→근무 중→완료 상태전이·감사) ⑤ **인재풀→인력풀 전환 제안**(`rcPoolPropose` — 탈락 패널 버튼·본인 동의 대기 avail=false·provenance) · 탈락 요약 캡션 삭제(§4-12 — 행 칩이 이미 기능) ⑥ TODO에 **잔여 백로그 레지스터(durable)** 섹션 신설 + 벤치마크 갭 레지스터 갱신(신규 갭: reply-in-thread·프레젦스 설정·예약 발송·저장 필터 프리셋·위치 인증 체크인).

- **2026-07-09 (45)**: **채용공고·내부 공모 스코프 정리 (directive 3회 수렴)** — 최종: 채용공고·내부 공모 모두 인사 그룹 postings 화면 유지(게시판 이관은 지시로 철회·복구 완료). **공개 범위 = typed enum(§4-19)**: rcData `scope`(internal/external) — r4 전산·r5 파견 코디네이터=내부 공모. **외부 인원 deny-by-omission**: cdOpen 필터 v6×internal 제외(존재 자체 비노출 — postings·candidate·자동완성 전파) + Cedar p12(외부·지원자 → 내부 공모 금지) 시행 + POL_BLOCKS 「외부 · 지원자」 principal + 행 en 「공개 범위」 칩(클릭=필터).

- **2026-07-09 (46)**: **공고 등록 컴포저 (directive)** — onRcNew=모달(`rcPost` state): 포지션명·현장 필수(fail-closed 에러 배너)·법인/고용 형태/공개 범위 세그먼트(일용직=pool 자동·「확정=인력풀 등재」 칩, 내부 공모=「외부 비노출 p12」 칩)·충원 스테퍼·마감·자격 요건 칩 입력(Enter/×) · 푸터=「초안 저장」(§3.9.0-③ 모집 비노출)+「게시 — 모집/내부 공모 시작」(§3.9.0-④ 게시 권한 행위·감사) · 초안 행=「초안 · 게시」 warn 칩 버튼(`rcPublishDraft`)·cdOpen `!rp.draft` 제외 · 내부 공모 행 purple 칩 · recruit 헤더 안내 캡션 삭제(§4-12) · Esc 체인 최상위 rcPost.

- **2026-07-09 (47)**: **백로그 드라이브 (레지스터 소진)** — ① 현장 모듈 4행 「지도에서 보기」(mapOv/mapSel 프리셋: dw=이슈·pt=계약·ansan=정비·sc=커버리지 — §4-14-④ 현장=지도 연동) ② 알림 풀뷰 행 스와이프=읽음 토글(`notifReadToggle` · swipe 공통 문법) ③ 인력풀 호출 수락 훅 — inboxConfirm에서 C-D* 건별 계약 수령 감지 → 「대근 투입 확정」 감사+토스트(계약 수령=호출 수락 · 급여 연동). TODO 레지스터 3항 갱신([~]).

- **2026-07-09 (48)**: **작업 트레이 = Dock 문법 + 지원 센터 + macOS 창 컨트롤 (directives)**. ① 트레이 Dock화: 칩 hover=**peek 미리보기 카드**(유형 칩·코드·제목·요청/마감·작성 중 초안 스니펫 — pointer-events 없는 순수 프리뷰, `trayPeek` state) · 칩 위로 드래그=꺼내기(dragEnd y 임계 140px → 복원) · hover 리프트 마이크로인터렉션 · 빈 트레이 캡션·「모두 닫기」 디클러터(항목 있을 때만). ② **macOS 창 컨트롤**: 자유 오버레이(비핀 플로트)는 hover 없이 최상위 z 플로트에 컨트롤 상시 노출(cardToolVals 폴백 — 핀/그리드 카드는 hover 유지=디클러터). ③ **지원 센터**(`screen:"support"` 모듈 서피스): SUP- 티켓 4종 시드(기술/계정/기능 요청/버그 — 유형·심각도 en enum·SLA·관련 화면 링크·지원팀 메시지) + FAQ 4종(해결 단계 kv·기능 직행 링크·FAQ→티켓 에스커레이션) · 「티켓 접수」 모달(유형·제목·심각도=SLA 자동 배정·fail-closed·감사) · nav 거버넌스 그룹+SLA 배지 · 전 페르소나 scr(v6 외부 포함)·MOBILE_SCR·MOD_TYPEOF 그래프 합류 · Esc 체인.

- **2026-07-09 (49)**: **백로그 드라이브 2** — ① **egress override 플로(§3.10 잔여)**: 차단 패널에 사유 입력+「예외 승인 요청」 — 사유 필수(fail-closed)·보안팀→임원 DoA 결재선 AP- 상신(`_autoAP`)·차단 유지·요청 자체 감사. ② **개체 카드 「활동 이력 — 감사 체인」**(§4-14 잔여): lcV에 auditEvents target 필터 최근 4건(액션·actor·시각·거부 칩·클릭=감사 화면) — as-of 배너 아래 섹션. TODO 레지스터 2항 [~] 갱신.

- **2026-07-09 (50)**: **메신저 3계층 완결 (directive — Slack/Teams)** — rail 3섹션: **# 채널(반영구) · ▸ 회의(임시 — LIVE 펌스 도트/「종료 날짜」 칩·종료 시 MT- 개체 보존) · 다이렉트(프레젦스 도트)**. 「새 대화」 팝오버 = 종류 세그먼트(채널/회의)+이름+Enter 개설(`msgrCreate` — 감사·MT- 채번). 활성 회의 헤더: LIVE 칩 + 「회의 종료」(임시 그룹 아카이브·읽기 보존·`msgrMtEnd`) · 종료 회의=「종료 — 읽기 보존」 칩. 헤더 position:relative 앵커.

- **2026-07-09 (51)**: **우측 패널 스니그핏 + 근무 형태 결재 + nav 하단 관례 (directives 3건)** — ① 개요 오늘 패널: 헤더+출근 칩 한 줄 병합(punchMeta는 캘린더 버튼 title로)·주간 스트립/스케줄 행 패딩 타이트닝·min-width 264 — 좀은 폭 스니그핏. ② **외근·출장·재택 = 결재**: lvReqs에 근무 형태 신청 3종 시드(AP- ref·사유) · type enum 칩 톤 맵(외근=ok·출장=warn·재택=info) · lvDecide target 「근무 형태」 분기+승인 토스트(타임테이블 반영) · 패널 제목 「연차·근무 신청 결재」. ③ **nav 하단 관례 섹션**(margin-top:auto 보텀 그룹): 지원 센터(거버넌스에서 이동)·**설정 팝오버**(방해 금지=전 배지 숨김 prefMuteAll 실동작 토글·passkey 관리→수신함·계정 지원→지원센터)·**정보 팝오버**(버전·감사 해시체인 앵커→감사 화면·단축키) · withMt 전 필터 경로·Esc 체인. + 메신저 3계층(50) 후속 앵커 픽스.

- **2026-07-09 (52)**: **결원 업무 인수인계 자동화 (directive)** — 벤치마크: PagerDuty 재라우팅·ServiceNow 스킬 라우팅·Asana 워크로드. `handoverRun(issueId)`: 결원자 잔여 업무를 가중도·적합도·범위·부하로 판정 — **기본=분담(자동 적용·건별 감사)** · **조율·연기=부서장 조율 사항(four-eyes — 알림 발송·자동 적용 제외)** · 결원인 참여 없음. 진입: hrissue 모달(danger 톤=미출근/무단결근) 「인수인계 자동화」 버튼. 결과 모달: 행별 가중도 칩·코드·범위·→배정자(적합도·부하)·부서장 대기 구분 · 재실행 멱등(handoverDone). 플랜 시드: 최민석(경비·4건)·조이슨(사업운영·3건). 잔여: 플랜을 실제 items/스케줄 개체에서 파생(현재 시드)·부서장 측 조율 큐 UI·워크플로 시드 등재.

- **2026-07-09 (53)**: **설정 가능화 1단계** — `HANDOVER_POLICY()` 정책 개체 추출: autoAct(분담)·escalate(조율/연기)·fitFloor(80% — 미달 시 자동 분담 금지→에스컬레이션 실동작)·deptHeads(팀→부서장 매핑 — 플랜 내 하드코드 제거). handoverRun은 정책만 판정·감사 reason에 정책 출처 명시. 잔여(다음 ①): 정책 개체를 no-code 캔버스 편집 대상으로 노출·결재선/SLA 등 잔여 하드코딩 스윕·PLANS→실제 개체 파생.

- **2026-07-09 (55)**: **인수인계 → 워크플로 스튜디오 + 정책 이전 (directive) + 워크플로 빌더 = typed·actionable config (n8n/Workato/Foundry depth)**. (a) **wf7 「결원 발생 → 업무 인수인계 자동 실행」** 시드(policy:"HO-01"·chain 근태·직원·인수인계) — wfRun wf7 = 당일 danger 결원 handoverRun 실행 · wf 상세 「HO-01 인수인계 정책」 칩 → `hoPolOpen` 단독 정책 에디터 모달(핸드오버 모달 밖에서 편집 — hoPolModalOn·Esc 체인·개정 스테이징 §3.9.0). (b) **워크플로 빌더 파라미터화** — `WF_BLOCKS` 트리거/액션에 `params[{key,label,kind:enum|text,options,def}]` + 인스턴스 `cfg` · **조건 = field·operator·value** (`WF_COND_FIELDS` 8종·타입 number/enum/bool·연산 ≥/≤/=/≠/∈·단위) — 완전 configurable 술어 · `wfCfgSet`·`wfCondAdd/Set/Remove`·`wfCondMeta/Text`·`wfTrigText/wfActText/wfActShort`. (c) **시뮬 = 실평가** — `_wfSampleEvents`(트리거 cfg 필터) + `_wfEvalCond`(typed 술어 평가) → 표본×통과 실계산 토스트+감사(dry-run). (d) 저장=cfg+typed 조건 라벨 영속(`wfBuilderSave`)·`wfEditOpen` cfg rehydrate. 검증 e2e: 근태·무단결근·전사 트리거 + 직원 결근 횟수 ≥3회 조건 + AP- 생성 액션 → 시뮬 「표본 1건·통과 1건」, 저장 「근태 이벤트 · 무단결근 · 전사 → AP- 생성」·조건 라벨 「직원 결근 횟수 (30일) ≥ 3회」.

- **2026-07-09 (56)**: **온톨로지 엔진 (single engine, multiple consumers — directive · Palantir Foundry/Maven 벤치마크) 1차**. **`ONT_TYPES()` = 단일 타입 레지스트리** (16 타입): 타입별 **typed 속성 스키마**(text/money/percent/date/enum/lifecycle/person…) + **관계(링크) 유형**([rel, toType]) + **액션(writeback)**([key,label]) + **분석(파생 속성)**([key, 산식]) — `state.ontTypeDefs` 오버레이로 no-code 편집(속성/링크/분석 추가·병합). (a) **그래프 = 1차 소비자** — OG_STATIC 플래그십 노드에 typed `props` 부여, 중심 개체 카드에 **액션 · ACTIONS**(invokable — `ogActionRun` 타입 정의 파생·전량 감사·lcOpen/exploreGo/openPerson/subOpen/modApprPrefill/화면 이동 등 실 dispatch), **속성 · PROPERTIES**(속성명 + 타입 배지 + 값), **분석 · 파생 속성**(산식) 패널 신설 — 단순 노드 이동 초월. (b) **정책 = 2차 소비자** — POL_BLOCKS resource = 큐레이트 + `ONT_TYPES` 키(「개체 타입 · <T>」) → 타입 추가 시 정책 대상 자동 노출(single source). 검증 e2e: C-207 계약 카드 = 액션 3(생애주기/수익성/갱신)·속성 7(거래처:text·월 계약금:money·상태:lifecycle…)·분석 2, 「수익성 분석」 클릭 → 분석 노드 재중심(자체 카드), logicError 0.

- **2026-07-09 (57)**: **온톨로지 엔진 2차 — 타입 카드 = configurable 에디터 (ⓐ 관계 유형 + ⓑ 분석 편집기)**. 타입 개체 카드(lcOpen OT-)에 「온톨로지 엔진 정의 · no-code」 섹션 신설: **관계(링크) 유형**(rel 입력 + 대상 타입 select + 추가) · **분석·파생 속성**(산식 입력 Enter) · **액션 · ACTIONS**(엔진 정의 표시). 편집 = `state.ontTypeDefs` 오버레이 write(ONT_TYPES가 병합 → 그래프·정책·워크플로 전 소비자 반영) · **활성 타입 = 개정 스테이징**(§3.9.0 — `ontDefPend` → 「적용 승인」 four-eyes v+1 발효·「철회」)·초안 = 직행. 메서드 `ontLinkAdd/ontAnaAdd/ontDefApprove/ontDefDiscard`·`_ontLabelOf`. 마크다운 전체 동기: ROADMAP 진행 로그+매트릭스(auto=typed·actionable 빌더·explore=온톨로지 엔진), DESIGN §4-20(엔진 헌장), HANDOFF §18(백엔드 계약). 검증 e2e: 계약(OT-01·활성) 「감사 대상→감사」 관계 추가 → 개정 대기 → 적용 승인 「온톨로지 정의 v3 발효 — 전 소비자 반영」. **잔여**: 모듈 서피스 엔진 소비(MOD_SCREENS→타입별 인스턴스 쿼리)·객체/타입 CRUD 단일 엔진화·정책/컴플라이언스 typed 실평가. **잔여(directive)**: 관계(링크) 유형 편집기·분석 편집기 UI·모듈 서피스가 엔진 소비(하드코딩 제거)·객체/타입 CRUD 단일 엔진화 · 정책·컴플라이언스 typed·actionable config.

- **2026-07-09 (58)**: **전면 온톨로지 커버리지 + 컴플라이언스 엔진화 + 카디널리티 + 구성 콘솔 헌장 (directive 다발 — "literally everything is ontology, interactions through workflow, add/modify/archive types, fully configurable console")**. (a) **관계 유형 카디널리티 typed 메타(ⓒ 완료)**: `ontLinkAdd` 3-tuple `[rel,to,card]`(1:1/1:N/N:1/N:N)·타입 카드 관계 편집기에 카디널리티 select+칩 배지·개정 대기 표시·계약/직원/급여 base 링크에 명시 카디널리티. (b) **컴플라이언스 = 온톨로지**: `ONT_TYPES()`에 컴플라이언스(CP-)·규제 파라미터(RG-)·표준 프레임워크(FW-) 3 타입(근거 규제·대상 개체·이행 기안·증거·감시 워크플로 typed 링크·산식 analytics) + `_modRowType`(compliance 행을 `en` 「구분」으로 타입 분기) — 정책 resource·워크플로 트리거 자동 노출. (c) **전면 커버리지**: 문서·기록물·전표·구매·재고·자산·정비·현장·예측·공지·지원 티켓·채용공고·메일·메신저·알림·연차·복리후생·평가·자동화·생산 파이프라인·시리즈 등 20+ 도메인 타입을 base에 추가 — 어느 화면 명사든 엔진 정의 보유. `ogActionRun`·`ONT_ACTION_LABEL`에 18개 화면 이동 액션 추가. (d) **신규 타입 1급화**: `ONT_TYPES()`가 `state.ontTypes` 레지스트리·사용자 제안 타입을 자동 병합(ONT_SCHEMA_DEF 기반 기본 스키마 파생·dataType 추론) → 신규 타입 즉시 관계·분석·속성 편집·그래프/정책/워크플로 활용(engineOn=true). (e) **문서**: HANDOFF §18.1(전면 커버리지)·§18.2(CRUD·거버넌스 — 정의 변경=개정 스테이징·구현/일몰 창·breaking change 방지)·**§19 구성 가능한 콘솔/대시보드 에디터**(컴포넌트 모델·거동 config·열 config·거버넌스·벤치마크 ≥8: Retool·Appsmith·ToolJet·Budibase·Superblocks·UI Bakery·Windmill·Power Apps·OutSystems/Mendix·Foundry Workshop — config=거버넌스되는 온톨로지 개체가 차별점) 신설. ⚠ HANDOFF 감사 완료(19섹션·최신). **잔여**: ⓐ 모듈 서피스가 엔진 쿼리 소비(MOD_SCREENS 하드코딩 제거) ⓑ 인스턴스 스토어 통합 · 구성 콘솔 에디터 UI 구현(§19).

- **2026-07-09 (59)**: **§19 구성 콘솔 1차 + 인스턴스 쿼리 + 개인 자동화 (directive)**. (a) **모듈 테이블 「구성」 모드(전 모듈 서피스 공통)**: 헤더 기어 버튼 → 구성 스트립 — **열 추가**(온톨로지 타입 propSchema ∪ 행 kv/en 키 → select, 커스텀 열 = 그리드·헤더·행 셀 동적 렌더·× 제거) · **행 선택 거동**(분할 패널 ↔ 개체 카드 = 기존 창 모델 재사용) · 기본값 복원 — per-screen `state.modConfig`, 전 변경 = 개인 뷰(§3.9.0-①) 감사(`modColAdd/modColRemove/modDetSet/modCfgReset`). (b) **단일 인스턴스 쿼리 `ontQuery(type)`**(ⓑ 착수 — 그래프=단일 스토어) + **타입 카드 「인스턴스 N건」 drill**(칩 클릭=개체 이동 — TODO 스키마 심화 잔여 해소). (c) **개인 자동화(directive)**: 워크플로 빌더 범위 세그먼트(전사/개인) — 개인 = 즉시 활성(§3.9.0-① 직행·감사)·소유자 한정 노출(autoWfList 필터·「개인」 칩)·본인 개체에만 작용, 전사 = 기존 초안→게시 승인. HANDOFF §6 스코프 계약 추가.

- **2026-07-09 (60)**: **§19 마감 스윕 + 타입 레지스트리 완결**. (a) **컴포넌트 팔레트 1차** — 모듈 구성 스트립 「+ 스탯 — 카운트」(전체 행 수 · en 필드 값별 카운트, 헤더 스탯 바 합류·클릭=목록 필터 drill — 죽은 숫자 금지 준수)·× 제거·`modStatAdd/Remove`. (b) **열 드래그 재정렬**(`modColMove` — 커스텀 열 칩 HTML5 dnd·감사). (c) **팀 배포 — 결재**(`modCfgDeploy` — 공유 레이아웃 배포 = 기안 프리필[열·스탯·거동 요약]·§3.9.0-④ 감사). (d) **타입 레지스트리 완결**: ontTypes +17종(OT-15~31: 전표·구매·재고·자산·공지·지원 티켓·메일·메신저·알림·연차·복리후생·평가·자동화·생산 파이프라인·문서·기록물·채용공고 — 전부 stage/steward/note 거버넌스) · base ot 코드 충돌 5건 해소(메신저 OT-27·알림 OT-28·문서 OT-29·기록물 OT-30·채용공고 OT-31) · **모듈 헤더 = 바운드 타입 칩**(OT- 코드·클릭=타입 카드 — 서피스↔타입↔인스턴스 왕복, ⓐ의 UI 계약). 잔여(ⓐ 완결): 행 데이터 자체를 엔진 인스턴스 스토어에서 파생(백엔드 계약 HANDOFF §18).

- **2026-07-09 (61)**: **전면 CRUD 스토리 완결 1차 (directive — New Analytics/Forecast/Insight/Workflow/Object/Property/Data · full CRUD · 초안 이후 변경=오버라이드)**. (a) **typed 개체 생성 위저드**(`objNewOpen/objNewSave` + 모달): 타입 select(레지스트리 전 타입)·이름 fail-closed·**타입 스키마 필드**(dataType 배지)·초기 관계 코드 → OB- 초안 생성(§3.9)·그래프 합류·카드 오픈. 진입: 탐색 「+ 새 개체」 Enter · 타입 카드 「+ 새 인스턴스」(인스턴스 섹션 상시 노출). 분석(OT-11)·예측(OT-32 신설·base ot 정합)도 위저드로 생성 = New Analytics/Forecast/Insight 경로. (b) **초안 편집 vs 데이터 오버라이드**: OB- 초안=위저드 직행(§3.9.0-③) · **초안 이후(시드·활성) 개체 = 오버라이드**(`objOverrides` — 사유 필수 fail-closed + `_autoAP` 결재선 + pending 상태·현행 값 유지) → 개체 카드 warn 배너 「적용 승인 — four-eyes」/「철회」(`ovApprove/ovDiscard` — 발효 시 `ONTOLOGY_GRAPH` props 머지·이전 값 감사 보존). 카드 칩 = 「속성 편집」(초안)/「데이터 오버라이드」(이후) 자동 분기. (c) `ogActionRun` default = explore 재중심(placeholder 토스트 제거 — 데드 액션 0). (d) **HANDOFF §20 전면 CRUD 감사 매트릭스** 신설(개체·타입·관계·분석·워크플로·정책·화면 구성·데이터 각각의 C/R/U/D UI 경로 + 잔여 갭 레지스터). Esc 체인 objNew 등록.

- **2026-07-09 (62)**: **파운드리 IA 재편 + 구성 폴리시 + 매니저 백엔드 (directives 다발)**. (a) **IA 연구·재편**: Foundry Automate(자동화 단일 진입점·시간+개체 조건→effects)·OMA Automations 탭·Explorer→모니터 생성 연구 근거로 nav 「파운드리」 그룹 신설 — 온톨로지(객체 탐색 흡수·현재 explore로 배선), 자동화 · Automate(워크플로+예약 통합 항목), 분석 · 감시(경영 분석 이동 — 분석=감시·자동화와 한 몸). 분석 그룹=대시보드·예측. NAV_SCR 동기. (b) **구성 모드 폴리시**(디렉티브 "예측에서 기어가 무용"): 열 옵션 = 실데이터 커버리지 필드만(k · n/N 표기·빈 열 방지), 스탯 옵션 = 상태(st) 값 카운트 추가(en 없는 모듈도 유효 — 예측 등), `__st` 스펙 파싱. (c) **온톨로지 매니저 백엔드**: `ontPropAdd`(이름+dataType·스테이징)·`ontActAdd`(액션 정의·미등록 key=explore 폴백 디스패치)·`ontDefApprove` pend 병합 확장(props·actions)·ONT_TYPES 오버레이 actions 병합(기존 누락 수정). (d) **신규 directives 등재**: ERP=SAP×Palantir(3-way match·원장 무결성·이동 문서·정비 오더 사이클), 모듈별 3문 리뷰 루프(Palantir/SAP/Slack → 갭 레지스터 환류), 온톨로지 매니저 워크스페이스(다음 최우선). TODO 3개 섹션 신설.

- **2026-07-09 (63)**: **온톨로지 매니저 워크스페이스 + Automate 완결 1차 + ERP 3-way match (Foundry OMA·Automate·SAP 벤치마크)**. (a) **온톨로지 매니저**: explore 화면 = [타입 · 매니저 | 그래프 · 탐색] 탭(`ontTab`) — 좌 타입 레지스트리(검색·스테이지 칩·인스턴스 수·「+ 타입 제안」) · 중 타입 에디터: 헤더(스테이지·ver·steward·생애주기 카드·「+ 새 인스턴스」 위저드) + 6 서브탭(속성[이름+dataType select 추가]·관계[대상 타입+카디널리티 추가]·액션[label+디스패치 select 추가]·분석[산식]·인스턴스[ontQuery drill]·자동화[작용 규칙+「+ 자동화 — 타입 프리필」]) + 개정 스테이징 배너(속성/관계/액션/분석/스키마 diff·적용 승인 four-eyes/철회). `om` vals 단일 객체. nav 「온톨로지」 = 이 화면. (b) **Automate**: explore 개체 패널 「+ 감시 규칙 — <타입> 트리거」(Object Monitor — 빌더 트리거 프리필) · 자동화 탭 스트립에 「분석 · 감시」(laborcost 연결). (c) **ERP 구매 = SAP 문서 흐름**: PO- 4행 `flow`(PR→PO→GR→IR→지급 스텝퍼 — done/cur/next 칩·코드=drill) + **3-way match**(PO-118 송장 ₩2.6M≠PO ₩2.4M 불일치 → 지급 차단 fail-closed·「예외 결재 — 단가 차이 소명」 CTA) — 제네릭 `flow`/`match` 필드(어느 모듈이든 사용: 정비 오더 사이클 등 후속). 신규 상태: ontTab/omTabSel/ontMgrSel/ontMgrQ/omProp*/omAct*.

- **2026-07-09 (64)**: **ERP 딥빌드 2차 (SAP×Palantir) + 3문 리뷰 1차 실행**. (a) **재무 = 차대 균형 게이트**: VC- 3행 flow(기표→차대 검증→승인→전기) + 균형 match 칩 — VC-2604 차·대 불균형(₩0.84M≠₩0.80M 원천세 누락) = **전기 차단 fail-closed**·기표 수정 유도, VC-2607/2606 균형 ok. (b) **정비 = 오더 사이클**: WO-2643 flow(접수→계획·부품 예약[PO-121 drill]→실행→정산→전표) — 구매·재고 문서 체인 왕복. (c) **재고 = 이동 문서**: IV-031 tl(입고 +6 PO-118 · 출고 −4 WO-2638 · 출고 예약 −2 WO-2643 · 입고 예정 +4 PO-121 점선) + 「현재고 산출 = 이동 문서 합계」 — 현재고=파생 원칙. (d) **3문 리뷰 1차(§4-21)**: Palantir(브랜치·머지 proposal·역방향 링크 이름·액션 폼 빌더·Workshop 임베드)·SAP(재고 평가·부분 입고/송장 대사·이중 분개 자동 생성·MRP)·Slack(reply-in-thread·예약 발송·huddle) 갭 → 벤치마크 갭 레지스터 환류.

- **2026-07-09 (65)**: **파운드리 패키지 일관성 (directive — coherent complete package · 전 모듈 소비 · 알고리즘-우선 정체성)**. (a) **Automate effect = 온톨로지 액션**: 워크플로 빌더 실행 팔레트가 트리거 타입의 액션 정의(writeback)를 자동 노출(`wfBuilderPickOntAct` — 「개체 액션 — <label>」 블록, wfActText/저장 호환) — 타입에 액션 추가 시 자동화가 즉시 소비(single engine). (b) **전 모듈 = Automate 소비**: 모듈 상세 제네릭 「작용 자동화」 칩 행(해당 타입을 만지는 규칙 ≤3 · 클릭=규칙 · 개인 규칙 소유자 게이트) + 「+ 감시 규칙」(타입 트리거 프리필 — Object Monitor 문법, `_modRowType` 기반 컴플라이언스 3타입 분기 포함). (c) **정체성 헌장(§4-20 서두)**: Palantir=AI-우선 ↔ 우리=알고리즘·기계적·프로그래매틱·no-code 우선 — UI/UX·기능 동급, AI 대체=규칙·템플릿·술어·시뮬.

- **2026-07-09 (66)**: **인라인 코멘트 라운드 (Jason 3건 + 사용자 4건)**. ① **그래프 저작 발견성**: 캔버스 좌상단 저작 스트립 — 「+ 새 개체」(위저드)·「+ 관계 — 코드 Enter」(objLinkAdd 인라인)·「+ 속성·분석 — 매니저」(타입 에디터 딥링크)·「+ 감시」(트리거 프리필). ② **범례 = 레이어 그룹**(exploreLegendG — 코어 체인/조직·거래/거버넌스/ERP/문서·데이터/인사·채용/커뮤·기타 + 보관 dim, 30 플랫 칩 해소). ③ **그래프 줌**(exZoom 0.6–1.5 — 우상단 −/%/+ 컨트롤, 반경·노드 scale) + **노드 정보 심화**(하류 연결 +N 배지·title 툴팁: 관계·타입·meta·전개 힌트). ④ **nav IA 재편(source-cited 답변)**: 전자결재→개요 그룹(그룹웨어·Workday 일상 업무 관례)·데이터 인제스트→파운드리(Pipeline Builder)·현장 운영을 ERP 앞으로(도메인 빈도 우선 — SAP Fiori 역할·빈도 원칙). ⑤ **split-pane 대응 일반화**: `effW`(vw − 사이드바 − rail − 핀 패널 차감) — 메일 3-pane은 effW<680에서 폴더=칩 스트립·목록↔읽기 스택, 메신저는 effW<700에서 목록↔채팅 스택(모바일 문법 재사용 — 핀 분할·좁은 창에서 수직 찌그러짐 해소). 회의 노트 카드=기존 컬럼 구조 적합 확인. 대시보드 nav 파운드리 최상단 이동.

- **2026-07-09 (67)**: **대시보드 = 자가 유지 구성 (directive) + 인사이트 = 예측·리스크 합류**. `.dc.html` 프로토타입 기록이며 현재 React Dashboard의 live `ontQuery` 증거가 아니다. (a) **대시보드 구성 모드**: 헤더 기어 → 구성 스트립 — 「+ 위젯 — 타입 카운트」(활성 온톨로지 타입 select) = **프로토타입 단일 스토어 쿼리 위젯**(`ontQuery` — 개체 변경 시 자동 갱신·손수 유지 없음·클릭=매니저 인스턴스 drill)·× 제거·「팀 배포 — 결재」(§3.9.0-④), 전 변경 감사(`dashWidgetAdd/Remove`). 대시보드=표시 전용, 산출은 온톨로지·자동화가 소유. (b) **인사이트 = 예측·리스크 모듈 합류(directive)**: DASH_INSIGHTS(AN-) 행이 forecast 모듈에 합류 — 근거 체인 links·「처방 실행」/「그래프 — 근거 체인」 acts·「산출 = 규칙 기반 파생, 재계산」 kv · `_modRowType` forecast 인사이트 행 → 분석 타입 분기. (c) nav 자동화/예약 작업 분리(사용자 코멘트) — 파운드리 그룹 5항목.

- **2026-07-09 (68)**: **정량 투영 — 초안=통계적 예측 (directive)**. typed 위저드(생성·오버라이드)에 「예상 결과 · PROJECTION」 패널 — 타입에 analytics 정의가 있으면 money/percent 속성 입력 즉시 **점추정·CI95·CVaR95(fat-tail)·가정(EWMA σ8% · student-t ν=4)** 칩 산출(결정적 quant·무AI — 미입력 시 산출 조건 표기). HANDOFF §18 분석 계약 = Monte Carlo·EVT/GPD 꼬리 적합 백엔드. 잔여(TODO 등재): 기안 컴포저 등 전 초안 소급 · what-if 슬라이더 시나리오 · 계약 갱신/해지 비교.

- **2026-07-09 (69)**: **가격·전투 선택 = 정량 프레임워크 (directive — "price ourselves, pick our battles")**. 예측·리스크에 입찰 판단 참조 구현 2건 — **FC-05 입찰 판단**(Bid-633: 제안가 밴드 ₩41.2–43.5M·승률 로짓 곡선·원가 적층·마진 분포 CI95/CVaR95·RG-101 연동 게이트·**단가 하한 미만=회피 fail-closed**·「입찰 기안 — 단가 하한 동봉」 prefill) · **FC-06 갱신 가격**(C-207: 인상 밴드 +4.8~6.2%·이탈 위험 18%<임계 25% 게이트·AN-201/SR-206 근거 체인·갱신 협상 기안). 비기술자 친화 = 판정 칩·밴드·게이트가 1면, 방법론(EWMA·t(ν=4)·CVaR·로짓)은 kv. 잔여(TODO): 승률 곡선 시각화·인제스트 Bid- 유입 시 자동 판단 생성(자동화 트리거)·포트폴리오 마진×리스크 뷰.

- **2026-07-09 (70)**: **대시보드 슬롯 프리셋 + 저작 스트립 상시화 + 검증 픽스**. (a) **대시보드 4 패널 = 슬롯**(directive — durable): 구성 모드에서 헤더 select로 프리셋 교체 — 수익성→입찰·갱신 판단/인사이트 · 추이→온톨로지 타입 카운트 · 커버리지→정비 SLA/컴플라이언스 기한 · 내 지표→감시 규칙. 전 프리셋=기존 소스 재사용(bids/types/sla/comp/watch — `altRowsOf`)·클릭=drill·전환 감사(`dashSlots`). (b) **그래프 저작 스트립 = 헤더 상시**(사용자 코멘트 — 플로팅 박스 제거): + 새 개체·+ 관계 코드·+ 속성·분석·+ 감시가 온톨로지 헤더에 항상 노출(양 탭 공통). (c) 검증 픽스: 신규 입찰 판단 행 FC-05/06 → **FC-07/08 renumber**(코드 중복 — _ogBuild 인덱스 충돌로 그래프 미합류였음). (d) directive 등재: 팔레트 결과 = 창 모델 후보 · 모듈 섹션 창 모델 소급(연차 등 CARD_META 미등재 리포트) — TODO §4-23 audit 항목에 구체화.

- **2026-07-09 (71)**: **설정·정보 = 풀 화면 + nav 하단 그룹 타이틀 (사용자 코멘트)**. nav 하단 그룹 라벨 「시스템」 · 설정/정보 팝오버 → `screen:"settings"`(탭 [설정|정보]), `can()` settings=보편 허용, 구 팝오버 navItem 중복 제거. setTab 상태 신설.
- **2026-07-10 (90)**: **devtools 탐지 → 세션 잠금·토큰 무효화 (directive)** — `onDevtoolsProbe`(1.5s 휴리스틱: outer−inner 뷰포트 델타>220 + debugger 타이밍>120ms) → `dlpLockSession` forbid anomaly 감사·전체 화면 잠금 오버레이(토큰 무효화 안내·passkey 재인증 `dlpRelock`). 정직성: 휴리스틱=우회·오탐 가능, 실제 강제=서버측 revoke(계층3). unmount interval 정리. (87·89와 함께 DLP 계층1 완비 — HANDOFF §13.1)
- **2026-07-10 (91)**: **레인 1 — 연차(leave) 카드 존 구현 (§4.7-2 창 모델 소급 · 구현 완료·e2e 검증 대기)** — CARD_META/CARD_TITLES/zoneRefs/cardMode에 `leave` 추가(main:[status]·side:[reqs,promote]·min 340/240/176·off 236), renderVals에 computeCardLay/cardDropVals/cardToolVals/splitBarOf("leave") + 존 emit(lvZoneRef/lvZoneH/lvLay{Status,Reqs,Promote}/lvDrop*/lvTool*/lvSplit*/lvLayMenu*/lvLayCustom/onLvLayReset) · **leaveScr=scrWrapOf**(scrWrapOf2→전환: 플로트 카드 화면 간 유지) · 템플릿: 3 섹션(현황·결재 큐·촉진)을 hr 존과 **동일 카드 래퍼**(헤더 드래그=팝아웃·더블클릭=핀·최소화=트레이·resize/corner 핸들·tool floater·drop indicator·split bar·배치 프리셋 메뉴·기본 배치 리셋)로 재작성 · **promote=상시 존 카드**(sc-if lvPromoteOn 제거 → lvPromoteEmpty 빈상태 CTA §4-22) · minChips 태그 맵 leave:"연차". 로직·템플릿 각 원자 편집(1스텝=일관 상태). **다음**: 검증(핀/플로트/트레이·split·프리셋·promote 빈상태 e2e + 콘솔 무오류) → 통과 시 레인 2(benefit·docs·appr) 동일 패턴 소급.
- **2026-07-10 (92)**: **레인 2(부분) — 복리후생(benefit) 단일 카드 존 소급 (§4.7-2)** — benefit를 CARD_META(main:[scheme]·side:[]·min scheme400·off 214)에 추가, zoneRefs·cardMode·computeCardLay/cardDropVals/cardToolVals/splitBarOf("benefit") 배선, benefitScr=scrWrapOf 전환, renderVals에 bnZoneRef/bnZoneH/bnLayScheme/bnTool*/bnModalBack* emit, minChips 태그 benefit:"복리후생". 템플릿: 제도 테이블 카드를 존 카드 래퍼(absolute·height=bnZoneH·hover tool floater[핀/최소화/닫기]·resize/corner·modal back)로 재작성. 단일 카드라 split/드롭/배치메뉴 미노출(무해 inert). 검증: absolute·w100%·h480px 존 카드 확인·콘솔 무오류(bn* 미해결 홀 없음). **다음**: docs → appr(탭 워크스페이스 — 별도 취급) 소급.
- **2026-07-10 (93)**: **레인 2 프로토타입 구현 범위 완료 · 폐루프 검증 대기 — 문서·기록물(docs) 단일 카드 존 소급 (§4.7-2)** — benefit 패턴 그대로 재사용(dz 접두사): CARD_META docs(main:[archive]·off 176), zoneRefs·cardMode·compute/drop/tool/split("docs"), docsScr=scrWrapOf, renderVals dz* emit, minChips docs:"문서". 템플릿: 기록물 테이블 카드(필터 헤더 포함)를 존 카드 래퍼로. 정적 구현 확인: absolute·w100%·h480px·콘솔 무오류. **폐루프 검증 대기:** 기능·엣지 케이스·사용성/마찰·source-cited 벤치마크·타 모듈 통합·목업 독립성·페르소나·레이아웃의 동작 증거가 아직 없다. **appr 제외 결정**: 전자결재는 탭 워크스페이스(결재함/상신함/기안 컴포저) — 참조 패널이 아니므로 플로트 가치 낮고 변환 위험 큼. 의도적 비존화(갭 아님). 레인 2의 프로토타입 구현 범위는 benefit+docs에서 닫혔지만 제품 슬라이스 완료는 아니다.
- **2026-07-10 (94)**: **레인 3 — 대시보드 구성 위젯 제네릭화 (온톨로지 쿼리 바인딩)** — `.dc.html` 프로토타입 구현이며 현재 React Dashboard의 live 경로가 아니다. dashWidgetAdd(kind, bind)로 일반화: 위젯 = {kind: count|trend|dist, bind}. **카운트**=타입 인스턴스 수(기존), **추이**=OBJ_SERIES(SR-) 실데이터 스파크라인(inst[].pct→polyline·현재점·클릭=시리즈 개체 exploreGo), **분포**=ontQuery 인스턴스 상태(meta 말단 세그먼트) 그룹 → 상태 칩 top4(클릭=인스턴스 drill). 구성 스트립에 3개 select(+카운트/+추이/+분포)·칩 라벨 kind 접두("추이 ·"/"분포 ·")·중복 방지·감사 로그 kind 표기. 타임라인(목록형)은 기존 슬롯 프리셋(bids/sla/comp 인스턴스 리스트)이 커버 — 중복 구현 안 함. 검증: 3종 렌더(스파크라인 1·분포 칩 2·카운트)·콘솔 무오류.
- **2026-07-10 (95)**: **레인 5 — 기안 구조화 확대 (§68 투영·fail-closed 소급)** — ① 증빙 fail-closed를 지출류 전체로 확장: reimburse만 → reimburse·expense·purchase (errE 게이트·"지출 증빙 (필수)" 라벨·유형별 첨부 힌트). ② **§68 금액 투영 패널**: 지출류 기안에 증빙 ≥1건이면 컴포저에 투영 스탯 바(합계·건수·최대 건 — 구조화 증빙 라인에서 실계산) + 파생 체인 칩(경비 청구="승인 → 급여 환급 반영" / 전표·구매="승인 → 전표 VC- 파생"). ③ 상신 로그·토스트에 합계 ₩ 포함. 검증: 지출결의서 컴포저에서 증빙 2건 추가 → 투영 패널·체인 칩 렌더·게이트 순차 동작·콘솔 무오류. 대상자 명단(개체 링크 req)·중복 점검(apprDup)은 기존 완비 확인.
- **2026-07-10 (96)**: **레인 9 — 대시보드 실데이터 파생 (시드 상수 제거)** — dashStats 시드 테이블(D) 제거: 7월(진행)=라이브 실계산 — 인건비율·계약 마진=DASH_CONTRACTS() 단일 소스(신설 메서드, 스탯 바·계약 테이블 공용 소비, C-208 scen=시뮬레이션 집계 제외, Σlabor/Σrev·매출가중 마진) · 출근율=ATT_SITES 법인 스코프 Σinn/Σplan · 미편성 결원=미출근 이슈(hrHandled 미처리)−subAssignments(대근 편성 시 라이브 감소) · 주 52h=WEEK52 비ok−att52Handled · 결재 SLA=items 미결·긴급 큐. 6월(확정)=마감 스냅샷 상수 유지(§3.9 보관 집계 — 정당한 스냅샷). 추이 차트=SR-205 시리즈 단일 소스 소비(3–7월 실적+8월 예상, §4-19). 검증: 6종 스탯 라이브 렌더·미편성 결원 1(lc8과 일치)·52h=3·SLA=4(원본 큐와 일치)·콘솔 무오류.
- **2026-07-10 (97)**: **레인 17(부분) — 급여 명부 J/K/Enter** — payListSorted() 단일 조회 지점 신설(§4-19 — renderVals·handleKey 공용), handleKey pay 분기(J/K=paySel 이동·Enter=인사 카드), payRows rowBg/ring 하이라이트(hr/leave 패턴 동일)·행 title "J/K 이동". 주의: 명부는 계산 게이트 뒤에 렌더 — 코드 경로는 hr/leave와 대칭. **잔여**: 공고(recruit)·월간(attMRows) J/K·포커스 순서·aria-label 스윕.
- **2026-07-10 (98)**: **레인 17 완결 — 공고·월간 J/K/Enter + aria 스윕** — recruit: rcSel J/K·Enter=공고 토글(rcOpen)·행 하이라이트·title "J/K 이동·Enter 열기". 월간(att month): attMRows 렌더 후처리(rowBg/ring)+`_attMNav` 캐시(렌더 선행 보장) → J/K=attMSel 이동·Enter=행 액션(드릴/인사 카드) 재사용. 검증: recruit 6행 중 jj→2행 선택·Enter 오픈 / 월간 4단위 중 KNL 선택·Enter 드릴(하위 2행). aria: 마운트 전 화면 대상 무명 아이콘 버튼 0건 확인.
- **2026-07-10 (99)**: **레인 16 — 미편성 결원 SLO 알림 시드** — 초기 notifs에 nslo1([SLO 위반] 대원강업 상주 결원 — 커버 편성 4h 경과·목표 2h·link→att) 시드, subAssign(대근 편성) 시 자동 해소(nslo1 제거). 탐지→편성→해소 체인 완결.
- **2026-07-10 (100)**: **레인 10 — 증거 WORM 뷰어 (HANDOFF §11)** — EVIDENCE에 media(EV-101 video·EV-102 audio)·entries(EV-103 ZIP 12파일: 경로·크기·해시) 데이터 추가. 생애주기 카드에 ① 미디어 자리: 원본 WORM 봉인 페인(클릭=접근 차단 감사 forbid·fail-closed) + 파생 프리뷰 버튼(파생본 칩·클릭=열람 감사) ② ZIP readonly 엔트리 트리(구성 파일 색인·읽기 전용 칩·엔트리 클릭=파생 프리뷰 열람 감사). evPreview() 단일 액션 지점. 검증: EV-101 페인·파생 재생 감사 토스트, EV-103 12엔트리·클릭 감사·콘솔 무오류.
- **2026-07-10 (101)**: **실행 큐 잔여 4종 + 커버 플래너 (레인 7·#11·체크인·§18.2·[~]13)**. ① **레인 7 인제스트 심화**: `ING_TPLS()` TP-01~07 매핑 템플릿 = 재사용 개체(`ingTplOv/User/Pend` 오버레이) · **no-code 에디터 모달**(목록[스테이지·버전·사용 N건]·매핑 행 src→tgt·변환 enum 7종·사용 작업 drill) · 활성=개정 스테이징(적용 승인 four-eyes=v+1 발효·철회)·초안→게시(fail-closed)·보관=참조 무결성 게이트(사용 중 차단) · **계보(lineage) 스트립**: 작업 상세 4노드 소스(→큐 필터)→변환·템플릿(→에디터)→검증(→저확신 필드)→개체(→lcOpen/objectLinkGo) · 진입 3곳(헤더 버튼·작업 템플릿 칩·계보 노드) · 인제스트 h1 기술 캐션 삭제(§4-12) · Esc 체인. ② **#11 퇴사·휴직**: `hrSepCases` 상태기계 + 단일 모달(인사 카드 「휴직/퇴사 처리」 진입) — 초안(사유 enum 5종·발효일)→사전점검(진행 업무·커버 필수·동결 창)→결재 SoD 4단계(AP- 상신·maker-checker)→발효(empSt 전환 — hrMatch 비활성·집계 제외 자동·커버 필수=충원·대근 알림)→[퇴사]**회수 정산 6항 체크**(자산·계정·인수인계·연차·급여·4대보험 — 미완=보관 차단 fail-closed·항목별 「이동」 칩)→보관 / [휴직]복직=재직 전환. 전 전이 logEvent. ③ **체크인 심화**: `attCheckVerify` 기기(지급 단말 등록)×지오펜스 게이트(미등록=forbid fail-closed·인증 결과=감사+내 업무 kv) · 체크인/아웃=`buildAttDayTL` 실적 트랙 실시간 반영(캐시 무효화·checkedInWho·체크인 칩) · **교대 스왑** `attSwapReq`(내 업무 액션 → lvReqs 결재 큐 합류 · 상호 동의→반장 승인→타임테이블 상호 교체 · AP-·알림·enum 톤 accent). ④ **§18.2 CRUD 거버넌스**: 정의 개정 대기 배너에 **발효일 입력**(구현 창 — 승인 감사·토스트 명시) · 속성/관계 행 「일몰」 버튼 = `ontDepr`(**deprecated 30일 유예 → 8/09 보관** — 활성=스테이징·초안=직행 ③·하드삭제 금지) · 일몰 행=dim+칩·pendItems 표시. ⑤ **커버 플래너 D+7 ([~]13)**: `coverPlan()` = lvReqs(재택·스왑 제외)×커버 필수 포지션×편성 상태 + 휴직/퇴사 발효=장기 충원 행 — 근태 헤더 팝오버(미편성 badge·행: 승인→「대근 편성」=미래 일자 subOpen(date 키)·대기→결재 큐·장기→채용) · subAssignments `date` 키(오늘만 타임라인·월간·인력풀 스탯 반영) · 예약 sc6 「주간 커버 점검」 시드. 잔여 엣지(반차 부분 커버·팀 최소 인원 게이트)=TODO 13.

- **2026-07-09 (72)**: **차트 정직 스케일링 (directive · DESIGN §4-24 신설)** — 대시보드 인건비 추이: 상대 분산 < 1/3 → 축 절단(base = min − 0.6×range) + **「축 절단 — 기준 ₩xxxM (0 아님)」 warn 칩 명시**, 분산 크면 0 기준(왜곡 없음). hPct 하드코딩 → 계산 파생. 전 차트 공통 규칙으로 헌장화.

- **2026-07-09 (73)**: **콘솔 변경 기안 (사용자 코멘트)** — APPR_TEMPLATES에 「콘솔 변경 기안」(id: console — 화면 구성·온톨로지·자동화 변경, 전산·데이터 거버넌스) 신설 · **지원 티켓 SUP- 행 acts「콘솔 변경 기안 — 티켓 연계」**(제목에 티켓 코드 자동 링크 → 완전 추적성·유사 건 점검 합류) · 화면/대시보드 「팀 배포 — 결재」 = console 유형으로 전환. + (72 후속) 축 절단 표시 = 텍스트 칩 → **절취선 시각화**(기준값 라벨 + 지그재그 break SVG + 「0 ≠ 기준」).

- **2026-07-09 (74)**: **그래프 팬·범례 접기 + 축 절단 = y축 절취선 + 역방향 링크 이름 (코멘트·리뷰 갭)**. (a) **y축 통계 관례**: 인건비 추이 = 좌측 y축 거터(상단 max 틱·하단 기준 틱[절단 시 강조·툴팁]·축 위 지그재그 break 글리프) — 배너 텍스트 칩 제거(Jason "not enterprise/statistics" 반영). (b) **그래프 팬**: 캔버스 드래그 = `exPanStart`(window 리스너·±40% 클램프) — 중심 카드·노드·엣지 exCx/exCy 파생, 줌 리셋=팬 리셋, cursor:grab. (c) **범례 접기**: 「범례 · 타입 N」 토글(기본 접힘 — 레이어 패널·타입 제안 입력은 펼침 시) — 화면 점유 해소. (d) **역방향 링크 이름(Foundry reverse link — 리뷰 갭 소진)**: linkTypes 4-tuple [rel,to,card,rev] · 매니저 관계 탭 역방향 표시(← 화살표)+입력. 잔여 등재: 팬 터치 지원 · 부분 GR/IR 대사(SAP 갭) · 창 모델 audit·팔레트 플로트·승률 공선 what-if·ERP 잔여·대형 에픽 = TODO 레지스터 (단일 출처).

- **2026-07-10 (75)**: **승률 what-if 슬라이더 + SAP 부분 대사 (레지스터 소진)**. (a) **FC-07 입찰 판단 = what-if**: 모듈 상세 제네릭 `sim` 필드 — 제안가 슬라이더(₩39.5–45.5M) → **승률(로짓)·마진·CVaR95(fat-tail)·기대 마진 실시간 재투영** + 판정 칩(단가 하한 미만/CVaR 음수=회피 fail-closed·승률<25%=조건부·그 외 추진) — 결정적 quant, 어느 모듈 행이든 sim 필드로 재사용. (b) **PO-119 = 부분 GR/IR 대사(SAP partial match — 리뷰 갭 소진)**: GR 22/44세트 · IR 입고분만 지급 승인·잔여 미결 match 칩.

- **2026-07-10 (76)**: **병렬 레인 방식 채택 + 레인 A 1차** — 턴마다 독립 레지스터 항목을 레인으로 병행(배치 recon → 배치 edit). **레인 A(창 모델)**: 팔레트에 **개체 코드 검색** 합류(`searchResults` objRows — 전 그래프 코드·타입 매칭, 결과=개체 카드 오픈: 핀 분할·자유 플로트·트레이 = 기존 창 모델 §4-23) — 팔레트→개체→창 모델 경로 완성. 레인 B(정비 WO-2638 오더 flow 확장·재고 이동 문서 추가 행)·레인 C(typed Cedar 실평가)는 다음 턴 계속. + (75) 승률 what-if·부분 대사 검증 통과.

- **2026-07-10 (77)**: **팬아웃 — 레인 B·C 완료**. **레인 B(ERP)**: WO-2638 예방정비 오더 사이클 flow(접수→계획·부품 예약[PO-118]→실행 cur→정산→전표[VC-2604]) · 재고 IV-054 이동 문서 tl(입고 +800 IN-0620·출고 −180 6월·예정 −180 7월 점선 + 「현재고 산출 = 이동 문서 합계」). **레인 C(typed Cedar 실평가)**: `polSim` = 표본 접근 이벤트 5건에 principal·resource·조건(관리 기기·업무시간) **술어 실평가** — 「대상 N건 → 허용/금지 M건 · 조건 미충족 = 거부(fail-closed)」 실계산(장식 시뮬 제거 §4-20). 레인 A(팔레트 개체 검색=창 모델)는 (76) 완료.

- **2026-07-10 (78)**: **정비 오더 사이클 전 행 완결 + 창 모델 정밀 조사**. WO-2641 flow(접수→계획·부품 예약 cur→실행→정산→전표) — 정비 3행 전부 오더 사이클 스텝퍼. CARD_META 정밀 조사: 등재 화면 = hr·review·att·pay 4종뿐 — leave·benefit·docs·appr 섹션 카드 존 소급 = 다음 세션 첫 슬라이스(leave 참조 구현 → 패턴 확정, TODO §4-23 register에 정밀 계획).

- **2026-07-10 (79)**: **20레인 팬아웃 1차**. 레인 19: `ONT_TYPES`/`ONTOLOGY_GRAPH` 참조 동등성 메모이제이션(렌더당 다중 재계산 제거 — `_ontTypesMemo`/`_ogMemo`, 스키마 편집=참조 교체로 자동 무효화). 레인 18: 설정 화면 MOBILE_SCR 합류. 레인 4: C-D 건별 계약 수령 → **SR-206 대근비 정산 회차 반영** 감사 체인(§4-15). 레인 12: **오버라이드 자기승인 차단**(요청자=승인자 → forbid 감사·fail-closed four-eyes). 레인 13·14 판정 완료(신규 표면 감사 커버리지·비기능 장식 0). 판정·잔여 = TODO 「10 레인 계획」 헤더. 다음 라운드 = 레인 1(leave 카드 존)·3·5·6.

- **2026-07-10 (80)**: **폐루프 리뷰 프로토콜 헌장화 (directive)** — DESIGN §4-25(6문 폐루프: 최선 패턴·워크플로/엣지 커버리지·마찰/시선 흐름·도메인 벤치마크·통합/재사용·**목업 독립성**) + AGENTS §4 관례 + README WORKING PROTOCOL 등재. 운영 방식 = 증분·체계적·철저한 개선 루프, 만족까지 검증 반복. 목업 독립성 판정 기준: 전 데이터 = state 파생 or UI 생성 경로 — 스텁·하드코딩-전용=갭 등재·마감, 백엔드 필요=HANDOFF 계약. 다음 팬아웃부터 이 프로토콜로 페이지 순회(레인 11과 통합).

- **2026-07-10 (81)**: **폐루프 프로토콜 확장 + SLO≠SLA 분리 (directives)**. (a) §4-25에 **⑦ 페르소나·유저 스토리 커버리지**(모듈별 관여 페르소나 명명·스토리 전수·UI 직관 동선 actionable 검증 — view-as e2e) 추가 · 연차 1차 답변: 본인/팀장/HR/관리자 커버 ✓, 갭 등재(본인 self-scope 뷰·반차/이월/월할 엣지·승인→대근 직행). (b) **§4-26 SLO≠SLA**: SLA=계약 대외 약정(계약·현장 유지) · SLO=내부 운영 목표 — 지원 티켓 표면 전체 SLO로 정정(접수 모달·타이머·스탯·시드 kv), 둘 다 설정 개체로 구성 가능(HO-01 문법) = 레지스터. (c) README/AGENTS 관례 프로토콜 등재(80 후속).

- **2026-07-10 (82)**: **대근 필수 포지션 사전 트리거 (directive)** — `lvDecide` 승인 훅: 대상자 포지션 = 상주 커버 필수(경비·미화·시설·기사·정비 판정)면 **승인 시점(사전)에** 「대근 필요 · 날짜」 알림(반장·HR 큐)+감사(자동화 규칙 후보 명시)+토스트 — 연차 리드타임만큼 선행 계획 확보. 사전 계획 완결 잔여(커버 플래너 포워드 큐·subOpen 미래 일자·주간 커버 점검 예약·반차/연속/휴직/최소 인원 게이트 엣지) = TODO 「10 레인」 헤더 등재. 지원 티켓 SLO 분리(81)와 함께 로드 클린.

- **2026-07-10 (83)**: **레인 8·15 완료 (팬아웃 2차)**. 레인 8: VIEWERS +v8 한미정(급여 전담 — pay·finance·laborcost·dashboard 스코프)·+v9 오세린(컴플라이언스 전담 — compliance·audit·policy·explore·ingest 스코프) — deny-by-omission e2e. 레인 15: **필터 프리셋 저장**(Palantir saved exploration 갭 소진) — 구성 스트립 「+ 필터 저장」=현재 modQ 저장(빈 필터 fail-closed), 프리셋 칩 클릭=적용·×=제거, 개인 뷰 감사. ⚠ 편집 사고 1건 자가 발견·복구: VIEWERS 치환 시 truncated old_string으로 잔재 글루 발생 → 즉시 read 확인·제거 (교훈 재확인: grep 출력 잘린 텍스트를 old_string으로 쓰지 말 것).

- **2026-07-10 (84)**: **레인 16 완료 (팬아웃 3차)** — **사용자 시리즈 값 입력**(스키마 심화 잔여 소진): 사용자 시리즈(SR-21x) 카드 인스턴스 바에 값 인라인 입력(`seriesValSet` — 숫자 파싱→바 pct·추세 문구 재계산·감사·초안 직행 ③), 시드 시리즈=읽기 유지. + 차트 y축 거터 44→30px(코멘트 — 시각 센터링).

- **2026-07-10 (85)**: **팬아웃 4차 — 레인 20·12·6·11 + 프로토콜 ⑧**. 레인 20 ✓: 모듈 빈 상태 = 액션형(「필터 해제 — 전체 목록」 + 주 액션 CTA — §4-22 2-스토리). 레인 12 잔여 판정 ✓: egress 우회 스윕 — 드래그 토큰=코드만(내용 비반출)·팔레트=personVisible/cedar 게이트·메일 egress·문서 내보내기 게이트 기존 — 클립보드/화면 캡처=계층3 배포 요건(HANDOFF §13) 유지, 앱 내 신규 우회 경로 0. 레인 6 판정 ✓(부분): 인제스트 적재·자동화 실행 = 화면 cedar 게이트+전량 감사로 ①⑥ 충족 — 체크리스트·four-eyes(②③④)는 API 계층 계약(HANDOFF §16), UI 잔여 없음 판정. 레인 11 부분: 대시보드·예측(3문+⑧ 레이아웃 렌즈 — y축 거터 센터링·슬롯 프리셋으로 대응 완료), 전 화면 순회는 상시 프로토콜로 지속. **DESIGN §4-25 ⑧ 레이아웃·마진 렌즈** 추가(directive). **잔여 대형 레인(1·2·3·5·7·9·10·17) = 새 세션 수술 대상** — 레인 1(leave 카드 존)부터, 정밀 계획은 TODO 레지스터.

- **2026-07-10 (86)**: **콘솔 UI·레이아웃 변경 = 정책 스코프 (directive)** — `can()`에 `console:configure`(내부 임직원만 — 외부 v6 제외)·`console:deploy`(민감정보+ 등급) 신설. 모듈·대시보드 구성 기어 = configure 스코프 sc-if(deny-by-omission 숨김), 배포 제안(`modCfgDeploy`·대시보드 onDeploy) = deploy 스코프 fail-closed 차단+forbid 감사. Cedar 단일 chokepoint(§4-19) 유지.

- **2026-07-10 (87)**: **DLP 클라이언트 하드닝 (directive)** — `componentDidMount`: 복사·잘라내기 억제(`onCopyGuard` — 입력 필드 편집은 무마찰 UX 예외, 그 외 clipboard 억제·[반출 통제 대상] 스텁 텍스트·forbid 감사·토스트) + PrintScreen 감지·인쇄(⌘/Ctrl+P) 억제(`onCaptureGuard` — forbid 감사·워터마크 추적 안내). 웹 원천차단 불가 항목(화면 캡처·devtools 소스 뷰·네트워크 스푸핑)은 HANDOFF §13.1 계층3(엔터프라이즈 브라우저·VDI/RBI·MDM·서버측 무결성·EME식 하드웨어 경로) 배포 요건으로 명시 — 앱은 억제+추적+게이트, 완전차단 약속 금지(보안극장 방지). unmount 리스너 정리. UX 원칙: 단계적 개입(승인 개체 일상 열람 무마찰).

- **2026-07-10 (88)**: **분석·감시 병합 (레인 잔여)** — 경영 분석(laborcost) 헤더 액션 「대시보드」 → 「감시 규칙 만들기」(빌더 열기 — Foundry Rules/Automate 병합: 분석 화면에서 임계 감시 규칙 직행 저작). 분석=관찰+감시+자동화 한 몸 원칙 반영.

- **2026-07-10 (89)**: **DLP 하드닝 2차 (directive)** — 우클릭 = 콘솔 컨텍스트 대체(`onCtxGuard` — 브라우저 기본 메뉴 억제·입력 필드 예외·개체 우클릭 빠른 액션은 카드/행 메뉴)·devtools 단축키 억제+감사(`onDevtoolsGuard` — F12·⌘⌥I/J/C·⌘U 소스 보기 forbid 감사). unmount 정리. 정직성 유지: 웹 원천차단 불가(devtools·네트워크 스푸핑·화면 캡처) = 계층1 억제·감사만, 완전 방어=계층3(HANDOFF §13.1). 서버 과다 전송 금지 원칙(정책 평가 후 전송) 백엔드 계약 명시.

> **다음 (우선순위 · 단일 출처)**: **TODO.md ▶ 실행 큐** 참조 — 완료: 레인 1·2·3·5·7·9·10·16·17 + #11 퇴사·휴직 + 체크인 심화 + §18.2 + 커버 플래너 (AGENTS 91–101, 2026-07-10). **남은 순서**: §4-22/23 전수 audit(실행 큐 12) → 진행 중 [~] 13 잔여 엣지(반차 부분 커버·팀 최소 인원 게이트)·14(what-if 소급)·15(온톨로지 엔진 ⓐⓑ)·17(대시보드 구성 확장)·18(벤치마크 갭 — Gmail 예약 발송·reply-in-thread 등) · 대형 에픽 19–23. **상시 프로토콜**: §4-25 8문 폐루프 페이지 순회 + §4-21 3문 벤치마크 → 벤치마크 갭 레지스터. (ROADMAP §6·§8)
