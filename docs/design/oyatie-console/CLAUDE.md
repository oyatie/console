# 작업 지침
- 이 프로젝트의 모든 설계 결정은 루트의 **DESIGN.md**(온톨로지 우선 헌장)를 따른다. 새 화면/컴포넌트/텍스트를 만들거나 수정하기 전에 DESIGN.md의 "UI/UX 불변식 audit 체크리스트"(§4)와 §4.7 패턴 전파 규칙을 적용할 것.
- 작업 목록은 루트의 **TODO.md**를 단일 출처로 유지한다 — 완료 시 체크, 새 directive는 TODO.md와 DESIGN.md 카탈로그에 함께 반영.

## 핵심 원칙 (요약 — 상세는 DESIGN.md)
- **모든 것이 개체(Object)다**: 사람·조직·문서·계약뿐 아니라 **액션·이벤트·현상**(결재·열람·무단결근·결혼·계약체결)도 개체. 온톨로지 3계층 = 의미(정체성)·동작(이벤트/전이)·역학(정책·자동화·파생분석)이 한 개체에 겹쳐 있다.
- **개체 체인**: 계약(C-) → 포지션(정책 프리셋) → 공고 → 지원자 → 직원 → 타임테이블/근태 → 연장근로 → 급여 → 분석 → 계약 수익성 환류. 새 기능은 이 체인의 상·하류 연결을 반드시 제공.
- **콘솔 전역 Cedar PBAC**: 화면·카드·행·액션·검색·집계 전부 정책 평가 결과. "전체"=인가 법인 합집합. 권한 있어도 열람은 기록. 비밀 구역은 deny-by-omission.
- **no-code 우선 · 비주얼 · 개체 기반**: 정책·워크플로·자동화·예약작업 전부 자연어/블록 캔버스 + 시뮬레이션·되돌리기.
- **깊이 우선(deep not wide), 예외 우선, 설명용 UI 금지**: 상태=칩, 기능 없는 텍스트 금지. 상세 보기 기본은 핀 패널.
- **전 직원 시스템 + 모바일**: 관리자 전용이 아님. 셀프서비스(본인 급여·개인 inbox)·passkey 수령확인. 커뮤니케이션은 rail↔main 승격.

## 벤치마크 (모듈별 best-in-class)
- 근태·급여·연차·복리후생 = Workday/Monday · 메신저 = Slack · 메일 = Gmail · 채용 = Greenhouse/Lever/Ashby · 감사로그 = CloudTrail/Splunk · 자동화 = Workato/ServiceNow Flow · 예약작업 = Airflow/Temporal · 객체 탐색·정책·워크플로 = Palantir Foundry. 전반 벤치마크 = Palantir.

## 다음 우선순위 (TODO 참조)
개인 Inbox(#35) → 연차촉진·노무수령거부권 결재화(#36) → passkey 열람(#37) → 커뮤니케이션 모듈 승격(#41) → 토큰 문법(#33) → 객체 탐색(#42) → 감사로그(#38) → 자동화·예약작업(#39·#40).
