"# Ultra-Review Synthesis — W1 backend (`d379f5a0`) + W2 web (`ced3ea3e`)\n\nSix review chunks (engine-core, finance-payroll, migrations-rls, fe-security, fe-surfaces, tests-quality) + W0 routing re-exam. Deduped across chunks, ranked by (severity, blast-radius). Overlaps merged are noted inline.\n\n---\n\n## mustFix\n\n**M1 — §16 four-eyes approval is unbound and replayable (systemic authz bypass).**\n`backend/crates/governance/adapter-postgres/src/lib.rs:478` (`four_eyes_approved_conn`), consumed by `backend/app/src/workflow_studio.rs:453` (publish_definition, trigger_definition_run), `backend/crates/ontology/rest/src/lib.rs:830` (instance_revision), and now the newly-wired projected dispatch.\nThe gate resolves approval with only `SELECT decision FROM gov_approvals WHERE request_ref=$1` — no binding to action *kind*, to the *requester/publisher*, or to the *target object*, and the ref is never consumed (single-use).\n*Scenario:* Publisher P passes `four_eyes_request_ref` = any already-approved `gov_approvals` row in P's org (a leave approval, an evidence-hold release, an unrelated/older workflow — the in-DB `approver_id<>requested_by` CHECK is satisfied by that row's own requester). Gate returns `approved`, publish/run/dispatch proceeds. Same ref replays across publish AND run AND repeats. *Merged from:* migrations-rls own#2 + tests-quality must#1 + the reachable half of engine-core#2. **Fix:** bind `request_ref` to the definition/target id and the gate's caller, verify the approval-request subject, and mark it consumed.\n*Rider (tests-quality must#2, `backend/app/tests/workflow_four_eyes_publish.rs:850`):* the gate tests only assert present→allow / absent→deny, so they green-light this hole — every fix needs a wrong-binding + replay + cross-kind negative.\n\n**M2 — Finance-GL has no four-eyes / SoD on voucher approval.**\n`backend/crates/finance-gl/adapter-postgres/src/lib.rs:406-452` (`transition`/`approve`), `rest/src/lib.rs:203`, migration `0160` (no `approved_by` column, no approver≠requester constraint).\n`approve()` never compares actor to `created_by`; there is no column to even record a distinct approver.\n*Scenario:* one user with `PeriodLockManage` and a single JWT runs 기표→제출→승인→전기→역분개 end-to-end — full self-approval of financial vouchers. The finance-gl FSM test itself drives submit→approve→post with one `actor`, encoding the gap. **Fix:** immutable `approved_by`, bind actor at approval, DB CHECK `approved_by IS NOT NULL AND approved_by<>created_by` for APPROVED/POSTED. (PeriodLockManage granularity in S6 compounds this.)\n\n**M3 — Evidence custody transfer & disposal are fabricated client-side.**\n`web/src/console/evidence/EvidenceCard.tsx:338` (`submitTransfer`), `~409` (`requestDisposal`).\nBoth mutate `custodian`/`disposed` in local state with **no server call** and prepend synthetic \"staged\" custody events into the chain-of-custody timeline (rendered `hashVerified=true` when fixity===VERIFIED). Directly violates §4-25-⑥ and this file's own header claim (\"the timeline is never client-synthesized\").\n*Scenario:* a SUPER_ADMIN types \"Alice\" + Transfer (or clicks Dispose) → UI shows custodian=Alice / DISPOSED chip / a new custody-transition event as though sealed; a compliance officer screenshots fabricated legal custody/disposal state that silently reverts on next refetch. **Fix:** hide/disable the un-wired buttons until the custody/disposal REST exists — do not fake state.\n\n---\n\n## shouldFix\n(ranked; sibling locations folded per item)\n\n**S1 — Deny-by-omission object-row PBAC not wired (ontology).** `backend/crates/ontology/rest/src/lib.rs:319` calls the *unfiltered* `PgInstanceStore::list_instances`; the Cedar §5d residual `list_instances_filtered` (`adapter-postgres/src/instances.rs:473`) has **no production caller**. Admin-gated today, but the \"list must apply the residual\" invariant is unmet and becomes optimistic-allow at row granularity the moment per-endpoint object-policies go live. *Caveat (engine-core own#2):* the filtered path hard-codes the owned-store JOIN and does **not** dispatch on `backing_kind`, so wiring it naively returns `[]` for every projected type — it must grow the same dispatch first.\n\n**S2 — Live evidence surface uses an optimistic client JWT-role gate; the real bulk gate is dead.** `web/src/console/policy/PolicyGated.tsx:176` — `authorizeBulk.ts` + `BulkPolicyGateProvider` are built + unit-tested but mounted **nowhere**; `IntegrityPage` gates on `roles.includes('SUPER_ADMIN')`, not the deny-by-omission server path. Server re-checks each mutation (no data bypass), but the invariant is unmet. *Folds fe-security own#1:* the dormant `subjectFingerprint()` (`authorizeBulk.ts:41`) joins roles with `','` and `cacheKey` glues fields with no separator → role-set collision (`['admin','auditor']` ≡ configurable role `'admin,auditor'`) = latent authz-cache poisoning once wired.\n\n**S3 — Client four-eyes self-decide hint fails OPEN on unknown identity.** `web/src/console/evidence/EvidenceCard.tsx:418` and `web/src/console/leave/LeaveConsole.tsx:744,316` — `selfDecide` is false when `currentUserId/selfUserId` is undefined, so Approve/Reject render to the requester under a view-as/legacy token (where `user_id` is absent). Backend enforces on the stored requester, so defense-in-depth only. **Fix:** treat unknown identity as `selfDecide=true`. *Merged two files.*\n\n**S4 — Projected-dispatch gate not re-checked inside the domain write tx (TOCTOU).** `backend/crates/ontology/rest/src/lib.rs:586-640` + `backend/app/src/lib.rs:287`. The gate chain is evaluated once pre-dispatch; the registry handler (`registry.update_equipment`, now the first live projected handler) opens its own tx without re-reading four-eyes/authority — unlike `instance_revision` which re-checks inside `with_audits`. Latent until a projected action is configured with `four_eyes`, but reachable now. (Distinct from M1: this is in-tx re-check, M1 is ref-binding.)\n\n**S5 — Governance API parsing fails OPEN.** `web/src/api/governance.ts:79` coerces missing `request_ref/requested_by` to `''` and any non-`approved` decision to `'rejected'`; `:163` `configureLifecycleTransition` substitutes client-requested `from/to_state` and defaults `requires_*` to false on a malformed 200 → an actually-gated transition renders as un-gated. **Fix:** throw on absent identifiers / unrecognized enums; fail closed on missing response fields.\n\n**S6 — `PeriodLockManage` is a coarse capability spanning create→draft→approve→post→reverse.** `backend/crates/finance-gl/rest/src/lib.rs:29`. Structurally blocks the preparer/approver split (compounds M2). Not deny-by-default breakage — deny-default holds — but wrong granularity. Registered as deferred `Feature::VoucherManage` split. **Fix:** split into VoucherDraft/Approve/Post.\n\n**S7 — Hand-keyed voucher accepts unverified source provenance.** `backend/crates/finance-gl/rest/src/lib.rs:111-113` (+ application:23-27) accepts an arbitrary `VoucherSourceRef{object_type,object_id}` with no existence/tenant/type check (free-string type). *Scenario:* preparer sets `source={object_type:'expense_approval', object_id:'NONEXISTENT'}`; a reviewer drilling voucher→source sees a fabricated \"derived from approved expense\" linkage. **Fix:** restrict source-setting to the trusted derive path or resolve+verify an org-owned object in-tx.\n\n**S8 — Fixity/TSA chips fail OPEN to VERIFIED.** `web/src/console/evidence/evidenceApi.ts:1346/1335` — `aggregateFixity([])→'VERIFIED'`; any unknown `worm_status`/TSA status falls through to VERIFIED. *Scenario:* evidence registered but original copy not yet uploaded (`copies=[]`) → green VERIFIED chip asserting integrity for content that does not exist. **Fix:** empty/unknown → PENDING/indeterminate.\n\n**S9 — Cross-tenant branch reference (single-column FK).** migration `0160:25` — `branch_id REFERENCES branches(id)` while every sibling FK is tenant-coupled composite; `branches` already carries `UNIQUE(id,org_id)`. *Scenario:* tenant A inserts a voucher referencing tenant B's branch; voucher RLS passes on `org_id=A`, a cross-tenant branch ref persists. **Fix:** `(branch_id, org_id) REFERENCES branches(id, org_id)`.\n\n**S10 — Quant unbounded compute DoS + non-finite results returned as success.** `backend/crates/analytics-quant/service/src/lib.rs:284-294` runs ~36.5M transcendental iterations **synchronously on a Tokio worker** (no `spawn_blocking`), no `MAX_SERIES_LEN`, no per-tenant admission; `rest:67-81` runs it in-handler. Separately (`service:167-185`) derived drift/sigma/terminals aren't finiteness-checked, so finite inputs can overflow to inf/NaN and the endpoint returns HTTP 200 `{\"point_estimate\":null,...}` — fabricated projection as success. **Fix:** cap series length, move to bounded `spawn_blocking` behind a semaphore, reject non-finite derived values.\n\n**S11 — Fail-open on unknown enum/state (surface cluster).** `web/src/console/leave/model.ts:128-138` (`ledgerStatus`→'정상' for unknown tone); `web/src/console/configconsole/widgets.tsx:134-144` (DistCard unknown lifecycle → blank label + live drill); `web/src/console/modules/moduleScreens.ts:676-683` (`getModuleScreen(unknown)` → **defaults to the finance module**, still PBAC-gated so no data bypass). **Fix:** neutral/unknown fail-closed state; not-found for unknown screen.\n\n**S12 — Config editor: audit reason dropped, no rehydration, duplicate writes, unapproved team view.** `web/src/console/configconsole/DashboardEditor.tsx` — mandatory save reason never passed to the action params (`:594-610`, audit reason lost); saved `console_view` never rehydrated (`:499`, `fetchConsoleViews` is dead code) → each session's first save creates a fresh instance (duplicate accumulation); `handleSave` has no in-flight guard (double-click → two `create`s); team deploy writes the `scope='team'` instance **before** governance approval, non-atomically (`:566-592`) → readable unapproved team layout if the approval POST fails. **Fix:** thread the reason, hydrate on mount, in-flight guard, order approval before the team write.\n\n**S13 — Forecast honesty (statistical + failure masking).** `web/src/console/forecast/ForecastScreen.tsx:202-270` — lifecycle-cost fetch failure is indistinguishable from \"no history\" (renders 'insufficient sample', violating §4-10 fail-closed); `:202-203` no `equipment_id` guard → stale cross-render cost shown under the new equipment's identity; `series.ts:41-52` drops zero-cost months and counts partial boundary months full (a \"3-month\" horizon can yield 4 buckets), biasing EWMA/CI95/CVaR; and (own-finding, adjacent `charts/ProjectionPanel.tsx`) `n=1` → sigma=0 → a zero-width CI95 band presented as a real Student-t 95% band. **Fix:** distinguish fetch error from empty, guard equipment identity, honest bucketing, require a minimum n before rendering CI/CVaR.\n\n**S14 — Finance FE model diverges from the shipped backend FSM.** `web/src/console/finance/financeApi.ts` header cites a non-existent source file; `financeModel.ts:124-192` models a split `lifecycle_state/posting_status/validation_status` triple whose values don't exist in the real single `VoucherStatus` enum (DRAFT→BALANCE_CHECKED→APPROVED→POSTED→REVERSED). `canPost(draft→post)` contradicts the FSM (only Approved→Posted). The \"wiring is a swap, no rework\" claim is false — every field mismatches on wire; a draft's Post button would 409. Also: stepper marks 'post' blocked while `rowActions` renders the Post button (`:128-159`); `EXCEPTION_STATUSES` omits `period_locked` (`:288`); `manual` source mislabeled 'dx' (`:78-105`); `sourceValue` id falls back to the kind string (`:66-76`). **Fix:** re-model against `finance-gl/domain/src/lib.rs` before wiring.\n\n**S15 — Voucher line count uncapped + N+1; VoucherComposeForm cancel doesn't abort.** `backend/crates/finance-gl/adapter-postgres/src/lib.rs:372-398` sequential per-line INSERT in the open audit tx (uncapped Vec), `list` does N+1 `load_summary` (`:269-273`). `web/src/console/finance/VoucherComposeForm.tsx:114-128` leaves Cancel enabled during an in-flight create with no AbortController → the POST still creates the voucher after the form closes. **Fix:** cap + bulk-insert, batch summaries, abort/disable on submit.\n\n**Lower should-fix (one line each):**\n- **S16** — Info disclosure: `from_kernel(ErrorKind::Internal)` returns `err.message` verbatim in the 500 body (`backend/crates/payroll/rest/src/lib.rs:267` + `finance-gl/rest/src/lib.rs:327`) — branch-scope/policy diagnostics reach the client. Map all Internal to a generic message.\n- **S17** — Notices DB backstops: no `enforce_org_id_immutable` trigger on `notices`/`notice_receipts` (migration `0162:38`, outlier vs 0160/0158); publication state unconstrained — a `status='published'` row with `code=NULL, published_at=NULL` is accepted (`0162:23`). Add the org-immutable trigger + `CHECK((status='published')=(code IS NOT NULL AND published_at IS NOT NULL))`.\n- **S18** — Finance DB header over-claims: no BEFORE-INSERT guard (a direct `INSERT status='POSTED'` with zero lines yields a permanent immutable voucher, `0160:116`); POSTED→REVERSED permits any column change with no contra linkage (`0160:85`). Not app-reachable today, but the advertised DB backstop has holes.\n- **S19** — Leave aggregate stats (headcount/remaining/burn-rate) render **un-gated** (`web/src/console/leave/LeaveConsole.tsx:523-540`) while the ledger table they summarize is `PolicyGated`. Wrap in the same gate.\n- **S20** — `runVerify` leaves stale MATCH chips on error (`web/src/console/evidence/EvidenceCard.tsx:321` — only sets verdicts on 'verified'|'failed', not 'unavailable'/throw). `collectedAt` = registration `created_at` shown under a \"수집/collected at\" label (`evidenceApi.ts:1363`) — legally material; relabel \"registered at\".\n- **S21** — `payroll /payslips/me` returns pre-calc readiness rows (no won amount), not an issued payslip (`payroll/rest/src/lib.rs:56,94`) — a client will misrender staging as a pay document. Rename the route.\n- **S22** — Leave `runDecide/sendPromotion` have no try/finally (`LeaveConsole.tsx:364-375`) → a transport throw leaves the row's buttons permanently disabled + unhandled rejection.\n- **S23** — Projected instances are listable but not resolvable: `get_current/get_as_of/history/traverse` query only `ont_instances` (`adapter-postgres/src/instances.rs`), so GET/history 404 for projected ids; list also has a hard `LIMIT 200` with no pagination — >200 rows silently dropped.\n- **S24** — a11y: voucher line inputs share identical accessible names across rows (`VoucherComposeForm.tsx:171-221`); trend sparkline bars all `aria-hidden` with no text value (`configconsole/widgets.tsx:226-244`).\n\n---\n\n## niceToHave\n(compressed by theme — full detail in source chunks)\n\n- **DB defense-in-depth backstops (migration 0160/0161/0162):** trigger doesn't enforce FSM edge-ordering (state-skips at pure-DB level); no partial-UNIQUE on `reversal_of_voucher_id` (two contras can claim one reversal) + no self-ref guard; `posted_at` not coupled to status; balance accumulators declared BIGINT but `SUM(bigint)→numeric` can overflow a legitimately-balanced voucher; header over-claims org-immutable on the lines table (harmless — lines have REVOKE UPDATE); `notice_receipts` broad UPDATE grant (no column immutability on the ack record); `0161` `resolved_at`/`resolved_by` half-resolved — the correct constraint is **asymmetric** `CHECK(resolved_by IS NULL OR resolved_at IS NOT NULL)` (codex's symmetric version is wrong — system sweeps legitimately set `resolved_at` with `resolved_by` NULL); `0161` `kind` CHECK trims but stores untrimmed.\n- **Projected read fidelity:** every attribute coerced to a JSON string via `::TEXT` (typed clients get `\"false\"`, `\"10\"`); `primary_key_property`/`title_property_key` used directly as SQL columns (breaks for an authored type where key≠backing_column); dispatch routes purely on `dispatch_target` with no target↔object_type binding check. All safe for seeded types today.\n- **fk_link cardinality inverted** (`seed.rs:155-161`): all 8 FK links authored `OneMany` on the child = inverse of reality; display-only (create_link enforces no cardinality). Add a `ManyOne` variant or author on the parent.\n- **No-code auto-attach can 409 a publish** (`adapter-postgres/src/lib.rs` + `0152` UNIQUE(object_type_id,stable_key)) — a same-keyed non-`instance_revision` action named 'create' makes publish fail with 23505. Guard on stable_key existence.\n- **FE inert/stub states:** `GenericModuleScreen.tsx:1062-1067` enabled handler-less primaryAction (only without a session); `:1193-1196` live-empty hint shown when `api` absent; synthesized `OT-`/`FC-` object-code-shaped chips for non-objects (`typeRegistry.ts:354-370`, `series.ts:62-64`); leave `pushResults` Map keyed by employeeId overwrites round-1 with round-2; `PolicyGate.tsx:29` dead divergent-context duplicate (DENY_ALL) — delete before someone imports it; `clearAuthorizeBulkCache` has zero non-test callers despite its \"called on logout\" doc.\n- **Minor authz hygiene:** fetch-before-authorize existence oracle in finance-gl (403 vs 404, intra-org, v4 UUIDs); `ProjectionBody` missing `deny_unknown_fields`; payroll unbounded OFFSET pagination.\n- **Test hardening:** payroll RLS test doesn't assert `relforcerowsecurity` and seeds indistinguishable Alice/Bob fields (a \"wrong single row\" regression could false-green).\n\n---\n\n## w0Verdict\n\n**Routing/model-allocation brief HOLDS.** All load-bearing tables reproduce from the re-fetched live artifacts (`leaderboard-live.json`, 113 tasks, generated 2026-07-09T20:23Z) to rounding: the top-3 statistical tie (sol-max/fable-xhigh/terra-max, overlapping CIs), the efficiency frontier (sol[high] 69.4%/$2.98/32 steps ≈ fable-max accuracy at ~1/6 cost / 40% steps), and the full per-language routing table (fable-5 beats every *measurable* codex model — gpt-5.4, gpt-5.5 — in all 5 languages). Both stated caveats are factually confirmed: the gpt-5.6 leaders (sol/terra/luna) are **absent** from the per-language heatmap (only 8 models broken out), and rust/js sample sizes are n=5 tasks.\n\nThe 4 rescinds and the FE-dynamic-grammar keep (TS, n=35, robust) are sound. The 3 Rust rescinds are correctly framed as risk-managed defaults, **not** \"data proves Claude>sol on Rust\" (impossible — sol is absent and ties fable within CI at top-line). The one conditional-keep (FE-policygate-bulk: codex-implements-under-a-Claude-correctness-backstop) is defensible but has two soft spots: its \"~parity\" premise extrapolates a top-line number to a TS lane, and on a security lane the Claude gate is mandatory anyway (codex-first adds a pass rather than replacing one).\n\n**\"Claude implements / codex reviews\" is only half evidence-backed.** DeepSWE measures *implementation*, so \"Claude implements correctness-critical lanes\" is directly supported. It measures **nothing** about review/critique quality, so \"codex reviews\" is out-of-distribution — a defensible cost-rational policy (decorrelated second opinion; sol[high] $2.98 vs fable[max] $19.23; existing cross-model review-gate discipline), but the benchmark can neither confirm nor refute it.\n\nDiscrepancies are confined to non-routing bottom-tier aux columns (gemini-3.1-pro/sonnet-4.6/kimi cost/steps/tokens read as a staler snapshot; pass@4 blanked in the brief but present live) and loose-but-non-binding rollout-count phrasing. **No verdict impact.**\n\n---\n\n## coverageGaps\n(implied by the invariants; examined by NO chunk)\n\n1. **Android/mobile was reviewed by zero chunks** — yet the git diff carries extensive changes: `PasskeyAuthRepository`, `SessionTokenStore`, `MessengerModels`, `MobileOperationsRepository`, `WorkOrderFlowTest`, `LoginStateMachine`. The same invariants (no-fabrication, authz, session-token handling) apply to the native client and its E2E path. Entirely unaudited.\n\n2. **The four-eyes binding hole (M1) was never swept across ALL consumers.** Reviews found it in workflow_studio and ontology; no chunk enumerated *every* caller of `four_eyes_approved_conn` (leave promotion, evidence-hold release, finance if it grows one) to confirm none of them bind `request_ref` to a subject. The shared helper is the root cause — a caller inventory is the actual fix boundary.\n\n3. **The `view_as` (impersonation) read-only wall vs the 4 new mutation routers.** migrations-rls *observed* the new routers sit inside the view-as wrap but no chunk verified a view-as token cannot reach finance voucher post / notice publish / payroll writes — and the FE four-eyes fail-open (S3) is specifically the view-as/legacy-token case, so this seam is load-bearing and untested.\n\n4. **The `notices` REST/application crate is unexamined.** Only migration `0162` (DB layer) was reviewed, and it has unconstrained publication state (S17), which means the app crate is the *sole* enforcer of the durable-document + receipt-acknowledgment contract — and nobody read it. Pair with: does `fetchConsoleViews` actually filter unapproved team views (the backend side of S12)?\n\n5. **The FE↔BE contract seam for the new surfaces.** No chunk read the regenerated `clients/{ts,kotlin,swift}` or confirmed `openapi.yaml` actually describes finance-gl's real `VoucherStatus` enum and payroll's readiness-row shape. This matters precisely because fe-surfaces found the FE finance model (S14) diverges from the backend FSM — the drift gate proves the *spec* matches routes, not that the spec matches the *domain*, and not that the FE consumes it correctly.\n\n6. **The deferred `posting→employee` link forward-ref (`POSTING_EMPLOYEE_LINK_KEY`).** engine-core flagged it as a tracked deferral and explicitly said \"verify the backfill lane actually resolves it before that chain is claimed traversable.\" No chunk verified the backfill lane runs — an open loop that leaves the HR object-graph non-traversable until proven."
  }