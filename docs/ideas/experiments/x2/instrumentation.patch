diff --git a/backend/crates/ontology/rest/tests/object_policy_attach_as_runtime_role.rs b/backend/crates/ontology/rest/tests/object_policy_attach_as_runtime_role.rs
index fe99f844d..24c3060c4 100644
--- a/backend/crates/ontology/rest/tests/object_policy_attach_as_runtime_role.rs
+++ b/backend/crates/ontology/rest/tests/object_policy_attach_as_runtime_role.rs
@@ -69,9 +69,25 @@ async fn an_attached_permit_is_the_only_thing_that_makes_instances_visible(owner
     // RED control for the assertion below AND the deny-by-default assertion in
     // its own right: with no policy attached the list is empty, so a list that
     // later returns rows can only be the attached permit's doing.
+    let (who, is_super): (String, bool) = fx
+        .as_runtime_role(|tx| {
+            Box::pin(async move {
+                sqlx::query_as(
+                    "SELECT current_user::text, \
+                     (SELECT rolsuper FROM pg_roles WHERE rolname = current_user)",
+                )
+                .fetch_one(&mut **tx)
+                .await
+                .unwrap()
+            })
+        })
+        .await;
+    println!("X2 CONTROL 0  current_user={who} rolsuper={is_super}");
+
     let before = fx
         .get(&format!("/api/v1/ontology/instances?type={type_id}"))
         .await;
+    println!("X2 HALF 1     no policy attached -> {} {}", before.status, before.body);
     assert_eq!(before.status, StatusCode::OK, "{:?}", before.body);
     assert_eq!(
         before.body,
@@ -95,6 +111,15 @@ async fn an_attached_permit_is_the_only_thing_that_makes_instances_visible(owner
     let after = fx
         .get(&format!("/api/v1/ontology/instances?type={type_id}"))
         .await;
+    println!(
+        "X2 HALF 2     policy attached ({}) -> {} titles={:?}",
+        attached.status,
+        after.status,
+        after.body.as_array().map(|rows| rows
+            .iter()
+            .map(|row| row["instance"]["title"].clone())
+            .collect::<Vec<_>>())
+    );
     assert_eq!(after.status, StatusCode::OK, "{:?}", after.body);
     assert_instance_titles(&after.body, &["visible-to-owner"]);
     assert!(
@@ -129,6 +154,10 @@ async fn an_attached_permit_is_the_only_thing_that_makes_instances_visible(owner
     let bare_list = fx
         .get(&format!("/api/v1/ontology/instances?type={bare_id}"))
         .await;
+    println!(
+        "X2 HALF 1 bis  second unpoliced type in the SAME request -> {} {}",
+        bare_list.status, bare_list.body
+    );
     assert_eq!(bare_list.body, json!([]), "no policy, no rows");
     let bare_by_id = fx
         .get(&format!("/api/v1/ontology/instances/{bare_instance}"))
