> **QUARRY / NON-AUTHORITY.** Idea or draft only. Cannot dispatch work, clear HOLDs, or override product scope. Current authority: repository README + [`docs/current/PRODUCT.md`](../current/PRODUCT.md) / ROADMAP / DELIVERY.

# ADR / pattern collisions — Collide phase output, UNADJUDICATED

> `Status: RAW FINDINGS — the adversarial adjudication did not run (quota exhaustion 2026-07-29).`
>
> 75 collisions found by four independent angles: what the ADRs forbid, what the code resists,
> what the contracts break, and one angle attacking the desired patterns themselves.
>
> **These are claims, not verdicts.** Each was to be argued from three lenses (steelman the
> incumbent, steelman the pattern, argue both wrong) then judged. None of that ran. Do not treat
> any line here as a decision, and verify every citation before relying on it.

## BLOCKING (14)

### ADR-0021 forbids removing the legacy engine, which is exactly what P7 is

- **Patterns:** P7
- **Incumbent:** ADR-0021 Cedar PBAC authorization strangler — dual-engine coexistence modes (§6) and the no-live-switch clause (§8)
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/ADR-0021-cedar-pbac-authorization-strangler.md:61-64 and :70-72; enforced decision function at /Users/jasonlee/Developer/console/backend/crates/platform/authz/src/lib.rs:1141 (`feature.matrix_row()[role.matrix_index()]`)`
- **Collision:** ADR-0021:61-64 defines the only legal migration states as `legacy_only`, `cedar_shadow_legacy_enforce`, `cedar_enforce_legacy_compare`, `cedar_only`, and makes a missing map entry DENY; three of those four modes require a live legacy engine to run or to compare against. ADR-0021:70-72 states live routes remain on current authorization behavior until a later goal supplies contracts, tests, observability, shadow evidence and an explicit promotion decision. P7 deletes the compile-time Feature matrix, which IS the legacy engine: `permission_for` at lib.rs:1141 is the executable allow decision. Deleting it makes `legacy_only` and `cedar_enforce_legacy_compare` unimplementable and performs the live switch ADR-0021 reserves for a separate accepted decision. Failure mode if violated: every action not yet enrolled in cedar-pbac-coexistence-map.json has no engine at all, so either every unenrolled route hard-denies (product dead) or falls back to allow — the precise fail-open ADR-0021 was written to prevent. P7 cannot proceed without a new accepted ADR promoting Cedar to `cedar_only` for the whole surface.

### authorize() hard-denies any approver outside the raising row's branch set

- **Patterns:** P8, P5
- **Incumbent:** ADR-0003 branch-scoped authorization, realized by the branch precheck in `authorize` and `authorize_org_wide`
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/authz/src/lib.rs:1185-1189 (`if !principal.branch_scope.allows(resource_branch) { return Err(forbidden) }`, before any feature check) and :1158-1161 (built-in org-wide authority is `matches!(role, Role::SuperAdmin | Role::Executive)`); /Users/jasonlee/Developer/console/docs/decisions/ADR-0003-branchscoped-authorization-model-nonnull-branch-scope.md:20`
- **Collision:** P8 requires 결재 routing to resolve ABOVE, LATERALLY or BELOW the raising unit, with standing coming from the 결재권 graph rather than scope descent. P5 requires decision scope to be separable from the mandatory operational branch_id. lib.rs:1185 rejects the request before consulting any grant whenever the principal's `BranchScope` does not contain the resource's branch — a lateral approver is denied by construction, and `repository_filter` (lib.rs:1592-1605) additionally emits `FALSE` or `branch = ANY($1)` so the row is never even materialized. The only escape is `BranchScope::All`, which lib.rs:1158-1159 and ADR-0003:20 reserve to SUPER_ADMIN/EXECUTIVE. Demonstrated failure mode: a HQ 재무팀 approver who must sign a 지점's 품의 above an amount band is forbidden unless promoted to EXECUTIVE, which simultaneously grants org-wide read of every other branch's data — the opposite of 전담. Implementing P8/P5 makes ADR-0003's decision (branch scope is the authorization model, default-deny by branch set) false rather than merely extended.

### A control edge between two legal entities has no tenant it can live in

- **Patterns:** P4, P1, P13
- **Incumbent:** ADR-0021 §2 (RLS is the row boundary; Cedar cannot widen org_id) plus the org-scoped FORCE-RLS shape of the only generic edge store and the single-org audited write helper
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/ADR-0021-cedar-pbac-authorization-strangler.md:49-51; /Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0102_create_object_types_and_links.sql:55 (`org_id UUID NOT NULL`) and :76-80 (FORCE RLS `org_id = current_setting('app.current_org')` in both USING and WITH CHECK); /Users/jasonlee/Developer/console/backend/crates/platform/db/src/audit_tx.rs:111 and :121 (`with_audits(pool, org: OrgId, ...)` calls `set_current_org` unconditionally); /Users/jasonlee/Developer/console/docs/decisions/ADR-0018-clean-room-rust-corporate-workflow-engine.md:231-233`
- **Collision:** P4 needs an edge whose two endpoints are two different legal entities, readable and derivable from both sides, so that group designation is DERIVED. object_links rows carry exactly one org_id under FORCE RLS, so the same edge is invisible from the counterparty tenant; there is no second row that could be kept consistent because `with_audits` binds one org GUC per transaction (audit_tx.rs:121), so a two-org write cannot be one atomic audited mutation. ADR-0021:49-51 forbids widening org_id to fix this, and ADR-0018:231-233 prescribes the opposite topology — a parent envelope spawning child runs inside each participating org — as the sanctioned way to be group-wide. Demonstrated failure mode: A owns 40% of B and B owns 10% of A (순환출자). Stored in A's tenant, B's operators cannot see the edge that determines their own group membership; stored twice, the two copies can diverge with no transaction able to observe both. Implementing P4 as a real cross-entity relation requires either a global (org_id-free) edge table — a new tenancy carve-out — or making ADR-0021 §2 false.

### Deleting the Feature matrix removes the only token-only authorization path and the only escalation floor

- **Patterns:** P7
- **Incumbent:** simplicity: the compile-time matrix is a free, offline, fail-open-to-nothing floor that costs zero queries because roles arrive in the JWT
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/authz/src/lib.rs:1140`
- **Collision:** authorize() already unions two sources: the matrix over roles from the verified token, and data-driven grants from policy_roles (/Users/jasonlee/Developer/console/backend/crates/platform/authz/src/lib.rs:1191-1197). Roles come from claims.roles, never the DB (/Users/jasonlee/Developer/console/backend/crates/platform/request-context/src/lib.rs:334-339). So authored roles ALREADY work alongside the matrix; the matrix is not what blocks a no-code canvas. Deleting it makes every decision depend on rows, and custom_role_runtime_feature_allowed at :1388-1396 must go with it because it is exactly what forbids RoleManage/ElevatedRoleGrant/AuditStreamRead from being data-granted. Demonstrated failure mode: an org whose policy_roles set is empty, mis-authored, or wiped by a bad canvas edit gets zero grants; authorize() then denies everything INCLUDING RoleManage, so nobody inside the product can repair it — recovery requires a DBA. Second failure mode: with the :1388 floor removed, a MEMBER holding an authored role that includes role_manage can grant itself the full catalog, and nothing in Rust or SQL stops it (policy_role_permissions.feature_key only FKs a catalog, it carries no privilege ordering — /Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0065_create_policy_roles.sql:92). The pattern should give way to: keep the matrix as the bootstrap/escalation floor, make the authored layer additive only, which is what is already shipped.

### A platform-level party row cannot use the single-GUC RLS shape that protects 141 tables, and users.id is already globally unique

- **Patterns:** P1
- **Incumbent:** simplicity: users.id is ALREADY a global PRIMARY KEY on id alone; a self-FK or a two-column link table gives 'one human, many companies' with zero RLS change
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0002_create_users.sql:8`
- **Collision:** 0002:8 is `id UUID PRIMARY KEY` — the (id, org_id) UNIQUE at 0034_enforce_org_id_rollout.sql:122 was ADDED on top, it did not replace the global PK. That is why group_role_grants can reference users(id) without org_id (0060_create_groups_and_membership.sql:43) — cross-tenant identity already works, with no party table. Meanwhile every RLS policy is the same one-GUC predicate: `org_id = NULLIF(current_setting('app.current_org', true), '')::uuid` (users' own policy at 0030_enable_rls.sql:51-53). A party row sits above the tenant boundary so it HAS no org_id, which leaves exactly two options, both worse than a link: (a) no RLS on the identity table — a person's name and phone readable by every tenant's connection, in a repo with zero PII/retention/erasure infrastructure; or (b) the pattern's own 'visibility mediated by EDGES', which makes the policy `EXISTS (SELECT 1 FROM party_visibility ...)` — a correlated subquery per row on the table every single request already reads to build a principal. Demonstrated failure mode for (a): a misconfigured tenant connection enumerates the full cross-company person roster and 0030's fail-closed NULLIF design cannot catch it, because there is nothing to compare. Demonstrated failure mode for (b): the users read at authz lib.rs:1261-1268 and the branch-scope read both run per request; adding an EXISTS join to the identity policy taxes every authenticated call to serve a requirement that `users.person_id UUID REFERENCES users(id)` satisfies for free. The pattern must give way unless someone names a requirement the link cannot meet.

### P14's 'systems light up' has no substrate: the only shipped extension mechanism is a hand-written closure registry, 1 of 7 types wired

- **Patterns:** P14
- **Incumbent:** simplicity: the existing ProjectedDispatchRegistry is an explicit per-target adapter, and its fail-closed default is honest about not being generic
- **Evidence:** `/Users/jasonlee/Developer/console/backend/app/src/lib.rs:2559`
- **Collision:** The production registry registers exactly ONE target — 'registry.update_equipment' at /Users/jasonlee/Developer/console/backend/app/src/lib.rs:2559-2562 — against the seven projected types seeded in seed.rs. Every unregistered target returns ActionError::NotWiredYet (/Users/jasonlee/Developer/console/backend/crates/ontology/rest/src/lib.rs:188-193), and each handler is a bespoke async closure into the owning domain crate. So 'declaring a new entity class declares which components it has, and the systems light up' is contradicted by the only mechanism the ontology actually has: declaring gets you a typed refusal. The same disconnect exists one level down — a declared link type with no config.link property writes zero edges, forever (/Users/jasonlee/Developer/console/backend/crates/ontology/adapter-postgres/src/instances.rs:986-990, with link_type_ids pushed only at :920) — which is the component-model failure in miniature: a declaration that no writer consults. Demonstrated failure mode: ship P14's component declaration on today's substrate and a tenant declares an entity with the 'approval' and 'ledger' components; the canvas shows both attached, and every action against them returns NotWiredYet until someone writes two Rust closures. That is fourteen bespoke integrations wearing a declaration. The pattern cannot be implemented until a generic component→behaviour contract replaces the closure registry; symmetry with game-engine ECS is not that contract.

### The six roles ride the verified token and are parse-or-401

- **Patterns:** P7, P6, P9
- **Incumbent:** users.roles CHECK over six literals + Role::from_str at the request-context boundary
- **Evidence:** `backend/crates/platform/db/migrations/0046_add_member_role.sql:18 (`roles <@ ARRAY['SUPER_ADMIN','ADMIN','MECHANIC','RECEPTIONIST','EXECUTIVE','MEMBER']`); backend/crates/platform/request-context/src/lib.rs:337 (`Role::from_str(role).map_err(|_| RequestContextError::InvalidClaim("unknown role"))`)`
- **Collision:** P7 makes roles authored data. Every tenant request resolves its principal by mapping each JWT `roles` string through the compile-time `Role` enum; an authored role slug in a token is not a soft-ignore, it fails the whole context resolution. Failure mode: issue a token for a tenant-authored role '현장소장' and every request 401s before any policy is consulted — not the role being ignored, the user being logged out. The enum therefore cannot be deleted while the token carries roles; the JWT claim shape has to be changed first, and `users.roles` (an audited table, see the migration-safety collision) still has the six-literal CHECK.

### A required CI test freezes the live GitOps inputs byte-for-byte against origin/main

- **Patterns:** P7, P14, P2, P3, P15
- **Incumbent:** check-command-database-wiring.test.mjs "live Argo, base, prod, and secret wiring remain DARK-topology-free"
- **Evidence:** `scripts/check-command-database-wiring.test.mjs:106-107 (`assert.doesNotMatch(prod, /components:|pr-473|governed-command-database/)`), :110-116 (same over argo/base/database/backend/secrets, incl. `ONTOLOGY_COMMAND_DATABASE_URL`), :134-138 (`assert.equal(diff.status, 0, 'live GitOps inputs differ from origin/main')`); wired unconditionally at .github/workflows/ci.yml:955-956`
- **Collision:** Every ontology WRITE runs on the command pool, which is None unless ONTOLOGY_COMMAND_DATABASE_URL is set, and only the two pr-473-expand-* overlays supply it. The gate is stronger than 'prod must not reference the component': :134-138 asserts `git diff --exit-code origin/main` over deploy/argocd/apps/console.yaml, deploy/apps/console/base, deploy/apps/console/overlays/prod and deploy/apps/secrets-management/wiring is EMPTY, so those four paths may not change at all. Failure mode: the one-line kustomize edit that makes no-code authoring reachable in production turns a green required job red, and it is not fixable by editing the assertion alone — the diff check makes the file's very mutation the violation. Any pattern whose surface is authored-at-runtime (P7 roles/actions, P14 component declarations, P2 employment types, P3 unit kinds) is undeployable until this is resolved.

### The database refuses to store an object policy for any action but `view`

- **Patterns:** P7, P9, P5
- **Incumbent:** the ont policy attach writer function (migration 0205)
- **Evidence:** `backend/crates/platform/db/migrations/0205_ont_policy_api_attach_writer.sql:248-251 (`IF p_normalized_row ->> 'action' IS DISTINCT FROM 'view' THEN RAISE EXCEPTION 'attach refused: normalized_row action % is not the object-policy action'`); mirrored in the published wire contract at backend/openapi/openapi.yaml:12269-12270 ("the action is fixed to `view`")`
- **Collision:** P7 makes the action vocabulary authored data. The only authoring surface that ships refuses, in plpgsql, to persist a policy whose action is anything but `view` — matching the closed five-verb AUTHORING_ACTIONS list, which itself has no schema-mutation or decision verb. Failure mode: an admin authors 'who may APPROVE a 지출결의' on the no-code canvas and gets a raised exception, not a validation message, because the DDL rejects the row. Read-visibility is the only authorable authority; every write/decide/approve authority stays compile-time. Adding verbs requires a new writer function AND a change to `applicable_object_policies` on the read path (0205:246-247 says both are pinned to the same constant).

### An organization may belong to at most one group, enforced by a UNIQUE constraint

- **Patterns:** P4
- **Incumbent:** group_memberships UNIQUE (org_id)
- **Evidence:** `backend/crates/platform/db/migrations/0060_create_groups_and_membership.sql:36 (`UNIQUE (org_id)`); the design intent is stated verbatim at docs/specs/org-hierarchy.md:96 ("-- an Org is in at most ONE group") and organizations.group_id is a second single-valued copy at 0060:27`
- **Collision:** P4 wants CONTROL EDGES between legal entities with group designation DERIVED, supporting joint ventures, nesting and 순환출자 cycles. A joint venture is by definition an org controlled by two parents; a nested holding structure is an org that is a member of a sub-group that is a member of a group. Failure mode: `INSERT INTO group_memberships` for the second parent of a 50/50 JV raises a unique_violation, so the JV can only be recorded by picking a winner and losing the other control relation. The duplicate storage (organizations.group_id nullable FK plus the membership row) means the derived-designation direction also has to unwind two writers, and `groups` is flat with no parent column, so nesting has nowhere to live either.

### ont_links shares ONE org_id column across both endpoints

- **Patterns:** P1, P4, P5
- **Incumbent:** ont_links composite FKs — the only versioned/effective-dated edge table in the schema
- **Evidence:** `backend/crates/platform/db/migrations/0155_create_ontology_instances.sql:66-78 (single `org_id UUID NOT NULL` at :68; `FOREIGN KEY (from_instance_id, org_id) REFERENCES ont_instances(id, org_id)` at :76 and `FOREIGN KEY (to_instance_id, org_id) REFERENCES ont_instances(id, org_id)` at :77 — both endpoints resolved against the SAME org_id column), FORCE RLS + org_isolation applied at :82-96`
- **Collision:** P1 and P4 both propose that cross-company facts be carried by EDGES rather than by scoping rows. ont_links is structurally incapable of holding such an edge: one org_id serves both endpoints, so from and to must be in the same tenant. Failure mode: INSERT of an edge whose two instances belong to different orgs is rejected by one of the two composite FKs (no matching (id, org_id) row) — not by policy, by referential integrity. object_links is no escape: it also has one org_id (0102_create_object_types_and_links.sql:53-68) and its ids are opaque TEXT with no FK, so a cross-tenant edge is writable but visible to exactly one side under org_isolation (:76-80), which makes a group-level view impossible. Any cross-org edge therefore needs a NEW table exempt from the single-GUC app.current_org doctrine that 141 tables and backend/crates/platform/db/tests/rls_isolation.rs enforce as fail-closed.

### The action vocabulary is migration-only in the DB and silently filtered by a compile-time Rust enum

- **Patterns:** P7, P6
- **Incumbent:** feature_catalog grants + policy_role_permissions FK + Feature::from_str filter in the custom-role resolver
- **Evidence:** `backend/crates/platform/db/migrations/0065_create_policy_roles.sql:64-65 (`REVOKE ALL ON feature_catalog FROM PUBLIC; GRANT SELECT ON feature_catalog TO console_rt;` — SELECT only), :92 (`feature_key TEXT NOT NULL REFERENCES feature_catalog(feature_key) ON DELETE RESTRICT`), and backend/crates/platform/authz/src/lib.rs:1371 (`let feature = Feature::from_str(&row.feature_key).ok()?;`)`
- **Collision:** P7 requires the action vocabulary to be authored data. Two independent stops. (1) console_rt holds only SELECT on feature_catalog, so a no-code canvas cannot add a key at runtime; every one of the 18 migrations that extends the vocabulary is a developer deploy (0107:225, 0147:8, 0148:7, 0151:4, 0153:16, 0156:5, 0157:6, 0174:2, 0178:3, 0179:6, 0182:6, 0187:10, 0190:9, 0198:8 …). A canvas INSERT into policy_role_permissions with an unregistered feature_key is rejected by the ON DELETE RESTRICT FK. (2) Even if the row existed, `.ok()?` at lib.rs:1371 DROPS it silently — no error, no log — so the grant is inert and the operator sees a saved role that grants nothing. This is the exact failure the thesis forbids: adding the next vertical's verbs requires a developer.

### Grant-condition attributes are closed to {branch, team} and an unknown attribute silently voids the whole role

- **Patterns:** P5, P6, P3
- **Incumbent:** effective_scope_for_custom_role_conditions match arm + team_policy_values, bound to the users.team CHECK
- **Evidence:** `backend/crates/platform/authz/src/lib.rs:1409-1428 (match on `condition.attribute.as_str()` with arms "branch" and "team" only, `_ => return None` at :1425), :1447-1453 (`team_policy_values` matching exactly "정비"/"예방"/"관리"/"접수"), :1405-1407 (`if !matches!(condition.operator.as_str(), "equals" | "in") { return None }`), backed by backend/crates/platform/db/migrations/0002_create_users.sql:16 (`team TEXT CHECK (team IN ('정비', '예방', '관리', '접수'))`)`
- **Collision:** P5 introduces COMPETENCE as a scope dimension distinct from the operational branch, and P6 folds grants from position/group/direct/delegation/assignment. Neither has an attribute slot. Failure mode is worse than rejection: a canvas-authored condition with attribute "org_unit" or "competence" hits `_ => return None`, effective_scope_for_custom_role_conditions returns None, the role is skipped at lib.rs:1354-1360, and the user loses EVERY grant from that role with no error surfaced. Additionally the only non-branch dimension that exists is four hardcoded Korean vehicle-workshop team names, in Rust and again in a DB CHECK — the most HR/shop-specific thing in the authorization core, and P5's decision scope has to be expressed through it or not at all.

### An authored action can only do one of two things, and the second requires a compiled Rust handler that exists for exactly one target

- **Patterns:** P14, P7, P10
- **Incumbent:** ont_action_types.dispatch CHECK + ProjectedDispatchRegistry
- **Evidence:** `backend/crates/platform/db/migrations/0152_create_ontology_registry.sql:97 (`dispatch TEXT NOT NULL CHECK (dispatch IN ('projected_usecase','instance_revision'))`); backend/app/src/lib.rs:2558-2563 (`ProjectedDispatchRegistry::new().register("registry.update_equipment", …)` — one registration, the whole registry); backend/crates/ontology/rest/src/lib.rs:188-192 (unknown target → `ActionError::NotWiredYet`)`
- **Collision:** P14 says declaring an entity class declares its components and the systems light up without hand-writing integrations. The CHECK admits only two effects: revise ontology instance rows, or route to a Rust-registered use-case. Every cross-cutting concern that actually lives in a domain table — approval, period lock, ledger posting, retention — is only reachable through the second, and the registry contains exactly one target. Failure mode: a canvas-authored action whose dispatch_target is anything other than "registry.update_equipment" returns NotWiredYet at execute; the only fix is a Rust `register()` call plus a redeploy. So of the 7 projected object types seeded by seed.rs, 6 have no working action, and the count of no-code-reachable domain effects is 1.

## MAJOR (53)

### Platform-tier audit rows are write-only, so party-level mutations are not provable

- **Patterns:** P1
- **Incumbent:** ADR-0002 audit-first transactional discipline (every state transition provable after the fact) versus the audit_events RLS read policy
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0035_enable_rls_rollout.sql:107-113 — `USING (org_id = NULLIF(current_setting('app.current_org', true), '')::uuid)` with `OR org_id IS NULL` present only in WITH CHECK; /Users/jasonlee/Developer/console/docs/decisions/ADR-0002-auditfirst-transactional-discipline-audit-event-in.md:17 and :20`
- **Collision:** P1 puts party identity above the org-scoped users table, so creating or re-pointing a party is a mutation with no owning org_id. ADR-0002:17 requires every state transition to be provable after the fact and :20 requires the audit INSERT in the same transaction. The RLS policy at 0035:110-113 lets an org_id-NULL audit row be INSERTed but its USING clause matches no row when org_id IS NULL, so under `console_rt` that row can never be SELECTed again. Demonstrated failure mode: merging two party records, or attaching a person to a second employer, writes an audit row that no runtime query can ever return — the mutation is recorded and simultaneously unprovable, which is the exact property ADR-0002 exists to guarantee. Fixing it means changing the audit read policy for every tenant, which the tenant-isolation gate treats as an isolation change (/Users/jasonlee/Developer/console/backend/ci/gates/tenant-isolation/src/lib.rs:16-21).

### One physical passkey cannot belong to a person who works at two companies

- **Patterns:** P1
- **Incumbent:** ADR-0004 passkeys-first identity, realized as a credential pinned to (user_id, org_id) with a globally unique credential_id and a credential→org login resolver
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0004_create_auth.sql:7 (`credential_id TEXT NOT NULL UNIQUE`); /Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0034_enforce_org_id_rollout.sql:143-144 (`auth_webauthn_credentials_user_same_org_fk FOREIGN KEY (user_id, org_id) REFERENCES users(id, org_id)`); /Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0038_auth_preauth_org_resolvers.sql:64-84 (`platform_resolve_credential_org` returns one org_id, `LIMIT 1`, and arms the GUC from it); /Users/jasonlee/Developer/console/docs/decisions/ADR-0004-passkeysfirst-auth-with-rotating-refreshtoken-families.md:20`
- **Collision:** P1 asserts one human exists once across companies. Authentication contradicts that below the data model: credential_id is UNIQUE table-wide (0004:7) and each credential is composite-FK'd to one (user_id, org_id) pair (0034:143-144), so enrolling the same authenticator at a second employer is rejected by the unique index; and login resolves tenant FROM the credential with LIMIT 1 (0038:78-81), so the session's org is decided before the person can choose. Demonstrated failure mode: a 4대보험 담당자 serving two group companies must enroll two distinct passkeys and cannot switch companies without a full re-authentication ceremony; a party-level credential would make platform_resolve_credential_org ambiguous and it would arm the wrong tenant GUC. P1 is implementable only with per-org credentials hanging off the party, i.e. the party is not the authentication subject — which is a real cost, not a free extension of ADR-0004.

### Effective-dated grants defeat the version/digest cache key ADR-0021 makes load-bearing

- **Patterns:** P6, P16
- **Incumbent:** ADR-0021 §4 (bundle cache keyed by immutable version/digest; no cross-request allow/deny cache) and §5 (mutation-driven subject-version bumps as the staleness detector)
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/ADR-0021-cedar-pbac-authorization-strangler.md:55-57 and :58-60`
- **Collision:** P6 makes authority a fold of effective-dated grants so it is replayable, and P16 asks for a decided answer on materialization. ADR-0021:58-60 makes freshness depend on a MUTATION synchronously bumping subject/policy versions; a grant that activates or expires at a timestamp involves no mutation at that instant, so no bump occurs and two evaluations with identical bundle digest and identical `authz_subject_version` legitimately return different decisions. Demonstrated failure mode: a 대결 grant with expires_at at midnight keeps authorizing after midnight from a digest-keyed cached bundle, which is precisely the stale-material-keeps-granting failure ADR-0021:58-60 exists to prevent. Separately, P16's materialized-with-invalidation option stores an effective allow/deny answer read by later requests, which ADR-0021:56-57 forbids outright. Resolving this requires ADR-0021 to add time to its cache key contract (an amendment), not just new tables.

### clearance_assignments proves the incumbent grant shape cannot hold non-overlapping future grants

- **Patterns:** P6
- **Incumbent:** The only effective-dated authority table in the schema stores active-ness as a column guarded by a partial unique index
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0147_ceo_covert_audit_stream.sql:20 (`status TEXT NOT NULL CHECK (status IN ('ACTIVE','REVOKED','EXPIRED'))`), :21-22 (starts_at/expires_at), :34-36 (`CREATE UNIQUE INDEX idx_clearance_assignments_active_unique ... WHERE status = 'ACTIVE'`)`
- **Collision:** P6 requires authority to be a fold over effective-dated grants, i.e. a pure function of (person, scope, instant). 0147 already has starts_at/expires_at but denormalizes active-ness into a stored `status` and then enforces at most one ACTIVE row per (org_id, user_id, stream_key, clearance_key) at :34-36. Demonstrated failure mode: granting the same 전결권 to a person for January and again for March requires two rows both legitimately ACTIVE-by-date; the partial unique index rejects the second insert, and it cannot be avoided by marking the first EXPIRED because January has not ended. Adopting P6 means this table's status column and its unique index must go, and every reader that filters `status = 'ACTIVE'` becomes wrong — so P6 is a rewrite of the existing grant precedent, not an addition beside it.

### Retroactive 반려 after closure is the reopened terminal run that ADR-0023/0025 forbid

- **Patterns:** P8
- **Incumbent:** ADR-0023 pre-terminal finalization model, re-adopted verbatim by ADR-0025 §5
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/ADR-0023-oyatie-console-authority.md:83-86 ("최종승인 and 수령확인 are pre-terminal `WAITING` nodes (never a reopened terminal run) with 사후 반려 modeled as a compensating document/event"); /Users/jasonlee/Developer/console/docs/decisions/ADR-0025-carbon-copy-console-shared-platform-spine.md:156-160`
- **Collision:** P8 requires that closure truncate the line without extinguishing other members' standing, and that a node earlier and lower than the closer may retroactively 반려. ADR-0023:83-86 binds the opposite representation: once a run is terminal it is never reopened, and any post-approval reversal must be a separate compensating document/event. Implementing the retroactive 반려 as a transition on the closed line makes ADR-0023's decision false, and it is the clause ADR-0023:119-123 says was chosen over the bespoke-approvals fallback, so it is load-bearing rather than incidental. Failure mode if violated: the workflow FSM gains an edge out of a terminal node, which is the exact structural change ADR-0023:47 records the M2 engine as forbidding, and every consumer that treats a terminal run as final (payroll consequence, document export) can be silently invalidated after the fact. P8 is implementable only if effective approval status becomes a query over run + compensating events — a real cost, since the 결재 line's own state then never tells the truth alone.

### A tenant cannot declare an entity class that participates in the generic edge store

- **Patterns:** P14, P10, P12
- **Incumbent:** object_types is a platform-global seeded registry that the runtime role may only SELECT, and object_links FKs to it
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0102_create_object_types_and_links.sql:18-24 (table), :29-45 (16 seeded kinds), :27-28 ("New kinds are added by appending a row in a later migration"), :47 (`GRANT SELECT ON object_types TO console_rt` — no INSERT), :56 and :58 (`src_kind`/`dst_kind` REFERENCES object_types(kind) ON DELETE RESTRICT); allowlist rationale at /Users/jasonlee/Developer/console/backend/ci/gates/tenant-isolation/src/lib.rs:43-46`
- **Collision:** P14 requires declaring a new entity class to be authored data; P10 makes 업무 a first-class entity that artifacts and actions link to; P12 builds a lineage DAG. All three need tenant-authored kinds to be linkable. The runtime role holds SELECT only on object_types (0102:47) and object_links.src_kind/dst_kind are FK-constrained to it (0102:56,:58), so a kind that a customer authors at runtime cannot appear on either end of any edge. Demonstrated failure mode: a tenant declares a 도급계약 object type in the ontology, then tries to link it to a work order — the object_links INSERT fails on the foreign key, and the only fix is a developer writing migration 0207, which contradicts the governing constraint that the corporation be manageable without developers. Note the existing seed has no `work`/`업무` kind at all (0102:29-45), so P10's handover-as-one-edge query has no kind to use today either.

### ADR-0001's layer gate makes "systems light up" require a developer per component

- **Patterns:** P14, P7
- **Incumbent:** ADR-0001 compiler-enforced layering, mechanised by the layer-boundary gate
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/ADR-0001-modularmonolith-cargo-workspace-with-compilerenforced-cleanarchitecture.md:20; /Users/jasonlee/Developer/console/backend/ci/gates/layer-boundary/src/lib.rs:94 (`Layer::Domain => &[Layer::Kernel]`), :128 (`Layer::Domain | Layer::Application => &["sqlx", "axum", "tokio"]` forbidden), :402-407 (violation on any dep outside `allowed_deps`)`
- **Collision:** P14's payload is that declaring which components an entity has causes the systems to light up with no hand-written integration; DN-0003:57 defines Systems as the existing domain use cases. lib.rs:94 forbids any domain crate from depending on another domain crate, and :128 forbids sqlx in domain/application crates, so a domain system cannot read the authored component declaration at all — the read must happen in an adapter/rest crate and be passed inward as typed arguments. Demonstrated failure mode: authoring a new `approvable` component on a new entity class changes no compiled code, so no domain use case can act on it until someone edits an adapter to fetch the declaration and a rest handler to route it — one developer per component, which is the outcome the thesis forbids. The lazy alternative (pass an untyped attribute bag into domain) forfeits the exhaustively-unit-testable pure domain ADR-0001:23 claims as its benefit. Resolving this needs ADR-0001 to name a sanctioned generic component seam, not just new tables.

### DN-0003 rejects a generic entity-component store by name

- **Patterns:** P14
- **Incumbent:** DN-0003 Operational Object Runtime (subordinate design note under accepted ADR-0025, activation IN PROGRESS)
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/notes/DN-0003-adr-0025-operational-object-runtime.md:22-24 ("must not adopt game styling, a generic entity-component database, runtime JavaScript plugins, or a second source of truth"), :66-67 ("Capabilities are interfaces attached to governed object types. They are not rows in a universal component table."), :195 (rejected: "A generic ECS/component store that becomes a second writer")`
- **Collision:** P14 is a component model where entities compose cross-cutting concerns and the declaration is data. DN-0003:66-67 binds the exact opposite: capabilities are interfaces on governed object types, explicitly not rows in a universal component table, and :22-24 rejects a generic entity-component database outright. Implementing P14 as authored component rows makes DN-0003:66-67 false as written. Failure mode DN-0003 is guarding against and that P14 must answer: two writers for the same fact — the domain-owned writer for a projected business object and a generic component row claiming the same concern — with no rule for which wins, so period locks, approvals and evidence links can be bypassed through the generic path. Note the authority level: per /Users/jasonlee/Developer/console/docs/decisions/README.md:10 a design note cannot supersede an accepted ADR, but it is the active implementation contract under ADR-0025 and must be amended explicitly, not out-voted by a plan.

### DN-0003's world is tenant-scoped, so a cross-company entity has no EntityRef

- **Patterns:** P1, P4, P13
- **Incumbent:** DN-0003 operational runtime model — one tenant-scoped graph, tenant-qualified entity identity
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/notes/DN-0003-adr-0025-operational-object-runtime.md:55-56 ("World | One tenant-scoped operational graph"; "Entity | Stable `EntityRef` containing tenant, object type, and object ID") and :88 (denied data omitted "including counts and relationship existence")`
- **Collision:** P1 and P4 require entities and edges that exist above or between tenants. DN-0003:56 makes tenant part of entity identity, so a party or a control edge that is not owned by one tenant cannot be referenced by the runtime at all — object focus (:94), the object card, traversal and Actions all key on a tenant-qualified ref. :88 additionally requires that denied relationship EXISTENCE be omitted, which forbids the honest "this party also exists elsewhere" affordance P1 needs to be useful. Failure mode: a group-level 재무 view that must aggregate across entities either fabricates a synthetic tenant for the group (a second source of truth, rejected at :24) or fans out to N tenant graphs with no single EntityRef for the group figure. Group-consolidated reads are already N sequential per-org connections by design (/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0060_create_groups_and_membership.sql:9-10), so P13's group P&L cannot be one query in one transaction with a reproducible recorded basis.

### users.roles is a database CHECK over six literals

- **Patterns:** P7, P6
- **Incumbent:** The role vocabulary is a column constraint on the users table, not just a Rust enum
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0046_add_member_role.sql:16-18 (`DROP CONSTRAINT users_roles_check, ADD CONSTRAINT users_roles_check CHECK (roles <@ ARRAY['SUPER_ADMIN','ADMIN','MECHANIC','RECEPTIONIST','EXECUTIVE','MEMBER'])`), originally /Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0002_create_users.sql:12-14; role-derived org-wide authority at /Users/jasonlee/Developer/console/backend/crates/platform/authz/src/lib.rs:1158-1161`
- **Collision:** P7 makes roles authored data configurable from a no-code canvas; P6 makes roles saved bundles of grants. The vocabulary is enforced in Postgres at 0046:16-18, so a tenant-authored role cannot be written to users.roles at all — the insert is rejected by the CHECK, not by application code. This is a third enforcement point beyond the Feature matrix and policy_roles, and 0046 proves the maintenance model: adding one role took a migration that drops and re-adds the constraint. Failure mode: authoring 팀장 in the canvas succeeds in policy_roles but the assignment never reaches the principal's built-in role list, so lib.rs:1158-1161's org-wide path and every `principal.roles.iter()` check keep answering from the six demo literals — the canvas appears to work and grants nothing. P7 must drop this constraint, which is a schema change to the users table that 141 RLS-armed tables' FKs point at.

### 파견 needs an operational row whose worksite belongs to another tenant

- **Patterns:** P2, P3
- **Incumbent:** ADR-0003 non-null branch scope day 1, with branch as the single mandatory operational dimension
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/ADR-0003-branchscoped-authorization-model-nonnull-branch-scope.md:20 and :23 ("No retrofit migration"); /Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0001_create_regions_branches.sql (regions = id/name, branches adds only region_id); enforcement shape at /Users/jasonlee/Developer/console/backend/crates/platform/authz/src/lib.rs:1592-1605`
- **Collision:** P2 separates EMPLOYER from WORKSITE so 파견 is expressible; P3 gives org units a KIND and a LIFETIME derived from a contract. ADR-0003:20 makes a single branch set both the operational scope and the authorization scope, and every operational row carries a non-null branch_id. A dispatched worker's attendance row has an employer in tenant A and a worksite that is a branch of tenant B; branch_id can name only one of them, and FORCE RLS on app.current_org means the row cannot reference the other tenant's branch at all. For P3, a TF that disbands leaves rows whose branch_id points at a unit that must not be deleted (branch FKs are ON DELETE RESTRICT throughout), so either units never die or the rows must be re-homed — the retrofit migration ADR-0003:23 claims the design avoids. `BranchScope::Branches(BTreeSet<BranchId>)` (kernel/core/src/branch.rs:14-17) has no time dimension, so an expiring unit membership cannot be expressed in the scope type at all.

### Imported person data granting authority is what ADR-0022 forbids

- **Patterns:** P6, P2
- **Incumbent:** ADR-0022 local identity only — person-data integrations may not grant roles
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/ADR-0022-local-identity-no-external-idp.md:38 ("HR roster, attendance, payroll, or person-data integrations are data integrations by default. They must not authenticate users, assert sessions, grant roles, or decide account status unless a separate identity-federation ADR names the real IdP/protocol/claims and passes security review."); the person store is an import row: /Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0063_create_employees.sql (source_filename, source_sheet, source_row, raw_row JSONB, unique (org_id, source_key))`
- **Collision:** P6 sources effective authority from the POSITION a person occupies, and P2 makes employee and position separate entities. Today the person record is a spreadsheet import row (0063), so deriving grants from position occupancy means an imported roster row decides authority — exactly what ADR-0022:38 prohibits without a named federation decision and security review. Failure mode: re-importing next month's roster with a shifted column silently moves 전결권, and because the grant is derived rather than granted there is no granted_by and no grant_reason to audit against — ADR-0002's provability degrades to "whatever the last spreadsheet said". P6 is implementable only if position occupancy is natively authored and audited rather than imported, which means the employees import table cannot be the position source.

### The real P7 defect is a silent grant drop, not the matrix — and it is a five-line fix

- **Patterns:** P7, P6
- **Incumbent:** simplicity: feature_catalog is ALREADY an authored data table with a slug CHECK, so the action vocabulary is already data
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0065_create_policy_roles.sql:12`
- **Collision:** feature_catalog(feature_key TEXT PRIMARY KEY CHECK (feature_key ~ '^[a-z][a-z0-9_]*$')) at 0065:12 with policy_role_permissions.feature_key REFERENCES it at 0065:92 means adding a verb is an INSERT today. What is compile-time is only the parse: /Users/jasonlee/Developer/console/backend/crates/platform/authz/src/lib.rs:1371 does Feature::from_str(...).ok()? inside a filter_map, so an authored feature_key with no Rust variant is DISCARDED WITHOUT ERROR. Demonstrated failure mode: a no-code canvas inserts feature_key 'payroll_recalc', an admin grants it, Policy Studio shows the grant, the runtime silently ignores it — an authorization that reads as granted and does nothing, indistinguishable from a bug in the feature itself. Deleting the matrix does not fix this; it universalises it, because then EVERY key parse-fails into silence. The pattern gives way to: make an unknown feature_key a hard error at grant time (one match on the catalog), which is a smaller diff than a rewrite and removes the actual live correctness hole.

### Effective-dating grants buys a replay that is silently wrong, because the fold's other inputs have no history

- **Patterns:** P6
- **Incumbent:** simplicity: authority today is reconstructible from audit_events before/after snapshots without temporalizing the org backbone
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/authz/src/lib.rs:1261`
- **Collision:** The fold reads two live, historyless inputs: users.team at authz lib.rs:1261-1268 (a mutable column, 0002_create_users.sql:16, no history table) and live_branch_scope from user_branches, whose entire DDL is (user_id, branch_id) with no validity dates (0002_create_users.sql:23-27). The condition evaluator then intersects branch scope and matches team (authz lib.rs:1410-1424). Demonstrated failure mode: a 2026-03 decision is replayed in 2026-09 after the employee moved 팀 and branch. The grant rows replay correctly, users.team and user_branches do not, and the replay returns a DIFFERENT permission with no indication that two of three inputs were read at wall-clock now. That is worse than no replay: it is an audit artifact that reads as authoritative. Making replay honest means temporalizing team, branch membership, and group membership — i.e. bitemporal history on the org backbone that 141 tables FK into — which is a far larger project than effective-dating grants and must be priced with P6, not after it.

### P15 is already shipped for authority changes, in a stronger form than a rollback dry run

- **Patterns:** P15
- **Incumbent:** simplicity: policy_assignment_preview_receipts — an epoch-bound, expiring, single-consume preview receipt that the write path verifies atomically
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/identity/adapter-postgres/src/lib.rs:2414`
- **Collision:** consume_policy_assignment_preview_receipt_tx (/Users/jasonlee/Developer/console/backend/crates/identity/adapter-postgres/src/lib.rs:2414-2450) UPDATEs the receipt to consumed only if consumed_at IS NULL, expires_at > now, and the previewed before-state (current_role_ids, current_branch_ids, current_system_roles), the intended after-state (role_ids, branch_ids, system_roles) AND policy_version all still match. That is simulate-before-commit WITH a serialisability guarantee that a BEGIN/ROLLBACK dry run does not give you: a rollback tells you what would have happened against a snapshot nobody carries forward, so between preflight and execute the policy can move and the user commits a change they never previewed. The DDL is at 0065_create_policy_roles.sql:159-174 (policy_version BIGINT at :167). The ontology surface already has the other half of the primitive too — strong ETag validators and a 412 stale-precondition mapping (/Users/jasonlee/Developer/console/backend/crates/ontology/rest/src/lib.rs:283, :2555) — while preflight_action returns a PreflightOutcome carrying no validator at all (:1238-1257). Demonstrated failure mode for the general simulation engine: it would run on the ontology command pool, which is None in every real overlay, so the dry run would be dead exactly where the write is dead — a simulator green in CI and absent in prod. The pattern gives way to: copy the receipt table for 전결규정, and return the existing validator from preflight with If-Match required on execute.

### Per-request fold is already 4-5 round trips, and the only invalidation epoch is per-org — month-end is the worst case

- **Patterns:** P6, P16
- **Incumbent:** simplicity: fold-on-demand with no cache, so there is nothing to invalidate and no staleness class to reason about
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/request-context/src/lib.rs:372`
- **Collision:** Every authenticated request already pays: resolve_branch_scope_in_org then resolve_effective_feature_grants_in_org (request-context/src/lib.rs:366-374), and the latter opens its own transaction and issues set_config + users.team + permissions + conditions (authz lib.rs:1256-1330). P6 adds four more grant sources (position, group, direct, delegation, assignment), roughly doubling that per request, on a path with no cache. The materialized branch does not escape it either: the only epoch is policy_versions, PRIMARY KEY (org_id) (0065_create_policy_roles.sql:177-178), read per token mint at /Users/jasonlee/Developer/console/backend/crates/platform/auth-rest/src/lib.rs:2866-2879. Demonstrated failure mode: one 전결규정 edit during month-end close bumps the org's single version, invalidating every principal in a 10k-employee tenant simultaneously, and all of them re-fold six sources against the same four tables at peak concurrency. A per-subject epoch exists (subject_authz_versions, read at /Users/jasonlee/Developer/console/backend/crates/platform/db/src/audit_tx.rs:290-300) but a policy edit is inherently org-wide. P16's 'decided answer' must be decided BEFORE P6 widens the fold, and the honest options are: keep fold-on-demand and cap the number of sources, or accept a thundering-herd mitigation as part of P6's cost.

### Competence (P5) is a missing match arm, not a missing relation

- **Patterns:** P5, P6
- **Incumbent:** simplicity: policy_role_conditions already declares 17 scoping attributes including position, assignment and department
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0065_create_policy_roles.sql:110`
- **Collision:** The attribute CHECK at 0065:110-128 already admits 'position', 'assignment', 'department', 'employment_status', 'purpose', 'action', 'resource', 'sensitive_action'. The runtime evaluator consumes exactly two of them — 'branch' and 'team' — and everything else hits `_ => return None` at /Users/jasonlee/Developer/console/backend/crates/platform/authz/src/lib.rs:1426, whose caller then drops the ENTIRE role via the `let Some(scope) = ... else { continue }` at :1367. Demonstrated failure mode (live today, not hypothetical): an admin authors a role scoped by attribute 'position'; the write is accepted by the CHECK, Policy Studio previews it, and at runtime the role contributes NOTHING — silently, with no denial reason naming the unsupported attribute. 'Which unit may DECIDE what' is a condition on a grant, and the table for it exists. A third relation orthogonal to control and structure adds a graph, a resolver and a fold source to serve a requirement that a match arm serves — and it inherits the same silent-drop bug. The pattern gives way unless someone shows a competence query that must be answerable with no grant in play.

### P13's dimension already exists on the voucher; the ledger that would make profit-as-a-query reproducible does not

- **Patterns:** P13
- **Incumbent:** simplicity: source_object_type/source_object_id plus a mandatory branch_id are already the object dimension, indexed
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0160_create_finance_gl_vouchers.sql:34`
- **Collision:** finance_gl_vouchers already carries branch_id NOT NULL (0160:25) and source_object_type/source_object_id (0160:34-35) with a dedicated index (0160:53-54). Pushing those two columns onto finance_gl_voucher_lines is the whole 'business object as a DIMENSION' requirement, at two columns. What P13 additionally promises — cost/revenue/profit as QUERIES, allocation 'reproducible with its basis recorded' — collides with the ledger it must query: account_code is bare TEXT with no master (0160:62), the only date is posted_at (0160:41) so there is no business/accounting date, currency is baked into the column name amount_won (0160:64), and period_locks does not guard this table at all (five opt-in Rust call sites, none in finance-gl; /Users/jasonlee/Developer/console/backend/crates/platform/db/src/period_lock.rs:69-96). Demonstrated failure mode: a profit query for FY2026-Q1 is run twice, three weeks apart, and returns different numbers, because a late voucher posted into a period that nothing froze and posted_at is the only date available to bucket it. An allocation whose result is not stable is not auditable, which is the opposite of the pattern's own stated goal. Order of operations must invert: business date + account master + lock enforcement first; the dimension is the cheap part and is half-built already.

### A quantity DAG with enforced conservation serves neither the first vertical nor the one already half-shipped

- **Patterns:** P12
- **Incumbent:** simplicity: scalar balance plus a delta log with a row-level conservation CHECK, which already ships
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0191_inventory_movement_conservation.sql:119`
- **Collision:** Vertical 1 is HR + payroll. It has no lot, no batch, no BOM, no yield and no scrap — and neither does the repo, in 206 migrations. The vertical that WOULD use a quantity DAG (inventory/production) already ships a cheaper model with conservation enforced per row at 0191:119. P12 asks for a split/merge DAG with 'children sum to parent' enforced, which in Postgres is not a CHECK: it is a cross-row invariant needing either a deferred constraint trigger on every insert or a denormalised parent total, on the hottest write path of a vertical that is not first. Demonstrated cost, not failure: the pattern's own prerequisites are absent — inventory_movements has no parent_movement_id and no from/to location pair, so today a transfer is not even one row (0191:91-127) — so P12 is not an increment on the incumbent, it is a replacement of it, undertaken while the FIRST vertical still has no PII infrastructure and a compliance register where all 175 control statuses are HOLD. The narrow fix that unblocks real work is parent_movement_id plus a from/to pair. Build the DAG when a customer names a lot.

### Capacity-bearing signatures dissolve the only DB-enforced four-eyes invariant in the repo

- **Patterns:** P9, P8
- **Incumbent:** simplicity: CHECK (approver_id <> requested_by) — separation of duties as a constraint no application bug can bypass
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0153_create_governance.sql:74`
- **Collision:** gov_approvals enforces self-approval-is-impossible in the DDL (0153:74), alongside UNIQUE (org_id, request_ref) at :76 giving one final decision per request. P9 makes the signature (signer, authorizing grant, scope), and P8 adds 대결 — a delegate signing under someone else's capacity. The moment capacity is what matters, `approver_id <> requested_by` is neither necessary nor sufficient, and the correct predicate — 'the capacity under which this was signed is not the requester's own capacity' — requires resolving a grant fold, which cannot live in a CHECK because it is not immutable. Demonstrated failure mode: a manager raises a 기안, then 대결 is exercised by their assistant holding a delegation FROM that same manager; approver_id differs, the CHECK passes, and the request is self-approved through a delegate with no mechanism objecting. That moves separation of duties from mechanism 1 to mechanism 3 in this repo's own ranking — 'Prefer the earlier one — later ones rely on discipline, and discipline is what fails' (/Users/jasonlee/Developer/console/docs/program/LANE-PROTOCOL.md:72-73). If P9 proceeds, the delegation-transitivity check must be a trigger and must be written in the same change as the capacity columns, not after.

### A 결재 graph with retroactive 반려 has no terminal state, and the ledger downstream cannot represent the reversal

- **Patterns:** P8
- **Incumbent:** simplicity: a bounded step vocabulary plus an ordered approval_line, which already runs a real 4-stage custody flow
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0072_equipment_ownership_transfer.sql:17`
- **Collision:** The shipped precedent bounds the shape: current_step over four literals (0072:17) with an ordered approval_line JSONB (0072:18) and append-only events (0072:35-46). P8's general graph adds three edge kinds, parallel 합의 branches, and — decisively — 'a node earlier and lower than the closer may retroactively 반려'. That removes the terminal state: an approved request is never final, so every derived write is provisional. Downstream, finance_gl models reversal only as a contra voucher pair (status CHECK at 0160_create_finance_gl_vouchers.sql:27-29, reversal FKs at :48-49) and has no business date to post it to (:41), and period_locks does not guard finance_gl (period_lock.rs:69-96, five call sites, none in GL). Demonstrated failure mode: a 기안 approved in March produces a posted voucher; in May an earlier-and-lower node exercises retroactive 반려; the system must post a contra voucher, and the ONLY date available is posted_at, so the reversal lands in May with no representation that it reverses March — a payroll period that period_locks claims is frozen. There is no way to refuse it either, because nothing in the DB enforces the freeze. If retroactive 반려 is genuinely required, it needs a business date and GL lock enforcement as prerequisites; otherwise a bounded line with a terminal state covers the 전결규정 cases and the reversal problem never arises.

### Employer/worksite separation collides with the mandatory same-org composite branch FK

- **Patterns:** P2, P3
- **Incumbent:** simplicity: worksite as an attribute of the employment record, because no operational row can be branch-scoped to a foreign tenant anyway
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0072_equipment_ownership_transfer.sql:26`
- **Collision:** Operational rows carry branch_id with a composite same-org FK — FOREIGN KEY (branch_id, org_id) REFERENCES branches(id, org_id), e.g. 0072:26 — and branches themselves are org-scoped under the single-GUC RLS policy (0030_enable_rls.sql:45-47). For 파견, the employer is tenant A and the worksite is a branch of tenant B. The employment row's org_id is A's, so the composite FK STRUCTURALLY cannot point at B's branch; making it possible means either dropping the composite FK (losing the guarantee that a row's branch belongs to its own tenant — the invariant 0034_enforce_org_id_rollout.sql:122 was added to create) or giving worksite its own cross-tenant reference outside RLS. Demonstrated failure mode of the naive version: model worksite as a branch FK and every 파견 assignment fails insertion; relax the FK and a bug can now scope tenant A's attendance row to tenant B's branch, which the RLS predicate will not catch because it only tests org_id. The stated requirement — 'this person works at the host site' — is satisfied by naming the host as a party reference on the employment record and leaving branch_id as the employer's own scope. That is one column and preserves the invariant. The employment-type half of P2 is separately cheap and worth doing: two conflicting CHECK vocabularies (0172:7 vs 0187:22) should be one lookup table, which does not require per-type rule authoring to fix.

### Derived group designation collides with UNIQUE (org_id) and turns a shareholding edit into cross-tenant authority

- **Patterns:** P4
- **Incumbent:** simplicity: stored membership with one group per 법인, resolved through a narrow owner-only SECURITY DEFINER function
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0060_create_groups_and_membership.sql:36`
- **Collision:** group_memberships is PRIMARY KEY (group_id, org_id) with UNIQUE (org_id) at 0060:36 — at most one group per legal entity, which a 50/50 joint venture violates by construction. Worse, group_id is the key that grants cross-tenant authority: group_role_grants(group_id, user_id, group_role) at 0060:40-49, with console_rt's raw SELECT revoked (0060:55-57) so runtime reaches membership only through the SECURITY DEFINER resolver group_member_org_ids at 0060:60. Making designation DERIVED from control edges means that resolver becomes a recursive CTE over shareholdings, executed on the hot path of every group-context request (/Users/jasonlee/Developer/console/backend/crates/platform/request-context/src/lib.rs:405-415 already calls the member resolver per request), and 순환출자 means the graph has cycles so the derivation is an SCC computation with no natural depth bound. Demonstrated failure mode: with A→B→C→A the SCC is one group; a finance user records a new shareholding row and a GROUP_ADMIN of A instantly acquires read/administer authority over C — an authority escalation caused by a non-authorization edit, with no approval step and no audit event that says 'authority changed'. Control edges are worth storing as data. Deriving the authority key from them is what must give way: keep group_id stored and explicit, and let the control graph inform a human decision to change it.

### '인계 완료 is a computable graph query' is not computable over object_links

- **Patterns:** P10
- **Incumbent:** simplicity: object_links already provides the generic edge, and its deliberate looseness is the reason the query cannot be trusted
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0102_create_object_types_and_links.sql:57`
- **Collision:** object_links constrains the KINDS (src_kind and dst_kind both REFERENCES object_types(kind), 0102:56 and :58) but the IDs are opaque TEXT with only a length CHECK (src_id at :57, dst_id at :59). So the edge table cannot detect a dangling endpoint. P10's payoff claim — handover completeness as a graph query, and partial handover via a work scope — depends on the edge set being complete and referentially sound. Demonstrated failure mode: an artifact row is deleted or re-keyed by its owning domain; its object_links edge survives pointing at nothing, and the completeness query either counts it (reports work still outstanding that no longer exists) or, if it inner-joins to resolve, silently drops it and reports 인계 완료 while an artifact is unaccounted for. Either way the answer is wrong and nothing in the schema flags it. The good half of P10 costs almost nothing: a work row plus edges via the existing table. The 'computable' guarantee needs per-kind referential validation — a trigger or a per-kind resolver registry — and that cost belongs to P10, not to a later cleanup. Note this is the same class of defect as the ontology's declared-link/no-edge-writer gap (instances.rs:986-990): a graph nobody validates.

### Attached object policies are physically unrevokable by the runtime role

- **Patterns:** P6, P7, P15
- **Incumbent:** append-only grants on ont_object_policies and cedar_policy_catalog_entries
- **Evidence:** `backend/crates/platform/db/migrations/0154_create_cedar_object_property_policies.sql:105-107 (`GRANT SELECT, INSERT ON ont_object_policies TO console_rt;` … `REVOKE UPDATE, DELETE ON ont_object_policies FROM console_rt;`); backend/crates/platform/db/migrations/0205_ont_policy_api_attach_writer.sql:170-171 (writer gets SELECT, INSERT only); asserted as a wire contract at backend/openapi/openapi.yaml:12273-12275 ("An attachment cannot be revoked")`
- **Collision:** P6 requires effective-dated grants so authority is REPLAYABLE, which means a grant must be endable. Here the only mechanism that makes an object type's instances visible at all is an append-only attachment with no UPDATE or DELETE grant to any runtime role, and the OpenAPI description publishes irrevocability as the contract. Failure mode: an admin attaches an over-broad permit on the no-code canvas; there is no API and no SQL privilege that undoes it, so the mistake is permanent for the tenant and the only remediation is a migration. A tombstone-plus-read-path scheme is possible, but it changes `applicable_object_policies` on every read of every type and makes the published "cannot be revoked" sentence false.

### A required preflight gate asserts exactly one jurisdiction and that every control stays HOLD

- **Patterns:** P2, P13, P8
- **Incumbent:** validate-console-truth-ledger.mjs jurisdiction validation
- **Evidence:** `scripts/console/validate-console-truth-ledger.mjs:290 (`if (targets.length !== 1 || targets[0] !== 'KR' || jurisdictionRows.length !== 1 || jurisdictionRows[0]?.id !== 'JUR-KR-001' …) fail('jurisdiction target must be exactly KR / JUR-KR-001')`) and :294 (`if (control.release_disposition !== 'HOLD') fail(...)`); run in the job everything else needs at .github/workflows/ci.yml:127`
- **Collision:** This is the same shape as the command-database gate: it asserts the CURRENT state as a contract, in the preflight job that gates the entire pipeline. Two consequences. First, the omni thesis (many verticals, and eventually more than one country) cannot register a second jurisdiction row — failure mode: adding JUR-JP-001 to the register fails a required preflight and every downstream job is skipped. Second, no control may ever be recorded as satisfied by this gate's own rule, so a pattern that touches 근로기준법 (P2's 파견/도급/일용 employment types) or a regulated accounting close (P13) can be built but can never be marked releasable inside this machinery. It also means no plan may propose unholding as a step.

### A required preflight test asserts the console frontend does not exist

- **Patterns:** P7, P15, P16, P3
- **Incumbent:** route-inventory.test.mjs empty-state contract + console-capability-registry route_presentation
- **Evidence:** `scripts/console/route-inventory.test.mjs:36-42 ("the tree under test holds no console route source", failing with "candidate tracks a console route source again") and :44-55 (`assert.deepEqual(declared, [], 'registry declares route keys but the candidate has no console route source')`); run at .github/workflows/ci.yml:136-137`
- **Collision:** P7's no-code canvas, P3's tree/canvas org editor, P15's simulate-before-commit UI and P16's realtime visibility are all user-facing surfaces, and 'manageable without developers' means the surface IS the product. Test 1 is escapable — it checks only the two TypeScript paths web/src/console/shell/nav.ts and web/src/console/screens/registry.ts (route-inventory.mjs:4-5), which a Leptos rebuild would not create. Test 2 is not: it asserts that NO capability in docs/program/console-capability-registry.json claims source_mounted / production_exposed / registry_body_present / nav_declared and that route_keys is empty everywhere. Failure mode: shipping the canvas and honestly recording it as mounted fails a required preflight; recording it as not-mounted keeps the gate green while lying about the shipped state, which is exactly the failure the register exists to prevent.

### regions, branches and users are audited tables that no migration may drop

- **Patterns:** P1, P3, P5
- **Incumbent:** console-gate-migration-safety built-in audited-table list
- **Evidence:** `backend/ci/gates/migration-safety/src/lib.rs:164-172 (`built_in_audited_tables() = [audit_events, regions, branches, users, user_branches]`), :278-281 (`DROP TABLE touches audited table`), :306-309 (`ALTER TABLE DROP COLUMN touches audited table`); run at .github/workflows/ci.yml:430-431; the mandatory-branch premise is ADR-0003 (docs/decisions/ADR-0003-branchscoped-authorization-model-nonnull-branch-scope.md, status accepted)`
- **Collision:** P3 replaces the flat regions/branches scheme with org-unit KIND and LIFETIME; P1 moves person identity above the org-scoped users row. Both are additive-only here. Failure mode: the migration that drops `regions` after org-units subsume it fails the gate as DropAuditedTable, and the migration that removes the now-duplicated identity columns from `users` after a `party` table owns them fails as DropAuditedColumn. So the flat two-level scheme and the duplicated person columns are permanent, every operational row keeps its non-null branch_id per ADR-0003 forever, and P5's split of decision scope from branch_id can only add a second dimension — it can never relieve the first. Worth naming as cost rather than defeat, but 'the old model is undeletable' is an invariant the patterns have to live inside.

### A platform-level party table has only two homes, and both break something

- **Patterns:** P1
- **Incumbent:** console-gate-tenant-isolation table classification
- **Evidence:** `backend/ci/gates/tenant-isolation/src/lib.rs:44-62 (`global_table_allowlist`), :108-131 (`owner_only_table_allowlist`, "a direct GRANT to console_rt/PUBLIC is a violation"), :657-680 (`check_owner_only_table_grants` → ViolationKind::OwnerOnlyTableGrant), :762-776 (UnclassifiedTable for any new table with neither org_id nor an allowlist entry); run at .github/workflows/ci.yml:433-434`
- **Collision:** P1 wants one human to exist once across companies with tenant visibility mediated by EDGES rather than by scoping the row — which means a `party` table with no org_id. The gate forces a choice. Global-allowlisted makes party rows readable by every tenant's runtime role, so a person's existence and identity leak across tenants — precisely the disclosure org-hierarchy.md FIX-1 was written to prevent. Owner-only classifies it but forbids any console_rt grant, so every person read must go through a SECURITY DEFINER resolver, RLS cannot mediate the visibility edges at all (the edges become procedural code, not policy), and the pattern's own premise — visibility by edges — is implemented outside the mechanism that enforces visibility. Failure mode either way is real: cross-tenant PII on one side, unpoliced DEFINER surface area on the other.

### The inter-org scope anchor is a closed five-value enum inside the JWT

- **Patterns:** P3, P5
- **Incumbent:** AccessScopeLevel as a token claim
- **Evidence:** `backend/crates/kernel/core/src/access_scope.rs:26-34 (`pub enum AccessScopeLevel { Group, Org, Region, Branch, Worksite }` with `serde::Deserialize`); backend/crates/platform/auth/src/jwt.rs:120 (`pub scope_level: Option<AccessScopeLevel>`); spec line docs/specs/org-hierarchy.md:172 (`AccessScope { level ∈ {Group,Org,Region,Branch,Worksite}, node_id }`), title line :1 fixes the whole chain as `Group → 법인 → Region → Branch → Worksite``
- **Collision:** P3 says org-unit KIND (부서/팀/TF/사업장/연락사무소/지사/본사) is an open set and P5 wants competence scope separate from structure. The scope anchor a principal carries is a compile-time enum serialized into the token. Failure mode: mint a token with scope_level:"tf" and serde Deserialize fails on the claim, so the request is rejected as an invalid token — an authored unit kind cannot be a scope anchor without a developer editing kernel/core and reissuing tokens, which is the exact 'developer required to add the next thing' the thesis disqualifies. Survivable by making every authored unit project down to level=Branch with a node_id, but then the token cannot express what kind of unit the authority came from, which is what P5 needs to record.

### A required gate pins the three-step work_order_approval_steps table and the linear ApprovalLine by name

- **Patterns:** P8, P9
- **Incumbent:** check:g005-workflow-lifecycle requiredSnippets
- **Evidence:** `scripts/check-g005-workflow-lifecycle.mjs:152-155 (each requiredSnippet must literally appear in the named file) with docs/benchmarks/g005-workflow-lifecycle-matrix.json:113 and :161 (`"work_order_approval_steps"`) and :152-153 (`"ApprovalLine"`, `"approve_next"`); run at .github/workflows/ci.yml:783-785. The pinned table is the one with `step_order SMALLINT CHECK (step_order BETWEEN 1 AND 3)` and `UNIQUE (work_order_id, role)``
- **Collision:** P8 replaces linear approval with a 결재권 graph (three edge kinds, parallel 합의 branches, 전결, 대결, retroactive 반려 by an earlier-and-lower node) and P9 records the CAPACITY a signature was made under. The gate is presence-only, so a graph can be added — but the incumbent can never be removed while the gate stands, which means the 3-step maximum, the role-CHECK over the demo roles and the once-per-role UNIQUE all survive alongside it, and two approval engines have to agree. Failure mode: delete work_order_approval_steps and `approve_next` after the graph lands, and check:g005-workflow-lifecycle fails with 'file must include "work_order_approval_steps"'. Also note `approve_next` is the shape being pinned: a single next-actor pointer cannot represent parallel 합의 branches at all.

### A closed entity-type allowlist is a foundation-gate rule, pinned literally by another gate

- **Patterns:** P14, P2, P12
- **Incumbent:** foundation-gates.md Gate C.3
- **Evidence:** `docs/specs/foundation-gates.md:49 ("Entity-type allowlists are mandatory: employee data maps only to people/HR schemas, assets only to equipment/inventory schemas, sites only to location/customer-site schemas."); the sentence fragment is asserted present by scripts/check-g006-asset-dispatch-lifecycle.mjs:162 (`requireIncludes("docs/specs/foundation-gates.md", "assets only to equipment/inventory schemas", …)`), run at .github/workflows/ci.yml:807-809`
- **Collision:** P14 makes entity classes an open set that COMPOSE declared components — declaring a new class should light the systems up without hand-written integration. Gate C.3 says the opposite: data of a known kind may only map to a fixed set of named schemas. Failure mode is doubled. Substantively, a newly authored entity class ('파견 계약 단위', 'lot') has no allowlisted schema, so import/export for it is out of contract. Mechanically, you cannot even soften the sentence: check:g006 asserts the exact substring 'assets only to equipment/inventory schemas' still appears, so rewording the rule fails a required gate that is nominally about equipment dispatch. Gate C.4 (:50) separately declares MES scope future-gated, which is where P12's lot/genealogy lineage lives.

### The policy condition-attribute vocabulary is a closed CHECK and a gate pins its literals

- **Patterns:** P5, P6, P3
- **Incumbent:** policy_role_conditions.attribute CHECK + check:g004 requiredSnippets
- **Evidence:** `backend/crates/platform/db/migrations/0065_create_policy_roles.sql:110-128 (18-literal `CHECK (attribute IN ('group','tenant','organization','org','department','team','position','employment_status','assignment','location','site','branch','device_posture','purpose','action','resource','sensitive_action'))`); the literals `'department'`, `'position'`, `'employment_status'`, `'group'`, `'tenant'`, `'organization'`, `'team'`, `'action'`, `'resource'`, `'sensitive_action'` are each asserted present by scripts/check-g004-identity-foundation.mjs:124-126 via docs/benchmarks/g004-identity-foundation-matrix.json:170-184; run at .github/workflows/ci.yml:779-781`
- **Collision:** P5 introduces COMPETENCE (전담) as a third relation — which unit may DECIDE what — separate from control and structure, and P6 folds grants from position/group/direct/delegation/assignment sources. There is no competence, decision-scope, org-unit-kind or delegation attribute in the closed list, and `operator` is closed to equals/not_equals/in (0065:129) so no band or interval predicate exists either. Failure mode: authoring a condition on a competence scope raises a check_violation on INSERT. The set can be extended by migration (additive, so the gate keeps passing), but it cannot be REPLACED with an authored attribute registry, because check:g004 requires 'department'/'position'/'employment_status' to remain literally in the migration text — which also freezes the assumption that department and position are attributes of a person rather than entities in their own right, the exact conflation P2 and P3 undo.

### The accounting spec explicitly excludes the general ledger P13 requires, and fixes one currency

- **Patterns:** P13, P12, P11
- **Incumbent:** docs/specs/accounting.md scope and invariants
- **Evidence:** `docs/specs/accounting.md:28 ("Excluded until follow-up slices: Persistent GL tables, fiscal periods, closing, reporting statements, and web UI."), :35 ("Accounting numbers are in integer KRW; no floating-point money."), :29 (multi-currency excluded), :3 (production financial statements blocked "until a licensed 세무사 validates golden cases and signs the release gate")`
- **Collision:** P13 is ONE economic spine with the business object as a dimension so cost/revenue/profit are queries. Its minimum parts — an account master, fiscal periods, a close — are the named exclusions of the governing spec, and the shipped finance-gl is two tables with a plain-TEXT account_code, no accounting date, no dimension and `amount_won BIGINT` (0160:62-64) hard-coding the currency into the column name. Failure mode for the currency invariant is concrete and already in the wire contract: an omni platform's first non-KRW subsidiary has nowhere to put the amount, and every generated client field is named *_won. Failure mode for the exclusion is governance: any PR that adds chart_of_accounts contradicts the spec it must cite, and :3 plus the KR-only HOLD register mean the close it enables cannot be declared releasable regardless.

### The six roles are a published wire enum behind a byte-exact drift gate

- **Patterns:** P7
- **Incumbent:** OpenAPI PlatformTenantRole + check:openapi-app *(gate since deleted — see the stale-citation note below)*
- **Evidence:** `backend/openapi/openapi.yaml:19491-19498 (`PlatformTenantRole: type: string enum: [SUPER_ADMIN, ADMIN, MECHANIC, RECEPTIONIST, EXECUTIVE, MEMBER]`); enforced byte-for-byte against the running binary by scripts/check-openapi-app.mjs:23 and :72-74 (`if (actual !== expected) throw new Error("App-served OpenAPI YAML differs")`), run at .github/workflows/ci.yml:899-901. The yaml is compiled in via backend/app/src/lib.rs:214 `include_str!``
- **Stale citation (2026-08-01):** `scripts/check-openapi-app.mjs` and its `ci.yml` step no longer exist. Comparing the served bytes to the file was tautological precisely because of the `include_str!` this entry cites, so the byte-exactness the collision rests on was never enforced by that script. The surviving route-inventory half is `check:platform-contract-drift`; `openapi_drift` (`ci.yml:597`) is the gate that actually runs.
- **Collision:** P7 deletes the compile-time role/feature model. The owner states these six are demo scaffolding that never modelled a real organization — but they are published as a typed enum on the platform API, and the platform-tier docs describe authority in role names throughout (openapi.yaml:709, :741, :4930, :9418 and ~25 more). Failure mode: removing MECHANIC/RECEPTIONIST is a breaking change for every generated typed client that exhaustively matches the enum, and any change at all must land in the same commit as the Rust change or the drift gate fails. Cheap to fix mechanically, expensive to fix honestly: the role names are load-bearing in the human-readable authority descriptions that other gates pin (see the foundation-gate phrase collision).

### The Cedar cutover contract forbids the materialized half of P16's question

- **Patterns:** P16, P6
- **Incumbent:** cedar-pbac-cutover.md invariant 3 + the coexistence map cachePolicy
- **Evidence:** `docs/specs/cedar-pbac-cutover.md:28-29 ("Compiled bundle cache only. v1 may cache parsed/validated Cedar policies, schema, entity templates, and bundle metadata by immutable keys. v1 must not cache allow/deny decisions across requests."); docs/specs/cedar-pbac-coexistence-map.json `"crossRequestAllowDecisionCache": false`; docs/specs/rbac-configurable.md:57-60 permits caching keyed by (org_id, policy_version) only, with a synchronous bump per RoleManage write`
- **Collision:** P16 asks for a DECIDED answer on fold-on-demand versus materialized-with-invalidation. The governing contract has already decided against materialization for v1: a stored effective-authority projection is a cross-request allow/deny cache by another name. Failure mode if violated: a revoke does not take effect globally, which is the exact H1 finding rbac-configurable R4 was written to close — so this is a constraint to respect rather than attack. The honest reading is that P16's answer is forced to fold-on-demand over a policy-version-keyed policy cache, and any 'realtime authority board' has to be a live fold, not a table. That is a real design constraint the pattern set should state rather than leave open.

### Approval gates are four booleans and instance lifecycle is five states, for every object type

- **Patterns:** P8, P3, P15, P14
- **Incumbent:** OpenAPI GateChainConfig and InstanceLifecycleState
- **Evidence:** `backend/openapi/openapi.yaml:32120-32127 (`GateChainConfig` with `required: [authority, self_checklist, four_eyes, egress_dlp]`, all booleans), :32107-32108 (`InstanceLifecycleState: enum [draft, active, locked, archived, disposed]`), :32100 (`backing_kind: enum [instance, projected]`); the preflight endpoint is described as gate-chain-only at :12502-12504`
- **Collision:** P8 needs 결재 routing as a 전결규정 lookup resolving above/laterally/below with three edge kinds and parallel 합의 branches; four booleans cannot express an ordering, a capacity, a band, or a branch. P3 needs a temporary unit's LIFETIME with disband and transfer-to-new-post as DISTINCT terminal transitions; the wire enum gives every authored type the same five states with one terminal (`disposed`), so 'disbanded' and 'transferred out' are indistinguishable to any client. Failure mode: an authored entity class cannot declare its own lifecycle through the API at all, so P14's 'declare a class, systems light up' stops at the lifecycle boundary. Adding enum values is additive on the server but breaking for exhaustive typed clients, and every change must pass the byte-exact drift gate plus the schema-ordering assumption in scripts/check-ontology-write-precondition.test.mjs:58-59, which slices the document between `ObjectTypeSummary` and `InstanceLifecycleState`.

### gov_approvals is INSERT-only with one row per request, so a 결재 line cannot exist in the governance spine

- **Patterns:** P8, P9
- **Incumbent:** gov_approvals append-only triggers + UNIQUE (org_id, request_ref) + revoked UPDATE grant
- **Evidence:** `backend/crates/platform/db/migrations/0153_create_governance.sql:76 (`UNIQUE (org_id, request_ref)`), :125-130 (`CREATE TRIGGER trg_gov_approvals_no_update BEFORE UPDATE … governance_append_only_record()` which unconditionally RAISEs, :83-88), :136 (`GRANT SELECT, INSERT ON gov_approvals TO console_rt`), :141 (`REVOKE UPDATE, DELETE ON gov_approvals FROM console_rt`)`
- **Collision:** P8 needs N nodes per request with three edge kinds, parallel 합의 branches, and retroactive 반려 by a node earlier and lower than the closer. gov_approvals permits exactly one immutable row per request_ref. Failure mode: insert decision='pending' (a value the CHECK at :72 admits) and the row can never be resolved — UPDATE raises 'append-only governance record gov_approvals forbids UPDATE' — and a second INSERT for the same request_ref violates the UNIQUE. Building the line in a new table means the four-eyes invariant enforced here (the DB CHECK at :74) no longer covers it, so the pattern lands at the cost of a broken invariant rather than not at all.

### A signature's legitimacy is checked on signer identity, not capacity, by a DB CHECK

- **Patterns:** P9, P8
- **Incumbent:** gov_approvals CHECK (approver_id <> requested_by)
- **Evidence:** `backend/crates/platform/db/migrations/0153_create_governance.sql:74 (`CHECK (approver_id <> requested_by),          -- self-approval blocked`) with composite FKs at :77-78 pinning both to (id, org_id) in users`
- **Collision:** P9 records signer + authorizing grant + scope, and P8 admits 전결 and 대결. Both permit the same human to appear twice in a line under different capacities — routine in a small 사업장 or where the 전결권자 is also the raising 팀장. The CHECK makes that row unrepresentable at the storage layer, so no application logic, delegation record or capacity column can permit it: the INSERT is rejected by the check constraint. The composite FKs additionally forbid a group-level or position-level approver, so a capacity that is not a single org-scoped user row has nowhere to go.

### org_change is a third approval engine with a third closed role vocabulary and a third hardcoded step ceiling

- **Patterns:** P8, P3
- **Incumbent:** org_change_approval_steps
- **Evidence:** `backend/crates/platform/db/migrations/0198_create_org_change.sql:56-67 (`step_order SMALLINT NOT NULL CHECK (step_order BETWEEN 1 AND 8)` at :57, `role_key TEXT NOT NULL CHECK (role_key IN ('hr','finance','legal','executive'))` at :58, `UNIQUE (org_id, request_id, step_order)` at :64, `FOREIGN KEY (decided_by, org_id) REFERENCES users(id, org_id)` at :66)`
- **Collision:** This is the newest approval table in the repo (migration 0198) and it repeats every shape P8 rejects, in a fourth vocabulary the verified facts do not name: four literal role keys, a numeric ceiling (8 here, 3 in work_order_approval_steps 0008:59), a single ordered chain with no parallel branch and no edge kind, and no capacity column. A generic 전결규정 resolver must therefore replace three mutually incompatible approval tables, and the newest one shows the pattern of each domain minting its own is still active — so the collision compounds with every new vertical rather than staying fixed.

### Org-change knows one flat ORG_UNIT kind, no unit lifetime, and no transfer-versus-disband distinction

- **Patterns:** P3
- **Incumbent:** org_change_requests CHECK vocabularies and column set
- **Evidence:** `backend/crates/platform/db/migrations/0198_create_org_change.sql:19 (`kind TEXT NOT NULL CHECK (kind IN ('NEW','REORG','DISSOLVE'))`), :23 (`target_kind TEXT NOT NULL CHECK (target_kind IN ('ENTITY','REGION','BRANCH','SITE','ORG_UNIT'))`), :26 (`effective_date DATE NOT NULL` — the only temporal column; no end/expiry), :74-75 (`item_key TEXT NOT NULL CHECK (item_key IN ('TRANSFER_EMPLOYEES','POSITIONS','COST_CENTERS','CLOSE_OPEN_DOCS','ASSETS','PAYROLL_SOCIAL_FINAL'))`)`
- **Collision:** P3 wants org-unit KIND (부서/팀/TF/사업장/연락사무소/지사/본사) and LIFETIME, with a temporary unit's lifetime derived from a contract, and disband distinct from transfer-to-new-post. Here all seven Korean unit kinds collapse to the single literal 'ORG_UNIT'; there is no sub-kind column and no second temporal column, so a TF whose lifetime ends with its contract has nowhere to record the end; and TRANSFER_EMPLOYEES is a BOOLEAN settlement checkbox at :76 hanging off DISSOLVE, i.e. transfer is a sub-step of disband rather than a distinct terminal transition. Failure mode for the pattern as stated: two different real transitions produce the same terminal status APPLIED on kind='DISSOLVE' and are indistinguishable downstream.

### org_id is immutable by trigger on ~100 tables including users, so nothing can move between legal entities

- **Patterns:** P3, P4, P1, P2
- **Incumbent:** enforce_org_id_immutable()
- **Evidence:** `backend/crates/platform/db/migrations/0031_runtime_role_and_immutable_org.sql:95-104 (function; `RAISE EXCEPTION 'org_id is immutable: cannot change tenant from % to % on % (id=%)'` at :98-100), attached to regions/branches/users/registry_* /work_orders at :106-129 (incl. `CREATE TRIGGER trg_users_org_immutable BEFORE UPDATE ON users` at :113) and to ~100 tables in total across the migration set (0198:118 and 0199:76 attach it by dynamic loop); regression-locked by backend/crates/platform/db/tests/rls_isolation.rs:362-401`
- **Collision:** P3's transfer-to-new-post, P4's group restructuring, and P2's 파견 (employer ≠ worksite) all imply a row whose controlling legal entity changes. Failure mode: `UPDATE branches SET org_id = …` raises the exception above — verified as intended behaviour by rls_isolation.rs:385-400, which asserts the abort even for an org the writer does not control. The only available path is delete-and-recreate in the target org, which mints a new id and therefore destroys exactly the identity continuity P3 needs to distinguish transfer from disband. This is extension-hostile rather than extension-friendly: the invariant is asserted in a test, so relaxing it flips a passing gate.

### Two entity-class registries, one runtime-writable and one migration-only, and the generic edge table FKs the wrong one

- **Patterns:** P14, P12, P10
- **Incumbent:** object_types global kind registry vs ont_object_types tenant registry
- **Evidence:** `backend/crates/platform/db/migrations/0102_create_object_types_and_links.sql:18-21 (`object_types (kind TEXT PRIMARY KEY …)`, global, no org_id — annotated as global reference data at :10), :47 (`GRANT SELECT ON object_types TO console_rt;` — SELECT only), :56 and :59 (`src_kind TEXT NOT NULL REFERENCES object_types(kind) ON DELETE RESTRICT`, same for dst_kind); versus backend/crates/platform/db/migrations/0152_create_ontology_registry.sql:156 (`GRANT SELECT, INSERT, UPDATE ON ont_object_types TO console_rt`)`
- **Collision:** P14 requires declaring a new entity class at runtime. It is possible in ont_object_types and impossible in object_types: console_rt cannot INSERT a kind, so every new class in the generic edge substrate is a migration. Failure mode: a canvas that declares a class in ont_object_types and then writes an object_links edge for it gets the FK rejected at :56/:59 because the kind is not in object_types. The two registries have no bridge — to_object_type_id is absent from openapi.yaml per the verified facts, and object_links keys on TEXT kinds with no FK to ont_object_types — so the no-code path and the generic-graph path cannot see each other's classes.

### object_links permits at most one edge of a type between a pair, so a split/merge DAG cannot be expressed even ignoring quantity

- **Patterns:** P12, P13
- **Incumbent:** object_links UNIQUE constraint
- **Evidence:** `backend/crates/platform/db/migrations/0102_create_object_types_and_links.sql:68 (`UNIQUE (org_id, src_kind, src_id, dst_kind, dst_id, link_type)`), with src_id/dst_id as unindexed opaque TEXT at :57 and :59, and GRANT SELECT, INSERT, DELETE only at :86 (no UPDATE)`
- **Collision:** P12 needs a quantity-bearing DAG where children sum to the parent. The verified facts note object_links is quantity-free; the additional stop is that the same pair cannot carry two 'derived_from' edges, so two successive partial draws from one lot, or two split events with different quantities, are the same forbidden row. Failure mode: the second edge INSERT violates the unique index. And because console_rt has no UPDATE on the table, an edge cannot be amended — only deleted and re-inserted, which destroys the audit line P12's conservation proof depends on.

### Money is pinned to KRW by single-value CHECK constraints in five tables

- **Patterns:** P13, P4
- **Incumbent:** currency CHECK (currency = 'KRW') and amount_won column naming
- **Evidence:** `backend/crates/platform/db/migrations/0172_create_employee_employment_profiles.sql:10 (`currency TEXT NOT NULL DEFAULT 'KRW' CHECK (currency = 'KRW')`), 0178_create_facilities_pilot.sql:84, 0179_create_logistics_pilot.sql:68 (`currency_code TEXT NOT NULL CHECK (currency_code = 'KRW')`), 0182_create_equipment_3r.sql:35, 0187_create_recruiting.sql:86`
- **Collision:** P13 wants ONE economic spine with the business object as a dimension; P4 admits joint ventures and nesting, which in practice means a non-Korean subsidiary. A single-value CHECK is the strictest possible form — it is not an extensible vocabulary, it is an assertion that the column is constant. Failure mode: recording a USD offer, a foreign lease cost or a foreign payroll line is rejected by the check constraint in whichever of the five tables it lands in. Combined with finance_gl_voucher_lines.amount_won (0160:64), where the currency is in the column NAME, the spine has no currency dimension to add — it has a naming decision to undo.

### The migration-safety CI gate forbids dropping columns from users, regions, branches and every audited table

- **Patterns:** P1, P2, P3, P10
- **Incumbent:** console-gate-migration-safety, a required CI job
- **Evidence:** `backend/ci/gates/migration-safety/src/lib.rs:164-172 (`built_in_audited_tables()` → audit_events, regions, branches, users, user_branches), :287-312 (`check_drop_column` raises DropAuditedColumn for ALTER TABLE … DROP COLUMN on any audited table), :262-285 (DropAuditedTable), :174-187 (any `-- console-gate: audited-table X` comment adds X to the set); run at .github/workflows/ci.yml:431 (`cargo run -p console-gate-migration-safety`)`
- **Collision:** P1 puts identity above users, P2 splits position out of the employee row, P3 replaces the flat regions/branches scheme, P10 replaces email_threads' hardcoded work columns. Each is naturally expressed as a column removal, and each trips this gate: the job fails and the PR is blocked. The useful boundary — this is what distinguishes must-change from must-extend — is that the gate checks only DROP TABLE and DROP COLUMN. It does NOT inspect DROP CONSTRAINT, so relaxing a composite FK or replacing a CHECK vocabulary is CI-clean. So every CHECK-vocabulary collision in this list is extension work; every column-removal collision is a gate to renegotiate, and the columns must be left in place and deprecated instead.

### Inbound email links to work through two hardcoded nullable FK columns on an audited table

- **Patterns:** P10, P14
- **Incumbent:** email_threads.linked_work_order_id / linked_customer_id
- **Evidence:** `backend/crates/platform/db/migrations/0053_create_comms_webmail.sql:94-95 (`linked_work_order_id UUID REFERENCES work_orders(id) ON DELETE SET NULL,` `linked_customer_id UUID REFERENCES registry_customers(id) ON DELETE SET NULL`), supporting partial indexes at :195-198, and the audited-table marker at :81`
- **Collision:** P10 requires inbound email to link deterministically to 업무 as a first-class entity. The schema offers a per-target column, not an edge: exactly two link targets exist, and adding a third entity class means a migration adding a third column — precisely the developer-per-vertical dependency the thesis forbids. Failure mode for the clean fix: replacing the two columns with a (kind, id) edge requires DROP COLUMN on email_threads, which is an audited table (:81), so console-gate-migration-safety fails the build. ON DELETE SET NULL also means the link silently vanishes when the target is deleted, so '인계 완료 is a computable graph query' would be computing over links that can disappear without a trace.

### todos is person-keyed by a NOT NULL composite FK with opaque JSONB links

- **Patterns:** P10, P11
- **Incumbent:** todos table — the closest existing thing to 업무
- **Evidence:** `backend/crates/platform/db/migrations/0108_create_todos.sql:16 (`owner_user_id UUID NOT NULL`), :29 (`FOREIGN KEY (owner_user_id, org_id) REFERENCES users(id, org_id) ON DELETE RESTRICT`), :21-22 (`links JSONB NOT NULL DEFAULT '[]'::jsonb CHECK (jsonb_typeof(links) = 'array')` — no FK, no index), :23 (`done BOOLEAN NOT NULL DEFAULT false`), :34-38 (index on owner)`
- **Collision:** P10 inverts exactly this: artifacts and actions link to the WORK, not the person, so handover is re-pointing one edge. Here the work IS a person's row. owner_user_id is NOT NULL and composite-FK'd to an org-scoped user, so a work item can be owned by neither a position, a group, nor nobody — handover is an UPDATE that rewrites the row's owner and leaves no prior-owner record (no history table, and the index at :34 is owner-keyed). `links` is opaque JSONB with no referential integrity and no index, so the '인계 완료 is a computable graph query' requirement has nothing joinable to compute over, and P11's projections would have to parse JSONB. `done BOOLEAN` also gives no room for P10's partial-scope handover.

### role_manage can never be carried by a tenant-authored role, so the no-code authority canvas cannot bootstrap itself

- **Patterns:** P7, P6
- **Incumbent:** custom_role_runtime_feature_allowed
- **Evidence:** `backend/crates/platform/authz/src/lib.rs:1387-1396 (`fn custom_role_runtime_feature_allowed(feature: Feature) -> bool { !matches!(feature, Feature::RoleManage | Feature::ElevatedRoleGrant | Feature::OrgWideQueueTriage | Feature::AuditStreamRead | Feature::AuditStreamAccessLogRead) }`), applied at :1373`
- **Collision:** P7 deletes the compile-time Feature matrix and makes roles authored data. But the authority to author roles (role_manage) and to grant elevated roles (elevated_role_grant) is hardcoded as non-delegatable to custom roles, so it can only be held via a built-in role from the demo six the owner calls scaffolding. Failure mode today: an org creates a custom role granting role_manage, the row saves and passes the FK, and the filter at :1373 drops the grant — the operator sees a saved role that silently cannot do the one thing it was created for. The pattern is implementable, but the ordering matters: the built-in roles cannot be deleted before the delegation of role_manage is reworked, or nobody can administer anything.

### Tenant force-removal is data-driven from single-column org_id FKs, so a platform-level party survives org deletion

- **Patterns:** P1
- **Incumbent:** platform_force_remove_direct_org_children
- **Evidence:** `backend/crates/platform/db/migrations/0196_platform_force_command_and_fk_closure.sql:111-152 (catalog-driven loop; `AND cardinality(fk.conkey) = 1` at :143 and `AND child_attr.attname = 'org_id'` at :144 select the deletion set), called from platform_force_remove_organization at :186`
- **Collision:** P1 puts a party row above the tenant boundary, so it has no org_id and no single-column FK to organizations. The catalog query therefore never selects it, and the tenant is force-removed while every party row and every visibility edge referencing it persists. Failure mode: after force-removing an org, the human's platform identity and the edge recording that they worked there remain readable — residual personal data in a codebase that, per the verified facts, has no erasure, DSR, retention-policy or purge table anywhere in 206 migrations and released the reserved evidence-retention slot empty (0201:25 is `SELECT 1;`). The pattern is implementable but its erasure story has to be built at the same time, not after.

### A real dry run exists but covers one of five authorization mechanisms

- **Patterns:** P15, P6
- **Incumbent:** cedar_pbac::authoring::simulate
- **Evidence:** `backend/crates/platform/authz/src/cedar_pbac/authoring.rs:706 (`pub fn simulate(policies: &[AuthoredPolicy], request: &SimRequest) -> SimulationOutcome`), documented as the single evaluator behind /policy/simulate and /policy/authorize at :19-20 and :695, over the five-verb closed vocabulary at :246-252 enforced at :293-298 (re-verified this session)`
- **Collision:** P15 asks for a dry run for authority and 전결규정 changes, given preflight is not one. A what-if evaluator does exist — but its signature takes only authored Cedar policies, so it cannot fold the Feature matrix (compile-time), policy_roles/user_role_assignments, group_role_grants or clearance_assignments, and its verb set has no schema-mutation or approval-routing action. Failure mode: simulating a change to a custom role's conditions or a group grant returns a decision computed from Cedar policies alone, i.e. an answer that is confidently wrong about the four mechanisms that actually gate most routes. This makes P15 an extension of an existing evaluator rather than new construction, but only after the other four mechanisms have a single fold to simulate against — which is P6.

### Realtime has three event variants, three NOTIFY channels with a silent default arm, and no fan-out dimension above branch

- **Patterns:** P16, P5, P4
- **Incumbent:** RealtimeEvent enum and the PgRealtimeHub listener dispatch
- **Evidence:** `backend/crates/platform/realtime/src/lib.rs:317-336 (`pub enum RealtimeEvent { MessagePosted, MessageAcked, NotificationCreated }`), :593-605 (match on `notification.channel()` over the three consts at :37-39 with `_ => Ok(())` at :604 — an unknown channel is DISCARDED, only the payload-handling error path logs at :606), :359-372 (fan-out keys are branch_id and thread_id only), :40 (`NOTIFY_PAYLOAD_LIMIT_BYTES: usize = 8000`)`
- **Collision:** P16 wants realtime visibility of authority and work state. There is no variant and no channel for either, and the silent `_ => Ok(())` means a newly emitted NOTIFY channel produces no event and no warning — the hardest possible failure to diagnose. The addressing dimensions are branch_id, thread_id and recipient user id, so a subscription scoped to a P5 competence or a P4 group has nothing to key on. Usefully, the 8000-byte NOTIFY ceiling at :40 settles P16's open question in one direction: a folded authority set cannot fit in a payload, so the transport must be a materialized-with-invalidation ping, not a fold pushed over the wire.

### 직책 and 직위 were merged into one free-text employees.position column at import

- **Patterns:** P2, P3
- **Incumbent:** employees.position and employees.company as denormalized TEXT
- **Evidence:** `backend/crates/platform/db/migrations/0066_hr_core_employee_fields.sql:10 (`ADD COLUMN position TEXT`), :26 (`position = COALESCE(NULLIF(btrim(raw_row->>'직책'), ''), NULLIF(btrim(raw_row->>'직위'), ''))`), :51-52 (`CREATE INDEX employees_org_unit_position_idx ON employees (org_id, company, org_unit, position)`)`
- **Collision:** P2 separates the employee from the position they occupy. The schema has no position entity at all — position is a TEXT column on the spreadsheet-import row — and worse, the backfill at :26 COALESCEs two distinct Korean concepts (직책 = post/duty, 직위 = rank) into one column, preferring 직책 and discarding 직위 whenever both are present. The distinction P2 needs was destroyed at ingest and survives only inside raw_row JSONB, so extracting a position entity from existing data cannot be a pure schema migration — it requires re-deriving from raw_row per tenant. Note also `company` in the index at :52: the legal entity is likewise a free-text string on the employee, which is precisely what P4 needs as a first-class node.

## MINOR (8)

### ADR-0023 defers the no-code canvas that P7 and P15 depend on

- **Patterns:** P7, P15
- **Incumbent:** ADR-0023 follow-ups named out of scope, still current authority under ADR-0025
- **Evidence:** `/Users/jasonlee/Developer/console/docs/decisions/ADR-0023-oyatie-console-authority.md:152-154 ("No-code policy/workflow visual canvas (DESIGN §4.6 makes it the baseline; this program ships read-only NL rows + simulation and defers the canvas)") and :155-157; fidelity gate at /Users/jasonlee/Developer/console/docs/decisions/ADR-0025-carbon-copy-console-shared-platform-spine.md:92-99`
- **Collision:** P7 requires roles and the action vocabulary to be configurable from a no-code canvas and P15 requires a simulate-before-commit surface for authority and 전결규정 changes. ADR-0023:152-154 records the canvas as an out-of-scope follow-up needing its own charter, and ADR-0025:92-99 makes any material divergence from the mirrored prototype fail the fidelity gate unless classified as an accessibility correction, a stronger design-charter rule, or a named positive polish improvement. This is friction rather than prohibition — ADR-0025:101-103 explicitly allows build-side state captures where the prototype has no target frame — but a canvas cannot be smuggled in as part of another slice: it needs the named charter ADR-0023:154 says it needs, and every new screen state needs classified build-side visual proof.

### A work ledger projected from audit_events keys on an unversioned free-text action code

- **Patterns:** P11
- **Incumbent:** simplicity: audit_events is immutable, carries before/after snapshots, and is written in the same tx as every mutation — the right substrate
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0003_create_audit_events.sql:16`
- **Collision:** P11's instinct is correct and the substrate exists: audit_events with before_snap/after_snap (0003:22-23), immutable by REVOKE plus trigger (0003:34-40). The flaw is the join key. action is `TEXT NOT NULL CHECK (action ~ '^[a-z0-9_]+(\.[a-z0-9_]+)+$')` (0003:16) — a regex, not a vocabulary table, and target_type/target_id are bare TEXT (0003:17-18). Demonstrated failure mode: a metric defined as 'count of work.handover_complete' is deployed; six months later the Rust action code is renamed during a refactor, the regex still passes, no FK complains, and the metric silently loses every prior row — a reporting regression with no error and no test that would catch it, since the projection is a query not code. The fix is small and belongs with P11: an action-code catalog table with an FK, mirroring what feature_catalog (0065:12) already does for features. Without it, 'projections of data already obliged' obliges data whose meaning is not pinned.

### period_locks' domain vocabulary is a closed CHECK, so open entity classes cannot be period-controlled

- **Patterns:** P13, P14
- **Incumbent:** simplicity: two domains, closed, because only two exist
- **Evidence:** `/Users/jasonlee/Developer/console/backend/crates/platform/db/migrations/0107_create_period_locks_versioning_lifecycle.sql:34`
- **Collision:** period_locks.domain is CHECK (domain IN ('payroll', 'accounting')) at 0107:34. P14's premise is that entity classes and cross-cutting concerns are OPEN sets and that declaring a class lights up the concerns it composes. 'Period control' is exactly such a concern, and it is closed at the DDL level, so a new vertical's freeze window requires a migration — a developer — which is the thing the thesis forbids. Demonstrated failure mode: a tenant declares a new entity class that needs monthly close; there is no domain value to lock it under, so the lock is either recorded under 'accounting' (wrong scope, and unlocking accounting now unlocks the new class too) or not recorded at all. This is minor as a fix — one CHECK becomes an FK to a domain catalog, the same shape feature_catalog already uses — but it is worth naming because it is representative: every closed CHECK in 206 migrations is a place where P14's promise quietly requires a developer, and enumerating them is a prerequisite to claiming the concern model is open.

### A required gate pins the OpenAPI sentence that says authority is feature-plus-branch

- **Patterns:** P5, P7
- **Incumbent:** check:foundation-gates openapi phrase assertions
- **Evidence:** `scripts/check-foundation-gates.mjs:207 (`requireIncludes("backend/openapi/openapi.yaml", "tenant RLS, feature authorization, and branch scope", "approval feed authz/RLS contract")`) against backend/openapi/openapi.yaml:194; also :210 pinning "Append-only Policy Studio audit evidence" against :29380; run twice at .github/workflows/ci.yml:123 and :768-770`
- **Collision:** P5 splits decision scope from the mandatory operational branch_id and P7 removes 'feature authorization' as a concept. The published description of the approval feed names exactly the three mechanisms being replaced, and a required gate asserts that sentence still appears verbatim. Failure mode: rewording the description to say 'authority is resolved from grants' fails check:foundation-gates, which runs in the preflight job that every other job needs — so a documentation change blocks the pipeline. Small in isolation; worth listing because it is the pattern of this repo: prose is contract, and there are at least five such pins in that one gate.

### Notice audience is a closed two-value wire enum, required on every notice

- **Patterns:** P10, P8
- **Incumbent:** NoticeSummary.audience_scope
- **Evidence:** `backend/openapi/openapi.yaml:32448-32449 (`audience_scope` and `audience_branches` in `required`) and :32478-32480 (`audience_scope: type: string enum: [org, branches]`); backing DDL is 0197 over 0162_create_notices.sql, whose notice_receipts has no body column and only `acknowledged_at` (:46)`
- **Collision:** P10 makes 업무 first-class and addresses artifacts and actions to the WORK. A notice cannot be addressed to a work item's line members, a party, or a role: the only granularities in the wire contract are the whole org or a branch list, and both fields are required so a client cannot omit them. Failure mode: 'notify this work item's current 결재 line' has no representable audience, so it degrades to a branch-wide broadcast — which also defeats P10's claim that 인계 완료 is a computable graph query, since the notice carries no edge to the work. Adding a third enum value is additive server-side but breaking for typed clients, and the ack-only receipt shape means the originator-closure state P10 implies has nowhere to live either.

### A unit test pins the exact number of compile-time features

- **Patterns:** P7
- **Incumbent:** platform-authz policy test
- **Evidence:** `backend/crates/platform/authz/tests/policy.rs:849-850 (`assert_eq!(Feature::ALL.len(), 96); assert_eq!(matrix.len(), Feature::ALL.len());`); run as a required suite at .github/workflows/ci.yml:658-660`
- **Collision:** P7 deletes the matrix. This assertion, and the full `Role::ALL × Feature::ALL` golden grid that docs/specs/rbac-configurable.md:64 requires, become unsatisfiable rather than merely wrong. Failure mode is trivial — the test is deleted with the thing it tests — but it is worth recording as the count-shaped tripwire that tells you how much of the codebase treats the feature list as a fixed inventory: 96 entries, each with a hand-written six-cell row, and rbac-configurable R7 additionally makes `feature_catalog` an FK-referenced table seeded FROM `Feature::ALL`, so the data-driven layer's referential integrity currently depends on the enum existing.

### permission_level is a four-value lattice fixed by CHECK, and the fold has no place to put anything else

- **Patterns:** P6
- **Incumbent:** policy_role_permissions.permission_level
- **Evidence:** `backend/crates/platform/db/migrations/0065_create_policy_roles.sql:93 (`permission_level TEXT NOT NULL CHECK (permission_level IN ('deny','request_only','limited','allow'))`), resolved through backend/crates/platform/authz/src/lib.rs:1372 (`PermissionLevel::from_str(&row.permission_level).ok()?`) with the ordering in `satisfies` at :863`
- **Collision:** P6 defines effective(person, scope) as fold(grants). A fold needs a lattice, and this one is fixed at four literals in the DB and mirrored by a Rust enum whose `from_str` again silently drops unknown values. A canvas cannot introduce a fifth level (say a co-sign or time-boxed level P8's 협조 edge would want) — the INSERT is CHECK-rejected, and were the CHECK dropped, the value would be dropped silently at lib.rs:1372. This is genuinely only friction: a four-level lattice is probably enough, and the fix if it is not is a DROP CONSTRAINT plus an enum variant, neither of which trips the migration-safety gate.

### The CI preflight gate pins ci.yml as exact ordered literal text

- **Patterns:** P7, P14, P15
- **Incumbent:** check-ci-preflight.mjs
- **Evidence:** `scripts/check-ci-preflight.mjs:34-47 (`postgresDomainReachabilityCommands` — an ordered array of verbatim ci.yml lines including trailing backslashes), :49-71 (`postgresWrapperContracts` — a hardcoded map of wrapper name to buck target)`
- **Collision:** Any of these patterns lands with new Postgres integration tests, and each new test must be added to ci.yml, to the wrapper-contract map, and to the reachability command list in matching order, or the preflight gate goes red. Nothing about the patterns is blocked — this is the cost of admission, and it is worth naming because it is a third place to edit that a plan will forget, and the failure surfaces as an unrelated-looking gate failure rather than as a missing test.

