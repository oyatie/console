V2 corrects the two new ontology-rest controls to use axum::http::StatusCode. That crate has axum but no direct http dependency. The V1 current-interface negative control would not compile; this is test source correction, not genuine product RED. Every app byte and all compatibility recovery/wrapper bytes remain V1-identical. V1 is preserved, and V2 needs its own immutable independent review. No Cargo, DB or shared source mutation occurred.

V3 adds one independent-review-requested assertion that the original approval decision row survives renewal byte-for-byte, alongside the existing original consumption retention check. All prior app/source bytes are retained except that additive assertion; compatibility.patch remains V2-identical. The ordinary workflow Operation/non-unknown-log control remains an explicit future closure HOLD and is not added to the primary candidate. V3 needs its own immutable review.

V4 fixes a static fixture blocker identified by Fast: seed_org_rls_off derives globally unique organizations.slug from tag, so the two workflow status iterations require different tags. Derive each tag from prior_status. No assertion is weakened or removed; primary source otherwise V3-identical, compatibility.patch unchanged. V3 would fail with duplicate slug after the PENDING case; this is not product RED. Prior artifacts remain historical.
