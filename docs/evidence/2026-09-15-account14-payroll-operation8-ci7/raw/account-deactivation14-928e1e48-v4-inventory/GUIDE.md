# Account deactivation14 private test candidate V4

Owner of this private draft: `/root/inventory_repair`. Original private14 author: `/root/fast_gate_review`. Sole integration/source/schema/generated-file/runtime writer: `/root`. Base: `928e1e4815f5e6336f85667bc27529b5c4fe8b0c`. Immutable target: `candidate.patch` SHA256 b8c1845c5bd33ae5952c53165cd910ca7325e4f87ea1b501c1eb1aac60f2a04c for only `backend/crates/identity/adapter-postgres/tests/deactivate_revokes_credentials.rs`. Complete candidate source SHA256 e06b77f8841cf3e7f5bc595756c1b8933c136e9cbfae1a536fac2862375dc471. This packet requests independent test-source review; it is not implementation admission or runtime acceptance.

The accepted guard design is the exact GUIDE `19f29899b2c7bf3d6ac448c0daec73f0cdfba6f648458a86b044333c73ddb773` plus V2 supplement `fee7092299e1a758c3e26d4334efa56e0671dc9f2a57f0a81632a9dc7293f35e`, independently approved by Capture as a bounded guard/race-test authoring contract. `references.json` binds those files and the approval. The original14 directory contained three Rust drafts and no frozen test approval/runtime metadata; no additional approval is inferred from its presence.

The design remains: one Business transaction calls a fixed deactivation-specific Account-owner guard before effects; validate/lock Company-scoped users, then Account root FOR UPDATE; enforce READ COMMITTED before locks/effects; separately read the existing strict fence in a fresh post-lock VOLATILE statement. Any security row preserves Account custody regardless of state; successful absence alone permits the existing exact legacy sweep. Any authority failure refuses the Company transaction. Guard/profile upgrades require prior exact root certification and must not re-run backfill. Narrow users-column grants and Business-only EXECUTE do not grant raw Account/credential authority. No implementation, migration, native enrollment, Auth7 cutover, or production action is in this packet.

## V4 exact profile and users-ACL corrections

Fast's full14 V2 review (`review.json` SHA256 `9b6a215946fbf2bb659c47db1da7eb532f304328548394d357a2b9d04fb6a820`) found the original profile oracle could accept table-level users grants or column grant options despite matching the three named column privileges. Existing root certification explicitly excludes users ACLs (`account_custody_state.sql:88`), so root-profile success cannot cover this gap. V4 adds bounded assertions to the same existing profile test; it adds no test names.

The existing column-pair assertion remains unchanged. New checks require the exact three non-grantable column grants, reject every raw Account-owner/PUBLIC table grant, reject effective SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER table privilege, and inspect every live users column's effective rights: SELECT only id/org_id, UPDATE only id, no INSERT/REFERENCES or grant options. The existing owner NOLOGIN/non-BYPASS/nonmembership checks remain. These checks preserve the surrounding roles' distinct rights instead of prescribing a full users ACL for everyone.

The guard metadata additionally requires PL/pgSQL, function kind, exactly one public routine of that name, and the exact safe function configuration `ARRAY['search_path=pg_catalog, pg_temp']`. This value is bound to the existing custody-generator convention; Company users validation must not inherit function-local row_security=off. Existing volatility/owner/EXECUTE/type checks remain intact.

Two scoped drift controls mutate only the fixture: table SELECT to console_account_owner, and SELECT(id) WITH GRANT OPTION. Each runs inside a separate owner transaction, captures its altered catalog, installs a savepoint, invokes the real production finalizer SQL, rolls back to the savepoint, captures the retained drift, then rolls back the outer transaction. After cleanup, each requires actual P0001 refusal, proves the fixture differed from installed state, compares retained drift, and proves exact installed catalog/full row restoration. Successful self-repair cannot pass because success fails the mandatory refusal assertion. The rollback comparisons prove no surviving repair, not that no intermediate SQL statement ran before refusal. The old volatility-drift/refusal test remains.

A small catalog_connection helper reuses the existing exact JSON catalog query to observe uncommitted ACL fixtures; catalog(PgPool) delegates with one acquired connection. `profile-only.diff` shows all V4 source changes from immutable V3. V2's NOWAIT/57P01 witnesses and V3's short slug are unchanged. V4 supersedes V3 as the proposed test candidate; V2/V3 and failed evidence remain frozen. Independent full14 V4 review is pending; source formatting and private mechanics do not grant runtime or implementation approval.

## V3 fixture-only correction

Fast's continuing whole14 review found an inherited fixture prerequisite failure: the foreign-company slug at the guard-scope test used `guard-{UUID}`,42characters, against migration0026's exact maximum40. V3 changes only that prefix to `g-{UUID}`,38characters, retaining the entire UUID and all assertion semantics. `fixture-only.diff` is the complete source change from frozen V2. Static nil/all-f UUID controls confirm the old slug fails and the new slug matches the unchanged schema regex; this is not database execution. No schema or production source changed.

V2 remains immutable at manifest `b3206d922de29c1a8bf775e799c29d5484ebc79ceafd9194fedfbdf93463194a`; V3 supersedes its test candidate only for this fixture correction. The independent witness-only review SHA256 `8f3748b7222036674b3999bbd9f69fc3d96da27e0142ff794caf624d3e286c94` applies to the unchanged witness code, not automatically to the complete14-test candidate. Full14 review remains in progress. Any additional material review finding requires a new revision, preserving V2 and V3.

## Exact preservation and strengthening

- The current target is byte-identical to original6 at historical1cef and private c60: SHA256 `0514aeecc993a126f98be1b24ab9a5d6d8052c373a1abcfc9cf28e59a3e34684`,26083bytes. Removing only the appended guard_races module recovers all original6 bytes. Its26081-byte prefix stays exact; the outer closing brace is merely moved past the new module. Existing legacy assertions, expected timestamp/history deltas, typed Conflict and FamilyRevoked, snapshots, Account roots/security/events and audit checks are unchanged.
- The prior private14 source and `witness-only.diff` are retained in the referenced frozen V2 packet. That diff compares its formatted form to V2: every semantic change is an addition, confined to one new observation helper and the existing company_wins helper. The eight source test names are unchanged; no test is dropped, ignored, filtered, relaxed or newly invented in V2. Other prior14 source is identical after formatting, except the explicit V3 fixture correction below.
- Before either company-wins race, a separate owner transaction runs `SELECT id ... FOR UPDATE NOWAIT` against the subject's seeded WebAuthn row and must return its exact known UUID. It rolls back, releasing its own probe lock.
- While the real Business action is blocked on the exact held family row, repeat that actual credential NOWAIT probe. Capture its result while tasks/blockers are live; after all are released/joined/closed, require PostgreSQL Database SQLSTATE55P03. A missing/deleted-visible row, generic error, timeout, or permission failure cannot satisfy the witness. The native Account-security FK must still report the exact Business PID as its blocker.
- The backend-death branch retains the existing exact `pg_terminate_backend(business_pid,5000)` true assertion and generic error assertion, then additionally requires the original owner error to be `PgOrgError::Db(DbError::Sqlx(Database))` with SQLSTATE57P01. The owner transaction helper preserves the original closure error across best-effort rollback. A task cancellation, pool error, SQL cancellation57014, or lock timeout55P03 cannot masquerade as observed backend death.
- After death, the same NOWAIT probe must return the exact restored key and prove its row lock was released; the existing full snapshot equality still proves Account/Company/credentials/audits restored, plus the later native fixture row. After successful commit, the probe must return no key; the existing exact legacy custody delta and audit assertions remain decisive. NOWAIT observes row contention, while the source-bound real sweep places DELETE before the family UPDATE; it is not an independent statement-history recorder.

The added probe uses a separate privileged fixture transaction with a2s statement bound and explicit rollback. It creates no grants or credential mutations. Existing Business/Auth pools remain genuine restricted LOGINs. Security INSERTs remain privileged test fixtures modeling the native immediate FK; these are not proof of an enrollment API or native authenticated sessions. Existing race deadlines and joins remain bounded, and new assertions run after active blockers/tasks are cleaned up.

## Counts and evidence

This source declares14 tests:6 previously reviewed tests plus8 prior draft tests. V2 modifies the shared witness used by2 race cases and adds0 test functions. Rust compiler discovery0, Rust execution0, Cargo0, DB0 and Docker0 for this preparation. Rustfmt parser/format checks pass; static preservation checks and four private apply/check/inverse invocations pass. The frozen V2 `mechanical-verification.json` lists all14 source names; V4 keeps that exact roster and records its own commands/results. These are not14 passing tests.

The only imported runtime claim is historical original6: exactly6 discovered/executed,2PASS/4FAIL at1cef, as bound in `references.json`. Two custody cases first failed at423 after actual Company deactivation; the nonactive loop only reached PENDING_ENROLLMENT. Two unavailable-authority cases first failed at526 after real Auth55P03, with later return/restoration checks unreached. Both legacy positives passed. Preserve this evidence; neither revised8 nor later loop states have runtime proof here.

All five target/design preimages match clean928e1e48, cleane276 and clean2931 and their original approved-design hashes: the test, identity owner source, app custody-state SQL, custody generator, and generated finalizer. `source-bindings.json` records exact bytes/hashes. The Account patch is independent of the active root-owned payroll recovery closure; this packet acquires no runtime lease and grants no permission to interrupt it.

## Mechanical handoff

1. Independently review this exact source/patch and the accepted design; root records explicit immutable test-source approval. Reviewers must assess fault oracles, lock/snapshot ordering, complete historical preservation, ACL/profile expectations and cleanup. Private formatting/source checks do not replace that review.
2. Root verifies target preimage0514aee and actual clean HEAD, then imports only `candidate.patch`. Do not copy whole private fixtures, source snapshots or guide into the integration tree by convenience. Root owns any required source-attribute baseline/CI/authority updates and generated records.
3. Under root's serialized runtime lease, use the existing unfiltered target command: `bash tools/ci/cargo_needs_postgres.sh --only identity-adapter-postgres-deactivate-revokes-credentials-pg --num-threads 1`. The underlying expected command is `cargo test --locked --manifest-path backend/Cargo.toml -p console-identity-adapter-postgres --test deactivate_revokes_credentials -- --test-threads=1`. Require actual14 discovery/execution names and exact first-failure classification. Missing guard/compile/environment failures are not the previously accepted four-case semantic RED.
4. The frozen six-case semantic adapter intentionally accepts only its exact6 roster and must not be reused unchanged for14. Root creates a new reviewed roster/probe artifact bound to actual14 results, preserving old logs. Implementation starts only through `tools/lanes/fanout.py admit` / `run` from the approved test head with the current named RED command. No implementer starts from this private draft alone.
5. Root implements only the separately admitted guard/profile/owner scope, then requires the same admitted14 probe green plus applicable preserved root/profile/legacy checks with actual counts. Any unexpected/new first failure returns for independent classification rather than broadening the RED map or weakening tests.

Remaining review/acceptance limits are explicit: the draft's dedicated death case does not itself prove SQL cancel behavior or unrelated-subject concurrent progress; its guard profile case exercises volatility drift/replay/upgrade but is not an exhaustive independent mutation of every profile field. The approved design's broader retained coverage must be supplied by existing bound tests or separately admitted test additions before claiming complete guard closure. The six historical positives/failures and this14 source census cannot clear these obligations. No native enrollment, Account-wide suspension, credential-reset, release, production, payment or legal authority is asserted.

Pre-mortem: a generic owner error passes as backend death; an empty key result passes as partial-sweep contention; cleanup is skipped by an early assertion; formatting weakens historical assertions; test source inventory is reported as runtime success; or a new helper profile re-enters root backfill. Blast radius: one test target in a private patch only. Detection: exact57P01/55P03, before/after NOWAIT controls, unchanged full state/history assertions, exact blocker PIDs, immutable base/inverse checks, and reviewed actual runtime roster. Rollback: reject the private artifact intact; after import root alone serializes any inverse while preserving evidence. Stop on preimage drift, weakened assertions, wrong/unknown fault cause, incomplete cleanup, unsupported extra schema/ACL changes, missing independent test approval, or green/missing/non-executable admission probe. Reviewer identities: Inventory author; Fast independently approved the bounded V2 witness additions (receipt8f3748b); whole14 review and root final test approval remain pending. Lenses: Cartesian doubt, Essentialism / YAGNI, Chesterton’s Fence, Red Team, Operability / Day-2, Blast-radius / cell-based, Zero-trust / defense-in-depth.
