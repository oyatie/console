# Deactivation guard contract supplement V2

Binds frozen package `/private/tmp/account-deactivation6-1cef89ea-v1-fast`, metadata SHA256 `e024df794331e12baa7b0e65b116d5291ea3a9041e750471544a47612297fcfa`, base `1cef89eadca88dca3f4993605c0b2eb28bf13265`. This supplement is design review input, not approval or implementation admission. Original package unchanged; no product/test/DB mutation.

The same-Business-transaction identity-owner guard remains preferred. Two details make the fresh-read/fail-closed contract concrete:

1. **Enforce READ COMMITTED before any guard effect or lock.** `backend/crates/platform/db/src/audit_tx.rs:111–120` starts a normal pool transaction; it does not explicitly select isolation. A VOLATILE guard with separate SPI statements still uses the transaction snapshot at REPEATABLE READ. Check `current_setting('transaction_isolation') = 'read committed'` and refuse other modes, including Serializable, under a fixed error contract before returning a legacy decision. Do not try to change isolation after `with_audits` already armed Company context with a query. Future race tests must run real repeatable-read/serializable sessions and prove refusal/zero effects.
2. **Reuse strict fence reading after root lock, without Company-RLS bypass.** Validate/lock the Company-scoped users row under existing Company RLS, acquire accounts root FOR UPDATE, then execute `SELECT public.account_legacy_fenced_v1(subject_id)` as a separate post-lock SPI statement inside the VOLATILE guard. The identity owner already has EXECUTE on this exact existing routine; Business does not gain it. That call retains the existing function-local `row_security=off` behavior and therefore cannot silently interpret a newly hidden security row as absence. Do not combine the root wait and STABLE presence call in one statement-start snapshot, copy a plain row SELECT that can become RLS absence, or set row_security=off around Company users validation.

The real race tests remain required to prove PostgreSQL lock/snapshot behavior: enrollment commits while deactivation waits; post-wait classification sees the fenced row and preserves custody; enrollment rollback selects legacy; deactivation-first FK insertion waits for the exact Business transaction; backend death rolls back effects and releases serialization together. Alternate isolation, hidden/missing authority and direct native-only/cross-Company probes must fail closed.

The root6 installer must treat absent new guard as a **narrow guard-profile upgrade after successful root certification**. It must never route an already-finalized root profile back through users-to-accounts backfill. Existing root routines/table ACLs and legacy7 metadata remain byte-exact; only the separately reviewed users-column ACL extension/new fixed guard routine may be installed. Replay after complete installation is no-op; corrupt existing guard metadata refuses rather than self-repairs.

Independent design review must bind both the frozen GUIDE and this supplement before root authorizes a race-test candidate. No additional parser/runtime machinery is proposed.
