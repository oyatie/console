# Account custody and payroll recovery verification

These records preserve local implementation and verification, not a production,
release, payment or legal approval. Console and the first native nonpayable
payroll workflow remain incomplete. Native Account enrollment, live sessions,
current contexts, persistent draft/action integration and the actual Leptos SSR
input/review/recovery flow remain required; HR/attendance/labor, communications,
approvals and the broader Foundry suite remain product commitments.

The separately reviewed historical import contains Account deactivation14,
typed payroll uncertainty, the ordinary-operation control and CI18 enrollment.
Its original failures, failed stale-build attempt and source-custody qualifications
remain preserved. See the parent PROPOSAL.md and allowlist for that exact chain.

## New measured results

- At23fa2771, `npm run verify` executed78 local fast steps and exited1 because
  Audit-coverage and RLS-arming each classified3 functions in the existing
  test-only recovery module as production.21 hosted/bootstrap entries were not
  mirrored. The complete original log is retained; this was not a successful
  full verifier run.
- Adding6 redundant function-level `cfg(test)` guards fixed both gates but
  exposed the independent recovery-source checker's blanket cfg rejection:
 111/114 Node tests passed and3 failed. That intermediate failure is retained.
- Independently reviewed additive tests were committed as6fa6443d. The clean-tree
  combined admission observed both3-violation gate failures and Node135PASS/9FAIL
  out of144, with0skipped. Only9 new positive controls failed; old114 and21 new
  hostile-condition controls passed.
- Implementation ec673d27 permits only scalar outer `cfg(test)` after Rust
  comment/string stripping. It preserves all other cfg/ignore, topology, case
  name/count and invocation checks. The identical combined probe passed both
  gates and144/144 Node tests. The existing Rust test bodies are byte-identical
  after removing the6 redundant guards. Separate compiled discovery lists each
  contain the same31 names, including8 App recovery cases; listing executed0.
- A direct CI-preflight invocation lacked sandbox access to Git write-tree and
  emitted34 unresolved-source diagnostics. A separately captured write-tree
  exit128 identifies the index.lock denial. The unchanged source passed when
  local Git metadata access was available. This is an environment-qualified
  failure, not removed evidence or a code repair.
- On clean ec673d27, all18 exact CI-supervised recovery invocations passed:
 9payroll-owner cases,1isolated observer installer case and8App cases. Each ran
  exactly1PASS,0FAIL,0ignored. Commands, per-run hashes and counts are recorded;
  filtered counts and repeated earlier executions are not additional coverage.
- The existing native registration HTTP test ran at23fa2771:0PASS/1FAIL,
 215filtered,0ignored. Its backtrace reaches start4528 via enrolled4654 and the
  test4818 after sequential terms/artifact assertions. Thus the404 is the absent
  registration/start adapter. No later activation, session or context assertion
  ran. The trace does not establish production terms-publication authority.

The two-file cfg/checker implementation and its tests received independent Fast
review; Capture independently verified the native failure trace. Recovery18
independent readback and the full supported verifier rerun are appended separately
when complete. No hosted CI or merge result is inferred from local commands.

## Custody and limits

The additions select66 explicit regular UTF-8 records.10 log derivatives replace
20 URI userinfo occurrences, each an exact compiler excerpt of the two cfg(test)
URL constants in ec673d27 app/src/lib.rs. They are not observed application log
leaks. Originals remain untouched outside this import. Original and derivative
hashes and transformation line numbers are recorded in redactions.json; historical
result/review digests continue to name original bytes. Pattern screening is not a
general secret or production logging certification.

Root is the sole integration writer. Reviewers are /root/fast_gate_review and
/root/capture_review for their named scopes. Import validates each exact source
and derived hash, regular file mode and prospective Git blob identity. No source
snapshot directories, databases, binaries, environment files or runtime credentials
are imported. Revert only this evidence import if custody fails; preserve original
records and all source/runtime failure evidence. Stop on changed bytes, omitted
admitted failures, nonregular files or unsupported completion claims.
