# Curated Account14 / typed payroll / Operation8 / CI7 evidence proposal

This proposal selects exactly 218 regular UTF-8 files: 206 original byte records
and 12 labeled log derivatives, totaling 2,027,946 bytes. `allowlist.json` is the
explicit member list; `allowlist.tsv` is its compact path/digest projection. It
performs no repository import. Root is the sole source, CI and authority writer.
Observed integration head: `23fa277133e83916d094f9b2423b48fd5af65ce4`.

The selected chain includes reviewed Account14 tests and guard/probe design,
actual semantic RED and admission, implementation and fixture approvals, stale
build failure and corrected GREEN, typed primary/compatibility tests and RED,
typed implementation review/F1 correction/runtime custody qualification,
Operation source/control evidence, retained successes and registration failure,
and CI map/admission/candidate/independent approval/root import receipt.

No directory is an import member. Private base/post/source snapshots, the large
CI validation archive, Git indexes, node_modules, environment files, Docker
state, databases and binaries are excluded. `omitted-packet-members.json` names
141 historical manifest members intentionally outside this curated projection;
imported old manifests retain their original meaning and do not claim a full
packet was copied. The unfrozen V1 draft is superseded and must not be imported.

Twelve selected log payloads have 24 URI userinfo replacements. Every occurrence
was verified against two constants in the immutable `#[cfg(test)]`
`command_database_config_tests` module at `backend/app/src/lib.rs:6126` and 6127
for c1301628, 59c38d60 and 0a625d32. They are rustc unused-constant source excerpts,
not application log events. `redaction-receipt.json` records original paths,
original and derivative digests, line numbers and redacted context. Originals
remain untouched locally. Only the userinfo is replaced; all other bytes and
newlines remain exact. Original review digests are not rewritten to derivative
digests. Bounded pattern screening reports no remaining candidates; this is not
a general security certification or a production logging assessment.

The CI admission command wrote a fixed path. Its original `probe.log` bytes are
bound through preserved `probe-red.log`; the currently writable `probe.log` is
excluded. The three typed admission RED logs similarly bind to the copies under
`payroll-59c38d60-verification/admission-red-preserved`, with explicit sanitized
payloads. All four historical aliases match their original admission digests.

Saved counts are separated in `counts.json`; none were newly executed here:

| Evidence | Saved result and limit |
| --- | --- |
| Account semantic baseline and actual admission | Each14 executed,3PASS/11FAIL;9semantic failures and2missing-guard prerequisites |
| Account stale artifact attempt |14 executed,1PASS/13FAIL; environment-qualified, not the semantic RED |
| Account corrected probe |14/14PASS at0a625d32; Fast bounded review retained |
| Typed primary baseline |5 separate invocations:3semanticRED,2existing-behavior positives |
| Typed corrected runtime |App3+REST11+workflow-domain5=19PASS at59c38d60; custody addendum required |
| Retained controls |57 executed,56PASS/1registration404FAIL at0a625d32; includes Operation1 and period2 |
| CI admission |84 executed,82PASS/2FAIL at0a625d32 |
| CI author and independent reviewer |Each same probe114/114PASS; these are repeated invocations |
| CI supporting checks |Node inventory15PASS, Python wiring15PASS, focused preflight2PASS with468mutations, recovery unit21PASS |
| Static source inventory |Account14, App197, ontology/rest21 attributes;8App names in18 supervised commands;387/387 reachable,3193attributes,dark1 |

The initial denied Docker invocation ran zero Rust tests. The CI no-index
preflight failure and ps-denied recovery-unit attempt remain alongside their
unchanged-source successful reruns. Source attributes, mechanical checks,
mutation counts, Node tests and Rust executions are separate measures. No unique
coverage total is inferred from repeated logs or supervisor copies.

## Proposed custody steps for root

1. Review the exact allowlist and choose the destination prefix; proposed prefix
   is `docs/evidence/2026-09-15-account14-payroll-operation8-ci7`. Freeze any later
   results separately before adding their exact members. No glob or recursive
   copy is authorized by this proposal.
2. For each member, require a regular file with no symlink path component, read
   its bytes once, and compare size and SHA-256 to the allowlist before copying
   those same bytes to `destination_relative`. Stop on any mismatch; do not
   silently accept new bytes from a reused path. Every imported evidence blob
   uses mode100644. Derivatives retain their `sanitized/` destination namespace.
3. Also import exactly these nine proposal records at the chosen prefix:
   `PROPOSAL.md`, `allowlist.json`, `allowlist.tsv`, `counts.json`,
   `selection-checks.json`, `provenance-checks.json`, `redaction-receipt.json`,
   `omitted-packet-members.json`, and `manifest.json`. The manifest hashes the
   other eight records and all12 derivative payloads; its own digest is supplied
   in the handoff. Do not import any other file merely because it shares a path.
4. After root stages the explicit files, bind the exact candidate Git tree and
   commit. Compare every candidate path, regular-blob mode and Git blob OID to
   the expected list, and read back blob bytes to confirm SHA-256. The proposed
   blob OIDs were computed using Git's SHA-1 blob framing, matching this repo's
   object format; computing them did not write Git objects. New final records
   require a correspondingly updated exact allowlist and candidate-tree review.
5. Preserve these historical records unchanged. Record final writer/reviewer
   identities and the candidate SHA separately, and run the actual required
   evidence/documentation gates before claiming final custody or closure.

This author checked all218 selected file digests and136 selected original
manifest/driver bindings with no mismatch. No Cargo, database, Docker, product
runtime, Node test, shared-worktree mutation or Git-object write was performed.

Pre-mortem: a reused output path mislabels GREEN as RED, a derivative is presented
as original bytes, or a recursive copy brings unrelated runtime state. Blast
radius: the proposed evidence prefix only. Detection: explicit hashes, four
history aliases, transform receipts and exact candidate regular-blob comparison.
Rollback: reject the proposal or revert only the later evidence-only import,
retaining source/runtime originals and historical failure records. Stop on
source drift, missing original-digest links, nonregular files, unreviewed content,
new payload categories or unsupported closure claims. Lenses: Cartesian doubt,
Essentialism, Operability, Blast radius and Zero trust.

HOLD: root's frozen final CI GREEN/gates, final runtime17, any registration
resolution, final candidate-tree import and complete-head review. Earlier review
statuses remain historical; the later receipts qualify or resolve only their
stated scopes. Broader Account/native-enrollment/Auth7/guard proof and production,
release, payment or legal authority remain outside this proposal.
