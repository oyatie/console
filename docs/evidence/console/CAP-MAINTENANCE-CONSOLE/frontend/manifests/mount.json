{
  "module": "CAP-MAINTENANCE-CONSOLE",
  "route": "/console/maintenance",
  "screen_key": "maintenance",
  "component": {
    "module_path": "web/src/console/maintenance",
    "export": "MaintenanceConsoleRoute",
    "props": "MaintenanceRouteContract { branchId: string } (fixture MAINTENANCE_ROUTE_CONTRACT_FIXTURE in routeContract.ts)",
    "note": "SCREEN_REGISTRY entries are ComponentType with no props; the integrator's registry adapter supplies branchId from the session's effective branch scope, e.g. maintenance: () => <MaintenanceConsoleRoute branchId={effectiveBranchId} />"
  },
  "shared_root_deltas": {
    "web/src/console/shell/nav.ts": "MOUNTED_SCREEN_KEYS += \"maintenance\" — the nav item already exists (fieldOps group, labelKey console.shell.nav.maintenance, icon wrench, gate OPERATIONAL_ROLES + work_order_read_all); EXPOSED_SCREEN_KEYS untouched (module stays dark per ADR-0025)",
    "web/src/console/screens/registry.ts": "maintenance → adapter mounting MaintenanceConsoleRoute (see component.note)",
    "web/src/i18n/ko.ts": "no delta — console.shell.nav.maintenance: \"정비\" already present"
  },
  "new_noncolliding_file": "web/src/i18n/maintenance.ts (module strings const, same mechanism as production.ts; committed by this lane)",
  "authz": {
    "read_gate": "work_order_read_all only (deny-by-omission; org_wide_queue_triage does NOT grant read)",
    "features": ["work_order_read_all", "work_order_create", "work_order_edit_intake", "work_order_start", "work_report_submit", "completion_review", "priority_manage", "assignee_manage", "target_manage", "evidence_attach", "settlement_submit", "settlement_review", "org_wide_queue_triage"]
  },
  "api_contract_sync_points": {
    "G2": "maintenance_type/maintenance_cause optional on WorkOrderListItem/WorkOrderDetail/CreateWorkOrderRequest + list filters — local optional declarations in maintenanceApi.ts to be superseded by regenerated @console/api-client-ts",
    "G3": "settlement routes GET/POST /api/v1/work-orders/{id}/settlement, POST /api/v1/settlements/{id}/submit|review|void — called through the authenticated client via a raw view until client regen",
    "G4": "lens aggregates preventive_on_time_rate + mttr_minutes optional — stats render only when present",
    "G1": "equipment_id list filter — used by asset-history drill chips"
  }
}
