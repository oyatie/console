Consolidated preparation decisions — IN PROGRESS, not authority or completion

Apply first principles to the required user outcome; Cartesian doubt to claimed evidence; design thinking to actual input/feedback/recovery; Systems Thinking to cross-owner and operational consequences. Security-sensitive choices additionally use Red Team, Zero Trust, Operability and Blast Radius. This explains selected decisions; lens names are not proof and no machine gate verifies philosophy.

1. Reuse source owners. Existing 73 suite anchors/632 source-declared test functions are an inventory, not executed coverage. Full map has55registrations/28sinks/12families plus6Account boundaries. Old hashes, legacy users and old role inboxes are not approved new object/capability semantics. Each new contract needs its actual owning entrypoint and valid fixture before decisive assertions can count.

2. Admission: no new generic wrapper. Existing fanout admit only checks nonzero (except126/127); it checks neither exactSHA nor clean state nor semantic cause. Root will bind immutable base/design/test/spec/command/environment, successful discovery, actual execution names/counts, source assertion, full logs and independent failure classification, read back cleanHEAD immediately, invoke admit, retain/reclassify its fresh log before any implementer starts. No unattended run or claimed machine-enforced bypass prevention. Catalog absence can admit catalog work; downstream missing schema cannot admit authentication behavior. Compiler/network/startup errors are prerequisites, never assertion RED. Reviewer fresh29_security independently accepted this simpler operator gate.

3. Build closure: run upstream scripts using actual compiler/target inputs; preserve wasm-bindgen-shared schema hashing while excluding enclosing Git identity. Same full generated probe isGREEN on b9454dab;12actual output targets and3host metadata closures built. All579archive checksums match unchanged lock;496prior archive definitions unchanged. No production or WASM/Emscripten execution claim.

4. Verus: pinned sourcecommit671956ec527d3b7164779f767bdbfe769bedce6c/asset561633588/SHA256288db7622ff81dbace1defe70fa744292740aa653781e32cccf5b2de7117fa50; Rust1.98.1 andZ3 4.16.0. Executable source-bound freshness computation proves supplied-value comparisons only. Four verified functions, one expected false assertion refusal and one mutated function refusal (two failed postconditions);7source-custody tests and6ordinaryRust extracted-body tests. Do not count this as the missing design30 projection/safe-event/egress relational proof. Dependencies, requirement selection, Cedar, SQL, OS, TLS, timing and complete caller paths remain outside.

5. Terms publisher: extend existing platform/auth owner and thin app operator binary; no new service/crate or public publish API. Borrow caller-owned Transaction; private checked publication/ref/artifact types, no arbitrary JSON/bool authority constructor. Registration Accountguard precedes termshead SHARE through commit. Login historical consent does not take current publication lock. Genesis competing inserts use singletonPK+unique receipt revision in one transaction; no advisory lock needed. Later publication locks head andCASes exact predecessor; loser rolls back complete receipt/head. Reuse existing candidateSSH-policy signature verification for an exact explicit terms-approval artifact in an immutable operator-authorized custody bundle; signature/digest/CI alone does not authorize legalcontent or publication. Protected signer policy and authenticated operator mapping must stay outside app control. Dedicated publisherDB credential is trusted process capability; SQL alone cannot verify OS custody. Runtime/business/Company principals cannot invoke publisher mutation. These are bounded interface decisions awaiting complete executable publisher tests, not implemented authority.

6. New Account storage candidates reuse app PostgreSQL harness and actual restrictedLOGIN.46actorFK entries are independently pinned expected data; tuple checks include source/target columns, validation and non-destructive actions. Effective table/column/PUBLIC/inherited grants and owner-role topology are distinct from rowRLS. Existing MEMBER/SET/USAGE restriction retained after independently checking prior approved rls_isolation test. New candidate's blanket cascade prohibition conflicts with permitted ephemeral ceremony-binding cleanup. Two automatic approval rejections left it unchanged. Exact-edge correction has independent approval but needs explicit user approval; this test and dependent implementation remainHOLD. Handoff nullable-column census alone misses Company-requiringCHECKs; new positive Account-only target storage candidate tests that specific CHECK without pretending to verify signed approval/login.

7. Remaining preparation: complete domain wire interfaces/literal and hostile fixtures; deterministic real-role cancellation/unknown/replay harness; bounded open-loop intended-arrival histories with independent receipt/effect/audit readback and14oracle mutations; local primary/standby/holder/fencing topology and fault controls; actual-exec projection/event/egress Verus obligations; full migration/upgrade/rollback/cutover/populated policy/browser/noJS/continuity/Group/custody first-vertical tests; final exactSHA independent review and authority/test custody reconciliation. Do not substitute this list, static inventory or green primitive for completion. Production-scale capacity, independent deployment failure domains, provider/keys/legal and exposure are separately scoped later evidence.
