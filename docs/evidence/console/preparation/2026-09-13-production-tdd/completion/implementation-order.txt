Implementation admission and execution order

This handoff is a preparation candidate, not blanket permission to implement or expose the first vertical. Final revision-bound design and test approval is a distinct gate under docs/current/DELIVERY.md. No proposed owner symbol or comment is treated as an implemented interface. The source candidate and remaining HOLDs must be bound in the final consolidated receipt.

Existing clean authority: design30 7b6e4c6836750a23fc5fc38bdd37ac391e970215 and BW31 5f9f3b15704deaf8e81a682e4475af489d164210; design30 current-authority adoption31f769accc08ac7dcb0d140709021c58d2a86c7e. The full Console capability-family/dependency ledger and12task usability protocol remain part of the package. First-vertical NONPAYABLE does not narrow Console to HR/payroll and does not authorize payment, real legal conclusions, destructive provider cleanup or later external sharing.

For every admitted implementation: create an isolated clean checkout at the exact approved test commit, declare one writer, exact paths and protected shared resources. Run the named probe and inspect its actual failing assertion; only then tools/lanes/fanout.py admit/run. Because admit checks exit status rather than failure meaning, root must reject toolchain/setup/compiler/fixture failures and unrelated upstream schema failures. The identical probe is required GREEN afterward, followed by the relevant broader regression/security/recovery checks and independent review. Never repair a test by deleting assertions, returning defaults, adding fake domain owners, or ignoring current authorization.

Reachable existing-owner lanes

1. Public request-context formatter. Boundary backend/crates/platform/request-context/src/lib.rs. Probe: RUSTUP_TOOLCHAIN=1.98.1 cargo test --locked --manifest-path backend/Cargo.toml -p console-platform-request-context --lib. Current20executed16PASS4behavioralRED; keep existing positive public status classes and every earlier test. Repair only the responsible public projection/formatter. Do not call this SQL-origin, general logging or whole-program noninterference proof.

2. Cedar diagnostic handling. Boundary platform/authz production evaluation and row/field helper call paths. Probe: RUSTUP_TOOLCHAIN=1.98.1 cargo test --locked --manifest-path backend/Cargo.toml -p console-platform-authz --test cedar_diagnostic_fail_closed. Current5executed2PASS3behavioralRED after real schema compilation and evaluation. Any runtime evaluation diagnostic in enforcement must fail closed while clean permits and matching forbids remain meaningful. Simulation/authoring diagnostic explanations are separate; do not suppress their authorized useful information. Broader policy tests must preserve the public semantics.

3. Company-bound realtime dispatch. Boundary backend/crates/platform/realtime/src/lib.rs and actual message/ack recipient dispatch. Probe: RUSTUP_TOOLCHAIN=1.98.1 cargo test --locked --manifest-path backend/Cargo.toml -p console-platform-realtime --test hub same_account_. Current2selected2behavioralRED;3other hub tests filtered by this probe and remain mandatory broader checks. Match actual event Company to active connection Company while preserving positive delivery and queue/drain behavior. Contextless notifications cannot derive Company from recipient; keep protected notification enrollment unavailable until its real source-context contract exists. This is separately governed intra-tenant Company access, not a substitute for cross-tenant isolation evidence.

4. Diagnostic tracing. Boundary backend/app/src/console_telemetry.rs actual From<DbError/RequestContextError>. Probe: RUSTUP_TOOLCHAIN=1.98.1 cargo test --locked --manifest-path backend/Cargo.toml -p console-app --lib security_diagnostic_sink_tests. Current2selected2behavioralRED. Preserve stable public event/capture positive witnesses and equal public logical event schedule. No raw private error Debug/Display may enter public tracing. Do not introduce nondeterministic incident identifiers and then scrub test output without a reviewed coupling/schema change. Ordinary unit variant executes these; test-postgres remains the23existing SQLx variant.

Catalog prerequisites and staged dependent owners

5. Account expansion and retained actor FK/custody roles. Current account_migration2tests produce actual old225 credentials/refresh/audits, run current numbered migrations, then fail at three missing relations. This may support only a catalog/role-owner lane, after exact reviewed source/DDL mapping and the blocked ephemeral-FK test correction are resolved. It cannot admit populated identity cutover, Account session authorization, context discovery or publisher implementation. Preserve immutable migration checksums; keep old auth producer compatible during this catalog stage or first introduce a reviewed frozen real legacy producer. No destructive rollback or revived legacy credential after a permanent fence. Require all46enrolled actor references plus discover/classify unexpected references.

6. Ordinary root deployment/terms/Account/identity/provider/policy bootstrap. Resolve initial deployment authority without requiring a yet-uncreated Account session. Test configuration may install TEST_ONLY issuer/operator keys in an isolated deployment; actual registration, signature validation, grant admission and owner receipts must use ordinary production owners. No test-only authority namespace or inserted verified/grant rows. Account activation, typed context and publisher test sources must have their real production prerequisites before assertions can execute. A missing operator/module/schema is not a successful refusal witness.

7. Typed schema/addressing, native codec and retained material; common drafts/attempts/typed action binding; actual source producers; work/capacity/transfer; native calculation; review/publication/correction; selected-Company Group coordination. Use fixed existing responsible owners and borrowed-transaction participants; session/Company/Group/worker contexts are distinct ephemeral bindings, not retained allow flags. Native prepare/calculate PENDING is not terminal receipt success. Every new intent is issued through ordinary intent-slot control; retries keep original command. Complete all six expected run fields using current authorized owner control reads. Test bodies/fixtures/oracles and exact missing interfaces are in the separately frozen native/security supplements; their authored/discovered/executed counts must not be conflated.

8. Mount real Leptos SSR flows and generate real owner-backed browser fixtures. Browser source has11application cases and8DOM oracle controls; all11application cases currently stop before request because runtime owners/fixture are absent. These failures cannot admit frontend behavior. Once prerequisites exist, run an actual HTTP/DOM assertion as the frontend lane RED; preserve no-JS input/save/validation/conflict/recovery, field disclosure and keyboard/reflow tests. JS-enabled SSR is not hydration proof. Required hydration/subscription, assistive technology and human task observations retain separate acceptance obligations.

Recovery boundary

The active tools/lanes/recovery harness is separate from ordinary PostgreSQL execution. Run the two exact supervised scenario/mode commands in active-runner-execution.json, separately. The remote_apply replay test's meaningful RED is actual premature success after primary receipt visibility and exact original backend termination, with paused standby and complete1intent/2attempt/1effect/1receipt accounting. The lost-COMMIT-reply case already passes. Timeout, setup error, unclassified typed UNKNOWN, or a different application waiting strategy requires a reviewed witness/classifier; it is not the same productRED. Do not claim acknowledged data loss from the measured remote_apply acknowledgement defect. Compare concurrency mechanisms by required invariants and measured workloads, not employee count or a preference for locks/MVCC.

Common stop, rollback and acceptance

Stop a lane on unclassified failure, changed authority/interface, loss of fixture custody, incomplete history or out-of-scope write. Preserve original user input and exact reconciliation identity under uncertain commits; never infer no effect from silence. Roll back code with a new scoped revert; roll back data only under its separately reviewed reversible transition contract. Maintain correctness under overload; latency/throughput budgets have measured operating envelopes. Before slice acceptance, all required deeper assertions must actually execute and pass, source-to-sink callers must be enrolled, and required failure/operating/usability experiments must be satisfied. Before real-data activation or production exposure, separately prove the actual deployment/backup/restore/credential/provider/legal boundaries. Local green tooling or reference tests do not supply that authority.
