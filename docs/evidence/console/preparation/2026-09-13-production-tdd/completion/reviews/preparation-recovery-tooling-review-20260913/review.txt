# Independent tooling implementation review

Reviewer /root/preparation_recovery; root is sole repository writer. Read-only source/diff review with focused read-only tests. No blocking findings in reviewed patch. Exact identities and independent command counts are in verification.json. Root still owns final clean-candidate verification and lane success recording.

Patch c78b7f10f064eed3b3d078549e45bd5d04341549fd5b05468fc48d1d06b5d765, author manifest3cb56a4197dac9cc42c2419b758b2f96cd30ed369ee3bd5e17b8db65dd879933, declared base e44e0448abee62ea7d296ffca4ace3b3dbfbdb85. All22declared path hashes match integrated root bytes. Approved regression is unchanged at ad665d1de72bb801e509305f869a5d9ecb8a0ff23b73da556e215913fe855487. Telemetry resource cfg correction remains separately root-reviewed and excluded from patch.

Generator/resource routing: migration=postgres, Cedar=none, recovery=postgres-recovery. Recovery has no needs-postgres compatibility label, no ordinary one-node map entry, and generic queue explicitly refuses it with dedicated-topology guidance. Existing bool callers retain their resource behavior; generator now carries the explicit resource string into integration target labels. Generated changes occupy only the three reviewed crate BUCK faces. Broad app mapped-source additions follow its existing nested-helper discovery and add publication_privileges.rs without creating a duplicate Rust root.

Execution coverage: ordinary migration gains one exact in_workflow map entry and safe wrapper with derived counts updated and no invented runtime duration. Cedar appends its named binary to the existing actual hermetic invocation, preserving earlier roots and keep-going. Domain-b remains the existing required facet: real pinned checkout/toolchain/image setup precedes ordinary tests; two separate recovery commands use exact root/argv/cut, !cancelled(), live selection and actual setup-outcome guards, so an ordinary or first recovery failure does not skip the next case. Existing45minute job bound remains; each supervised topology retains600second wall plus10second cleanup grace. Runtime performance under cold CI is not measured here.

Custom recognizer is narrow to the exact supervisor/Cargo surface and validates complete discovered scenario union, exact modes, setup custody, step separation, failure conditions and effective shell. Reuses existing executable command extraction, rejects malformed/control-flow/non-gating forms and returns no attributed invocation on failure. Recovery source/harness changes trigger live-Postgres path classification. New wiring, supervisor/relay and recognizer suites are reachable from actual preflight run commands. Protected preflight step inventory and exhaustive mutation coverage increase rather than shrink.

Baseline: only test_attribute_baseline changes; no old entry decreases or disappears. Existing sole SeaweedFS dark exception and feature-pinned binary set are byte-identical. Inherited source-attribute increases are lexical source accounting, not runtime case counts. No baseline acceptance of newly dark tests was introduced.

Fixture promotion: all six tools/lanes/recovery files byte-match their retained completion/recovery counterparts. No harness behavior change was hidden in relocation. Existing process-group supervision, fixed pinned image, loopback publishing, owned labels/names/volumes and scoped cleanup remain. Historical failed recovery evidence remains in place. No Docker, Cargo, PG or live recovery command was executed by reviewer.

Independent exact commands at integrated root: nine-method wiring probe9PASS; recovery recognizer tests17PASS, zero skips. Author manifest's five test logs all match declared hashes and sum188tooltests; this is verification of evidence custody, not a claim reviewer independently ran all188. Root is performing integrated candidate gate reruns separately. Known Cedar/recovery behavioral REDs, Account catalog/publisher prerequisites and browser/native fixture gaps remain independent.

Pre-mortem: recovery misrouted one-node, second case skipped after failure, fake custom-runner reachability, cold image absence, nested source missing, baseline darkening. Detection: exact probe/hostile recognizer tests, path/hash/copy and no-baseline-decrease checks. Blast radius: tooling scheduling/build metadata only. Rollback: targeted new commit preserving tests/evidence. Stop if approved test semantics change, additional paths appear, generated provenance drifts, product failures are relabeled or final clean candidate differs from reviewed hashes.
