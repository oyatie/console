# Frozen native S1 independent source review

Reviewer /root/preparation_recovery; root is sole repository writer. Packet /private/tmp/console-native28-owner-assertions-s1-20260913, manifest SHA2562fd88d2fcdc3edf2d985a96e8c4ef384ba65569f32aa75012a2b2f96a7a21958. All9declared file hashes and lengths independently match. Source inventory exactly matches17authored test functions:10explicit tokio functions plus7macro expansions.55registration assignments,11with partial authored assertions. No Cargo target, compiler discovery or runtime execution claimed.

Independently reran the four exact rustfmt --edition2021 --emit stdout --config skip_children=true FILE commands recorded in syntax-execution.json for native-owner-tests.rs, binder-sequence.rs, native-owner-producer.rs and native-fixture.rs. All exit0. Output discarded; frozen files unchanged. This is parser proof only, not typechecking or4tests. Cargo discovered0/executed0, Rust compiled=false.

S1 corrects R2 preparation run state to STAGED, review.decide target to actual review_basis, four-nullable expectation wording, and pending/terminal native result correlation against original AttemptRef command/Company. Fixed SQL readback fields match the frozen native18 physical contract. The blocked-close negative now requires nonempty ALL-blocked units, preserving valid mixed populations. Distinct per-scenario Company seed entries are checked against actual authenticated context; repeated runs require fresh real seed namespace. This resolves the provisional shared-population wage-mutation fixture concern without fake owner state.

Confirmed remaining source blocker: native_two_original_calculate_intents_have_one_current_winner invokes bind_and_seal twice with the same untrusted target selection. common14 draft-attempt-contract.txt:4 says the partial unique active-intent binding prevents duplicate active drafts, and an explicit new request requires a NEW OWNER-ISSUED intent slot. The helper does not request two slots or assert distinct command/attempt IDs. Thus the proposed body cannot yet establish two independently admitted competing commands. Author acknowledged and is preserving S1 while preparing S2 with explicit owner-issued new-intent operation and distinct-ID assertions.

Additional requested source strengthening: create/close/review-open/submit/decide generic Completed results should assert original attempt/Company receipt binding, as native prepare/calculate already do. This is test-composition proof, not permission to invent receipt provenance. Four-nullable old/current transitions, deterministic lock rendezvous/unknown-COMMIT, real authority/source bootstrap, retained byte writer/decoder and independent retention obligations remain explicitly SOURCE_NOT_AUTHORED, separate from owner API implementation prerequisites.

Disposition: retain S1 as reviewed partial authored source with the above source correction outstanding; not product RED or implementation admission. S2 must supersede only through a new immutable manifest and review. No stale authority-adoption HOLD is asserted. No repository edit, Cargo/PG/VM run, test skip/deletion or production action was performed.
