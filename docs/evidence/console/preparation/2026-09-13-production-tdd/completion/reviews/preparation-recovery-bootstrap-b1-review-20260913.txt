# Independent B1 TEST_ONLY bootstrap source review

Reviewer /root/preparation_recovery; author /root/fresh29_workflows; root is sole repository writer. Reviewed frozen /private/tmp/console-native28-test-bootstrap-b1-20260913/freeze-manifest.json SHA2569c4cc953b0a6448cee4ddb9bfd3a834c4e37b96c32d0028494432a5aeb9e1d65, base58b6f758ac2a49b28589b9506628a43b6158e925. All3declared file lengths/hashes match.

No blocking source/interface finding for reuse as a proposed TEST_ONLY disposable bootstrap prerequisite. This is not a working bootstrap, implementation admission, authority-grant proof, production approval or legal identity assertion. compiled=false/executed0 is correct. Existing current product authority is adopted; no stale design-adoption HOLD is added.

Account producer calls proposed real auth owner terms/enrollment/login entries, obtains actual server challenges, signs with WebauthnAuthenticator<SoftPasskey>, and verifies the returned Account token/refresh-family/assurance via proposed actual configured Account verifier. The allowCredentials local SoftPasskey hint follows existing auth/tests/webauthn_ceremony.rs; server stored challenge/cryptographic verifier are not replaced. No legacy Company JWT, direct user/credential INSERT or public Principal-to-authority constructor is used. Missing Account-aware modules remain explicit implementation prerequisites.

Issuer producer loads operator-configured ES256 keys, emits signatures with explicit issuer/kid/audience/iat/nbf/exp/jti and exact deployment/group binding. Identity statement maps submitter and alias to synthetic subjectA and reviewer toB; different Accounts alone are never claimed independent Humans. Actual identity owner must verify configured trust, time/replay/Account/deployment guards before normal immutable binding/root writes. TEST_ONLY synthetic equivalence is not real Human/legal verification.

Provisioning is separately authorized through an actual operator-only SQL pool plus cryptographically verified current operator session and locked disposable deployment control. It must refuse production topology even for valid test signatures. The exact preinstalled immutable acceptance manifest bounds finite action/scope/field grants and topology; Human root/AccountIDs/role strings cannot grant Company authority. Signed Company enrollment binds root receipt, scenario, original request and deployment/group. Actual returned Companies must be distinct and match the Group/request. No universal fixture owner, raw verified/grant INSERT or arbitrary table/function is supplied. These guards are normative proposed owner implementation requirements, not evidence that comments/signing code has implemented them.

Two exact independent parser commands, cwd B1:
rustfmt --edition 2021 --emit stdout --config skip_children=true /private/tmp/console-native28-test-bootstrap-b1-20260913/account-enrollment-producer.rs
rustfmt --edition 2021 --emit stdout --config skip_children=true /private/tmp/console-native28-test-bootstrap-b1-20260913/test-issuer-bootstrap.rs
Both exit0 with output discarded. Two parsed files, zero compiled files, zero discovered/executed tests. No Cargo, DB, VM, network, operator/bootstrap action or repository edit occurred. No dependency or production configuration was installed.

Remaining implementation proof: exact supported disposable deployment custody and real operator/current session authorization; cryptographic bad-alg/key/audience/time/replay/deployment negatives; production-topology rejection; immutable manifest grant enforcement; normal identity/policy mutations and actual fixture output. Native workforce/profile/source producer and complete security/native assertions remain separate authored-source work. Reuse of B1 must carry these limits and must not relabel absent owner bootstrap as product RED.

Lenses: zero-trust, Red Team, operability and blast radius with Cartesian doubt. Pre-mortem: signed test data mistaken for production authority, issuer root confused with grants, stale session or replayed claim accepted. Detection: explicit owner guards and source/evidence classification. Rollback: decline implementation/adoption while preserving B1. Stop if a test bool/row INSERT replaces actual guarded owner admission, production scope appears, frozen bytes change, or missing modules are reported as actual bootstrap execution.
