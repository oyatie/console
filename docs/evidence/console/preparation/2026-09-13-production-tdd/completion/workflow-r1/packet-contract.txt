# Workflow15 assertion source — authoring candidate

Owner /root/preparation_recovery. Repository writer /root. Exact base58b6f758ac2a49b28589b9506628a43b6158e925. Immutable target is15 R2 registered owner entries: governance.execution_gate.{request,decide}, payroll.review.{release,replace_projection,request,request_append,request_withdraw,respond}, work.transfer.{submit,execute,stop}, group.operation.{create,activate,resume,stop}. Mechanical guide: R2 manifest764e3e4454a0e059337a9d6faa7ae0c861f586ca2762f27a6a484b404582649e, adopted design30 action/owner binding and workflow16 ordinary owner contracts. No product implementation, migration, lockfile, generated authority, or repository edits.

Lenses: Cartesian doubt; Systems Thinking; Red Team; Operability/Day-2; Blast-radius/cell-based; Zero-trust/defense-in-depth. Pre-mortem: a plausible owner DTO hides fabricated authority or setup state, or replay accepts another command's receipt. Detection: exact producer chain and receipt binding plus immutable owner readbacks; independent author review and explicit zero compilation/runtime. Blast radius is this scratch packet only. Rollback is decline integration, preserving evidence. Stop on missing author-owned dependency, design contradiction, or any need for a special privileged test business owner. Reviewers root/native/security; author does not approve own source.

Fixture lifecycle: every sqlx::test owns one migrated disposable database. Native build_native_deployment(pool,scenario,count) publishes actual genesis terms from Ed25519 custody, redeems the configured deployment root proof, enrolls real Account passkeys, registers/adopts actual signed provider/Human bindings, creates Group and each actual Company, grants ordinary policy, admits serving/worker context, and publishes actual source/profile/person/Employment prerequisites. No JSON seed index or callbacks. This dependency is separately authored and must be frozen/hash-pinned before this packet is called source-complete. Company fixtures contain actual opaque resolver contexts and native choices. Tests require exact role pools bound to the same generated database. Ordinary owners remain proposed and absent; parser-only is never product RED.

Additional fixture input is published through ordinary typed payroll/workflow/Group configuration owners, under real current operator authority. These are ordinary production configuration siblings using the existing common immutable configuration revision store, its current expected heads, typed guards and receipts. They are not one of the55 business action registrations and cannot mutate business rows or construct permission contexts. Test-only labels describe synthetic config content; no test flag bypasses validation. Definitions reference the actual registered payroll case handler action and case object; coverage is exact Company+selected Account+definition+finite interval. Group capacity/availability is actual operator-published finite input. Publication projections/category/gate policy are current governed owner configurations referencing real registered fields/actions. Returned configuration refs, not client booleans or preset revisions, enter sealed requests.

No force, admit_fixture, seed_ready or arbitrary publisher callbacks. General owner read contracts below are fixed authorized DTO projections over the named owner relations; missing/incomplete reads fail, never invent defaults. UntrustedActionTargetSelection::create_registered::<I>() requests actual owner allocation for the registered object kind, not a fabricated existing object. existing_publication/case/transfer_plan/execution_gate and with_group_slot contain untrusted exact actual IDs; each normal binder resolves current authority and stored provenance. Group controls preserve R2 GroupRequestIdentity and never enter Company envelopes.

Ordinary additional read shapes: payroll read_complete_publication_targets(run,completion) returns all exact HistoricalSubject+ResultRef+CompletionRef for the existing closed membership; read_publication_content(ref) returns immutable content/source/subject; read_case_content(case) returns immutable submitted content with exact items; read_case_control(id) returns CaseExpected. Workflow read_assignment(task) returns actual assignment Account/epoch/control/charge; read_transfer_plan(plan) returns current exact immutable content and control. Group read_group_operation(operation) and read_registered_group_action<I> return current authorized ordinary control data. None creates facts. Runtime integration must implement these in their existing owners with normal current disclosure, budgets and same-Company guards.

Cases use actual publication for the fixture's enrolled subject, not arbitrary employee UUID. Configuration producer explicitly binds that existing subject Account using the actual canonical Person/Employment/Human binding owned by native fixture. An unrelated Account cannot request/withdraw; handler differs by verified Human. Transfer candidates retain the same prerequisite ordinary scoped rights; nomination never grants them. Group child drafts are created only after actual PREPARING parent and use actual stored parent/slot refs through ordinary Company binders. Activation never runs a child early. Tests do not claim Group-wide atomicity or production failure-domain durability.

Exact native signature agreed with author: crate::native_fixture::build_native_deployment(pool:PgPool,scenario:&str,company_count:usize)->NativeDeployment. NativeDeployment holds runtime/passkey service/origin, verifier, serving, Group/operator, AccountBootstrapPhase including transfer_recipient (third distinct Human), and real Company Fixture.facts. fresh_submitter_login directly calls the authored real Account producer; no opaque seed or login callback. Native source remains independently author-owned and under review; root integration must pin its next accepted freeze. This packet never claims a future passing constructor merely from the signature.

Publication internal read DTO is PublicationRecord{publication,subject/source binding, result:ResultRef,completion:CompletionRef,projection:GovernedRevision,content:PublicationContent}. PublicationContent retains exactly control30 closed subject/period/purpose/fields/source_digest; extra source/control refs stay outside content. ResultRef remains only calculation_id/version, never an invented batch member. Existing SQL payable lives on payroll_line_calculations. Gate consumption readback joins existing gov_approval_consumptions.approval_id to gov_approvals.id/request_ref.

The15 methods include positive result and identity/receipt assertions, with targeted replay, stale CAS/issue, independence, all-child activation and stopped provenance checks. They are not a claim that every concurrency/hidden-target/maximum-cardinality proof case for these15 registrations is authored. The full55registration test coverage and all original security/native proof-cases require their independent packets and explicit census.
