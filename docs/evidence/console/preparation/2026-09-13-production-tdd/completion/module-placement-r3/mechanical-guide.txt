Mechanical placement candidate — root owns installation, base fd94667fb9133f1ecb55831f43192bdc46776dc0.

1. Keep immutable packets. module-placement.json pins each selected source and all26 compile-time assets. Apply S5/security, R4/browser, S8/native oracle, R4/migration remainder, S4/lock overlays; S8 includes the selected common-reason mismatch assertion body. Do not layer two definitions of the same module. The generator reads repository/scratch evidence only and writes only this scratch directory. No runtime product files are generated here.

2. Copy selected source bytes from source_repository_path under the explicit --root checkout to their target paths and copy every include asset to its target. Codec assets belong beside the payroll adapter owner test modules at src/codec-goldens; business-readback SQL, source registry JSON, wage golden, assignment/publication readbacks, migration CSV/catalog SQL and commit-relay Python belong beside their consuming support/native modules. The JSON records all relative include resolutions and hashes. No source may resolve an include from private scratch after installation.

3. Append each supplied *-declarations.rs to the corresponding integration root (native_acceptance.rs proposed; auth_rest.rs/account_migration.rs already exist). Preserve every existing declaration/test. Shared dependencies are closed per root from actual crate:: references. native_projection_alias_acceptance is intentionally the same auth_rest sibling used by projector_counterfactual_acceptance::grant_visible_line, not a duplicated helper/test copy. negative_assertions exports crate::assert_registered_refusal; mount its macro module once at every root that needs it.

4. migration_remaining_tests must be a child of migration_transition_acceptance, not a root sibling: it imports that module's private source_keys/mapping_partition/snapshot_mapping. Append #[path = "migration-remaining-tests.rs"] mod migration_remaining_tests; to migration-transition-acceptance.rs. Its existing #[path] children are legacy_transport and migrated_primary; retain those exact aliases and do not additionally mount them at the crate root. migrated_primary uses super::legacy_transport. Migration transition and rollback remain direct children of account_migration to reuse the original private225 legacy producer, catalog checks and material_snapshot.

5. context_owner_fixture, terms_publication_acceptance, publication_boundary_acceptance and publication_completion_tests stay direct children of auth_rest: super::prepare_http_database and super::account_transport_urls are actual private functions in auth_rest.rs. They cannot be mounted under a generic nested support module or another integration binary without relocating their intended parent references.

6. Owner-local tests use --lib, never a nonexistent security_owner_acceptance integration binary. native28_codec_tests and native28_stream_budget_tests are crate-root sibling modules of the proposed payroll adapter native28 owner; their use super::native28 requires this level. authority-vector source already contains a cfg(test) child: append it directly inside request_context::account (inline lib.rs or account.rs according to actual responsible owner), not inside an extra wrapper. Both telemetry append packets similarly append directly inside existing backend/app/src/console_telemetry.rs. The collector references console_app::console_telemetry; a public scoped owner surface is needed for integration callers, not a new observability crate.

7. Apply exact dev-dependencies.json under one root-owned manifest/lockfile transaction and regenerate existing lane build metadata with its normal tool. Existing Tokio features remain; add fs/process explicitly. rand0.8.6 matches retained OsRng.fill_bytes, not newer incompatible rand APIs. Existing workspace/path dependencies are reused. New cookie/csv locks are deliberate fixture-only dependencies already compiled in browser helper proof. Actual socket2/futures-util/tokio-tungstenite are also required by migration remainder source, not optional transitive imports.

8. Discovery commands in each entry are proposed until interfaces exist. Require expected compiler-discovered leaves and then execute those same leaves. Source test-name scans are an inventory, not counts of executed tests or expansion of macro-generated cases. bounded_process_output contains its own helper tests; mount once under account_migration and count those explicitly. Do not treat parser/import/missing-owner failures as behavioral RED.

9. Proposed ordinary interfaces still requiring actual implementation: Account13/serving admission and workload/issuer/grant owners; action30 draft/binder/source/review/assignment owners; native28 codec/calculation and Group control owners; upload/storage projections; ui30 routes/renderer/form binder; identity/field/replay projections and process health; ordinary migration batch CLI and versioned runtime compatibility. Fixtures call these owners, never seed their resulting authority rows. current_controls and the common reason/control compiler remain a pending design amendment; mounting source does not approve that transport contract.

10. Copy final BrowserR4 playwright config/spec/helper assets listed in runtime_assets to backend/app/tests/browser; historical completion/browser is not the activation input. Pass actual absolute installed paths through the declared CONSOLE_BROWSER_* environment variables, exact candidate SHA and actual serving build hash; run native browser fixture leaves with --test-threads=1. Their ordinary SQLx pool/server must remain live throughout asynchronous child execution. Browser child process-tree cleanup remains a topology supervisor obligation, not a passed Chromium lifecycle result.

This mapping checks selected Rust module/asset placement against regenerated reviewed source selection; it does not prove compilation or production owner APIs. Architecture/layer checks, compiler discovery and same-boundary RED/GREEN remain required after admission.

R2 independent machinery review corrections: checker regenerates from retained selection-base.json and reviewed overlay rules, recomputes actual source refs/includes/hashes, and compares complete mapping plus exact emitted declaration bytes. 1positive and9hostile controls include empty/omitted roster, erased refs, wrong parent, erased declarations, omitted include, wrong hash, duplicate module and altered declaration file. All inputs are repository-relative; run python3 check.py --root /absolute/checkout. No private scratch source dependency remains. BrowserR4 includes the selected hydration/reconnect/real WASM observer sources and all review corrections; final mapper selects its playwright folder, not historical completion/browser.

Final R3 mapping: selected S8 native oracle, S4 lock, SecurityS5, BrowserR4 and migration-remainderR4. All26 compile-time assets plus10 runtime/checker assets are pinned. Runtime assets include existing draft/history_oracle.py and independent test_history_oracle.py plus8 final Playwright .cjs files. The helper checker remains a normal Python unittest in its mounted support/draft directory. Native fixture browser discovery has15application leaves; standalone Playwright oracle has10 machinery leaves. Neither count claims actual owner/production behavior execution.
