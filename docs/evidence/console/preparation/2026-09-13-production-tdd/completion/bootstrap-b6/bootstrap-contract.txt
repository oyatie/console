# B6 ordinary owner bootstrap proposal; prior revisions preserved

B1 is preserved as rejected architecture evidence. Its acceptance_bootstrap/admit_test_identity_root path is NOT adopted. B3 uses ordinary production deployment-root, provider-registration, supervised identity-root, Company enrollment and policy-grant owners. Synthetic subjects and TEST_ONLY keys belong in isolated test deployment configuration and signed evidence, never a privileged alternate business namespace. All new Account-aware owner symbols below are explicit proposed interfaces; their absence is implementation prerequisite, not executable proof.

## Existing owner reuse and first operator provenance

Immutable58b6 backend/crates/platform/provisioning/src/lib.rs owns BootstrapCredentialStore (164), seed_cold_start_credential (661), PlatformProvisioner (1000), onboard_tenant (1021), create_group (1301), and current Group assignment. Current cold-start uses the legacy platform sentinel Company and Company-scoped auth bootstrap; it cannot produce an ACCOUNT_V1 session. B3 proposes Account-aware replacements/siblings IN THESE SAME ordinary owners. No DevPrincipalProvisioner or fixture authority seeder is used.

BootstrapCredentialStore::seed_account_cold_start_credential(startup_auth_pool,configured_root_secret,ttl,now)→RootEnrollmentSeed{credential_id,account_id}. This normal deployment-startup entry reuses existing single-use hash/expiry/idempotence mechanics while invoking identity-owned pending Account creation under global account_security, instead of users/sentinel Company. The real privileged startup-auth role is provided by deployment initialization (same process/database deployment, inaccessible from Company runtime SQL); it is not an already-authenticated Account. It may initialize only the designated never-enrolled deployment root. Current active root/registered key, existing different open proof or disallowed deployment state refuses; existing root credentials are never overwritten. The secret is configured once for the actual local deployment, read securely by startup, protected as a hash, and never logged.

start_account_root_enrollment(auth_pool,service,RootEnrollmentStart{credential_id,root_secret,terms,browser_nonce_digest}) consumes that actual root proof into a normal pending Account registration ceremony. It returns no access JWT. Existing real SoftPasskey signs the actual challenge. Generic account_enrollment::finish_account_enrollment verifies signature/origin/RP/nonce/terms, activates Account and real refresh family atomically, and returns ACCOUNT_V1. Identity owner activates the designated deployment-operator entitlement from the locked stored root proof, not a caller flag. verify_account_session validates the actual returned JWT/family. enroll_deployment_operator is the fully authored source for this path; no assumed initial VerifiedAccountSession creates a cycle. All later provisioning operations reload current operator authority/session under their real owner guards; VerifiedAccountSession alone is never an admin grant.

## Ordinary provider, topology, identity and grants

PlatformProvisioner::create_account_group(pool,operator,GroupEnrollmentInput{command_id,slug,name})→GroupEnrollmentReceipt{deployment_id,group_id,receipt_id,...}; this is Account-aware current create_group, not a test function. identity::register_identity_provider(pool,operator,IdentityProviderRegistration{command_id,group_id,issuer,key_id,public_es256_pem,accepted_audience,evidence_classification})→IdentityProviderRevision{provider_id,revision,content_digest,...}. Registration requires ordinary current authority to manage that deployment/Group's trusted identity providers. Test config admits the synthetic issuer as SYNTHETIC in an isolated deployment. Production config rejects SYNTHETIC providers/evidence by policy in the SAME path. A matching signature cannot widen issuer/Group/deployment scope. Config must be fixed before enrollment, with custody recorded through normal owner revision/receipts.

identity::admit_supervised_identity_root(pool,operator,SupervisedIdentityRootAdmission{command_id,group_id,provider,signed_evidence})→SupervisedIdentityRootReceipt{group_id,receipt_id,...}. Same normal initial-root route production uses for an operator-supervised configured provider; verify exact ES256/issuer/audience/kid/iat/nbf/exp/jti replay, exact deployment/Group/provider revision and actual Account list, then normal identity-owner immutable root receipt and bindings. It does not infer Humans from Account IDs. The initial-root exception is exactly approved Account13 supervised root; subsequent identity bindings still require ordinary two-person propose/verify and cannot re-run root. No root proof grants Company business rights.

PlatformProvisioner::onboard_account_company(pool,operator,CompanyEnrollmentInput{command_id,group_id,slug,name,administrative_account_id})→CompanyEnrollmentReceipt{org_id,group_id,original_command_id,receipt_id,...}. This Account-aware normal counterpart to onboard_tenant invokes existing Company/Group topology, actor and admin authority owners; it returns no employer-issued OTP/Account sign-in. The actual registered deployment operator has current provisioning authority; each Company is freshly owner-allocated. Ordinary identity::read_company_policy_control returns exact current policy head; identity::apply_company_grant_plan(pool,operator,ApplyCompanyGrantPlan{command_id,org_id,expected,plan}) validates and writes a normal scoped grant transaction. CompanyGrantPlan is untrusted typed data: {account_id,scope:closed approved PolicyScope,actions:Vec<ActionRef>,field_projection:GovernedRevision,valid_from,valid_to,reason}. refs point to actual registered current Company action/field definitions; resolve them by normal current schema owner reads after actual Company enrollment. Production uses these same grant APIs/guards. No bootstrap manifest silently seeds business grants. This is an exact proposed ordinary owner interface for the approved policy model, not a test-only bypass.

## Consumers and remaining proof

Files/modules: deployment_root_producer→deployment-root-producer.rs; account_enrollment_producer→account-enrollment-producer.rs; ordinary_issuer_bootstrap→ordinary-issuer-bootstrap.rs. Common TestResult is only Box<dyn Error+Send+Sync> alias, no fixture loader. AccountBootstrapPhase and EnrolledAccount retain B1 shapes. BootstrapOutputs{group,provider,identity_root,companies}; every member is actual returned owner data, not constructed verified state. Call publish_genesis_then_enroll_operator FIRST with actual deployment custody and configured root secret; enroll_fixture_accounts through actual crypto; then enroll_authority_and_companies(pool,&operator,&accounts,&FixtureIssuer,scenario_names,&untrusted-grant-choices). Normal current R2 Company/Group context resolvers consume the returned actual Account tokens.

All functions have0compiled/0executed status. Real Account-aware deployment/migrations/owners are absent. Raw fixture JSON or a signed custody hash alone never claims bootstrap ran. The source records concrete missing ordinary interfaces, not deferral of producer bodies. Workforce/Employment/profile/rule provenance/source19+24 composition and per-registration assertion coverage remain separate source work. Exact native monetary golden semantics and actual approved profile/build binding remain required before the fixture can run.

## B3 sequencing correction

B2's preassembled CompanyGrantPlan parameter was invalid: actual future Company ActionRef/field refs cannot exist before enrollment. B3 takes CompanyGrantChoices only: {account_id:actual already-enrolled Account UUID, action_keys:closed registered action keys, field_projection_key:registered projection identifier, scope_intent:closed Company/selection intent,valid_from,valid_to,reason}. This is untrusted user/config intent with no Company-scoped future refs. After each actual CompanyEnrollmentReceipt, fixed ordinary identity::read_registered_company_grant_plan(pool,current_operator,actual_org_id,&choices) resolves actual current Company scope/action/field definitions and returns CompanyGrantPlan as plain data. It does not grant permission. Then read_company_policy_control obtains exact current head, and apply_company_grant_plan authorizes/finalizes under real locks. No arbitrary callbacks, bootstrap result factories or generic fixture reference replacement. Account choices refer only to actual earlier-enrolled Accounts. B2 remains preserved with its independent sequencing finding.

## B4 terms predecessor and explicit verifier

The first actual operation is publish_genesis_then_enroll_operator in publish-then-root-producer.rs. It reuses the independent security packet's operator_custody_producer::CustodyFixture for real Ed25519 challenge signing and immutable operator approval/content custody; this is input generation, not an authenticated publisher constructor. Ordinary auth::terms_publication::{load_deployment_custody,begin_operator_authentication,finish_operator_authentication,publish_terms} verify the external OS/deployment credential and exact OperatorApprovalRef through dedicated console_terms_publisher role, without an Account session. Actual PublicationReceipt{id,previous_revision:None,revision:1,manifest_sha256} precedes root seed. Account read_current_account_terms returns exact BW31 TermsReleaseRef in reference; root producer compares receipt_id/revision/manifest_sha256 to actual publisher output before seeding. This reuses approved BW31 separate release authority (contract12;storage128-132), not new root Account authority over genesis terms.

Account/root producers now accept explicit &JwtVerifier from the SAME actual deployment config as the Account issuer; no ambient configured_account_verifier lookup can accidentally validate against another parallel test's issuer. Existing crypto owner configured PEM verification remains unchanged. Constructor/verifier alone never grants session authority: verify_account_session still validates actual DB Account/family/security. B3 future grant ref fix remains intact.

Integration dependency: copy the independently reviewed security packet operator-custody-producer.rs as module operator_custody_producer; this B4 source does not reimplement or weaken its verification/custody. Current source supplied by /root/readiness_final_verifier at /private/tmp/console-security-owner-assertions-s2-20260913/operator-custody-producer.rs; final frozen integration must record that exact file hash. This dependency is an authored source file, not missing fixture code.

## B5 signed provider revision and dependency custody

IdentityRootClaims now includes provider:IdentityProviderRevision with EXACT closed fields provider_id/revision/content_digest from the actual preceding register_identity_provider result. The verifier must compare this signed full ref to admission.provider and current configured provider head under owning guards. Same ES256 key reused in another provider revision cannot transfer the proof. The signature binds deployment,Group,provider revision/content,Account list,time,jti and synthetic evidence classification together.

Pinned independently authored publisher dependency is included byte-for-byte at dependencies/operator-custody-producer.rs, SHA256 2bc844f66b850e5eca954cd1f2cae13bce4cd871dedb4e35bf65a1be245d7f03. Root integration must preserve this version or independently review/hash a newer explicit dependency; an external mutable path is no longer sufficient custody. Existing consumers mount it as crate::operator_custody_producer.

## B6 actual fourth Account and third Human

AccountBootstrapPhase adds transfer_recipient: EnrolledAccount, enrolled with the same actual WebAuthn flow as the other Accounts. Supervised identity root binds submitter and same_human_alias to Human A, reviewer to Human B, and transfer_recipient to independent Human C. This supports real two-subject payroll and transfer responsibility fixtures; no Account ID is treated as Human identity. FixtureIssuer::issuer returns the configured signed issuer string. Native S2 composes actual Workforce/source/attendance owners after this bootstrap. All ordinary proposed owner interfaces remain implementation prerequisites; no runtime execution claimed.
