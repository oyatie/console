Migration readiness test candidate

Base fd94667fb9133f1ecb55831f43192bdc46776dc0. Root sole author; R1 review findings corrected; independent R2 review pending. Seven Rust owner tests are source candidates, not compiled product tests. Three root Rust files parse. Snapshot fixture machinery independently compiles using pinned Rust1.98.1/SQLx0.9 and runs actual PostgreSQL18.6 with seven assertions. No fake product implementation is supplied to make absent owners pass.

MIG01 extends integrated30 M0: independent logical catalog captures relations, schemas, indexes, columns/defaults, constraints/ordered FK tuples, definer functions and definitions/configuration, policies, triggers, roles/memberships, effective column privileges and default ACLs. ACL grant order and generated internal FK trigger numeric IDs are normalized; logical trigger definitions remain. This is an explicit catalog projection, not a claim to enumerate every PostgreSQL extension object's semantics. The owner must compare the SAME registered projection and version. Original46 mapped references must physically point to accounts(id) or company_actors(org_id,account_id), validated and without delete/update cascades; fenced references require actual writer-fence evidence. Historical nullability and populated legacy material preservation belong to existing account_migration tests and security MIG02 source, not inferred from CSV descriptions.

MIG04 exercises an actual drained business owner with incomplete native table catalog, rejected readmission, independent Account read, exact schema restoration and normal readmission before a fresh-context native action. It does not confuse missing source facts with dormant owner enrollment. Cold incomplete-release startup and M1-M5 mapping closure compose with security migration-transition-acceptance.rs. Account identity is admitted independently; an invalid business release cannot gain command service. Production pause/resume remains an authenticated operational owner; a caller-constructed AppConfig/descriptor/pause receipt confers no authority.

Exact responsible owner interface refinements for review: console_app::serving_admission load_installed_release/capture_enrollment_observations/read_actor_migration_classification/admit_process/pause_and_drain_effects/enter_compatible_read_only/advance_recovery_generation/read_recovery_generation; resume_effects(config,&actual_pause,&actual_admission) authenticates the configured operational principal, validates exact current observed release+pause and atomically resumes only the matched generation. load_installed_release returns a verified installed descriptor accepted by admit_process; descriptor parsing alone is untrusted. capture_enrollment_observations returns actual catalog and installed source, migrations, codecs, projections, aliases, credential holders and their hashes, never caller-filled proof.

Native Runtime retains the actual migration AppConfig and role-verified migrator pool already used by run_migrations. Release admission compares actual binary/catalog/codecs/projection/policy/alias+current independent recovery authority. enter_compatible_read_only returns a no-command-service capability and ordinary replica configuration for the registered compatible reader binary/hash/environment. The same real account browser ceremonies authenticate the reader. Existing completed intent may return original projected receipt; existing pending intent remains pending. A separate READY never-dispatched intent must get the exact transient OwnerActionError::ServingEffectsPaused (not semantic cancellation or an unrelated infrastructure error), no effect or command receipt. This guard error and resume_effects are explicit proposed interfaces, not currently implemented facts.

MIG06 uses the current source-image snapshot taken before a later actual native receipt and recovery-generation advance, restores bytes through pg_dump/pg_restore18.6, and retains the independently current authority endpoint. for_restored_database_preserving_authority_source changes ONLY the scoped data connection; the restored image cannot choose its own current authority. The test requires catalog equality before checking refusal, an absent later receipt in restored image and exactly one in original, precise stale/incomplete-timeline error, unchanged authoritative generation and original receipt replay.

Target placement: migration admission tests under backend/app/tests with native support; rollback tests as a child of existing account_migration.rs sharing its real immutable225 fixture. Existing legacy fixture old-migration tests remain unchanged. The migration-source APIs absent on base are prerequisite blockers, not behavioral RED admission for other owners. Main package will supply exact lane targets after consolidation.

Snapshot fixture safety: only current-server _sqlx_test_ databases with exact52-character suffix; generated owned restore identifiers; password only environment; pinned image already local; archive0600; actual catalog+row positive controls; mandatory awaited close plus scoped unwind cleanup; per-command90s deadline with named-container removal; cleanup10s deadline. Machinery runner owns one randomly named container, verifies final PID1 PostgreSQL readiness, always removes own volume/container. Seven checks calibrate the test machinery, not migration/authz behavior. Host pg_dump16.15 is not used against18.6.

Remaining gate boundaries: independent source review before test approval; full product compilation/owner tests after actual interfaces exist; maximum deployment/catalog size cost measured before slice acceptance; production restore/launch requires independent operational authority. No payment/legal or cross-tenant theorem is claimed.

R2 corrections: every remaining users FK requires an explicitly fenced writer; a live mapped writer with both new and legacy FKs fails. Full Company payroll_draft_runs before/after rows replace the nonexistent correlation_id column oracle. PayrollActionError::Owner(OwnerActionError::ServingEffectsPaused) is the exact proposed transparent domain-error wrapper preserving the common guard classification; does not coerce unrelated errors. Root migration machinery bytes unchanged except final deterministic SQL already validated in R1 run4; no new machinery runtime claim.
