Native S6 ordinary test input and negative oracle correction. Dependency: nativeS2 manifest7e9b50db134a0f45365e210f86190dacac1626a2373980c96e55b9ab4d66424d. Root sole repository writer. Only named files replace S2 counterparts; no owner implementation or authority activation.

Source19 state-contract requires common top-level nonblank revoke reason. Source24 contract8/14 retains it in CCF1 command, outside source19 input. RevokePayload must not gain a duplicate reason field. Existing binder omitted common reason. Add pure compile_registered_input_patches_with_reason(schema_snapshot, registered_input, reason:&str) beside existing patch compiler in responsible ontology application. It resolves common reason by ordinary registered schema field address and emits regular typed draft patches, including unchanged input patches. Missing/noneditable common field, nonblank requirement, max length and NUL constraints fail normally. No test registration/evaluator bypass. Existing no-reason entry preserves existing behavior; explicit test fixture callers author reason text. Source24 input bytes remain unchanged while normalized CCF1 command.reason uses actual saved common draft field. For body.reason registrations, duplicate values must match. A conflicting body/common value refuses; do not silently overwrite. Existing nativeR2 body.reason-or-null rule applies only when no explicit common reason exists and must be amended at consolidated design approval.

This surface is a material reason-mapping clarification requiring root independent design/test review. The signed immutable command contains the common reason; owner execution must enforce required reason even if a caller bypasses UI. Parser success is not behavioral execution. No approved typed public denial code enum was discovered; none invented.

Negative macro proves classified definitive no effect only. Caller controls: source proposal actual same-actor success plus malformed predecessor; source verify has actual same alias verification of another independently authored proposal and original independent reviewer success, preserving actual professional credential; source revoke actual same-actor successful current head after rejected stale head; nullable review-open actual same-actor correct snapshot success; native7 expected mutations now same-actor exact current control success after refusal. Withdrawal only proves that this submitter cannot withdraw another reviewer's decision while actual reviewer can; not a universal attribution theorem.

Pre-mortem: unrelated denial masks targeted invalid input; missing common reason prevents positive revoke setup. Detection: same-actor ordinary positive controls and source revoke owners accepting explicit common reason through unchanged input payload. No fabricated authority or hardcoded new reason codes. Source family revoke tests now reject blank common reason then succeed with authored reason and independently query exact ont_source_submissions command_bytes/input_bytes under the test readback role. They assert exact common reason and unchanged source19 payload separation. Readback role must have approved test-only SELECT custody for this table; no runtime grant is added. Full mismatched duplicate common/body reason proof for other registrations remains their owner test obligation; no duplicate body reason exists for these three source19 revoke actions. Root must reconcile material refinement with other binder changes (current_controls) before implementation.
