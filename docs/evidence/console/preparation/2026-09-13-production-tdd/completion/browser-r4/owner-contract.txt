BROWSER R4 — selected acceptance source and independently executable machinery
Base fd94667fb9133f1ecb55831f43192bdc46776dc0; root sole repository writer. Parent R1/R2/R3 manifests retain prior evidence and findings.

15ordinary-owner/Playwright fixtures:8no-JS,3JS-enabled SSR,4hydrated. Ordinary issuer/Account/Company grants, source upload/verification, native payroll/review/publication and Draft owners create state; no successful business or authority rows are fabricated. Existing11cases remain, with readonly expected canaries strengthened from3to4 to match actual forbidden person name, employee number, wage note and artifact narrative. These canaries do not prove aggregate coverage-count absence.

Four hydrated sources exercise real input-triggered autosave without Save, real offline stale conflict against a second browser commit, actual WASM asset abortion and labelled manual fallback, and offline input/reconnect preserving unacknowledged state. The native WebAssembly observer forwards unchanged inputs to captured native compile/instantiate APIs and records only actual successful instances tied to exact input SHA; tests require that SHA to match fetched appbytes. It neither fulfills routes nor replaces instances nor injects application events. Hashing instrumentation adds observation overhead; these tests are not performance benchmarks. The10browser-oracle controls include native WASM success/different-bytes and rejection controls, not Console production proofs.

Browser-observed DraftAck or actual manual form command must match exactly one new ordinary history receipt and the unique NEW head revision. Old receipts/revisions are unchanged; pagination checks complete counts and a pre-fetch page budget. A stale command cannot receive a save receipt. Each Draft browser case additionally reads actual payroll::read_contract_wage_history before/after and requires exact equality, so an unlinked erroneous wage effect cannot hide behind the Draft manifest. The existing no-JS save case uses this same receipt comparison.

transport-refinement.json explicitly proposes JSON200DraftAck/409safe ResultObservation on the existing composer POST path. This wire shape and UI copy require consolidated approval; earlier autosave behavior approval is not approval of invented response fields. Both transports use the same ordinary owner with operation, command, expected revision, editing token, CSRF and current Account/session/policy checks. Final current-context/current-policy emission fencing applies equally to JSON and SSR. No raw private diagnostics or hidden disclosure is permitted.

6Rust parser checks,8JavaScript parser checks,15compiled helper tests PASS;10actual browser-oracle tests PASS with pinned Node24.16/Playwright1.63/Chromium153. Discovery identifies15application+10oracle leaves. Owner Rust compilation0; actual application behavior executions0. Null/missing owner implementations remain execution prerequisites, never meaningful behavioral RED for unrelated lanes. Source mocks/fake owner outputs may not fill them.

Private fixture/receipt files0600 in0700directories; exact candidate and actual serving build identity checked. Runtime stays live while bounded asynchronous child runs (180s,8MiB per stdout/stderr, direct child kill/reap on error). Actual Chromium process-tree cleanup belongs the separate topology lifecycle gate; direct-child proof does not certify it. All resource/money fixtures synthetic and NONPAYABLE, without legal/payable claims.

Activate through the admitted root-owned test-wiring lane and final mechanical mapping: existing native_acceptance plus shared support/native modules; Playwright folder includes all required .cjs imports. Current_controls/common reason control compiler remains a pending consolidated design amendment.
