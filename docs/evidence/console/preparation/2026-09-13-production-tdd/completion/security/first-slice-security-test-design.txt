First-slice security and populated migration test-design handoff
Author: /root/fresh29_security, independent design adviser; root remains sole production/test writer.
Immutable source base: 58b6f758ac2a49b28589b9506628a43b6158e925.
Status: AUTHORED TEST DESIGN, NOT COMPILED/DISCOVERED/EXECUTED TESTS. No product or test files changed. No database, network, deployment or implementation work performed.
Lenses: Red Team; Zero Trust; Systems Thinking; Day-2; blast radius.

1. Authority and admission boundary

Design30 operations-recovery-information-flow.txt D/E/F and integration-corrections.txt10/11 are the primary obligation sources. source-sink-map.csv enumerates enrolled sinks. storage-mapping.json supplies Account/company_actors/context storage shapes. actor-migration.csv supplies the46 exact historical actor-reference mappings. PRODUCT27 admits the broad first vertical; proposed canonical enrollment at PRODUCT61/65 still needs its exact current-authority adoption before dependent implementation. These cases do not activate later communications, general export, external sharing or destructive provider cleanup.

Use these evidence states in root's matrix:
- SOURCE_PRESENT_NOT_RUN: actual named test source exists; this handoff does not assert it passed.
- BEHAVIORAL_CASE_READY_TO_AUTHOR: an existing production function can receive the fixture now. Its current RED is a hypothesis until exact compiled execution reaches the intended assertion.
- OWNER_INTERFACE_ABSENT: design names/types or owner transition are absent. A prose case or failing import is not behavioral RED and cannot admit an unrelated implementation lane. Root must bind an approved callable interface and reviewed executable prerequisite probe.
- CATALOG_PREREQUISITE_BLOCKED: a compiled DB test may fail at the exact missing relation/column/role assertion. It can admit only that catalog/role owner; downstream behavior remains unreached.
- EXECUTION_ENVIRONMENT_BLOCKED: toolchain, connection or fixture setup failed; do not relabel product RED.
- ACTUAL_BOUNDARY_RED/GREEN: exact candidate, named command, independently approved test, discovered/executed/reached counts and intended assertion recorded.

Preserve the existing account_storage ForeignKey::binds assertion and its blocked ephemeral correction exactly. The migration proposals below do not create a substitute test to bypass that correction. Ephemeral child-FK deletion semantics are separately reviewed; historical actor preservation is not a universal ban on every credential child cascade.

2. Existing executable evidence to reuse without overclaiming

A. Account/auth ownership: backend/app/tests/auth_rest.rs has actual HTTP helpers legacy_fence_fixture at3494, insert_account_fence at3563, account_browser::fixture at4191, snapshot at4360, company_free_registration_me_and_empty_contexts at4456, generation revocation at5511, and existing session/terms outage cases at5756/5824/5898. account_storage.rs has all46 exact FK census at96, Account custody/catalog tests157/231/268/303/358. The prior59-case run reached25 pass34 fail; this handoff does not rerun or reinterpret that evidence. Missing Account catalog blocks deeper cases. A fixture's direct insert_account_fence is not proof that a real M1 migrator drains sessions or safely maps populated data.
B. Freshness: backend/app/tests/cedar_freshness_mint.rs231/286/362 exercises DB freshness load, issuance and stale-after-bump; identity/adapter-postgres/tests/subject_authz_versions_freshness_rls.rs127/234 covers tenant floor and store bumps. platform/authz/tests/policy.rs449 and cedar_pbac_readiness_cases.rs452 reach the existing boundary with supplied requirements. None constructs design30 ActiveContext or proves exact context generation at final delivery.
C. Policy rows: platform/authz-rest/tests/cedar_authoring_rls_as_runtime_role.rs180/233/252/269/306 covers draft not enforced, simulation diagnostics, deny-by-omission, cross-principal row and forbid. ontology/rest/tests/object_policy_attach_as_runtime_role.rs239/462/751/2774/2994/3118/3451 covers hidden-row alias/graph/action closure and malformed persisted policy rows. Preserve all these tests. They do not establish field-group projection across actual payroll/identity SSR/live/export sinks. Source search finds property_field_visible only at its definition and authorize_property_field called by the policy endpoint, not by general resource serialization.
D. Public errors: platform/request-context/src/lib.rs963+ contains the already-reviewed source tests. Four actual formatter cases fail body equality after status/header checks; later assertions unreached. Do not count them as SQL-origin, trace or all-route closure.
E. UI: existing SSR/health10/10 evidence is separate-Group B mutation with positive controls and exact A status/header/HTML equality. It does not exercise field policies within the same Company, Account-context switching, hydration execution, public cache headers or all enrolled routes.
F. Live: platform/realtime/tests/hub.rs11/32/75 covers orphan prevention, bounded queue and recipient isolation; postgres_bridge.rs21/98/179/232/291 covers actual notification/replay. All hub fixtures use one Company. app/tests/realtime_ws.rs8 is only mounted/auth-gated upgrade. No current proof of post-upgrade revocation or Account-context generations.
G. Storage: platform/storage/tests/seaweedfs_worm.rs and evidence_processing_rls_surfaces_as_runtime_role.rs remain provider/processing fixtures, not egress admission or dedicated-issuer credential closure.
H. Existing populated-migration patterns: platform/db/tests/group_of_one_expand_contract.rs38 uses sqlx::migrate!().run_to to construct an actual old catalog; ontology/adapter-postgres/tests/key_revision_migration_upgrade.rs854/1186 cover exact old-binary writes and bounded populated upgrade for0165. Reuse the pattern and immutable old migration bytes; do not call their results Account migration evidence or reconstruct an old catalog by weakening new constraints.
I. Verus: preparation/2026-09-13-production-tdd/verus-source-binding.json explicitly binds SubjectFreshness::satisfies (platform/authz/src/cedar_pbac.rs77) and only that supplied-value computation. It proves >= comparisons including optional step-up, not exact active-context equality, authentic DB loading, projection/event/egress noninterference. Preserve its source hash, native6 tests, positive4 proof units and known-false/mutant records as historical bounded evidence.

3. Concrete behavioral test candidates at existing owners

SEC-CEDAR-01 — Allow plus runtime diagnostic must fail closed at compiled engine.
Owner: platform/authz/src/cedar_pbac/engine.rs; existing signatures compile_bundle_from_sources(OrgId,u64,&str,&str,&str)->Result<CompiledBundle,KernelError>, evaluate(&AuthorizationRequest,&CompiledBundle)->CedarEvaluation.
Proposed test file: backend/crates/platform/authz/tests/cedar_diagnostic_fail_closed.rs.
Fixture: use ROLE_MANAGE_SCHEMA with two policies: permit(principal, action == Action::"role_manage", resource); and forbid(principal, action == Action::"role_manage", resource) when { principal.subject_version + 1 < 0 }; use a real AuthorizationRequest with same Company principal/resource and subject_version=i64::MAX as u64. The schema declares subject_version:Long and production engine builds it at368. Assert strict compilation succeeds before evaluating. This should produce an evaluation overflow alongside the matching permit; inspect diagnostic witness through a separate direct Cedar evaluation in test if necessary. If strict validation rejects this vector, that is fixture failure, not the targeted behavioral RED; choose a strict-valid runtime-error vector without weakening validation.
Oracle: production result is fail-closed (Error or Deny), not Allow. Positive companion with subject_version=1 and same bundle must Allow; forbid-matching ordinary case must Deny if an additional well-defined forbid fixture is used. Current code engine.rs461 returns Allow without inspecting diagnostic errors; predicted intended failure. Run as cargo test --manifest-path backend/Cargo.toml -p console-platform-authz --test cedar_diagnostic_fail_closed (after authoring; not run here). Do not change the >= freshness contract to force this result; this tests the Cedar adapter's stricter diagnostic policy.

SEC-CEDAR-02 — Field and row helper must not discard evaluation errors.
Owner: authoring.rs706 simulate(&[AuthoredPolicy],&SimRequest)->SimulationOutcome; property_field_visible(&[AuthoredPolicy],SimSubject,SimResource,impl Into<String>)->bool at945; object_row_visible immediately before it.
Fixture: same-Company SimSubject, Resource owner=None, read_field action and field="salary". Policies: clean permit for Action::"read_field" and forbid for same action when { resource.owner == principal.user_id }. Missing optional owner produces a diagnostic. Independently assert simulate.errors is nonempty before asserting property_field_visible=false. Positive companion removes failing forbid and proves readable field=true; ordinary valid forbid case proves=false. Add equivalent object-row case if root chooses that helper's owner lane.
Meaning: malformed/persisted source handling at an existing public helper. This does NOT assert no-code strict authoring admits the missing-attribute policy. Preserve simulation diagnostics as protected authoring data; do not expose raw errors through generic object responses. No mutation of stored production policy needed.

SEC-LIVE-01 — Exact Company must participate in actual message fanout.
Owner: platform/realtime/src/lib.rs866 dispatch_event(org,event,authorized_users), public test seam dispatch_local_for_test(org,RealtimeEvent) at547 delegates to same production method.
Append to existing tests/hub.rs to reuse message_event helper. Make one Account/UserId, two distinct OrgId values and two connected principals with BranchScope::All, one per org. None replay cursor; buffer8. Send B message via dispatch_local_for_test(B,event). Await completion, hub.shutdown(), drain both receivers to closure. B must receive exactly that event (positive), A must receive no B event; then do a separate A event/control or compare two fresh fixtures with distinct B bodies. This uses no timeout as proof of non-delivery. Current filter899 omits org equality. Test seam bypasses actual DB membership so scope is fanout selection, not an end-to-end authorized-user exploit. Actual PostgreSQL bridge case must later load B data under B scope and have current same Account entitlement there without granting B observations while A is active.
Notification limitation: dispatch_notification(recipient,event) at838 and NotificationSummary have no Company/source context parameter. Do not invent org from a branch or recipient. Require a reviewed source-context-bearing interface (or keep enrolled notification adapter unavailable) before authoring exact-Company notification assertion. A same-Account/two-context notification test using the current contextless seam has no independent event-owner oracle and would overspecify guessed ownership.

SEC-EVENT-01 — Public telemetry input must use registered values, not syntax-only private labels.
Owner: app/src/console_telemetry.rs121 normalize(RouteTelemetryRequest), private test module can call it without DB; actual HTTP router40 and record_route_telemetry110 add persistence test.
Fixture: valid event_kind/route_surface, an actual shipped public route template as positive, and a known registered release identity. One-at-a-time negatives use a private-looking but grammar-valid segment path /people/salary_83000, error_name=private_salary_83000, release_cycle=private_salary_83000. Use accepted registered enum/code fixtures from the final admitted map; do not assert all current syntax-safe names are legitimate. Intended oracle: refusal or conversion to the same registered public class; raw supplied string never survives normalized public-event output. Existing normalize_route_path157 and normalize_bounded_label175 return those values unchanged. Root must fix the exact public registry contract before test approval, since syntax-only refusal is not sufficient and arbitrary invented allowlists are overspecification.
HTTP follow-up: existing app/tests/console_route_telemetry.rs21 has signed token/real route+row helpers. After positive row insertion, snapshot rows and submit invalid labels. Assert no raw private label reaches rows/public tracing; do not merely check422. Authorization failure and malformed input have separate expected public outcomes.

SEC-EVENT-02 — Real DbError/request-context conversion cannot log private diagnostics publicly.
Owner: app/src/console_telemetry.rs259 From<DbError>,276 From<RequestContextError> currently tracing::error!(error=%err).
Fixture: install a test-only tracing subscriber/writer on scoped dispatch, convert two errors differing only in synthetic private suffix, call actual IntoResponse, capture fields/event sequence. Use DbError::Sqlx(sqlx::Error::Protocol(private_string)) for a deterministic formatter-boundary case; it is not a database-generated witness. Also use RequestContextError::EffectivePolicy(private_string). Compare safe public error status/body and captured public registered fields; require no canary. If random incident IDs differ, compare under shared injected public randomness or verify same public schema plus independent opaque-random identity; never strip arbitrary bytes to manufacture equality. Keep protected audit channel separate and explicitly scoped. Current raw error fields predict behavioral RED. Once formatter is repaired, force a real DB error through the admitted HTTP route with synthetic payload and audit public vs restricted exporters; that is a distinct owner/sink test.
Event schedule companion: two equal public requests with private source-state variation must have equal public event count/order/code, even if bodies contain no canary. Single string scan is insufficient.

SEC-EGRESS-01 — Actual configured HTTP client must not follow redirects.
Owner: platform/storage/src/lib.rs268 SeaweedS3Storage::from_config(&S3StorageConfig), get_bytes(&str,&str)388 and delete_object_version417. Current from_config uses reqwest::Client::new().
Fixture: local synthetic server A returns302/307 to local server B containing a unique synthetic object path. Construct through production from_config, not from_parts with a pre-hardened client. Use GET get_bytes, since this can be exercised without real storage or destructive effects. Positive A200 returns exact synthetic bytes. Redirect case must return refusal and B request counter remains zero after producer completion/connection closure. B returns immediate200 if contacted so completion is observable; do not use sleep-only absence. This catches a real HTTP boundary; does not prove DNS/TLS/provider credentials or egress policy.
Scope judgment: current adapter permits local development HTTP; do not globally require TLS on every from_parts/local fixture. Production protected binding must require exact scheme/host/port+provider/credential identity, no userinfo/URL token, no automatic redirects and no environment proxy. The protected admit_bound_egress interface is absent. Testing localhost HTTP transport is a fixture, not production HTTP authorization. Proxy influence test belongs in an isolated subprocess with synthetic proxy listener to avoid shared env mutation. TLS wrong-host/wrong-CA tests require a local certificate fixture and production protected constructor, not a permissive reqwest client substituted by the test.
Do not broaden this GET test into executing real provider DELETE, external sharing or later communications.

4. Missing owner interfaces: exact target shapes and concrete acceptance cases

The four names below are absent from production sources. Signatures are proposed mechanical seams preserving design30 types, not claims of existing APIs. Root must bind the concrete owning module and approved exact type definitions before compiling tests. Prefer existing application owners and adapters; do not add a parallel security framework or depend on untrusted payload booleans.

SEC-CONTEXT-01 — bind_active_context at existing request-context/auth application boundary.
Existing caller resolve_principal_from_bearer_token(&JwtVerifier,&PgPool,&str)->Result<Principal,RequestContextError> at310; it trusts verified legacy token freshness and resolves branch/policy material. Proposed internal async fn bind_active_context(authenticated_session: &AuthenticatedAccountSession, requested: ContextSelection, expected_context_generation: i64, authority: &CurrentAuthorityOwner)->Result<ActiveContext,PublicFailure>. Account/session must come from authentication; ContextSelection may name a requested Company/Group but is not evidence of entitlement. ActiveContext fields exactly account_id/session_id/security_generation/context_generation/group_id/company_id/topology_revision/policy_revision/serving_generation from OR28D. No public Deserialize or public constructor bypass.
Fixture: Account X with current sessions and authorized A/B contexts, A active; Account Y with only A; two actual Groups for cross-tenant case plus same-Group two-Company case. Positive A render includes A permitted marker. Request B resource under active A is denied even if X could switch to B. Perform real context switch and require generation increment; old cursor/projection/control handle/subscription is refused, fresh B session-context succeeds. Mutate one field at a time: security generation, context generation, topology revision, policy revision, serving generation, session ID, Account, Company membership incarnation. Both stale and unexpected-future generation must fail if exact active binding required. Existing SubjectFreshness minimum comparison remains intact; enforce exact context at its new owner boundary.
DB tests must use real restricted LOGIN connections and current readback, not caller-supplied is_current=true. Catalog prerequisite: accounts/account_security/company_actors and scoped current authority; missing row is fail-closed with no business write. Bootstrap Account with no Employment/Company remains able to use independently authorized identity operations; no universal login guarantee for suspended/recovery state.

SEC-PROJECTION-01 — project_authorized_result at owning application projection.
Existing places: identity/rest::VisibleDirectoryPerson consumed in app/lib.rs3725; payroll/rest owner reads256; ontology/rest row-policy owners1416; app SSR load3597. Proposed internal fn project_authorized_result(context:&ActiveContext, request:&ProjectionRequest, source:OwnerResult, decisions:CurrentFieldDecisions)->Result<AuthorizedProjection,PublicFailure>, with nonforgeable decisions constructed by actual policy owner and output privately constructed for serialization. This signature is a schematic seam: no assumption that supplied decisions from tests prove current Cedar. Real adapter integration must establish their origin and decision linearization.
ProjectionRequest fields: resource_ref, registered_action, field_group_ids, purpose_id, sink_kind, recipient_binding?, expected_context_generation. SinkKind exact SSR/JSON_RESPONSE/LIVE_UPDATE/AUTHORIZED_AUDIT/OPERATIONAL_EVENT/BOUND_OBJECT_EXPORT. Output cannot expose internal Result, PreparedNative, private receipt payload, whole-object revision or hidden fields through blanket Serialize.
Fixture pair1: identical A-visible state and public environment; mutate B private name/salary/run count and ensure B mutation actually persisted under B owner with positive authorized B read. Observe A HTTP status/full headers/body/byte length, SSR HTML+hydration props, cache-visible data, live topic/event count/order/content, public events and admitted outbound destination/body. Equal logical outputs across pair; no canaries. Pair2: same Company, permitted base salary and hidden tax/other employee field; permit independently controlled field group only. A positive allowed value must appear, hidden values absent from every enabled alias including list/detail/history/search/graph/link title/count/conflict/preflight/replay. Existing row-only tests are reused, not relabeled as field evidence.
Snapshot_revision is an opaque server-stored256-bit projection handle (integration-corrections10). Do not demand equal independently randomized opaque handles across worlds without coupling public randomness; test identical authorized projection state and absence of dependence on secret revision. Change hidden field and verify old visible projection handle remains valid under the scoped contract unless an independently authorized precondition changed.
SEC-PROJECTION-02: execute once, retain original receipt, then revoke a field/resource grant and replay same command ID. Business effect count remains1; returned outcome is only currently authorized projection, possibly denied/unavailable. No second effect or unconditional historical payload disclosure. Positive unrevoked replay confirms receipt continuity. Optional gate/review routes must not disclose global coverage to a scoped reviewer.
SEC-PROJECTION-03: CONTROL_REFERENCE15minute exact typed handle bound to Account/session/context,Company-or-Group/action/field/schema/expiry. Cross-binding replay or expired handle fails before any effect and preserves permissible user input; fresh authorized render succeeds. A raw private reference in public form must not substitute for handle. Do not require global-cycle equality for scoped basis card.

SEC-EVENT-03 — build_safe_operational_event at existing telemetry/tracing boundary.
Proposed internal fn build_safe_operational_event(code:RegisteredEventCode,route:PublicRouteTemplate,incident:OpaqueIncidentId,result:PublicResultClass)->SafeOperationalEvent. No arbitrary String for code/route/result and no raw tenant/person/resource ID, salary hash, URL query, SQL/binds or Debug credential. Random identity factory must be outside secret-dependent computation. Test each constructor path and public Serialize output; compile-fail tests can verify private constructor/raw Result cannot be serialized, but compilation failure is a type-boundary result, not the relational proof.
Pair fixtures require equal public event count/order, not only safe individual records. Secret-dependent branch emitting an extra perfectly sanitized event is a required negative mutation. Optional duration buckets are explicitly outside logical noninterference; don't pretend timings identical or make wall-clock tests flaky.

SEC-EGRESS-02 — admit_bound_egress at storage/notification transport boundary.
Proposed internal fn admit_bound_egress(context:&ActiveContext,projection:AuthorizedProjection,binding:&AdmittedEgressBinding,recipient:RecipientBinding,purpose:RegisteredPurpose,now:CurrentTime)->Result<BoundEgressRequest,PublicFailure>. Admission binds exact scheme/host/port/provider/credential/recipient/purpose and current projected bytes. Actual transport accepts only BoundEgressRequest. Use synthetic A/B destinations and opaque fake credentials, mismatch each binding field independently and assert no outbound request. Positive exact binding makes one request with only allowed bytes. Revocation after projection/before actual admission must block; define exact admission/linearization and post-admission in-flight semantics rather than promise instantaneous recall. Inspect retries/redirect/proxy paths separately.
No email/sharing token implementation admitted; first-slice unavailable adapters must refuse before constructing a network request. Raw presigned URLs and general CSV exporter marked UNAVAILABLE_FIRST_SLICE in source-sink map must be tested at actual routes as unavailable for protected data, while preserving unrelated authorized legacy behavior until explicit enrollment.

5. Populated migration acceptance: build actual old state and drive real owners

Owner boundary: existing platform/db migration runner plus identity/auth migration/cutover owner, with domain owners for the46 actor fields. No Account M1/M5/M6 migration runner function exists at this base. Do not invent a test-only SQL backfill and call its green result migration acceptance. Required callable production interface must be chosen and independently test-reviewed; proposed forms below specify receipt semantics, not new public APIs.
Use #[sqlx::test(migrations=false)], apply immutable old migrations through the exact approved pre-Account boundary using sqlx Migrator::run_to, seed populated old data through allowed old owners, snapshot exact rows, then invoke the actual new migration owner as real migration LOGIN. Never run a destructive downgrade against shared workspace/database. Fresh final-catalog tests remain separately useful.

MIG-01 M0 snapshot closure.
Catalog snapshot includes pg_class namespaces/owners, pg_attribute, pg_constraint with ordered key tuples, pg_policy, pg_trigger, pg_proc proowner/prosecdef/proconfig/function definition, pg_roles membership/inheritance, ACL/default ACL/effective column grants; actor-migration46 plus all discovered extra references explicitly classified. Bind immutable source SHA, migration checksums, action/alias/codec/projection manifest and credential-holder enrollment digest. Mutate one protected ACL/function search_path/alias manifest in disposable fixture; actual enrollment probe must reject mismatch. No regex-only catalog assertions or superuser SELECT masquerading as runtime isolation.

MIG-02 M1 identity and credential continuity with irreversible auth fence.
Fixture: two Companies in different Groups, at least three legacy users (normal, already revoked family, unresolved natural-person linkage), an active and revoked passkey, refresh family with used predecessor/replaced_by link and current/revoked token hashes, outstanding WebAuthn ceremony, employer OTP and device handoff. Seed through actual auth helpers where available, record credential byte hashes and correlation IDs without printing secrets. Include populated user_role_assignments, subject_authz_versions and representative workflow/governance/payroll/audit actor rows with valid nonnull and historically nullable actors.
Invoke actual cutover owner after readiness verification; Account IDs preserve legacy users.id and CompanyActor preserves legal Company attribution. Bytes/counter/revocation timestamps/token-family correlations remain identical except explicitly specified security-generation/fence changes. Every selected actor FK matches Account or exact(org_id,account_id) CompanyActor target. Employee/Person subject FKs remain Employee/Person, not retargeted as actors. Historical nullable actors remain nullable and are never filled with fabricated human identity. Migration may quarantine unresolved source mapping; it must not invent verified human/grant/provenance.
Global cutover denies pre-cutover access JWT/body refresh/cookie refresh/OTP completion/passkey session creation and upgraded streams. This requires concurrent actual old transport attempts at a test barrier around durable fence, not only inserting account_security manually afterward. A fresh independently authorized identity session can succeed when current policy permits. Returning RECOVERY_REQUIRED or SUSPENDED to ACTIVE must not revive old credentials/sessions. No unrelated Company payroll mutation occurs. Existing tests3371-3756 supply precise transport oracles and must remain.

MIG-03 M5 restartable bounded mapping.
Proposed owner method migrate_batch(manifest_digest,source_cursor,source_version,batch_id,limits)->MappingReceipt with source keys+versions, mapped/quarantined keys, target IDs, next cursor and durable exact receipt; receipt must be owner-produced, not constructed in test. At least501 small valid inputs tests row boundary; a separately sized payload set crossing4MiB tests byte boundary. Batches <=500rows and4MiB, configured statement candidate limit2s; observe real settings and deterministic completion/lock outcomes, not an assertion on elapsed wall time alone.
After a committed first batch, simulate response loss by dropping only test client's response, rerun same batch ID, require exact same mapping/receipt and no duplicate target. Interrupt worker before commit and after commit using barriers; restart actual owner. Verify source input set equals mapped disjoint-union quarantined by exact source key+version, not counts alone. B-private and A-private same-looking identifiers cannot collide. Unresolved source provenance stays explicit quarantine and blocks activation for affected required population. A quarantine fixture must be valid under old schema (e.g. optional missing natural-person/source evidence), not seeded by disabling old constraints.
Concurrent legacy writer changes source version after snapshot and before mapping: actual migration either excludes writer at a witnessed lock/fence or rejects/reconciles that exact batch. Test no stale payload silently accepted and no omitted required payroll person. No success from merely retrying a test helper SELECT.

MIG-04 M2-M4 dormant coupling.
After additive schema but before all required source/definition/draft/gate/work/projection/codec owners exist, real enrolled commands must refuse new-format activation while safe independent identity operations remain available. Assert no active native input, receipt, consumed gate or public projection for incomplete enrollment. Existing historical read/receipt bytes preserved. This is an enrollment contract, not permission to require every later suite capability before first-slice work.

MIG-05 M6 mixed binary and manifest enrollment.
Actual startup/enrollment interface should consume observed catalog+binary+codec+projection+policy+writer-alias manifest and current serving/recovery generation. Launch one old and one candidate instance against populated additive schema; old writer remains fenced or a specifically proven compatible path. Mutate each enrolled identity independently; incompatible instance must not acquire command service authority. Concurrent startup cannot apply migrations independently; witness single migration owner lock and second process refusal/wait without partial catalog writes. Do not use a test-only compares_equal(manifest) function as proof startup obeys it.

MIG-06 rollback before/after new writes.
Before new-format writes: rollback keeps additive schema dormant and preserves old readable business rows, but cannot undo permanent auth fence. After a new-format write: run exact supported compatible reader binary against preserved new rows; pause new effects; preserve identity/receipt/UNKNOWN attempt/protection/revocation and immutable source references. Replay old queue item must not create fresh effect. Restore an older disposable snapshot missing a later revocation/receipt and require actual admission owner to remain isolated absent independently current timeline/generation. Missing newer effect in old snapshot is not no-effect proof.
No physical disposal drill is required to author first-slice migration tests; any actual disposal/cutover remains X/production gated. Where independent current authority holder/timeline is absent, recovery readmission test is owner-prerequisite blocked, never simulated green authority.

6. Verus and sink closure: concrete proof preparation

VERUS-01 source binding: preserve original supplied-value proof. For each actual new function bind source Git blob, exact executable body/struct-field identity, feature/toolchain/compiler/Verus/Z3 pins. Ghost views may describe source data but cannot replace projection body with a toy model. Any rename needs mechanical mapping to OR28D four functions and all callers. List actual assume/external_body/unsafe/FFI and refine each external loading/output relation; no assumption that allowed=true or adapter outputs match across private worlds.
VERUS-02 relational predicates: define low_equivalent(context,policy,public_environment,state_left,state_right,allowed_releases). Cross-Group private-state variation and separately same-Company hidden-field variation. Logical observe includes status/headers/body/length, SSR/hydration, cache-visible output, stream count/order/topic/content, public event schedule and egress destination/projected payload. Integrity property compares untouched B-owned state after an A command. Couple public randomness/clock where specified; exclude real timing/traffic/shared-resource and ciphertext equality explicitly. Current ActiveContext exact equality is distinct from minimum freshness predicate.
VERUS-03 nonvacuity/mutations: valid A value actually emitted, valid B operation actually changes B, hidden value omitted; independently mutate wrong Company acceptance, missing field redaction, extra secret-dependent safe log event, and unbound egress. Each breaks the designated source-bound proof or actual boundary test. Import/parse/toolchain failure is not proof rejection. Report verified units/failed obligations separately from runtime tests and source-discovered fixtures.
VERUS-04 actual sink enrollment: for each source-sink-map row record route/job caller, owner loader, projector/error policy, concrete serializer/emitter/client, current-context recheck, actual test and remaining external assumption. ADAPT requires working binding; UNAVAILABLE_FIRST_SLICE/ADAPT_OR_DISABLE requires exercised fail-closed route or non-enrollment. Grep and compile-private types help locate bypasses but cannot close SQL operand loading, dynamic route alias, external logging or process credential boundaries.

7. Overspecification challenges and stop condition

Do not: change SubjectFreshness >= into equality; require global status for scoped reviewer; require all Account identities always login; serialize original receipt unconditionally on replay; forbid every nullable historical actor; force all FKs to one deletion action; claim fake-Buck mutational tests establish product credential security; demand global TLS prohibition in explicitly local test adapter; activate later communications/sharing for egress coverage; require production recovery/provider drills before writing tests; or defer populated migration safety until public exposure.

Stop this adviser task when source-bound existing-owner cases and absent-interface/populated-migration cases are concrete enough for root's reviewed test candidate. Completion of this handoff is not completion of executable first-slice preparation. Root must author, independently approve exact test SHA, compile/discover, execute actual positive/hostile/prerequisite probes, record reached assertions and retain every remaining absent-owner HOLD before any implementation admission.


8. Concrete populated Account context and operator publication assertions

CTX-01 authorized populated pagination: seed32Group/256Company bounded maximum via actual topology, Account grant and context-source owners. Distinct permitted labels, one permitted context identity with denied label, and one undiscoverable context; query GET /api/v2/accounts/me/contexts?page_size=1. Exact collected context_ref set equals independently authorized fixture set once each; no hidden item/count/label or denied-derived sort key. Omitted label still has entry_ref:{kind:WORKSPACE}; no CompanyActor rows created by reads. Positive membership/grant source rows must be read back before query. This is not achievable by inserting fabricated candidates alone.
CTX-02 current authority and temporal cursor: after page1, revoke a previously allowed context or make a previously denied candidate allowed through actual policy owner; increment proper source/context generations in same transaction. Old cursor409 stale_cursor, no hidden ownership distinction; fresh whole scan reflects authorized change. Use a fixed clock/boundary around earliest whole-scan temporal transition to catch denied->allowed candidates not in page1. Expired cursor at5minutes, foreign Account cursor, changed query and unexpected-future generation all refuse. unavailable authority503 navigation_unavailable; empty successful scope200 contexts:[] without next_cursor. These precise cases remain catalog/owner-prerequisite blocked until populated discovery exists.
CTX-03 keys across replicas: BW31 single-key issuer/public fingerprint exact at startup. Two app replicas with distinct fixture keys and same DB cannot both report admitted Account service; mixed-key readiness fails. Stop/drain/revoke Account families, replace all keys and restart; old access/refresh/CSRF invalid, fresh primary login works, original identity and historical terms remain. The existing csrf_wrong_key test validates request signatures only; it does not witness startup homogeneity, drain or rollback. No key-rollover implementation is assumed.

PUB-01 external operator custody: BW31 contract12 publication, not user endpoint. Proposed actual publisher call/CLI must resolve a local immutable TermsPublicationApproval in admitted deployment custody, authenticate approved_by and independently qualified content_authority_refs, validate exact manifest/artifact digests, then mutate. Supply identical JSON/hash via an untrusted app request/path; refuse before any receipt/head change. Missing/unreadable custody artifact, unknown operator or missing qualified content reference also refuse; snapshot head+receipts exact. Trusted TEST_ONLY fixture may be used only with explicit test environment; production mode rejects TEST_ONLY. A syntactically correct approval hash alone is never the positive control.
PUB-02 tuple/CAS: with head revision7, valid actual custody approval expected7/next8 and exact digest M yields one immutable receipt(id,previous_revision7,revision8,manifestM,exact approval bytes) and head(8,M,same receipt). Two concurrent publishers with expected7: exactly one succeeds, loser reports current conflict without second receipt or head mutation. Same manifest bytes republished get a new revision/receipt; A->B->A cannot revive old registration. Genesis1 from absent head is separately admitted; max signed bigint fails closed without wrap.
PUB-03 atomicity: trigger a real insert/commit failure in disposable DB after approval validation but before publication transaction commits; both receipt and head remain old. Exact response loss/retry behavior must be specified by actual publisher registration before choosing idempotency oracle; do not invent replay semantics absent approval. Independent receipt/head readback after success required.
PUB-04 custody integrity: immutable manifest1..8 required distinct kinds, each digest resolves exact registered UTF-8 content <=64KiB and wrapper<=16KiB. Alter one content byte, alias digest/path, duplicate kind, unknown kind, unsupported locale/title shape, symlink/path escape or unavailable registered content; actual publisher refuses without mutation. Existing HTTP artifact reads prove serving behavior, not custody publication approval.
PUB-05 publication vs finish locks: actual registration holds account_security then head FOR SHARE while successful finish commits. Pause through a witnessed lock; publisher head update waits until consent commits with exact old tuple, or publisher wins and finish rejects terms_changed with no partial activation. Assert pg_locks/blocker identity and correlated receipt/head/acceptance/security event after release. FOR KEY SHARE is a targeted negative mutation because a non-key digest/revision UPDATE could pass. Do not count a timeout as contention evidence or directly update head in place of actual publisher.
PUB-06 privileges: console_rt and company command LOGIN cannot modify head/receipt via table/column grants, default grants, role membership, PUBLIC or callable SECURITY DEFINER bypass. Authorized Account publisher role can perform only admitted operation; auth readers can load current/retained authority but not publish. Inspect effective privileges plus actual denied statements and successful publisher positive. Role name/ACL existence alone insufficient. Head lock must not take locks on all Accounts.
PUB-07 rollback/mixed release: restore old artifact configuration while DB head newer; enrollment503 authority_unavailable, existing independently eligible Account operations remain safe; no stale acceptance/default head. New binary missing a retained accepted digest must not delete historical acceptance or invent a release receipt. Production publisher/qualified content custody remains separate authority HOLD even when these synthetic operator tests pass.

Publisher production source is absent in inspected auth/tool paths at58b6. Existing seed_terms4168 and seed_next_terms_head5604 are test-owned SQL insert/update helpers and cannot be used as the publisher under test. Until root binds the actual operator entrypoint and custody resolver, PUB cases are OWNER_INTERFACE_ABSENT with authored exact assertions, not test-discovered, executed or approved publication authority.
