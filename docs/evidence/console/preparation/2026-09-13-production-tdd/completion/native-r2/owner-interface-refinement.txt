# R2 exact proposed owner boundaries and sequential fixture composition

This is an approval-ready interface refinement, not a claim these new symbols exist. Base58b6, R1 preserved separately. All new Rust symbols are explicitly enumerated in owner-entry-map.json and signature-contract.rs; a missing module/type is an implementation prerequisite, not an invented product assertion failure. The R2 reference tests concern byte/schema behavior only. No Rust owner fixture is claimed compiled, discovered or executed.

## Exact responsible modules and mapping

Use existing owning packages: console-ontology-adapter-postgres, console-payroll-adapter-postgres, console-attendance-adapter-postgres, console-governance-adapter-postgres, console-workflow-runtime-adapter-postgres, console-docs-adapter-postgres, console-leave-adapter-postgres and console-platform-group. Runtime imports use their underscore names; workflow includes `runtime`. Existing Group package is backend/crates/platform/group, with group_member_orgs/group_admin_member_orgs/consolidated_read at lib.rs:38/77/114. The R1 claim that no Group crate exists and proposed ontology Group placement were wrong and are withdrawn. Extending this existing Group owner with action30 control commands remains an explicit new interface requiring Group owner review.

Each55 registration has a fixed proposed Rust action30::<key_with_underscores> command entry, separately mapped to its approved private finalizer label. Labels such as attendance_source_api.commit_calendar and payroll_source_api.commit_proposal are schema-qualified SQL identifiers, never Rust expression paths. Shared source SQL functions retain fixed registration/family switches; callers cannot choose family/SQL/function. The map preserves every approved label byte-for-byte and names the exact proposed Rust wrapper independently.

Concrete inputs/results remain in the responsible existing owner module, using/reexporting existing owner-domain types where available; new adapter command DTOs do not force domain→ontology/application. Pure shared schema/writer/patch and observation/locator DTOs remain ontology/application and acquire no SQLx. Test composition can use existing owners as dev-dependencies (the current workflow manifest explicitly uses payroll this way); no adapter-to-adapter normal dependency is introduced. Fixed borrowed participant ports remain data/trait contracts implemented by the existing composition, never arbitrary callback/SQL interfaces.

## Authentication binding is separate from immutable command identity

Existing platform-authz Principal (lib.rs:946) is public-field/Deserialize and lacks exact session/context identity. It is neither accepted as the new command entry's only authentication input nor converted into a private capability.

Propose `console_platform_auth::account_session::VerifiedAccountSession` with private constructor/fields, no Serialize/Deserialize/From<Principal>/test constructor. Existing auth owner cryptographically validates ACCOUNT_V1 using configured ES256 issuer/audience/kind and actual13 claims: sub(Account), sid(existing refresh-family UUID), jti, iat/nbf/exp, security_generation, auth_time, typed assurance. sid is the existing durable refresh family, not a new sessions table. The auth owner validates the exact family Account/protocol/revocation/absolute lifetime and actual Account security generation; no missing generation or arbitrary live-family lookup. Existing ACCOUNT_V1 issuer/bootstrap is an explicit implementation prerequisite.

Propose `console_platform_request_context::account::resolve_company_context(pool,&JwtVerifier,raw_account_access_token,&UntrustedCompanySelection,&ServingAdmissionBinding)->AuthenticatedCompanyContext` and parallel `resolve_group_context(...,&UntrustedGroupSelection,...)->AuthenticatedGroupContext`. Only these actual resolver paths create the opaque context types. Their private data identify verified account/family, token generation/expiry/authentication age/assurance and exact selected Company or Group plus current context_generation and the independent current serving-generation binding. Selection is untrusted input and cannot grant Company/Group authority. ServingAdmissionBinding is issued by the existing process/recovery admission owner, not a test bool or retained command field. Tests obtain the same resolver outputs from real owner-issued access credentials. No raw token or ephemeral session/context/serving value is serialized into CCF1 or OWNER_SUBMISSION.

Worker routes accept a distinct `console_platform_request_context::worker::AuthenticatedWorkerContext`, issued from actual service authentication and fixed worker/job admission, with exact worker principal, job/operation namespace, Company/Group scope and serving binding. It cannot be derived from a human context or flag. Artifact verification/draft expiry and native preparation progression use this type. Workers retain the originating command Account attribution and independently revalidate its required current business authority; a worker credential does not impersonate the original Account/session.

Each owner private `resolve_current_ctx_in_conn(&mut PgConnection,&AuthenticatedCompanyContext,&StoredCommandLocator)->OwnerCommandCtx` reloads the exact named Account/family/current context/serving state and immutable registration/original command custody in the real owning transaction. The Group variant accepts AuthenticatedGroupContext and GroupCommandRef without Company RLS. Worker variant accepts actual worker binding plus stored original command/job linkage. OwnerCommandCtx has no public constructor or deserialization. Finalizer rechecks current Account/session/context/serving/Company or Group policy/field/Human/credential/task/source/gate/protection guards under the complete approved lock order immediately before effects; a private context is not a timeless allow proof. Expiry is checked at authoritative final time. Receipt-first reconciliation remains before new-effect guards.

## Admission classes and exact observations

For the first fixture chain, use actual draft admission for business actions, rather than declaring every registration DIRECT. Business entry signature: `async fn key(pool:&PgPool,auth:&AuthenticatedCompanyContext,attempt:&AttemptRef)->Result<OwnerExecution<ResultType>,OwnerActionError>`. It loads exact original stored attempt/command/input and never takes a second replacement input. Source19 final actions require actual source24 preparation+seal, then this same exact attempt locator. Ordinary supported direct business routes, if separately admitted, can receive a distinct UnadmittedDirectRequest<I> and perform common.admit_direct_intent before effects; no such direct authority is inferred merely to ease these fixtures.

Existing common draft/attempt controls are explicitly direct control commands. Signature: `async fn key(pool,&AuthenticatedCompanyContext,UnadmittedControl<Input>)->Result<OwnerExecution<Result>,OwnerActionError>`. UnadmittedControl has only new original command_id and exact typed input, plus actual registered control binding if that action requires one. Owner fixed dispatch loads current control registration, normalizes/reserves original input through the actual direct-intent protocol, then finalizes. It does NOT require preexisting direct custody for first admission. `attempt.execute` control loads the original domain attempt; no wrapper OWNER_SUBMISSION is created. `artifact.verify_version` and `draft.expire` use UnadmittedWorkerControl<I> and AuthenticatedWorkerContext. Four Group commands use GroupRegisteredActionRequest<I> with `GroupRequestIdentity {command:GroupCommandRef,action:GroupActionRef,original_account_id}` and AuthenticatedGroupContext. Group control fingerprint/receipt format remains its separately approved algorithm; it never enters the18-field Company envelope or uses a sentinel Company.

`OwnerExecution<R>` is the exact internal distinction `{Completed(R), Observation(ResultObservation)}`. R is the approved actual terminal owner result, never a pending success-shaped object. ResultObservation remains the existing closed action30 SUCCEEDED/DEFINITIVE_NO_EFFECT/UNKNOWN/UNAVAILABLE/PENDING schema; PENDING carries original ReconciliationSubject and phase BUILDING/READY_TO_FINALIZE. Native prepare/calculate first commit command-bound BUILDING and return PENDING with no terminal business receipt. A fixed authenticated worker progresses real bounded staged evidence to SEALED and activation under original command/current guards. Observation of the actual original owner receipt then yields Completed(R); only that permits the next input binder step. Known active is PENDING, uncertain response is UNKNOWN; unavailable/blocked stops, never invents terminal results. Public transport still returns closed PublicOutcome, not these private results.

## Exact sequential binder/read/producer

`binder-sequence.rs` calls real proposed fixed ontology entries: draft_start with actual resolved registration/schema/target; compile exact typed input to registered stable-field patches through a pure sealed-type/schema function; draft_save against actual returned head/token; source_prepare_submission when the fixed action is source24; reread current draft continuation and compare saved head; draft_seal with actual preparation unit; then actual originating attempt execution. Each control command UUID is allocated immediately before its own actual invocation. There are no future preassembled RegisteredActionRequest values, caller closures or private witness constructors. Inconclusive/unknown control result stops and exposes its original reconciliation subject before any next command is constructed.

Proposed fixed `payroll::action30::read_run_control(pool,auth,run_id)->RunExpected` reads all six exact approved fields: run_id/run_revision/input_revision/calculation_revision/close_basis/review_cycle_id. It verifies current auth/disclosure for this internal owner use; public clients receive only allowed fields or existing CONTROL_REFERENCE handles through the common binder. Never derive missing fields from RunResult: it lacks close_basis/review_cycle_id. `read_preparation_control` adds actual expected_membership_revision from the current owner source-selection control. All fields are fixed before sealing; concurrent changes after read are stale refusal, not automatic refresh during finalization.

`native-owner-producer.rs` now accepts unsealed user choices and actual auth/worker contexts, performs create, reads full controls, constructs/binds/executes prepare, advances/observes the original preparation until actual completion, then reads/binds close, calculate, review-open, each submit, and each independent decision. Every next request is created only after its actual predecessor result/control read. Preparation polling is bounded; it never treats timeout as rollback or allocates a replacement command. It stops with the same subject when outcome stays unknown. Review decisions use the actual returned request, not an already-built future decision. The producer reads all actual cycle bases under a bounded complete owner read and final completion, without hidden-ref HTTP export.

Actual auth/bootstrap, docs upload/verification, issuer-trust and independently verified source19+24 facts remain needed to provide the typed starting user choices and evidence refs. They must be created through their real owners; no fake source verification, grant or issuer bool is authorized. The proposed source producer includes the concrete draft/source preparation sequence, but this package is not an already runnable all55 deployment/fixture installer. Missing modules/compiler prerequisites are now exact implementation tasks, not guessed code described as ready. Rust compilation/execution and actual guard/current-source tests remain separately gated.

## Reference-scope and retention corrections

R2 reference decoder validates closed fixed registration/owner/target/body shape, checked UUID/revision/scalar syntax, and native guard/source/blocker canonical sequences before hashing. Actual current registration rows, active permissions, issuer evidence, source membership completeness, monetary rows and full source-domain correctness still require actual owners; byte/schema success never proves them. normalize_typed is only used after closed typed validation; production normalization uses the emitted per-registration schema path projection, never arbitrary key-name stripping. normalized-source is a versioned retained-form schema projection, not a second business authority or a public input schema that omits admission nonces.

Identical paired EvidenceRef aliases deduplicate physical materialization and identical pinner/purpose/obligation tuples only. Distinct purpose/obligation protections remain distinct even on the same unit. Conservative covered-budget rules remain; releasing one purpose cannot release another or evade a late hold. Actual two-obligation/late-hold disposal proof is an owner-test prerequisite; this packet does not substitute a fake retention model for it.
