CM1 — Enrollment and remaining implementation preparation

1 Source-backed changes only
Reuse immutable registry/action snapshots, common instance revisions and Company receipts; add minimal schema/binding/control/attempt data and typed owner request participation. Preserve six existing canonical owners and their tests. Named new wage/tax owner registration must enumerate actual owned tables, domain commands and receipt discriminants before activation; no open-string owner bypass. No alternate event store, Git branch database, generic workflow service or LLM semantic resolver is needed for this requested small-form history.

2 Ordered migration proposal
Inventory source schema/RLS/ACL/trigger/receipt variants and actor refs at product05200, then applied catalog before writing DDL. Add the four control tables with Company-correlated keys and Account/CompanyActor references from13; schema-candidate is a proposed physical contract, not executed SQL. Publish exact immutable input schema/owner mappings using the existing registry command role. Extend canonical closed rosters and0223CHECKs only for named reviewed objects/actions; allow old receipt writer compatibility during transition. Add receipt format/fingerprint metadata with an explicit LEGACY_V0 default for old rows/writers, no fake domain owner. CCF1 new owner receipts enforce exact owner/action/type/actor/digest/result schema. Add common integrity_protocol and bind/protect all new hash fields in the append-only trigger; do not backfill new digests into old rows.
Consolidate common guarded draft save/writeback into owner; no raw audit/receipt payload copies for the registered class. Extend borrowed-connection helpers and enroll every generic alias before admitting the type; omit/refuse unsupported surfaces uniformly. Apply current Account/Cedar/writer guards from13 and compatible WorkItem/HandoverPlan/Group/capacity order from09/12. Slot and command locks must be byte-defined and shared by all adapters; no mixed ordering during activation. Keep existing ordinary object reads/writes unchanged unless directly sharing the corrected responsible helper, in which case regression proof is required.
Runtime console_rt direct DML cannot be a privacy/ownership bypass for draft controls/payload. Final DDL must restrict draft mutation to the admitted common owner functions, preserve ordinary business access, prevent caller-set GUCs/type tags from authorizing a draft write and deny generic owner helpers for the draft class without validated owner participation. Same caller connection must have narrowly scoped function access for participating owners, no SET ROLE elevation. Company RLS is necessary but not sufficient for private Account custody. Exact table/function grants and provenance checks are W04b-05 proof; an application-only guard is not advertised as DB-enforced isolation.
No real sensitive draft enrollment until RR1 payload-copy inventory, eligible disposal path, pin/adoption semantics, independent journal/recovery fencing and qualified retention policy can actually be operated. Common head pointer is currently not an FK: new draft helpers verify head/revision/Company and one-open-row invariant under lock. Add a narrowly scoped consistency constraint/trigger compatible with RR1 terminal quarantine before activation; do not introduce a permanent restrictive FK that prevents authorized payload disposal. Epochs/control/tombstone and immutable fingerprints remain through accepted replay/recovery horizons.
Rollback after new drafts/CCF1 uses readers that understand both formats and controls; pause new effects/publication while retaining current security, receipts, reconciliation and RR1 tombstones. Never revert to a generic raw history reader, old writer ignoring epochs, or legacy hash assumption. If old schema lacks proof of supported normalization, preserve history and block execution until a compatible owner is available. Existing ambiguous legacy hash data needs separate diagnosis/repair, not an automatic reseal migration.

3 Meaningful proof candidates
proof-cases.csv is non-executable; fingerprint-vectors.json supplies literal bytes/hashes independent of current Rust. Verify exact source Git blobs, packet manifest, refs, canonical numeric-free vectors and static reader links only here. No code tests, Cargo, DB roles, migrations, browser/a11y or performance runs occur in this design packet. Later approved tests must include both allowed roleless/private/work-bound success and adversarial denial, same-command alias replay, commit-response loss, concurrent cancellation/handover, JSONB integrity roundtrip, wrong schema/duplicate keys, private history and retention adoption.
Proposed interactive bounds follow FD1/PA1 (save64KiB; one save in flight/tab; at most2rebase attempts;50history entries; compare2revisions). Required independent load cases include sustained coalesced autosave and many retained revisions, policy change under execution, long-running domain calculation and unknown outcome. No latency or storage forecast is proved by2000people. Exact lock-time/connection/WAL/memory envelope remains W04b-05, including the already documented full-snapshot growth.

4 Bounded readiness
Candidate14 settles common ownership, typed schema binding, physical control shape, lock sub-order, executor/receipt semantics and new encoding proposal. W04b-02 source-specific10command nested DTOs deliberately depend on W04b-03 artifact/qualification/temporal owners; no generic JSON replacement counts as closure. W04b-03 next fixes those actual source refs/locators/selection and historical/scheduled input APIs. W04b-04 fixes workflow/capacity/Group/PC1/AC1/PR1 ports plus deferred Account navigation discovery from13. W04b-05 unifies exact DDL/ACL/adoption/retention/migration/harness and measured budgets. W04 is still incomplete.
W05 current-authority scope alignment and final4independent integrated reviews/consensus/explicit design SHA approval remain, followed by W06 executable test candidate/independent review/explicit test SHA approval and actual RED probe admission. Scoped14 acceptance is neither whole-design approval nor permission to implement, merge, release, pay or make legal/production claims. Full Console/Foundry and remaining HR/common suites remain separate from this first vertical.
