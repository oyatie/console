# Native owner assertion supplement S1

Immutable base58b6. Root alone owns repository integration; author writes scratch only. R2 is preserved at manifest SHA764e3e4454a0e059337a9d6faa7ae0c861f586ca2762f27a6a484b404582649e. This supplement supplies actual proposed test/fixture source bodies, not a product implementation, compiled Rust test or admitted lane. Current approved design authority is already adopted by root; this artifact does not introduce a stale blanket design-approval HOLD. New proposed interface implementation still requires its exact review and named RED admission.

S1 amends R2 source: prepared native run remains STAGED (native18 owner contract and action30 close guard); review.decide targets actual review_basis, with actual returned GovernanceRequestRef still inside input; every native pending and terminal result is bound to original AttemptRef command/Company, including Completed-first observations; native complete helper is public only for test composition. R2's phrase both nullable fields is corrected: RunExpected input_revision, calculation_revision, close_basis and review_cycle_id are all nullable.

The authored source invokes the proposed fixed real owners, resolves opaque auth contexts from actual credentials, builds each typed input after actual controls, and uses fixed read-only native18 SQL after owner calls. No fake owner, verified bool, test context constructor, direct business INSERT or expected money calculator is supplied. Fixture loading fails when prerequisites are absent; it never skips or passes. The seed holds untrusted present user choices and refs and is NOT real issuer/bootstrap execution. Exact SOURCE_NOT_AUTHORED gaps are listed in source-inventory.json and assigned to explicit subsequent lanes across all55 registrations. The first native slice is partial authored coverage, not all55 readiness.

Additional explicit proposed interfaces beyond R2: auth/account_session::configured_account_verifier obtains existing configured issuer/audience/JWK policy; request-context/serving::admit_current_process obtains actual current admitted process/recovery binding; request-context/worker::resolve_worker_context authenticates actual service token and namespace/scope. These are real production resolver implementation prerequisites, not test issuers. AuthenticatedCompanyContext::company_id only reads verified identity. Governance read_current_reviewer_identity returns exact current account_id/human_id under real current identity authority; read_review_decision_history returns immutable current disclosed decisions for exact request. PayrollActionError::BlockedNativeInput and ::IncompleteReview denote typed current owner refusals; exact public transport remains closed PublicOutcome. Enum names PreparationPhase::Building, ReviewDecision::Approve, ReceiptRef::Company/Group and ReconciliationSubject::CompanyCommand/Attempt are proposed Rust bindings for existing exact schema variants. Owner command business refusals expected here return closed DEFINITIVE_NO_EFFECT before new-effect BUILDING. If approved owner error API selects another exact typed carrier, mechanical adaptation must retain these assertions and no-effect readback.

Test composition belongs in existing workflow adapter dev-test composition or owning app integration harness using allowed dev dependencies; no normal adapter→adapter dependency. The standalone scratch files have no Cargo target and cannot be called product RED. Integration uses the existing admitted lane tool and immutable source manifest. Native physical-contract.sql.txt remains the binding basis for readback table/column names; migration is an implementation prerequisite. Privileged readback credentials are test-only and never passed to command owners.

Pre-mortem: untrusted seed mistaken for issuer authority; missing fixture counted as product RED; same Human accounts treated independent; retained expected fields silently refreshed; wrong terminal receipt accepted; reference tests substituted for native owner execution. Detection: authenticated real owner routes, explicit original attempt/Company receipt assertions, complete native SQL readback, declared discovered/executed0 and exact SOURCE_NOT_AUTHORED inventory. Blast radius: scratch tests/proposed interfaces only, no deployed effects. Rollback: decline adoption while preserving R1/R2/S1 evidence; no history rewrite. Stop: missing source bootstrap, unresolved exact owner/API/lock protocol, incomplete population/basis, unexpected observation, changed frozen bytes, non-admitted probe. Independent reviewer /root/preparation_recovery; repository head remains immutable58b6 for source authority; runtime head not claimed.

Fixture isolation: CONSOLE_NATIVE_OWNER_SEED_INDEX maps each exact scenario key to an actual distinct Company and seed path. Loader rejects duplicate Company identities and checks the resolved authenticated Company matches the entry. Source mutation tests cannot share a population with another scenario. The absent real bootstrap must produce these isolated Companies through real owners; it is SOURCE_NOT_AUTHORED, not a fabricated fixture. Initial eventual Cargo execution is serialized with --test-threads=1 as an additional control; repeated executions require a newly owner-produced seed namespace. Missing/mismatched seed is fixture failure, never product RED. The blocked-close negative now explicitly requires a nonempty entirely BLOCKED population, preserving valid mixed-population behavior.
