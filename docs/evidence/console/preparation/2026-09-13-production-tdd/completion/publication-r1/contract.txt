Publication completion source

Root author; base fd94667fb9133f1ecb55831f43192bdc46776dc0. Extends security S3 manifest8559a2fdb678dcffd881db8435bea0fd94fdab746c548b3a20db9f0f44620fcb using its exact ExpandedCustodyFixture. Five owner tests are source-only until actual terms_publication owner exists; no synthetic implementation substituted. Place as auth_rest child module so prepare_http_database is existing real bootstrap.

Positive/negative cases: title1/128 and locale2/32 Unicode-codepoint length per exact BW31 JSON Schema (not byte length); out-of-bound cardinalities; UTF8 content65536-byte valid boundary,65538bytes and nonUTF8 refusal; genuine operator challenge expiry using actual authoritative database time; local digest addressing with inert URL content, forbidden URL/extra field, missing digest despite friendly alias; current role membership/SET/superuser and table/column write closure including all actual account_publish_terms overloads. Each refusal has unchanged head/receipt and an actual successful signed-custody publication positive.

Challenge expires_at() is read-only metadata on the actual owner-issued challenge. Expiration is checked by the real verifier with real time; test never fabricates a challenge or edits its stored expiry. Proposed operator-challenge maximum five minutes bounds the test to305seconds, before acceptance execution. OperatorChallengeExpired is the exact proposed owner error. Content-too-large/nonUTF8/digest grammar/cardinality/unknown field use InvalidArtifact; missing exact local object uses CustodyUnavailable. These classifications require consolidated interface review before test approval.

Trust boundary: the operator-only publisher DB LOGIN and its deployment custody signer are privileged publication TCB. Compromise of that credential is not covered by ordinary-user/runtime denial, and BW31 does not mandate PostgreSQL verification of Ed25519. No new crypto topology or fake verified-token pipe is introduced to satisfy an inferred finding. All actual runtime logins must lack privilege escalation, direct publication table/column writes and EXECUTE on publication entrypoints.

Alias closure composes rather than repeats inventory: CONFIGURED_ROUTE_SURFACES and exported route constants at actual app composition, S3 source-sink-requirements.json and its exact28source-sink map, root MIG M0 full actual pg_proc/ACL/function-definition/dependency projection, and source-bound sink enrollment review. The five tests alone do not discover arbitrary dynamically constructed SQL or undocumented HTTP handlers. Review each callable helper source/body and registered route against the exact installed manifest; any unclassified alias is an admission failure, not assumed safe by name. Existing S3 public signed-approval POST refusal tests remain required. The dedicated-role-compromise finding is explicitly rejected as an unsupported expansion of current guarantee, not silently waived.

The loopback listener observes the synchronous publication calls plus a bounded200ms post-return interval; it is not proof against arbitrary delayed tasks. Static local-only custody owner/source enrollment supplies that boundary. No actual external request or real audience is involved. All artifacts and signed roots are TEST_ONLY; no legal approval of content.

Validation: rustfmt parsing only for Rust source; actual approved JSON schema validates ten title/locale/digest cases plus two UTF8 byte-count vectors. Runtime tests and challenge wait are blocked only on absent real owner dependencies after source review; parser/schema results are not behavioral RED. No existing tests changed, removed or weakened.
