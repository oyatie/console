Browser acceptance fixture and scope

Nine cases: six no-JavaScript form/read cases and three JavaScript-enabled SSR read cases. Enabling JavaScript does not establish hydration. Actual client hydration, autosave/reconnect, keyboard traversal, reflow, assistive technology and human task comprehension remain separate acceptance cases. This runner does not route or fulfill application requests, invent a session, or insert new owner truth.

Use pinned @playwright/test1.63.0 / Chromium153.0.8010.12 v1243 in an isolated npm prefix. The recorded source is test code only; repository dependency and lockfiles remain unchanged. Canonical launch command is recorded in execution.json. Screenshots, video and traces are off; fixtures contain only synthetic owner-issued sessions and must stay0600 in owned temporary storage, never committed. No screenshots contain real payroll or narrative data.

CONSOLE_BROWSER_FIXTURE is an owner-produced JSON document. Its raw SHA256 and actual tested commit are supplied separately as CONSOLE_BROWSER_FIXTURE_SHA256 and CONSOLE_BROWSER_CANDIDATE_SHA. A digest is custody, not evidence that setup ran. The fixture producer must separately record actual auth/identity/source/draft/payroll/review/custody owner calls, immutable receipts and privileged post-commit readback. Candidate metadata must match the running application's build; self-reported fixture metadata alone cannot establish that fact.

Document fields: kind=SYNTHETIC_OWNER_PRODUCED_BROWSER_FIXTURE; candidate_sha40hex; origin owned loopback HTTP/HTTPS; cases keyed exactly project/test-title. Each case has path (canonical mounted /_ui/ read target), cookies (actual owner-issued synthetic credential cookies), company_label/visible_subject_label/historical_period/visible_evidence_narrative as relevant. Canaries are distinct CONSOLEPRIVATE plus24uppercase-alphanumeric markers stored in actual denied employee/name/wage/narrative/coverage fields and witnessed through their separately authorized owner readback. Never place a hidden value in an otherwise public label simply to make a scanner pass. Every case/project gets isolated Company/object/command IDs; all fixture fields have provenance.

Native source scenario uses actual supported salary1200000 with current acknowledged input, changed to1234567. Stale-save test opens two real pages before the second page commits1250000; first page then submits1234567 with its original token. It expects409 and preserved values. No preassembled future receipt or simulated409. Invalid-submission fixture uses an incomplete scalar editor that can retain1..2 without silently coercing it to a business number. Labels 기본급, 저장, 상신, 현재 내용 확인 and a business composer accessible name 업무 입력 are proposed UX test contracts requiring design review; they are not claims about the current UI. A global own-account logout form is permitted and is not mistaken for this object's business composer.

Scoped review fixture has one permitted subject/evidence and four denied canaries; it must not force disclosure of hidden global cycle coverage. Historical-own fixture has ended Employment plus ACTIVE Account and a currently authorized immutable NONPAYABLE_REVIEW publication; amount1200000 is deliberately synthetic. Read-only fixture permits the subject label and denies three fields plus business mutations on this object. Server action denial is exercised by owner/HTTP authorization suites separately; button/form omission alone is not authorization proof.

Raw canary scanning is a bounded disclosure detector, not complete information-flow proof. Encoded/transformed values, other browser storage and unobserved outbound channels need source-bound relational and sink-specific evidence. JS-enabled SSR checks do not claim hydration readiness. Browser task outcomes do not prove durable owner effects without associated owner readback and receipt tests.

Actual current execution: browser installed and launched; all9cases failed at absent CONSOLE_BROWSER_FIXTURE before application requests/assertions. Classification PREREQUISITE_BLOCKED, not applicationRED, success, usability observation or implementation admission. Real Account/native fixture production is dependent on the missing approved owners; no opaque fixture is fabricated to hide that dependency.
