SE19 — Precise formats and bounded source packages
Reuse14/18 CCF1 UTF8/ASCII-key sorting, canonical decimal strings and UUIDs, no JSON numbers, duplicate raw keys, decoded NUL, invalid Unicode, trim or NFC normalization. Numeric patterns in source-types.schema.json are lexical only; owners perform exact i64/date/range checks. Required nullable fields are present as null. All arrays are explicitly ordered below and duplicate-free; unknown fields are rejected before source19 hash/storage. Leaf<=65536bytes/depth8; no individual generic array>256 except schema-bounded support counts<=100. Scope/type/subject and real immutable source existence are mandatory beyond schema syntax.
Scalar limits: Minute0..2880, event durations checked microseconds and whole-minute exactness for NP1; PositiveRevision checked positivei64; tax PDF page/table1..10000,row1..100000,column1..256; CSV record1..1000000; dependent1..99 and child0..99. Coverage event scan earning window plus24h each side: max4096 in-window rows using limit+1, plus at most one distinct prior boundary anchor. Total4097; all rows preserved including next-month closing clock. No incomplete scan can return a COMPLETE package/coverage. If no valid prior anchor exists, opening state remains UNKNOWN unless an exact owning initialization fact/rule proves it; no-events alone never initializes OFF_DUTY.
H(tag,x)=SHA256(UTF8(tag)||0x00||CCF1(x)). Source admission digest tag console.source19.admission hashes exactly {org_id,root_id,admission_revision,body}; body is AdmissionPayload including explicit nullable supersedes_ref. Source event custody tag console.source19.revision hashes {org_id,root_id,revision,event_kind,admission_digest,actor_account_id,command_id,recorded_at_us,body}. Event-kind body is declared in state-contract.json; no ambiguous choice of semantic/custody digest. Calendar/coverage/leave-disposition semantics use console.source19.calendar, console.source19.coverage, console.source19.leave-disposition over the exact payload plus org_id. source_family generations/observation time are kept in separate owner custody record, not semantic equality. Native18 unit commitments refer to exact source revisions, not these hashes alone.
Ordering: weekly by ISO weekday; overrides/framed days by Date; duties/breaks/day intervals by start,end; plan contributions by anchor_date; calendar assignments and primitive UUID/ref sets by canonical ref bytes; qualification/bundle/evidence refs by canonical bytes; insurance by scheme; obligations by family; supported numeric counts numeric ascending; withholding options ASCII; CSV columns retain physical declared column order; locators/rule-note sets canonical bytes. Blockers use18 ordering. Equal start/tie conflicts that violate interval semantics are rejected, not UUID-resolved into fake chronology. Explicit member and event stream formats below exceed a command array by framing bounded leaves; commands reference exact admitted artifacts.

Attendance event package reuses exact-version Docs custody/storage, not a new storage service or arbitrary source-query JSON. File SOURCE19_NDJSON_V1 consists of one canonical EventPackageHeader line followed by event_count canonical EventWitness lines; each line terminated by single LF, UTF8 no BOM/blank lines. Header and event line<=8192bytes, file<=40MiB, count<=4097. Events ordered by occurred_at_us numerical ascending then UUID raw16, unique same Company/employee. If prior_anchor_ref is nonnull it must resolve to exactly one included event, strictly before query_from_us; every other event lies in the selected half-open query window. Count includes anchor once. Header query bounds/period and exact owner query completeness must match; package is not an admission checkbox. Preserved legacy actor IDs are provenance, not Account authentication. Decode exactly header-declared count, reject trailing/missing/additional lines or mismatched IDs/rows.
Event-set commitment = SHA256(UTF8("console.source19.event-set")||0x00||H("console.source19.event-header",header)raw32||U32BE(count)||for ordered records:event_uuid_raw16||H("console.source19.event",EventWitness)raw32). SourcePackageRef.event_set_digest is this commitment; its ArtifactRef separately attests exact whole-file byte hash/version. These different hashes are never interchangeable. Coverage interval event_refs and boundary anchor must occur in this package; no dangling dependency. Original owning event rows and package bytes are retained/pinned through adoption; privacy-field projections happen before serving content. Direct raw Docs access to the package requires authority for every included field/record, not just salary read or link possession.
No network call holds source transaction locks. Capture a bounded exact source snapshot under guards, build/upload immutable package through trusted Docs outside transaction, verify exact version/bytes, then reacquire source guards and re-resolve full current semantics. If changed, do not publish that stale package as current coverage; restart or store BLOCKED repair evidence. An unrelated epoch change may retain the package only after exact semantic re-resolution. Final coverage publication + artifact adoption/pin + receipt is one Company transaction. Unadopted upload is staged/orphan material under F03; upload success is not source publication. Source19 does not claim current generic get_bytes is version-addressed; extend/reuse exact-version adapter from15/17 before enrollment.

Close coverage-member artifact uses SOURCE19_CLOSE_MEMBERS_V1 NDJSON: canonical header {format,org_id,period,scope_basis_ref,member_count}, then exact CloseMember lines ordered employee UUID raw16, one LF per line, no BOM/blank/trailing records; count1..2000, each line<=8192bytes, whole<=20MiB. member_set_digest=SHA256(UTF8("console.source19.close-members")||0x00||H("console.source19.close-member-header",header)raw32||U32BE(count)||each employee_uuid_raw16||H("console.source19.close-member",member)raw32). Coverage refs are same Company/employee/period, current COMPLETE semantics. coverage_semantic_digest equals H(console.source19.coverage,{org_id,payload}) for the exact referenced coverage. Native close additionally requires18 resolved input units, source/assessment/operational close validity, and complete same population; this file is not close authority. Final close-basis/adoption/pins/receipt atomic after exact current readback; no storage call under DB locks.
Amendment set commitment = SHA256(UTF8("console.source19.close-amendments")||0x00||org_uuid_raw16||close_uuid_raw16||U32BE(count)||sorted unique amendment UUIDraw16). Exact owner immutable amendment rows retained, each matches close/Company; complete set enumerated under ATTENDANCE guard with limit257 sentinel. More than256 blocks native close until a reviewed wider contract. A new amendment changes set identity; an empty set is valid only from complete guarded query, not omitted payload. No nonexistent amendment_revision is minted. Amendment prose alone does not change wage/paid time; source-owning correction actions carry business effects.

CSV tax source normalization: declared UTF8, optional single leading BOM only when selected; decode strictly. Declared LF or CRLF record separators are enforced outside quoted fields; quoted embedded newline remains part of one logical record. Header is first logical record and excluded from record_ordinal. Empty/blank data records and duplicate or unknown/missing header aliases are rejected; no trim/case-fold/Unicode normalization. Exact column aliases map once to immutable header_text; same headers must not occur twice and each required semantic role is satisfied per registered decoder. Quotes/doubled quote obey RFC4180. No spreadsheet formula execution. Integer amount cells use canonical decimal syntax in this initial codec; unsupported formatted numbers require a separately qualified normalizer, never locale guessing. Checked multiply by qualified unit1 or1000 exactly once; negative/overflow/refused input blocks. Bounds on whole source bytes/rows and approved normalization conformance remain provider/source-supply enrollment conditions; no parser claimed implemented.
PDF location binds exact source ArtifactRef and exact reviewed locator-map artifact; page/table/row/column1-based bounds checked against that map. OCR/text extraction is a derivative with provenance, not authority from matching a printed number. Both tax locators and complete applicable note/adjustment set must map to the same qualified edition and actual selection. A legal source's note outside the chosen income cell cannot be ignored. Manual located-row entry remains an owner-reviewed fallback when normalizer unavailable; all required source evidence appears beside proposed values. The absence of automated parsing is visible readiness work, not permission for an always-blocked production journey or2000 unnecessary retyped amounts.

Container-depth convention: root object is depth1; only object/array containers add depth; scalar children do not. Calendar weekly/override entries place day-plan fields directly beside their weekday/date, avoiding a redundant plan envelope. Their rule_ref governs duties and each break treatment; no per-break duplicate qualification ref. Source semantic calendar envelope {org_id,payload} fits depth8 including nonempty break arrays. Byte/depth limits apply to the complete hashed leaf; common command transport nesting and payload-ref integration remain an explicit F01/F02 enrollment check, not an implied exemption from14.

Classified-day package SOURCE19_COVERAGE_DAYS_V1 is canonical CoverageDayPackageHeader (<=8192bytes) + day_count canonical CoverageDay lines (each<=65536bytes/depth8), every line one LF, UTF8 no BOM/blanks/trailing records. Count1..31 for an existing package, whole file<=2MiB. Header commits Company/employee/Employment/period/exact event_set_digest. Days are unique real dates inside period, sorted ASCII YYYY-MM-DD. COMPLETE requires28..31 real month days, BLOCKED package may preserve known whole subset. A zero-day result has null coverage_days_ref, no empty admitted package.
Day-set digest=SHA256(UTF8("console.source19.coverage-days")||0x00||H("console.source19.coverage-day-header",header)raw32||U32BE(day_count)||for each day:DateASCII10||H("console.source19.coverage-day",CoverageDay)raw32). Whole-file hash remains a separate exact Docs bytes identity. Framing is fixed-width by DateASCII10, not JSON key iteration. No individual command array holds the entire day stream. Root coverage semantic H("console.source19.coverage",{org_id,payload}) commits both exact package refs; it never re-embeds their bytes. A changed plan/date/blocker/interval changes day hash, day-set digest and current coverage semantic digest. SourceRepairRef is a flat tagged object {kind,...exact native ref fields}, preserving actual identities without a redundant nested ref envelope.
