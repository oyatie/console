SC24 — Source input, sealed intent and independently retained submission
1. Resolve20 digest ambiguity explicitly
Source19 admission/semantic/event bytes and tags remain unchanged.20 called its preseal reference a source19 payload digest, although an event custody record includes a future domain command and execution timestamp and not every input kind has a specified source19 semantic tag. This is an incomplete proposed boundary, not an implemented codec to reinterpret. For selected SOURCE_INPUT24 draft-backed registrations replace the proposed20 ref with SourcePayloadRef24 and pin the new registered codec/schema revision. No fallback decoding/ref20 hash reuse under the new protocol. Other registered/native18/direct paths retain their own contract and are not enrolled by24.
I = SHA256(UTF8('console.source24.input')||NUL||CCF1(typed frozen input)). Input is the exact registered19 action payload including all required narrative fields; this is an input-only commitment, not qualified source truth or eventual event custody. Admission and event hashes still use19 tags and their declared root/actor/command/time. Target/custody hashes use H('console.source24.target',exact14 target binding) and H('console.source24.custody',exact14 custody binding). Equal input hash across Company/schema/action/target cannot establish equal intent: Preparation24 binds all of those exact identities and owner compares original typed tuples, not only hashes.
Preparation24 canonical bytes <=16384/depth8; preparation_digest=H('console.source24.preparation',Preparation24). Its unit ID is the actual distinct17 retained_units row allocated by registered preparation owner, with selector SOURCE_PREPARATION24 and exact preparation identity_digest. A typed selector chooses a fixed adapter, never arbitrary table/SQL/object key. Preparation binds existing acknowledged draft revision UUID/version/common revision hash, schema, control revision, editing epoch, immutable action/type/target/custody and source input. No future domain command/attempt/gate result is present. A change to required facts makes preparation stale; ordinary no-change read does not create a new unit. This packet specifies selector payload, not complete17 physical state storage.

2. Seal and one-way identities
Under14/20 fences seal verifies exact prepared source/control state, current actor/assignment and unit. It allocates the domain command and attempt, adds one immutable unit->attempt/command linkage, and stores the canonical command once. CCF1 body is SourcePayloadRef24, binding exact preparation unit/digest and source identity. CCF1 includes original Account, expected refs, reason, exact gate-request ref and all14 envelope fields. Metadata copies preparation plus attempt/command/fingerprint; equality to authoritative attempt/command/unit must be validated. A new sealed attempt requires a new prepared unit; no reassignment of a cancelled/unknown unit. No unit allocation or network occurs under final publication locks. Missing real row refuses; UUID existence is not lock proof.
Dependency DAG: typed input -> input digest -> preparation -> CCF1 command/fingerprint -> SubmissionMetadata24 + retained snapshot -> publication/effect/adoption evidence. Command never references its own final snapshot digest, generated source custody digest, final receipt/effect digest or future approval outcome. Gate REQUEST identity is part of intent; later gate decision/consumption/current-authority evidence is separately retained by its owning records and linked through adoption. Snapshot does not get rewritten to append those later facts.

3. Exact carrier and limits
Store three raw canonical UTF8 JSON components separately: metadata1..16384bytes, command1..65536bytes, input1..65536bytes, EACH depth<=8. command_bytes contains CCF1(normalized_command), not the prefixed fingerprint frame. Input bytes are the exact typed source input, never source event with future actor/time. Every component uses14 canonical byte rules: raw duplicate keys rejected before JSON value conversion, valid UTF8/scalars, no decoded NUL, no unknown fields, canonical decimal strings, no trim/NFC, literal reason/notes retained. Schema numeric/date patterns require real range/date parsing. Do not nest, JSON-string escape or base64 one component into another. Public Action request remains<=64KiB and carries the small ref; internal SQL/storage carrier is separately bounded, not a transport exemption.
Snapshot frame = UTF8('console.source24.submission')||NUL || U8(1)||U32BE(metadata bytes)||metadata || U8(2)||U32BE(command bytes)||command || U8(3)||U32BE(input bytes)||input. Fixed order, single each section, exact EOF; reject unsupported section/reorder/extra/truncated bytes and declared size beyond cap BEFORE allocation. snapshot_digest is raw SHA256(frame). Component raw SHA256/byte length may be independently recorded in retained location inventory and are not interchangeable with tagged input/command/snapshot hashes. Whole frame maximum is specified and checked by framing-vectors.json. This is byte bound, not process-memory or SLA proof; decode/sort/allocator/driver/network buffer costs remain F08.
Source event/day/member packages remain exact Docs refs, never inlined multi-MiB sections. Required notes outside typed normalized input remain in canonical command reason/expected or separately identified retained evidence; registered field-to-evidence mapping must account for every required note. Dropping a narrative during normalization is a failure, not an invitation to infer it later. Exact old source/event bytes remain native owner truth; the snapshot is the submitted intent evidence and does not replace published source payload storage.

4. Small missing byte holder, existing custody owner
17 enumerates retained units/locations/pins/adoptions but no concrete shared source submission byte table;21 requires a distinct independently readable domain copy shared across compatible source results. Select one ont_source_submissions row per Company/unit/attempt/command, linked to that existing17 unit, as the fixed relational payload location. Store command/input/metadata once; many results refer via existing domain_adoptions. No per-result copied command, no editable revisions in this table, no separate versioning/approval/receipt namespace. The table is proposed new storage, not claimed already present. Native source tables still own emitted source facts.
Use13 CompanyActor composite FK(org_id,actor_account_id)->company_actors(org_id,account_id), not invented CompanyActor UUID or equality with legacy users/Employee. Snapshot actor = attempt.original_account_id = canonical command.actor_account_id, and emitted event attribution follows the submitting actor. Creator, previous editor, preparation actor, business assignee, executing worker and predecessor event actor may differ legitimately; retain their own provenance, never rewrite them to match. Account and anchor remain after employment ends; their presence grants neither current work authority nor snapshot disclosure. Different Account UUIDs alone do not prove independent natural persons; current AI1 evidence remains separate.
SQL provides bounded bytes/hash checks, exact unit/Account/attempt/receipt references, immutable row guard and proposed confined-role/RLS grants. Hash checks do not parse or bind metadata JSON fields to scalar columns, do not prove source authenticity, and do not supply policy. The exact fixed owner participant must validate these correlations against locked persisted preparation/attempt, registered codec and source bytes. Default/inherited/schema/role grants remain mandatory catalog audit. Snapshot->receipt FK alone cannot force every source effect to have an adoption; converse source/result/purpose obligations remain finalizer/adoption integration dependencies. No source enrollment from this fragment alone.

5. Owner participant contract and evidence protection
Selected internal participant: common custody owner's finalize_source_submission_in_conn on the already borrowed PgConnection, reachable only through20's fixed owning SQL commit path; not a public RPC. Typed inputs: exact Company/unit/attempt/command IDs; expected prepared unit state+revision and immutable linkage; bounded canonical metadata/command/input bytes plus expected digests; complete predeclared result/purpose adoption plan and required evidence unit refs. Result is exact existing unit plus snapshot digest/size, provisional until COMMIT, never a permission token. Complete SQL parameter/composite types and body are F02/F03 work; this names data dependencies and storage contract rather than pretending a conceptual interface is compiled code.
Sequence after22 provisional receipt header: validate locked unit/attempt/current source/actor context; derive and compare canonical components and all scalar bindings; insert one immutable snapshot row and exact registered payload location under existing custody guard; acquire/confirm independent DOMAIN pins or adopted copies for every required source/artifact/decision/credential/AI1 reference; establish exact per-result/purpose domain_adoptions and their obligations; validate source/result/receipt membership and all other participants; release only eligible private pins; final current-time checks and one COMMIT. Finalizer owns no pool, network, autonomous task, COMMIT or arbitrary callback. A source row, snapshot and adopted evidence must succeed together or all roll back.
Reference lists are not proof of availability/protection: required byte location, exact version/hash/size, current retention/hold state and independent domain ownership must be verified under declared guards. Sharing an exact artifact is allowed through independent domain pins; holding only a private draft pin is insufficient.21 preserves distinct snapshot unit even when underlying provider deduplicates bytes. Complete inventory/cardinality/lock admission for all dependencies remains F01/F03/F08; do not pass an unbounded generic list into this proposed participant. Future gate decision and external verification evidence have separate immutable records/lifetimes, not cycles into sealed snapshot.
Historical replay follows20 receipt/provenance-first flow without requiring source bytes to still be live or preparing new unit. Missing/unavailable historical content produces currently permitted unavailable content, not permission to reexecute. Lost acknowledgement is UNKNOWN; preserve original command/unit/pins and reconcile. Proven no-effect failure permits owner-directed retry only for same still-valid intent; stale input requires review/reseal/new unit after conclusive old-attempt termination. No-JS manual save/submit uses the same11 owners.

6. Disclosure, lifecycle and remaining integration
Raw snapshot contains all submitted fields; access to a parent object/receipt/one effect or linked chat object is insufficient. Normal UI obtains current field/history/derived projections through owning readers. Raw download requires authority over every contained field; no direct bytea/API/log export by Company administrator assumption. Active customer tenant context applies independently of intra-tenant Company grants. SQL diagnostics/trace/event occurrence remain F04 sensitive sinks. SSR/hydration/reference previews follow same current Cedar; no client-side hiding of forbidden bytes.
Row immutability initially blocks UPDATE/DELETE and runtime TRUNCATE; no disposal escape function is introduced. Lawful disposal requires a separately reviewed confined17 transition preserving minimum identity and proving all obligations/pins/copies; until then cleanup of enrolled payload is unavailable, not a claimed perpetual lawful retention rule. Actual provider issuance/recovery/legal policy remain separate authority/evidence.
24 closes scoped input digest, preparation/submission schema/framing, concrete byte holder and actor/attempt identity requirements. Full source bodies/projections and semantic current-head guards, sealed unit binding physical DDL/constraints, complete required-evidence inventory and adoption reciprocity, final SQL owner signatures/bodies/ACLs, migration/restore and executable tests/proofs are still open. Follow22 finite W01-W04/F01-F08 map; no claim of global F01/F02 closure or implementation admission.
