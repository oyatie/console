# Native S3 unique retention and runtime-custody source

Root /root remains sole repository writer; /root/fresh29_workflows authors scratch source only. Immutable58b6 base and frozen S2 dependency are recorded in inventory. Mechanical guide: real S2 fixture -> actual native/retention observations -> ordinary hold/disposal/current owner calls -> real lock/runtime-DML probes -> parser -> independent source review -> root integrated test candidate. Exact intended target is7additional authored methods; source file modules mount with S2 support; native-stream-budget-tests.rs mounts inside native28 owning module with S2 codec-goldens directory.

Pre-mortem: dedup may erase a second purpose; hold release may drop domain pins; stale eligibility may admit disposal; role GUC may masquerade as private command; concurrency may not overlap; malformed length may force oversized read/allocation. Detection: actual distinct hold/purpose/obligation refs and byte/copy inventories, exact survivor pin equality, stale+fresh disposal refusal with no jobs/receipts,12 classified runtime SQL denials with immutable snapshots, pg_blocking_pids witness, real io::Read/Write fault adapters over independent goldens. Blast radius is isolated SQLx database and scratch source only. Rollback: reject/supersede this packet without erasing frozen history; real hold effects stay governed in disposable test database. Stop on fake ready/receipt, unexpected immutable changes, unclassified infrastructure error, missing exact lock witness or insufficient retirement eligibility. Review /root/preparation_recovery and root; heads and hashes recorded in manifest. No Rust compilation/runtime or production/legal/whole-program readiness is claimed.

Source gaps are not renamed implementation prerequisites. Concrete native retention/custody/rendezvous and bounded streaming oracles are now present. Actual positive retirement/disposal must use real eligible shared owner lifecycle; root explicitly chose reuse of shared draft/retention execution evidence for that engine, with native-specific current mandatory-pin eligibility blocking demonstrated here. Full streaming allocation/RSS and multi-Company maximum workload envelope are measured gates; short-read bodies alone do not close them. Historical S2 inventory accurately describes source before this supplement.
