# Workflow15 assertion source — authoring candidate

Owner /root/preparation_recovery. Repository writer /root. Exact supplement base fd94667fb9133f1ecb55831f43192bdc46776dc0; original source base 58b6f758ac2a49b28589b9506628a43b6158e925. Immutable target is15 R2 registered owner entries: governance.execution_gate.{request,decide}, payroll.review.{release,replace_projection,request,request_append,request_withdraw,respond}, work.transfer.{submit,execute,stop}, group.operation.{create,activate,resume,stop}. Mechanical guide: R2 manifest764e3e4454a0e059337a9d6faa7ae0c861f586ca2762f27a6a484b404582649e, adopted design30 action/owner binding and workflow16 ordinary owner contracts. No product implementation, migration, lockfile, generated authority, or repository edits.

Lenses: Cartesian doubt; Systems Thinking; Red Team; Operability/Day-2; Blast-radius/cell-based; Zero-trust/defense-in-depth. Pre-mortem: a plausible owner DTO hides fabricated authority or setup state, or replay accepts another command's receipt. Detection: exact producer chain and receipt binding plus immutable owner readbacks; independent author review and explicit zero compilation/runtime. Blast radius is this scratch packet only. Rollback is decline integration, preserving evidence. Stop on missing author-owned dependency, design contradiction, or any need for a special privileged test business owner. Reviewers root/native/security; author does not approve own source.

Fixture lifecycle: every sqlx::test owns one migrated disposable database. Native build_native_deployment(pool,scenario,count) publishes actual genesis terms from Ed25519 custody, redeems the configured deployment root proof, enrolls real Account passkeys, registers/adopts actual signed provider/Human bindings, creates Group and each actual Company, grants ordinary policy, admits serving/worker context, and publishes actual source/profile/person/Employment prerequisites. No JSON seed index or callbacks. This dependency is separately authored and must be frozen/hash-pinned before this packet is called source-complete. Company fixtures contain actual opaque resolver contexts and native choices. Tests require exact role pools bound to the same generated database. Ordinary owners remain proposed and absent; parser-only is never product RED.

Additional fixture input is published through ordinary typed payroll/workflow/Group configuration owners, under real current operator authority. These are ordinary production configuration siblings using the existing common immutable configuration revision store, its current expected heads, typed guards and receipts. They are not one of the55 business action registrations and cannot mutate business rows or construct permission contexts. Test-only labels describe synthetic config content; no test flag bypasses validation. Definitions reference the actual registered payroll case handler action and case object; coverage is exact Company+selected Account+definition+finite interval. Group capacity/availability is actual operator-published finite input. Publication projections/category/gate policy are current governed owner configurations referencing real registered fields/actions. Returned configuration refs, not client booleans or preset revisions, enter sealed requests.

No force, admit_fixture, seed_ready or arbitrary publisher callbacks. General owner read contracts below are fixed authorized DTO projections over the named owner relations; missing/incomplete reads fail, never invent defaults. UntrustedActionTargetSelection::create_registered::<I>() requests actual owner allocation for the registered object kind, not a fabricated existing object. existing_publication/case/transfer_plan/execution_gate and with_group_slot contain untrusted exact actual IDs; each normal binder resolves current authority and stored provenance. Group controls preserve R2 GroupRequestIdentity and never enter Company envelopes.

Ordinary additional read shapes: payroll read_complete_publication_targets(run,completion) returns all exact HistoricalSubject+ResultRef+CompletionRef for the existing closed membership; read_publication_content(ref) returns immutable content/source/subject; read_case_content(case) returns immutable submitted content with exact items; read_case_control(id) returns CaseExpected. Workflow read_assignment(task) returns actual assignment Account/epoch/control/charge; read_transfer_plan(plan) returns current exact immutable content and control. Group read_group_operation(operation) and read_registered_group_action<I> return current authorized ordinary control data. None creates facts. Runtime integration must implement these in their existing owners with normal current disclosure, budgets and same-Company guards.

Cases use actual publication for the fixture's enrolled subject, not arbitrary employee UUID. Configuration producer explicitly binds that existing subject Account using the actual canonical Person/Employment/Human binding owned by native fixture. An unrelated Account cannot request/withdraw; handler differs by verified Human. Transfer candidates retain the same prerequisite ordinary scoped rights; nomination never grants them. Group child drafts are created only after actual PREPARING parent and use actual stored parent/slot refs through ordinary Company binders. Activation never runs a child early. Tests do not claim Group-wide atomicity or production failure-domain durability.

Exact native signature agreed with author: crate::native_fixture::build_native_deployment(pool:PgPool,scenario:&str,company_count:usize)->NativeDeployment. NativeDeployment holds runtime/passkey service/origin, verifier, serving, Group/operator, AccountBootstrapPhase including transfer_recipient (third distinct Human), and real Company Fixture.facts. fresh_submitter_login directly calls the authored real Account producer; no opaque seed or login callback. Native source remains independently author-owned and under review; root integration must pin its next accepted freeze. This packet never claims a future passing constructor merely from the signature.

Publication internal read DTO is PublicationRecord{publication,subject/source binding, result:ResultRef,completion:CompletionRef,projection:GovernedRevision,content:PublicationContent}. PublicationContent retains exactly control30 closed subject/period/purpose/fields/source_digest; extra source/control refs stay outside content. ResultRef remains only calculation_id/version, never an invented batch member. Existing SQL payable lives on payroll_line_calculations. Gate consumption readback joins existing gov_approval_consumptions.approval_id to gov_approvals.id/request_ref.

The original15 methods include positive result and identity/receipt assertions, with targeted replay, stale CAS/issue, independence, all-child activation and stopped provenance checks. They are not a claim that every concurrency/hidden-target/maximum-cardinality proof case for these15 registrations is authored. The full55registration test coverage and all original security/native proof-cases require their independent packets and explicit census.

## R2 independent review corrections

R1 is preserved by parent-r1-manifest.json (SHA7b1c271fda522816633dc91a1fc095daf3436abd78ce813614da9c800eb04454). Root independently identified weak publication-only oracles, no actual paused/pending resume witness, duplicate-erasing set comparison and undiscoverable migration configuration. R2 adds a source30-exact privileged READ ONLY publication snapshot (all publications/slots/controls/events plus original selected command ledger rows) and exact row/set cardinalities. Replay/refusal cannot create another publication, advance a slot or rewrite an original receipt unnoticed. Refusal's own new command receipt remains permitted and is separately identified.

Every sqlx test uses migrations=../crates/platform/db/migrations and no leaf feature cfg. Proposed integration is backend/app/tests/workflow15_owner.rs with #[path] ordinary modules workflow_fixture,workflow_owner_tests,workflow_race_projection_tests and shared native_fixture/binder/account/deployment/native producers under the same test crate. The runner target is cargo test --locked --manifest-path backend/Cargo.toml -p console-app --test workflow15_owner -- --list, then the same target without --list for execution under ordinary real test role/database configuration. This packet does not alter Cargo or the repository; exact root integration registers that test target and counts discovered/executed methods. Native dependency is the agreed native_fixture::build_native_deployment entry, not an invented native_deployment module.

Group resume uses actual workflow::read_group_child_by_attempt -> claim_group_child -> dispatch_claimed_group_calculation as ordinary existing-workflow transport refinements. Claim object is opaque and produced under current worker mandate/parent/current original Account guards; dispatch invokes the same fixed payroll.calculate_run owner and original AttemptRef. It is not a second calculation implementation or test-only transport. Native complete_native drains actual pending result on the same subject. Actual first Company receipt is committed and preserved while Group ACK is intentionally not observed; second child is actually PREPARED with no receipt. Ordinary Account logout revokes original family; second claim must return OriginalSessionEnded. Current Group readiness remains ACTIVE header with ORIGINAL_SESSION_ENDED dispatch reason (no invented PAUSED/UNKNOWN rows). Actual SoftPasskey primary login then ordinary Group resume reconciles the first receipt, keeps second pending, retains every original command and increments dispatch epoch. Replay leaves both Company snapshots/first receipt unchanged. No crash, failure-domain or cross-Company atomicity claim is inferred.

Additional ordinary read refinements are fixed workflow GroupChild{reference,state,receipt}, GroupDispatchReadiness{reason}, and current company grant enumeration. logout_account_session is the ordinary Account session family revocation owner, not fixture mutation. Company concealed authorization failure maps to registered public_code not_found or ResultObservation.UNAVAILABLE.error.code=not_found. No technical error counts as successful nondisclosure. Unknown actual integration error taxonomy must be resolved before execution, not weakened to any-error PASS.

Gate policy publication stores an available governed policy only. It does not install a mandatory gate rule for every Company calculate invocation. The gate tests explicitly select that current permitted policy on their editable calculation draft through governance.execution_gate.request, preserving the normal exact envelope binding. If actual Company policy requires a gate for another action, the ordinary binder must enforce it; the fixture does not disable, bypass or silently waive mandatory gates. This distinction was independently raised by native reviewer.

R3 explicitly withdraws the invented six-dimension census. There is no approved requirement for15 registrations times6 dimensions. Historical-cell-audit.json reclassifies its50 absent cells against actual owner obligations; no percentage or mechanically repeated test now stands for completeness. Registration-coverage.json maps action-cases.tsv before/after/refusal, actual unique source methods, shared invariant evidence, and structural enrollment separately. The11 Company registrations may inherit the accepted16-method Draft/Attempt source only after actual shared-engine enrollment is independently established. The4 Group controls have separate GROUP_CONTROL_V1 envelopes/receipts and do not inherit Company Draft tests.

R3 has30 authored SQLx methods across3 assertion sources, with one shared fixture source. New unique bodies cover valid same-Company forbidden-field atomic release plus separate cross-Company refusal; own-subject request/withdraw; exact stale assignment after actual transfer; recipient availability and revoked domain authority; actual two-task capacity conflict; unavailable authoritative transfer receipt retaining actual assignment/charge; wrong actual Company membership incarnation; Group replay allocation/body integrity; original Account identity despite same-Human alias grant; and parent stop preserving unavailable Company outcome with subsequent real receipt reconciliation. Existing shared/owner source is reused instead of copying universal draft tests.

The lock-fault probes use only the SQLx disposable-database admin connection to take ACCESS EXCLUSIVE on the actual Company command ledger and witness a real owner backend blocked via pg_blocking_pids. No business/permission/status row is inserted or edited. An actual committed owner effect is produced first; ordinary owner reconciliation must return bounded UNKNOWN or precise unavailable evidence, retain truthful assignment/capacity, and recover the original receipt when the lock releases. Group stop must complete without opening Company child/business transactions. This assumes the ordinary Group read seam performs bounded per-Company authorized receipt reconciliation, as GN16 describes; a mirror-only GET cannot satisfy the probe. Exact runtime timeout/taxonomy integration remains unexecuted, never counted as product RED.

Independent corrections before freeze: physical payroll_review_request_controls key is request_id while public CaseExpected uses case_id; raw readbacks now map it explicitly. Stale-handler after-transfer assertions read exact current assignment by controlled join, avoiding stale authorization contexts. Wrong-incarnation refusal snapshots all Group parents/slots, not only the original header. Gate fixture permits both actual decider Accounts so self refusal exercises independence; raw gate decisions are unchanged by refusal and exact consumption remains zero after permit/replay. The same-Company publication probe retains an actual positive single-first-target execution under identical policy, so all-denied setup cannot satisfy the negative case.

Remaining HOLDs: native two-supported producer final accepted freeze and NDJSON UploadRequest MIME additive amendment are independently owned; ordinary owner/fixture implementations and test registration/compile/runtime remain absent/unexecuted. Full security source matrices and broader configuration/candidate-budget/repair coverage remain independent evidence obligations, not this packet's claimed completeness. Author /root/preparation_recovery does not self-approve. Root and native/security independent reviewers must approve exact frozen bytes before integration.
