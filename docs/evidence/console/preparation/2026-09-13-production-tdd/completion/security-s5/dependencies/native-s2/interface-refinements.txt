# Explicit ordinary owner interface refinements for consolidated adoption

This document names production owner surfaces consumed by authored source. New interfaces are proposals until root's consolidated design amendment; none is an implemented fixture loader. Existing approved design remains authority. Absence of a proposed implementation is recorded separately from missing test source.

1. console_app::deployment::{load_config,start_services,DeploymentConfig,RunningServices} composes ordinary startup, validates deployment custody/installed build/worker trust, mounts router and real storage, and exposes local_origin. DeploymentConfig contains app AppConfig, keys/limits/endpoints, compiled_bundle_manifest, synthetic_issuer, workload, jwt_settings/public_pem, account_root_secret, terms_publication_custody and test deployment classification; no Account/Company/source/result refs. startup source uses actual run_migrations(AppRole::Migrate), then dedicated same-host/port exact-fresh-database LOGIN pools for console_app, console_auth_startup, console_auth_rt, console_rt, console_native_readback. Runtime retains actual migrator and migrate_config. start_runtime_with_config accepts plain untrusted normal config; build_identity_deployment_with_runtime accepts a real Runtime and still performs ordinary bootstrap/admit_current_process. Neither accepts ready=true or constructed admission receipt. Per-owner installed release activation, if implemented, must verify actual catalog/binary/codec/policy/aliases/current recovery and return a real owner receipt; business fixture completion is not that transition.

2. Ordinary identity/provisioning bootstrap is B6 exact manifest 4173bce3087ba47eb47e0215dbc5c309a4b423d4a3eb145e8d822bc1e2d4f0ee, containing real passkey Account C and independent signed Human C. Native S2 copies all4 B6 top-level source files byte-for-byte and custody dependency. Current Company capabilities/workload grants are separately owner-authorized after enrollment. Group control grants are workflow source responsibility.

3. payroll::ensure_person_for_human_binding and enroll_employee resolve actual signed Account/Human binding into canonical Person and Employment, owner-issued IDs/receipts and current entitlement. Scope/profile setup commands accept ordinary current authority, actual committed subject refs and actual source revision refs. No binding from Account UUID equality. Employment starts May31 to cover actual prior attendance anchor. Professional scope covers both A and C before source credentials are verified.

4. payroll::admit_installed_kernel_build verifies signed configured installed code/rule metadata against actual running binary. InstalledKernelBuild identifies installed build only; it contains no future Company qualification or citation-set refs. bind_qualified_rule_bundle accepts actual committed calendar/applicability/tax qualification refs and computes actual citation_set digest. RULE_BUNDLE qualification and create_supported_profile bind this real returned ref. Native fixture retains installed/build/profile/source controls for assertions.

5. Source admission control reads by actual Company effect-command+effect ordinals return exact persisted target, input/normalized artifact refs, draft/head/current revisions/digest, admitted kind/state, source payload, current reviewer credential/grant and last effect receipt. They do not create fixtures. Separate fixed Qualification/Eligibility/TaxEvidence Propose/Verify/Revoke input newtypes map exact9 registered keys even where shared wire payload schema is reused. Credential authenticity verification alone permits null reviewer_credential_ref and requires actual independent verified Human and credential_operator_grant_ref, as source19 already permits; other source reviews require current actual nonnull professional credential. Same-Human alias tests have real professional credential, so missing qualification cannot stand in for independence denial.

6. ProfessionalIssuerRegistration evidence_serialization=JwsFlattenedJsonEs256V1 pins ordinary RFC7515 flattened JSON verification. Signed claims contain actual provider/trust-profile/Person/scope/time/JTI refs. Exact protected/payload/signature strings preserve real ES256 signing input. All evidence goes through normal Docs upload tickets, same-origin no-redirect PUT and fixed authenticated worker verification. Source issuer_id is actual registered ASCII issuer identity; JWT iss may be a URL.

7. UploadRequest MIME refinement is exactly application/x-ndjson added to PDF/CSV/JSON enum; original schema SHA and enum pinned in mime-contract-delta.json. Actual source19 attendance packages are NDJSON and cannot be mislabeled application/json. Root authorized authoring the narrow proposal; consolidated adoption remains separate. All upload targets come from actual owner-issued explicit NEW slot then registered binding; no future artifact target refs.

8. Exact current source/native/Docs read interfaces expose authenticated owning projections, not raw result files: read_complete_native_source_refs, read_complete_supported_scope_subjects, read_current_native_projection with units/results, actual source24 preparation bytes/control and exact Docs evidence/governed revisions. Native owner read controls return current six-field expected snapshots including4nullable members. No timestamp/default coercion. source24 encoding and native28 codecs are fixed owner-private registered formats, not public serde_json Value normalization.

9. Two-subject fixture builds both actual full payroll subjects, combined scope/profile, re-admits first source refs against actual predecessor refs, signs/uploads both current-profile evidence packages and reads exact Docs evidence for preparation/review. Complete owner-derived attendance close enumerates exactly those2subjects. FixtureFacts.second_supported:Option<SupportedSubjectFacts> retains actual WorkforceFacts, SourceFoundations, PayrollSourceFacts and AttendanceFacts for second-subject consumers; ordinary one-subject fixture sets None. It is data from actual owner calls, not claimed authority.

10. Typed negative glue recognizes explicit classified semantic no-effect; transport/setup/unavailable owner failures are not positive acceptance evidence. Worker-account credential negative consumes actual account token and requires ContextError::InvalidCredential. Concrete ProducerStop conversion is authored. Additional protected native retention/lock/custody/load source obligations remain in inventory until authored S3.
