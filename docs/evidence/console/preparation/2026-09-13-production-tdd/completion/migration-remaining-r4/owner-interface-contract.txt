Bounded migration remaining-source candidate, 2026-09-14
Base fd94667fb9133f1ecb55831f43192bdc46776dc0. Root alone integrates. No implementation or authority adoption.

Integration: append migration-remaining-tests.rs as child of security S4 migration-transition-acceptance.rs, itself child of backend/app/tests/account_migration.rs, sharing existing immutable225 seed_legacy/apply_current helpers. Mount migration-batch-process.rs at integration crate root. Reuse native S2 deployment-runtime-producer and ordinary signed custody producer; no separate fake fixture endpoint. Declare required native support modules in that integration target.

Five exact obligations: related credential source revocation when users.xmin is unchanged; two distinct concurrent batch IDs for one source cursor; startup blocked on real migration ledger then real DDL mismatch; actual child process terminated after database commit and before observing its receipt; actual active legacy credentials and accepted WebSocket denied/drained through cutover and remaining denied after fresh primary activation.

Required ordinary owner interfaces (candidate refinements, not claimed existing): account_migration BatchRequest/BatchReceipt and typed SourceChanged/WriterBusy/CursorAlreadyConsumed; serving_admission current catalog under actual migration-ledger coordination; AppConfig::process_environment and AppRole::Migrate one-batch CLI (CONSOLE_MIGRATION_OPERATION=account-batch, CONSOLE_MIGRATION_REQUEST_PATH). CLI emits only its complete bounded receipt on stdout after commit; stderr never fixture credential material. Installed release descriptor binary digest must match actual invoked binary. Unix bounded-output test is process-loss evidence, not network ACK-loss evidence. Native actual startup custody/environment remain prerequisites.

Primary activation is the ordinary account-enrollment owner: start_account_primary_activation(auth,passkeys,PrimaryActivationStart{account_id,terms,browser_nonce_digest}); finish_account_primary_activation(auth,passkeys,PrimaryActivationFinish{activation_id,ceremony_id,browser_nonce,terms,credential}) returns ordinary issued Account session. Untrusted Account lookup cannot confer ownership. Owner must hold actual Account security/retained credential bindings, challenge exact preserved key, recheck terms/expiry/nonce/credential eligibility, consume ceremony once, increment security generation, atomically append terms assent and session. Missing/deleted/revoked credential cannot activate. This transport/interface refinement needs consolidated security/design review; it is not a new employer reset authority. Subsequent login uses shared ordinary Account authentication.

Actual legacy setup preserves original225 fixtures, adds real SoftPasskey and ordinary pre-cutover OTP reset, actual signed JWT/body+cookie refresh/device approval and Group grants as LEGACY fixture inputs only. Post-cutover grants/ACTIVE/fence rows are never seeded. Stored credential_id is JSON decoded before insertion into WebAuthn allowCredentials. Real denied attempts require HTTP401 plus classified unauthorized JSON; route404/storage500 is failure. Existing accepted WebSocket requires explicit1008 cutover close then bounded connection drain. Old credential request refusal must preserve security source rows.

Source limit: four files parse on Rust1.98.1; no owner compilation, Cargo discovery, behavioral execution, or RED claim. Product owner interfaces/catalog do not exist on base. No fake implementation is authored to manufacture RED. Existing boundary probes must independently admit implementation at their actual layer.

R2 review corrections: run_and_read now calls independently compiled bounded-process-output.rs; reads at most4MiB+1, timeout15s, explicit direct-child kill/reap on overflow/read error/timeout, stderr null. Three actual process controls cover exact small output/infinite output/stall; no process-tree guarantee. Mount bounded_process_output at integration crate root. Primary activation composition executes five fresh-ceremony wrong nonce/terms/ceremony/activation/key cases and actual RECOVERY_REQUIRED Account start refusal before positive activation. All require uniform PrimaryActivationError::InvalidProof, never arbitrary Err, and unchanged actual Account security/terms acceptance/refresh family/token rows. Failure may consume only ceremony. Refusal variant is explicit candidate ordinary owner contract; no new authority entrypoint or role is introduced. Six negative cases are inside the existing migration transport test, not six separately discovered Rust tests.

R3 fixture correction: legacy225 organization inserts ALREADY mint Group and attach UNIQUE(org_id) membership via0225 before/after triggers. Legacy transport now reads existing organizations.group_id joined to matching actual membership/Group, requires exactly1match and two distinct source Group identities. It creates only intended legacy GROUP_ADMIN fixture grants, not redundant Group/membership or UPDATE organization. This changes no post-cutover Account grants and no production migration. Source migration0225 unchanged; no runtime owner proof claimed.

R4 closure: existing auth_rest concurrent enrollment test exercises fresh enrollment, not retained-primary activation; no coverage inheritance. New actual primary activation challenge expires at independently read auth_webauthn_ceremonies.expires_at. Uses actual DB clock_timestamp and bounded360s wait for existing5minute policy, then exact expired assertion refuses InvalidProof with protected security/assent/family/token snapshot unchanged. After positive activation, replay exact captured successful activation/ceremony/nonce/terms/assertion (no new challenge) refuses same classified error with unchanged protected rows. Subsequent fresh primary login still succeeds. These two cases are nested in the existing source test (8nested negatives total), not new independently discovered tests. Runtime cost may be about5minutes; no shortened test-only authority/policy is introduced. No owner execution is claimed.
