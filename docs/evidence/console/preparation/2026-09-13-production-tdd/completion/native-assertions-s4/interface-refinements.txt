# S3 ordinary shared retention read/control refinements

Base source is native S2 manifest7e9b50db134a0f45365e210f86190dacac1626a2373980c96e55b9ab4d66424d. Root is sole integrator. These exact method/DTO additions are source-level proposals for consolidated adoption in existing payroll and common/Docs retention owners. There is no fixture-returning production function, special test authority, new cleanup service, arbitrary SQL adapter, or instantiated owner implementation here.

payroll::read_native_retained_streams(pool,current Company context,run) returns NativeRetainedStreams{semantic_manifest:EvidenceRef,custody_manifest:EvidenceRef,outcome_manifest:EvidenceRef} from actual committed native preparation/calculation controls. It verifies current authenticated disclosure and exact owning Company+run/head, uses the adopted NATIVE_INPUT_STREAM/NATIVE_CALCULATION_STREAM selectors and loads actual retained reference. It cannot synthesize refs from caller UUIDs. This narrowly describes a production owning read; no arbitrary raw table or caller predicate. Input aliases must name same actual UnitRef/digest/size; calculation names a distinct actual UnitRef.

Shared console_ontology_adapter_postgres::retention delegates typed native selectors and Docs holds to their fixed registered owners (same common retention facade as root draft R3). read_exact_retained_payload(pool,auth,EvidenceRef) returns actual admitted immutable bytes only with current complete field/disclosure authority and matching retained digest/size. read_unit_control returns actual unit, current generation, policy_revision:GovernedRevision and target:UntrustedActionTargetSelection for the typed current unit. The target comes from owning storage and does not grant permission. read_native_stream_alias_bindings returns semantic_locations/custody_locations as deterministic ordered actual location refs; copies have exact admitted identity and appear once per actual physical location, regardless of stream alias.

read_unit_protection_snapshot returns actual unit, locations, domain_pins, active hold_pins, disposal_jobs and disposal_receipts. Location records retain exact current copy identity/version/digest/size/state. Pin records retain unit, purpose_ref, obligation_ref and hold_ref for actual holds; domain pins retain actual result/purpose/obligation provenance. Historical pins and release receipts remain immutable even though current active-pin projection removes released entries. read_unit_protection_snapshot is a currently authorized administration/owner projection and not a public unrestricted evidence API.

place_hold uses root R3 ordinary HoldProposal{target,expected_policy_revision,reason}, returning actual reference and receipt. Each explicit new hold establishes its own real governed hold, preservation purpose and obligation on exact retained unit; identical underlying content is deduplicated, distinct hold obligations are not. release_hold accepts UnadmittedControl<HoldRelease{hold:GovernedRevision,expected_policy_revision:GovernedRevision,reason:String}>. It rechecks current manage-hold authority and exact hold/policy head; returns original actual receipt on exact command replay. It releases only that hold's pins. Any native domain/adoption obligations and another hold remain protected. No direct pin deletion API is exposed to fixture.

read_disposal_eligibility(pool,auth,unit) performs fresh complete guarded reverse-protection/copy inventory and returns unit,generation,policy_revision,eligible,blockers. A blocker contains code and optional actual hold_ref, not caller-approved boolean. Active native domain obligations produce active_domain_obligation. Holds produce actual hold identity. No staging/pin change during this read. prepare_disposal accepts UnadmittedControl<DisposalPreparation{unit,expected_generation,expected_policy_revision}>; real owner locks/rechecks same authoritative eligibility, admits quarantine only if all protections permit, and returns OwnerExecution. Stale generation/policy, active hold or active domain obligation are exact definitive-no-effect reasons in this packet. No job or receipt may be inserted on those refusals; byte identity remains exact. This supplements ordinary shared RR17 retirement/disposal coverage; it does not simulate years of retention or declare native data disposable.

Two hold purposes are authorized administrative synthetic preservation choices, not legal compliance claims. Active native stream obligations deliberately remain throughout tests. The positive physical removal lane must use genuinely retirement-eligible content under normal shared retention rules and exact holder/issuer/provider enrollment. Root explicitly selected reuse of draft/shared disposal tests; native unique proof here is actual retained stream/copy identity and no premature disposal. No zero-retention override, fake years, artificial vanished obligation, direct deletion or alternate provider path is authored.

Hostile SQL uses real console_rt and only fixed six-table native18 roster plus UPDATE/DELETE literals; SQLx AssertSqlSafe covers this audited formatting only. Company/run UUIDs stay bound. Matching committed rows are read back before each group; exact42501 denial and unchanged business snapshot required. Existing RLS context GUC does not grant UPDATE/DELETE. The test never INSERTs business facts or disables guards. The run-row rendezvous holds actual payroll_draft_runs row in SQLx admin transaction after real close and two actual binder commands; both owner futures must block before barrier release. pg_blocking_pids establishes observed contention, not chosen winner order. All progress is bounded and same real owners must finish with exactly one winner and one semantic no-effect.

Three in-owner streaming tests use existing S2 immutable goldens and actual fixed codec APIs. Fragmented reads prove consumption exactly once; u32MAX header must refuse without any body read; partial output failure must never return completed evidence summary. These are bounded I/O/hostile-length oracles, not measured process RSS or2000-person scalability claims. Production compiler/allocator/OS memory limit and actual workload accounting remain measured acceptance gates. EP26 counters/limits remain unchanged.
