BROWSER OWNER PREPARATION R2 — source candidate, not product implementation
Base fd94667fb9133f1ecb55831f43192bdc46776dc0. Root sole repository writer.
Eleven SQLx fresh-pool producers invoke exactly eleven retained Playwright cases: eight no-JS and three JS-enabled SSR. This is not hydration verification. Ordinary issuer/account/company grants, actual source upload/verification, native payroll calculations, review basis submissions and publication owners produce fixtures. No native business/permission rows seeded by SQL. Independent SQL readback is read-only.
The ordinary owners and ui30 routes are absent from the current implementation boundary; these sources are authored acceptance candidates, not runnable behavioral RED. Compiler/preparation failures cannot admit unrelated UI/authentication implementations. Existing retained browser bodies are unchanged.
Wire these sources into existing native_acceptance integration support after applicable lane admission: browser_case_producer, browser_acceptance_producer, browser_runtime_helpers, browser_auth_producer, payroll_source_facts_producer and two_subject_producer. Reuse native S2 fixture/source/bootstrap and workflow fixture; prefer final consolidated successors and recheck their exact signatures. Browser auth source is an explicit S4 helper refinement retaining actual Set-Cookie headers/timestamps and deleting expired Max-Age cookies. It is not a new authentication owner. Cookie0.18.2 and Tokio process/time feature availability are required; helper dependencies are locked.
Native ui30 proposed reads must return policy-checked registered paths and presentations. read_target_presentation is preflight custody, not the browser oracle; actual page assertions and post-owner readbacks remain separate. running_build_identity must describe the app actually serving requests. No arbitrary fixture route or preassembled successful result qualifies.
Run integration with --test-threads=1; SQLx pools isolated per case. Configure actual absolute Node/Playwright/config/module paths and exact candidate SHA through declared CONSOLE_BROWSER_* environment variables. Child waits use tested asynchronous Tokio supervision (180s per discovery/execution,8MiB cap per stdout/stderr stream, kill+reap on deadline/output error), preserving host runtime progress. Browser grandchildren cleanup remains owned by pinned Playwright runner; process kill is not a process-tree cleanup guarantee.
Private evidence and fixture material are 0600 inside0700 directories, retained for diagnosis and require ordinary test artifact lifecycle cleanup after review. Public export of raw cookie fixtures is prohibited. Actual source fixtures are synthetic; no legal/payable payroll claim.
Proof: ten product-independent helper tests compiled and passed; six Rust owner/helper sources parser-checked; actual Playwright discovered11 selected cases, application executions0. Helper CSV check validates retained approved46-row shape, not migration correctness. Missing owner compilation is explicit.

R2 independent review corrections: Expires-only past cookies are deleted consistently with Max-Age precedence. Read-only fixture now supplies all four denied-field canaries, including hidden evidence narrative. Existing eleven browser assertion bodies unchanged.

R3 selected hydration completion:15real-owner/Playwright cases (8noJS,3JS-enabled SSR,4hydrated). The existing11cases remain; the read-only expectation is strengthened to4canaries to match actual fixture, closing the R2 mismatch. New cases exercise real input-triggered autosave without Save, real offline stale conflict against a second browser commit, actual WASM request abortion with labelled manual form fallback, and offline input/reconnect preserving unacknowledged state. Actual WASM bytes/status/content-type plus input-generated fetch and receipt establish the selected path, not a marker.

The existing no-JS draft-save case also records its actual form command. Rust rereads baseline/final paged history using ordinary projection owner and compares client-observed typed DraftAck or manual command to exactly one new retained owner receipt/head revision. All old receipts/revisions remain byte-equal; no business effect changes. Stale command cannot have a save receipt. Readback covers the same actual draft throughout. Four new receipt-checker tests (with hostile mutations) join retained10helpers;14helpers pass.

200application/json DraftAck and409serialized definitive conflict at the existing composer POST pathname are explicitly proposed in transport-refinement.json. Accept/content type, operation, CSRF normalization, exact DraftSave input and current authorization remain one ordinary-owner pipeline. This response shape is not silently treated as previously approved.

Validation:6Rust parser checks,7JavaScript parser checks,14compiled helper tests pass. Actual Playwright discovery finds15application cases plus8oracle cases. No new application test execution or owner Rust compilation claimed; missing ordinary owners/ui remains genuine execution prerequisite. Human task observations and Chromium process-tree lifecycle remain separate later gates.
