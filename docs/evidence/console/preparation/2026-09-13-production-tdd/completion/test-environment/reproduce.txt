Preparation test environment
This is pinned test-tool evidence, not a production dependency change. Root Cargo and Node lockfiles are unchanged by this evidence record.

Rust owners: use repository rust-toolchain and backend/Cargo.lock; RUSTUP_TOOLCHAIN=1.98.1 cargo test --locked --manifest-path backend/Cargo.toml with the exact selected target. The ordinary PostgreSQL map owns one-node LOGIN fixtures. Recovery requires tools/lanes/recovery/supervise_recovery.py and the exact named two-node case/mode. Never substitute the one-node durability-disabled runner for recovery.

Reference oracle: create a disposable Python venv and install the exact reference-requirements.txt versions. Run Python unittest test_reference and schema_check.py in completion/native-r2. These are reference/fixture checks, not Rust owner coverage. Versions are recorded; pip distribution integrity and platform resolution remain normal environment supply-chain checks, not source-bound Verus evidence.

Browser: create an owned disposable npm prefix; copy browser-package.json to package.json and browser-package-lock.json to package-lock.json there, then npm ci --prefix that directory under Node24.16.0. Set NODE_PATH to its node_modules and use that prefix's playwright CLI. Install chromium via that same pinned CLI into PLAYWRIGHT_BROWSERS_PATH. For this final candidate run completion/browser-r4/playwright/playwright.config.cjs; completion/browser remains historical. After approved mounting use backend/app/tests/browser/playwright.config.cjs. The lock pins Playwright1.63.0 including integrity hashes; the installed Chromium identity is153.0.8010.12/v1243. Supplying an arbitrary fixture JSON cannot establish real owner setup or application build provenance; the reviewed producer is required. Empty fixture env reports prerequisite failure before application requests, never productRED.

Recorded paths /private/tmp/console-account30-cargo, /private/tmp/console-wire31-validator, /private/tmp/console-preparation-browser, and /private/tmp/console-preparation-browsers are disposable cache/env locations for this run, not required production absolute paths. Exact execution invocations are in per-boundary records.

Final-candidate commands using the already recorded owned cache paths (no owner fixture execution implied):
NODE_PATH=/private/tmp/console-preparation-browser/node_modules PLAYWRIGHT_BROWSERS_PATH=/private/tmp/console-preparation-browsers fnm exec --using=24.16.0 /private/tmp/console-preparation-browser/node_modules/.bin/playwright test --config docs/evidence/console/preparation/2026-09-13-production-tdd/completion/browser-r4/playwright/playwright.config.cjs --list
NODE_PATH=/private/tmp/console-preparation-browser/node_modules PLAYWRIGHT_BROWSERS_PATH=/private/tmp/console-preparation-browsers fnm exec --using=24.16.0 /private/tmp/console-preparation-browser/node_modules/.bin/playwright test --config docs/evidence/console/preparation/2026-09-13-production-tdd/completion/browser-r4/playwright/playwright.config.cjs --project=oracle

Actual application fixture invocation, only once actual owners exist and approved sources are mounted:
Set CONSOLE_BROWSER_NODE to the absolute Node24.16.0 executable, CONSOLE_BROWSER_PLAYWRIGHT_CLI to the pinned node_modules/playwright/cli.js, CONSOLE_BROWSER_PLAYWRIGHT_CONFIG to the absolute mounted backend/app/tests/browser/playwright.config.cjs, CONSOLE_BROWSER_NODE_MODULES to the pinned node_modules directory and PLAYWRIGHT_BROWSERS_PATH to its installed browser directory. Set CONSOLE_BROWSER_CANDIDATE_SHA to the exact compiled app source SHA. The ordinary app's signed running_build_identity must match it. The producer creates the private fixture/observation paths itself; do not invent a successful fixture.
RUSTUP_TOOLCHAIN=1.98.1 cargo test --locked --manifest-path backend/Cargo.toml -p console-app --features test-postgres --test native_acceptance browser_acceptance_producer -- --test-threads=1
Missing interfaces, unmounted target, failed setup or zero discovery are prerequisites, not a frontend behavioral RED. Final mapping records exact selected leaves; discover and require15 application fixture leaves after mounting. Keep each native fixture alive during its browser invocation. Retained raw fixture cookies/evidence stay private synthetic test artifacts with ordinary lifecycle cleanup; no audience publication.
