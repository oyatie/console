Console first native NONPAYABLE payroll vertical — consolidated preparation candidate

This packet is evidence and a proposed design/test amendment, not current product authority or permission to expose production. README and docs/current/{PRODUCT,ROADMAP,DELIVERY}.md remain authoritative. The selected first vertical already exists in current authority; this is not a new HR-only product roadmap.

Custody and ownership
Root /root is the sole writer of /private/tmp/console-production-tdd-preparation-20260913. Preparation base: fd94667fb9133f1ecb55831f43192bdc46776dc0. The final review receipt must name the exact candidate commit, not this base. Main /Users/jasonlee/Developer/console remains at05200e43af13b527ce031b04369e05852862f1d6 with94 staged paths; cached binary diff SHA2562a4a15c33cb1db7f512654f42c26d494843c6a45e997740594de43601d6d69c3. Do not import or overwrite that work. Never copy a scratch directory wholesale: required-packets.json pins each manifest and relocation-verification.json pins every retained member.

Review targets and precedence
Approved design30:7b6e4c6836750a23fc5fc38bdd37ac391e970215. Approved BW31:5f9f3b15704deaf8e81a682e4475af489d164210. Earlier bounded test approval:1e2ea6d2060a3d875e531a9a9e8f749984d0a171. Design30 scope authority was adopted at31f769accc08ac7dcb0d140709021c58d2a86c7e. Those approvals do not silently approve the amendments below. Historical predecessor packets are preserved; use only final named source overlays in module-placement.json. A later packet does not implicitly replace unrelated files in another family.

Material amendments proposed for one consolidated approval
1. native-r2/contract-refinement.txt and owner-interface-refinement.txt supply exact paired native stream grammar, tagged versus raw commitments, bounded allocation,22 retained selectors, and CCF1 owner-submission addressing. Native S2/S4 supplement actual immutable custody, streaming and contention assertions. Source24 command bytes stay distinct from source input bytes. Apply registered normalization paths, not generic property-name removal.
2. native-oracle-s8 refines common reason binding: keep editable blank draft input; ordinary seal and owner validation refuse missing required nonblank reason. Explicit common reason is retained in CCF1 command.reason. Source19 RevokePayload has no duplicate reason. If a registration has both body/common reason, disagreement refuses instead of overwriting. The previous nativeR2 body.reason-or-null mapping applies only without an explicit common field. Full retained input equality and independent command+attempt byte readback are authored assertions. NativeS8 also authors conflicting common/body reason refusal for actual ReviewWithdraw, unchanged history and same-actor matching success. No ad hoc new denial enum is inferred from generic no-effect.
3. bootstrap-b6, security-s5 and migration-r3 define ordinary deployment/admission and actual current policy/context reads. current_controls must come from the real authorized owner before sealing; a later concurrent change is a stale refusal, never a silent reseal. Fixed read APIs are test/application adapters over existing responsible owners, not fixture-only permission constructors. Account identity survives Employment changes; Group is tenant and Company is the legal/RLS cell. Job title never grants policy authority.
4. migration-remaining-r4 adds the ordinary retained-primary activation transport and classified InvalidProof boundary, with exact key/nonce/terms/ceremony/activation/recovery-state/actual-expiry/consumed-proof-replay negatives and an actual retained-key positive. This is an explicit security interface refinement. Account lookup is untrusted; no migrated ACTIVE/session/grant/fence row may be seeded. Reuse legacy225 Group-of-one identities. CLI output and post-commit process loss remain bounded, independently observed fixtures.
5. SecurityS5 uses existing backend/app/src/console_telemetry.rs for safe-event and telemetry registry ownership. No observability crate is added. Verus execution inputs must include the exact bound owner extraction and reviewed harness; mutations and compiler dependency closure are hash-bound. Existing toy/freshness/helper proofs do not prove product-wide information flow. Unsupported/missing owner/dependency evidence is a prerequisite failure.
6. The NDJSON UploadRequest MIME extension remains a proposed schema amendment in ../native-assertions-s2/mime-contract-delta.json; it is not already approved. Existing approved stream grammar is preserved. Publication tests separately cover Unicode codepoint title/locale limits, UTF8 content byte limits, actual current publisher scope and retained local digest. Dedicated publisher LOGIN is privileged TCB; this candidate does not invent database-level Ed25519 verification against theft of that credential. Ordinary runtime/helper/HTTP aliases must remain closed.
7. browser-r4 uses15 isolated native SQLx producers invoking exactly15 real Playwright cases while the actual application lives. Reuse ordinary auth/source/workflow/calculation owners. Actual historical/scoped gross is3000000; editable draft1200000 is a separate flow. String canaries stay in denied string properties. Cookie deletion honors Max-Age precedence and Expires. Bounded child output is verified. Four additional authored cases cover real WASM instantiation plus autosave receipt, stale conflict/input preservation, WASM failure/manual save and offline/reconnect. Actual wage history is independently unchanged by draft saves; each new receipt binds the exact new draft revision. New200JSON DraftAck/409 typed observation content negotiation is an explicit proposed transport refinement, retaining the same owner, CSRF and final current-policy emission fence. Browser process-tree lifecycle, actual application execution and user task observations are separate acceptance gates. Final browser sources/config are under ../browser-r4/playwright; ../browser remains historical.
8. draft-r3, attendance-r2 and workflow-r5 retain coherent shared action/draft/assignment semantics. Transfer reasons do not create different ownership engines. Automatic and manual paths use the same policy/current capacity/owner checks. Group controls are not Company Draft/Attempt records. Registration-coverage.json maps51 Company actions and4 Group controls without fabricating a55-by-six test matrix.

Mechanical implementation entry
First approve this exact candidate's material design amendments and test sources under DELIVERY; record design SHA and test SHA. The exact ephemeral-FK correction below needs its separately explicit trusted approval. Then assemble approved test files at module-placement.json targets, preserving source hashes except reviewed mechanical paths; serialize Cargo manifests/lockfile, generated Buck/test mappings, migrations and authority records. The reviewed mechanical guide, declaration fragments, dev-dependency map and executable regeneration checker are in ../module-placement-r3/. Run its check.py --root with the actual repository root; it covers76selected source placements,26compile-time assets and10runtime assets. Final concrete browser tool commands are in ../test-environment/reproduce.txt. Discover tests at the actual owning target. Missing owner/compiler/schema is a prerequisite only; no stub implementation may manufacture RED.

The first available bounded product lane is the HTTP formatter in backend/crates/platform/request-context/src/lib.rs. Allowed change: that owner only, and its reviewed tests only if no semantic weakening. Forbidden: migrations, auth policy, payroll, lockfiles, generated records, current authority and other worktrees. Probe and success command are identical:
RUSTUP_TOOLCHAIN=1.98.1 cargo test --locked --manifest-path backend/Cargo.toml -p console-platform-request-context --lib
On the preparation base20 tests execute:16PASS,4 meaningful diagnostic-independent body equality failures. After final test approval, rerun on a clean exact approved SHA and use python3 tools/lanes/fanout.py admit --probe '<same command>'; start an implementer only through fanout.py run --spec docs/evidence/console/preparation/2026-09-13-production-tdd/completion/consolidated/first-lane.candidate.json after binding the approved SHA in its lane setup receipt. The tooling expects the designated isolated lane worktree under its existing LANES_ROOT; do not repurpose another lane or rewrite that tooling for this task. Admission failure, already green, missing executable or unrelated infrastructure failure stops. One first lane passing does not complete the vertical.

Remaining execution order and exact existing probes are in ../implementation-order.txt and retained boundary execution records. Context formatter, actual Cedar evaluation, realtime current-Company delivery and telemetry formatter each have their own reachable behavioral RED. Catalog-only missing Account tables cannot admit auth/session/policy/UI work. Native sources become executable as their owners arrive; re-establish a classified RED at each real boundary before its implementation. Owner-local tests use the actual package --lib target, not a nonexistent integration binary. All selected deeper tests must execute before slice acceptance.

Pre-mortem and containment
Likely failures: unrelated denial masquerades as targeted refusal; stale controls invalidate a positive setup; a checker omits an entire packet/request/effect; public diagnostic reveals private state; cancellation or replay invents a new command; a fixture bypasses ordinary grants; an output cap is checked after unbounded allocation. Detection: same-actor/current-control positives, exact SQL/owner readback, complete identity-bound histories, custody negative controls, source-bound proof input/dependency controls and bounded child failure injection. Source-only parser checks are never listed as runtime acceptance.
Blast radius: isolated synthetic disposable test deployments and a single admitted owner. No real customer data, legal conclusion, payment, provider enrollment, credential reset, audience publication or production exposure. Stop for unclassified failure, changed contract, source custody drift, active duplicate writer, unexpected role/cross-tenant access or unexplained missing effect. Preserve original command/input/reconciliation identity under ambiguity. Roll back source through a new scoped revert; data restoration is a separate approved recovery procedure, never inferred from a software rollback.

Gate boundaries and remaining authority
gate-assessment.json assigns every checklist requirement to before-implementation, before-slice-acceptance and before-production stages, plus earlier real-data activation when relevant. Later capabilities preserve identity/policy/version/event/addressing dependencies now; they do not require production certification today. The23-family whole-Console ledger and12-task usability protocol remain in ../../readiness-review/final/. Pilot is one Group tenant, six Companies,2000 people;50k/1m evaluation is workload/hotpath/operating-envelope driven, not a mandate to adopt locks or MVCC.
Consolidated review must bind the same exact commit across architecture/security/domain/UX/concurrency/recovery/evidence machinery. Record the reviewer identities and unresolved findings; do not infer approval from this handoff. Product implementation, slice acceptance, release and production exposure remain different decisions.

Explicit automatic approval restriction
Automatic approval review twice rejected changing backend/app/tests/auth_rest/account_storage.rs to permit only public.auth_webauthn_ceremony_bindings(ceremony_id) -> public.auth_webauthn_ceremonies(id) DELETE CASCADE, requiring that edge and allowing only UPDATE NO ACTION/RESTRICT while preserving every other destructive-edge assertion. The proposed correction is independently reviewed and justified by existing migration0144 and expired/consumed ceremony retention, but the automatic reviewer requires trusted-user approval. It has not been applied. The exact unapplied diff and original source hash are in ephemeral-fk-correction.proposed.patch.txt and ephemeral-fk-correction.proposal.json; see ../../storage-test-correction-receipt.json. This prevents claiming unrestricted implementation readiness; it does not justify pausing unaffected preparation.
