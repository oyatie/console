[
  {
    "lane": "native30-context-formatter",
    "probe": "RUSTUP_TOOLCHAIN=1.98.1 cargo test --locked --manifest-path backend/Cargo.toml -p console-platform-request-context --lib",
    "allow": [
      "backend/crates/platform/request-context/src/lib.rs"
    ],
    "prompt": "Implement only the approved diagnostic nondisclosure correction in backend/crates/platform/request-context/src/lib.rs. First verify HEAD exactly equals the approved test SHA recorded in the revision-bound approval receipt and the root-designated lane worktree is clean. Read README and current DELIVERY. Keep one writer per root. Preserve all20 tests and their semantics; do not change tests to make them green. Reuse existing error formatter ownership; remove private diagnostic influence from public response body while preserving independently approved status/header/actionable public class. The named probe must begin with exactly20 executed,16pass,4body-equality behavioral failures, not compiler/setup failure, and finish20pass. No auth policy, DB, payroll, manifests, generated or authority files. Stop on new interface choice, mismatching base/probe, unclassified failure or outside-scope need; report exact diff, command/counts and remaining HOLDs. Pre-mortem: blanket error hides actionable status or another formatter path still leaks; existing positive class tests and diagnostic equivalence tests detect both. Blast radius: formatter only. Rollback is a scoped source revert preserving tests; no data effects. This spec is preparation, and must not run before consolidated design/test approval. Reviewer identities for this preparation: /root/review_completeness (lane scope,11+8 gates), /root/finish_migration (source/oracle security corrections), /root/finish_browser (UI/test assembly). Consult final revision-bound receipts; names alone do not grant approval. Remaining HOLDs: new material design/test approval, exact ephemeral-FK correction approval, all dependent owner tests and actual slice acceptance, production/payment/legal authority."
  }
]
