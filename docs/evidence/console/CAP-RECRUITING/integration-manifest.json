{
  "lane": "CAP-RECRUITING",
  "stage": "backend-implementation",
  "generated": "2026-07-24",
  "shared_root_entries": [
    {
      "file": "web/src/console/shell/nav.ts",
      "change": "Add \"recruit\" to MOUNTED_SCREEN_KEYS (nav item already exists at line ~159 with gate DIRECTORY_ROLES + employee_directory_read; optionally widen gate features to [\"employee_directory_read\", \"recruiting_read\"]). Do NOT touch EXPOSED_SCREEN_KEYS — module lands DARK per ADR-0025."
    },
    {
      "file": "web/src/console/screens/registry.ts",
      "change": "Add `recruit: RecruitingScreenBody` (prop-less ComponentType) imported from \"../recruiting\"."
    },
    {
      "file": "web/src/i18n/ko.ts",
      "change": "None. console.shell.nav.recruit already exists (\"채용\", line ~970). Module strings live in the new module-owned web/src/i18n/recruiting.ts (not a collision root)."
    },
    {
      "file": "backend/openapi/openapi.yaml",
      "change": "Merge docs/evidence/console/CAP-RECRUITING/manifests/openapi-fragment.yaml (21 operations, every one `tags: [recruiting]`; components as named in the fragment). Then regenerate and commit clients/{ts,kotlin,swift} (3 independent CI drift gates)."
    },
    {
      "file": "backend/app/src/lib.rs (CONFIGURED_ROUTE_SURFACES)",
      "change": "NOT registered by this lane on purpose — openapi_drift.rs::openapi_yaml_covers_configured_route_inventory would fail while openapi.yaml lacks the recruiting paths. When merging the openapi fragment, add ConfiguredRouteSurface { name: \"recruiting\", paths: console_recruiting_rest::RECRUITING_ROUTE_PATHS } plus the app-owned hire path \"/api/v1/recruiting/applicants/{applicant_id}/hire\" (backend/app/src/recruiting_hire.rs), and add a CONFIGURED_ROUTE_SOURCES entry for crates/recruiting/rest/src/lib.rs in backend/app/tests/openapi_drift.rs."
    },
    {
      "file": "backend/crates/platform/db/migrations/0187_create_recruiting.sql",
      "change": "IMPLEMENTED in this lane (provisional number). Renumber to the next free number immediately before push — 0180 was the max at scout time; this lane also had to renumber a colliding duplicate (see notes)."
    }
  ],
  "implemented_by_this_lane": [
    "backend/crates/recruiting/{domain,application,adapter-postgres,rest} — new four-crate module (workspace members + path deps registered in backend/Cargo.toml)",
    "backend/crates/platform/authz/src/lib.rs — Feature::RecruitingRead [D,D,D,A,A,A] (\"recruiting_read\"), Feature::RecruitingManage [D,D,D,A,D,A] (\"recruiting_manage\"); Feature::ALL is now 82",
    "backend/app/src/hr.rs — create_employee_core + employee_create_audit extracted from create_employee (same idempotency reservation, tables, lifecycle event, audit); handler now composes them",
    "backend/app/src/recruiting_hire.rs — app-level hire route sharing ONE with_audits transaction across hire_context → create_employee_core → apply_hire; idempotency key recruit-hire-{applicant_id}",
    "backend/app/src/lib.rs — build_router merges console_recruiting_rest::router + recruiting_hire::router next to hr::router",
    "backend/app/tests/recruiting_pipeline_api.rs — console_rt integration story + PBAC/cross-tenant/audit-readback coverage",
    "backend/crates/platform/db/migrations/0187_create_recruiting.sql — recruit_postings/applicants/offers/stage_events, org RLS + FORCE, console_rt grants (no DELETE; stage_events append-only), feature_catalog inserts"
  ],
  "pre_existing_spine_breaks_fixed_here": [
    "backend/crates/platform/auth/src/jwt.rs — commit b0d31af8 added the 7th actor_home_org parameter to issue_access_token_inner without updating two call sites (did not compile); fixed with None (the non-delegated default) + clippy too_many_arguments allows",
    "backend/crates/logistics/adapter-postgres — sha2 0.11 removed LowerHex on digest output; fingerprint() now uses the house hex::encode pattern (hex dep added)",
    "backend/crates/facilities/rest — did not compile: missing .with_state(state), missing Serialize on DueCaseBody, borrowed `to`/&Principal captured by 'static audit closures; minimally repaired",
    "backend/crates/production/rest — did not compile: Transaction<'_> missing lifetime (now 'static), source_version moved thrice (now cloned), hmac 0.13 KeyInit import, PlanSummary missing plan_digest read; minimally repaired",
    "backend/crates/platform/db/migrations — TWO migrations were numbered 0170 (financial_purchase_request_queue_index vs harden_object_policy_attachment_and_blockers), breaking every sqlx::test migration run; the independent financial index migration was renumbered to 0181 (0171 textually depends on the ontology 0170, which kept its number). Re-verify against main before merge — deployed _sqlx_migrations version history must not contain the financial file as version 170.",
    "backend/app/src/hr.rs create_employee (People & Workforce, landed 2026-07-23 on the spine) violated migration 0166's home-branch command boundary: it INSERTed employees.home_branch_id as console_rt, which the leave_api.employee_leave_writer_guard trigger rejects (hr_people_create_api failed with leave_write.command_required). Repaired at the shared core: create_employee_core inserts WITHOUT home_branch_id; both callers (People create + recruiting hire) fail closed without the leave command capability and establish the first routing branch post-commit via leave_api.set_employee_home_branch (console_leave_cmd; its own CAS + employee.home_branch_set audit; race-convergent on same-branch first assignment). AppState gained with_leave_command_database(pool) so integration tests can arm the capability; PgLeaveStore gained has_leave_command_pool(). The employee.create audit snapshot field home_branch_id was renamed requested_home_branch_id (assignment is separately audited)."
  ],
  "cross_lane_dependencies": [
    "ontology posting→employee link binding (POSTING_EMPLOYEE_LINK_KEY) stays unresolved — separately governed compatibility change, out of this lane",
    "candidate self-service apply + v6 persona, passkey offer inbox receipt, talent-pool→workforce-pool conversion, POOL_DAILY hire registration — deferred per gap-analysis section 4 (hire on a POOL_DAILY posting returns 422 POOL_REGISTRATION_UNAVAILABLE; never fakes a registration)"
  ]
}
