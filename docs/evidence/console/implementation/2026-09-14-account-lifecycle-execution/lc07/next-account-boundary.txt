ACCOUNT NEXT OWNER BOUNDARY — read-only diagnosis
Reviewer /root/finish_browser. Root /private/tmp/console-production-tdd-preparation-20260913; no product writes/Cargo or database execution. Current source inspected after pure authority helper integration.

EXACT THREE FAILURES (NOT AN AUTH-FENCE RUN)
Evidence docs/evidence/console/implementation/2026-09-14-aws-lc/awslc-combined-custody-green.log.gz; raw /private/tmp/console-account-lifecycle-execution-20260914/awslc-combined-custody-green.log lines315–343. Actual36executed=33pass3fail58filtered:
1. account_storage::account_custody_has_no_company_ownership_or_destructive_foreign_keys, account_storage.rs:177: auth_refresh_token_families.org_id is still NOT NULL.
2. account_storage::account_custody_owners_and_effective_column_grants_are_separate_from_business, line388: seven retained legacy auth tables are still LOGIN-owned console_app. Six new tables have correct distinct NOLOGIN owners.
3. account_storage::company_login_cannot_read_or_mutate_account_custody_even_with_company_context, line361: real console_rt SELECT/DELETE probe succeeds (0rows), where42501 required. Positive authorized branches read passed; this is actual excessive privilege, not missing table.
ACCOUNT_RELATIONS contains13 tables: six new plus auth_webauthn_credentials, auth_webauthn_ceremonies, auth_refresh_token_families, auth_refresh_tokens, auth_bootstrap_credentials, auth_webauthn_ceremony_bindings, auth_device_login_handoffs. Do not rename this run33/33GREEN or say the3failures are caused by crypto. Do not enlarge frozen0226 six-table finalizer in place to hide them.

SMALLEST NEXT EXISTING-API RED (NO SCHEMA NEEDED)
The five account_auth_database_* tests at backend/app/tests/auth_rest.rs:3208–3281 execute actual AppConfig::from_pairs. Historical mapped-baseline.log records4fail1pass. Current AppConfig still has no auth_database_url field and ignores AUTH_DATABASE_URL; its API parser only validates runtime/leave/ontology/force credentials. Thus the next narrowly attributable owner is AUTH_DATABASE_URL configuration validation, not seven-table DDL or all Account auth.
Proposed exact probe for root to execute on its immutable candidate:
RUSTUP_TOOLCHAIN=1.98.1 RUSTC_WRAPPER= SQLX_OFFLINE=true CARGO_TARGET_DIR=/private/tmp/console-account30-cargo cargo test --locked --manifest-path backend/Cargo.toml -p console-app --test auth_rest account_auth_database_
No runtime DB is needed by these5 #[test] functions; compiling auth_rest may load SQLx assets but test execution itself is pure config. I did not execute current probe because root reserved Cargo; label4fail1pass historical until fresh run.
Requirements already asserted: required when API auth enabled; nonempty explicit URL; exact console_auth_rt username; nonempty percent-decoded password distinct from runtime and all3command passwords; reject identity/role/query overrides; config errors identify AUTH_DATABASE_URL without secret echo; valid config preserves runtime URL and enabled auth_rest. Reuse validate_database_url_identity, ensure_distinct_database_credentials and URL normalization; no second DSN parser. Sibling worker/migrate/noDB/disabled-auth config must retain explicit intended behavior. Accepted-positive test currently does not assert an auth_database_url field because field absent: independent source review must ensure value is retained, not validation-only discarded.
Allowed next first mutation: AppConfig field/parser/constructor ownership plus necessary explicit fixtures; no database privileges, credentials or token issuance implied. Root should establish clean exact RED/admission and independently review fixture changes; don't silently weaken old API config tests that omit the newly required transport.

NEXT AFTER CONFIG: RESTRICTED AUTH POOL + LEGACY FENCE
AS1.3 account-contract.txt:8–15 explicitly selects console_auth_rt separate LOGIN and permanent account_security-row-existence fence. Existing AuthRestState::new uses only business pool; AppState::from_config/new wires same pool, and request-context legacy resolver is separate. Runtime pool cannot directly SELECT protected account_security after finalization. Adding a business-role SELECT grant or broadly callable private-data helper to make fence tests pass is forbidden.
Before meaningful fence GREEN, provide reviewed restricted auth-role topology and narrow custody read path, bind live connection identity/reset hooks as with other pools, and decide exact owner function/table verbs per already selected least-privilege design. New auth pool may not obtain arbitrary Company business access or owner membership. Existing config tests alone do not certify this transport.
Useful existing fence probe after those prerequisites:
cargo test --locked --manifest-path backend/Cargo.toml -p console-app --test auth_rest account_fence_
7SQLx cases plus1pure denial-oracle case: unmigrated positive; PENDING_ENROLLMENT/ACTIVE/SECURITY_SUSPENDED/RECOVERY_REQUIRED; recovery back toACTIVE; outstanding employerOTP; safe denial oracle. Fixture inserts actual Account/security rows via fixture admin only, preserves key bytes and checks unrelated unmigrated subject. These are existing real HTTP boundaries, not fake verified contexts.
Freeze Account cutover activation until all actual sibling authenticators fence; row existence denies regardless state, Company enrollment or current Company. No recovery-to-ACTIVE exception; no legacy JWT sid/generation inference; no employer refresh/OTP promotion into AccountV1. DB failure/missing read authority cannot mean unmigrated allow.

SIBLING PATHS THAT A SINGLE HANDLER FIX WOULD MISS
- platform/request-context/src/lib.rs resolve_principal/resolve_principal_with_peer and Group resolver: shared tenant route/SSR identity path.
- platform/auth-rest/src/lib.rs local principal_from_headers: self passkey list/delete, handoff approval, Group context and related auth routes outside ordinary tenant middleware.
- start/finish WebAuthn registration/login and mobile step-up; signup/redeem_otp; admin OTP and credential reset; device enroll/start/poll/approve/session approval; body AND cookie refresh; development session feature and alternate JWT minting.
- issue_token_pair/issue_access_token/load_user_auth_context_in_org/tx; merely changing load_user_auth_context is insufficient because refresh::rotate has already mutated family/token state before that read (auth-rest:2120). Fence must precede successful issuance and be serialized with cutover at authoritative transaction boundary, not checked only afterward.
- platform/auth/src/refresh.rs still requires OrgId for family/token issuance and resolves token->Company; webauthn similarly relies on Company credential lookup. Account path needs actual Account correlation, not nil/sentinelOrg.
- realtime websocket/other authenticated transports must use same Account fence/currentness before enrollment; existing same_account subscription cleanup is not global authentication fence evidence.
These must be audited for cutover readiness; narrow first config/fence work need not falsely claim all siblings converted.

REQUIRED LEGACY CUSTODY MIGRATION (SEPARATE VERSIONED PACKAGE)
1. Preserve historical0001–0226 and the reviewed v1 six-table finalization checks. Current owner prevents ordinary console_app alteration of finalized Account tables, so new DDL must use the separately versioned operator/application ordering identified in lifecycle refinement; do not grant console_app owner membership.
2. Backfill immutable accounts.id from existing users.id, preserve mapping one-to-one; create mandatory account_security guard under controlled cutover. No merging by email/name; nonACTIVE rows already trigger permanent legacy fence. Activation follows full refusal/drain/source-binary compatibility, not table creation alone.
3. Seven retained auth tables retarget user references to accounts; remove both simple users FKs and composite Company ownership FKs only after validated replacements. org_id becomes nullable provenance. Preserve credential IDs, WebAuthn handles/passkey JSON/counters, refresh hashes, family membership, timestamps, existing revoked_at/reason and original audit attribution. Token->family FK must bind same Account. Handoff target_user_id must bind Account with nullable target_org_id. Keep only approved ephemeral ceremony-binding DELETE CASCADE; retained custody edges nondestructive.
4. Remove permissive Company credential RLS policies rather than OR new checks; revoke table/column/PUBLIC/inherited business access and owner escalation; transfer legacy custody under nonLOGIN owner; explicitly grant narrow console_auth_rt operations. Catalog3cases plus both populated225-to-current migration tests must prove actual conversion, preservation and revocations.
5. Contain second writers/employer destructive paths: identity/adapter-postgres::deactivate_user/revoke helpers (lines488,2238+); auth-rest admin reset/OTP and both passkey deletes; platform/provisioning direct credential delete(line330); latest platform force-remove SQL (0196 onward and any successor) direct auth deletes plus catalog FK sweep. Do not modify historical migrations—add current versioned replacement functions. Employer exit/purge must not delete Account credentials or revoke unrelated Account sessions.
6. Keep rollback Account-capable after first Account-only rows: code rollback is not data restoration; security generations/fence cannot decrease/disappear. No old binary requiringCompany/roles for every login may serve after cutover.

EXACT CONSERVATION PROBES TO PRESERVE
backend/app/tests/account_migration.rs:305 populated_225_to_current_preserves_credential_bytes_token_correlations_and_original_audits;:328 populated_225_to_current_preserves_preexisting_family_and_token_revocations. Current dormant0226 intentionally has no backfill; first test checks every original subject exists in accounts and second preserves logout revocations. Their failure is separate from the3catalog tests. All46 actor FK conversion and employer-independent handoff have separate account_storage tests, not exercised by selected36case run.

RECOMMENDATION
Next dispatch only existing5case AUTH_DATABASE_URL config RED; in parallel prepare reviewed restricted-auth topology/pool/fence migration interface package. Follow with permanent legacy fence on actual endpoints and preserved unmigrated positives; only then versioned legacy-custody conversion/cutover. This is a concrete route toward current-context -> common draft owner -> Leptos, without reopening whole Console planning or pretending13-table ownership is already done.
