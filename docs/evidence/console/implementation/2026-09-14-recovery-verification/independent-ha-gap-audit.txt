HA / RECOVERY / DATA LOSS GAP AUDIT
Reviewer /root/review_completeness. Read-only at requested8da3bd1d05e05d665ee847d401252e71496a2f8f; no infra, Cargo, deployment or product edits. Facts distinguish source/configuration, historical execution, planned contracts. Current authority PRODUCT23 leaves replication evidence-based; all production exposure remains separately held.

ANSWER
Not complete. There are backup/PITR scripts, historical local restore successes, an optional synchronous CNPG HA overlay and a real two-node durability test harness. They do not yet establish production HA, zero acknowledged-write loss, cross-store recovery, split-brain prevention or safe external-effect replay. One actual owner remote-apply durability test historically failed; the CNPG restore checker also has demonstrable false-success assertions.

IMPLEMENTED CONFIGURATION / CODE
- ops/compose.yml:41-65 is one PostgreSQL18.4 container with local volume WAL archiving, archive_timeout300s. It is not replicated HA. archive_command copies locally; local VM/disk failure can lose data and WAL together until off-VM transfer succeeds.
- deploy/apps/console/base/database.yaml:7-11 explicitly has one CNPG instance. Barman plugin plus ObjectStore35d and daily ScheduledBackup appear :72-135; configuration files are not live backup success evidence. PostgreSQL image18.4 differs from recent18.6 Account tests.
- deploy/apps/console/overlays/on-prem/cnpg-ha-patch.yaml:12-55 declares3instances, sync1 required, failoverQuorum, hostname-aware standby election. Anti-affinity is preferred and ScheduleAnyway; this alone does not prove independent failure domains or availability under node/zone loss. No serving deployment synchronous_commit=remote_apply found; this exact setting exists in test fixture.
- deploy/apps/console/base/backend.yaml:8 has2API replicas and termination grace; worker.yaml:8 has1worker with drain/PDB policy. More app replicas do not make a single database/object-store/secrets backend highly available.
- ops/backup/backup.sh:212-246 creates logical dump/globals, physical base backup, local WAL archive tar, SeaweedFS live volume tar and file hashes; artifact SHA256+retention exist. DB and object-store backups are not one coordinated consistent recovery checkpoint; files may change between live tar and later manifest. Object-reference consistency or accepted upload loss is not proven by separate row counts/file checksums.
- deploy/apps/object-store/manifests/statefulset.yaml:13 declares one DARK onprem SeaweedFS replica. deploy/apps/console/base/mox.yaml has persistent mail volume and replicas0; no complete mail+DB recovery proof found in selected backup scripts. Secrets/key custody has its own durability requirements; restoring database backups alone cannot restore or safely revoke missing signer/identity keys.

WHAT WAS ACTUALLY TESTED
- ops/backup/drill-logs/20260612T102439Z-restore-drill.log records local logical restore, matching SQLx metadata/row counts and SeaweedFS manifest/service. Historical synthetic Compose success only, not current schema/data sizes or production/offsite restoration.
- ops/dr/drill-logs/20260612T111539Z-pitr-drill.log records actual before-row exists/after-row absent around target and scratch teardown; useful timestamp-boundary evidence. Not an HA promotion/split-brain drill or currentRPO/RTO measurement under declared load.
- docs/evidence/console/preparation/2026-09-13-production-tdd/completion/active-runner-execution.json: candidatefd94667f. Actual two-node owner replay_cannot_acknowledge_a_locally_visible_receipt_before_remote_apply:1executed,1FAILED (classified meaningful ownerRED); lost_commit_transport_reply_replays_one_real_payroll_effect:1executed,1PASS. This is historical evidence; do not claim current source is fixed or re-executed. SameVM failure-domain limit is explicit.
- tools/lanes/recovery/recovery_control.sh:26-44 asserts fsync/full_page_writes, remote_apply and exact admitted streaming standby and captures LSN/timeline. recovery_postgres.sh:111-114 configures required synchronous test standby; supervise_recovery.py bounds lifecycle. This is genuine executable infrastructure, not just a proposed test document.

CRITICAL GAPS / WHY THEY MATTER
1. FALSE RESTORE ORACLE: ops/dr/cnpg-restore-drill.sh:297-325 treats count(pg_stat_user_tables)>0 as verify_row_counts=ok, prints estimated n_live_tup instead of comparing independent expected exact values, then emits verify_pitr_target=ok without comparing target/before-after commit boundary (last commit query may become n/a). A partially restored/wrong-time DB can therefore receive success. Fix oracle before trusting this script as restore proof; require independently captured exact manifests and target-boundary controls. Root is taking this boundary.
2. ACKNOWLEDGED DURABILITY: design30 operations-recovery-information-flow.txt:51 requires remote_apply against independent admitted standby, no async fallback and no replay success from locally visible commit while remote application is pending. Existing historical ownerRED demonstrates this distinction matters. Test current owner at same boundary after restoration-oracle correction; don't replace RED with availability-only tests.
3. SPLIT BRAIN / EXTERNAL EFFECTS: same contract:51-53 requires old-primary writes AND egress fencing, exact promotion watermark/timeline ancestry, UNKNOWN for unresolved dispatch, and isolation if complete authority timeline unavailable. Multicluster runbook is procedure, not executed fenced promotion proof. A new primary lease/healthy endpoint does not prove old workers stopped sending mail, uploads or provider requests. Provider terminal receipts/reconciliation cannot be inferred from missing restored rows.
4. OFFSITE LOSS WINDOW: ops/dr/DR-POLICY.md:23-24 targets RPO<=5min/RTO<=1h; :34-39 says archive_timeout300s plus off-VM sync at least every5min. Offsite sync implementation/schedule/success-age monitoring not found in selected ops/deploy scripts. Archive delay+independent copy delay can exceed5min; measure end-to-end durable remote WAL lag, not schedule periods. Nightly logical backup cannot meet5minRPO.
5. BACKUP CONFIDENTIALITY / KEY CUSTODY: backup scripts create dump/globals/tars with restrictive local modes, not application-encrypted archives. Barman config specifies gzip and HTTPS endpoint but no explicit customer key lifecycle/decryption-restore test. This does not prove cloud storage is unencrypted; it means encryption-at-rest, access separation, immutable retention, deletion protection and key-loss/rotation recovery are not established by repository evidence. Globals include sensitive role state. Restoration also must reapply post-snapshot erasures/legal holds/current Account revocations before exposing old data; signatures/local deletes alone do not prove erasure or current authority.
6. COMMON DATA COMPLETENESS: restored DB receipts and object versions/content, mail state, draft history, audit seals/independent anchors and egress journal must share an explicit recoverable boundary. No complete first-slice cross-store restore proof found. Single SeaweedFS replica and independent tar sequence leave a real dependency not covered by SQLx row counts.
7. MEASURED CAPACITY: no current measured failover/RPO/RTO under2,000employee6Company pilot hotpaths found;50k/1m targets are workload evaluation paths, not reasons to claim certification. Synchronous durability may intentionally stop writes during replica loss: define graceful read/UNKNOWN/retry behavior and recovery backlog limits rather than silently weaken data durability.

EXISTING COMMANDS (NOT EXECUTED IN THIS AUDIT)
- Pure static checks: node --test scripts/check-production-hardening.test.mjs ; node scripts/check-production-hardening.mjs. Configuration assertions are not live HA proof.
- Historical local restore: ops/backup/restore-drill.sh --scratch-project <isolated> --backup-dir <reviewed-artifact>.
- Timestamp recovery: ops/dr/pitr-drill.sh --source-project <synthetic> --scratch-project <isolated> --backup-root <scratch> --target-timestamp <future-RFC3339>.
- CNPG restore: ops/dr/cnpg-restore-drill.sh --help describes target/namespace options, but its success oracle must first be corrected; do not run against production merely for this review.
- Real owner durability: CONSOLE_RECOVERY_CUT= python3 tools/lanes/recovery/supervise_recovery.py <repo> -- cargo test --locked --manifest-path backend/Cargo.toml -p console-payroll-adapter-postgres --test recovery replay_cannot_acknowledge_a_locally_visible_receipt_before_remote_apply -- --exact --test-threads=1 --nocapture . Separate retained scenario uses CONSOLE_RECOVERY_CUT=after-commit-response and lost_commit_transport_reply_replays_one_real_payroll_effect. Needs explicit isolated Docker resources/candidate/toolchain custody; not ordinary singlePGCI.

RECOMMENDED NEXT BOUNDARY
First make CNPG restore evidence trustworthy with independently reviewed exact-content and PITR boundary controls, including known wrong/missing data cases. Then execute/fix the existing owner remote-apply acknowledgmentRED using declared durable commit/replay semantics. These precede any HA/data-loss completion claim. Follow with isolated old-primary+egress fencing/promotion/lost-ack replay drill and cross-store backup restore at measured pilot load. Account LC07 migration orchestration remains a separate implementation dependency; it neither solves nor proves these HA/recovery guarantees. No live deployment is authorized by this gap map.
