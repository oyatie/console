--- account-auth-refresh6-driver.sh
+++ account-legacy225-replay6-driver.sh
@@ -11,7 +11,7 @@
 export FENCE_CONTAINER_RECORD="${evidence_dir}/container-name.txt"
 export FENCE_BUILD_RECORD="${evidence_dir}/prebuild-dispatch.txt"
 export FENCE_IMAGE='postgres:18.6@sha256:4ef4dbc939d61acea57712655ddb4b4ab27419c913f94cca0cd57cb3ea3c2280'
-export FENCE_LABEL="account-auth-refresh6-$(openssl rand -hex 16)"
+export FENCE_LABEL="account-legacy225-replay6-$(openssl rand -hex 16)"
 map_path="${evidence_dir}/postgres-map.json"
 
 snapshot() {
@@ -22,6 +22,8 @@
  'backend/app/Cargo.toml','backend/app/tests/auth_rest.rs',
  'backend/app/tests/auth_rest/account_fence_projection.rs','backend/app/src/lib.rs',
  'backend/app/tests/auth_rest/account_fence_transport.rs',
+ 'backend/app/tests/account_migration.rs',
+ 'backend/app/tests/fixtures/account-legacy225-owner-output-v1.json',
  'backend/crates/platform/auth-rest/src/lib.rs',
  'backend/crates/platform/auth/src/refresh.rs',
  'backend/app/tests/auth_rest/fixtures/account-custody-dormant-v1-7af6dfd4.sql',
@@ -46,13 +48,12 @@
 python3 - "${FENCE_ROOT}" "${map_path}" <<'PY'
 import json,pathlib,sys
 root=pathlib.Path(sys.argv[1]); data=json.loads((root/'tools/ci/postgres-cargo-map.json').read_text())
-entries=[e for e in data['entries'] if e.get('name')=='app-auth-rest-pg']
+entries=[e for e in data['entries'] if e.get('name')=='app-account-migration-pg']
 assert len(entries)==1
 e=entries[0]
-assert e['package']=='console-app' and e['test']=='auth_rest'
-expected=['cargo','test','--locked','--manifest-path','backend/Cargo.toml','-p','console-app','--test','auth_rest','--','--test-threads=1']
+assert e['package']=='console-app' and e['test']=='account_migration'
+expected=['cargo','test','--locked','--manifest-path','backend/Cargo.toml','-p','console-app','--test','account_migration','--','--test-threads=1']
 assert e['cargo_argv']==expected, 'unreviewed underlying test command'
-e['cargo_argv'].insert(e['cargo_argv'].index('--'),'account_fence_transport::account_fence_refresh_')
 # Preserve the whole map: partition validation precedes --only selection.
 pathlib.Path(sys.argv[2]).write_text(json.dumps(data,indent=2)+'\n')
 PY
@@ -62,8 +63,8 @@
   # command is passed untouched; the actual Python subprocess test invocation
   # in cargo-test-runner.sh does not resolve this Bash function at all.
   if [[ $# == 7 && "$1" == test && "$2" == --locked && "$3" == --manifest-path && "$4" == "${FENCE_ROOT}/backend/Cargo.toml" && "$5" == --no-run && "$6" == -p && "$7" == console-app ]]; then
-    printf '%s\n' 'matched exact seven-argument prebuild; appended --test auth_rest' >> "${FENCE_BUILD_RECORD}"
-    command cargo "$@" --test auth_rest
+    printf '%s\n' 'matched exact seven-argument prebuild; appended --test account_migration' >> "${FENCE_BUILD_RECORD}"
+    command cargo "$@" --test account_migration
   else
     command cargo "$@"
   fi
@@ -91,7 +92,7 @@
 export CARGO_TARGET_DIR=/private/tmp/console-account30-cargo SQLX_OFFLINE=true
 cd "${FENCE_ROOT}"
 set +e
-bash tools/ci/cargo_needs_postgres.sh --map "${map_path}" --only app-auth-rest-pg --num-threads 1 > "${evidence_dir}/probe.log" 2>&1
+bash tools/ci/cargo_needs_postgres.sh --map "${map_path}" --only app-account-migration-pg --num-threads 1 > "${evidence_dir}/probe.log" 2>&1
 probe_status=$?
 set -e
 printf '%s\n' "${probe_status}" > "${evidence_dir}/probe-exit.txt"
--- account-auth-refresh6-probe.py
+++ account-legacy225-replay6-probe.py
@@ -2,6 +2,6 @@
 import pathlib, subprocess, uuid
 root=pathlib.Path.cwd()
 head=subprocess.check_output(['git','rev-parse','HEAD'],cwd=root,text=True).strip()
-evidence='/private/tmp/account-auth-refresh6-'+uuid.uuid4().hex
+evidence='/private/tmp/account-legacy225-replay6-'+uuid.uuid4().hex
 print(evidence,flush=True)
-raise SystemExit(subprocess.run(['bash','/private/tmp/account-auth-refresh6-driver.sh',str(root),head,evidence],cwd=root).returncode)
+raise SystemExit(subprocess.run(['bash','/private/tmp/account-legacy225-replay6-driver.sh',str(root),head,evidence],cwd=root).returncode)
