GN16 — Group control is not a Company; Account navigation is not a grant
GN16.1 Existing Group owners and incarnation
Keep platform/group consolidated_read's one with_org_conn per Company. Existing helper aborts on first error; extend a typed per-target result at that owner seam without generic callbacks writing business facts. Existing Group discovery uses legacy role grants; cannot stand in for Cedar. Membership SQL remains with existing protected provisioning helpers; extend their rows/receipts with fresh immutable membership_incarnation UUID on each attach/remint, append topology history and group_authority_guards from13. created_at upsert is not incarnation. Current status/grant/attach/remove and imported maintenance aliases acquire Group guards before current affected Account/Company guards. Retain old incarnation/control anchors; stale operation/charge refs cannot revive on re-add.

GN16.2 One durable operation and independent Company effects
group_operations holds immutable selected manifest/digest, original Account, action registration, state PREPARING|ACTIVE|STOPPED|COMPLETE, control/dispatch epochs and current session reference/generation/validity, never bearer token. group_operation_slots stores immutable Company/incarnation/target/action/child/draft-attempt/command bindings and monotonic verified observation, no payroll payload. group_operation_command_receipts gives (Group,command) immutable actor/action/digest/result binding for parent controls; Company effects use existing Company CCF1 receipts. Company child control is workflow-owned and reuses the bound common attempt; it holds parent/slot binding and durable claim generation/status. Generic Action callers resolve parent provenance server-side even if caller omits it.
Create PREPARING header before sealing children, each via own Company tx with parent SHARE. Selected max6 Companies/one registered action family in initial proof. Family admission is a static owner capability registration; initially payroll.calculate_run. This is an execution proof boundary, not a separate calculate-only Group engine. No approval/payment/release implicitly added. Zero selection is no operation; blocked targets are retained for repair or user explicitly selects an exact ready subset. Frozen selection never expands when a Company joins later.
Sealed bound child cannot edit/cancel/execute while PREPARING; stop parent before cleanup. Group accepts preparation only from exact owning receipt. Activation parent EXCLUSIVE confirms every selected slot's immutable preparation and explicit original human intent; no Company tx opened while holding it. Sessions/source permissions may become stale later; child checks current ones. Parent/claim/reference discovery happens before locks; mismatched locked plan causes rollback/replan. Child claims are fenced transport ownership, not business authority. Reclaim advances generation under Company child guard only after owner reconciliation; an old worker's generation fails, and a succeeded command never reexecutes. Unknown effect remains unknown, not expired success/failure.
Dispatch locks Group/Account/Company/work prefix, parent SHARE, child/draft/attempt/command, workflow and domain/governance in total order. Exact active parent/incarnation/claim/session/original actor/payload and both worker mandate + current human permission required for new effect. Company owner commits effect, common attempt/control, child terminal result, mandatory receipt/audit/task/capacity atomically. Group observation happens afterward from authorized owner receipt; missing ACK changes progress only. Resume requires same original Account's explicit fresh session confirmation and context epoch; another human uses governed successor intent, not substituted attribution.
Parent stop uses Group/policy guard then parent EXCLUSIVE, no Company child/task/business locks; admitted SHARE effects finish, later ones cannot start. Cleanup each Company independently after parent stop. COMPLETE comes only from all fixed authoritative child outcomes, with no UNKNOWN treated terminal. Stopped partial work stays stopped with actual outcomes; continuation names exact unresolved work and new explicit manifest. No cross-Company rollback fiction. Per-target reads return allowed success/unavailable/repair, never a hidden target count/name/error. Each Company DTO has its own authorized_at/currentness; this is not a simultaneous all-Company business snapshot. Broader consolidated totals need PA1 derived disclosure, not a sum over whatever succeeded.

GN16.3 Account-owned candidate discovery
Add identity-owned account_context_candidates: immutable source root key -> Account + COMPANY or GROUP selector + current source revision/incarnation/status. It is an owner-only overapproximate directory hint, never permission, roles, salary or display label. Source keys identify already admitted entitlement/capability/engagement/own-record relationship; links do not require current Employment. Group candidates expand current member Company identity through protected Group owner. Candidate membership alone never arms RLS or permits context display. Group control context itself needs explicit discovery; a Company can remain discoverable through an own-record relationship after leaving its old Group.
All creation/revocation/rebinding of candidate-producing sources joins an identity borrowed helper in the same owner transaction and advances account_security.context_generation under Account EXCLUSIVE before Company facts. Pure policy changes need not fan out candidate updates: existing conservative candidate links remain and each current Company policy epoch is checked. Broad Group capability/relationship sources create a Group hint, expanded through current topology; subject-group clauses must have a bounded registered hint source covering every potentially entitled Account. No permission source may activate if it can make a context discoverable without a candidate link or registered Group expansion. Retain ended hints where historical entitlement could apply; final policy decides visibility. Existing company_actors anchors can seed hints only with inspected provenance; anchor existence is not an entitlement or current Group discovery. Unknown migration links block discovery enrollment, not guess homeCompany.
AccountRoot's own settings/login remain usable with zero contexts. Applicants/former workers/partners are not persona partitions: resource owners supply current own-case/record/engagement rights as available. Their future modules are not implemented by this packet. No /switch token endpoint mints stronger rights. Selecting a context simply addresses the same registered action resolver with that target.

GN16.4 Bounded pages and current authorization without Company cursor custody
GET /api/v2/accounts/me/contexts uses identity-owned account_context_cursors (Account,cursor_hash,snapshot/query binding,context_generation,Group topology and Company authority vectors,authorized last key,expiry). Store random handle hash, five-minute expiry, capped per authenticated Account via auth rate limits; no Company GUC/sentinel, no reuse of13's Company cursor. Handle is continuation only; current Account/session required and no raw hint/proof/hidden ID encoded to browser.
First page: capture the entire Account hint set AND account_security.context_generation in ONE identity-owned transaction under Account SHARE before releasing that snapshot. A preliminary unlocked read may plan keys but cannot supply completeness evidence. Independently capture each Group expansion AND its topology epoch/incarnation set in the SAME Group-guarded owner transaction; never pair an older enumerated set with a generation read later. New hints/expansion entries between enumeration and generation capture must force restart, not be accepted as the latest snapshot. Bound candidate graph to32 Groups/256 Companies per navigation resolution (operator-tunable only through reviewed protocol change, not an org/employment quota). Exceeding work budget gives uniform navigation_unavailable with a known-object entry/retry; no truncated all-context result or hidden counts. Pilot1Group6Companies is within the candidate bound, performance unmeasured. Resolve each candidate in its own Group-control or one-Company authorized owner transaction, with current discovery/residual and field projection; no cross-Company business join. Gather those snapshot-bound epoch/source-generation vectors and a conservative decision_stable_until for EVERY candidate discovery decision and Group expansion, including denials. This bound is the earliest possible truth-changing time from any applicable or future-start entitlement, forbid, relationship, assurance or registered temporal predicate, not merely the expiry of displayed rows. Owner policy evaluation must include the full bounded future/current temporal dependency set; loading only currently active allows is insufficient. Unsupported time predicates or an unprovable bound make navigation unavailable rather than inventing infinity. A stable timeless decision may explicitly carry no finite transition. The aggregate minimum applies to the entire permitted-list completeness proof, including hidden-to-visible transitions. Sort/deduplicate authorized context identities by stable type/UUID only, never hidden values. Page default25/max100, lookahead only over independently authorized contexts. Do not return intermediate scan pages, scan lengths or post-filter cursor gaps.
Before DTO release, a short identity-owned validation transaction prelocks all relevant Group guards sorted, Account/session SHARE, then Company authority guards sorted; checks captured candidate generation/topology/authority vector and authoritative current clock against every allow AND deny decision-stability bound and Group expansion bound. Current time at or beyond any transition invalidates the result, even if all currently displayed rows remain readable. It reads only protected control facts, not other Company business rows. This proves separately built projections still share the captured authority facts at release; salary/business versions are not a global snapshot. Changed vector => discard buffered page, retry at most3; then unavailable/stale_cursor preserving known-object navigation. Missing newly created hint detected by context_generation; hidden-to-visible existing hint by Company epoch; Group additions/removals by topology epoch. Any source operand used for context title/discovery must participate in these authority epochs before enrollment.
Continuation verifies Account/query/expiry, complete captured vector and the stored whole-scan decision_stable_until (including denials/future starts); reaching a transition restarts the list even with unchanged epochs, then rebuilds/rechecks bounded owner projections from the source hint set; it cannot serve a stale cached allow result. Drift restarts the list, not silent page mixing. Authorized empty returns [] without hidden counts. Technical failure is not empty: navigation_unavailable; if the entire candidate scan cannot be established, do not present a misleading partial complete list. Known-object lookup remains independently current-authorized. Context labels/links, SSR data/hydration/ARIA/errors and lookahead follow PA1 before serialization; no business mutation or CompanyActor mint on GET.

GN16.5 Exact Group command fence/encoding
Parent control commands use the same restricted canonical JSON encoder as14 with a distinct registered GROUP_CONTROL_V1 envelope and domain prefix console.group-command.ccf1 followed by NUL. group-command-witnesses.json fixes fields and sample bytes. UUID/decimal-string/duplicate/Unicode/size rules are unchanged; Company CCF1/legacy hashes are not rewritten. Group scope cannot enter Company receipts or app.current_org. Immutable manifest stores only exact control refs. Group+command is the replay identity; exact actor/action/target/schema/body/expected/reason remains fingerprint-bound.
For Group control create/activate/resume/stop/observation acquire class6 Group command advisory fence BEFORE sorted parent row locks; key uses first64 SHA256 bits of console.group-command-lock.v1 NUL GroupUUID NUL CommandUUID interpreted signed big-endian, full UUID identity checked in receipt. A Company child effect takes only parent SHARE, never this Group command fence or a Group receipt write. All parent control paths obey command-before-parent. This avoids parent creation absence races and parent->command inversions. Control create allocates parent/slots after fenced authoritative absence, persists allocated target+exact bindings with receipt; retry reconstructs from original target. Historical readback precedes current new-effect state/permission rejection. Parent control never waits for a Company transaction while holding its exclusive parent. Correlation UUID from client is separate from allocated identity.
