UI28 — One current input-to-result crosswalk
Reuse11's16 surfaces,160 state rows, input overlays and journeys; this file and action-bindings.csv replace logical command placeholders where specified. action-types.schema.json defines decoded typed inputs/results, and CCF1 supplies canonical wire normalization. Registered IDs resolve these closed owner dispatch keys; names here are proposed registrations, not currently deployed routes. Navigation/read is not a mutation Action.32 unique registered keys cover33 binding rows (intentional shared own-request/follow-up); static verifier reports actual count.
Transport design: Company action execution uses existing ontology application dispatch through proposed POST /api/v2/companies/{org_id}/actions/{action_type_id}/execute; common drafts use POST /api/v2/companies/{org_id}/drafts/{draft_id}/{save|restore|seal|cancel}, with explicit start action allocating draft, not GET. Group commands use POST /api/v2/groups/{group_id}/operations/{operation_id}/{activate|resume|stop} and POST collection for create. Account contexts use GET /api/v2/accounts/me/contexts. Object/history/receipt reads use GET Company-scoped owner adapters. All are proposed v2 adapters over existing use cases; v1 aliases cannot bypass enrolled owner checks.
No-JS composer GET /_ui/companies/{org_id}/actions/{action_type_id} displays authorized typed input; POST to same path carries CSRF, exact command/expected/draft token and an explicit submit operation. Server calls same owner use case then303 to authorized result or returns422/409 input page with preserved acknowledged values and focusable error summary. GET never saves or prepares. Hydrated autosave and no-JS save share receipts and exact conflict behavior. Use button name/operation to distinguish save/preflight/prepare/seal/execute; preflight never stages. Cookie same-site policy, origin validation, CSRF and Account/session currentness remain mandatory.
Known-object return is an authorized SafeRepair target; stale context cursor returns NAVIGATION_UNAVAILABLE with workspace or current known-object entry, not an empty results claim. Signed/opaque cursors bind Account/current customer/query/authority vector/expiry and cannot carry hidden Company labels; each page rescans current eligibility/temporal boundary. Context switch preserves draft identity at original Company and never retargets it.
Reopen inputs appears in V04 and V07 for prepared/closed/calculated pre-review state. Preview lists authorized invalidated input/close/calculation pointers, preserves immutable prior evidence and requires reason+expected refs. Successful receipt returns STAGED; reopening does not unlock statutory period freeze. REVIEWING uses revise/cancel cycle; APPROVED uses supersession and new STAGED successor. Old input is never edited in place.
Source repair uses V04's common composer with Company/Employment/period and permitted exact refs prefilled. Calendar/assignment/coverage/close/source qualification/eligibility/tax proposal/verification/revocation use existing19 closed payloads and20 fixed owners. Source preparation stores real24 preparation under separate action receipt; edition, locator, original narrative, differences and required qualification appear at decision time. Missing qualification, preparation failure/staleness, unavailable evidence or budget overflow preserves acknowledged inputs and routes accountable source repair; UNKNOWN offers original-command reconciliation only. VERIFY_SOURCE chooses exact source registration, never a generic verified checkbox.
Source rules are objects/typed facts; narrative remains immutable evidence and is brought into preview, review differences and responsible work. It is never silently interpreted as an enum, discarded as a note, or relied on as hidden qualification. Required reading/acknowledgment can bind exact evidence revision, but checkbox does not prove human comprehension or legal adequacy.
Profile/scope selectors reference18 exact registered definitions. Visible people are not payroll population authority. Hidden or unsupported required members cannot be removed to obtain a false complete run. Surface permitted blocker summary and responsible work without disclosing hidden count/identity/amount. Reviewers see mandatory evidence only for their exact Unit/Common obligation; incomplete coverage blocks whole-run Completion, not unauthorized disclosure. Generic review keys inherit underlying payroll natural-person independence; changing route prefix cannot weaken it.
Case publication is monetary NONPAYABLE_REVIEW per10, including authorized historical Employment after departure. Render immutable published amounts through its projection, separate from current source/correction status. Applicant/retiree/external/internal are not hardcoded role classes. Account survives employment departure; current resource/field/action policy decides access. Case submit succeeds with an unassigned exception when no eligible handler; response marks CORRECTED only after exact corrected publication exists and covers every issue item. Requester append/withdraw/follow-up are distinct actions; a closed case is not mutated through handler response.
Transfer uses one composer and exact task set. TEMPORARY and PERMANENT describe responsibility duration; no PARTIAL_SHARE assignment mode. Partial delegation selects some tasks, each with one accountable assignee. Absence/workload/instruction are reason/context objects; candidate ranking uses current rights, scope, relationships, finite capacity/availability and stable fairness order. Preview shows only permitted reasons and candidates, with uncertainty and no-candidate fallback. Revalidate at commit; newly assigned person cannot inherit another actor's sealed command or private draft. Work-custodied editable drafts rotate editing epoch; original authors/receipts persist. Unknown old attempts remain protected/charged until resolved. A temporary return is a new validated assignment transition, never restores stale authority by timer.
Group launch UI shows preparation then active dispatch and independently authorized Company outcomes. Stop cannot undo completed children, and a different actor makes successor intent rather than resumes original sealed command. Loading, empty, denied, partial, stale, conflict, unknown and repair states preserve11 keyboard/focus/error/live-region contracts. Read-only screens show no state-changing affordance and server rejects direct POST too. Restricted labels/links never live in hidden DOM, HTML data attributes or hydration payloads. Mobile/keyboard and no-JS walkthroughs are executable acceptance cases after approval.
Full Console scope remains full Foundry-equivalent capability target; this first vertical does not complete native mail/messenger/calendar or collaborative sheets. Communications stay a dedicated persistent surface beside work navigation; no40-icon sidebar. General object/action/policy/continuity contracts here must support later common tools without payroll-specific roles or storage roots. First-slice boundaries and later commitments are explicit in authority-proposal.txt; proposed evidence does not itself change current PRODUCT.

Editor completeness refinement: SET_INCOMPLETE stores bounded exact editor text under a registered field plus stable parent item IDs; it is not a typed business value or object link and blocks submission when unresolved. CLEAR stores Unset unless explicit nullable business NULL was selected; NULL and missing remain distinct. Nested record/list forms derive from the compiled owner body schema, with exact field-to-schema path binding and stable repeated-item IDs. No second independent validation language or downloaded renderer. A schema path is not an arbitrary JSON/SQL mutation path; only registered fields/items for this exact schema can be addressed. Error focus follows field+parent-item path; stale or absent items return preserved-input conflict rather than retargeting a neighboring row.

Integrated corrections are specified in integration-corrections.txt sections1-7. In particular, internal owner result schemas never serialize directly; PublicOutcome/AuthorizedScreen is the projection boundary. ReviewBasis is scoped and has no global head. CREATE_CONTAINER makes incomplete record/list editors constructible; row errors include parent_item_ids. Native preparation is the existing action's durable BUILDING stage. UNKNOWN accepts Company/Group command IDs without requiring an attempt. Publications retain2,000 atomic selected subjects with paged UI. TransferSubmit reason_revision is nullable with explanation<=1000; exact policy can require rationale, but labels do not select authority or a different workflow. Explanation is versioned/custodied and surfaced to recipient where permitted, not discarded because it is free text.
