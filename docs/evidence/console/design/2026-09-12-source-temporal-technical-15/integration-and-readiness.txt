SI1 — Typed source closure and finite readiness

This packet specifies source-family fencing, historical Employment/wage coverage, governed calendar/attendance completeness, exact-version Docs custody and purpose-specific qualification/assessment/tax evidence. Source schemas are proposals; actual DDL/ACL, ports and tests remain behind integrated design/test approval. It reuses existing owners and pure pairing/calculation functions, extending the source and owner contracts where current code is insufficient.

wire-types.json gives closed nested reference/scalar/locator/selection definitions and complete source-sensitive fields for10NP1 commands. It refines12: EmploymentCoverageRef contains several revisions of one Employment; CREATE_RUN uses an explicit allocated create target in the common envelope; PREPARE_INPUT accepts only versioned scope/profile and optional expected source-generation witnesses, not a caller-selected population or arbitrary source_refs list. Source references are server resolved and frozen from exact predicates. Missing supplied expectations do not skip re-resolution. Pinned old snapshot reads and new execution compatibility remain separate.

Common CCF1 from14 applies: typed normalized numbers are decimal strings; unknown/duplicate fields/NUL/float/sub-microsecond inputs rejected. New codecs bind exact action/type/input schema and immutable owner refs. np1-command-vectors.json has one synthetic normalized envelope per10commands with literal canonical bytes/fingerprints; it proves representation consistency only, not source authenticity, domain eligibility or a passing Rust/DB decoder. Tax examples are NONPRODUCTION_FIXTURE data, never legal rates/source conclusions. Two positive arithmetic examples remain12's evidence; this packet adds temporal/custody/selection oracles instead of rerunning arithmetic under new labels.

Source supply is a real prerequisite, separate from code. A first production source edition/normalizer needs qualified provenance/equivalence proof; complete calendars and insurance/tax applicability sources must exist. Missing them gives named blocked work, not invented zero. Synthetic harness composition supplies exact source artifacts/clock/qualified verifier fixtures only in disposable test deployment and then exercises normal propose/verify/admit/prepare actions. No production request/role/tenant switch enables fixtures. No actual credentials or employee PII used here.

Before next implementation stage: unify immutable source row keys, supported ranges, writer enrollment and CCF1 decoders with14. W04b-04 still needs exact workflow/capacity/Group/PC1/AC1/PR1 participating SQL/API and Account context discovery from13. W04b-05 supplies one compatible migration/ACL/retention/adoption/harness/resource/workload candidate, including self-clock contention, artifact provider version-readback and restore proof. Scoped acceptance here does not close these dependencies.

W05 requires current-authority scope alignment plus final4independent integrated design rounds/consensus/explicit design SHA approval. W06 then writes executable tests, obtains independent review/explicit test SHA approval, and admits implementation only on an actual red named probe. First vertical readiness, pilot production and the full Console/Foundry/common/HR suite remain separate. No product code/tests/migration/deploy/remote or legal authority changed by15.

EA1.6 closes exact zero/one/many selection and atomic explicit successor activation; EA1.7 closes the professional credential owner and nonrecursive issuer root contract. Concrete issuer trust bootstrap evidence/ACL remains source-supply/W04b-05 HOLD. Wire definitions count22, non-executable proof candidates47. No real qualified issuer/source or applied schema is supplied.
