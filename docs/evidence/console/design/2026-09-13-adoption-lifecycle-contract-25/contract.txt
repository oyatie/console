AL25 — Preserve outcome lineage without retaining every byte forever
1. Chosen ownership and exact scope
Reuse17 retained_units/domain_adoptions/content_pins and24 submitted byte holder. Add one ont_source_submission_bindings subtype, not source-specific fields on every retained unit. This is a proposal over unimplemented17/14 prerequisites, not an applied schema or whole F02/F03 closure. Lifecycle protection is still owned by the common custody and registered domain owners; source family/reason/person/title never selects another retention engine.
Binding stores bounded24 canonical preparation metadata and its immutable digest/input identity, preparation's OWN existing command receipt, later one-time seal tuple and later one-time snapshot digest/size. preparation_command_id is not the future domain command; its actual registered action/actor/unit correlation must be verified by owning enrollment/finalizer. The retained unit identity_digest remains the preseal preparation digest. Seal and publication never rewrite it, preserving24 hash DAG. Foreign keys bind the same Company. No Account/Employment deletion cascade.

2. Three facts, not one overloaded lifecycle state
Preparation exists: domain command/attempt/originalAccount/fingerprint all NULL; publication digest/size both NULL. Seal binds: all four command fields become nonnull together while publication fields remain NULL. Publication identity records: same sealed tuple, snapshot digest/size become nonnull together. Binding guard refuses prebound INSERT, preparation changes, partial tuples, skipping seal, resetting/cycling or retargeting any bound identity. Same no-change retries reconcile existing receipts; they do not blindly UPDATE the binding. Cancelled/rejected attempts retain their immutable tuple; bound does not mean succeeded or still authorized.
Generic retained unit payload availability, active protections, disposal-job outcome, backup status and current business eligibility remain orthogonal.25 does not add a published/deleted-all lifecycle enum. Account identity persists independently. Preparation metadata and minimal binding identities are policy-governed personal metadata, not an assertion of unlimited lawful retention; any eventual metadata retirement needs its own reviewed horizon/foreign-key/recovery contract. This packet permits no deletion.
Prestage creates actual unit and binding through the registered common preparation action before domain seal, with exact acknowledged draft/control/schema/custody. Seal must validate preparation receipt/provenance and complete compatible prepared state under14/20 guards; mere row/receipt existence in an unfinished same transaction is insufficient. Full fixed owner action dispatch and prep/seal provenance SQL bodies remain required. No data/schema/actor/target hash is permission.

3. Published bytes reference identity, never the inverse forever
24 byte row gains an exact FK to binding(unit,attempt,command,Account,command fingerprint,input digest), plus binding's publication(unit,command,snapshot digest). Neither a unit-only pointer nor matching bytes from another command can pass. Binding is allowed to exist without bytes before publication and after future lawful disposal; byte rows cannot retarget a different seal.
At the first publication update, a deferred read-only SECURITY DEFINER validator loads exactly one24byte row by PK and verifies tuple, digest and framed size. It also performs one indexed receipt lookup requiring SOURCE_EFFECTS21.22 then requires a nonempty complete effect set; mandatory effect/adoption references and UNIQUE(binding org,command) tie that set to this published binding. A legacy-shaped receipt cannot make an effect-free published binding pass. This does not replace exact action/actor/attempt/SUCCEEDED/current-protection correlation, which remains the owning integration prerequisite. SECURITY DEFINER restores confined writer visibility after outer entrypoint returns to LOGIN; fixed pg_catalog search_path and row_security=on/current Company checks follow22. Total frame overhead43bytes derives from24 prefix+3 section headers; accepted frame46..147499. The byte row's own24hash CHECKs verify frame/components. This validator establishes live exact bytes at that check, not valid JSON/custody semantics/current authorization or evidence protection. Complete canonical decoder/schema/preparation/metadata/command correlation still required from the fixed owning participant.
The trigger runs once for the publication transition, not once per effect/adoption and not forever on reads or disposal. Permanent effects/adoptions reference retained binding/unit identities, never24payload rows. Thus future separately authorized disposal can remove bytes without lying about the original result->submission link. Current read path must report authorized content unavailable when no live admitted payload remains; it cannot reconstruct from private drafts, silently recompute inputs, rerun old Actions or expose raw identifiers to explain absence.
Early SET CONSTRAINTS only validates current structure/bytes. It does not stop later pin release or payload-state mutation. Today24immutable trigger and runtime ACLs prevent byte-row deletion/update;25 leaves them intact. Before any disposal path is admitted, every relevant unit/pin/adoption/hold/quarantine/location mutation must enforce current obligations and active-publication/attempt fences under the same unit guard, plus final owner validation before commit. No guard may treat an already-fired deferred trigger as ongoing protection. No source enrollment may claim this full lifecycle property from25's bytes check alone.

4. Every effect requires the submitted-intent relationship
22 effect rows gain exact submission_unit_id/snapshot_digest and generated submitted_intent_slot=1. These are durable relationship projections, not added input fields. Keep22 SOURCE_EFFECTS21 hash bytes/order/count unchanged: its native effect identity remains stable; new submission relationship is uniquely tied to that same command through immutable binding and CCF1 input/preparation. Adding multiple possible bindings for one command would break this argument and is prohibited by UNIQUE(org,command).
Extend17 common domain_adoptions with source_command_id/source_effect_ordinal/source_snapshot_digest. Their all-null state is exclusively for other existing owner variants; a source row requires all active fields, snapshot unit and exact governed obligation_ref. Generated source_submission_slot=1 means the protocol's mandatory SUBMITTED_INTENT relationship. It is NOT a legal retention policy, duration, user role or substitute for17 purpose/obligation objects. The owner must correlate the canonical owner_result_ref, purpose_ref/obligation_ref and receipt_ref with these typed source projections; those common reference schemas/guard bodies are still required, not supplied by checking obligation_ref IS NOT NULL.
Effect->adoption FK demands that exact submitted-intent slot, Company command ordinal, same unit and snapshot digest. Adoption->effect matches the same tuple; adoption->binding matches the same command's published identity. Zero adoption, another result's adoption, all-null other variant, wrong-purpose substitute or another command's snapshot cannot satisfy it. Existing22PK/23source reciprocal identity establishes the native family/root/revision/kind behind command+ordinal. No second mutable adoption-ID pointer on effects and no per-family adoption table.
Each source effect has one immutable mandatory adoption, even when1024 effects share one snapshot. Additional independently required purposes/obligations use their own existing governed relations and protection records; they cannot replace mandatory slot1. Source adoption rows cannot update/delete/convert to or from another variant through normal writers. Actual policy changes/new holds affect current obligation/protection truth, not rewriting historical adoption. Future metadata retirement remains separate qualified lifecycle work.
The source variant's new nonnull projections and immutable existing22effects are an enrollment barrier for prior proposed rows: F05 must stage/version/validate any existing data before activation, not fabricate adoption/provenance.25 gives no migration number, executable backfill, NOT VALID exemption or runtime permission to bypass guard.

5. Live protections and shared snapshots
Historical adoption is not a forever-active pin, and inactive-looking policy does not itself release a pin. At publication the shared custody participant must establish actual independent domain pins or adopted copies for snapshot AND every required source/artifact/decision/credential/AI1 evidence item, with exact version/digest/size and governed policy/obligation. A list of refs, passed=true result, hash, private-only pin or committed submission identity is not adequate. New state-changing use rechecks current source/policy even when historical bytes remain readable.
At later disposal, enumerate ALL currently applicable obligations/pins/holds for the unit under its guard. One result's expiry cannot release a snapshot used by other results or another purpose; unknown/incomplete inventory refuses disposal. New pin/adoption after quarantine refuses rather than resurrecting dependency. Adding hold or changing policy must invalidate or pause affected in-flight disposal as17 specifies. Unknown attempt/provider effects stay protected; timeout never proves absence. A publication that races quarantine either wins protection under the guard or refuses; no check outside that guard counts.
Deduplicate evidence by exact retained unit/version before locking or reading bytes, preserving every result/purpose obligation link.22effect cap1024 is NOT a bound on total evidence refs/bytes/locks. Complete per-action decoder-derived inventory and budgets remain F01/F03/F08 and must be specified before enrollment. Do not multiply shared source package byte reads by1024 or inline40MiB packages into24snapshot. A known finite complete plan is required; over-limit/unknown dependency blocks before effect, preserving acknowledged input and the owning repair path.

6. Owning transaction and lifecycle transitions
Preparation: existing registered common owner creates real17unit + immutable preparation binding + own receipt atomically. No source effect. Seal: existing14owner locks current control/prepared unit, creates attempt/reservation, one-time updates binding, commits exact seal receipt; new attempt needs new unit. Publication: use20receipt/provenance-first resolution and22provisional header-first final owner transaction; with all predeclared guards/rows locked validate current sources/actor/gates/bytes; write24snapshot and registered location; set binding publication digest/size; write source/effects + mandatory adoptions + independent protections; finalize exact gate/attempt/work through their owners; verify all deferred constraints and final live/protection/current-time checks; COMMIT once. No network/new pool/late unit allocation.
A bound unit with cancelled/rejected/no-effect attempt or abandoned preparation is not a source result. Its eligible cleanup follows the existing17policy/job machinery after proving terminal/no unknown effects; preserve required reservation/replay/preparation receipt metadata. No fake source effect/adoption is invented to make orphan cleanup fit. Successful publication's later private cleanup cannot delete the domain snapshot. Later domain snapshot cleanup separately requires every applicable protection to permit it and17independent journal/quarantine/provider/readback protocol;25provides no deletion function.
Historical replay returns original currently disclosed receipt without demanding live source/snapshot bytes or creating new units. Lost COMMIT acknowledgement remains UNKNOWN until original command reconciles. Restoration validates both durable graph and independently current authority/protection/journal state before service, including payload deliberately removed since backup. An FK-valid restore is not current admission authority. Company is legal RLS cell; active customer Group tenant isolation still applies independently of intra-tenant cross-Company Cedar grants.

7. Limits and next closure
Concrete25SQL includes binding transition guard, source-adoption immutability guard, once-at-publication byte validator and reciprocal identity keys. No complete retention policy/protection enum/body or arbitrary SQL selector is invented. Full registered unit/obligation/purpose/schema/metadata decoder, preparation/seal/action/Account/receipt provenance and default/inherited role/catalog closure remain prerequisite; conceptual existing17tables are not claimed deployed. Fixed SQL/Rust participant signatures and all live/protection lifecycle mutation bodies must still be completed together. The next finite output is a closed per-action required-evidence/pin inventory and current-state transition contract on those owners, followed by remainingF05-F08; no new independent engine or revised global checklist.
Actual SQL parse/typecheck/catalog/concurrency/load, full end-to-end product tests and Verus proofs were not run. Static artifacts and scoped review are design evidence only. Whole W04/F01-F08,4final integrated design reviews/exact owner approval, approved executable tests and actual RED lane admission remain required.
