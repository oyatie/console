APM1 — Activation order, verification candidates and remaining boundaries

1. Preserve existing protection intent
Credential deletion on legacy deactivation prevents old sessions from surviving a deliberate identity shutdown. Replace that coupling with explicit Account security suspension + current work-relation revocation; do not merely stop revocation without adding the new checks. Legacy deactivated users have mixed/unknown reasons. Backfill Account root without granting new access: preserve every credential deletion/revocation, map source-proven security suspension to SECURITY_SUSPENDED, ambiguous denial to RECOVERY_REQUIRED pending independent Account recovery, and scoped work inactivity to its Company relationship. Active users with retained valid credentials can migrate to ACTIVE with a generation baseline; previously deleted passkeys are unrecoverable bytes, not restored from history. Do not infer independent humans or add permission during this migration.

2. Ordered expand, closure and enable
a Source/custody inventory: exact product/schema SHA; all auth/actor FK constraints, CHECKs/RLS/default privileges/functions/roles/triggers; current credential IDs/key handles/hashes/revocation counts; Account-security state mapping evidence. Source-confirmed minimums are schema-candidate and writer-enrollment, not a live catalog census.
b Add Account/security/CompanyActor and guard rows, Account terms/security audit, typed immutable policy revisions. Preserve IDs, timestamps and receipt bytes. Batch backfill with stable IDs and resumable checkpoints; do not run destructive shared-tree Git or database operations as design work. Before creating a subject's authentication cutover fence, pause its legacy admission and drain or terminate its accepted legacy requests, streams and refresh writers; unresolved effects must reconcile before resuming. All serving versions must recognize the permanent fence. An optimistic lookup miss cannot let an in-flight old request complete after cutover. Compatible maintenance/admission controls and their migration proof are required, not a promise of zero-downtime backfill. Company/group guard initialization is atomic with new Company/Group provisioning before any enrollment.
c Migrate BOTH global and composite auth FKs, mandatory org CHECKs, Company RLS, preauth resolvers, device handoff and consent/audit reads. New restricted auth DB role has no Company business privilege, and console_rt has no credential access/role escalation. Runtime catalogs must prove default grants do not silently re-add revoked DML. Existing auth keys/counters/refresh rotation/revocation and ceremony proof semantics remain byte-compatible or have a separately reviewed explicit conversion.
d Migrate/disable all employer reset/OTP/deactivate/delete paths and direct/catalog Company purge effects on Accounts/credentials. Keep hard Company purge blocked while legacy Company-provenance FKs or retained records still require it. Upgrade duplicate self-key handlers to one Account guard protocol; do not rely only on a frontend removed button. Outstanding employer login tokens cannot survive as new Account invitations.
e Deploy Account session resolver and owner-bound Company/Group resource contexts; legacy parsers reject ACCOUNT_V1. Before activating any Account, install AS1.3's global legacy-token refusal on every successful authentication path: existence of account_security permanently fences legacy access/refresh, regardless of security_state or Company policy enrollment. Suspension/recovery/compatible rollback never re-enables those tokens; fresh primary authentication is required. Complete reached receipt/audit/actor display/FK closure before enabling that domain action. Native HR/recruiting/own publication still need their exact owner use cases; Account-only sign-in alone is not applicant workflow completion.
f Publish verified current policy+field schema and complete correlated assignments with missing-row-safe Company guards. Existing role labels are not blindly translated into new payroll action permits. Explicit mapping/proof defines legacy compatibility; no default SUPER_ADMIN bypass. Every path reaching enrolled protected data joins projection/enforcement or refuses. Verify policy attach/source hierarchy/assignment/field changes bump authority epoch before facts in the same transaction. Then enable only the exact tested Company/object/operation surfaces.
Rollback after first Account-only data remains Account-capable: pause new business effects/assignment/policy publication, retain sign-in/self recovery, corrected policy readers, current revocations/epochs and historical receipts. Never install a binary that requires users.org_id/roles for every login or use Company purge to remove new Accounts. Policy content rollback appends a current revision; generation counters never decrease. Restore uses candidate06 current-policy/readmission requirements and preserves unresolved effect/retention pins; no actual restore drill is claimed.

3. Concrete proof candidates (non-executable now)
proof-cases.csv enumerates Account authentication without Company/roles, ongoing account through exit, unrelated Company access preservation, employer takeover/purge refusal, key-delete race, refresh replay, separate human identity, correlated capability combinations, allow+evaluation-error denial, policy mutation races, missing guard, field/query/SSR/history leakage, cursor freshness and raced receipt disclosure. Each case requires restricted runtime invocation and durable owner readback, not mocked principal booleans. Tests for applicant/certificate actions are accepted behavior dependencies and need their actual owner implementation; do not create test-only identity shortcuts to call them complete.
Reuse platform/auth tests for JWT/WebAuthn/refresh; platform/auth-rest session/Group context tests; identity/rest and adapter policy/security tests; platform/authz engine/mode/residual tests; authz-rest Cedar runtime-role tests; ontology object-policy attach and residual-runtime tests. Use synthetic second Group plus6Company/2,000person pilot, no PII or real credentials. Existing SUPER_ADMIN/Company fixtures are preserved regression coverage and insufficient for new roleless Account positives.
Before adding tests, approved integrated design SHA is still required. Before implementation, independently reviewed approved test SHA and valid RED probe are still required. This packet runs only evidence identity/reference validation. Exact DDL/migration rehearsal, DB roles/pools, CPU/memory/lock-time budgets, auth cookie/CSRF transport compatibility and actual discovered/executed product counts remain required W04/W06 work. No product tests, browser/a11y checks, network identity enrollment or production credential changes performed here.

4. Readiness accounting
Candidate12 established the native-payroll owning transaction approach and arithmetic oracle. Candidate13 specifies Account/session/credential ownership, proposed schema keys/constraints, correlated capability storage/evaluation, current guard and projection/query requirements. It does not close all W04b-01 physical proof: live FK/writer closure and exact migration SQL/harness still need the unified candidate. Next technical priority is W04b-02 shared object/action/schema/draft/attempt/receipt registration and its exact owner ports, followed by W04b-03 source/evidence/temporal input closure and W04b-04 workflow/capacity/Group/review/publication SQL/ports. Account-only context discovery/navigation/continuation is specifically still required in W04b-04: candidate13 withdrew its under-specified API rather than assign Company cursor custody to a Company-independent Account. Server-owned candidate discovery, additions/revocations, current disclosure and bounded continuation need their actual owner and proof before integrated approval; no truncation or fake Company fallback. W04b-05 assembles one compatible migration/test environment and measured workload plan. These are concrete dependency closures, not permission to repeat prose indefinitely or claim18proposed tables are18finished features.
W05 authority alignment and final4independent design rounds/consensus/explicit design SHA approval, then W06 executable tests/review/explicit test SHA approval remain. Pilot operation and complete Console/Foundry/HR/shared-module coverage are separate; broad product requirements remain recorded rather than shrunk to payroll.
