LO20 — Ordered source composition, exact enforcement responsibility
Retain12/14/15/16 total-order classes1..8 and10.20 refines only the source-publication part of class9 and defines where retained-unit locks participate; callers reaching these records must converge before enrollment. Mode is predeclared SHARE/EXCLUSIVE; no upgrade and no discovering an earlier key after taking a later lock. Sort advisory i64 keys by signed numeric value; deduplicate hash collisions, retaining full resource identity for every check. Row keys use Company, registered family/table key, raw UUID/revision order. These are not authorization tokens.
9a existing canonical period keys via lock_period_lock_key, including all periods touched by a cross-month source mutation (max3 for this monthly/24h profile), in sorted advisory-key order. Calendar multi-period publication instead selects all affected period keys in its finite declared range before family locks, bounded14months for a contiguous range of<=366dates with one-day lookback and at most one-day forward spill; compute the exact union, including boundary months; no silent3-period truncation. Period openness lookup alone does not serialize writers.
9b preinitialized source_family_guards sorted (org_id,family). Pure current reads use SHARE; changed source families use EXCLUSIVE and checked generation+1 in same commit. Pin mutation alone does not change source semantic generation; current artifact state/disposition does. Missing guard, generation exhaustion or unsupported family denies enrollment/use. Any changed fact that is a Cedar operand takes earlier Company-authority EXCLUSIVE too. The exact dependency union must be declared before acquiring these locks; an unexpected new family causes rollback/replan.
9c fixed domain advisory keys, ordered by registry subrank: attendance month/close key, leave charge/request serialization key, employee/day eligibility keys (sorted), other registered source-root creation keys. Existing leave request-row-before-day ordering and attendance month-before-period ordering are not admitted unchanged. Trigger-acquired employee/day locks from0189 must be reentrant acquisitions of the same pre-acquired keys; a trigger must not discover a new earlier key. Exact legacy key derivations are reused, not replaced by a parallel mutex. Register every reached trigger/alias before claiming closure.
9d sorted retained-unit and current hold/adoption eligibility locks. SHARE only for read-only use with no new pin; EXCLUSIVE for creating/releasing/adopting a pin or quarantine. Distinguish dependency units from a private draft payload identity. A source consumer rechecks exact unit/copy/state/hold eligibility; it cannot revive a quarantined unit by pinning it. Units must already exist: stage Docs objects/codes/copies before final publication, so no late evidence-code counter allocation inside9d.
9e fixed native/common source heads and rows, including run, source roots, current revisions, existing leave employee ledger and request rows, exact Docs attestation/current-state rows, and current projections. All multi-root keys sorted, source successor claims included. Since registered private helpers are read/mutation participants rather than new executors, they may only reuse the declared set. Guard SHARE holds concurrent fact writers until transaction outcome; heads still use exact CAS and composite identity.
10 exact governance bindings/decision rows, then only already-owned mutations/finalization. No app callback may acquire a new9a-9e resource after gate consumption. Group helper involvement stays narrow control metadata, never mixed-Company payroll or Group RLS.

Finite plans are server-derived, not client lists. Source19 qualified dependency bound256nodes remains for qualification/credential/profile/artifact closure; it is not a maximum2000-person population. Native population manifests stream per unit under18. Enrollment must specify deduplicated full command lock/byte bound independently; this packet does not conflate256source-trust nodes with all event/day or population rows. Source publication limits and codecs already constrain a single source payload; batch/native maximum lock footprint and measured latency remain F02/F08 integration outputs.
Semantic source changes increment the declared family once per transaction, irrespective of changed row count. Epoch mismatch alone triggers exact current re-resolution; SAME_BASIS does not force manual edits. Predicate completeness includes hidden competitors and negative results. Current correction/head and source/trust revocation validity are checked beyond immutable FK existence.

Database backstop plan (not applied DDL): guard-and-owner-schema.sql.txt gives two concrete control tables/constraints/ACL templates. Native18/source15 rows and common13/14/16 records stay their proposed/actual owning stores. constraint-owner-matrix.csv maps invariants to SQL vs trusted Rust and names still-missing function bodies/constraint migrations. No CHECK(jsonb_typeof='object'), row hash, native FK or definer function name substitutes for a complete owning command. Protected source-owned common instance payloads/head/lifecycle/link mutations require both the immutable binding and confined writer boundary. Runtime generic rows remain compatible only where no protected owner binding/control/enrollment applies; caller protocol=LEGACY cannot bypass a protected target.
