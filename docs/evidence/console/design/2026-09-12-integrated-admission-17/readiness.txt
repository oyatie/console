IR17 — What is settled and what still blocks implementation
This packet closes consolidation/admission decisions, not all W04b-05 physical schema work.71 conceptual records are inventoried, repeated extensions and key aliases resolved, named constraint mechanisms assigned, common retention/adoption/recovery protocol specified, and a finite deployment-role/pool/dataset/measurement contract proposed. Scoped acceptance is evidence only.
Before final integrated design review, complete F01-F08 outputs in closure-register.json: exact nested DTO/schema constraints, SQL/index/FK/trigger/function signatures and source-copy/authority/recovery deployment contracts. These are real missing artifacts, not external permissions and not tasks to leave for implementers to guess. F01/F02 may be prepared together; one orchestrator writer consolidates shared schema/ACL. Do not reserve a migration number or create executable product tests before the approved design sequence. W04 remains false until the outputs are mutually consistent and independently reviewed.
Then W05: reconcile proposed new scope with current PRODUCT/ROADMAP/DELIVERY in an explicit current-authority candidate; four final independent integrated design rounds, consensus and exact design SHA approval. Existing scoped03-17 reviews do not count as those rounds or authorize old HOLDs. W06: executable tests against approved design, independent review and exact test SHA approval; only then implementation starts from approved test commit via fanout.py on a named executable probe red on the clean tree. Missing/green/nonexecutable probe is stop. Actual implemented migration/ACL/restore/load proof occurs after these approvals; none is a prerequisite that requires unauthorized code first.
Pilot production is later and separate: current issuer/legal/tax/calendar/identity/retention/operational inputs, actual infrastructure and independent custody, demonstrated recovery/performance, release/exposure authority. Full Console/Foundry/common suites and all HR employment cases remain the product direction, not claimed completed by a first payroll vertical. Don't reduce the common Action/object system to this proof slice, and don't advertise unsupported domain semantics as available.
