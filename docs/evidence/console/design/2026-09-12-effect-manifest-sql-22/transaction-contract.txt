TX22 — Concrete structural SQL scope and refinement of21 ordering
This file changes PROPOSED21 participant statement order, not existing production behavior or previous evidence. Under20's existing owner transaction and all prior fences/current checks, compute the complete prepared source-result manifest. INSERT the sole receipt provisionally FIRST, then insert its source members and source/adoption/gate/attempt/work records, validate deferred constraints and final current-time/authority, and COMMIT once. All referents may be mutually deferred where their owning contracts require it. The MEMBER->RECEIPT FK is now immediate, because the header exists. Source/member/claim reciprocal constraints remain future deferred integration. This replaces21's receipt-after-effects ordering for these enrolled source commands only; native18 batches retain their owning protocol.
The receipt is not externally visible until commit; no separate committed pending/running receipt state is introduced. A returned SQL result is provisional. The same transaction must not re-enter20's historical replay path after creating this header. On rollback it never happened; on lost acknowledgement reconcile the SAME command in a new transaction. No notification, network send, external success or work successor runs from a provisional receipt. Same-transaction existence is not proof of a committed business outcome.

One bounded check and no additional token registry
Current0177 receipt immutability is preserved. Member INSERT uses the effective INVOKER role and exact Company context, indexed receipt lookup, same-top-level-transaction xmin check, exact source protocol/count and ordinal<count. PK(org,command,ordinal) and immutable members allow each slot only once. The single receipt deferred trigger validates full count, contiguous ordinal set, canonical identity order and binary digest. It uses bounded ordered aggregation, not one full scan per member or repeated growing bytea concatenation. Expected work is one indexed lookup per insert and one bounded result scan/sort/aggregation per receipt; no runtime latency or allocator-complexity measurement is claimed.
After a successful full check, all0..count-1 slots are occupied. Count and members cannot change. No additional member can be inserted, even after SET CONSTRAINTS has executed the deferred check early, because every permitted ordinal collides and all other ordinals are refused. This saturation argument is the durable invariant. It does NOT prove source/claim rows cannot be appended outside membership; exact reciprocal source/member/claim FKs and immutable owner guards are separately mandatory admission dependencies.
Existing0165/0166 use xmin=pg_current_xact_id()::xid as transaction-local evidence. Reuse that pattern without treating 32-bit xmin as a durable identity, tenancy token or proof across freeze/wraparound/restore. Saturation and immutable complete membership remain independently necessary. PostgreSQL's normal transaction/MVCC behavior and trusted database administration are assumptions. Physical/logical restore and replication paths must validate every completed manifest and full source/adoption graph before admission; a restored marker never grants authority.
No caller SAVEPOINT or PL/pgSQL EXCEPTION subtransaction may surround receipt creation. pg_current_xact_id gives the top-level ID; a subtransaction-born header may fail the xmin check even after RELEASE. This is conservative refusal, not an alternative success path. Inspect the complete Rust/SQL call chain; a source helper may not catch partial mutation and continue. Successful no-effect validation remains before the SQL mutation phase.
If the named deferred trigger is already IMMEDIATE at header insertion, the empty member set fails and rolls back. This candidate chooses fail-closed behavior, not automatic mutation of arbitrary caller constraint modes. The admitted owner starts with the named constraint initially deferred. Setting it immediate after all members exist validates normally; no mode change may skip terminal source/adoption/gate checks. Member FK itself is NOT DEFERRABLE.

Privileges and closed limits
INSERT guards are SECURITY INVOKER: current_user must identify20's confined outer writer, not a role set by the guard itself. The deferred read-only validator is SECURITY DEFINER with fixed search_path=pg_catalog,row_security=on and explicit function owner, because trigger execution may occur after the outer finalizer returned to the LOGIN context. No LOGIN gets function EXECUTE or new table DML in this packet. Mandatory deployment verifies role flags, membership/SET ROLE/inheritance, schema ownership/CREATE, overload ACLs and same-creation-tx default revocation; NOLOGIN alone is insufficient.
This SQL validates marked SOURCE_EFFECTS21 structural headers only. All-null fields preserve legacy storage compatibility. The SQL cannot infer an erased/missing action identity from that null triple. Before admission, exact registered action/type/Account/CCF1/attempt provenance must REQUIRE SOURCE_EFFECTS21 and refuse legacy-shaped source receipts through every raw/direct/alias path. Actual0219 supplies action_key/object_type_id, not action_type_id; actual0223 owner/target roster does not include the new source owners. Those are explicit F02/F04 integration dependencies, not silently fabricated existing columns or an already closed downgrade defense. No source may be enabled by installing this SQL fragment alone.
Family codes1..7 and claim semantics retain21. Conditional source/member keys, exact creating-command/digest correlations, old predecessor versus new successor attribution, independently retained domain submission and per-result/purpose adoption remain mandatory. Frame equality establishes byte membership only, not current qualified source meaning, cross-customer authorization or monetary correctness. All IDs/counts/digests remain internal until current owner disclosure, including logs and error handling; Only explicit RAISE messages here contain fixed categories without row payload. Native PostgreSQL CHECK/NOT NULL/UNIQUE/FK/cast diagnostics can contain failing rows, identifiers and digests. Treat raw database diagnostics (including DETAIL/HINT/internal query/CONTEXT and driver Display/Debug) as sensitive. Before enrollment, the owning adapter must map failures to approved typed public errors and every response/log/trace/telemetry path must use sanitized projections; never forward raw DbError. Database-side diagnostic logging is also an F04 sink requiring a separately verified configuration. Error occurrence/status/count can still disclose information and remains in the relational information-flow model. This packet provides neither the sanitizer implementation nor its execution proof; F04 remains OPEN.
Structural storage bounds: count1..1024, member115bytes, accepted commitment frame<=117821bytes. The defensive LIMIT1025 permits detection of malformed imported data, with bounded117936 framed bytes before rejection. SQL/table row storage and sort memory exceed frame bytes and need F08 measurements. Limits are invariant bounds, not a memory/SLA promise.
