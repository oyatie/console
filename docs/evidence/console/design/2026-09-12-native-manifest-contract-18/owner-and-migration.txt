NO18 — Command ownership, checks and compatibility
This SQL text fixes native field/key/null shapes and exact referenced target columns, not the entire executable F02 schema. Future13 company_actors, future15 source keys and registered common profile/scope contents remain named dependencies. RLS/ACL/deferred trigger definitions, binary catalogs and real-role tests still need unified F02/F04/F05 closure before enrollment. Jsonb CHECKs here are local container checks only. Closed parsing, source semantics and digest equality are N18-INPUT/N18-BATCH responsibilities, not alleged proof from jsonb_typeof.

N18-WAGE: payroll owner, complete CORRECT_WAGE transaction. After inherited authenticated disclosure/replay/command fences, take WAGE EXCLUSIVE and sorted base/head locks. Base belongs to same employee/Company and cannot change. Absent head means0. Expected predecessor must equal head; checked next=predecessor+1. Parse exact WageCorrectionBody and top-level NonBlankReason (at least one non-Unicode-White_Space scalar; preserve original text), same-Company artifact refs/current disclosure/custody, source_note<=500 scalars. Insert correction, CAS head or first insert, increment family generation, append command/audit/work/gate outcome together. No head0/backfill correction invented. Reuse same resolver for native preparation and insurance-only payslip draft; legacy numeric DTO conversion stays explicit. Correction payload digest is H(console.payroll.native18.wage-correction,{org_id,employee_id,body,reason,revision,predecessor_revision,actor_account_id,command_id,recorded_at_us}); body carries exact immutable values/evidence refs, base/effective date remains owner base fact. Reference validation does not allow changing base employee/effective_from.

N18-LINE: every insert/update/delete route, including privileged purge, validates actual parent run protocol under the same run fence before choosing legacy/native behavior. Client cannot pass protocol=LEGACY to override an NP1 parent. For native rows require native_protocol=NP1, same Employee/Company, canonical source key and no import refs. Protect (org,run,id,employee,native_protocol,source_key) against change. Existing roster materialization UPDATE is refused for NP1; native prepare creates/reuses stable identity instead. Preserve existing legacy nullable/source-key semantics; dirty historical duplicate employees block native migration, not deleted/relabelled rows. Current native projections/readers never count excluded historical lines. Row identity immutability is distinct from permitted projection refresh.

N18-INPUT: payroll owner complete PREPARE transaction. Acquire existing14-16 order before run/source reads; no lock upgrade. Current expected run/head state and selected scope/profile must match; full population resolved independently of caller visibility. All selected source references go through owning source read/validation and retained dependency pins. Validate NM-V01..12, real dates/numeric bounds, exact selected/candidate cardinality and ordinal order, current authority/expiry/absence re-resolution. For each unit stream <=64KiB canonical leaf, persist typed columns and source rowset; BLOCKED never receives arithmetic. Header period/pay_date/zone/profile/scope must equal current run intent and its registered profile. Write complete input and immutable refs, pins, run head advance and single command/gate/work/audit outcome together. Deferred native18_input_complete trigger on header AND unit/ref inserts targets the affected header, verifies member identities/counts, subtype/cardinality and stored commitments exactly; it cannot trust caller count/digest. Source semantic qualification was checked before insert by confined owner, not re-invented as SQL legal rules. Revalidation at commit shares held guards. Append-only guards reject late insertion into a committed header, not only UPDATE/DELETE: N18-SEALED below.

N18-RUN: reusable owning lifecycle branch. Before native enrollment, replace existing status/enabled CHECKs with exact disjunction: LEGACY preserves all existing0186 clauses; NP1 only existing12/16 designed states and owner transitions (STAGED,ATTENDANCE_CLOSED,CALCULATED,REVIEWING,APPROVED plus explicit reviewed cancellation/successor states as defined there). This packet does not close the complete PC1 FSM/SQL matrix. Native operational close refs and active calculation pointer are null on create/reopen; reopen preserves history, has reason/CAS and cannot globally unlock period. Close requires complete nonempty RESOLVED input and valid15 close basis, matching Company/period/scope/coverage/membership/current amendments. Batch snapshot close remains historical even if current pointer clears. N18-CALCULATE takes exact current close/input and only valid ATTENDANCE_CLOSED initial path;16 selective review recalculation must use the same input/outcome invariants through its owning protocol, never legacy calculate. Exact PC1 binding remains F02.

N18-BATCH: allocate checked i32 batch revision from batch header PK descending lookup under run fence. Current input remains immutable and closed/resolved; shape FK does not authorize a kernel call. Revalidate source/authority/dependency expiry and close before new effects. Build all outcomes with existing arithmetic owner, Some(explicit pension basis), supported profile/minimum-wage evidence and checked amounts. Expected kernel refusal maps only registered typed blocker; unexpected panic/DB/infrastructure/unknown is whole-command rollback or reconciliation, not a fake completed blocker. Precompute bounded header/outcome commitments, insert batch, existing success money rows and one outcome per input employee in same transaction; cyclic success/outcome FKs deferred until commit. Deferred native18_batch_complete trigger on header/outcome AND native money inserts validates exact input membership and all units RESOLVED, success-row bijection, counts, input_manifest_digest equality and input_unit_digest equality to the referenced unit custody_digest (not semantic_digest), same line/source identities and batch commitment. Run/current head + effect receipt/gate/work/audit commit atomically. All-blocked valid calculation is durable; failed precondition creates none. Receipt replay cannot allocate again. Do not infer MAX from success rows.

N18-SEALED: no child append into an already committed input/batch. Use the current owning command fence and durable command receipt existence to establish create-only membership: inserting parent and all children occurs within one complete command transaction, parent command_id equals reserved current command identity, receipt inserted only after child validation. Any later transaction sees that receipt and may only replay the original outcome; no child append. Direct runtime/private helper DML is denied. Deferred checks on every touched parent are necessary but not sufficient to establish this fence. Exact SQL function/session credential provenance and trigger implementation are unified F02/F04 artifacts; do not use a caller-set GUC command token or allow anyone with INSERT to fabricate this ownership. Until that complete command boundary exists no native writes are enrolled. All immutable rows forbid UPDATE/DELETE; head/current-pointer transitions remain owner CAS. Domain-retained source records and completed command receipts are not private drafts; F03 controls any eventual disposition, not privileged TRUNCATE/cascade.

Native read enrollment/caller inventory:
- payslip_draft::contract_wage_in_force_in_tx and rest payslip_draft caller: correction-aware resolver and correct explicit date, current filtered source proof.
- pay_run::stage_draft_run_inner and roster::materialise_roster_in_tx: existing legacy import branch preserved; native header/prepare branch owns native population.
- lifecycle::calculate_run_in_tx: legacy path cannot accept NP1; native composition uses N18-BATCH.
- lifecycle summary/current-version reads and adapter lib run/line listings: native current input/batch/outcome joins, not MAX success or all lines.
- legacy submit/decide/issuance/disbursement endpoints, common Action, Group Company child, background routes: dispatch16 NP1/PC1 owning transaction or refuse. Issuance current-users identity fallback forbidden for native publications.
-0196 Company purge and any Docs/source cleanup aliases: refuse enrolled retained native records until exact F03/F05 safe disposition path exists; no cascade through receipts/lines.
-0222 existing runtime column INSERT remains forgeable by append. Preserve current UPDATE/DELETE/payable prohibitions, close native append via complete owner boundary and compatible legacy dispatch. No table-wide default grants or invented writer role are considered closed by this document.

Backfill is additive and serial per17. Legacy run/line/result native columns remain null/LEGACY; no computed employee/source provenance fabricated. New exact FK may expose preexisting invalid legacy employee mapping: census/quarantine and reviewed mapping, never silently skip a failing constraint or delete test/data. Preserve native revision0 base mapping and Account attribution transition; no default head/evidence trust. Unused additive schema can remain dormant. After first native effect use compatible readers and pause new effects on rollback; never old MAX-success summaries, reset pointers/heads to hide history, raw source reclassification or old Account credential model. No migration/test has run in18.
