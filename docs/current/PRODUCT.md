# Console product authority

Status: active product authority.

## Product boundary

Console builds one governed company object system in this order:

1. Ontology / Foundry-style object engine and deterministic policy.
2. Company, OrgUnit, JobPosition, Person, and Employment. People/recruiting directory create mints Person in the same transaction as the employees row (#966; `person_id = employee_id`). `hr.appoint` remains a distinct four-eyes assignment.
3. HR appointment, promotion, and transfer through one canonical assignment writer.
4. Payroll projected from existing payroll truth as PayRun. Draft calculate is admitted (`POST /api/v1/payroll/runs/{id}/calculate` already persists versioned won drafts; `payable` stays false). Korea compliance conclusions, wage-statement legal sign-off, and payment execution remain **HOLD**.
5. A Leptos SSR frontend. ADR-0030 substrate gates are green and `Layer::Ui` is accepted (ADR-0041); first full-depth vertical is payroll execution. GET `/` serves deny-by-omission SSR (`console-payroll-ui` merged by `console-app`; #952). Unauthenticated and MEMBER get the empty shell; `PayrollRunRead` gets OpenAPI `PayrollRunSummary` required fields as `data-run-*` attributes, no won (#959). Those authorized runs are a Leptos `#[island]` (`AuthorizedRuns`, #962) hydrated by committed release WASM at `/pkg` (#964). Shipping screens (organization, HR, payroll) are composed at `/organization`, `/hr`, and `/payroll` (#976); payroll drill-through hrefs and empty-vs-denied SSR landed (#982). Persona real-backend E2E on `/`, `/organization`, `/hr`, and `/payroll` landed (#978). That is the ADR-0025 §4 bar for calling those GET screens shipped. Live exposure remains **HOLD**.

**Tenant is always a Group.** Every Company has a real `groups` row (Palantir-uniform account shape). A standalone 법인 is a Group of one, minted 1:1 with that Company. A conglomerate is the same Group object with multiple member Companies. A Company belongs to at most one Group (`UNIQUE (org_id)` on `group_memberships`). Group is not a legal person and is not the employer.

**Company is the legal entity and the RLS cell.** Employment, OrgUnit, JobPosition, PayRun, and receipts always carry one `org_id`. Group never arms `app.current_org`. Writes stay one Company. Consolidated reads remain N× `with_org_conn`; empty grant → empty view.

**Cross-entity work is policy, not tenancy.** Staff of Company A and Company B may share an office; that does not merge OrgUnits or employments. If policy allows, an actor legally employed at B may operate member-company actions (including another company's PayRun) as a principal authorized on that Company — still that Company's `org_id`, four-eyes, and ledger. It is not one mixed group payroll.

The overlay already exists (migration 0060 `groups` / `group_memberships`, grants `GROUP_ADMIN` / `GROUP_VIEWER` / `GROUP_FINANCE`, fail-closed `group_member_org_ids`, `console-platform-group::consolidated_read`). Every Company has a real Group row and `organizations.group_id` is NOT NULL (#971); remove from a multi-member Group remints a Group-of-one. Group still does not arm `app.current_org`. OrgUnit kinds Site, Department, and Team under one Company are enforced in revision attributes (#975); parent cycles longer than 1 are refused (#981). Operational `sites` / `regions` / `branches` stay the auth spine and are not OrgUnits. It does not add `ObjectKey::Group` in this record. The canonical `ObjectKey` roster stays six until a later reviewed design proves Group identity writes need a port.

Console adopts Foundry / Palantir **system patterns** (ontology on data, lineage of writes, applications on objects, write-back through owning actions) on this Postgres + object-type substrate. It does not use Palantir SaaS and does not import or export Palantir files.

**Palantir AIP is the target** for the intelligence layer. That layer is built in the separate Intelligence repository until Intelligence has a named, SHA-bound stable base; cloning it into Console is then authorized. Until that clone lane, AI judgment remains out of Console's tenant product. Console-owned **seams** (fail-closed application port, same `Principal` and ontology actions, no second writer, no Palantir client) are admitted so a later clone can connect without rewriting Console. Owner lock (2026-08-29): those seams include **bind-only** Intelligence HTTP, fail-closed to `127.0.0.1` only, implemented as `GET http://127.0.0.1:<listen-port>/internal/intelligence/bind` on the existing listener (#972). That route is not in published OpenAPI, not shipping UI, and not under `/api/v1`. Unset or non-loopback bind is fail-closed. No chat, no inference text in tenant UX, no HR/payroll write, no autonomous merge. Intelligence is not a tenant application and is not autonomous merge authority. Do not call Palantir APIs.

ERP and finance modules, communications, compliance products, ingest/evidence products, office editing, payment execution, and unrelated verticals remain out of scope. Existing code and documents in those areas are historical, quarry, evidence, or maintenance-only; their presence does not authorize expansion.

## Product invariants

- Commands are deterministic and revision-aware, with replay-safe receipts and auditable mutations.
- Tenant isolation, deny-by-omission, and nondisclosure apply at every read and write boundary. The tenant **account** is always a Group (standalone Company = Group of one with a real row). The RLS **cell** is always a Company (`org_id` / `app.current_org`). Group never arms that GUC.
- Effective-dated truth uses half-open intervals; history is closed and appended, never overwritten. Generalizing that substrate across core HR (`asOf` / `from` / `to`, slices, corrections, delta; #316) is **admitted direction**, not a claim that every HR object already has an as-of read surface.
- Projected objects have exactly one domain writer. Ontology and adapters do not create alternate write paths.
- Requester and approver are distinct natural persons for `company.*`, `hr.*`, and `payroll.*` approvals, even when their capacities differ. All other kinds — including `organization.*` and `people.*` — hold only the account-level `approver_id <> requested_by` bar. `requires_natural_person_four_eyes` (`backend/crates/governance/domain`) is prefix-scoped and a test asserts the exclusion. Extending the bar to the remaining kinds is **unscheduled, not blocked**: migration 0076 shows only that `users.employee_id` is nullable, and a NULL resolution can fail closed exactly as the enforced kinds already do. Whether unbound accounts actually exist is an open question no census has answered, so the compatibility risk is a hypothesis on HOLD rather than a reason to leave six of thirteen dispatch targets outside the invariant.
- Preflight uses the same authorization, policy, state, revision, and input validation as execute and performs no mutation.
- Legal sources are versioned evidence, not transferable compliance conclusions. Production exposure and compliance claims require separate authority.
- The repository integration and source-release branch is `dev`. Ordered environment mirrors are `dev` → `staging` → `canary` → `production`. Creating or fast-forwarding those names is not production exposure.

## Architecture

The existing Rust backend is reused as verified substrate rather than rewritten wholesale. The product exposes governed company objects and authorized actions:

`Company → OrgUnit(Site/Department/Team/…) → JobPosition → Person/Employment → HR action → PayRun`

The published HTTP contract for those six objects is generated from a typed semantic manifest (`backend/crates/contracts/src/semantic_manifest.json` → composed OpenAPI, generated Rust codecs/validators, generated API docs, and an in-repo TypeScript SDK; #989–#1018). Free-form `CreateObjectTypeDraft` children remain outside that contract. Execute/preflight params are typed onto the thirteen DispatchTarget Inputs (#997). ADR-0031's accepted generator target is landed for that six-object roster; the 2026-08-03 ADR observation that no emitter existed is historical. Palantir/SAP names remain comparative evidence; this record does not clone OData or OSDK. Python/Java SDKs and npm publish remain unscheduled. The served OpenAPI document uses OpenAPI 3.1 JSON Schema null unions (#990), JSON error envelopes (#991), top-level bearer security and explicit `security: []` on public operations (#992). Bidirectional request/response conformance is admitted; the body-gate floor is 75 resolved of 291 census with `BODY_UNDECIDABLE_MAX` 206 leftover non-Head bodies (`scripts/check-request-body-contract.mjs`). `info.version` is stamped from the crate version (#1015); tying it to release-please remains unscheduled. Company, OrgUnit, Person, JobPosition, and Employment Head instance and collection GETs are published; OrgUnit→JobPosition reverse collection is published from the existing `list_for_org_unit` port (#1016); Head GET/list `permissions` are `employee_directory_read` from existing authz (#1017); Head FK links bind published GET `operationId`s (#1013). OpenAPI and generated clients remain one writer.

The tenant account is always a Group (standalone = Group of one with a real row). Group is not a node on this legal chain and not an `ObjectKey`. OrgUnit kinds live under one Company; a shared physical office is not a shared OrgUnit. REST and any future server functions are *intended as* sibling adapters over the same application-layer use cases. This is an aspiration, not an enforced boundary: `Layer::allowed_deps` lets `Layer::Rest` reach Adapter, Platform, Domain and Kernel directly, and 6 of the 34 non-platform REST crates (analytics-quant, consulting, facilities, logistics, orgchange, production) declare no application dependency at all. Counting every package whose name ends `-rest` the ratio is 9 of 37: `console-platform-auth-rest`, `console-platform-authz-rest` and `console-platform-rest` also declare none, and are excluded above only because the layer gate classifies `crates/platform/*` as Platform before it reaches the `-rest` suffix. Converging them is unscheduled work. The frontend reads real contracts, omits unauthorized data server-side, and contains no client-side business authority. Cargo is the target build system; existing Buck paths remain repository reality until a dedicated, evidence-backed convergence change removes them without losing test coverage. `-ui` members are skipped by first-party Buck generation until Leptos is vendored.

## Holds

- Frontend **shipping** is admitted (owner 2026-08-29). ADR-0030 substrate gates are green and `Layer::Ui` is accepted (ADR-0041). GET `/` is mounted (deny-by-omission SSR, Leptos 0.9.0-beta, #952). Contracts-backed authorized reads (#959), the `AuthorizedRuns` island (#962), and WASM hydration of that island (#964) do not put business authority in the client. Org/HR/payroll screens are composed (#976) with payroll drill-through (#982). Persona real-backend E2E (ADR-0025 §4) for those GET routes landed (#978). React tombstone paths stay absent. Deny-by-omission is server composition. Import/export is not the data-entry base except 자료실 (later, not this slice). The comms rail is not in this slice. 0.8.x is the rollback if 0.9.0-beta blocks production. Live exposure remains **HOLD**.
- Company, Person, Employment, and PayRun projection fan-out is **RELEASED** (2026-08-19, by repository-owner decision). The stated condition — an explicit owning port and a proven single-writer boundary for each — is met, and stays checked rather than asserted once: `node tools/ci/hold-release-conditions.mjs` runs in the preflight sweep and reports the owning crate, owned tables, proving port suite and PostgreSQL-job wiring for all six canonical objects; the writer-ownership gate holds all twenty owned tables with an empty `KNOWN_SECOND_WRITERS` ratchet that `stale_exemptions()` can only shrink; and the boundary is mutation-proven — injecting one second writer of `employment_heads` or of `payroll_disbursements` into a non-owner crate turns three tests red, and reverting returns the suite to green. This releases projection fan-out only. It confers no authority over any other hold in this list, and it does not assert per-row attribution on the shared receipt store, which is a separate boundary tracked apart from projection.
- Live production, DNS, TLS, secret, exposure, payment, credential-reset, and compliance-claim actions are **HOLD** without separate authority and evidence. Environment-branch promotion that would change live DNS, TLS, or production exposure remains **HOLD** until a later go-live PRODUCT. Creating or fast-forwarding `dev` / `staging` / `canary` / `production` is not that promotion. Published OpenAPI must not present `POST /api/v1/auth/admin/credential-reset` as a generally available recovery product while this HOLD stands; documenting it as live recovery is a security-truth defect, not clearance to execute production credential reset.
- Korea compliance conclusions remain **HOLD** pending qualified authority.
- The grandfathered OCI Ampere A1 instance (4 OCPU / 24 GB) is standing **off-limits**, not a program HOLD: it must never be destroyed, terminated, resized, or reprovisioned; re-creation permanently loses the reserved capacity.
- Full-disk erase of the owner workstation **already occurred** (post-wipe, owner fact 2026-08-28). This is not a future HOLD. Do not erase again as a program action. Do not invent a pre-wipe custody ledger for bytes that are gone. Remaining local P0 categories (signing identity, account recovery and passkeys/2FA, OCI/Talos access, local secret files, restricted business inputs, unpublished repository/worktree bytes) are re-issue or off-device confirmation on this machine, not a reason to wait.
- Palantir-class company-object remainder stays **HOLD** (not cleared by #989–#1018): `ObjectKey::Group` (roster stays six); PayRun is still `PayrollRunSummary`, not a versioned Head GET, and `payable` stays const false; no linked `as_of` on OrgUnit/JobPosition/Person/Company (Employment `as_of` / `from` / `to` already published); no JobPosition SSR directory (shipping GET `/` `/organization` `/hr` `/payroll` only); no Events/AsyncAPI / public audit catalog; no HTTP ETag/If-Match; no mapping Feature::ALL onto ~587 paths; do not bring back `/_ui`; Person wire stays `id`, `display_name`, `legal_name`, `version` only.
- Cloning Intelligence into Console is **HOLD** until Intelligence names a SHA-bound stable base. Until that clone lane, AI judgment remains out of Console's tenant product. Console-owned seams are admitted, including owner-locked **bind-only** Intelligence HTTP (`127.0.0.1` only; fail-closed if unset or non-loopback; not published OpenAPI, not shipping UI, not `/api/v1`). They are not a clone and not a tenant Intelligence product.
- Historical documents, branches, chats, handoffs, and transient runtime state are context or evidence only. They cannot clear these holds; the current handoff may inventory custody evidence without becoming product authority.
