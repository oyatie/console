# DR prerequisite checklist — t_3455f923

Checked at: 2026-07-09T15:16:57Z
Scope: redacted preflight only. No credential values, token values, bucket secret values, phone numbers, GPS coordinates, 주민번호, customer data, or real contact rosters are recorded here.

## Summary verdict

Status: BLOCKED / needs operator input or remediation before a production CNPG/Barman DR drill.

The current machine has only the `k3d-maintenance-relay` Kubernetes context. That context can perform local scratch namespace/PVC dry-run checks, but it does not contain the production `maintenance` namespace and therefore does not prove production DR permissions. OCI Object Storage access confirms bucket existence and private access, but live bucket versioning is `Disabled`, which conflicts with the repository OpenTofu contract requiring `versioning = "Enabled"` for `mnt-db-backups`.

## Redacted prerequisite checklist

| Prerequisite | Result | Redacted evidence |
|---|---:|---|
| Kubeconfig reaches the production `maintenance` namespace | FAIL / unconfirmed | Sanitized `kubectl` probe at 2026-07-09T15:13:00Z: current context `k3d-maintenance-relay`; context list contains only that context; `kubectl get namespace maintenance` returned NotFound. |
| Permission to create/delete scratch `mnt-dr-*` namespaces | UNCONFIRMED for production | On current local k3d context only, `kubectl auth can-i create namespaces` and `delete namespaces` returned `yes`; server-side dry-run create for a redacted scratch namespace returned `namespace/mnt-dr-preflight-redacted`. This is not production evidence because the `maintenance` namespace is absent and the context is local k3d. |
| Permission to create/delete scratch PVCs | UNCONFIRMED for production | On current local k3d context only, `kubectl auth can-i create/delete persistentvolumeclaims -n mnt-dr-preflight-redacted` returned `yes`. No production scratch namespace/PVC mutation was performed. |
| Live `oci-objectstore-creds` Secret validity for the `mnt-backups` ObjectStore | FAIL / unconfirmed | The production namespace is unavailable from the current kubeconfig, so live `secret/oci-objectstore-creds` and `objectstore.barmancloud.cnpg.io/mnt-backups` could not be read. Repository contract expects Secret keys `ACCESS_KEY_ID` and `ACCESS_SECRET_KEY` referenced by `deploy/apps/maintenance/base/database.yaml`. |
| `mnt-db-backups` bucket exists | PASS | Sanitized OCI probe at 2026-07-09T15:14:18Z: `oci os bucket get` found bucket name `mnt-db-backups` in the configured tenancy namespace. Namespace and compartment identifiers are redacted here. |
| `mnt-db-backups` bucket is private | PASS | Sanitized OCI probe: bucket `public_access_type` is `NoPublicAccess`. |
| `mnt-db-backups` bucket versioning is enabled | FAIL | Sanitized OCI probe: bucket `versioning` is `Disabled`. Repository OpenTofu declares `deploy/opentofu/storage.tf` bucket `mnt-db-backups` with `versioning = "Enabled"`. |
| Bucket has an OCI lifecycle policy | INFO / not required by current Barman retention contract | Sanitized OCI probe at 2026-07-09T15:16:18Z: OCI returned `LifecyclePolicyNotFound` for `mnt-db-backups`. The repository Barman policy currently relies on no CNPG/Barman `retentionPolicy` in `deploy/apps/maintenance/base/database.yaml`, documented as indefinite retention. |
| Covered by accepted OCI/Barman policy | PARTIAL / live coverage unconfirmed | Repository evidence exists: `docs/decisions/ADR-0015-dr-posture-wal-archiving-continuous-pitr.md` is accepted; `deploy/apps/maintenance/base/database.yaml` declares CNPG Barman Cloud Plugin `barman-cloud.cloudnative-pg.io` and ObjectStore `mnt-backups` targeting `s3://mnt-db-backups/`; `ops/dr/cnpg-restore-drill.md` documents the production CNPG/Barman/OCI restore drill. Live Kubernetes ObjectStore status cannot be confirmed from the current kubeconfig, and bucket versioning currently fails the OpenTofu contract. |
| Compose production path: VM/Compose access | N/A unless Compose is confirmed active; otherwise UNCONFIRMED | Current Kubernetes/CNPG docs say the production restore path runs against the live cluster in `deploy/apps/maintenance/base/database.yaml`. Older Compose DR docs still exist. No production VM/Compose access was proven from this environment. |
| Compose production path: off-VM WAL sync cadence <= 5 minutes | N/A unless Compose is confirmed active; otherwise UNCONFIRMED | Repository docs require this: `ops/dr/DR-POLICY.md` and `ops/backup/RUNBOOK.md` both state the Compose WAL archive volume must be synced off-VM at least every five minutes. No live VM/cron/systemd/object-store evidence was available in this environment. |

## Repository evidence references

- `deploy/apps/maintenance/base/database.yaml:58-91` — CNPG Barman Cloud Plugin and `mnt-backups` ObjectStore contract, including redacted credential Secret key names.
- `deploy/apps/maintenance/base/database.yaml:93-107` — daily CNPG scheduled backup contract.
- `deploy/opentofu/storage.tf:6-12` — OpenTofu bucket contract for `mnt-db-backups`, `NoPublicAccess`, and `versioning = "Enabled"`.
- `ops/dr/cnpg-restore-drill.md:15-23` — production object mapping: live cluster, ObjectStore, Barman plugin, credential Secret name/key names, serverName.
- `ops/dr/cnpg-restore-drill.md:40-52` — safety model: scratch namespace, read-only recovery cluster, copied Secret/ObjectStore, live resources untouched.
- `ops/dr/cnpg-restore-drill.md:75-82` — required success markers for a future drill after prerequisites pass.
- `ops/compose.yml:52-61` — Compose Postgres WAL archiving settings including `archive_timeout=300s`.
- `ops/dr/DR-POLICY.md:20-31` and `ops/backup/RUNBOOK.md:67-70` — Compose off-VM WAL sync contract of at least every five minutes.

## Required operator follow-up

1. Provide or select the production kubeconfig/context that can read namespace `maintenance`, CNPG `Cluster/mnt-db`, Barman `ObjectStore/mnt-backups`, and Secret metadata for `oci-objectstore-creds` without exposing Secret values.
2. Confirm production RBAC/admission/storage permissions for creating and deleting scratch `mnt-dr-*` namespaces and recovery PVCs, preferably with server-side dry-run or a short-lived scratch namespace whose teardown is recorded.
3. Remediate `mnt-db-backups` bucket versioning to `Enabled` or explicitly update the accepted policy if disabling versioning is intentional.
4. If Compose is still the active production path, provide redacted VM/Compose access evidence and off-VM WAL sync evidence proving cadence <= 5 minutes.

## Data-handling note

All live evidence above is redacted to high-level pass/fail/status. No Secret `.data` values were printed or stored in this file.
