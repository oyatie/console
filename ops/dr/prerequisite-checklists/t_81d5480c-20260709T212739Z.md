# t_81d5480c self-hosted S3 backup/checksum validation

Timestamp: 2026-07-09T21:27:39Z
Task: `t_81d5480c` — Validate backups and checksum settings against self-hosted S3

## Substrate used

- Local task-owned SeaweedFS S3 endpoint: `chrislusf/seaweedfs:4.32`, path-style endpoint `http://127.0.0.1:18333` / Docker-network alias `http://seaweedfs:8333`.
- Local source Postgres: `postgres:18.4` with a probe table `kanban_backup_probe`.
- Barman tooling: `ghcr.io/cloudnative-pg/postgresql:18.4`, `barman-cloud-backup 3.19.1`.
- Current Kubernetes context was `k3d-maintenance-relay`; it did not expose CNPG/Barman CRDs, so this was a local Barman Cloud drill using the CNPG Postgres image/tooling rather than an in-cluster CNPG operator recovery.

## Checksum behavior

Command class: boto3/botocore signed `PutObject`/`GetObject` to SeaweedFS with path-style addressing.

- Default botocore checksum behavior, with `AWS_REQUEST_CHECKSUM_CALCULATION` and `AWS_RESPONSE_CHECKSUM_VALIDATION` unset:
  - `plain_put_get_match=True`
  - `checksum_put_get_match=True`
  - observed default plain PUT headers included `x-amz-sdk-checksum-algorithm=CRC32` and `x-amz-checksum-crc32=...`
- `when_required` compatibility mode:
  - `plain_put_get_match=True`
  - `checksum_put_get_match=True`
  - observed plain PUT omitted the default CRC32 flexible-checksum headers.

Conclusion: SeaweedFS accepted and returned objects under default boto3 flexible-checksum behavior. The OCI workaround is not needed for the self-hosted S3 context.

## Barman backup/WAL/restore behavior

All Barman commands below ran with the AWS checksum env overrides unset.

- `barman-cloud-backup --cloud-provider aws-s3 --endpoint-url http://seaweedfs:8333 --addressing-style path --gzip --immediate-checkpoint --jobs 1 ... s3://mnt-db-backups mnt-db-restore`
  - `refreshed_backup_exit=0`
  - backup list status: `DONE`
  - backup ID: `20260709T211045`
  - begin/end WAL: `000000010000000000000006`
- `barman-cloud-wal-archive ... s3://mnt-db-backups mnt-db-restore <wal>`
  - `refreshed_wal_archive_000000010000000000000006_exit=0`
  - `refreshed_wal_archive_000000010000000000000007_exit=0`
- `barman-cloud-restore ... s3://mnt-db-backups mnt-db-restore 20260709T211045 /restore/pgdata`
  - `refreshed_restore_exit=0`
  - `refreshed_restored_pg_version=18`
  - `refreshed_restored_pg_control=present`
- Recovered Postgres boot with `barman-cloud-wal-restore` restore command:
  - `recovered_postgres_ready=ok attempt=2`
  - logs restored WAL `000000010000000000000006` and `000000010000000000000007`
  - `recovered_probe_rows=2`
  - `recovered_in_recovery=false`
  - `recovered_query_exit=0`

## Object readability

Signed object-read probe against the Barman bucket:

- `bucket=mnt-db-backups`
- `object_count=11`
- `backup_info_key=mnt-db-restore/base/20260709T211045/backup.info`, `backup_info_size=1296`, `backup_info_range_read_bytes=32`
- `base_tar_key=mnt-db-restore/base/20260709T211045/data.tar.gz`, `base_tar_size=4156531`, `base_tar_range_read_bytes=32`
- `wal_key=mnt-db-restore/wals/0000000100000000/000000010000000000000006.gz`, `wal_size=16527`, `wal_range_read_bytes=32`

## Rendered configuration result

- `kubectl kustomize deploy/apps/maintenance/overlays/on-prem`:
  - `onprem_render_exit=0`
  - `onprem_cluster_has_checksum_env=false`
  - `onprem_cluster_has_env_field=false`
  - `onprem_objectstore_endpoint_self_hosted=true`
  - `onprem_configmap_endpoint_self_hosted=true`
- `kubectl kustomize deploy/apps/maintenance/base`:
  - `deploy/apps/maintenance/base_has_checksum_env=true`
- `kubectl kustomize deploy/apps/maintenance/overlays/prod`:
  - `deploy/apps/maintenance/overlays/prod_has_checksum_env=true`
- `kubectl kustomize deploy/apps/maintenance/overlays/oci-guest`:
  - `deploy/apps/maintenance/overlays/oci-guest_has_checksum_env=true`

Final config decision: keep the OCI checksum workaround in the OCI/base/prod/oci-guest contexts, but remove inherited `spec.env` from the on-prem CNPG overlay so self-hosted S3 uses default boto3/Barman checksum behavior.

## Repository verification

- `node --test scripts/check-production-hardening.test.mjs` → `production_hardening_unit_test_exit=0` (24 tests passing).
- `npm run check:production-hardening` → `check_production_hardening_exit=0` (157 checks across 3 groups).
