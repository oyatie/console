# Dark Argo CD project for the ADR-0024 self-hosted observability stage.
# This file is intentionally under deploy/apps/observability/, not deploy/argocd/apps/,
# so merging it does not add observability to the live app-of-apps root.
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: console-observability-dark
  namespace: argocd
  annotations:
    console.nousresearch.com/dark-stage: "true"
    console.nousresearch.com/context: "onprem"
spec:
  description: Dark ADR-0024 self-hosted observability stage; manual apply/sync only after founder/operator activation.
  sourceRepos:
    - https://github.com/jason931225/console.git
  destinations:
    - server: https://kubernetes.default.svc
      namespace: console-observability
  # The rendered stack owns only the console-observability namespace plus
  # namespace-scoped workloads inside it. Keep the project isolated instead of
  # broadening the live maintenance project during a no-op merge.
  clusterResourceWhitelist:
    - group: ""
      kind: Namespace
  namespaceResourceWhitelist:
    - group: "*"
      kind: "*"
  orphanedResources:
    warn: true
