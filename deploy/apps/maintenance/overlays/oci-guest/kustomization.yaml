apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Explicit deployment-context alias for the live OCI guest. It intentionally
# renders the production overlay unchanged so OCI Object Storage remains the
# default for the current cluster while on-prem can diverge in its own overlay.
resources:
  - ../prod
