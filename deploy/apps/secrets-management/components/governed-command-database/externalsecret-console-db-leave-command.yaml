# ExternalSecret: project the dedicated leave-command connection from OpenBao.
# CNPG binds the password to the managed `console_leave_cmd` login while only the
# API consumes its URI as `LEAVE_COMMAND_DATABASE_URL`. The migration and worker
# roles never receive this credential.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: console-db-leave-command
  namespace: console
  labels:
    app.kubernetes.io/part-of: console-substrate
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao-console
    kind: ClusterSecretStore
  target:
    name: console-db-leave-command
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: console/db/leave-command
        property: username
    - secretKey: password
      remoteRef:
        key: console/db/leave-command
        property: password
    - secretKey: uri
      remoteRef:
        key: console/db/leave-command
        property: uri
