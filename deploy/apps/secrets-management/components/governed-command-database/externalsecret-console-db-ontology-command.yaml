# ExternalSecret: project the dedicated ontology-command connection from
# OpenBao. CNPG binds the password to `console_ontology_cmd`; only the API receives
# the URI as `ONTOLOGY_COMMAND_DATABASE_URL`.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: console-db-ontology-command
  namespace: console
  labels:
    app.kubernetes.io/part-of: console-substrate
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao-console
    kind: ClusterSecretStore
  target:
    name: console-db-ontology-command
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: console/db/ontology-command
        property: username
    - secretKey: password
      remoteRef:
        key: console/db/ontology-command
        property: password
    - secretKey: uri
      remoteRef:
        key: console/db/ontology-command
        property: uri
