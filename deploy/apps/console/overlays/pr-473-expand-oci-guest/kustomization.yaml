apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# DARK OCI-guest rehearsal. It preserves the exact live production transform,
# but this path is not referenced by an Argo CD Application.
resources:
  - ../prod

components:
  - ../../components/governed-command-database
