apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
# Opt-in admission audit resources. Requires sigstore policy-controller CRDs;
# not referenced by base/prod until the controller is installed on the cluster.
resources:
  - clusterimagepolicy.yaml
