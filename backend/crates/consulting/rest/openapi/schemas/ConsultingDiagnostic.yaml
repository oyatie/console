type: object
required:
  - id
  - summary
  - created_at
properties:
  id:
    $ref: '#/components/schemas/Uuid'
  summary:
    type: string
  document_id:
    oneOf:
      - {$ref: '#/components/schemas/Uuid'}
      - {type: 'null'}
  created_at:
    type: string
    format: date-time
