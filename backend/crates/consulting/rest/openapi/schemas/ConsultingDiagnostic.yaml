{ type: object, required: [id, summary, created_at], properties: { id: { $ref: '#/components/schemas/Uuid' }, summary: { type: string }, document_id: { $ref: '#/components/schemas/Uuid', nullable: true }, created_at: { type: string, format: date-time } } }
