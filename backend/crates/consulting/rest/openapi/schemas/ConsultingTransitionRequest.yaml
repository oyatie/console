type: object
additionalProperties: false
required:
  - toStatus
  - expectedVersion
  - reason
properties:
  toStatus:
    type: string
    enum:
      - PROPOSED
      - APPROVED
      - IMPLEMENTED
      - MEASURED
      - SUSTAINED
      - CORRECTIVE
  expectedVersion:
    type: integer
    format: int64
    minimum: 1
  approvalId:
    oneOf:
      - {$ref: '#/components/schemas/Uuid'}
      - {type: 'null'}
  reason:
    type: string
    minLength: 1
