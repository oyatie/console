[package]
name = "console-platform-rest"
version = "0.1.0"
description = "REST API for the SaaS-vendor PLATFORM tier (tenant onboarding, list, suspend)."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
axum = { workspace = true }
console-kernel-core = { workspace = true }
console-platform-auth = { path = "../auth" }
console-platform-authz = { path = "../authz" }
console-platform-db = { workspace = true }
console-platform-provisioning = { path = "../provisioning" }
console-platform-request-context = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sqlx = { workspace = true }
thiserror = { workspace = true }
time = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
http = { workspace = true }
tokio = { workspace = true }
tower = { workspace = true, features = ["util"] }
console-platform-db = { workspace = true }
console-platform-request-context = { workspace = true }
# Dev-only: the onboarding-seed wiring test installs an engine-backed seeder.
# The production impl lives in the App tier (console-app); the platform tier itself
# must NOT depend on the ontology ADAPTER (layer boundary), so this is dev-only.
console-ontology-adapter-postgres = { workspace = true }
serde_json = { workspace = true }
p256 = { version = "0.13.2", features = ["ecdsa", "pkcs8", "pem"] }

[lints]
workspace = true
