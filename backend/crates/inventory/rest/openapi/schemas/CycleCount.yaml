type: object
required: [id, ccCode, branchId, stockLocation, status, version, openedBy, lineCount, varianceLineCount, createdAt, updatedAt]
properties: { id: { $ref: '#/components/schemas/Uuid' }, ccCode: { type: string }, branchId: { $ref: '#/components/schemas/Uuid' }, stockLocation: { $ref: '#/components/schemas/InventoryStockLocationSummary' }, status: { type: string, enum: [DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED] }, version: { type: integer }, openedBy: { $ref: '#/components/schemas/Uuid' }, submittedBy: { $ref: '#/components/schemas/Uuid', nullable: true }, submittedAt: { type: string, format: date-time, nullable: true }, decidedBy: { $ref: '#/components/schemas/Uuid', nullable: true }, decidedAt: { type: string, format: date-time, nullable: true }, decisionMemo: { type: string, nullable: true }, lineCount: { type: integer, format: int64 }, varianceLineCount: { type: integer, format: int64 }, createdAt: { type: string, format: date-time }, updatedAt: { type: string, format: date-time } }
