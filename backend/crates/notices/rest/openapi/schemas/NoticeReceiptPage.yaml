type: object
required:
- items
- total
properties:
  items:
    type: array
    items:
      $ref: '#/components/schemas/NoticeReceipt'
  total:
    type: integer
    format: int64
    description: Total matching rows before limit/offset.
