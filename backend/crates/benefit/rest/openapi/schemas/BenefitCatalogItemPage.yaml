type: object
required: [items, limit, offset, total]
properties:
  items: { type: array, items: { $ref: '#/components/schemas/BenefitCatalogItem' } }
  limit: { type: integer }
  offset: { type: integer }
  total: { type: integer }
