type: object
description: Immutable replay receipt for an accepted instance_revision action command.
required:
- command_id
- payload_digest
- instance
- gates
properties:
  command_id:
    $ref: '#/components/schemas/Uuid'
  payload_digest:
    type: string
    pattern: '^[a-f0-9]{64}$'
  instance:
    $ref: '#/components/schemas/InstanceState'
  gates:
    $ref: '#/components/schemas/GateChainOutcome'
