type: object
required: [to_state]
properties:
  to_state: { $ref: '#/components/schemas/InstanceLifecycleState' }
  reason: { type: string, nullable: true }
  checklist_all_acknowledged: { type: boolean, nullable: true }
  four_eyes_request_ref: { type: string, format: uuid, nullable: true }
