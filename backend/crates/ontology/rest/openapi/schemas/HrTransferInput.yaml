type: object
description: >-
  Typed params for `hr.transfer` (`EmploymentQuery::Transfer`). Same shape as
  promote; the receipt records a different dispatch target. Four-eyes is
  natural-person.
required:
- employment_id
- valid_from
- attributes
properties:
  employment_id:
    $ref: '#/components/schemas/Uuid'
  valid_from:
    $ref: '#/components/schemas/Timestamp'
  attributes:
    $ref: '#/components/schemas/EmploymentAttributesInput'
