type: object
required:
- dispatch
- gates
properties:
  dispatch:
    type: string
    enum: [instance_revision, projected_usecase]
  gates:
    $ref: '#/components/schemas/GateChainOutcome'
  instance:
    $ref: '#/components/schemas/InstanceState'
  projected:
    type: object
    additionalProperties: true
  receipt:
    $ref: '#/components/schemas/OntologyActionCommandReceipt'
