type: object
additionalProperties: false
required:
- op
- customerId
- name
properties:
  op:
    type: string
    enum: [CREATE_SITE]
  customerId:
    $ref: '#/components/schemas/Uuid'
  name:
    type: string
    minLength: 1
    maxLength: 120
