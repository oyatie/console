type: object
additionalProperties: false
required:
- op
- regionId
- name
properties:
  op:
    type: string
    enum: [RENAME_REGION]
  regionId:
    $ref: '#/components/schemas/Uuid'
  name:
    type: string
    minLength: 1
    maxLength: 120
