type: object
required:
- decision
properties:
  decision:
    type: string
    enum: [APPROVE, REJECT]
  reason:
    type: string
    description: Required for REJECT (a blank reason is rejected with 422).
