type: object
required:
- status
properties:
  status:
    type: string
    enum: [SCHEDULED, SUBMITTED_TO_BANK, PAID, FAILED]
  reason:
    type: string
    description: Required for FAILED (a blank reason is rejected with 422).
