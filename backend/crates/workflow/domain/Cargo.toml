[package]
name = "mnt-workflow-domain"
version = "0.1.0"
description = "Pure workflow-runtime domain: run/node/waiting-task/outbox status enums, FSM transition tables + guards, and the DB-access port trait the runtime engine drives."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
mnt-kernel-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
time = { workspace = true }
uuid = { workspace = true }

[lints]
workspace = true
