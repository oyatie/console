[package]
name = "console-workflow-runtime"
version = "0.1.0"
description = "Workflow runtime engine: idempotency-key derivation, node interpreter, FSM-driven advance logic over the domain port, and the Cedar/PBAC authorization-request builder (LegacyOnly at M2). No sqlx/axum/tokio."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
console-kernel-core = { workspace = true }
console-workflow-domain = { workspace = true }
console-platform-authz = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
time = { workspace = true }
uuid = { workspace = true }

[lints]
workspace = true
