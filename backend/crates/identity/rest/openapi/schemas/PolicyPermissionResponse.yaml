type: object
required:
- feature_key
- permission_level
properties:
  feature_key:
    type: string
  permission_level:
    type: string
    enum:
    - deny
    - request_only
    - limited
    - allow
