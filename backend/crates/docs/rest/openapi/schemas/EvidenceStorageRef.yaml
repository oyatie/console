type: object
required: [provider, object_id]
properties:
  provider: { type: string }
  object_id: { type: string }
  key_ref: { type: string, nullable: true }
  version_id: { type: string, nullable: true }
