type: object
required: [id, site, role, cover_date, from_minutes, to_minutes, covered_employee_id, covered_name, reason_kind, worker_name, worker_type, status, created_by, created_at]
properties: { id: { $ref: '#/components/schemas/Uuid' }, site: { type: string }, branch_id: { $ref: '#/components/schemas/Uuid', nullable: true }, role: { type: string }, cover_date: { type: string, format: date }, from_minutes: { type: integer }, to_minutes: { type: integer }, covered_employee_id: { $ref: '#/components/schemas/Uuid' }, covered_name: { type: string }, reason_kind: { type: string }, reason_detail: { type: string, nullable: true }, worker_employee_id: { $ref: '#/components/schemas/Uuid', nullable: true }, worker_name: { type: string }, worker_type: { type: string }, worker_rate: { type: string, nullable: true }, status: { type: string, enum: [ASSIGNED, CANCELLED] }, exception_id: { $ref: '#/components/schemas/Uuid', nullable: true }, created_by: { $ref: '#/components/schemas/Uuid' }, created_at: { type: string, format: date-time } }
