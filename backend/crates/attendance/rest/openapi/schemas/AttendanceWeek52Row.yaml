type: object
required:
  - employee_id
  - name
  - week_start
  - current_hours
  - projected_hours
  - tone
  - acked
properties:
  employee_id:
    $ref: '#/components/schemas/Uuid'
  name:
    type: string
  team:
    type:
      - string
      - 'null'
  week_start:
    type: string
    format: date
  current_hours:
    type: number
  projected_hours:
    type: number
  tone:
    type: string
    enum:
      - OK
      - WARN
      - DANGER
  acked:
    type: boolean
  acked_at:
    type:
      - string
      - 'null'
    format: date-time
