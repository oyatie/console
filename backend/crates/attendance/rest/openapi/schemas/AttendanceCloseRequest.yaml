type: object
additionalProperties: false
required:
  - month
properties:
  month:
    type: string
    pattern: ^\\d{4}-\\d{2}$
  branch_scope:
    oneOf:
      - {$ref: '#/components/schemas/Uuid'}
      - {type: 'null'}
  attest:
    type:
      - boolean
      - 'null'
