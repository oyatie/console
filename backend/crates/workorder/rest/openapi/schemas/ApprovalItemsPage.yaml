type: object
required:
- items
- sources
- limit
- offset
- total
properties:
  items:
    type: array
    items:
      $ref: '#/components/schemas/ApprovalItem'
  sources:
    type: array
    items:
      $ref: '#/components/schemas/ApprovalItemSource'
  limit:
    type: integer
    format: int64
  offset:
    type: integer
    format: int64
  total:
    type: integer
    format: int64
