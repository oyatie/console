type: object
required:
- legalHold
properties:
  legalHold:
    type: boolean
  retentionUntil:
    type: string
    format: date
    description: ISO date; omit or null to clear the retention deadline.
