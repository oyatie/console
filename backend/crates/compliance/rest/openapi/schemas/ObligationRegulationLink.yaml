type: object
required: [id, obligation_id, regulation_impact_id, relationship, created_by, created_at]
properties:
  id: { $ref: '#/components/schemas/Uuid' }
  obligation_id: { $ref: '#/components/schemas/Uuid' }
  regulation_impact_id: { $ref: '#/components/schemas/Uuid' }
  relationship: { $ref: '#/components/schemas/ObligationRegulationRelationship' }
  rationale: { type: [string, 'null'] }
  created_by: { $ref: '#/components/schemas/Uuid' }
  created_at: { $ref: '#/components/schemas/Timestamp' }
