[package]
name = "mnt-kernel-core"
version = "0.1.0"
description = "Shared kernel: typed IDs, branch scope, audit event, trace context, error taxonomy, transition results. Pure — no async, no sqlx, no axum."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
time = { workspace = true }

[lints]
workspace = true
