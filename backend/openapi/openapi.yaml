openapi: 3.1.0
info:
  title: Console API
  version: 0.1.0
security:
- bearerAuth: []
paths:
  /.well-known/apple-app-site-association:
    get:
      tags:
        - system
      security: []
      operationId: appleAppSiteAssociation
      summary: Apple App Site Association (native passkeys)
      description: Public, unauthenticated association document Apple fetches over the RP origin to authorize the native iOS
        app's passkeys. The `webcredentials.apps` list is sourced from `CONSOLE_IOS_APP_IDS`; an unconfigured deployment serves
        a valid empty document. Served as `application/json` (no file extension, per Apple's requirement).
      responses:
        '200':
          description: The Apple App Site Association document.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppleAppSiteAssociation'
  /.well-known/assetlinks.json:
    get:
      tags:
        - system
      security: []
      operationId: androidAssetLinks
      summary: Android Digital Asset Links (native passkeys)
      description: Public, unauthenticated Digital Asset Links document Android fetches to authorize the native app's passkeys
        for the RP domain. The package and signing-cert fingerprints come from `CONSOLE_ANDROID_PACKAGE` / `CONSOLE_ANDROID_CERT_SHA256`;
        an unconfigured deployment serves an empty JSON array. Served as `application/json`.
      responses:
        '200':
          description: The Android Digital Asset Links statements.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AndroidAssetLinkStatement'
  /api/approval-items:
    get:
      tags:
        - approval-items
      operationId: listApprovalItems
      summary: List federated approval workflow items
      description: Returns a server-owned, paginated approval queue across typed workflow objects. Each item carries ontology,
        workflow, and policy context so clients can render RBAC/PBAC/ABAC-aware approval tasks without composing source queues
        in the browser. The backend enforces authentication, tenant RLS, feature authorization, and branch scope before returning
        any item; source-specific mutation endpoints re-check authorization.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 200
          default: 100
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Federated approval items for the caller's policy scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalItemsPage'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/audit:
    get:
      tags:
        - audit
      operationId: listAuditLog
      summary: List branch-scoped audit log records
      description: Returns append-only audit records visible to the caller's branch scope. Filters are optional and the result
        is paginated to keep the evidence surface bounded.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 200
          default: 50
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      - name: target_type
        in: query
        required: false
        schema:
          type: string
      - name: actor
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Paginated audit records.
          content:
            application/json:
              schema:
                type: object
                required:
                - items
                - limit
                - offset
                properties:
                  items:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  limit:
                    type: integer
                    format: int64
                  offset:
                    type: integer
                    format: int64
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/daily-work-plans:
    get:
      tags:
        - daily-work-plans
      operationId: listDailyWorkPlans
      summary: List branch-scoped daily work plans
      description: Returns branch-scoped daily work plans (the approval queue) ordered newest plan date first, with NO status
        filter so DRAFT/REQUESTED plans surface to approvers. Admins see the whole org; other roles see their branch set.
      security:
      - bearerAuth: []
      parameters:
      - name: plan_date
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Date'
        description: Optional single-day filter (YYYY-MM-DD). Absent returns every day.
      responses:
        '200':
          description: Daily work plans for the caller's scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DailyPlanListPage'
    post:
      tags:
        - daily-work-plans
      operationId: createDailyWorkPlan
      summary: Create a daily work plan
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDailyPlanRequest'
      responses:
        '201':
          description: Daily plan created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DailyPlanSummary'
  /api/daily-work-plans/{planId}:
    get:
      tags:
        - daily-work-plans
      operationId: getDailyWorkPlan
      summary: Read a daily work plan by id
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PlanId'
      responses:
        '200':
          description: Daily plan found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DailyPlanSummary'
  /api/daily-work-plans/{planId}/confirm:
    post:
      tags:
        - daily-work-plans
      operationId: confirmDailyWorkPlan
      summary: Confirm an approved daily work plan
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PlanId'
      responses:
        '200':
          description: Daily plan confirmed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DailyPlanSummary'
  /api/daily-work-plans/{planId}/request-review:
    post:
      tags:
        - daily-work-plans
      operationId: requestDailyWorkPlanReview
      summary: Send a daily work plan for review
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PlanId'
      responses:
        '200':
          description: Daily plan moved to requested.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DailyPlanSummary'
  /api/daily-work-plans/{planId}/review:
    post:
      tags:
        - daily-work-plans
      operationId: reviewDailyWorkPlan
      summary: Review a daily work plan
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PlanId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReviewDailyPlanRequest'
      responses:
        '200':
          description: Daily plan reviewed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DailyPlanSummary'
  /api/messenger/channels:
    get:
      tags:
        - messenger
      operationId: listMessengerChannels
      summary: Discover joinable channels in the caller's branch scope
      description: 'Lists channel-visibility threads within a branch the caller is scoped to, whether or not the caller is
        already a member, so a member can find a room to join. Deny-by-omission: direct threads and out-of-scope channels
        are never returned.

        '
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 50
      responses:
        '200':
          description: Joinable channels visible to the caller.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerThreadListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/messenger/members:
    get:
      tags:
        - messenger
      operationId: listMessengerMembers
      summary: List active branch members eligible for messenger thread creation
      description: 'Authenticated branch members can discover active coworkers in a branch they are scoped to without requiring
        the admin-only user-management endpoint. The response intentionally excludes phone numbers and other profile fields
        that are not needed to start a work conversation.

        '
      security:
      - bearerAuth: []
      parameters:
      - name: branch_id
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 100
      responses:
        '200':
          description: Active messenger participants visible in the branch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerMemberListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/messenger/members/{userId}:
    get:
      tags:
        - messenger
      operationId: getMessengerMember
      summary: Fetch one branch member's summary for a person pin panel
      description: 'Returns a single active branch member''s summary using the same non-admin branch directory as the member
        list, so any employee can open a coworker''s person card. Viewing another person records a person.view audit event
        (열람 — 기록 남음); a self-view records none. A target outside the caller''s branch returns 404 with no audit trail
        (deny-by-omission).

        '
      security:
      - bearerAuth: []
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: branch_id
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The branch member's summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerMemberSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/messenger/messages/{messageId}/ack:
    post:
      tags:
        - messenger
      operationId: toggleMessengerMessageAck
      summary: Toggle the caller's ack on a message
      description: 'Toggle the caller''s ack: if present it is removed, else added. This is not idempotent
        and is not safe to blindly retry; clients should reconcile from the returned MessengerAckSummary
        acked and ack_count values. The caller must be a member of the message''s thread; a non-member gets
        403.

        '
      security:
      - bearerAuth: []
      parameters:
      - name: messageId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The message's post-toggle ack state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerAckSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/messenger/search:
    get:
      tags:
        - messenger
      operationId: searchMessengerMessages
      summary: Search branch-scoped messenger messages visible to the authenticated member
      security:
      - bearerAuth: []
      parameters:
      - name: q
        in: query
        required: true
        schema:
          type: string
          minLength: 1
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 50
      responses:
        '200':
          description: Matching messenger messages.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerMessageListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/messenger/threads:
    get:
      tags:
        - messenger
      operationId: listMessengerThreads
      summary: List branch-scoped messenger threads for the authenticated member
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 50
      responses:
        '200':
          description: Messenger threads visible to the authenticated member.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerThreadListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags:
        - messenger
      operationId: createMessengerThread
      summary: Create a branch-scoped messenger thread
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMessengerThreadRequest'
      responses:
        '201':
          description: Messenger thread created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerThreadSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/messenger/threads/{threadId}/join:
    post:
      tags:
        - messenger
      operationId: joinMessengerChannel
      summary: Join a channel thread the caller can see in scope
      description: 'Adds the caller as a member of a channel-visibility thread. Idempotent (re-joining is a no-op). A direct
        thread is not joinable and returns 403; a thread outside the caller''s branch scope returns 404 (deny-by-omission).

        '
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ThreadId'
      responses:
        '200':
          description: The thread the caller joined.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerThreadSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/messenger/threads/{threadId}/messages:
    get:
      tags:
        - messenger
      operationId: listMessengerMessages
      summary: Page messenger messages before an optional cursor
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ThreadId'
      - name: before_message_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 50
      responses:
        '200':
          description: Cursor-stable message page.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerMessagePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags:
        - messenger
      operationId: sendMessengerMessage
      summary: Send a message with optional evidence attachments
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ThreadId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendMessengerMessageRequest'
      responses:
        '201':
          description: Message persisted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerMessageSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/messenger/threads/{threadId}/mute:
    put:
      tags:
        - messenger
      operationId: setMessengerThreadMute
      summary: Set the caller's personal mute for a thread
      description: 'Direct-save personal setting. A muted thread still records every message; it only suppresses the caller''s
        mention notifications and is excluded from their unread badge total. The caller must be a thread member.

        '
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ThreadId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetMessengerThreadMuteRequest'
      responses:
        '200':
          description: The thread's post-toggle mute state for the caller.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerThreadMuteSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/messenger/threads/{threadId}/presence:
    get:
      tags:
        - messenger
      operationId: getMessengerThreadPresence
      summary: Activity-derived presence for members of a thread
      description: 'Returns online/away/offline per thread member, derived from the age of each member''s last real action
        (message/read/ack) — not a live socket, so "online" means "acted within the freshness window". The caller must be
        a thread member; a non-member gets 403 and sees no presence.

        '
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ThreadId'
      responses:
        '200':
          description: Presence of every member of the thread.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerMemberPresenceListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/messenger/threads/{threadId}/read-receipt:
    put:
      tags:
        - messenger
      operationId: markMessengerThreadRead
      summary: Mark the authenticated member's thread read cursor
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ThreadId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MarkMessengerThreadReadRequest'
      responses:
        '200':
          description: Read receipt upserted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessengerReadReceiptSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/objects/{kind}/{id}:
    get:
      tags:
        - objects
      operationId: resolveObject
      summary: Resolve any object to a compact head (code, title, status)
      description: 'Dereferences a (kind, id) pair to an ObjectHead so any object chip/code can be rendered and navigated.
        Reuses each domain''s tenant + branch scoping: an object outside the caller''s org/branch scope resolves identically
        to a missing id (exists=false), the deny-by-omission guarantee. A well-formed but unregistered kind returns 404.
        Routing is the frontend objectRegistry''s responsibility; this endpoint never returns a route/URL.'
      security:
      - bearerAuth: []
      parameters:
      - name: kind
        in: path
        required: true
        description: Object kind slug (e.g. work_order, equipment, support_ticket, org_unit, person, approval_run).
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: The resolved object head (exists=false when not visible/absent).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectHead'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/objects/{kind}/{id}/graph:
    get:
      tags:
        - objects
      operationId: getObjectGraph
      summary: Walk the bounded object-link neighborhood of an object
      description: 'Bounded level-by-level walk over object_links up to `depth` hops (clamped 1-5), org-scoped under
        RLS. Every returned node passed the SAME per-kind visibility guard as resolveObject; deny-by-omission governs
        discovery itself here, not just display: a node the caller cannot resolve is OMITTED (never returned as a
        stub) and the walk never expands through it, so an edge touching an omitted node is omitted too. `truncated`
        is true when the response was cut short by the node cap before `depth` was exhausted.'
      security:
      - bearerAuth: []
      parameters:
      - name: kind
        in: path
        required: true
        description: Root object kind slug.
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Root object id.
        schema:
          type: string
      - name: depth
        in: query
        required: false
        description: Walk depth in hops, clamped to 1-5 (default 1).
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: The bounded neighborhood of the root object (empty if the root itself does not resolve).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectGraphResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/platform/groups:
    get:
      tags:
        - platform
      operationId: listPlatformGroups
      summary: List platform groups and member organizations (platform vendor tier)
      description: Lists group identities and their subsidiary organization memberships. Platform token and GroupManage authority
        required.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Platform groups with member organization identities.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PlatformGroup'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    post:
      tags:
        - platform
      operationId: createPlatformGroup
      summary: Create a platform group identity (platform vendor tier)
      description: Creates a group identity that can own multiple tenant organizations. Platform token and GroupManage authority
        required.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePlatformGroupRequest'
      responses:
        '201':
          description: The created platform group.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformGroup'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/groups/{id}:
    patch:
      tags:
        - platform
      operationId: updatePlatformGroup
      summary: Update a platform group identity or lifecycle status
      description: Updates group slug, name, and/or status. Platform token and GroupManage authority required.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePlatformGroupRequest'
      responses:
        '200':
          description: The updated platform group.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformGroup'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/groups/{id}/accounts:
    get:
      tags:
        - platform
      operationId: listPlatformGroupAccounts
      summary: List tenant-anchored accounts with group roles
      description: Lists tenant user accounts that hold one or more grants for the selected platform group. Platform token and
        GroupManage authority required.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Group account summaries.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PlatformGroupAccount'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    post:
      tags:
        - platform
      operationId: createPlatformGroupAccount
      summary: Create a tenant-anchored user account with a group role
      description: Creates one tenant account in the target organization, grants a group role, and returns the one-time setup
        OTP exactly once. Tenant roles default to MEMBER and group_role defaults to GROUP_ADMIN when omitted.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePlatformGroupAccountRequest'
      responses:
        '201':
          description: The created group account plus one-time setup OTP.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatePlatformGroupAccountResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/groups/{id}/accounts/{user_id}/roles/{group_role}:
    delete:
      tags:
        - platform
      operationId: revokePlatformGroupRole
      summary: Revoke one group role from a tenant-anchored account
      description: Removes one group-level role grant from the selected account. Platform token and GroupManage authority required.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: user_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: group_role
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/PlatformGroupRole'
      responses:
        '204':
          description: The group role grant was revoked.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/groups/{id}/organizations/{org_id}:
    delete:
      tags:
        - platform
      operationId: removePlatformOrgFromGroup
      summary: Remove a tenant organization from a platform group
      description: Removes the tenant organization from the selected group. Platform token and GroupManage authority required;
        the write is audited to the target organization.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: org_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The tenant organization after group removal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformOrg'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    put:
      tags:
        - platform
      operationId: assignPlatformOrgToGroup
      summary: Assign or move a tenant organization into a platform group
      description: Assigns the tenant organization to the selected group. Platform token and GroupManage authority required;
        the write is audited to the target organization.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: org_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The tenant organization after group assignment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformOrg'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/ops:
    get:
      tags:
        - platform
      operationId: getPlatformOps
      summary: Read cross-tenant platform operations health (platform vendor tier)
      description: Returns per-tenant health and usage counts from the sanctioned SECURITY DEFINER platform rollup. Authorized
        with TenantHealthRead and audited as a platform read; no row-level tenant data is returned.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Platform operations dashboard rollup.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformOpsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/orgs:
    get:
      tags:
        - platform
      operationId: listPlatformOrgs
      summary: List tenant organizations (platform vendor tier)
      description: Lists all tenant organizations through the platform extractor. A tenant token is rejected before the handler
        runs; reads are authorized with TenantList and audited as a cross-tenant platform read.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Tenant organizations visible to the platform operator.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PlatformOrg'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    post:
      tags:
        - platform
      operationId: onboardPlatformOrg
      summary: Onboard a new tenant organization (platform vendor tier)
      description: Creates a tenant organization, seeds its first SUPER_ADMIN account, and returns the one-time bootstrap OTP
        exactly once. Platform token required; tenant tokens are rejected before the handler runs.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePlatformOrgRequest'
      responses:
        '201':
          description: Tenant onboarding succeeded; deliver the OTP out-of-band and do not log it.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformOrgOnboardingResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/orgs/{id}:
    delete:
      tags:
        - platform
      operationId: removePlatformOrg
      summary: Remove a tenant — guarded shell removal, or opt-in force-delete with data (platform vendor tier)
      description: 'Remove a tenant organization. Platform-super-admin (vendor tier) ONLY: the route sits behind the platform
        extractor, so a tenant token is rejected with 403 before the handler runs and a tenant''s own admin can never reach
        it.


        Two paths, selected by the opt-in `delete_data` query parameter (default false):


        - `delete_data=false` (default) — GUARDED removal, audited as `platform.tenant.remove`. Deletes only an empty/test
        tenant''s onboarding shell (the seeded admin user, its auth credentials, branch memberships, branches, and regions).
        REFUSED with 409 (`code` = `tenant_has_data`) when the tenant owns real operational data (equipment, work orders,
        sites, customers, inspections, sales, financial, messenger, consents, attendance, or governance findings) — archive
        the tenant instead.


        - `delete_data=true` — FORCE removal, audited as `platform.tenant.force_remove`. The DESTRUCTIVE path: erases the
        org AND all of its data. Fail-closed by a status rail — REFUSED with 409 (`code` = `tenant_active`) unless the tenant
        is ARCHIVED, so an active tenant can never be force-wiped by a single call; archive it (reversible) first.


        Both paths delete in one transaction and preserve the tenant''s immutable audit trail (re-homed to the platform sentinel).'
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: delete_data
        in: query
        required: false
        description: Opt-in FORCE removal. When true, erase the tenant AND all of its data (requires the tenant to be ARCHIVED
          first). Defaults to false — the guarded path that removes only an empty onboarding shell.
        schema:
          type: boolean
          default: false
      responses:
        '204':
          description: The tenant (and, when forced, all of its data) was removed.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The removal was refused. On the guarded path (`delete_data` false), the tenant has operational data
            and cannot be removed (error body `code` is `tenant_has_data`) — archive it instead. On the force path (`delete_data`
            true), the tenant is not ARCHIVED (error body `code` is `tenant_active`) — archive the tenant before force-removing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    patch:
      tags:
        - platform
      operationId: setPlatformOrgStatus
      summary: Change a tenant lifecycle status (platform vendor tier)
      description: Sets a tenant organization status to ACTIVE, SUSPENDED, or ARCHIVED. Platform token required; every write
        is cross-tenant and audited to the target organization.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePlatformOrgRequest'
      responses:
        '200':
          description: The updated tenant organization.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformOrg'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/tenant-context:
    post:
      tags:
        - platform
      operationId: startPlatformTenantContext
      summary: Start a writable platform-managed tenant context
      description: Mints a short-lived writable tenant token with SUPER_ADMIN for exactly one active tenant. Platform token and
        TenantManage authority required; the operator identity comes from the verified bearer token and the start is audited.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlatformTenantContextStartRequest'
      responses:
        '200':
          description: The short-lived writable tenant token and banner context.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformTenantContextStartResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/tenant-context/exit:
    post:
      tags:
        - platform
      operationId: exitPlatformTenantContext
      summary: End a writable platform-managed tenant context audit trail
      description: Records the platform.tenant_context.stop audit event. Called with the operator's real platform token after
        restoring the platform session.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The exit audit acknowledgement.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformExitResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/view-as:
    post:
      tags:
        - platform
      operationId: startPlatformViewAs
      summary: Start a read-only platform view-as session for one tenant
      description: Mints a short-lived tenant token pinned to the requested organization and role with view_as/read_only flags.
        Platform token and TenantHealthRead authority required; the operator identity comes from the verified bearer token.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlatformViewAsStartRequest'
      responses:
        '200':
          description: The short-lived read-only tenant token and banner context.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformViewAsStartResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/platform/view-as/exit:
    post:
      tags:
        - platform
      operationId: exitPlatformViewAs
      summary: End a read-only platform view-as session audit trail
      description: Records the platform.view_as.stop audit event. Called with the operator's real platform token, not the
        impersonation token.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The exit audit acknowledgement.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlatformExitResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/target-change-requests/{requestId}/review:
    post:
      tags:
        - target-change-requests
      operationId: reviewTargetChange
      summary: Review a target due-date change request
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/RequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReviewTargetChangeRequest'
      responses:
        '200':
          description: Target change review persisted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TargetChangeRequestSummary'
  /api/v1/analytics/projection:
    post:
      tags:
        - analytics
      operationId: computeAnalyticsProjection
      summary: Project a value series forward with a fat-tail confidence band
      description: >
        Deterministic, read-only projection. Given a historical value series and
        a horizon, returns a point estimate, a 95% confidence band, and a CVaR95
        (expected shortfall) under EWMA volatility + Student-t(ν=4) innovations,
        computed via a seeded Monte-Carlo (same input → same output) with an EVT
        (Generalized-Pareto) lower-tail fit. No persistence, no PII.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectionRequest'
      responses:
        '200':
          description: The projection result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectionResult'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/approval-inbox/bulk-tasks:
    get:
      tags:
        - workflow-tasks
      operationId: listBulkApprovalInbox
      summary: List canonical bulk-approval tasks
      description: A keyset-paginated, authorization-filtered personal inbox. Only explicit approval_decide workflow tasks are exposed; omitted rows do not affect page cardinality.
      security:
      - bearerAuth: []
      parameters:
      - name: cursor
        in: query
        required: false
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
      responses:
        '200':
          description: A page of authorization-filtered canonical approval tasks.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkApprovalInboxResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/attendance/closes:
    get:
      tags: [attendance]
      operationId: listAttendanceCloses
      summary: List branch-scoped close records
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: month, in: query, required: true, schema: { type: string, pattern: '^\\d{4}-\\d{2}$' } }
        - { name: branch_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
      responses:
        '200': { description: Typed close board, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceCloseBoard' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [attendance]
      operationId: closeAttendanceMonth
      summary: Commit a server-validated monthly close
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceCloseRequest' } } } }
      responses:
        '201': { description: Committed close, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceMonthClose' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/closes/preflight:
    post:
      tags: [attendance]
      operationId: preflightAttendanceClose
      summary: Calculate close gates without writing a close
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceCloseRequest' } } } }
      responses:
        '200': { description: Server-derived close readiness, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceClosePreflight' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/closes/{close_id}/amendments:
    post:
      tags: [attendance]
      operationId: amendAttendanceClose
      summary: Append an idempotent post-close amendment
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: close_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 1, maxLength: 200 } }
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceCloseAmendmentRequest' } } } }
      responses:
        '200': { description: Recorded amendment, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceCloseAmendment' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/exceptions:
    get:
      tags: [attendance]
      operationId: listAttendanceExceptions
      summary: List branch-scoped attendance exceptions
      security: [{ bearerAuth: [] }]
      parameters:
        # Supply exactly one selector: `month`, `work_date`, or the complete
        # `from_date` + `to_date` pair. The server rejects ambiguous, absent,
        # and partial ranges.
        - { name: month, in: query, required: false, schema: { type: string, pattern: '^\\d{4}-\\d{2}$' } }
        - { name: work_date, in: query, required: false, schema: { type: string, format: date } }
        - { name: from_date, in: query, required: false, schema: { type: string, format: date } }
        - { name: to_date, in: query, required: false, schema: { type: string, format: date } }
        - { name: status, in: query, required: false, schema: { type: string, enum: [OPEN, RESOLVED] } }
        - { name: employee_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: branch_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 200 } }
        - { name: offset, in: query, required: false, schema: { type: integer, minimum: 0 } }
      responses:
        '200': { description: Typed exception page, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceExceptionPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [attendance]
      operationId: raiseAttendanceException
      summary: Raise an idempotent attendance exception
      security: [{ bearerAuth: [] }]
      parameters: [{ name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 1, maxLength: 200 } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RaiseAttendanceExceptionRequest' } } } }
      responses:
        '201': { description: Created exception, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceException' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/exceptions/{exception_id}:
    get:
      tags: [attendance]
      operationId: getAttendanceException
      summary: Get one branch-authorized attendance exception
      security: [{ bearerAuth: [] }]
      parameters: [{ name: exception_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses:
        '200': { description: Typed exception detail, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceException' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/attendance/exceptions/{exception_id}/resolve:
    post:
      tags: [attendance]
      operationId: resolveAttendanceException
      summary: Resolve an exception with an auditable reason
      security: [{ bearerAuth: [] }]
      parameters: [{ name: exception_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ResolveAttendanceExceptionRequest' } } } }
      responses:
        '200': { description: Resolved exception, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceException' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/me/exceptions:
    get:
      tags: [attendance]
      operationId: listMyAttendanceExceptions
      summary: List only the signed principal's linked employee exceptions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: month, in: query, required: false, schema: { type: string, pattern: '^\\d{4}-\\d{2}$' } }
        - { name: work_date, in: query, required: false, schema: { type: string, format: date } }
        - { name: from_date, in: query, required: false, schema: { type: string, format: date } }
        - { name: to_date, in: query, required: false, schema: { type: string, format: date } }
        - { name: status, in: query, required: false, schema: { type: string, enum: [OPEN, RESOLVED] } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 200 } }
        - { name: offset, in: query, required: false, schema: { type: integer, minimum: 0, maximum: 1000000 } }
      responses:
        '200': { description: "Linked employee exception page, or an empty page when the principal is unlinked", content: { application/json: { schema: { $ref: '#/components/schemas/OwnAttendanceExceptionPage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/me/week52:
    get:
      tags: [attendance]
      operationId: getMyAttendanceWeek52
      summary: Get the signed principal's linked employee Week-52 projection
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: week_start, in: query, required: true, schema: { type: string, format: date }, description: ISO week Monday. }
      responses:
        '200': { description: "Linked employee Week-52 availability and projection", content: { application/json: { schema: { $ref: '#/components/schemas/OwnAttendanceWeek52Response' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/substitution-candidates:
    get:
      tags: [attendance]
      operationId: listAttendanceSubstitutionCandidates
      summary: List eligible substitute candidates for one covered shift
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: branch_id, in: query, required: true, schema: { $ref: '#/components/schemas/Uuid' }, description: Required even for organization-wide principals. }
        - { name: covered_employee_id, in: query, required: true, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: cover_date, in: query, required: true, schema: { type: string, format: date } }
        - { name: from_minutes, in: query, required: true, schema: { type: integer, minimum: 0, maximum: 1439 } }
        - { name: to_minutes, in: query, required: true, schema: { type: integer, minimum: 1, maximum: 1440 } }
        - { name: search, in: query, required: false, schema: { type: string, maxLength: 120 } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 200 } }
        - { name: offset, in: query, required: false, schema: { type: integer, minimum: 0, maximum: 1000000 } }
      responses:
        '200': { description: Eligible substitute candidate page, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceSubstitutionCandidatePage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/substitutions:
    get:
      tags: [attendance]
      operationId: listAttendanceSubstitutions
      summary: List branch-scoped substitute assignments
      security: [{ bearerAuth: [] }]
      parameters:
        # Supply exactly one selector: `month`, `work_date`, or the complete
        # `from_date` + `to_date` pair. The server rejects ambiguous, absent,
        # and partial ranges.
        - { name: month, in: query, required: false, schema: { type: string, pattern: '^\\d{4}-\\d{2}$' } }
        - { name: work_date, in: query, required: false, schema: { type: string, format: date } }
        - { name: from_date, in: query, required: false, schema: { type: string, format: date } }
        - { name: to_date, in: query, required: false, schema: { type: string, format: date } }
        - { name: branch_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 200 } }
        - { name: offset, in: query, required: false, schema: { type: integer, minimum: 0 } }
      responses:
        '200': { description: Typed substitution page, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceSubstitutionPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [attendance]
      operationId: assignAttendanceSubstitute
      summary: Create an idempotent substitute assignment
      security: [{ bearerAuth: [] }]
      parameters: [{ name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 1, maxLength: 200 } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AssignAttendanceSubstituteRequest' } } } }
      responses:
        '201': { description: Created assignment, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceSubstitution' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/substitutions/{substitution_id}/cancel:
    post:
      tags: [attendance]
      operationId: cancelAttendanceSubstitution
      summary: Cancel a substitute assignment
      security: [{ bearerAuth: [] }]
      parameters: [{ name: substitution_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CancelAttendanceSubstitutionRequest' } } } }
      responses:
        '200': { description: Cancelled assignment, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceSubstitution' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/week52:
    get:
      tags: [attendance]
      operationId: listAttendanceWeek52
      summary: List weekly working-hour projections
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: week_start, in: query, required: true, schema: { type: string, format: date } }
        - { name: branch_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
      responses:
        '200': { description: Week-52 projection board, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceWeek52Board' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/attendance/week52/acks:
    post:
      tags: [attendance]
      operationId: acknowledgeAttendanceWeek52
      summary: Idempotently acknowledge a week-52 adjustment
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceWeek52AckRequest' } } } }
      responses:
        '200': { description: Acknowledged complete Week-52 row, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceWeek52Row' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/audit-streams/ceo-covert/access-events:
    get:
      tags:
        - audit
      operationId: listCeoCovertAuditAccessEvents
      summary: Read the audit-of-access stream for CEO/top-clearance audit reads
      description: Cedar/PBAC clearance-gated access log for reads of the CEO covert audit stream. Successful reads of this
        stream are themselves audited.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 100
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paged audit-stream access events, plus the audit-of-access event id created by this read.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditStreamPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/audit-streams/ceo-covert/events:
    get:
      tags:
        - audit
      operationId: listCeoCovertAuditEvents
      summary: Read the CEO/top-clearance covert audit stream
      description: Cedar/PBAC clearance-gated stream of CEO/top-clearance-labeled audit events. The backend loads clearance
        facts under org-scoped RLS and records an audit-of-access event for every successful read.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 100
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paged covert audit events, plus the audit-of-access event id created by this read.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditStreamPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/audit/attestation:
    get:
      tags:
        - audit
      operationId: getAuditChainAttestation
      summary: L20 tamper-evident audit-chain attestation for the caller's tenant
      description: 'Read-only recompute-and-compare verdict for the org''s cryptographically-sealed audit-event hash
        chain: walks every stored seal, re-derives batch_hash/seal_hash from the underlying audit_events, checks
        chain continuity and coverage (no committed row sits in a gap the sealed ranges do not cover), and verifies
        the signature. `ok` reflects tamper integrity only; `unsealed_tail` is a separate freshness signal (rows
        committed but not yet sealed by the background worker) and never forces `ok=false` on a healthy live chain.
        Unlike GET /api/audit, which can branch-filter rows for branch-scoped admins, this whole-tenant attestation
        requires org-wide AuditLogRead authority; built-in access for this feature is SUPER_ADMIN, not ADMIN.
        Never mutates.'
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The org's audit-chain integrity verdict.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditChainAttestation'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          description: verify_org_chain hit a genuine DB/infra error while recomputing the chain (never on
            a tamper finding, which is a 200 with ok=false).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: JWT verification is not configured, or the database is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/auth/admin/credential-reset:
    post:
      tags:
        - auth
      deprecated: true
      summary: Admin credential-reset (HOLD — not generally available)
      description: HOLD — not generally available. Live credential-reset remains HOLD without separate
        authority and evidence. This path is retained so the published contract names the HOLD; it is
        not a shipped recovery API.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminCredentialResetRequest'
      responses:
        '200':
          description: HOLD — not generally available; not a shipped recovery API.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCredentialResetResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/auth/admin/otp/issue:
    post:
      tags:
        - auth
      summary: Issue a one-time sign-in code (admin)
      description: Issues a single-use, high-entropy 8-character one-time code for a pre-provisioned zero-credential user
        so they can perform their first sign-in. Authz-gated to ADMIN / SUPER_ADMIN within branch scope. The code is returned
        once; only its hash is stored. `ttl_seconds` is optional and defaults to 24 hours.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminIssueOtpRequest'
      responses:
        '200':
          description: The issued one-time code and its expiry.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminIssueOtpResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/auth/device-login/approve:
    post:
      tags:
        - auth
      security: []
      operationId: approveDeviceLogin
      summary: Approve a desktop QR login with an existing phone passkey
      description: Phone-side approval for an existing passkey user. The phone submits the approve token plus a usernameless
        passkey assertion; the approved user is resolved from that assertion. This endpoint does not mint a phone token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceLoginApproveRequest'
      responses:
        '204':
          description: The desktop handoff was approved.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /api/v1/auth/device-login/approve-session:
    post:
      tags:
        - auth
      operationId: approveDeviceLoginSession
      summary: Approve a desktop QR login from an authenticated phone session
      description: Phone-side approval used after first-time QR enrollment. The phone has redeemed the handoff OTP and enrolled
        a passkey; this authenticated call links that phone-registered passkey back to the waiting desktop poll token.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceLoginApproveSessionRequest'
      responses:
        '204':
          description: The desktop handoff was approved from the authenticated phone session.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /api/v1/auth/device-login/poll:
    post:
      tags:
        - auth
      security: []
      operationId: pollDeviceLogin
      summary: Poll a desktop QR login handoff
      description: Desktop-only polling endpoint. Pending/expired responses carry no tokens. Once a phone has approved the
        paired approve token, this endpoint consumes the poll token exactly once and returns a normal token pair; in web cookie
        transport the refresh token is set as `console_refresh` and is omitted from the JSON body.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceLoginPollRequest'
      responses:
        '200':
          description: Pending, expired, or approved desktop-login status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeviceLoginPollResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /api/v1/auth/device-login/start:
    post:
      tags:
        - auth
      security: []
      operationId: startDeviceLogin
      summary: Start desktop QR login handoff
      description: Creates a short-lived desktop login handoff. The desktop keeps the `poll_token` and renders `approve_url`
        as a QR. A phone opens the approve URL and proves a passkey; the desktop then polls once for its own token pair. No
        phone session is minted by this endpoint.
      responses:
        '200':
          description: Poll token and phone approval URL for the desktop QR.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeviceLoginStartResponse'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /api/v1/auth/logout:
    post:
      tags:
        - auth
      security: []
      summary: Logout and revoke refresh-token family
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LogoutRequest'
      responses:
        '204':
          description: Refresh-token family revoked.
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/auth/otp/redeem:
    post:
      tags:
        - auth
      security: []
      summary: First sign-in by redeeming a one-time code
      description: First sign-in for a pre-provisioned user. Redeems a single-use, expiring one-time code (admin-issued, or
        the cold-start secret) and mints a normal session token pair. The code is consumed atomically on success only; a wrong
        or expired code returns a single generic 401. Rate-limited per client (IP and optional device). `requires_passkey_setup`
        is true when the user has no passkey yet, so the client should force passkey enrollment in initial settings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OtpRedeemRequest'
      responses:
        '200':
          description: Session token pair plus passkey-setup flag.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OtpRedeemResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /api/v1/auth/passkey/enroll-handoff:
    post:
      tags:
        - auth
      summary: Mint a cross-device passkey-enrollment handoff (self)
      description: Mints a fresh single-use, short-lived (5 minutes) one-time code for the CURRENTLY AUTHENTICATED user so
        they can finish passkey enrollment on a SECOND device (typically a phone scanning a QR shown on the desktop console).
        SELF-ONLY — the user and org come from the verified access token, never the request body, so a caller can only ever
        mint a handoff for itself. When the caller is ALREADY enrolled (adding a device), a fresh `step_up` assertion of an
        existing passkey (user verification required) is mandatory, exactly like `passkey/register/start`; a mid-onboarding
        caller (no passkey yet) omits it. The response returns the code once (only its hash is stored) plus the ready-to-encode
        enrollment URL. The phone opens that URL, redeems the code via `POST /api/v1/auth/otp/redeem`, and enrolls a platform
        passkey — no Bluetooth required.
      security:
      - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnrollHandoffRequest'
      responses:
        '200':
          description: The minted single-use enrollment code, its expiry, and the QR enrollment URL.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnrollHandoffResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/auth/passkey/login/finish:
    post:
      tags:
        - auth
      security: []
      summary: Finish passkey login
      description: Verifies the passkey assertion and issues an ES256 access JWT plus an opaque rotating refresh token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasskeyLoginFinishRequest'
      responses:
        '200':
          description: Access and refresh tokens.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenPairResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/auth/passkey/login/start:
    post:
      tags:
        - auth
      security: []
      summary: Start usernameless passkey login
      description: Starts a discoverable (usernameless) authentication ceremony. No request body is required; the challenge
        has an empty allowCredentials list and the user is resolved from the asserted credential at finish. Rate-limited per
        client.
      responses:
        '200':
          description: Authentication ceremony challenge.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasskeyLoginStartResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /api/v1/auth/passkey/register/finish:
    post:
      tags:
        - auth
      summary: Finish passkey registration (authenticated)
      description: Finishes a passkey registration ceremony for the authenticated session user.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasskeyRegisterFinishRequest'
      responses:
        '201':
          description: Passkey registered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasskeyRegisterFinishResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/auth/passkey/register/start:
    post:
      tags:
        - auth
      summary: Start passkey registration (authenticated)
      description: Starts a passkey registration ceremony for the authenticated session user. Used during initial-settings
        passkey enrollment after an OTP first sign-in, or to add a device later. Requires a bearer token. Adding a passkey
        when the user already has one requires a fresh `step_up` assertion of an existing passkey (user verification required);
        omitting it returns 401. Initial enrollment (zero existing passkeys) needs no step-up.
      security:
      - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasskeyRegisterStartRequest'
      responses:
        '200':
          description: Registration ceremony challenge.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasskeyRegisterStartResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/auth/passkey/step-up/start:
    post:
      tags:
        - auth
      operationId: startMobilePasskeyStepUp
      summary: Start an authenticated action-bound mobile passkey step-up ceremony
      description: Starts a discoverable WebAuthn ceremony for a signed-in native client and persists the submitted action
        binding beside the ceremony. The resulting assertion may authorize only the bound approval or poll-vote action.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MobilePasskeyStepUpStartRequest'
      responses:
        '200':
          description: Action-bound authentication ceremony challenge.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MobilePasskeyStepUpStartResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/ValidationError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /api/v1/auth/passkeys:
    get:
      tags:
        - auth
      operationId: listAuthPasskeys
      summary: List the authenticated user's own passkey credentials
      description: Returns the caller's own WebAuthn credentials (id and registration / last-use timestamps) through the auth
        namespace. This self-only route is not behind tenant middleware, so it works for both tenant sessions and platform-admin
        sessions. No secret material is ever returned.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's passkey credentials.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PasskeySummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/auth/passkeys/{id}:
    delete:
      tags:
        - auth
      operationId: deleteAuthPasskey
      summary: Revoke one of the authenticated user's own passkey credentials
      description: Deletes a credential owned by the caller through the auth namespace. A credential that is not the caller's
        own returns 404. The caller's last remaining passkey cannot be deleted (409) so they do not lock themselves out. This
        route works for both platform-admin and tenant sessions.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '204':
          description: The passkey was revoked.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/auth/privacy-consent/accept:
    post:
      tags:
        - auth
      summary: Accept required first-login privacy terms
      description: Persists the authenticated user's required initial-login acknowledgement of the privacy collection/use
        notice and service terms as a tenant-scoped audit event. Both required agreements must be accepted explicitly; optional
        marketing and GPS/location consent are excluded.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PrivacyConsentAcceptRequest'
      responses:
        '200':
          description: The accepted current required privacy consent status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrivacyConsentStatusResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/auth/privacy-consent/status:
    post:
      tags:
        - auth
      summary: Read required first-login privacy consent status
      description: Returns whether the authenticated user has accepted the current required Korean privacy collection/use
        notice and service terms version. This status is checked before initial passkey enrollment. Optional marketing consent
        and GPS/location consent are not bundled here and remain separate flows.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Current required privacy consent status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrivacyConsentStatusResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/auth/signup:
    post:
      tags:
        - auth
      security: []
      summary: Open self-service signup
      description: Public, unauthenticated open signup. Creates a new lowest-privilege MEMBER account in the default organization
        and emails it a single-use one-time sign-in code; the caller then redeems that code via `POST /api/v1/auth/otp/redeem`
        and enrolls a passkey. The response reveals nothing about whether the email was newly registered (no account-existence
        oracle) and never mints a token. The new account sees the minimal role-gated surface until an admin elevates it. Rate-limited
        per client (IP and optional device).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignupRequest'
      responses:
        '202':
          description: The signup was accepted and a one-time code was sent.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignupResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '502':
          $ref: '#/components/responses/BadGateway'
  /api/v1/auth/token/refresh:
    post:
      tags:
        - auth
      security: []
      summary: Rotate refresh token
      description: Rotates an opaque refresh token; reuse of an old token revokes the whole family and returns 401.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenRequest'
      responses:
        '200':
          description: New access and refresh tokens.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenPairResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /api/v1/benefit-catalog/items:
    get:
      tags: [benefit-catalog]
      operationId: listBenefitCatalogItems
      summary: List tenant-scoped legal or optional benefit catalog items
      security: [{ bearerAuth: [] }]
      parameters:
      - { name: category, in: query, schema: { type: string, enum: [legal, extra] } }
      - { name: branchId, in: query, schema: { $ref: '#/components/schemas/Uuid' } }
      - { name: siteId, in: query, schema: { $ref: '#/components/schemas/Uuid' } }
      - { name: q, in: query, schema: { type: string } }
      - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100 } }
      - { name: offset, in: query, schema: { type: integer, minimum: 0 } }
      responses:
        '200': { description: Tenant-visible benefit rows, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogItemPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [benefit-catalog]
      operationId: createBenefitCatalogItem
      summary: Create an audited benefit catalog item
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogCreateRequest' } } } }
      responses:
        '201': { description: Created audited row, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogItem' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/benefit-catalog/items/{benefit_id}:
    get:
      tags: [benefit-catalog]
      operationId: getBenefitCatalogItem
      summary: Read one tenant-visible benefit catalog item
      security: [{ bearerAuth: [] }]
      parameters: [{ name: benefit_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses:
        '200': { description: Benefit row, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogItem' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    patch:
      tags: [benefit-catalog]
      operationId: updateBenefitCatalogItem
      summary: Update an audited benefit catalog item
      security: [{ bearerAuth: [] }]
      parameters: [{ name: benefit_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogUpdateRequest' } } } }
      responses:
        '200': { description: Updated row, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogItem' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/benefit-catalog/items/{benefit_id}/conditions:
    put:
      tags: [benefit-catalog]
      operationId: replaceBenefitCatalogConditions
      summary: Replace audited eligibility condition rows without physical delete
      security: [{ bearerAuth: [] }]
      parameters: [{ name: benefit_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogReplaceConditionsRequest' } } } }
      responses:
        '200': { description: Updated row, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogItem' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/benefit-catalog/items/{benefit_id}/tiers:
    put:
      tags: [benefit-catalog]
      operationId: replaceBenefitCatalogTiers
      summary: Replace audited tier rows without physical delete
      security: [{ bearerAuth: [] }]
      parameters: [{ name: benefit_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogReplaceTiersRequest' } } } }
      responses:
        '200': { description: Updated row, content: { application/json: { schema: { $ref: '#/components/schemas/BenefitCatalogItem' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/branches:
    get:
      tags:
        - branches
      operationId: listBranches
      summary: List all branches (also backs support-ticket triage)
      security:
      - bearerAuth: []
      responses:
        '200':
          description: All branches.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BranchSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    post:
      tags:
        - branches
      operationId: createBranch
      summary: Propose creating a branch inside a region
      description: Creates a governed DRAFT containing one CREATE_BRANCH proposal. The live branches table is unchanged
        until the org-change lifecycle is approved and effectuated. Requires an Idempotency-Key; an exact replay returns
        the existing org-change request with 200.
      security:
      - bearerAuth: []
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        schema:
          type: string
          minLength: 16
          maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBranchRequest'
      responses:
        '200':
          description: Idempotent replay of the existing org-change request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '201':
          description: Governed branch-create draft created; no branch row was applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/branches/{id}:
    delete:
      tags:
        - branches
      operationId: deactivateBranch
      summary: Propose deactivating a branch
      description: Creates a governed DRAFT containing one DEACTIVATE_BRANCH proposal. The live row is unchanged; preflight
        and apply retain the active-user and equipment guards.
      security:
      - bearerAuth: []
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        schema:
          type: string
          minLength: 16
          maxLength: 200
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Idempotent replay of the existing org-change request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '201':
          description: Governed branch-deactivation draft created; no branch row was applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    get:
      tags:
        - branches
      operationId: getBranch
      summary: Fetch one branch's summary for an org-unit pin panel
      description: 'Returns a single branch summary using the same non-sensitive read gate as the branch list; org-RLS scopes
        it to the caller''s org. No audit — org-structure metadata, not PII. A branch outside the caller''s org returns 404.

        '
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The branch summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BranchSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags:
        - branches
      operationId: updateBranch
      summary: Rename a branch or move it to another region
      description: Creates a governed DRAFT containing one RENAME_BRANCH proposal; it does not mutate the live row.
      security:
      - bearerAuth: []
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        schema:
          type: string
          minLength: 16
          maxLength: 200
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBranchRequest'
      responses:
        '200':
          description: Idempotent replay of the existing org-change request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '201':
          description: Governed branch-update draft created; no branch row was applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/collaboration/calendar/events:
    get:
      tags:
        - collaboration
      operationId: listCollaborationCalendarEvents
      summary: List tenant-scoped collaboration calendar events
      description: Returns server-filtered collaboration calendar events for the caller's tenant. Personal events are visible
        only to the creator, while org/team/department scopes carry explicit server policy metadata for future PBAC/ABAC expansion.
      security:
      - bearerAuth: []
      parameters:
      - name: from
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: to
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 30
      responses:
        '200':
          description: Calendar events visible to the caller.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalendarEventListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
    post:
      tags:
        - collaboration
      operationId: createCollaborationCalendarEvent
      summary: Create an audited collaboration calendar event
      description: Creates a tenant-scoped calendar event with explicit audience scope and optional source object link. The
        server records an audit event and an append-only lifecycle event under forced RLS.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCalendarEventRequest'
      responses:
        '200':
          description: Created calendar event.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalendarEventResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/collaboration/polls:
    get:
      tags:
        - collaboration
      operationId: listCollaborationPolls
      summary: List collaboration polls
      description: Returns open collaboration polls by default with aggregate option counts, caller vote state, target-scope
        policy metadata, and source-object links. Anonymous polls expose aggregate results but still keep server audit lineage.
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/PollStatus'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 30
      responses:
        '200':
          description: Polls visible to the caller.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PollListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
    post:
      tags:
        - collaboration
      operationId: createCollaborationPoll
      summary: Create an audited collaboration poll
      description: Creates a poll with explicit target scope, anonymous/named vote mode, source-object link, option validation,
        audit event, and append-only lifecycle event. The UI no longer depends on placeholder/static poll text.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePollRequest'
      responses:
        '200':
          description: Created poll.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PollResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/collaboration/polls/{id}/vote:
    post:
      tags:
        - collaboration
      operationId: voteCollaborationPoll
      summary: Submit or replace the caller's poll vote
      description: Upserts the caller's vote after validating poll lifecycle, option ownership, single-vs-multiple selection
        policy, tenant RLS, and audit/lifecycle evidence.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VotePollRequest'
      responses:
        '200':
          description: Updated poll with aggregate counts and caller vote state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PollResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/companies:
    get:
      operationId: listCompanies
      summary: List canonical Company heads
      description: >-
        Returns zero or one current Company Head for the armed tenant — the same
        row `list` already returns. The tenant IS the company (`org_id`). as_of /
        from / to are omitted: `company_revisions` has no valid-time columns, and
        using `created_at` as `valid_from` would be a second time model. Empty when
        the tenant has no revision. `legal_name` / `reg_no` come from the latest
        revision attributes, never from provisioning-owned `organizations.name`.
        EmployeeDetail as_of remains HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      responses:
        '200':
          description: Current Company heads for the armed tenant (zero or one).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Company'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/companies/{id}:
    get:
      operationId: getCompany
      summary: Get a canonical Company head
      description: >-
        Returns one Company Head for the armed tenant. The tenant IS the company:
        path `id` is `org_id`. as_of is omitted: `company_revisions` has no
        valid-time columns, and using `created_at` as `valid_from` would be a second
        time model. A path id that is not this tenant, or a tenant with no revision,
        is 404 (deny-by-omission). `legal_name` / `reg_no` come from the latest
        revision attributes, never from provisioning-owned `organizations.name`.
        EmployeeDetail as_of remains HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Current Company head.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Company'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/compliance/control-obligation-coverage:
    post:
      tags: [Compliance]
      operationId: linkComplianceControlObligation
      summary: Create an audited control-to-obligation coverage link
      description: Relation links are write-only in this catalog API; no coverage-list route is implemented.
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/LinkControlObligationRequest' } } } }
      responses:
        '200': { description: Created control-obligation coverage link., content: { application/json: { schema: { $ref: '#/components/schemas/ControlObligationCoverage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/compliance/evidence-bindings:
    get:
      tags: [Compliance]
      operationId: listComplianceEvidenceBindings
      summary: List compliance evidence bindings
      security: [{ bearerAuth: [] }]
      parameters:
      - { name: control_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
      - { name: obligation_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
      - { name: target_type, in: query, required: false, schema: { $ref: '#/components/schemas/EvidenceTargetType' } }
      - { name: status, in: query, required: false, schema: { $ref: '#/components/schemas/EvidenceBindingStatus' } }
      - { name: limit, in: query, required: false, schema: { type: integer, format: int64, minimum: 1, maximum: 1000, default: 100 } }
      - { name: offset, in: query, required: false, schema: { type: integer, format: int64, minimum: 0, default: 0 } }
      responses:
        '200': { description: Paged evidence bindings., content: { application/json: { schema: { $ref: '#/components/schemas/EvidenceBindingPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
    post:
      tags: [Compliance]
      operationId: createComplianceEvidenceBinding
      summary: Create an audited evidence binding
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateEvidenceBindingRequest' } } } }
      responses:
        '200': { description: Created evidence binding., content: { application/json: { schema: { $ref: '#/components/schemas/EvidenceBinding' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/compliance/evidence-bindings/{id}/accept:
    post:
      tags: [Compliance]
      operationId: acceptComplianceEvidenceBinding
      summary: Accept a proposed compliance evidence binding
      description: >-
        Applies the immutable PROPOSED-to-ACCEPTED lifecycle action. The server
        preserves evidence identity and provenance and records an audit snapshot.
      security: [{ bearerAuth: [] }]
      parameters:
      - { name: id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }
      responses:
        '200': { description: Accepted evidence binding., content: { application/json: { schema: { $ref: '#/components/schemas/EvidenceBinding' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/compliance/framework-controls:
    get:
      tags: [Compliance]
      operationId: listComplianceFrameworkControls
      summary: List controls for one framework
      security: [{ bearerAuth: [] }]
      parameters:
      - { name: framework_id, in: query, required: true, schema: { $ref: '#/components/schemas/Uuid' } }
      - { name: q, in: query, required: false, schema: { type: string } }
      - { name: status, in: query, required: false, schema: { $ref: '#/components/schemas/ControlStatus' } }
      - { name: limit, in: query, required: false, schema: { type: integer, format: int64, minimum: 1, maximum: 1000, default: 100 } }
      - { name: offset, in: query, required: false, schema: { type: integer, format: int64, minimum: 0, default: 0 } }
      responses:
        '200': { description: Paged controls for the framework., content: { application/json: { schema: { $ref: '#/components/schemas/ComplianceControlPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
    post:
      tags: [Compliance]
      operationId: createComplianceFrameworkControl
      summary: Create an audited framework control
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateComplianceControlRequest' } } } }
      responses:
        '200': { description: Created framework control., content: { application/json: { schema: { $ref: '#/components/schemas/ComplianceControl' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/compliance/frameworks:
    get:
      tags: [Compliance]
      operationId: listComplianceFrameworks
      summary: List compliance frameworks
      security: [{ bearerAuth: [] }]
      parameters:
      - { name: q, in: query, required: false, schema: { type: string } }
      - { name: status, in: query, required: false, schema: { $ref: '#/components/schemas/FrameworkStatus' } }
      - { name: kind, in: query, required: false, schema: { $ref: '#/components/schemas/FrameworkKind' } }
      - { name: limit, in: query, required: false, schema: { type: integer, format: int64, minimum: 1, maximum: 1000, default: 100 } }
      - { name: offset, in: query, required: false, schema: { type: integer, format: int64, minimum: 0, default: 0 } }
      responses:
        '200': { description: Paged frameworks., content: { application/json: { schema: { $ref: '#/components/schemas/ComplianceFrameworkPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
    post:
      tags: [Compliance]
      operationId: createComplianceFramework
      summary: Create an audited compliance framework
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateComplianceFrameworkRequest' } } } }
      responses:
        '200': { description: Created framework., content: { application/json: { schema: { $ref: '#/components/schemas/ComplianceFramework' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/compliance/obligation-regulation-links:
    post:
      tags: [Compliance]
      operationId: linkComplianceObligationRegulation
      summary: Create an audited obligation-to-regulation link
      description: Relation links are write-only in this catalog API; no relation-list route is implemented.
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/LinkObligationRegulationRequest' } } } }
      responses:
        '200': { description: Created obligation-regulation link., content: { application/json: { schema: { $ref: '#/components/schemas/ObligationRegulationLink' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/compliance/obligations:
    get:
      tags: [Compliance]
      operationId: listComplianceObligations
      summary: List RLS-scoped compliance obligations
      security: [{ bearerAuth: [] }]
      parameters:
      - { name: q, in: query, required: false, schema: { type: string } }
      - { name: status, in: query, required: false, schema: { $ref: '#/components/schemas/ObligationStatus' } }
      - { name: severity, in: query, required: false, schema: { $ref: '#/components/schemas/ComplianceRiskLevel' } }
      - { name: scope_type, in: query, required: false, schema: { $ref: '#/components/schemas/ComplianceScopeKind' } }
      - { name: branch_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
      - { name: site_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
      - { name: limit, in: query, required: false, schema: { type: integer, format: int64, minimum: 1, maximum: 1000, default: 100 } }
      - { name: offset, in: query, required: false, schema: { type: integer, format: int64, minimum: 0, default: 0 } }
      responses:
        '200': { description: Paged obligations within the principal branch scope., content: { application/json: { schema: { $ref: '#/components/schemas/ComplianceObligationPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
    post:
      tags: [Compliance]
      operationId: createComplianceObligation
      summary: Create an audited obligation and optional regulation links
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateComplianceObligationRequest' } } } }
      responses:
        '200': { description: Created obligation., content: { application/json: { schema: { $ref: '#/components/schemas/ComplianceObligation' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/compliance/regulations:
    get:
      tags: [Compliance]
      operationId: listComplianceRegulations
      summary: List RLS-scoped regulation impacts
      security: [{ bearerAuth: [] }]
      parameters:
      - { name: q, in: query, required: false, schema: { type: string } }
      - { name: status, in: query, required: false, schema: { $ref: '#/components/schemas/RegulationImpactStatus' } }
      - { name: risk_level, in: query, required: false, schema: { $ref: '#/components/schemas/ComplianceRiskLevel' } }
      - { name: limit, in: query, required: false, schema: { type: integer, format: int64, minimum: 1, maximum: 1000, default: 100 } }
      - { name: offset, in: query, required: false, schema: { type: integer, format: int64, minimum: 0, default: 0 } }
      responses:
        '200': { description: Paged regulation impacts visible to the principal., content: { application/json: { schema: { $ref: '#/components/schemas/RegulationImpactPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
    post:
      tags: [Compliance]
      operationId: createComplianceRegulation
      summary: Create an audited regulation impact
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateRegulationImpactRequest' } } } }
      responses:
        '200': { description: Created regulation impact., content: { application/json: { schema: { $ref: '#/components/schemas/RegulationImpact' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/console/dispatch/queue:
    get:
      tags: [p1-dispatches]
      operationId: listConsoleDispatchQueue
      summary: List the bounded operational dispatch queue
      security: [{ bearerAuth: [] }]
      parameters:
      - name: status
        in: query
        style: form
        explode: false
        schema:
          type: array
          items:
            $ref: '#/components/schemas/DispatchQueueStatus'
        description: Comma-separated DispatchQueueStatus values serialized as one CSV query value.
      - name: limit
        in: query
        schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
      - name: after
        in: query
        schema: { type: string }
        description: Opaque cursor returned by a previous page.
      responses:
        '200': { description: Dispatch queue page., content: { application/json: { schema: { $ref: '#/components/schemas/DispatchQueuePage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/console/kill-switch:
    post:
      tags:
        - console
      operationId: updateConsoleLegacyKillSwitch
      summary: Force or release org-wide legacy-console routing
      description: Super-admin endpoint that overrides all individual new-console opt-ins and records an audit event.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - enabled
              properties:
                enabled:
                  type: boolean
                reason:
                  type: string
                rollout_note:
                  type: string
      responses:
        '200':
          description: Updated console rollout status after applying the kill switch.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/console/rollout:
    get:
      tags:
        - console
      operationId: getConsoleRollout
      summary: Read the current user's new-console rollout state
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Console rollout status.
          content:
            application/json:
              schema:
                type: object
                required:
                - flag_key
                - org_enabled
                - org_rollout_enabled
                - user_opted_in
                - legacy_kill_switch_enabled
                - kill_switch_active
                - effective_new_console
                - effective_route
                - effective_route_for_opted_in_user
                - effective_route_for_opted_out_user
                - overrides_individual_toggles
                properties:
                  flag_key:
                    type: string
                  org_enabled:
                    type: boolean
                  org_rollout_enabled:
                    type: boolean
                  user_opted_in:
                    type: boolean
                  legacy_kill_switch_enabled:
                    type: boolean
                  kill_switch_active:
                    type: boolean
                  effective_new_console:
                    type: boolean
                  effective_route:
                    type: string
                    enum: [legacy, new_console]
                  effective_route_for_opted_in_user:
                    type: string
                    enum: [legacy, new_console]
                  effective_route_for_opted_out_user:
                    type: string
                    enum: [legacy]
                  overrides_individual_toggles:
                    type: boolean
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/console/rollout/opt-in:
    put:
      tags:
        - console
      operationId: updateConsoleRolloutOptIn
      summary: Update the current user's new-console opt-in preference
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - opt_in
              properties:
                opt_in:
                  type: boolean
      responses:
        '200':
          description: Updated console rollout status.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/console/rollout/org-flag:
    put:
      tags:
        - console
      operationId: updateConsoleRolloutOrgFlag
      summary: Update the org-level new-console rollout flag
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - enabled
              properties:
                enabled:
                  type: boolean
                rollout_note:
                  type: string
      responses:
        '200':
          description: Updated console rollout status.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/console/telemetry/route:
    post:
      tags:
        - console
      operationId: recordConsoleRouteTelemetry
      summary: Record tenant console route-adoption/RUM telemetry
      description: Ingests one cardinality-safe route-selection or RUM event for the authenticated tenant. Org and user are derived
        from the bearer token; clients must not send raw URLs, query strings, secrets, or row-level payloads. Platform operators
        read only per-org aggregates through `GET /api/platform/ops`.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConsoleRouteTelemetryRequest'
      responses:
        '202':
          description: Telemetry event accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConsoleRouteTelemetryAccepted'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/consulting/engagements:
    get:
      tags: [consulting]
      operationId: listConsultingEngagements
      security: [{ bearerAuth: [] }]
      parameters:
      - { name: q, in: query, schema: { type: string } }
      - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100 } }
      - { name: offset, in: query, schema: { type: integer, minimum: 0 } }
      responses: { '200': { description: Tenant-visible engagements, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingEngagementPage' } } } } }
    post:
      tags: [consulting]
      operationId: createConsultingEngagement
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingEngagementCreateRequest' } } } }
      responses:
        '201': { description: Created or idempotently replayed original response for this exact idempotency key and request payload, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingEngagement' } } } }
        '409': { description: Idempotency key was previously used with a different request payload, content: { application/json: { schema: { $ref: '#/components/schemas/ErrorBody' } } } }
  /api/v1/consulting/engagements/{engagement_id}:
    get:
      tags: [consulting]
      operationId: getConsultingEngagement
      security: [{ bearerAuth: [] }]
      parameters: [{ name: engagement_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses: { '200': { description: Evidence and KPI-lineage detail, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingEngagementDetail' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/v1/consulting/engagements/{engagement_id}/diagnostics:
    post:
      tags: [consulting]
      operationId: createConsultingDiagnostic
      security: [{ bearerAuth: [] }]
      parameters: [{ name: engagement_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingDiagnosticCreateRequest' } } } }
      responses: { '201': { description: Created diagnostic, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingDiagnostic' } } } }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/v1/consulting/engagements/{engagement_id}/findings:
    post:
      tags: [consulting]
      operationId: createConsultingFinding
      security: [{ bearerAuth: [] }]
      parameters: [{ name: engagement_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingFindingCreateRequest' } } } }
      responses: { '201': { description: Created evidence-linked finding, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingFinding' } } } }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/v1/consulting/engagements/{engagement_id}/history:
    get:
      tags: [consulting]
      operationId: listConsultingEngagementHistory
      security: [{ bearerAuth: [] }]
      parameters: [{ name: engagement_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses:
        '200':
          description: Append-only engagement history
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/ConsultingHistoryEntry' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/consulting/engagements/{engagement_id}/initiatives:
    post:
      tags: [consulting]
      operationId: createConsultingInitiative
      security: [{ bearerAuth: [] }]
      parameters: [{ name: engagement_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingInitiativeCreateRequest' } } } }
      responses: { '201': { description: Created KPI-referenced initiative, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingInitiative' } } } }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/v1/consulting/engagements/{engagement_id}/observations:
    post:
      tags: [consulting]
      operationId: createConsultingBenefitObservation
      security: [{ bearerAuth: [] }]
      parameters: [{ name: engagement_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingObservationCreateRequest' } } } }
      responses: { '201': { description: KPI/evidence lineage reference, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingBenefitObservation' } } } }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/v1/consulting/engagements/{engagement_id}/transition:
    post:
      tags: [consulting]
      operationId: transitionConsultingEngagement
      security: [{ bearerAuth: [] }]
      parameters: [{ name: engagement_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingTransitionRequest' } } } }
      responses: { '200': { description: Transitioned engagement, content: { application/json: { schema: { $ref: '#/components/schemas/ConsultingEngagement' } } } }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/v1/customers:
    post:
      tags:
        - customers
      operationId: createCustomer
      summary: Create a customer (고객) directly
      description: Admin-gated (EquipmentManage, the same feature as the site PATCH). Creates a customer in the caller's org
        on the caller's branch (an org-wide SUPER_ADMIN/EXECUTIVE lands it on the default HQ branch). The name is required,
        trimmed, and bounded (≤ 200 characters). A same-name customer on that branch is a 409 conflict — an explicit create
        is a distinct intent from the importer's idempotent upsert, so it is surfaced, not merged.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCustomerRequest'
      responses:
        '201':
          description: Customer created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatedCustomer'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/devices:
    post:
      tags:
        - devices
      operationId: registerMobileDevice
      summary: Register or refresh a mobile device binding
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/XDeviceId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceRegistrationRequest'
      responses:
        '200':
          description: Registered device binding.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeviceRegistrationResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/directory/people:
    get:
      tags:
        - directory
      operationId: listDirectoryPeople
      summary: List people visible through the caller's effective directory scope
      description: >-
        Returns the tenant people directory after intersecting live branch
        membership with EmployeeDirectoryRead grants. A requested branch_id
        narrows that effective scope; it never widens it. Results are ordered by
        display_name then id, and branch_ids are redacted to the resulting scope.
        Items are DirectoryPerson records (UserSummary fields except phone).
      security:
      - bearerAuth: []
      parameters:
      - name: search
        in: query
        required: false
        description: Case-insensitive display-name search after trimming and lowercase normalization.
        schema:
          type: string
          maxLength: 200
      - name: team
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Team'
      - name: branch_id
        in: query
        required: false
        description: Exact branch filter, intersected with the caller's effective directory scope.
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: include_inactive
        in: query
        required: false
        description: Include archived users; defaults to false.
        schema:
          type: boolean
          default: false
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 200
          default: 50
          description: Values above 200 are rejected with 422; they are never silently clamped.
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Filtered page of people and the exact total within the effective directory scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DirectoryPage'
        '400':
          description: Malformed query string (for example an unparsable boolean or repeated scalar parameter); no directory query ran.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/employees:
    get:
      tags:
        - employees
      operationId: listEmployees
      summary: Paginated tenant HR employee directory
      description: Executive/admin/super-admin read of first-class employee rows. Employees are not auth users.
      security:
      - bearerAuth: []
      parameters:
      - name: company
        in: query
        required: false
        schema:
          type: string
        description: Filter by workbook sheet/company name.
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Case-insensitive name or employee-number typeahead query.
      - name: home_branch_review_required
        in: query
        required: false
        schema:
          type: boolean
        description: Filter to employees whose active authoritative home branch is missing/inactive (true) or usable (false).
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paginated employee list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags:
        - employees
      operationId: createEmployee
      summary: Create an employee and governed employment profile
      description: >-
        HR directory managers only. Creates the stable employee identity, employment
        profile, ONBOARD lifecycle event, and audit record atomically. Reusing an
        idempotency key replays the same payload; a different payload conflicts.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEmployeeRequest'
      responses:
        '201':
          description: Employee created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeDetail'
        '200':
          description: Idempotent replay of the original employee create.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/employees/export.csv:
    get:
      tags:
        - employees
      operationId: exportEmployeesCsv
      summary: Export standardized employee directory CSV
      description: Exports canonical employee fields only. Raw import ledger values, payroll data, account numbers, resident
        identifiers, and import provenance are not included.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Standardized employee CSV.
          content:
            text/csv:
              schema:
                type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/employees/import:
    post:
      tags:
        - employees
      operationId: importEmployees
      summary: Import payroll workbook sheets into the employee directory
      description: Admin/super-admin multipart .xlsx upload. Each worksheet is treated as a company, row 1 as headers, and
        rows with non-empty 성명 are upserted by deterministic source filename/sheet/row key. Raw row values and source metadata
        are preserved as JSONB.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Payroll employee .xlsx workbook.
      responses:
        '200':
          description: Import counts by company.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeImportReport'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/employees/import/preview:
    post:
      tags:
        - employees
      operationId: previewEmployeeImport
      summary: Preview an employee workbook through the governed import ledger
      description: Creates an immutable tenant-scoped raw import run, preserves every non-empty source row, masks restricted/PII/payroll/location
        values in the response, and returns a column-to-employee schema mapping preview. No employee rows are written until
        dry-run and apply are completed.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Employee .xlsx workbook.
      responses:
        '200':
          description: Import run preview with masked sample rows and mapping profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeImportPreviewResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/employees/import/{run_id}/apply:
    post:
      tags:
        - employees
      operationId: applyEmployeeImport
      summary: Apply a dry-run employee import run
      description: Applies only candidate rows from a DRY_RUN import run; preserved rows remain raw-ledger evidence and are
        not promoted into employee records.
      security:
      - bearerAuth: []
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImportApplyRequest'
      responses:
        '200':
          description: Applied import counts by company.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeImportReport'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/employees/import/{run_id}/dry-run:
    post:
      tags:
        - employees
      operationId: dryRunEmployeeImport
      summary: Compute employee import insert/update counts without writing employees
      security:
      - bearerAuth: []
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Dry-run candidate counts by company.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeImportDryRunSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/employees/{id}:
    get:
      tags:
        - employees
      operationId: getEmployeeDetail
      summary: Read privileged employee employment detail
      description: HR directory managers only. Includes compensation and normalized phone; the ordinary directory never includes these fields.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Privileged employee detail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/employees/{id}/home-branch:
    put:
      tags:
        - employees
      operationId: setEmployeeHomeBranch
      summary: Assign an employee's authoritative home branch
      description: >-
        Audited explicit assignment; the server never infers a branch from user
        memberships. The target branch must be active and in the same tenant.
        Reassignment requires EmployeeDirectoryManage for both the prior and new
        branch. An employee with no prior branch requires org-wide manage
        authority. `expected_updated_at` prevents silent concurrent overwrite.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetEmployeeHomeBranchRequest'
      responses:
        '200':
          description: The explicit authoritative home-branch assignment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeHomeBranch'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The employee changed after the supplied precondition; reload before retrying.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: The separately credentialed leave command database is not configured or unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/employees/{id}/lifecycle-events:
    get:
      tags:
        - employees
      operationId: listEmployeeLifecycleEvents
      summary: List audited employee lifecycle events
      description: Tenant-scoped append-only lifecycle ledger for onboarding, offboarding, termination, and intra-group/company
        transfer decisions.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Employee lifecycle events, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeLifecycleEventPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags:
        - employees
      operationId: createEmployeeLifecycleEvent
      summary: Record an employee lifecycle transition with legal signoffs
      description: Mutates the canonical employee status/company/org-unit/position only through an audited event requiring
        Korean labor, privacy, and payroll signoff gates.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEmployeeLifecycleEventRequest'
      responses:
        '200':
          description: Recorded lifecycle event.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeLifecycleEvent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/employments:
    get:
      operationId: listEmployments
      summary: List canonical Employment heads (current or overlapping from/to)
      description: >-
        Returns Employment Heads whose `[valid_from, valid_to)` overlaps the query
        window `[from, to)`. Absent from and to is current *open* heads
        (`employment_heads.valid_to IS NULL`), the same rows `get`/`list` already
        return. from and to copy the instance-GET Timestamp shape: optional RFC3339
        instants. Overlap is the same half-open algebra as GET
        /api/v1/employments/{id}?as_of= and GET /api/v1/ontology/instances/{id}?as_of=
        (`valid_from < to` AND `valid_to IS NULL OR from < valid_to`; a missing bound
        is unbounded). Each Head is reconstructed at `greatest(from, valid_from)` when
        from is set, otherwise at the latest revision with `valid_from < to`. Inverted
        `from >= to` is 422. Closed (EXITED) windows appear only when the query window
        overlaps them. The Head never includes phone, salary, bank_account, rrn, or
        base_pay. EmployeeDetail as_of remains HOLD. Corrections vs new slice and
        delta transmission remain HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      parameters:
      - name: from
        in: query
        required: false
        description: Inclusive RFC3339 start of the half-open collection window; absent = unbounded below.
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: to
        in: query
        required: false
        description: Exclusive RFC3339 end of the half-open collection window; absent = unbounded above.
        schema:
          $ref: '#/components/schemas/Timestamp'
      responses:
        '200':
          description: Employment heads overlapping [from, to), or current open heads when both are absent.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Employment'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/employments/{id}:
    get:
      operationId: getEmployment
      summary: Get a canonical Employment head (current or as-of)
      description: >-
        Returns one Employment Head. Absent as_of is the current *open* head
        (`employment_heads.valid_to IS NULL`), the same row `get`/`list` already
        return. as_of is the RFC3339 instant for a bi-temporal as-of read, copied
        from GET /api/v1/ontology/instances/{id}: the revision whose half-open
        window covers that instant (`valid_from <= as_of < coalesce(valid_to, ∞)`).
        A closed (EXITED) window is omitted by current get and by as_of at or after
        valid_to (deny-by-omission, same as a missing id). Collection from/to lives on
        GET /api/v1/employments (same half-open algebra). The Head never includes
        phone, salary, bank_account, rrn, or base_pay. EmployeeDetail as_of remains
        HOLD. Corrections vs new slice and delta transmission remain HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: as_of
        in: query
        required: false
        description: RFC3339 instant for a bi-temporal as-of read; absent = current head.
        schema:
          $ref: '#/components/schemas/Timestamp'
      responses:
        '200':
          description: Employment head at current time or as_of.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Employment'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/equipment:
    get:
      tags:
        - equipment
      operationId: autocompleteEquipment
      summary: Autocomplete branch-scoped equipment by management number, equipment number, or model
      security:
      - bearerAuth: []
      parameters:
      - name: q
        in: query
        required: true
        schema:
          type: string
          minLength: 1
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 20
          default: 10
      responses:
        '200':
          description: Prefix-search equipment matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentAutocompletePage'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags:
        - equipment
      operationId: createEquipment
      summary: Create a single equipment master row on the HQ branch
      description: Admin-gated (EquipmentManage). The equipment number prefix derives the manufacturer, kind, and power codes.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEquipmentRequest'
      responses:
        '201':
          description: Equipment created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateEquipmentResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/equipment-3r/dispositions/{disposition_id}/completion:
    post:
      tags: [equipment-3r]
      operationId: completeEquipment3rDisposition
      summary: Complete a repair/refurbish/resale disposition; no GL posting occurs
      security: [{ bearerAuth: [] }]
      parameters: [{ name: disposition_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, properties: { costMinor: { type: integer, format: int64, minimum: 0 }, saleAmountMinor: { type: integer, format: int64, minimum: 0 }, buyerName: { type: string, minLength: 1, maxLength: 160 } } } } } }
      responses:
        '200': { description: Disposition completed (financeGlPosting is always null), content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rDispositionView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unknown or cross-org disposition }
        '409': { description: Disposition not OPEN }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/equipment-3r/rental-cases:
    get:
      tags: [equipment-3r]
      operationId: listEquipment3rRentalCases
      summary: List rental cases, newest first (org-wide observe)
      security: [{ bearerAuth: [] }]
      responses:
        '200': { description: Rental cases, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/Equipment3rCaseView' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [equipment-3r]
      operationId: quoteEquipment3rRentalCase
      summary: Idempotently open a rental-case quote against an unsold unit
      security: [{ bearerAuth: [] }]
      parameters: [{ name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 16, maxLength: 200 } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [branchId, unitId, customerName, siteReference, monthlyRateMinor, durationMonths, currencyCode], properties: { branchId: { $ref: '#/components/schemas/Uuid' }, unitId: { $ref: '#/components/schemas/Uuid' }, customerName: { type: string, minLength: 1, maxLength: 160 }, siteReference: { type: string, minLength: 1, maxLength: 200 }, monthlyRateMinor: { type: integer, format: int64, minimum: 1 }, durationMonths: { type: integer, minimum: 1, maximum: 120 }, currencyCode: { type: string, enum: [KRW] } } } } } }
      responses:
        '200': { description: Idempotent replay of the stored quote (replayed=true), content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rCaseView' } } } }
        '201': { description: Quote created, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rCaseView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unit not found in branch }
        '409': { description: "Key reused with different fingerprint, or unit is SOLD" }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/equipment-3r/rental-cases/{case_id}:
    get:
      tags: [equipment-3r]
      operationId: getEquipment3rRentalCase
      summary: Rental-case detail with approval, legs, inspections, and assessment
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses:
        '200': { description: Case detail, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rCaseDetailView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unknown or cross-org case }
  /api/v1/equipment-3r/rental-cases/{case_id}/approval:
    post:
      tags: [equipment-3r]
      operationId: decideEquipment3rApproval
      summary: Four-eyes approval; APPROVED reserves the unit (single winner)
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [decision], properties: { decision: { type: string, enum: [APPROVED, DECLINED] }, reason: { type: string, minLength: 1, maxLength: 500 } } } } } }
      responses:
        '200': { description: Decision recorded, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rCaseView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { description: "Missing grant, branch outside scope, or four-eyes violation" }
        '404': { description: Unknown or cross-org case }
        '409': { description: Case not QUOTED or unit not AVAILABLE }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/equipment-3r/rental-cases/{case_id}/assessment:
    post:
      tags: [equipment-3r]
      operationId: assessEquipment3rReturn
      summary: Post the return assessment; closes the case and opens the disposition
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [conditionGrade, findings, disposition], properties: { conditionGrade: { type: string, enum: [A, B, C, D] }, findings: { type: string, minLength: 1, maxLength: 2000 }, disposition: { type: string, enum: [REPAIR, REFURBISH, RESALE, REDEPLOY] } } } } } }
      responses:
        '200': { description: Case closed with disposition opened, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rCaseDetailView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unknown or cross-org case }
        '409': { description: Case not RETURNED }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/equipment-3r/rental-cases/{case_id}/dispatch:
    post:
      tags: [equipment-3r]
      operationId: dispatchEquipment3rCase
      summary: Record the physical delivery leg of an approved case
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [carrierName, vehicleReference], properties: { carrierName: { type: string, minLength: 1, maxLength: 120 }, vehicleReference: { type: string, minLength: 1, maxLength: 120 } } } } } }
      responses:
        '200': { description: Case dispatched, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rCaseView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unknown or cross-org case }
        '409': { description: Case not APPROVED }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/equipment-3r/rental-cases/{case_id}/handover:
    post:
      tags: [equipment-3r]
      operationId: handoverEquipment3rCase
      summary: Customer handover with immutable evidence; unit goes ON_RENT
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [recipientName, evidenceObjectId, handedOverAt], properties: { recipientName: { type: string, minLength: 1, maxLength: 160 }, evidenceObjectId: { $ref: '#/components/schemas/Uuid' }, handedOverAt: { type: string, format: date-time } } } } } }
      responses:
        '200': { description: Case handed over, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rCaseView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unknown or cross-org case }
        '409': { description: Case not DISPATCHED }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/equipment-3r/rental-cases/{case_id}/inspections:
    post:
      tags: [equipment-3r]
      operationId: inspectEquipment3rCase
      summary: Append an on-rent inspection or maintenance record
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [outcome, findings], properties: { outcome: { type: string, enum: [PASS, MAINTENANCE_PERFORMED] }, findings: { type: string, minLength: 1, maxLength: 2000 }, maintenanceNote: { type: string, minLength: 1, maxLength: 2000 } } } } } }
      responses:
        '201': { description: Inspection recorded, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rInspectionView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unknown or cross-org case }
        '409': { description: Case not HANDED_OVER }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/equipment-3r/rental-cases/{case_id}/return:
    post:
      tags: [equipment-3r]
      operationId: returnEquipment3rCase
      summary: Record the unit return; unit enters assessment
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [returnedAt], properties: { returnedAt: { type: string, format: date-time } } } } } }
      responses:
        '200': { description: Case returned, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rCaseView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unknown or cross-org case }
        '409': { description: Case not HANDED_OVER }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/equipment-3r/units:
    get:
      tags: [equipment-3r]
      operationId: listEquipment3rUnits
      summary: List 3R units, newest first (org-wide observe)
      security: [{ bearerAuth: [] }]
      responses:
        '200': { description: Units, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/Equipment3rUnitView' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [equipment-3r]
      operationId: registerEquipment3rUnit
      summary: Register a serialized rental unit (org-unique serial)
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [branchId, serialNo, modelName, capacityClass, acquisitionCostMinor], properties: { branchId: { $ref: '#/components/schemas/Uuid' }, serialNo: { type: string, minLength: 1, maxLength: 80 }, modelName: { type: string, minLength: 1, maxLength: 120 }, capacityClass: { type: string, minLength: 1, maxLength: 40 }, acquisitionCostMinor: { type: integer, format: int64, minimum: 0 } } } } } }
      responses:
        '201': { description: Unit registered, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rUnitView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { description: Duplicate serial in organization }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/equipment-3r/units/{unit_id}:
    get:
      tags: [equipment-3r]
      operationId: getEquipment3rUnit
      summary: Unit detail with active case and open disposition links
      security: [{ bearerAuth: [] }]
      parameters: [{ name: unit_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses:
        '200': { description: Unit detail, content: { application/json: { schema: { $ref: '#/components/schemas/Equipment3rUnitDetailView' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unknown or cross-org unit }
  /api/v1/equipment-3r/units/{unit_id}/history:
    get:
      tags: [equipment-3r]
      operationId: getEquipment3rUnitHistory
      summary: Transition history for the unit, its cases, and dispositions, newest first
      security: [{ bearerAuth: [] }]
      parameters: [{ name: unit_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses:
        '200': { description: History entries, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/Equipment3rHistoryEntry' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { description: Unknown or cross-org unit }
  /api/v1/equipment-by-location:
    get:
      tags:
        - equipment-by-location
      operationId: listEquipmentByLocation
      summary: Aggregate equipment by site for the dispatch map
      description: Every site visible to the principal with its equipment counts and admin-entered coordinates. Read access
        (WorkOrderReadAll, all roles); branch-scoped like the substitute search, so a non-SUPER_ADMIN sees only their own
        branches. Sites with no entered coordinates come back with null latitude/longitude and are listed as ungeocoded rather
        than pinned.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Sites grouped with equipment counts and coordinates.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentByLocationPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
  /api/v1/equipment-substitutions:
    post:
      tags:
        - equipment-substitutions
      operationId: assignEquipmentSubstitute
      summary: Assign a substitute (대차) unit to a down/under-repair equipment
      description: Audited equipment-lifecycle write. Requires EquipmentManage (ADMIN/EXECUTIVE/SUPER_ADMIN).
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignSubstituteRequest'
      responses:
        '201':
          description: Substitute assignment recorded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubstituteAssignment'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/equipment-substitutions/{id}/return:
    post:
      tags:
        - equipment-substitutions
      operationId: returnEquipmentSubstitute
      summary: Return an active substitute (대차) assignment
      description: Audited equipment-lifecycle write. Requires EquipmentManage (ADMIN/EXECUTIVE/SUPER_ADMIN).
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentSubstitutionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReturnSubstituteRequest'
      responses:
        '200':
          description: Substitute assignment returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubstituteAssignment'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/equipment/import:
    post:
      tags:
        - equipment
      operationId: importEquipmentMasterList
      summary: Import the 지게차 master-list workbook into a fresh registry
      description: Org-wide-authority-gated (SUPER_ADMIN for built-in roles; MasterListImport) multipart upload of the K&L
        master-list .xlsx. The importer writes the tenant HQ master and therefore denies branch-scoped ADMIN principals. Upserts
        equipment by equipment number and returns a created/updated/error summary.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              properties:
                file:
                  type: string
                  format: binary
                  description: The master-list .xlsx workbook.
      responses:
        '200':
          description: Per-row import summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistryImportReport'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/equipment/list:
    get:
      tags:
        - equipment
      operationId: listEquipment
      summary: Paginated, filterable, branch-scoped equipment list
      description: 'Read access (WorkOrderReadAll — all authenticated roles). Non-SUPER_ADMIN principals see only rows in
        their own branch(es). The `q` parameter is normalized like the 호기-lookup: strips a leading `#` and a trailing `호기`
        suffix then matches leading-zero-insensitively across management_no, equipment_no, model, maker, customer name, site
        name, and VIN.

        '
      security:
      - bearerAuth: []
      parameters:
      - name: q
        in: query
        required: false
        schema:
          type: string
        description: Free-text search (호기-normalized, leading-zero-insensitive).
      - name: status
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/EquipmentStatus'
      - name: branch_id
        in: query
        required: false
        schema:
          type: string
          format: uuid
        description: Filter to a specific branch (UUID).
      - name: customer_id
        in: query
        required: false
        schema:
          type: string
          format: uuid
        description: Filter to a specific customer (UUID).
      - name: site_id
        in: query
        required: false
        schema:
          type: string
          format: uuid
        description: Filter to a specific site (UUID).
      - name: model
        in: query
        required: false
        schema:
          type: string
        description: Filter by model name (case-insensitive exact match).
      - name: maker
        in: query
        required: false
        schema:
          type: string
        description: Filter by maker name (case-insensitive exact match).
      - name: sort
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/EquipmentSortBy'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paginated equipment list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentListPage'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/equipment/lookup:
    get:
      tags:
        - equipment
      operationId: lookupEquipment
      summary: Resolve branch-scoped equipment by management number
      security:
      - bearerAuth: []
      parameters:
      - name: management_no
        in: query
        required: true
        schema:
          type: string
          minLength: 1
        description: Equipment management number. A leading `#` is accepted and ignored.
      responses:
        '200':
          description: Matching equipment, model, customer, and site.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentLookupResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/equipment/ownership-transfer-requests/{id}/decisions:
    post:
      tags:
        - equipment
      operationId: decideEquipmentOwnershipTransfer
      summary: Approve or reject the current ownership-transfer signoff step
      description: Advances the ordered transfer signoff line one step at a time. The final accounting approval writes the
        new `asset_owner` and records an audited workflow event; rejection makes the request terminal without mutating the
        asset owner.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DecideOwnershipTransferRequest'
      responses:
        '200':
          description: Transfer request after the decision.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OwnershipTransfer'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/equipment/{id}:
    delete:
      tags:
        - equipment
      operationId: deleteEquipment
      summary: Soft-delete one equipment master row
      description: Admin-gated (EquipmentManage). Marks the row 폐기 (disposed) rather than hard-deleting, so work-order and
        substitution references stay intact.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentId'
      responses:
        '204':
          description: Equipment soft-deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          description: JWT verification is not configured.
    get:
      tags:
        - equipment
      operationId: getEquipment
      summary: Read one branch-scoped equipment master row
      description: 'Read access (WorkOrderReadAll — all authenticated roles). Non-SUPER_ADMIN principals only receive rows
        in their own branch scope; a foreign or missing id returns 404 without revealing existence.

        '
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentId'
      responses:
        '200':
          description: Equipment master row.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentListItem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
    patch:
      tags:
        - equipment
      operationId: updateEquipment
      summary: Update fields on one equipment master row
      description: Admin-gated (EquipmentManage). Only supplied fields are written; nullable fields explicitly set to null
        are cleared.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEquipmentRequest'
      responses:
        '204':
          description: Equipment updated.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/equipment/{id}/ownership-transfer-requests:
    get:
      tags:
        - equipment
      operationId: listEquipmentOwnershipTransfers
      summary: List legal ownership transfer workflow requests for one equipment asset
      description: Returns the durable ownership-transfer request ledger for a single asset. A transfer changes only the legal
        owner fact after ordered sending-org, receiving-org, legal, and accounting signoff; the equipment tenant/org discriminator
        stays immutable.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentId'
      responses:
        '200':
          description: Transfer requests, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OwnershipTransferPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags:
        - equipment
      operationId: createEquipmentOwnershipTransfer
      summary: Request a cross-org legal ownership transfer
      description: Creates a pending transfer request and approval line. This endpoint does not immediately mutate `asset_owner`;
        final mutation happens only after sending-org, receiving-org, legal, and accounting approval decisions.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOwnershipTransferRequest'
      responses:
        '201':
          description: Transfer request created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OwnershipTransfer'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/equipment/{id}/substitutes:
    get:
      tags:
        - equipment
      operationId: listEquipmentSubstitutes
      summary: List compatible spare equipment candidates for a down unit
      description: Same branch by default. SUPER_ADMIN may set all_branches=true to search across branches.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentId'
      - name: all_branches
        in: query
        required: false
        schema:
          type: boolean
          default: false
        description: SUPER_ADMIN-only flag to include candidates outside the down unit's branch.
      responses:
        '200':
          description: Compatible spare equipment candidates ranked by exact ton then nearest-above capacity.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubstituteCandidatePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/equipment/{id}/timeline-graph:
    get:
      tags:
        - equipment
      operationId: getEquipmentTimelineGraph
      summary: Read one equipment lifecycle ribbon and relationship graph
      description: 'Read-only equipment lens for object-detail pages. Returns real lifecycle events and the customer-site-equipment-work-order
        graph visible within the caller''s branch scope. A foreign or missing equipment id returns 404 without revealing existence.

        '
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentId'
      responses:
        '200':
          description: Equipment lifecycle and relationship graph lens.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentTimelineGraph'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
  /api/v1/equipment/{id}/versions:
    get:
      tags:
        - equipment
      operationId: listEquipmentVersions
      summary: List the append-only version history of one equipment master row
      description: 'Generalized non-destructive versioning (BE-LC). Every equipment update captures the post-update content
        as a new version (the pre-update content backfills version 1 on first capture). Read tier mirrors the equipment
        detail read.'
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentId'
      responses:
        '200':
          description: Version history, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentVersionList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
  /api/v1/equipment/{id}/versions/{version}/rollback:
    post:
      tags:
        - equipment
      operationId: rollbackEquipmentVersion
      summary: Roll one equipment master row back to a prior version
      description: 'Admin-gated (EquipmentManage), audited. Re-applies the target version''s stored content through the
        normal update path and appends a NEW version (status ROLLBACK, sourceVersion = target); version history is never
        mutated.'
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentId'
      - name: version
        in: path
        required: true
        schema:
          type: integer
          format: int32
      responses:
        '201':
          description: Rollback landed as a new version.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentRollbackResult'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/evaluation/cycles:
    get:
      tags: [evaluation]
      operationId: listEvaluationCycles
      summary: List evaluation cycles
      security: [{ bearerAuth: [] }]
      parameters:
      - name: stage
        in: query
        required: false
        schema: { $ref: '#/components/schemas/EvaluationCycleStage' }
      - name: limit
        in: query
        required: false
        description: Normalized to the inclusive range 1 through 100; defaults to 50.
        schema: { type: integer, format: int64, default: 50 }
      - name: offset
        in: query
        required: false
        description: Negative values normalize to zero.
        schema: { type: integer, format: int64, default: 0 }
      responses:
        '200': { description: Evaluation cycle page., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationCyclePage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
    post:
      tags: [evaluation]
      operationId: createEvaluationCycle
      summary: Create a draft evaluation cycle
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateEvaluationCycleRequest' } } } }
      responses:
        '201': { description: Draft cycle created., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationCycleDetail' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/cycles/{cycle_id}:
    get:
      tags: [evaluation]
      operationId: getEvaluationCycle
      summary: Get an evaluation cycle with enrolled subjects
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationCycleId' }]
      responses:
        '200': { description: Evaluation cycle., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationCycleDetail' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/cycles/{cycle_id}/open:
    post:
      tags: [evaluation]
      operationId: openEvaluationCycle
      summary: Open a draft cycle after its server-side preflight passes
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationCycleId' }]
      responses: &evaluationTransitionResponses
        '200': { description: Transitioned cycle., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationCycleDetail' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/cycles/{cycle_id}/archive:
    post:
      tags: [evaluation]
      operationId: archiveEvaluationCycle
      summary: Archive a finalized cycle after preflight
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationCycleId' }]
      responses: *evaluationTransitionResponses
  /api/v1/evaluation/cycles/{cycle_id}/finalize:
    post:
      tags: [evaluation]
      operationId: finalizeEvaluationCycle
      summary: Finalize a calibrated cycle after preflight
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationCycleId' }]
      responses: *evaluationTransitionResponses
  /api/v1/evaluation/cycles/{cycle_id}/preflight:
    get:
      tags: [evaluation]
      operationId: getEvaluationCyclePreflight
      summary: Read the next transition's blocking and advisory conditions
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationCycleId' }]
      responses:
        '200': { description: Preflight report., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationPreflightReport' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/cycles/{cycle_id}/start-calibration:
    post:
      tags: [evaluation]
      operationId: startEvaluationCalibration
      summary: Move an open cycle into calibration after preflight
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationCycleId' }]
      responses: *evaluationTransitionResponses
  /api/v1/evaluation/employees/{employee_id}/reviews:
    get:
      tags: [evaluation]
      operationId: listEmployeeEvaluationReviews
      summary: List a managed employee's finalized evaluation ledger
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationEmployeeId' }]
      responses:
        '200': { description: Finalized review ledger., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationLedgerPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/my-tasks:
    get:
      tags: [evaluation]
      operationId: listMyEvaluationTasks
      summary: List the authenticated submitter's evaluation tasks
      security: [{ bearerAuth: [] }]
      responses:
        '200': { description: Submitter task page., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationTaskPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/subjects:
    post:
      tags: [evaluation]
      operationId: addEvaluationSubject
      summary: Enroll an employee in a draft evaluation cycle
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AddEvaluationSubjectRequest' } } } }
      responses:
        '201': { description: Enrolled evaluation subject., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationSubjectDetail' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/subjects/{subject_id}:
    get:
      tags: [evaluation]
      operationId: getEvaluationSubject
      summary: Get an evaluation subject
      description: Submit-only callers receive 404 for subjects not assigned to them; read-capable callers can read visible subjects.
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationSubjectId' }]
      responses:
        '200': { description: Evaluation subject., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationSubjectDetail' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/subjects/{subject_id}/calibrate:
    post:
      tags: [evaluation]
      operationId: calibrateEvaluationSubject
      summary: Record a calibrated grade for an evaluation subject
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationSubjectId' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CalibrateEvaluationSubjectRequest' } } } }
      responses:
        '200': { description: Calibrated subject., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationSubjectDetail' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/subjects/{subject_id}/goals:
    put:
      tags: [evaluation]
      operationId: replaceEvaluationGoals
      summary: Replace all typed goals for an assigned or managed subject
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/EvaluationSubjectId' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ReplaceEvaluationGoalsRequest' } } } }
      responses:
        '200': { description: Subject with replacement goals., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationSubjectDetail' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/subjects/{subject_id}/reviews/{kind}:
    put:
      tags: [evaluation]
      operationId: saveEvaluationReview
      summary: Save a draft self or manager review
      description: Review kind is lowercase `self` or `manager`; unassigned submitters receive 404.
      security: [{ bearerAuth: [] }]
      parameters:
      - $ref: '#/components/parameters/EvaluationSubjectId'
      - $ref: '#/components/parameters/EvaluationReviewKindPath'
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/SaveEvaluationReviewRequest' } } } }
      responses:
        '200': { description: Saved review draft., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationReview' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evaluation/subjects/{subject_id}/reviews/{kind}/submit:
    post:
      tags: [evaluation]
      operationId: submitEvaluationReview
      summary: Submit a saved self or manager review
      description: Submission is terminal; the backend enforces grade/evidence gates and returns conflicts for unmet gates or replays.
      security: [{ bearerAuth: [] }]
      parameters:
      - $ref: '#/components/parameters/EvaluationSubjectId'
      - $ref: '#/components/parameters/EvaluationReviewKindPath'
      responses:
        '200': { description: Submitted review., content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationReview' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/evidence/objects:
    get:
      tags: [Evidence]
      operationId: listEvidenceObjects
      security:
        - bearerAuth: []
      summary: List EV objects (RLS-scoped) with filters.
      parameters:
        - { name: q, in: query, required: false, schema: { type: string } }
        - { name: source_type, in: query, required: false, schema: { $ref: '#/components/schemas/EvidenceSourceType' } }
        - { name: source_id, in: query, required: false, schema: { type: string } }
        - { name: admissibility_status, in: query, required: false, schema: { $ref: '#/components/schemas/AdmissibilityStatus' } }
        - { name: legal_hold_state, in: query, required: false, schema: { $ref: '#/components/schemas/LegalHoldState' } }
        - { name: custody_stage, in: query, required: false, schema: { $ref: '#/components/schemas/CustodyStage' } }
        - { name: classification, in: query, required: false, schema: { $ref: '#/components/schemas/EvidenceClassification' } }
        - { name: limit, in: query, required: false, schema: { type: integer, format: int64 } }
        - { name: offset, in: query, required: false, schema: { type: integer, format: int64 }, description: "Backward-compatible offset accepted by the runtime. When cursor is supplied, offset must be omitted or zero." }
        - { name: as_of, in: query, required: false, schema: { type: integer, format: int64 }, description: "Immutable evidence-register sequence returned by the first page; omit to establish the current snapshot. When cursor is supplied, as_of must match that cursor's snapshot." }
        - { name: cursor, in: query, required: false, schema: { type: string, pattern: '^[A-Za-z0-9_-]+$' }, description: Opaque unpadded-base64url continuation token returned as next_cursor by the preceding page. }
      responses:
        '200': { description: A page of EV objects., content: { application/json: { schema: { $ref: '#/components/schemas/EvidenceObjectPage' } } } }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/evidence/objects/{id}:
    get:
      tags: [Evidence]
      operationId: getEvidenceObject
      security:
        - bearerAuth: []
      parameters: [ { name: id, in: path, required: true, schema: { type: string, format: uuid } } ]
      responses:
        '200': { description: Full EV object detail., content: { application/json: { schema: { $ref: '#/components/schemas/EvidenceObjectDetail' } } } }
        '404': { description: 'Not found (or cross-tenant, hidden by RLS).' }
  /api/v1/evidence/objects/{id}/hold:
    post:
      tags: [Evidence]
      operationId: holdEvidenceObject
      security:
        - bearerAuth: []
      summary: Apply or release a legal hold (release is four-eyes gated).
      parameters: [ { name: id, in: path, required: true, schema: { type: string, format: uuid } } ]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/EvidenceHoldRequest' } } }
      responses:
        '200': { description: Hold released., content: { application/json: { schema: { $ref: '#/components/schemas/LegalHoldRecordView' } } } }
        '201': { description: Hold applied., content: { application/json: { schema: { $ref: '#/components/schemas/LegalHoldRecordView' } } } }
        '403': { description: Release requires a distinct-approver four-eyes approval (code four_eyes_required). }
  /api/v1/evidence/objects/{id}/verify:
    post:
      tags: [Evidence]
      operationId: verifyEvidenceObject
      security:
        - bearerAuth: []
      summary: Recompute WORM fixity for every copy; audited.
      parameters: [ { name: id, in: path, required: true, schema: { type: string, format: uuid } } ]
      responses:
        '200': { description: Fixity report (outcome + per-copy verdict)., content: { application/json: { schema: { $ref: '#/components/schemas/EvidenceVerifyReport' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '503':
          description: >-
            Fixity storage is unavailable only when ErrorBody.error.code is
            evidence_store_unavailable (for example, storage is not configured).
            Generic auth or platform service failures also use HTTP 503 but retain
            the generic unavailable code and must remain retryable rather than
            being presented as an evidence-storage condition.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorBody' }
  /api/v1/evidence/presign:
    post:
      tags:
        - evidence
      operationId: presignEvidenceUpload
      summary: Issue a presigned upload ticket for work-order evidence
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EvidencePresignRequest'
      responses:
        '200':
          description: Presigned evidence upload ticket.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EvidencePresignResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: Evidence storage is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/evidence/staging-presign:
    post:
      tags:
        - evidence
      operationId: presignEvidenceStagingUpload
      summary: Issue a presigned STAGING upload ticket for mechanic evidence and begin server-side media processing (transcode/optimize
        before storage)
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EvidenceStagingPresignRequest'
      responses:
        '200':
          description: Presigned staging upload ticket; the evidence row is created in PROCESSING and an async transcode job
            is enqueued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EvidenceStagingPresignResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: Evidence storage or processing is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/evidence/{evidenceId}/confirm:
    post:
      tags:
        - evidence
      operationId: confirmEvidenceUpload
      summary: Confirm direct evidence upload completion and trigger replica verification
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EvidenceId'
      responses:
        '200':
          description: Evidence confirmation and replica status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EvidenceConfirmResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: Evidence storage is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/evidence/{evidenceId}/status:
    get:
      tags:
        - evidence
      operationId: getEvidenceProcessingStatus
      summary: Poll the server-side processing status of an evidence row
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EvidenceId'
      responses:
        '200':
          description: Evidence processing status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EvidenceStatusResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: Evidence storage is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/exports/daily-status:
    get:
      tags:
        - exports
      operationId: getDailyStatusExport
      summary: Export the daily work-progress status workbook
      description: Fills the real 일일업무진행현황 Excel template from live work-order, daily-plan, and regular-inspection schedule
        data for the requested date.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ReportDate'
      responses:
        '200':
          description: Daily status workbook.
          headers:
            Content-Disposition:
              schema:
                type: string
              description: Attachment filename for the generated workbook.
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/exports/kpi:
    get:
      tags:
        - exports
      operationId: getKpiExport
      summary: Export the KPI rollup workbook
      description: Exports the same branch-scoped KPI rollups served by GET /api/v1/kpi as a downloadable Excel workbook.
      security:
      - bearerAuth: []
      parameters:
      - name: period
        in: query
        required: true
        schema:
          type: string
          pattern: ^[0-9]{4}-[0-9]{2}-[0-9]{2}\.\.[0-9]{4}-[0-9]{2}-[0-9]{2}$
        example: 2026-06-01..2026-07-01
        description: Inclusive start date and exclusive end date in UTC day boundaries.
      - name: scope
        in: query
        required: false
        schema:
          type: string
          default: company
        examples:
          company:
            value: company
          region:
            value: region:00000000-0000-0000-0000-000000000000
          branch:
            value: branch:00000000-0000-0000-0000-000000000000
          technician:
            value: technician:00000000-0000-0000-0000-000000000000
      responses:
        '200':
          description: KPI rollup workbook.
          headers:
            Content-Disposition:
              schema:
                type: string
              description: Attachment filename for the generated workbook.
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/exports/work-diary:
    get:
      tags:
        - exports
      operationId: getWorkDiaryExport
      summary: Export the confirmed or generated work-diary workbook
      description: Exports the 업무일지 workbook for the requested date, preserving the template's additional sheets and using
        an existing draft when one has been edited or confirmed.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ReportDate'
      responses:
        '200':
          description: Work diary workbook.
          headers:
            Content-Disposition:
              schema:
                type: string
              description: Attachment filename for the generated workbook.
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/facilities/cases:
    get:
      tags: [facilities]
      operationId: listFacilitiesCases
      summary: List the facilities cases the caller may observe
      security: [{ bearerAuth: [] }]
      responses:
        '200': { description: Facilities cases ordered by creation time, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/FacilitiesCase' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
    post:
      tags: [facilities]
      operationId: createDueFacilitiesCase
      summary: Create or replay an idempotent due facilities case
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesDueCaseRequest' } } } }
      responses:
        '200': { description: Newly created case or the prior idempotent result, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesCase' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/facilities/cases/{case_id}:
    get:
      tags: [facilities]
      operationId: getFacilitiesCase
      summary: Get one facilities case with its derived service readback
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses:
        '200': { description: Case including derived SLA and energy delta, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesCase' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/facilities/cases/{case_id}/acceptance:
    post:
      tags: [facilities]
      operationId: decideFacilitiesAcceptance
      summary: Record customer acceptance or rejection of completed facilities work
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesAcceptanceRequest' } } } }
      responses:
        '200': { description: Closed or rework-required case, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesCase' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/facilities/cases/{case_id}/assign:
    post:
      tags: [facilities]
      operationId: assignFacilitiesCase
      summary: Assign a scheduled facilities case to a technician
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesAssignRequest' } } } }
      responses:
        '200': { description: Assigned case, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesCase' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/facilities/cases/{case_id}/observations:
    post:
      tags: [facilities]
      operationId: recordFacilitiesObservation
      summary: Record facilities energy or cost observations and return the case readback
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesObservationRequest' } } } }
      responses:
        '200': { description: Case with derived energy and cost readback, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesCase' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/facilities/cases/{case_id}/start:
    post:
      tags: [facilities]
      operationId: startFacilitiesCase
      summary: Start assigned facilities execution after safety acknowledgement
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses:
        '200': { description: Safety-acknowledged case in progress, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesCase' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/facilities/cases/{case_id}/submit:
    post:
      tags: [facilities]
      operationId: submitFacilitiesExecution
      summary: Submit execution evidence and await customer acceptance
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesSubmitRequest' } } } }
      responses:
        '200': { description: Case awaiting customer acceptance, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesCase' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '412': { description: Required execution evidence is not confirmed, content: { application/json: { schema: { $ref: '#/components/schemas/ErrorBody' } } } }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/facilities/cases/{case_id}/triage:
    post:
      tags: [facilities]
      operationId: triageFacilitiesCase
      summary: Schedule a due facilities case for service
      security: [{ bearerAuth: [] }]
      parameters: [{ name: case_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesTriageRequest' } } } }
      responses:
        '200': { description: Scheduled case, content: { application/json: { schema: { $ref: '#/components/schemas/FacilitiesCase' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
        '500': { $ref: '#/components/responses/InternalServerError' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /api/v1/field/sites:
    get:
      tags:
      - field
      operationId: listFieldSites
      summary: List branch-scoped customer sites with their issue, visit, and SLA rollups
      description: The field read gate is work_order_read_all, checked server-side because the overview carries customer
        contact and geo data. Every count derives from the same aggregation as the rows.
      security:
      - bearerAuth: []
      parameters:
      - name: q
        in: query
        required: false
        description: Substring match on site name or customer name.
        schema:
          type: string
      - name: customer_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: sla
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/FieldSlaState'
      - name: limit
        in: query
        required: false
        description: Page size; clamped server-side to 1..=100.
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
      - name: cursor
        in: query
        required: false
        description: Keyset cursor — the site id of the previous page's last row.
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: A keyset page of field sites visible in the principal's branch scope, plus the unpaged total for the
            same filters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FieldSitePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/field/sites/{id}:
    get:
      tags:
      - field
      operationId: getFieldSite
      summary: Get a field site with its SLA rollup and traversable ticket, work-order, attendance, and acceptance history
      description: Scope resolution runs before the feature check, so an out-of-scope site is a 404 and never a 403. Each
        history list is capped at 50, most relevant first.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Field site detail with its object chain.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FieldSiteDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/finance-gl/accounts/{account_code}/entries:
    get:
      tags:
      - finance-gl
      operationId: accountDrill
      summary: Account drill — voucher lines for an account with voucher/source links
      security:
      - bearerAuth: []
      parameters:
      - name: account_code
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Account entries.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AccountDrillEntry'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/finance-gl/vouchers:
    get:
      tags:
      - finance-gl
      operationId: listVouchers
      summary: List general-ledger vouchers (tenant-scoped; optional branch/status filter)
      security:
      - bearerAuth: []
      parameters:
      - name: branch_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: status
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/VoucherStatus'
      responses:
        '200':
          description: Vouchers visible to the caller.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/VoucherSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags:
      - finance-gl
      operationId: createVoucherDraft
      summary: Open a draft voucher (기표)
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateVoucherRequest'
      responses:
        '201':
          description: Draft voucher created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoucherSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/finance-gl/vouchers/{voucher_id}:
    get:
      tags:
      - finance-gl
      operationId: getVoucher
      summary: Fetch one voucher with its lines and source linkage
      security:
      - bearerAuth: []
      parameters:
      - name: voucher_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The voucher.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoucherSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/finance-gl/vouchers/{voucher_id}/approve:
    post:
      tags:
      - finance-gl
      operationId: approveVoucher
      summary: 차대검증 → 승인
      security:
      - bearerAuth: []
      parameters:
      - name: voucher_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Voucher approved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoucherSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/finance-gl/vouchers/{voucher_id}/post:
    post:
      tags:
      - finance-gl
      operationId: postVoucher
      summary: 승인 → 전기(posted); lines become immutable
      security:
      - bearerAuth: []
      parameters:
      - name: voucher_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Voucher posted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoucherSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/finance-gl/vouchers/{voucher_id}/reverse:
    post:
      tags:
      - finance-gl
      operationId: reverseVoucher
      summary: 전기 → 역분개; creates a linked contra voucher (returns the contra)
      security:
      - bearerAuth: []
      parameters:
      - name: voucher_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReverseVoucherRequest'
      responses:
        '201':
          description: Contra voucher created; original marked REVERSED.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoucherSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/finance-gl/vouchers/{voucher_id}/submit:
    post:
      tags:
      - finance-gl
      operationId: submitVoucher
      summary: 기표 → 차대검증 (balance gate; unbalanced is rejected)
      security:
      - bearerAuth: []
      parameters:
      - name: voucher_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Voucher advanced to 차대검증.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoucherSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/financial/equipment/{equipmentId}/cost-ledger:
    get:
      tags:
        - financial
      operationId: listEquipmentCostLedger
      summary: List equipment cost ledger entries
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentIdV2'
      responses:
        '200':
          description: Equipment cost ledger entries.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CostLedgerEntrySummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/financial/equipment/{equipmentId}/cost-ledger/manual:
    post:
      tags:
        - financial
      operationId: appendManualCostLedgerEntry
      summary: Append a manual admin equipment cost and recompute residual value
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentIdV2'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppendManualCostLedgerRequest'
      responses:
        '201':
          description: Cost ledger entry appended.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostLedgerEntrySummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/financial/equipment/{equipmentId}/lifecycle-cost:
    get:
      tags:
        - financial
      operationId: getEquipmentLifecycleCost
      summary: Per-asset lifecycle cost, TCO, and gross margin
      description: Read-gated (EquipmentCostLedgerRead). Returns the asset's acquisition cost (with a source tag), maintenance
        total split by source, read-only outsource cost, current residual, latest realized sale price, total cost of ownership,
        gross margin, and per-month/per-hour maintenance intensity.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/EquipmentIdV2'
      responses:
        '200':
          description: Asset lifecycle cost summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssetLifecycleCostSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/financial/purchase-requests:
    get:
      tags:
        - financial
      operationId: listPurchaseRequests
      summary: List a branch-scoped purchase-request queue
      description: >-
        Returns only purchase requests visible to the caller in the required
        branch. Repeat the plain `status` query key to filter lifecycle states;
        `status[]`, comma-delimited values, unknown keys, and malformed values
        are rejected. The response is an offset page with required metadata.
      security:
      - bearerAuth: []
      parameters:
      - name: branch_id
        in: query
        required: true
        description: Branch whose purchase-request queue is requested.
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: status
        in: query
        required: false
        description: Repeat this plain key to filter one or more lifecycle states.
        style: form
        explode: true
        schema:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseStatus'
      - name: limit
        in: query
        required: false
        description: Page size; defaults to 25 and must be between 1 and 100.
        schema:
          type: integer
          format: int64
          default: 25
          minimum: 1
          maximum: 100
      - name: offset
        in: query
        required: false
        description: Zero-based offset; defaults to 0.
        schema:
          type: integer
          format: int64
          default: 0
          minimum: 0
      responses:
        '200':
          description: Branch-scoped purchase-request page.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '500':
          description: An unexpected persistence or infrastructure error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    post:
      tags:
        - financial
      operationId: createPurchaseRequest
      summary: Attach a 거래명세표 evidence record and open a purchase request
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePurchaseRequest'
      responses:
        '201':
          description: Purchase request created in statement-attached state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequestSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
  /api/v1/financial/purchase-requests/attachments/presign:
    post:
      tags:
        - financial
      operationId: presignPurchaseRequestAttachment
      summary: Create a purchase attachment record and return a presigned upload URL
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseAttachmentPresignRequest'
      responses:
        '201':
          description: Purchase attachment upload ticket.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseAttachmentPresignResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/financial/purchase-requests/attachments/{attachmentId}/confirm:
    post:
      tags:
        - financial
      operationId: confirmPurchaseRequestAttachment
      summary: Confirm a completed direct purchase attachment upload
      security:
      - bearerAuth: []
      parameters:
      - name: attachmentId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Confirmed purchase attachment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseAttachmentUploadRecord'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/financial/purchase-requests/preferences:
    get:
      tags:
        - financial
      operationId: getPurchaseRequestPreferences
      summary: Fetch the current user's purchase request workspace preferences
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Purchase request workspace preferences.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseFeaturePreferences'
        '401':
          $ref: '#/components/responses/Unauthorized'
    put:
      tags:
        - financial
      operationId: savePurchaseRequestPreferences
      summary: Save the current user's purchase request workspace preferences
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SavePurchasePreferencesRequest'
      responses:
        '200':
          description: Saved purchase request workspace preferences.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseFeaturePreferences'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/financial/purchase-requests/{purchaseRequestId}:
    get:
      tags:
        - financial
      operationId: getPurchaseRequest
      summary: Fetch one purchase request
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PurchaseRequestId'
      responses:
        '200':
          description: Purchase request summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseRequestSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/financial/purchase-requests/{purchaseRequestId}/approve-admin:
    post:
      tags:
        - financial
      operationId: approvePurchaseRequestAdmin
      summary: Admin approval for a submitted purchase request
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PurchaseRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FinancialStepUpRequest'
      responses:
        '200':
          description: Admin-approved purchase request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
  /api/v1/financial/purchase-requests/{purchaseRequestId}/approve-executive:
    post:
      tags:
        - financial
      operationId: approvePurchaseRequestExecutive
      summary: Executive final approval for above-threshold purchase requests
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PurchaseRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FinancialStepUpRequest'
      responses:
        '200':
          description: Purchase request ready to execute.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
  /api/v1/financial/purchase-requests/{purchaseRequestId}/attachments/{attachmentId}/download:
    get:
      tags:
        - financial
      operationId: downloadPurchaseRequestAttachment
      summary: Return a short-lived download URL for a confirmed purchase attachment
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PurchaseRequestId'
      - name: attachmentId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Purchase attachment download URL.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseAttachmentDownloadResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/financial/purchase-requests/{purchaseRequestId}/execute:
    post:
      tags:
        - financial
      operationId: executePurchaseRequest
      summary: Execute an approved purchase request and feed the amount into the equipment cost ledger
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PurchaseRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FinancialStepUpRequest'
      responses:
        '200':
          description: Executed purchase request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
  /api/v1/financial/purchase-requests/{purchaseRequestId}/prepare-expenditure:
    post:
      tags:
        - financial
      operationId: preparePurchaseExpenditure
      summary: Record expenditure number and route to execution or executive approval
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PurchaseRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PrepareExpenditureRequest'
      responses:
        '200':
          description: Purchase request routed after expenditure preparation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
  /api/v1/financial/purchase-requests/{purchaseRequestId}/reject:
    post:
      tags:
        - financial
      operationId: rejectPurchaseRequest
      summary: Reject a purchase request before execution
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PurchaseRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RejectPurchaseRequest'
      responses:
        '200':
          description: Rejected purchase request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
  /api/v1/financial/purchase-requests/{purchaseRequestId}/restart:
    post:
      tags:
        - financial
      operationId: restartPurchaseRequest
      summary: Restart a rejected purchase request with a replacement statement evidence record
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PurchaseRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RestartPurchaseRequest'
      responses:
        '200':
          description: Restarted purchase request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/financial/purchase-requests/{purchaseRequestId}/submit:
    post:
      tags:
        - financial
      operationId: submitPurchaseRequest
      summary: Submit a statement-attached purchase request for admin approval
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/PurchaseRequestId'
      responses:
        '200':
          description: Submitted purchase request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/financial/rental-quotes:
    post:
      tags:
        - financial
      operationId: createRentalQuote
      summary: Compute and persist a rental quote for equipment
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRentalQuoteRequest'
      responses:
        '201':
          description: Rental quote persisted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RentalQuoteSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/financial/rental-quotes/compute:
    post:
      tags:
        - financial
      operationId: computeRentalQuote
      summary: Compute a rental quote from explicit financial inputs without persisting it
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ComputeRentalQuoteRequest'
      responses:
        '200':
          description: Computed rental quote.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComputedRentalQuote'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/financial/rental-quotes/{quoteId}:
    get:
      tags:
        - financial
      operationId: getRentalQuote
      summary: Fetch a persisted rental quote
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/QuoteId'
      responses:
        '200':
          description: Rental quote summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RentalQuoteSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/governance/approvals:
    post:
      tags:
        - Governance
      operationId: createGovernanceApproval
      security:
        - bearerAuth: []
      summary: Open a pending four-eyes approval request
      requestBody:
        required: true
        content:
          application/json:
            schema: {type: object, required: [request_ref, kind], properties: {request_ref: {type: string, format: uuid}, kind: {type: string, maxLength: 80}, target_ref: {type: [string, 'null'], format: uuid, description: 'The object this approval is FOR (a hold id, a workflow definition id, an ontology instance id). A four-eyes gate binds the approval to the action''s target, so an approval decided for one object can never satisfy a gate for another. Omit for create-style actions with no pre-existing target.'}, payload_summary: {type: object, additionalProperties: true}}}
      responses:
        '201':
          description: Pending approval request created
          content:
            application/json: {schema: {type: object, required: [id, request_ref, kind, requested_by, payload_summary, created_at], properties: {id: {type: string, format: uuid}, request_ref: {type: string, format: uuid}, kind: {type: string}, requested_by: {type: string, format: uuid}, payload_summary: {type: object, additionalProperties: true}, created_at: {type: string, format: date-time}}}}
        '403':
          description: Forbidden
        '409':
          description: A request already exists for this ref
        '422':
          description: Validation error
  /api/v1/governance/approvals/decide:
    post:
      operationId: decideGovernanceApproval
      summary: Decide a four-eyes approval that is already open (step 2 of 2)
      description: >-
        Decides a four-eyes request that MUST already be open. Step 1 is POST /api/v1/governance/approvals for the
        same request_ref; deciding without it is a 409, not an implicit create. The open row — not the request body —
        is authority for the requester and for the object the approval binds to. 409 when no open pending request
        exists for request_ref, and 409 when kind differs from the kind that request was opened with. 403 when the
        approver is that row's recorded requester (self-approval).
      tags:
      - Governance
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GovernanceDecideApprovalRequest'
      responses:
        '201':
          description: Approval decision recorded.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/governance/lifecycle/preflight:
    post:
      operationId: preflightLifecycleTransition
      summary: Preflight a lifecycle transition
      description: Evaluates the gate chain for a lifecycle edge WITHOUT committing. An unconfigured edge is fail-closed (denied).
      tags:
      - Governance
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GovernanceLifecyclePreflightRequest'
      responses:
        '200':
          description: Lifecycle gate-chain preflight status.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/governance/lifecycle/transitions:
    post:
      operationId: configureLifecycleTransition
      summary: Configure a lifecycle transition's requirements
      description: Configures the reason / four-eyes / checklist requirements for one object-type lifecycle edge.
      tags:
      - Governance
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GovernanceConfigureTransitionRequest'
      responses:
        '201':
          description: Transition requirements configured.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/governance/overrides:
    post:
      operationId: openGovernanceOverride
      summary: Open a post-draft override
      description: Opens a governance override (reason + before-value snapshot) for editing a non-draft object, recorded append-only.
      tags:
      - Governance
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GovernanceOpenOverrideRequest'
      responses:
        '201':
          description: Override opened.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/group-admin/groups:
    get:
      tags:
        - group-admin
      operationId: listGroupAdminGroups
      summary: List group-admin managed tenant organizations
      description: Returns only group memberships for which the authenticated actor has GROUP_ADMIN authority. Used before
        minting a bounded tenant context for a member organization.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Group-admin visible groups and member organizations.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/group-admin/tenant-context:
    post:
      tags:
        - group-admin
      operationId: startGroupAdminTenantContext
      summary: Start a bounded group-admin tenant context
      description: Mints a short-lived tenant token scoped to a member organization controlled by the actor's group-admin grant.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org_id
              properties:
                org_id:
                  $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Tenant context token for the selected group member organization.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/group-admin/tenant-context/exit:
    post:
      tags:
        - group-admin
      operationId: exitGroupAdminTenantContext
      summary: End a group-admin tenant context audit trail
      description: Records the group-admin tenant-context stop event for the selected member organization.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org_id
              properties:
                org_id:
                  $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Group-admin tenant context ended.
          content:
            application/json:
              schema:
                type: object
                required:
                - ended
                properties:
                  ended:
                    type: boolean
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/hr/absence-exit-dashboard:
    get:
      tags:
        - hr
      operationId: getHrAbsenceExitDashboard
      summary: Read absence-to-exit HR dashboard
      description: Lists absence alerts and exit-case workflow state for HR users who can triage Korean labor-law exit flows.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
      - name: employee_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Absence and exit-case dashboard data.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/hr/attendance-import/preview:
    post:
      tags:
        - hr
      operationId: previewAttendanceImport
      summary: Preview a direct attendance workbook or CSV through the governed import ledger
      description: Creates an immutable attendance_direct import run, preserves raw source rows, masks restricted values in
        the response, and records coordinate-free attendance facts only after dry-run and apply. Imported rows are lineage
        for payroll readiness, not payable payroll lines.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Direct attendance .xlsx workbook or UTF-8 .csv file.
      responses:
        '200':
          description: Attendance import preview with masked sample rows and mapping profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceImportPreviewResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/hr/attendance-import/summary:
    get:
      tags:
        - hr
      operationId: listAttendanceImportSummary
      summary: List governed direct attendance import runs
      description: Lists attendance_direct import runs and summaries for authorized HR readers without exposing raw source
        rows.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Direct attendance import run summaries.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceImportSummaryPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/hr/attendance-import/{run_id}/apply:
    post:
      tags:
        - hr
      operationId: applyAttendanceImport
      summary: Apply a dry-run direct attendance import as append-only coordinate-free facts
      description: Applies only a DRY_RUN attendance_direct import with no unresolved row errors. It writes lineage-preserving
        attendance_direct_import_events and leaves geofence-derived site_attendance_events unchanged.
      security:
      - bearerAuth: []
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImportApplyRequest'
      responses:
        '200':
          description: Apply counts for append-only direct attendance facts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceImportApplyReport'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/hr/attendance-import/{run_id}/dry-run:
    post:
      tags:
        - hr
      operationId: dryRunAttendanceImport
      summary: Resolve employees and branches for a direct attendance import without writing facts
      security:
      - bearerAuth: []
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Attendance import row readiness and per-row rejection reasons.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceImportDryRunSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/hr/attendance-records:
    get:
      tags:
        - hr
      operationId: listEmployeeAttendanceRecords
      summary: List employee attendance records for HR/payroll review
      description: Authorized HR/payroll readers can inspect direct attendance records and their payroll material lineage.
      security:
      - bearerAuth: []
      parameters:
      - name: employee_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: branch_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Attendance records within the caller's HR review scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAttendanceRecordPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/hr/attendance-records/me:
    get:
      tags:
        - hr
      operationId: listMyEmployeeAttendanceRecords
      summary: List the signed-in employee's attendance records
      description: Returns only records for the employee row explicitly linked to the authenticated user account.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Self-service attendance records and payroll material refs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAttendanceRecordPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags:
        - hr
      operationId: createMyEmployeeAttendanceRecord
      summary: Record the signed-in employee's attendance transition
      description: Appends a mobile/PC attendance fact and an immutable payroll material reference. Payroll calculation remains blocked by the legal gate.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEmployeeAttendanceRecordRequest'
      responses:
        '200':
          description: Created or idempotently replayed attendance record.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAttendanceRecord'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/hr/attendance-summary:
    get:
      tags:
        - hr
      operationId: listHrAttendanceSummary
      summary: List branch-scoped attendance summaries
      description: Read-only summary over durable site attendance events. It exposes business clock-in facts, not raw geolocation
        pings.
      security:
      - bearerAuth: []
      parameters:
      - name: branch_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Attendance summaries for users within the caller's branch scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceSummaryPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/hr/exit-cases:
    post:
      tags:
        - hr
      operationId: reportEmployeeExitCase
      summary: Report an employee exit case from an absence signal
      description: Creates an audited employee exit case that HR can confirm and prepare for settlement/approval workflows.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - employee_id
              - effective_exit_date
              - site_manager_note
              properties:
                employee_id:
                  $ref: '#/components/schemas/Uuid'
                branch_id:
                  $ref: '#/components/schemas/Uuid'
                  description: >-
                    Optional consistency assertion only. It must match the
                    durable branch derived from the named absence alert, the
                    employee home branch, or the latest attendance fact. It
                    never assigns or overrides authorization; a mismatch is 404.
                absence_alert_id:
                  $ref: '#/components/schemas/Uuid'
                  description: >-
                    Optional durable absence signal. When supplied, it must
                    exist for this exact employee or the request is 404; its
                    stored branch takes precedence in branch authorization.
                effective_exit_date:
                  type: string
                site_manager_note:
                  type: string
      responses:
        '201':
          description: Employee exit case created.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/hr/exit-cases/{id}/approval-draft:
    post:
      tags:
        - hr
      operationId: draftEmployeeExitApproval
      summary: Draft or submit employee exit approval documents
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                submit:
                  type: boolean
                  default: false
                note:
                  type: string
                settlement_input:
                  type: object
                  additionalProperties: true
      responses:
        '200':
          description: Employee exit case with generated approval payloads.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/hr/exit-cases/{id}/confirm:
    post:
      tags:
        - hr
      operationId: confirmEmployeeExitCase
      summary: Confirm an employee exit case for HR/HQ review
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                decision:
                  type: string
                hq_confirmation:
                  type: boolean
                  default: false
                note:
                  type: string
                settlement_input:
                  type: object
                  additionalProperties: true
      responses:
        '200':
          description: Employee exit case after confirmation.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/hr/leave-balances:
    get:
      tags:
        - hr
      operationId: listHrLeaveBalances
      summary: List employee leave-balance rows
      description: Read-only HR leave summary extracted from imported employee workbooks. Payroll and sensitive columns remain
        preserved in raw import JSON unless explicitly modeled.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Leave-balance rows and aggregate totals.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveBalancePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/hr/org-chart:
    get:
      tags:
        - hr
      operationId: getHrOrgChart
      summary: Read the tenant HR organization chart
      description: Executive/admin/super-admin read model grouped by company, org unit, and position. Source workbook rows
        remain preserved in the employee directory.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Organization chart grouped from imported employee rows.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HrOrgChartResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/hr/readiness-summary:
    get:
      tags:
        - hr
      operationId: getHrReadinessSummary
      summary: Read HR data readiness counters
      description: Returns import, payroll, annual-leave, and attendance readiness counters for org-wide HR operators without
        exposing raw workbook rows. `payroll.active_close_runs` counts only STAGED, BLOCKED_LEGAL_GATE,
        READY_FOR_REVIEW, and APPROVED payroll runs; terminal ISSUED and VOID history is excluded.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: HR readiness counters.
          content:
            application/json:
              schema:
                type: object
                required:
                - payroll
                properties:
                  payroll:
                    type: object
                    required:
                    - active_close_runs
                    properties:
                      active_close_runs:
                        type: integer
                        format: int64
                        minimum: 0
                        description: Number of inspectable non-terminal payroll close runs.
                    additionalProperties: true
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/inspections/my-schedules:
    get:
      tags:
        - inspections
      operationId: listMyInspectionSchedules
      summary: List the authenticated mechanic's assigned regular inspection schedules
      security:
      - bearerAuth: []
      parameters:
      - name: due_start
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Date'
      - name: due_end
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Date'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 500
          default: 200
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: A branch-scoped page of schedules assigned to the authenticated mechanic.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InspectionSchedulePage'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/inspections/schedules:
    get:
      tags:
        - inspections
      operationId: listInspectionSchedules
      summary: List branch-scoped regular inspection schedules
      security:
      - bearerAuth: []
      parameters:
      - name: due_start
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Date'
      - name: due_end
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Date'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 500
          default: 200
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: A page of inspection schedules due in the requested range.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InspectionSchedulePage'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    post:
      tags:
        - inspections
      operationId: createInspectionSchedule
      summary: Create a regular inspection schedule
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateInspectionScheduleRequest'
      responses:
        '201':
          description: Inspection schedule created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InspectionScheduleSummary'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/inspections/schedules/{schedule_id}/rounds:
    post:
      tags:
        - inspections
      operationId: completeInspectionRound
      summary: Complete a scheduled regular inspection round
      security:
      - bearerAuth: []
      parameters:
      - name: schedule_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteInspectionRoundRequest'
      responses:
        '201':
          description: Inspection round completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InspectionRoundSummary'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/integrity/findings:
    get:
      tags:
        - integrity
      operationId: listIntegrityFindings
      summary: List governance findings (anomaly / 검토 필요) for the current org
      description: 'Lists governance findings raised by the integrity engine (#34): records that need human review such as
        self-approval (자가 승인 기록) and price anomalies (이상 징후). Findings are framed as "검토 필요" — items requiring review, NOT
        accusations of wrongdoing. Gated to EXECUTIVE and SUPER_ADMIN only (IntegrityFindingsRead); an ADMIN must not read
        findings about themselves. RLS-armed to the caller''s org. Results are ordered by severity (CRITICAL first) then detected_at
        descending. This endpoint is not paginated.'
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        description: Optional lifecycle filter. Omit to return findings of every status.
        schema:
          $ref: '#/components/schemas/FindingStatus'
      responses:
        '200':
          description: The list of governance findings for the current org.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/GovernanceFinding'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/integrity/findings/{id}/triage:
    post:
      tags:
        - integrity
      operationId: triageIntegrityFinding
      summary: Triage (review) a governance finding
      description: Transitions an OPEN finding to REVIEWED, DISMISSED, or ESCALATED with an optional reviewer memo. A memo
        is required when dismissing or escalating. Only OPEN findings can be triaged. The triage itself is audited. Gated
        to EXECUTIVE and SUPER_ADMIN only (IntegrityFindingTriage).
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TriageFindingRequest'
      responses:
        '200':
          description: The updated governance finding after triage.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GovernanceFinding'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/inventory/cycle-counts:
    get:
      tags: [inventory]
      operationId: listInventoryCycleCounts
      summary: List tenant- and branch-scoped cycle counts
      security: [{ bearerAuth: [] }]
      parameters: [{ name: branchId, in: query, required: true, schema: { $ref: '#/components/schemas/Uuid' } }, { name: status, in: query, required: false, schema: { type: string, enum: [DRAFT, SUBMITTED, APPROVED, REJECTED, CANCELLED] } }, { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 100 } }, { name: offset, in: query, required: false, schema: { type: integer, minimum: 0 } }]
      responses: { '200': { description: Cycle count page, content: { application/json: { schema: { $ref: '#/components/schemas/CycleCountPage' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' } }
    post:
      tags: [inventory]
      operationId: openInventoryCycleCount
      summary: Open a cycle count in an authorized branch and stock location
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/OpenCycleCountRequest' } } } }
      responses: { '200': { description: Cycle count, content: { application/json: { schema: { $ref: '#/components/schemas/CycleCountDetail' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '422': { $ref: '#/components/responses/ValidationError' } }
  /api/v1/inventory/cycle-counts/{count_id}:
    get:
      tags: [inventory]
      operationId: getInventoryCycleCount
      summary: Read one authorized cycle count with count lines and applied adjustment movement ids
      security: [{ bearerAuth: [] }]
      parameters: [{ name: count_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses: { '200': { description: Cycle count detail, content: { application/json: { schema: { $ref: '#/components/schemas/CycleCountDetail' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/v1/inventory/cycle-counts/{count_id}/cancel:
    post:
      tags: [inventory]
      operationId: cancelInventoryCycleCount
      summary: Cancel a DRAFT or SUBMITTED count without mutating ledger history
      security: [{ bearerAuth: [] }]
      parameters: [{ name: count_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CycleCountVersionRequest' } } } }
      responses: { '200': { description: Cancelled cycle count, content: { application/json: { schema: { $ref: '#/components/schemas/CycleCountDetail' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/v1/inventory/cycle-counts/{count_id}/decision:
    post:
      tags: [inventory]
      operationId: decideInventoryCycleCount
      summary: Distinct checker approves idempotent adjustments or rejects with a memo
      security: [{ bearerAuth: [] }]
      parameters: [{ name: count_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/DecideCycleCountRequest' } } } }
      responses: { '200': { description: Decided cycle count, content: { application/json: { schema: { $ref: '#/components/schemas/CycleCountDetail' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '409': { $ref: '#/components/responses/Conflict' }, '422': { $ref: '#/components/responses/ValidationError' } }
  /api/v1/inventory/cycle-counts/{count_id}/lines:
    post:
      tags: [inventory]
      operationId: upsertInventoryCycleCountLine
      summary: Upsert a DRAFT count line; a nonzero variance requires a typed reason
      security: [{ bearerAuth: [] }]
      parameters: [{ name: count_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/UpsertCycleCountLineRequest' } } } }
      responses: { '200': { description: Updated cycle count, content: { application/json: { schema: { $ref: '#/components/schemas/CycleCountDetail' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '409': { $ref: '#/components/responses/Conflict' }, '422': { $ref: '#/components/responses/ValidationError' } }
  /api/v1/inventory/cycle-counts/{count_id}/submit:
    post:
      tags: [inventory]
      operationId: submitInventoryCycleCount
      summary: Submit a nonempty DRAFT cycle count using optimistic version control
      security: [{ bearerAuth: [] }]
      parameters: [{ name: count_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CycleCountVersionRequest' } } } }
      responses: { '200': { description: Submitted cycle count, content: { application/json: { schema: { $ref: '#/components/schemas/CycleCountDetail' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '409': { $ref: '#/components/responses/Conflict' } }
  /api/v1/inventory/items:
    get:
      tags: [inventory]
      operationId: listInventoryItems
      summary: List tenant-scoped inventory items
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: branch_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: site_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: stock_location_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: status, in: query, required: false, schema: { type: string } }
        - { name: low_stock, in: query, required: false, schema: { type: boolean } }
        - { name: q, in: query, required: false, schema: { type: string } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 200 } }
        - { name: offset, in: query, required: false, schema: { type: integer, minimum: 0 } }
      responses:
        '200': { description: Inventory page, content: { application/json: { schema: { $ref: '#/components/schemas/InventoryItemPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /api/v1/inventory/items/{item_id}:
    get:
      tags: [inventory]
      operationId: getInventoryItem
      summary: Get one tenant-authorized inventory item
      security: [{ bearerAuth: [] }]
      parameters: [{ name: item_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses:
        '200': { description: Inventory item, content: { application/json: { schema: { $ref: '#/components/schemas/InventoryItem' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/inventory/items/{item_id}/consumptions:
    get:
      tags: [inventory]
      operationId: listInventoryConsumptions
      summary: List immutable item-consumption events
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: item_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: source_kind, in: query, required: false, schema: { type: string } }
        - { name: work_order_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: dispatch_id, in: query, required: false, schema: { $ref: '#/components/schemas/Uuid' } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 200 } }
        - { name: offset, in: query, required: false, schema: { type: integer, minimum: 0 } }
      responses:
        '200': { description: Consumption event page, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/InventoryConsumptionEvent' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    post:
      tags: [inventory]
      operationId: consumeInventoryItem
      summary: Record an idempotent, non-negative inventory consumption
      security: [{ bearerAuth: [] }]
      parameters: [{ name: item_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ConsumeInventoryItemRequest' } } } }
      responses:
        '200': { description: Consumption result, content: { application/json: { schema: { $ref: '#/components/schemas/InventoryConsumptionResult' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/ValidationError' }
  /api/v1/inventory/items/{item_id}/movements:
    get:
      tags: [inventory]
      operationId: listInventoryMovements
      summary: Read the tenant- and branch-scoped unified ISSUE, RECEIPT, and ADJUSTMENT ledger
      security: [{ bearerAuth: [] }]
      parameters: [{ name: item_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }, { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 100 } }, { name: offset, in: query, required: false, schema: { type: integer, minimum: 0 } }]
      responses: { '200': { description: Unified movement ledger, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/InventoryMovement' } } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '404': { $ref: '#/components/responses/NotFound' } }
  /api/v1/inventory/items/{item_id}/receipts:
    post:
      tags: [inventory]
      operationId: receiveInventoryItem
      summary: Idempotently record a positive receipt and its before delta after ledger row
      security: [{ bearerAuth: [] }]
      parameters: [{ name: item_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RecordInventoryReceiptRequest' } } } }
      responses: { '200': { description: Receipt result, content: { application/json: { schema: { $ref: '#/components/schemas/InventoryReceiptResult' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '404': { $ref: '#/components/responses/NotFound' }, '409': { $ref: '#/components/responses/Conflict' }, '422': { $ref: '#/components/responses/ValidationError' } }
  /api/v1/inventory/mrp:
    get:
      tags: [inventory]
      operationId: getInventoryMrp
      summary: Deterministic movement-derived MRP with explicit zero inbound and reservation values until those modules exist
      security: [{ bearerAuth: [] }]
      parameters: [{ name: branchId, in: query, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      responses: { '200': { description: MRP lines, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/InventoryMrpLine' } } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' } }
  /api/v1/job-positions:
    get:
      operationId: listJobPositions
      summary: List canonical JobPosition heads
      description: >-
        Returns current JobPosition Heads in the armed tenant — the same rows `list`
        already returns (latest revision, `MAX(version)`). as_of / from / to are
        omitted: `job_positions` / `job_position_revisions` have no valid-time
        columns, and using `created_at` as `valid_from` would be a second time
        model. Empty when none are visible. Distinct from recruiting postings and
        from `employees.position` TEXT. EmployeeDetail as_of remains HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      responses:
        '200':
          description: Current JobPosition heads.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/JobPosition'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/job-positions/{id}:
    get:
      operationId: getJobPosition
      summary: Get a canonical JobPosition head
      description: >-
        Returns one JobPosition Head — the current latest revision (`MAX(version)`),
        the same row `get` already returns. as_of is omitted: `job_positions` /
        `job_position_revisions` have no valid-time columns, and using `created_at`
        as `valid_from` would be a second time model. A miss or a foreign tenant is
        404 (deny-by-omission). Distinct from recruiting postings and from
        `employees.position` TEXT. EmployeeDetail as_of remains HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Current JobPosition head.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobPosition'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/kpi:
    get:
      tags:
        - kpi
      operationId: getKpiReport
      summary: Fetch branch-scoped KPI rollups for the seven standard metrics
      description: Computes KPI metrics from approved work-order reports within the requested approval period. Metrics whose
        source domains have not merged yet are returned in `unavailable_metrics` instead of fabricated values.
      security:
      - bearerAuth: []
      parameters:
      - name: period
        in: query
        required: true
        schema:
          type: string
          pattern: ^[0-9]{4}-[0-9]{2}-[0-9]{2}\.\.[0-9]{4}-[0-9]{2}-[0-9]{2}$
        example: 2026-06-01..2026-07-01
        description: Inclusive start date and exclusive end date in UTC day boundaries.
      - name: scope
        in: query
        required: false
        schema:
          type: string
          default: company
        examples:
          company:
            value: company
          region:
            value: region:00000000-0000-0000-0000-000000000000
          branch:
            value: branch:00000000-0000-0000-0000-000000000000
          technician:
            value: technician:00000000-0000-0000-0000-000000000000
      responses:
        '200':
          description: KPI report with company, region, branch, and technician rollups constrained by the authenticated principal
            branch scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KpiReport'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/leave/balances:
    get:
      tags:
        - leave
      operationId: listLeaveBalances
      summary: Per-employee annual-leave balance roster (직원별 연차 현황)
      description: >-
        Reads the existing employee leave ledger (grant/used/left) — the same
        source of truth as the balances aggregate; not a second store. Requires
        `employee_directory_read`. Org-scoped.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The leave-balance roster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRosterPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/leave/promotions:
    post:
      tags:
        - leave
      operationId: pushLeavePromotion
      summary: Serve a §61 연차 사용 촉진 (round 1 or 2)
      description: >-
        Requires `employee_directory_manage` in the target `branch_id` (which is
        validated against the actor's scope). Delivers a receipt-gated 연차촉진
        notice into the target's 개인 수신함 and records the push. The engine AP-
        run binds once the 연차촉진 submittable definition exists; until then the
        push carries `ap_submission: pending_engine_definition`. Idempotent per
        (target, round).
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeavePromotionRequest'
      responses:
        '200':
          description: The recorded push, including the delivered notice id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveStatutoryPushView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          description: Invalid round (§61 allows 1 or 2).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/leave/refusal-notices:
    post:
      tags:
        - leave
      operationId: pushLeaveRefusalNotice
      summary: Serve a 노무수령거부 notice (after a round-2 promotion)
      description: >-
        Requires `employee_directory_manage` in the target `branch_id`. Delivers
        a receipt-gated 노무수령거부 notice into the target's 개인 수신함 and records
        the push. Same engine-binding semantics as promotions. Idempotent per
        target.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveRefusalRequest'
      responses:
        '200':
          description: The recorded refusal push, including the delivered notice id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveStatutoryPushView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/leave/requests:
    get:
      tags:
        - leave
      operationId: listLeaveRequests
      summary: List the branch-scoped leave-request approval queue (연차 결재함)
      description: >-
        Pending-first, then newest. Requires `employee_directory_read`. The
        queue is confined to the caller's branches (resolved from the JWT); an
        out-of-scope request is invisible (deny-by-omission).
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        description: >-
          Filter to one status; omitted returns every status, including the
          read-only historical `returned`/`rejected` rows.
        schema:
          type: string
          enum: [pending, approved, time_change_consult, returned, rejected]
      - name: limit
        in: query
        required: false
        description: Page size (clamped server-side to 1..=200; default 100).
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: A branch-scoped page of leave requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequestPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/leave/requests/{id}/decide:
    post:
      tags:
        - leave
      operationId: decideLeaveRequest
      summary: Approve a pending leave request or open a 시기변경 협의
      description: >-
        Requires `employee_directory_manage` in the request's branch. An
        APPROVE writes the leave ledger (used += days, remaining -= days) in the
        same audited transaction. Separation of duties — a request cannot be
        decided by its own requester (403). 근로기준법 §60 forbids refusing 연차:
        `return`/`reject` are retired (422) and the only non-approve decision is
        `time_change` (§60⑤ 시기변경 협의), which requires a comment stating the
        사업 운영에 막대한 지장 grounds and never touches the ledger.
        A non-pending request is 409; an out-of-branch / unknown request is 404.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveDecideRequest'
      responses:
        '200':
          description: The decided leave request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequestView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The request is not pending and cannot be decided again.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          description: Missing mandatory comment or unknown decision.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/lifecycles/{objectType}/{objectId}:
    get:
      tags:
        - lifecycles
      operationId: getObjectLifecycle
      summary: Read one object's lifecycle state and transition log
      description: Lifecycle-authority gated (LifecycleManage, org-wide). Returns the current FSM state, legal hold /
        retention flags, and the append-only transition history (newest first).
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/LifecycleObjectType'
      - $ref: '#/components/parameters/LifecycleObjectId'
      responses:
        '200':
          description: Lifecycle record.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectLifecycle'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/lifecycles/{objectType}/{objectId}/hold:
    post:
      tags:
        - lifecycles
      operationId: setObjectLifecycleHold
      summary: Set or clear legal hold / retention on one object's lifecycle
      description: Lifecycle-authority gated (LifecycleManage, org-wide), audited. Creates the lifecycle row at draft when
        absent so a hold can be placed before the object ever transitions.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/LifecycleObjectType'
      - $ref: '#/components/parameters/LifecycleObjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetLifecycleHoldRequest'
      responses:
        '200':
          description: Hold updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectLifecycle'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/lifecycles/{objectType}/{objectId}/transition:
    post:
      tags:
        - lifecycles
      operationId: transitionObjectLifecycle
      summary: Transition one object's lifecycle state
      description: 'Lifecycle-authority gated (LifecycleManage, org-wide), audited. The transition is validated against the
        seeded per-object-type rule table (document: draft → submitted → approved → active → revised → archived → disposed);
        an illegal transition returns 409. The disposed transition additionally fails closed with 409 while the object is
        under legal hold or its retentionUntil lies in the future. The first legal transition of an unknown object
        implicitly registers its lifecycle at draft.'
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/LifecycleObjectType'
      - $ref: '#/components/parameters/LifecycleObjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransitionLifecycleRequest'
      responses:
        '200':
          description: Transition applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectLifecycle'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/link-types:
    get:
      tags:
        - link-types
      operationId: listLinkTypes
      summary: List the edge-type registry (object-link relationship vocabulary)
      description: Returns the seeded, platform-wide set of relationship labels an object_link may use. createObjectLink
        validates link_type against this registry.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The edge-type registry.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LinkTypeResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/location-consent/grant:
    post:
      tags:
        - location-consent
      operationId: grantLocationConsent
      summary: Grant GPS location collection consent
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationConsentTransitionRequest'
      responses:
        '200':
          description: Updated location consent status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationConsentStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/location-consent/resume:
    post:
      tags:
        - location-consent
      operationId: resumeLocationConsent
      summary: Resume GPS collection after an off-switch suspension
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationConsentTransitionRequest'
      responses:
        '200':
          description: Updated location consent status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationConsentStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/location-consent/status:
    get:
      tags:
        - location-consent
      operationId: getLocationConsentStatus
      summary: Read the authenticated user's location consent status
      security:
      - bearerAuth: []
      parameters:
      - name: branch_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Current location consent status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationConsentStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/location-consent/suspend:
    post:
      tags:
        - location-consent
      operationId: suspendLocationConsent
      summary: Suspend GPS collection with the always-visible off switch
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationConsentTransitionRequest'
      responses:
        '200':
          description: Updated location consent status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationConsentStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/location-consent/withdraw:
    post:
      tags:
        - location-consent
      operationId: withdrawLocationConsent
      summary: Withdraw location consent and destroy destructible location data
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationConsentTransitionRequest'
      responses:
        '200':
          description: Updated location consent status after destructive withdrawal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationConsentStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/location-consents/ledger:
    get:
      tags:
        - location-consents
      operationId: listLocationConsentLedger
      summary: Read the consent lifecycle ledger
      security:
      - bearerAuth: []
      parameters:
      - name: user_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: branch_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 100
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paged consent lifecycle ledger.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationConsentLedgerPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/location-consents/ledger.csv:
    get:
      tags:
        - location-consents
      operationId: exportLocationConsentLedgerCsv
      summary: Export the consent lifecycle ledger as CSV
      security:
      - bearerAuth: []
      parameters:
      - name: user_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: branch_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 100
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: CSV consent lifecycle ledger export.
          content:
            text/csv:
              schema:
                type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/location-pings:
    post:
      tags:
        - location-pings
      operationId: recordLocationPing
      summary: Ingest an on-duty GPS ping when consent is granted
      description: Coordinates are destructible and never written to audit_events.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationPingRequest'
      responses:
        '204':
          description: Location ping accepted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/location/arrival-events:
    get:
      tags:
        - location
      operationId: listArrivalEvents
      summary: Read the site arrival/departure events log (#13)
      description: Ops-facing feed of mechanic site arrivals/departures derived from on-duty location pings (geofenced). OpsDashboardRead-gated;
        tenant + branch scoped.
      security:
      - bearerAuth: []
      parameters:
      - name: user_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: branch_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 1000
          default: 100
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paged site arrival/departure events.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArrivalEventPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/logistics/asns:
    post:
      tags: [logistics]
      operationId: createLogisticsAsn
      summary: Create a pilot ASN for one branch warehouse
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [branchId, warehouseCode, externalReference, sku, expectedQuantity], properties: { branchId: { $ref: '#/components/schemas/Uuid' }, warehouseCode: { type: string }, externalReference: { type: string }, sku: { type: string }, expectedQuantity: { type: integer, minimum: 1 } } } } } }
      responses: { '201': { description: ASN created, content: { application/json: { schema: { $ref: '#/components/schemas/LogisticsAsnCreated' } } } }, '401': { $ref: '#/components/responses/Unauthorized' }, '403': { $ref: '#/components/responses/Forbidden' }, '422': { $ref: '#/components/responses/ValidationError' } }
  /api/v1/logistics/asns/{asn_id}/putaway:
    post:
      tags: [logistics]
      operationId: putawayLogisticsAsn
      summary: Put received stock away into the pilot warehouse
      security: [{ bearerAuth: [] }]
      parameters: [{ name: asn_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [branchId], properties: { branchId: { $ref: '#/components/schemas/Uuid' } } } } } }
      responses: { '200': { description: Stock available for fulfillment, content: { application/json: { schema: { $ref: '#/components/schemas/LogisticsAsnPutaway' } } } }, '409': { description: Illegal state } }
  /api/v1/logistics/asns/{asn_id}/receipts:
    post:
      tags: [logistics]
      operationId: receiveLogisticsAsn
      summary: Record an idempotent full or partial receipt; over-receipt is rejected
      security: [{ bearerAuth: [] }]
      parameters: [{ name: asn_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }, { name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 16, maxLength: 200 } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [branchId, receivedQuantity], properties: { branchId: { $ref: '#/components/schemas/Uuid' }, receivedQuantity: { type: integer, minimum: 1 } } } } } }
      responses: { '200': { description: Receipt recorded or replayed, content: { application/json: { schema: { $ref: '#/components/schemas/LogisticsAsnReceipt' } } } }, '409': { description: "Conflicting replay, over-receipt, or illegal state" }, '422': { $ref: '#/components/responses/ValidationError' } }
  /api/v1/logistics/fulfillments:
    post:
      tags: [logistics]
      operationId: releaseLogisticsFulfillment
      summary: Atomically reserve available pilot stock; never oversells
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [branchId, warehouseCode, sku, requestedQuantity, dueAt], properties: { branchId: { $ref: '#/components/schemas/Uuid' }, warehouseCode: { type: string }, sku: { type: string }, requestedQuantity: { type: integer, minimum: 1 }, dueAt: { $ref: '#/components/schemas/LogisticsTimeTuple' } } } } } }
      responses: { '201': { description: Fulfillment released, content: { application/json: { schema: { $ref: '#/components/schemas/LogisticsFulfillmentReleased' } } } }, '409': { description: Insufficient available stock } }
  /api/v1/logistics/fulfillments/{fulfillment_id}/dispatch:
    post:
      tags: [logistics]
      operationId: dispatchLogisticsShipment
      summary: Dispatch one packed carrier and vehicle leg
      security: [{ bearerAuth: [] }]
      parameters: [{ name: fulfillment_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: false, required: [branchId, carrierName, vehicleReference], properties: { branchId: { $ref: '#/components/schemas/Uuid' }, carrierName: { type: string, maxLength: 120 }, vehicleReference: { type: string, maxLength: 120 } } } } } }
      responses: { '201': { description: Shipment dispatched, content: { application/json: { schema: { $ref: '#/components/schemas/LogisticsShipmentDispatched' } } } }, '409': { description: Illegal transition } }
  /api/v1/logistics/fulfillments/{fulfillment_id}/pack:
    post:
      tags: [logistics]
      operationId: packLogisticsFulfillment
      summary: Pack a picked or short-picked fulfillment
      security: [{ bearerAuth: [] }]
      parameters: [{ name: fulfillment_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: false, required: [branchId], properties: { branchId: { $ref: '#/components/schemas/Uuid' } } } } } }
      responses: { '200': { description: Fulfillment packed, content: { application/json: { schema: { $ref: '#/components/schemas/LogisticsFulfillmentPacked' } } } }, '409': { description: Illegal state } }
  /api/v1/logistics/fulfillments/{fulfillment_id}/pick:
    post:
      tags: [logistics]
      operationId: pickLogisticsFulfillment
      summary: Pick reserved stock, recording explicit short-pick when applicable
      security: [{ bearerAuth: [] }]
      parameters: [{ name: fulfillment_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: false, required: [branchId, pickedQuantity], properties: { branchId: { $ref: '#/components/schemas/Uuid' }, pickedQuantity: { type: integer, format: int64, minimum: 0 } } } } } }
      responses: { '200': { description: Pick outcome, content: { application/json: { schema: { $ref: '#/components/schemas/LogisticsFulfillmentPicked' } } } }, '409': { description: Illegal state } }
  /api/v1/logistics/shipments/{shipment_id}/pod:
    post:
      tags: [logistics]
      operationId: verifyLogisticsPod
      summary: Verify recipient-confirmed immutable evidence and derive calendar-time SLA
      security: [{ bearerAuth: [] }]
      parameters: [{ name: shipment_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: false, required: [branchId, recipientName, evidenceReference, confirmedAt], properties: { branchId: { $ref: '#/components/schemas/Uuid' }, recipientName: { type: string, maxLength: 160 }, evidenceReference: { type: string, minLength: 19, maxLength: 411, pattern: '^evidence://[A-Za-z0-9._/-]+$' }, confirmedAt: { $ref: '#/components/schemas/LogisticsTimeTuple' } } } } } }
      responses: { '200': { description: POD verified with MET or BREACHED SLA, content: { application/json: { schema: { $ref: '#/components/schemas/LogisticsPodVerified' } } } }, '409': { description: Illegal transition or reused evidence } }
  /api/v1/logistics/shipments/{shipment_id}/settlements:
    post:
      tags: [logistics]
      operationId: settleLogisticsOperationalCost
      summary: Settle one KRW operational transport cost; no finance or GL posting
      security: [{ bearerAuth: [] }]
      parameters: [{ name: shipment_id, in: path, required: true, schema: { $ref: '#/components/schemas/Uuid' } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, additionalProperties: false, required: [branchId, currencyCode, amountMinor, settledAt], properties: { branchId: { $ref: '#/components/schemas/Uuid' }, currencyCode: { type: string, enum: [KRW] }, amountMinor: { type: integer, format: int64, minimum: 0 }, settledAt: { $ref: '#/components/schemas/LogisticsTimeTuple' } } } } } }
      responses: { '200': { description: Operational cost settled, content: { application/json: { schema: { $ref: '#/components/schemas/LogisticsShipmentSettlement' } } } }, '409': { description: Verified POD required } }
  /api/v1/mail/account:
    get:
      tags:
        - mail
      operationId: getMailAccount
      summary: Get the tenant's configured webmail account (write-only password)
      description: Returns the configured corporate mailbox for the caller's tenant. The response NEVER contains a password;
        the has_smtp_password / has_imap_password booleans signal whether a credential is on file. Requires the MailAccountManage
        feature.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The configured mailbox.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MailAccountView'
        '204':
          description: No mailbox is configured for this tenant.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/MailUnavailable'
    put:
      tags:
        - mail
      operationId: configureMailAccount
      summary: Configure (create or replace) the tenant's webmail account
      description: 'Upserts the mailbox config. Passwords are write-only: a present password is sealed (envelope AEAD) and
        only the ciphertext is stored; an absent/null password leaves the stored secret unchanged. A first-time configure
        requires both the SMTP and IMAP password. Audited. Requires the MailAccountManage feature.'
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigureMailAccountRequest'
      responses:
        '200':
          description: The configured mailbox (write-only view).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MailAccountView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/account/test:
    post:
      tags:
        - mail
      operationId: testMailAccountConnection
      summary: Test the SMTP connection with the stored credentials
      description: Authenticates to the tenant's configured SMTP server using the stored credentials and reports a structured
        result. Never leaks the secret; the error_code is a stable, non-secret token. Requires the MailAccountManage feature.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The structured test-connection result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MailTestConnectionResult'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/attachments/{id}/download:
    get:
      tags:
        - mail
      operationId: downloadMailAttachment
      summary: Get a presigned download URL for an attachment
      description: Resolves the attachment under the caller's org (cross-tenant invisible) and returns a short-lived presigned
        GET URL — the raw object key is never exposed. RLS-armed to the caller's org. Requires the MailUse feature.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: A short-lived presigned download URL.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MailAttachmentDownload'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/folders:
    get:
      tags:
        - mail
      operationId: listMailFolders
      summary: List the tenant mailbox folders
      description: Lists the configured mailbox's folders (Inbox/Sent/…) with per-folder unread + total counts. RLS-armed
        to the caller's org. Requires the MailUse feature.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The folders for the tenant's mailbox (empty if unconfigured).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MailFolderView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/forward:
    post:
      tags:
        - mail
      operationId: forwardMail
      summary: Forward a message (sets In-Reply-To / References)
      description: Forwards a message through the tenant's SMTP server, stamping the In-Reply-To and References threading
        headers, and persists it as direction=OUT. Audited (email.forward). Requires the MailUse feature.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendMailRequest'
      responses:
        '201':
          description: The forward was sent and persisted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendMailResult'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/messages/{id}:
    get:
      tags:
        - mail
      operationId: getMailMessage
      summary: Get one message with its attachments
      description: Returns one message (with attachment metadata; bytes are fetched via the attachment download endpoint).
        body_html is verbatim and MUST be sanitized by the client. RLS-armed to the caller's org. Requires the MailUse feature.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: The message and its attachments.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MailMessageView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/reply:
    post:
      tags:
        - mail
      operationId: replyMail
      summary: Reply to a message (sets In-Reply-To / References)
      description: Sends a reply through the tenant's SMTP server, stamping the In-Reply-To and References threading headers,
        and persists it as direction=OUT. Audited (email.reply). Requires the MailUse feature.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendMailRequest'
      responses:
        '201':
          description: The reply was sent and persisted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendMailResult'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/send:
    post:
      tags:
        - mail
      operationId: sendMail
      summary: Compose and send a new message
      description: Sends a new outbound message through the tenant's SMTP server and persists it as a direction=OUT message.
        The From is constrained to the configured account address. Audited (email.send). Requires the MailUse feature.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendMailRequest'
      responses:
        '201':
          description: The message was sent and persisted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendMailResult'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/threads:
    get:
      tags:
        - mail
      operationId: listMailThreads
      summary: List mail threads (paginated, unread filter, search)
      description: 'Lists the mailbox''s threads newest-first. Optional filters: unread-only, a full-text search term q, a
        folder id, and a keyset cursor (before, a unix-second last_message_at). RLS-armed to the caller''s org. Requires the
        MailUse feature.'
      security:
      - bearerAuth: []
      parameters:
      - name: unread
        in: query
        required: false
        schema:
          type: boolean
      - name: q
        in: query
        required: false
        schema:
          type: string
      - name: folder
        in: query
        required: false
        schema:
          type: string
          format: uuid
      - name: before
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: A page of threads (newest first).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MailThreadView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/threads/{id}:
    get:
      tags:
        - mail
      operationId: getMailThread
      summary: Get one thread with its messages
      description: Returns the thread and its messages (oldest first). body_html is returned verbatim and MUST be sanitized
        by the client before render. RLS-armed to the caller's org. Requires the MailUse feature.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: The thread and its ordered messages.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MailThreadDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/mail/threads/{id}/read-state:
    patch:
      tags:
        - mail
      operationId: setMailThreadReadState
      summary: Mark a mail thread read or unread
      description: Sets every inbound message in the visible thread to read or unread and recomputes thread/folder unread
        aggregates. The action is RLS-armed to the caller's org and audited without duplicating message content. Requires
        the MailUse feature.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MailThreadReadStateRequest'
      responses:
        '204':
          description: Thread read-state updated.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/MailUnavailable'
  /api/v1/me/action-inbox:
    get:
      tags:
        - me
      operationId: listMyActionInbox
      summary: Unified action inbox for the authenticated principal
      description: >-
        One server-side fan-in of the caller's actionable items across every
        source that owns a person-scoped list: workflow/approval tasks awaiting
        the caller (the ?assignee=me path), pending P1 dispatch offers, support
        tickets assigned to the caller, and work orders assigned to the caller.
        Each source is queried through the exact predicate its own list endpoint
        uses, so the aggregate never widens visibility (deny-by-omission). Items
        are bucketed by urgency (now/today/wait) with a derived due tone. Fields
        the overview prototype carries but no backend source can supply are
        omitted (entity, amount, detail, files, stats, mailId); site/who/submitted
        are present only for the sources that carry them. Attendance exceptions
        are not aggregated (no exception object exists yet). Results use an
        immutable `(created_at, kind:id)` traversal key plus an `as_of` admission
        boundary, so rows created after the first page are excluded. Membership is
        deliberately live rather than a repeatable full-state snapshot: resolved,
        reassigned, or expired rows can disappear between page requests. Totals are
        recalculated from live membership admitted by `as_of`; `total_is_exact` is
        false when an authorization-filtered source reaches its bounded counting
        budget. Source failures fail the whole request rather than returning a
        deceptively partial queue.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        description: Page size, clamped server-side to 1..=200.
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 100
      - name: cursor
        in: query
        required: false
        description: Opaque immutable-traversal cursor returned as `next_cursor` by the previous page.
        schema:
          type: string
      responses:
        '200':
          description: The caller's actionable items in immutable creation order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActionInboxResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          description: The action-inbox cursor is malformed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '500':
          description: A source failed; no partial action-inbox response is returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/authz:
    get:
      tags:
        - me
      operationId: getCurrentUserAuthz
      summary: Get the authenticated caller's authorization projection
      description: >-
        The caller's authorization projection: org, branch scope,
        roles-as-principal-attributes, and the legacy-matrix capability grants
        the console needs for deny-by-omission rendering. NON-AUTHORITATIVE
        (`authority = "advisory_ui_only"`) — the server remains the sole
        enforcer; this is a rendering hint that lets the frontend gate on grants
        instead of hardcoded role lists. `source = "legacy_matrix"` today; the
        Cedar enforce-flip later flips `source` to `"cedar"` with this shape
        unchanged. Capabilities are deny-by-omission: a feature the caller cannot
        use is omitted (never emitted at `deny`).
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The caller's authorization projection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeAuthzResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/dispatch-offers:
    get:
      tags:
        - me
      operationId: listMyDispatchOffers
      summary: List the authenticated mechanic's pending P1 dispatch offers
      description: >-
        BROADCASTING dispatches that fanned out to the caller as a TECHNICIAN,
        still inside the accept window, with no response from the caller yet.
        Person-scoped by construction (deny-by-omission): a caller only ever
        sees offers addressed to them. Respond via
        /api/v1/p1-dispatches/{dispatchId}/responses.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The caller's pending dispatch offers.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MyDispatchOfferPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/inbox-docs:
    get:
      tags:
        - me
      operationId: listMyInboxDocs
      summary: List the authenticated user's statutory-notice vault (개인 수신함)
      description: >-
        Metadata only — a locked legal notice's body never appears in the list.
        The recipient is bound from the JWT; a non-recipient sees nothing.
      security:
      - bearerAuth: []
      parameters:
      - name: filter
        in: query
        required: false
        description: >-
          확인 필요 (`action`) = legal notices awaiting receipt; 급여명세 (`pay`) =
          payslips; 완료 (`done`) = confirmed; 전체 (`all`, default).
        schema:
          type: string
          enum: [action, pay, done, all]
      - name: before
        in: query
        required: false
        description: Keyset cursor; return documents strictly older than this id.
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        description: Page size (clamped server-side to 1..=200; default 50).
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: A page of the caller's inbox documents.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InboxDocPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          description: Unknown filter value.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/inbox-docs/{id}:
    get:
      tags:
        - me
      operationId: getMyInboxDoc
      summary: Read one of the authenticated user's inbox documents
      description: >-
        Reading a LOCKED legal notice returns its metadata with `payload`
        omitted and does NOT auto-confirm receipt. A payslip (and an
        already-confirmed legal notice) returns its `payload`.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The document (body withheld while locked).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InboxDocDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/inbox-docs/{id}/confirm-receipt:
    post:
      tags:
        - me
      operationId: confirmMyInboxDocReceipt
      summary: Confirm receipt of a legal notice (the legal receipt evidence)
      description: >-
        Requires a FRESH passkey step-up: confirmation IS the legally
        significant act of receipt (열람 = 법적 수령), audited with receipt
        semantics. Idempotent — a second confirm returns the existing stamp.
        Only a legal notice can be confirmed; a payslip is a frictionless
        self-view (422). Another user's / unknown document is 404.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InboxDocConfirmReceiptRequest'
      responses:
        '200':
          description: The confirmed document, unlocked and stamped.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InboxDocSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: The document is a payslip and cannot be receipt-confirmed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '428':
          description: A fresh passkey step-up is required to confirm receipt.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: JWT verification or passkey step-up is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/notification-policies:
    get:
      tags:
        - me
      operationId: listMyNotificationPolicies
      summary: List the authenticated user's notification routing policies, newest first
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The caller's routing policies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPolicyList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    put:
      tags:
        - me
      operationId: upsertMyNotificationPolicy
      summary: Upsert a mute policy for the authenticated user (idempotent per target)
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpsertNotificationPolicyRequest'
      responses:
        '200':
          description: The stored policy; a repeat upsert of the same target returns the same row.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPolicySummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/notification-policies/{id}:
    delete:
      tags:
        - me
      operationId: deleteMyNotificationPolicy
      summary: Delete (= unmute) one of the authenticated user's notification policies
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '204':
          description: The policy is removed and its rows regain attention.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/notifications:
    get:
      tags:
        - me
      operationId: listMyNotifications
      summary: List the authenticated user's notifications, newest first
      security:
      - bearerAuth: []
      parameters:
      - name: unread
        in: query
        required: false
        description: When true, return only unread notifications.
        schema:
          type: boolean
      - name: before
        in: query
        required: false
        description: Keyset cursor; return notifications strictly older than this id.
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        description: Page size (clamped server-side to 1..=200; default 50).
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: A page of the caller's notifications.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/notifications/by-object:
    get:
      tags:
        - me
      operationId: listMyNotificationObjectGroups
      summary: Aggregate the authenticated user's notifications by source object, newest activity first
      security:
      - bearerAuth: []
      parameters:
      - name: unread
        in: query
        required: false
        description: When true, return only groups that still hold at least one unread notification.
        schema:
          type: boolean
      - name: before
        in: query
        required: false
        description: Opaque keyset cursor from a previous page's next_cursor; an undecodable or foreign cursor yields an empty page.
        schema:
          type: string
      - name: limit
        in: query
        required: false
        description: Page size (clamped server-side to 1..=200; default 50).
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: A page of the caller's notifications grouped by source object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationObjectGroupPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/notifications/read-all:
    post:
      tags:
        - me
      operationId: markAllMyNotificationsRead
      summary: Mark all of the authenticated user's unread notifications read
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The number of notifications marked read.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationReadAllResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/notifications/summary:
    get:
      tags:
        - me
      operationId: getNotificationsSummary
      summary: Per-category unread breakdown for the comms-rail badge, plus the total
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Unread counts grouped by category, for the caller only. Mute-suppressed rows are excluded from total_unread and by_category and tallied separately as muted_unread.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationCountsSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/notifications/unread-count:
    get:
      tags:
        - me
      operationId: getMyUnreadNotificationCount
      summary: Count the authenticated user's unread notifications
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The number of the caller's unread notifications that want attention; rows suppressed by the caller's mute policies are excluded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnreadNotificationCountResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/notifications/{id}/read:
    post:
      tags:
        - me
      operationId: markMyNotificationRead
      summary: Mark one of the authenticated user's notifications read
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The updated notification.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/notifications/{id}/unread:
    post:
      tags:
        - me
      operationId: markMyNotificationUnread
      summary: Mark one of the authenticated user's notifications unread again (read toggle)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The notification, unread again. read_at keeps the first-read timestamp.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/todos:
    get:
      tags:
        - me
      operationId: listMyTodos
      summary: List the authenticated user's todos, open first then newest first
      security:
      - bearerAuth: []
      parameters:
      - name: include_done
        in: query
        required: false
        description: When true, also return completed todos (default false).
        schema:
          type: boolean
      - name: limit
        in: query
        required: false
        description: Page size (clamped server-side to 1..=200; default 100).
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: The caller's todos.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TodoPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    post:
      tags:
        - me
      operationId: createMyTodo
      summary: Create a todo owned by the authenticated user
      description: >-
        The owner is always the authenticated principal. Scope chips
        (person/team/site/entity refs) and object links (kind+id pairs) are
        validated ref lists of at most 20 entries each. Audited as todo.create.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTodoRequest'
      responses:
        '201':
          description: The created todo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TodoSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/todos/{todoId}:
    delete:
      tags:
        - me
      operationId: deleteMyTodo
      summary: Delete one of the authenticated user's todos
      description: >-
        A cross-user id is a 404, never another user's row. Audited as
        todo.delete.
      security:
      - bearerAuth: []
      parameters:
      - name: todoId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '204':
          description: The todo was deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/todos/{todoId}/done:
    post:
      tags:
        - me
      operationId: setMyTodoDone
      summary: Mark one of the authenticated user's todos done or undone
      description: >-
        Explicit target state so the same endpoint supports done AND undo.
        A cross-user id is a 404, never another user's row. Audited as
        todo.done / todo.undone.
      security:
      - bearerAuth: []
      parameters:
      - name: todoId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetTodoDoneRequest'
      responses:
        '200':
          description: The updated todo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TodoSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/me/workbench:
    get:
      tags:
        - me
      operationId: getMyWorkbench
      summary: Read the authenticated principal's bounded operations workbench
      description: >-
        Composes the native action-inbox, owner-scoped todo, and collaboration
        calendar reads under one captured request instant. Each source remains
        independently authorized and may return a redacted denied or unavailable
        envelope; the aggregate never owns mutations or duplicates source data.
      security:
      - bearerAuth: []
      parameters:
      - name: from
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: to
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: branch_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: action_limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
      - name: todo_limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
      - name: calendar_limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
      responses:
        '200':
          description: Workbench source snapshots at one request ceiling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MyWorkbenchResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/me/workspace:
    get:
      tags:
        - me
      operationId: getCurrentUserWorkspace
      summary: Get the authenticated user's console workspace layout
      description: >-
        Returns the caller's saved Oyatie Console window/panel layout. The
        `layout` is an opaque, frontend-owned JSON object; a user with no saved
        layout gets the empty default `{}`.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The caller's workspace layout.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    put:
      tags:
        - me
      operationId: putCurrentUserWorkspace
      summary: Upsert the authenticated user's console workspace layout
      description: >-
        Stores the caller's Oyatie Console layout verbatim. The `layout` must be
        a JSON object and is bounded in size by the server.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkspaceUpsertRequest'
      responses:
        '200':
          description: The stored workspace layout.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/mobile/collaboration/polls/{id}/vote:
    post:
      tags:
        - collaboration
      operationId: voteMobileCollaborationPoll
      summary: Submit or replace a native mobile poll vote with passkey step-up
      description: Mobile-scoped poll vote route. It validates poll lifecycle and option ownership only after a fresh passkey
        step-up envelope is verified for this poll vote.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MobileVotePollRequest'
      responses:
        '200':
          description: Updated poll with aggregate counts and caller vote state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PollResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/mobile/work-orders/{workOrderId}/approve:
    post:
      tags:
        - work-orders
      operationId: approveMobileWorkOrder
      summary: Approve the next pending approval step from a native mobile client
      description: Mobile-scoped approval route. It requires a fresh passkey step-up envelope bound to this work order before
        the approval mutation or audit side effect runs.
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MobileApproveWorkOrderRequest'
      responses:
        '200':
          description: Updated work-order approval state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/notices:
    get:
      tags:
      - notices
      operationId: listNotices
      summary: List notices (published-only for most callers; NoticeManage sees drafts too)
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: Notices visible to the caller, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/NoticeSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags:
      - notices
      operationId: createNoticeDraft
      summary: Create a draft notice (NoticeManage only)
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNoticeDraftRequest'
      responses:
        '201':
          description: Draft notice created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoticeSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/notices/{id}:
    get:
      tags:
      - notices
      operationId: getNotice
      summary: Fetch one notice (a draft is NotFound unless the caller holds NoticeManage)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The notice.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoticeSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags:
      - notices
      operationId: updateNoticeDraft
      summary: Edit a DRAFT notice (NoticeManage only); every field optional, audience replaced whole, 409 once published
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateNoticeDraftRequest'
      responses:
        '200':
          description: The updated draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoticeSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/notices/{id}/ack:
    post:
      tags:
      - notices
      operationId: acknowledgeNotice
      summary: 수령확인 — the caller confirms receipt of a published notice (owner-scoped)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '204':
          description: Acknowledged.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/notices/{id}/progress:
    get:
      tags:
      - notices
      operationId: getNoticeProgress
      summary: 수령확인 progress (done/total) for one notice (NoticeManage only)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Acknowledgment progress.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoticeProgress'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/notices/{id}/publish:
    post:
      tags:
      - notices
      operationId: publishNotice
      summary: Publish a draft (NoticeManage only); issues the NT- code, snapshots recipients, fans out notifications
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The published notice.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoticeSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/notices/{id}/receipts:
    get:
      tags:
      - notices
      operationId: listNoticeReceipts
      summary: 수령확인 receipts drill for one notice (NoticeManage only); acknowledged=false is the outstanding chase list
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: acknowledged
        in: query
        required: false
        schema:
          type: boolean
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 200
          default: 50
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Recipient receipts, newest acknowledgment first, then display name.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoticeReceiptPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/object-actions/catalog:
    get:
      tags:
        - object-actions
      operationId: getObjectActionCatalog
      summary: Read governed object-action descriptors for one object
      description: Returns typed, UI-renderable action descriptors for a console object. The initial CAP-5 vertical slice
        supports equipment actions and requires EquipmentManage. Missing or foreign equipment returns 404 through the same
        branch-scoped object read used by the equipment detail page.
      security:
      - bearerAuth: []
      parameters:
      - name: object_type
        in: query
        required: true
        schema:
          type: string
          enum:
          - equipment
      - name: object_id
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Governed action descriptors for the object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectActionCatalogResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/object-actions/execute:
    post:
      tags:
        - object-actions
      operationId: executeObjectAction
      summary: Execute a governed audited object action
      description: Executes a typed object write-back action through the server-side action executor. Sensitive actions require a fresh passkey step-up assertion
        so the append-only audit event binds the write to the human actor. The initial slice supports `equipment.update_profile`.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExecuteObjectActionRequest'
      responses:
        '200':
          description: Object action execution receipt with audit provenance.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectActionExecutionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: JWT or passkey step-up verification is not configured.
  /api/v1/object-links:
    get:
      tags:
        - object-links
      operationId: listObjectLinks
      summary: List the generic links touching one object, in both directions
      description: Returns every object_link where the given (kind, id) is the source (outgoing) or the destination
        (incoming). Tenant-scoped by forced RLS; only links in the caller's org are visible.
      security:
      - bearerAuth: []
      parameters:
      - name: kind
        in: query
        required: true
        description: Object kind slug (must be a known object type).
        schema:
          type: string
      - name: id
        in: query
        required: true
        description: Object id/reference (a UUID or issued code, ≤200 chars).
        schema:
          type: string
      responses:
        '200':
          description: Links touching the object, split into outgoing and incoming.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectLinksListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
    post:
      tags:
        - object-links
      operationId: createObjectLink
      summary: Create a generic, audited link between two objects
      description: Creates one directed edge (src -> dst) of a given link_type between two known object kinds. Both kinds
        must exist in the object-type registry. An identical link is rejected with 409. Audited via with_audit.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateObjectLinkRequest'
      responses:
        '200':
          description: The created link.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectLinkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/object-links/{id}:
    delete:
      tags:
        - object-links
      operationId: deleteObjectLink
      summary: Remove an object link (audited)
      description: Hard-deletes the link; the audit event's before-snapshot preserves the removed edge. An unknown id or a
        link owned by another tenant both return 404 (deny-by-omission).
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '204':
          description: The link was removed.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/object-types:
    get:
      tags:
        - object-types
      operationId: listRegistryObjectTypes
      summary: List the object-type registry with caller-visible instance counts
      description: 'Returns every seeded object kind (kind, code_prefix, label, lifecycle status) plus active_count —
        the number of instances of that kind visible to the caller. The count respects the SAME per-kind visibility as
        resolveObject: org via forced RLS (no cross-org counting), narrowed by the caller''s branch scope and the
        domain feature gate; a kind the caller cannot read counts 0. Type proposal/transition flows are out of scope
        for this slice (read surface + status only).'
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The object-type registry.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ObjectTypeResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/object-types/{kind}:
    get:
      tags:
        - object-types
      operationId: getRegistryObjectType
      summary: Fetch one object type with its caller-visible instance count
      description: Returns a single object type by kind slug (404 if unknown), with the same caller-visible active_count
        as listObjectTypes.
      security:
      - bearerAuth: []
      parameters:
      - name: kind
        in: path
        required: true
        description: Object kind slug.
        schema:
          type: string
      responses:
        '200':
          description: The object type.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectTypeResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/office/callback:
    post:
      tags:
        - office
      operationId: officeCallback
      security:
        - officeCallbackToken: []
          officeDocumentServerToken: []
      summary: DocumentServer force-save callback (machine, JWT-verified)
      description: The unauthenticated (no user principal) machine callback from DocumentServer. Verified by the ONLYOFFICE
        JWT plus a host-issued callback token binding it to (org, document). On status 2 (ready to save) or 6 (force-save)
        the produced document is fetched and stored as an IMMUTABLE new version (idempotent per editing-session key). Always
        responds with the ONLYOFFICE error-code JSON body (error 0 on success), never an HTTP error status.
      parameters:
      - name: ct
        in: query
        required: true
        description: Host-issued callback token binding the request to (org, document).
        schema:
          type: string
          minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                token:
                  type: string
                  minLength: 1
                  description: The ONLYOFFICE-signed callback payload (v7.1+ in-body token).
                status:
                  type: integer
                  description: ONLYOFFICE document status code (2 = ready to save, 6 = force-save, other values are no-ops).
                url:
                  type: string
                  format: uri
                  description: Short-lived DocumentServer URL for the produced document when status is 2 or 6.
                key:
                  type: string
                  description: The document.key of the editing session that produced this callback.
                filetype:
                  type: string
                  enum: [docx, xlsx, pptx]
                  description: ONLYOFFICE file type for the produced document.
                users:
                  type: array
                  items:
                    type: string
                  description: ONLYOFFICE user identifiers participating in the callback, when supplied.
                actions:
                  type: array
                  items:
                    type: object
                    additionalProperties: true
                  description: ONLYOFFICE action metadata, when supplied.
                history:
                  type: object
                  additionalProperties: true
                  description: ONLYOFFICE history payload, when supplied.
      responses:
        '200':
          description: Always 200. `error` is 0 on success/ignore, non-zero on a rejected callback.
          content:
            application/json:
              schema:
                type: object
                required: [error]
                properties:
                  error:
                    type: integer
                    minimum: 0
  /api/v1/office/documents/{documentRef}/versions:
    get:
      tags:
        - office
      operationId: listOfficeDocumentVersions
      summary: List a document's immutable version history (newest first)
      security:
      - bearerAuth: []
      parameters:
      - name: documentRef
        in: path
        required: true
        schema:
          type: string
          minLength: 1
          maxLength: 200
      responses:
        '200':
          description: The append-only version list.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/DocumentVersion'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/office/documents/{documentRef}/versions/{versionNo}/restore:
    post:
      tags:
        - office
      operationId: restoreOfficeDocumentVersion
      summary: Non-destructively restore a version (re-publish it as a new version)
      description: Rollback is non-destructive — it appends a NEW version that re-publishes the target version's blob, with
        restoredFrom recording the lineage. Audited (office.document_version.restore). Gated on LifecycleManage.
      security:
      - bearerAuth: []
      parameters:
      - name: documentRef
        in: path
        required: true
        schema:
          type: string
          minLength: 1
          maxLength: 200
      - name: versionNo
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
      responses:
        '200':
          description: The newly appended (restored) version.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentVersion'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/office/sessions:
    post:
      tags:
        - office
      operationId: createOfficeSession
      summary: Issue a signed DocumentServer editor config for a document
      description: Returns the ONLYOFFICE editor configuration (with its signed JWT) for the LATEST version of a document.
        The host owns storage/versions/PBAC; document.key is a per-version hash so the editor never serves a stale cache;
        permissions map from the caller's authz (slice 0 gates on LifecycleManage). Slice 0 opens an EXISTING document
        (initial-version creation is a records-module concern).
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [documentRef]
              properties:
                documentRef:
                  type: string
                  minLength: 1
                  maxLength: 200
                  description: Trimmed logical document reference; control characters are rejected.
      responses:
        '200':
          description: The editor config to hand to DocumentServer.
          content:
            application/json:
              schema:
                type: object
                required: [documentServerUrl, config]
                properties:
                  documentServerUrl:
                    type: string
                  config:
                    type: object
                    description: The ONLYOFFICE DocEditor config object, carrying its signed `token`.
                    additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: Office editor or document storage is not configured.
  /api/v1/ontology/actions/{action_key}/execute:
    post:
      operationId: executeOntologyAction
      summary: Execute an object action (single mutation path)
      description: Re-evaluates the §16 gate chain inside the writeback transaction (TOCTOU-safe), then dispatches an instance-revision
        (append fixity-chained revision) or a projected use-case. Humans and automation use this same path.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      parameters:
      - name: action_key
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OntologyActionRequest'
      responses:
        '200':
          description: Action executed; the mutation and its audit row committed atomically.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OntologyActionExecuteOutcome'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          description: The supplied expected_revision is stale.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          $ref: '#/components/responses/ValidationError'
        '501':
          description: A projected-use-case dispatch is not wired yet.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/actions/{action_key}/preflight:
    post:
      operationId: preflightOntologyAction
      summary: Preflight an object action's gate chain
      description: Runs the ordered §16 gate chain (authority / self-checklist / four-eyes / egress) for an action and returns each
        gate's status WITHOUT committing anything.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      parameters:
      - name: action_key
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OntologyActionRequest'
      responses:
        '200':
          description: Gate-chain preflight status.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/instances:
    get:
      operationId: listOntologyInstances
      summary: List object instances of a type
      description: Lists current-state instances of one object type (RLS-scoped, Cedar residual-filtered). The type is the object-type
        version id whose head instances to list.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      parameters:
      - name: type
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Instance states for the object type.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/instances/aggregate:
    get:
      operationId: aggregateOntologyInstances
      summary: Aggregate object instances of a type
      description: |
        Returns subject-scoped GROUP BY counts over current-state instances of one object type
        without materialising rows. Uses the same Cedar residual lowering as the list path
        (deny-by-omission / forbid-wins). group_by is allowlisted: lifecycle_state, object_type_id,
        or attribute (requires attribute_key naming a declared property).
      tags:
      - Ontology
      security:
      - bearerAuth: []
      parameters:
      - name: type
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: group_by
        in: query
        required: true
        schema:
          type: string
          enum:
          - lifecycle_state
          - object_type_id
          - attribute
      - name: attribute_key
        in: query
        required: false
        description: Declared property key; required when group_by=attribute.
        schema:
          type: string
      responses:
        '200':
          description: Aggregate buckets (group key + count) for the visible residual.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OntologyInstanceAggregateBucket'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/instances/{id}:
    get:
      operationId: getOntologyInstance
      summary: Get an object instance card
      description: Returns one instance's current (or as-of) state. Property policies null forbidden fields (deny-by-omission).
      tags:
      - Ontology
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: as_of
        in: query
        required: false
        description: RFC3339 instant for a bi-temporal as-of read; absent = current head.
        schema:
          $ref: '#/components/schemas/Timestamp'
      responses:
        '200':
          description: Instance state.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/instances/{id}/acting:
    get:
      tags: [Ontology]
      operationId: listInstanceActing
      security:
        - bearerAuth: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: automation rules + policies acting on the object type
          content:
            application/json:
              schema: { type: array, items: { $ref: '#/components/schemas/ActingRule' } }
        '404': { description: instance not found (RLS-scoped) }
  /api/v1/ontology/instances/{id}/history:
    get:
      operationId: listOntologyInstanceHistory
      summary: List an instance's revision history
      description: Returns the append-only, fixity-chained revision timeline for one instance (hash-verified).
      tags:
      - Ontology
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Revision history for the instance.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/instances/{id}/lifecycle:
    post:
      tags: [Ontology]
      operationId: commitInstanceLifecycle
      security:
        - bearerAuth: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/LifecycleRequest' }
      responses:
        '200': { description: committed, content: { application/json: { schema: { $ref: '#/components/schemas/LifecycleOutcome' } } } }
        '403': { description: gate denied / unconfigured edge }
        '409': { description: illegal FSM edge or concurrent state change }
        '404': { description: instance not found (RLS-scoped) }
  /api/v1/ontology/instances/{id}/traverse:
    get:
      operationId: traverseOntologyInstance
      summary: Traverse the object graph around an instance
      description: Search-around graph traversal from one instance over ontology links, depth-bounded, returning a node/edge payload.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: link_type
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: depth
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
      responses:
        '200':
          description: Traversal node/edge graph.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/object-types:
    get:
      operationId: listObjectTypes
      summary: List ontology object types
      description: Lists the tenant's registry object types (Cedar-filtered) with their current schema version and lifecycle state.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Object type summaries for the caller's tenant.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    post:
      operationId: createObjectType
      summary: Create a draft object type
      description: Creates a DRAFT object type schema (with its property/link/action/analytic children). Publishing is a governed,
        separate lifecycle step; this only stages a draft.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateObjectTypeDraft'
      responses:
        '201':
          description: Draft object type created.
          headers:
            ETag:
              description: Strong tenant/key write validator for the created object type.
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectTypeSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/object-types/{key}:
    get:
      operationId: getObjectType
      summary: Get an object type definition
      description: Returns the object type definition plus its property/link/action/analytic children. An optional version pins an
        immutable, content-addressed published schema version; absent returns the head.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      parameters:
      - name: key
        in: path
        required: true
        schema:
          type: string
      - name: version
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
      responses:
        '200':
          description: Object type definition.
          headers:
            ETag:
              description: Strong tenant/key write validator for subsequent mutations.
              schema: { type: string }
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    put:
      operationId: stageObjectTypeRevision
      summary: Stage an object type schema revision
      description: Stages a version+1 revision of an object type schema (draft-staged, governed publish). Immutable published versions
        are preserved for rollback and as-of schema reads.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      parameters:
      - name: key
        in: path
        required: true
        schema:
          type: string
      - name: If-Match
        in: header
        required: true
        description: Exactly one strong ontology tenant/key write validator.
        schema:
          type: string
          pattern: '^"ont-object-type-key:[0-9a-f]{32}:r[1-9][0-9]*"$'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateObjectTypeDraft'
      responses:
        '201':
          description: Staged schema revision.
          headers:
            ETag:
              description: Strong successor tenant/key write validator.
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectTypeSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          $ref: '#/components/responses/PreconditionFailed'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/object-types/{key}/acting:
    get:
      tags:
        - Ontology
      operationId: listObjectTypeActing
      summary: Acting rules keyed by object-type
      description: >
        Automations (bound workflow definitions) and object policies acting on
        the object type identified by stable key. Type-centric sibling of
        /instances/{id}/acting — it may return rules for a type that has no
        instances yet. An unknown key is 404 (deny-by-omission, RLS-scoped).
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: automation rules + policies acting on the object type
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ActingRule'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/ontology/object-types/{key}/lifecycle:
    post:
      tags:
        - Ontology
      operationId: transitionObjectTypeLifecycle
      summary: Drive the object-type schema lifecycle
      description: >
        Transitions one object-type version through the schema FSM. The legal
        edges are draft->review_pending, review_pending->draft,
        review_pending->published, published->superseded, published->retired and
        superseded->retired; every other edge — including draft->published — is
        409. review_pending->published additionally consumes an approved,
        unconsumed ontology.schema.publish approval requested by this same actor
        and decided by a different one, so publishing without four-eyes is 403.
        {key} resolves to the published-preferred head, so a revision staged
        behind a published version must be addressed with ?version=.
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: version
          in: query
          required: false
          description: Schema version to transition. Defaults to the published-preferred head.
          schema:
            type: integer
            format: int64
            minimum: 1
        - name: If-Match
          in: header
          required: true
          description: Exactly one strong ontology tenant/key write validator.
          schema:
            type: string
            pattern: '^"ont-object-type-key:[0-9a-f]{32}:r[1-9][0-9]*"$'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - to_state
              properties:
                to_state:
                  type: string
                  enum: [draft, review_pending, published, superseded, retired]
      responses:
        '200':
          description: Transition committed.
          headers:
            ETag:
              description: Strong successor tenant/key write validator.
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectTypeSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          $ref: '#/components/responses/PreconditionFailed'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/object-types/{key}/policies:
    post:
      tags:
        - Ontology
      operationId: attachObjectTypePolicy
      summary: Attach an object policy to an object type
      description: >
        Authors one enforced Cedar catalog policy and attaches it to the object
        type identified by stable key, in one audited transaction. The attached
        policy is what makes that type's instances visible: with none attached
        the list and every single-instance read serve nothing (deny-by-default).
        The body carries only the rule — `resource_type` is derived from {key}
        and the action is fixed to `view`, so a policy that could never match its
        own object type is unrepresentable. A rule the authoring validator cannot
        strict-validate (for example a condition over a Date property, which is
        not representable as a Cedar attribute) is 422 and nothing is written.
        An attachment cannot be revoked — both tables are append-only and no role
        holds UPDATE on the policy catalog — so `conditions` is capped at 32:
        every condition is re-validated and lowered into SQL on every later read
        of the type, permanently.
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - effect
              properties:
                effect:
                  type: string
                  enum: [permit, forbid]
                conditions:
                  type: array
                  default: []
                  maxItems: 32
                  items:
                    type: object
                    required:
                      - attr
                      - op
                      - value
                    properties:
                      attr:
                        type: string
                      op:
                        type: string
                        enum: [eq, ne, contains]
                      value:
                        type: object
                        required:
                          - kind
                          - value
                        properties:
                          kind:
                            type: string
                            enum: [literal, subject_attr, bool]
                          value:
                            oneOf:
                              - type: string
                              - type: boolean
      responses:
        '201':
          description: The policy was authored, attached and audited.
          content:
            application/json:
              schema:
                type: object
                required:
                  - id
                properties:
                  id:
                    type: string
                    format: uuid
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/ontology/resolve:
    get:
      tags: [Ontology]
      operationId: resolveInstanceByCode
      security:
        - bearerAuth: []
      parameters:
        - { name: code, in: query, required: true, schema: { type: string } }
      responses:
        '200': { description: resolved, content: { application/json: { schema: { $ref: '#/components/schemas/ResolvedInstance' } } } }
        '404': { description: no instance resolves that code (deny-by-omission — also cross-tenant) }
  /api/v1/ops/summary:
    get:
      tags:
        - ops
      operationId: getOpsSummary
      summary: Per-tenant operational dashboard rollup
      description: 'Point-in-time operational rollup for the authenticated tenant: work-order funnel counts by stage, aging
        work orders, P1 SLA risk, mechanic utilization, equipment-status distribution, active substitutions, pending approvals,
        and open support tickets. The current unfiltered tenant-wide view requires org-wide authority (SUPER_ADMIN for built-in
        roles); every read is org-scoped under RLS. Branch-scoped ADMIN principals are denied until the query itself is branch-filtered.'
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Operational summary scoped to the authenticated tenant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OpsSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/org-changes:
    get:
      tags:
      - org
      operationId: listOrgChanges
      summary: List org changes
      description: RLS-filtered page of the tenant's org-change requests, newest first.
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/OrgChangeStatus'
      - name: kind
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/OrgChangeKind'
      - name: limit
        in: query
        required: false
        description: Defaults to 50; the server clamps the value into 1..200 rather than rejecting it.
        schema:
          type: integer
          format: int64
      - name: offset
        in: query
        required: false
        description: Defaults to 0; the server clamps the value into 0..100000 rather than rejecting it.
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: Page of org-change summaries.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    post:
      tags:
      - org
      operationId: createOrgChange
      summary: Draft an org change (조직 개편 기안)
      description: Creates a DRAFT org-change request. Requires an Idempotency-Key; a replay of the same body under the
        same key returns the existing request with 200, a different body under the same key conflicts. A revision may
        supersede an existing request only when that request is REJECTED.
      security:
      - bearerAuth: []
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        schema:
          type: string
          minLength: 16
          maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrgChangeRequest'
      responses:
        '200':
          description: Idempotent replay of the existing request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '201':
          description: Draft created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-changes/{id}:
    get:
      tags:
      - org
      operationId: getOrgChange
      summary: Read one org change
      description: Out-of-scope rows are concealed as 404 (deny by omission).
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The request with proposal, preflight receipt, approval chain, settlement items, and history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    patch:
      tags:
      - org
      operationId: updateOrgChangeDraft
      summary: Edit an org-change draft
      description: DRAFT or PRECHECKED only; every edit returns the request to DRAFT, so a stored preflight receipt must
        be re-run before submit. Omitted fields are left unchanged.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrgChangeDraftRequest'
      responses:
        '200':
          description: Draft updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-changes/{id}/approval-steps/{stepId}/decision:
    post:
      tags:
      - org
      operationId: decideOrgChangeApprovalStep
      summary: Decide one approval step
      description: IN_APPROVAL only. Steps decide strictly in step order, the drafter can never decide (a second SoD net
        is enforced by the gov_approvals CHECK), and a rejection ends the request as REJECTED.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: stepId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrgChangeDecisionRequest'
      responses:
        '200':
          description: Decision recorded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-changes/{id}/archive:
    post:
      tags:
      - org
      operationId: archiveOrgChange
      summary: Archive a settled dissolve
      description: SETTLING only, and refused while any settlement item is open. The deferred dissolve ops replay inside
        the archive transaction, so uncleared dependents roll the whole action back (참조 무결성).
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Archived.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-changes/{id}/cancel:
    post:
      tags:
      - org
      operationId: cancelOrgChange
      summary: Cancel a draft
      description: DRAFT or PRECHECKED only; anything past submit resolves through the approval chain, never a cancel.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelOrgChangeRequest'
      responses:
        '200':
          description: Cancelled.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-changes/{id}/effectuate:
    post:
      tags:
      - org
      operationId: effectuateOrgChange
      summary: Apply the approved change
      description: APPROVED only, and never before the effective date (발효일). NEW and REORG replay the proposal
        atomically to APPLIED; DISSOLVE seeds the six settlement items and opens SETTLING.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Applied, or settling for a dissolve.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-changes/{id}/preflight:
    post:
      tags:
      - org
      operationId: preflightOrgChange
      summary: Run the impact preflight (read-only; persists nothing)
      description: DRAFT or PRECHECKED only. Computes the blocker/warning receipt in-transaction and RETURNS it, and
        persists NOTHING — no status change, no stored receipt, no event, no audit row. A clean run does NOT promote
        the request, so a client must never wait for PRECHECKED before calling submit; submit is reachable straight
        from DRAFT and recomputes the same receipt inside its own transaction, which is the only writer.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Receipt computed and returned inside the request detail. Nothing was written; `summary.status`
            is whatever the last write left, and re-reading the request afterwards returns the receipt stored by an
            earlier submit — or none at all — not this one.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-changes/{id}/settlement-items/{itemId}/complete:
    post:
      tags:
      - org
      operationId: completeOrgChangeSettlementItem
      summary: Complete one settlement item
      description: SETTLING only; an already-completed item conflicts.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: itemId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteOrgChangeSettlementItemRequest'
      responses:
        '200':
          description: Item completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-changes/{id}/submit:
    post:
      tags:
      - org
      operationId: submitOrgChange
      summary: Submit for SoD approval
      description: DRAFT or PRECHECKED only — running preflight first is optional and is never a prerequisite, because
        submit recomputes the receipt inside its own transaction (blockers found there are a 409) and is what stores
        it, before seeding the ordered hr → finance → legal → executive approval chain. PRECHECKED is still accepted
        only so requests written before preflight became read-only keep working; their stored receipt is replaced,
        never trusted.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Now IN_APPROVAL with the pending approval chain.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-entities:
    get:
      tags:
      - org
      operationId: listOrgEntities
      summary: List group member entities (법인)
      description: Active member orgs of every group the caller holds a live grant for; fails closed to an empty list
        when the caller holds no grant.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Entity list, ordered by org id.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OrgEntitySummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/org-units:
    get:
      operationId: listOrgUnits
      summary: List canonical OrgUnit heads
      description: >-
        Returns current OrgUnit Heads in the armed tenant — the same rows `list`
        already returns (latest revision, `MAX(version)`). as_of / from / to are
        omitted: `org_units` / `org_unit_revisions` have no valid-time columns, and
        using `created_at` as `valid_from` would be a second time model. Empty when
        none are visible. Sites, regions, and branches stay the operational auth
        spine and are not OrgUnits. EmployeeDetail as_of remains HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      responses:
        '200':
          description: Current OrgUnit heads.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OrgUnit'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/org-units/{id}:
    get:
      operationId: getOrgUnit
      summary: Get a canonical OrgUnit head
      description: >-
        Returns one OrgUnit Head — the current latest revision (`MAX(version)`), the
        same row `get` already returns. as_of is omitted: `org_units` /
        `org_unit_revisions` have no valid-time columns, and using `created_at` as
        `valid_from` would be a second time model. A miss or a foreign tenant is 404
        (deny-by-omission). Sites, regions, and branches stay the operational auth
        spine and are not OrgUnits. EmployeeDetail as_of remains HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Current OrgUnit head.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgUnit'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/org-units/{id}/job-positions:
    get:
      operationId: listOrgUnitJobPositions
      summary: List JobPosition heads under one OrgUnit
      description: >-
        Returns current JobPosition Heads whose `org_unit_id` is this OrgUnit — the
        same rows `list_for_org_unit` already returns (latest revision,
        `MAX(version)`). as_of / from / to are omitted: `job_positions` /
        `job_position_revisions` have no valid-time columns, and using `created_at`
        as `valid_from` would be a second time model. Empty when the unit has no
        positions or is invisible to the armed tenant (same as the port; a missing
        OrgUnit is GET /api/v1/org-units/{id} 404, not this collection). Distinct
        from GET /api/v1/job-positions (tenant-wide list) and from recruiting
        postings / `employees.position` TEXT. EmployeeDetail as_of remains HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Current JobPosition heads under the OrgUnit.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/JobPosition'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/p1-dispatches/{dispatchId}:
    get:
      tags:
        - p1-dispatches
      operationId: getP1Dispatch
      summary: Fetch P1 dispatch status
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/DispatchId'
      responses:
        '200':
          description: P1 dispatch status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/P1DispatchSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/p1-dispatches/{dispatchId}/candidates:
    get:
      tags: [p1-dispatches]
      operationId: listP1DispatchCandidates
      summary: List manager-authorized ranked dispatch candidates
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/DispatchId' }]
      responses:
        '200': { description: Dispatch candidate page., content: { application/json: { schema: { $ref: '#/components/schemas/DispatchCandidatePage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/p1-dispatches/{dispatchId}/force-assign:
    post:
      tags:
        - p1-dispatches
      operationId: forceAssignP1Dispatch
      summary: Manager force-assign a P1 dispatch after accept-window escalation
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/DispatchId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ForceAssignP1DispatchRequest'
      responses:
        '200':
          description: Force-assigned P1 dispatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/P1DispatchSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/p1-dispatches/{dispatchId}/responses:
    get:
      tags: [p1-dispatches]
      operationId: listP1DispatchResponses
      summary: List authorized P1 dispatch responses
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/DispatchId' }]
      responses:
        '200': { description: Dispatch response page., content: { application/json: { schema: { $ref: '#/components/schemas/P1DispatchResponsePage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    post:
      tags:
        - p1-dispatches
      operationId: respondP1Dispatch
      summary: Accept or decline a P1 dispatch broadcast
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/DispatchId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RespondP1DispatchRequest'
      responses:
        '200':
          description: Updated P1 dispatch state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/P1DispatchSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/passkeys:
    get:
      tags:
        - passkeys
      operationId: listPasskeys
      summary: List the authenticated user's own passkey credentials
      description: Returns the caller's own WebAuthn credentials (id and registration / last-use timestamps). No secret material
        is ever returned.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's passkey credentials.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PasskeySummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/passkeys/{id}:
    delete:
      tags:
        - passkeys
      operationId: deletePasskey
      summary: Revoke one of the authenticated user's own passkey credentials
      description: Deletes a credential owned by the caller. A credential that is not the caller's own returns 404. The caller's
        last remaining passkey cannot be deleted (409) so they do not lock themselves out.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '204':
          description: The passkey was revoked.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/payroll/employees/{employeeId}/contract-wages:
    post:
      tags:
      - payroll
      operationId: createEmployeeContractWage
      summary: Record an effective-dated 근로계약 wage for an employee
      description: >
        Append-only. A raise is a NEW row with a later `effectiveFrom`; the wage
        in force on a pay date is the greatest `effectiveFrom <= payDate`. This
        is payroll's only wage source — `employee_employment_profiles.base_pay`
        is HR-directory display data with no period label and no effective
        dating, and payroll never reads it. Amounts are integer won.
      security:
      - bearerAuth: []
      parameters:
      - name: employeeId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [effective_from, wage_kind, amount_won, monthly_standard_hours]
              properties:
                effective_from:
                  type: string
                  format: date
                wage_kind:
                  type: string
                  enum: [MONTHLY, HOURLY]
                amount_won:
                  type: integer
                  format: int64
                  minimum: 1
                  description: Integer won. Never a decimal or floating-point amount.
                monthly_standard_hours:
                  type: integer
                  minimum: 1
                  maximum: 400
                  description: >
                    월 소정근로시간 (209 for a 주40시간 worker, 유급주휴 8시간 포함).
                    The minimum-wage comparator's denominator; mandatory so that a
                    comparison which cannot be made never reads as a pass.
                source_note:
                  type: string
                  maxLength: 500
      responses:
        '201':
          description: The created contract-wage row id.
          content:
            application/json:
              schema:
                type: object
                required: [id]
                properties:
                  id:
                    $ref: '#/components/schemas/Uuid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/payroll/employees/{employeeId}/payslip-draft:
    get:
      tags:
      - payroll
      operationId: getEmployeePayslipDraft
      summary: 4대보험 급여명세서 draft for one employee and period (audited read)
      description: >
        Read-only. Computes 국민연금·건강보험·장기요양·고용보험 from the contract
        wage in force on `payDate`. Every figure carries its instrument, article
        and 시행일자.


        The period's real attendance is read and GATES the draft; it drives no
        figure. Nothing here is prorated by attendance. Recorded working days
        short of the period's working days — including zero 근태기록 — or a
        CLOCK_IN count that does not match its CLOCK_OUT count adds
        `ATTENDANCE_INCOMPLETE` (with the counts) to `blockers[]` and forces
        `issuable: false`. A draft must not read as a full month on a month
        nobody evidenced.


        ASSERTS NO LEGAL COMPLIANCE. 원천징수 (소득세·지방소득세) is deliberately
        NOT computed: `not_computed[]` names 소득세법 시행령 별표 2 as the
        instrument that would supply it, `net_pay_won` is null, and `issuable` is
        false. A deduction this engine did not compute is never emitted as a zero.


        산재보험 appears with `employer_only: true` and a null employee amount
        (징수법 제13조제5항), so its absence from the deduction total is stated
        rather than silent. Its `total_won`, `basis_won`, `rate_num` and
        `rate_den` are null too, not 0: the 사업종류별 요율 lives in 고용노동부고시
        제2025-91호's un-ingested 별지, so the employer figures are unknown rather
        than zero.


        Where a rounding unit is not settled by any instrument read, the engine
        computes every candidate and emits the won only if they agree; otherwise
        the component carries `blocked_by` with the open question id and the
        totals are null. Returns 200 in that case, not 409, so the caller can
        see which question blocked which line.
      security:
      - bearerAuth: []
      parameters:
      - name: employeeId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: period
        in: query
        required: true
        description: 급여계산기간, `YYYY-MM`.
        schema:
          type: string
          pattern: '^[0-9]{4}-[0-9]{2}$'
      - name: pay_date
        in: query
        required: false
        description: >
          지급일. Defaults to the period end. A SEPARATE input from the period on
          purpose: the pay date selects both the rate row and the 기준소득월액
          band, so a June period paid in July must not reuse the June band.
        schema:
          type: string
          format: date
      responses:
        '200':
          description: >
            The draft, including blockers. Also returned when the draft is not
            issuable — the blocker list is the payload.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollPayslipDraft'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/payroll/payslips/me:
    get:
      tags:
      - payroll
      operationId: listMyPayrollLines
      summary: List the signed-in employee's own payroll draft-line (readiness) rows
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
      responses:
        '200':
          description: Page of the caller's own payroll draft-line rows, newest run period first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MyPayrollLinePage'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/payroll/runs:
    get:
      tags:
      - payroll
      operationId: listPayrollRuns
      summary: List payroll draft runs (admin; EXECUTIVE/SUPER_ADMIN only, audited read)
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
      responses:
        '200':
          description: Page of payroll draft runs, newest period first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/payroll/runs/{id}:
    get:
      tags:
      - payroll
      operationId: getPayrollRun
      summary: Get one payroll draft run and its per-employee readiness lines (admin)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
      responses:
        '200':
          description: The run, its legal/source-summary context, and a page of lines.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/payroll/runs/{id}/calculate:
    post:
      tags:
      - payroll
      operationId: calculatePayrollRun
      summary: Per-line draft calculation (ATTENDANCE_CLOSED only); a line without a verified gross + NTS source row stays blocked with truthful blockers, income tax is never estimated
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The run detail after the calculation, including the latest calculation summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/payroll/runs/{id}/close-attendance:
    post:
      tags:
      - payroll
      operationId: closePayrollRunAttendance
      summary: Attested attendance close (STAGED/BLOCKED_LEGAL_GATE/READY_FOR_REVIEW to ATTENDANCE_CLOSED); a blocked preflight is a 409 carrying the failing checks in error.details
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClosePayrollAttendanceRequest'
      responses:
        '200':
          description: The run detail after the close.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/payroll/runs/{id}/close-preflight:
    get:
      tags:
      - payroll
      operationId: getPayrollRunClosePreflight
      summary: Attendance-close preflight for a payroll run (audited read; EXECUTIVE/SUPER_ADMIN org-wide)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The attendance-material / period-lock / pending-leave checks and whether the run may be closed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollClosePreflight'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/payroll/runs/{id}/decision:
    post:
      tags:
      - payroll
      operationId: decidePayrollRun
      summary: APPROVE or REJECT a SUBMITTED run under maker-checker separation of duties (the decider must not be the submitter)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DecidePayrollRunRequest'
      responses:
        '200':
          description: The run detail after the decision.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/payroll/runs/{id}/disbursement/attest:
    post:
      tags:
      - payroll
      operationId: attestPayrollDisbursement
      summary: Operator attestation FSM SCHEDULED to SUBMITTED_TO_BANK to PAID or FAILED (reason required) and FAILED back to SCHEDULED; no bank API exists, these are operator records
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttestPayrollDisbursementRequest'
      responses:
        '200':
          description: The attested disbursement record; PAID also moves the run to PAID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollDisbursement'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/payroll/runs/{id}/exceptions:
    get:
      tags:
      - payroll
      operationId: listPayrollRunExceptions
      summary: Exception review queue for a run (audited read; OPEN rows first, then danger/warn/info, then age)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
      responses:
        '200':
          description: Page of run exceptions with the open count.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollExceptionPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/payroll/runs/{id}/exceptions/{exceptionId}/resolve:
    post:
      tags:
      - payroll
      operationId: resolvePayrollRunException
      summary: Resolve one exception with CONFIRM or HOLD (HOLD requires a reason and carries the row to the next run of the series); the run must be CALCULATED
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: exceptionId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolvePayrollExceptionRequest'
      responses:
        '200':
          description: The resolved exception row.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollException'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/payroll/runs/{id}/issue-payslips:
    post:
      tags:
      - payroll
      operationId: issuePayrollPayslips
      summary: Issue payslips for a PAID run into each recipient's inbox vault; hard-gated by the registered 노무사/세무사 release-gate record (409 legal_gate otherwise)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The delivery summary after issuance; the run moves to ISSUED.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollPayslipDeliverySummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/payroll/runs/{id}/payslip-delivery:
    get:
      tags:
      - payroll
      operationId: getPayrollPayslipDelivery
      summary: Payslip delivery and acknowledgement readback for a run (audited read)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 500
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
      responses:
        '200':
          description: Delivery counts and a page of delivered payslip links.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollPayslipDeliverySummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/payroll/runs/{id}/schedule-disbursement:
    post:
      tags:
      - payroll
      operationId: schedulePayrollDisbursement
      summary: Schedule the transfer for an APPROVED run (one disbursement per run)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SchedulePayrollDisbursementRequest'
      responses:
        '201':
          description: The created disbursement record; the run moves to DISBURSEMENT_SCHEDULED.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollDisbursement'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/payroll/runs/{id}/submit:
    post:
      tags:
      - payroll
      operationId: submitPayrollRun
      summary: Submit a CALCULATED run for approval; fail-closed while any exception is still OPEN (409 carries the open count in error.details.open)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The run detail after submission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/payroll/runs/{id}/withdraw:
    post:
      tags:
      - payroll
      operationId: withdrawPayrollRun
      summary: Withdraw a REJECTED run back to CALCULATED, clearing the submit/decide columns so a fresh maker-checker pair is admissible
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The run detail after the withdrawal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/period-locks:
    get:
      tags:
        - period-locks
      operationId: listPeriodLocks
      summary: List payroll/accounting period locks
      description: Close-authority gated (PeriodLockManage, org-wide). Lists lock history newest first; active locks are
        rows with no unlockedAt.
      security:
      - bearerAuth: []
      parameters:
      - name: domain
        in: query
        required: false
        schema:
          type: string
          enum: [payroll, accounting]
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 200
      responses:
        '200':
          description: Period locks, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PeriodLockList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
    post:
      tags:
        - period-locks
      operationId: createPeriodLock
      summary: Lock a payroll/accounting period (freeze window)
      description: 'Close-authority gated (PeriodLockManage, org-wide), audited. While the lock is active every
        date-stamping write in the domain whose business date falls inside [periodStart, periodEnd] fails closed with 409.
        Overlapping an existing active lock in the same domain is refused.'
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePeriodLockRequest'
      responses:
        '201':
          description: Period locked.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PeriodLock'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/period-locks/{lockId}/unlock:
    post:
      tags:
        - period-locks
      operationId: unlockPeriodLock
      summary: Unlock a period lock (one-shot, reason required)
      description: Close-authority gated (PeriodLockManage, org-wide), audited. Unlock is one-shot and immutable; re-locking
        the same window appends a new lock row.
      security:
      - bearerAuth: []
      parameters:
      - name: lockId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnlockPeriodLockRequest'
      responses:
        '200':
          description: Period unlocked.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PeriodLock'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/persons:
    get:
      operationId: listPersons
      summary: List canonical Person heads
      description: >-
        Returns current Person Heads in the armed tenant — the same closed
        four-field projection `list` already returns (latest revision,
        `MAX(version)`). as_of / from / to are omitted: `persons` /
        `person_revisions` have no valid-time columns, and using `created_at` as
        `valid_from` would be a second time model. Empty when none are visible.
        The Head never includes phone, salary, bank_account, or rrn.
        EmployeeDetail as_of remains HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      responses:
        '200':
          description: Current Person heads.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Person'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/persons/{id}:
    get:
      operationId: getPerson
      summary: Get a canonical Person head
      description: >-
        Returns one Person Head — the current latest revision (`MAX(version)`), the
        same closed four-field projection `get` already returns. as_of is omitted:
        `persons` / `person_revisions` have no valid-time columns, and using
        `created_at` as `valid_from` would be a second time model. A miss or a
        foreign tenant is 404 (deny-by-omission). The Head never includes phone,
        salary, bank_account, or rrn. EmployeeDetail as_of remains HOLD.
      tags:
      - Ontology
      security:
      - bearerAuth: []
      permissions:
      - employee_directory_read
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Current Person head.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/policy/assignments:
    get:
      tags:
        - policy
      operationId: listPolicyAssignments
      summary: List custom-role assignments for one user
      description: Returns custom-role assignments for a visible user. ACTIVE assignments are audited and can add runtime-effective
        ordinary feature grants on the next request; DRAFT/RETIRED roles remain inert.
      security:
      - bearerAuth: []
      parameters:
      - name: user_id
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Planned custom-role assignments for the requested user.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PolicyRoleAssignmentResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/policy/audit-events:
    get:
      tags:
        - policy
      operationId: listPolicyAuditEvents
      summary: List Policy Studio audit evidence
      description: Returns append-only policy audit events for the current tenant. This is a read-only evidence timeline for
        RoleManage holders and includes only policy role and planned-assignment events, not unrelated tenant audit rows.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 20
      responses:
        '200':
          description: Recent policy audit evidence rows.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PolicyAuditEventResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/policy/authorize:
    post:
      operationId: authorizePolicyDecision
      summary: Live Cedar authorization decision
      description: The live point decision (the same fail-closed evaluator the guardrail gate calls), optionally scoped to one object
        type or property's attached policies.
      tags:
      - Policy
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolicyAuthorizeRequest'
      responses:
        '200':
          description: Authorization outcome.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/policy/authorize/bulk:
    post:
      tags: [Policy]
      operationId: authorizeBulk
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/BulkAuthorizeBody' }
      responses:
        '200': { description: per-check decisions (index-aligned with checks), content: { application/json: { schema: { $ref: '#/components/schemas/BulkDecisionResponse' } } } }
  /api/v1/policy/catalog:
    get:
      operationId: listPolicyCatalog
      summary: List Cedar policy catalog entries
      description: Lists the tenant's Cedar policy catalog entries (enforced / shadow / draft / …), optionally filtered by status.
      tags:
      - Policy
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Catalog entries for the caller's tenant.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/policy/decisions:
    get:
      tags: [Policy]
      operationId: listPolicyDecisions
      security:
        - bearerAuth: []
      parameters:
        - { name: since, in: query, required: false, schema: { type: string, format: date-time } }
      responses:
        '200':
          description: recent decisions, newest first, capped at 200
          content:
            application/json:
              schema: { type: array, items: { $ref: '#/components/schemas/DecisionLogRow' } }
  /api/v1/policy/drafts:
    get:
      operationId: listPolicyDrafts
      summary: List no-code policy drafts
      description: Lists the tenant's no-code Cedar policy drafts and their review status.
      tags:
      - Policy
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Policy drafts for the caller's tenant.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    post:
      operationId: createPolicyDraft
      summary: Create a no-code policy draft
      description: Creates a no-code policy draft; the blocks are normalized and compiled to Cedar text. A draft can never create a
        live or shadow enforcement row.
      tags:
      - Policy
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolicyCreateDraftRequest'
      responses:
        '201':
          description: Draft created.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/policy/drafts/{draft_id}:
    get:
      operationId: getPolicyDraft
      summary: Get a policy draft
      tags:
      - Policy
      security:
      - bearerAuth: []
      parameters:
      - name: draft_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The policy draft.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    put:
      operationId: updatePolicyDraft
      summary: Update a policy draft
      description: Edits a per-user draft (re-normalized and re-compiled to Cedar text). Cannot promote a draft to shadow/enforced.
      tags:
      - Policy
      security:
      - bearerAuth: []
      parameters:
      - name: draft_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolicyUpdateDraftRequest'
      responses:
        '200':
          description: Updated draft.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/policy/drafts/{draft_id}/review:
    post:
      operationId: reviewPolicyDraft
      summary: Four-eyes review a policy draft
      description: Records a four-eyes review decision (reviewer must differ from the author) moving the draft to
        approved_for_promotion or rejected.
      tags:
      - Policy
      security:
      - bearerAuth: []
      parameters:
      - name: draft_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolicyReviewRequest'
      responses:
        '200':
          description: Review decision recorded.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/policy/drafts/{draft_id}/submit:
    post:
      operationId: submitPolicyDraft
      summary: Submit a policy draft for review
      description: Moves a validated draft to review_pending (validation must be valid).
      tags:
      - Policy
      security:
      - bearerAuth: []
      parameters:
      - name: draft_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Draft submitted for review.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/policy/drafts/{draft_id}/validate:
    post:
      operationId: validatePolicyDraft
      summary: Strict-validate a policy draft
      description: Strict-validates the draft's generated Cedar against the authoring schema and returns any errors. Does not activate
        anything.
      tags:
      - Policy
      security:
      - bearerAuth: []
      parameters:
      - name: draft_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Validation result.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/policy/features:
    get:
      tags:
        - policy
      operationId: listPolicyFeatures
      summary: List canonical policy feature catalog for Policy Studio
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Canonical features and built-in matrix defaults.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PolicyFeatureResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/policy/role-templates:
    get:
      tags:
        - policy
      operationId: listPolicyRoleTemplates
      summary: List safe starter templates for tenant custom roles
      description: Returns built-in starter templates that can be copied into a tenant custom role definition. Templates never
        grant elevated policy-management features. Templates do not affect runtime authorization until copied into an ACTIVE
        assigned custom role; unsupported/elevated grants remain inert.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Safe custom-role starter templates.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PolicyRoleTemplateResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/policy/roles:
    get:
      tags:
        - policy
      operationId: listPolicyRoles
      summary: List built-in role templates and tenant custom roles
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Built-in system role templates and tenant custom role definitions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyRoleCatalogResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    post:
      tags:
        - policy
      operationId: createPolicyRole
      summary: Create a tenant custom role definition
      description: Creates an audited, tenant-scoped custom role definition. Custom roles never replace built-in users.roles,
        but ACTIVE assignments can add runtime-effective ordinary feature grants on the next request. Elevated policy-management
        and scope-widening features are rejected.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePolicyRoleRequest'
      responses:
        '201':
          description: Custom role definition created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyRoleResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/policy/roles/{id}:
    patch:
      tags:
        - policy
      operationId: updatePolicyRole
      summary: Update a tenant custom role definition
      description: 'Updates mutable custom-role metadata, permissions, and ABAC/PBAC conditions. The role key and lifecycle
        status are not changed by this route. This is a sensitive policy-definition action: callers must have RoleManage,
        pass delegated-scope guardrails, and supply a fresh passkey step-up assertion. ACTIVE assigned custom-role changes
        become runtime-effective ordinary feature grants on the next request; unsupported ABAC/PBAC conditions and elevated/scope-widening
        features remain inert.'
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePolicyRoleRequest'
      responses:
        '200':
          description: Updated custom role definition.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyRoleResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: JWT or passkey step-up verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/policy/roles/{id}/status:
    patch:
      tags:
        - policy
      operationId: updatePolicyRoleStatus
      summary: Publish, roll back, or retire a tenant custom role
      description: 'Changes a custom role lifecycle state. This is a sensitive policy action: callers must have RoleManage
        and supply a fresh passkey step-up assertion proving the actor, not just the bearer token, performed the change.'
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePolicyRoleStatusRequest'
      responses:
        '200':
          description: Updated custom role.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyRoleResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: JWT or passkey step-up verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/policy/roles/{id}/status-preview:
    post:
      tags:
        - policy
      operationId: previewPolicyRoleStatus
      summary: Preview custom-role lifecycle status impact
      description: Returns a read-only before/after impact summary for publishing, rolling back, or retiring a custom role.
        This endpoint never mutates policy, does not require a passkey assertion, and reminds callers that the subsequent
        status update is a sensitive passkey step-up action. Allowed transitions are DRAFT→ACTIVE, ACTIVE→DRAFT, and ACTIVE→RETIRED;
        RETIRED is terminal.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolicyRoleStatusPreviewRequest'
      responses:
        '200':
          description: Read-only status impact preview.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyRoleStatusPreviewResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/policy/simulate:
    post:
      operationId: simulatePolicyDecision
      summary: Simulate a Cedar policy decision
      description: Evaluates a point decision (with an optional what-if draft overlay) and returns Allow/Deny plus matched policies and
        diagnostics. Deny-by-omission is the default.
      tags:
      - Policy
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolicySimulateRequest'
      responses:
        '200':
          description: Simulation outcome.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/policy/users/{id}/assignment-preview:
    post:
      tags:
        - policy
      operationId: previewPolicyAssignments
      summary: Preview requested custom-role assignment impact for one user
      description: 'Computes requested-vs-current planned custom-role assignment deltas and feature grants for a visible user.
        This is an impact-preview surface only: it does not write assignments and does not publish runtime authorization.'
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplacePolicyRoleAssignmentsRequest'
      responses:
        '200':
          description: Preview of assignment delta and resulting grants.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyAssignmentPreviewResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/policy/users/{id}/assignments:
    put:
      tags:
        - policy
      operationId: replacePolicyAssignments
      summary: Replace one user's custom-role assignments
      description: Replaces custom-role assignments for a visible user and bumps the tenant policy version. This is a sensitive
        policy action requiring a fresh passkey step-up. ACTIVE assigned roles publish supported ordinary feature grants into
        runtime authorization on the next request; unsupported conditions/elevated grants remain inert.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplacePolicyRoleAssignmentsRequest'
      responses:
        '200':
          description: Planned assignments after replacement.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PolicyRoleAssignmentResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: JWT or passkey step-up verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/production/capacity-slots:
    get:
      operationId: listProductionCapacitySlots
      tags: [Production]
      security: [{bearerAuth: []}]
      parameters:
      - {name: branch_id, in: query, required: true, schema: {$ref: '#/components/schemas/Uuid'}}
      - {name: capacity_date, in: query, required: true, schema: {type: string, format: date}}
      responses:
        '200': {description: Tenant and branch scoped capacity slots, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/ProductionCapacitySlot'}}}}}
  /api/v1/production/plans:
    get:
      operationId: listProductionPlans
      tags: [Production]
      security: [{bearerAuth: []}]
      parameters:
      - name: branch_id
        in: query
        required: true
        schema: {$ref: '#/components/schemas/Uuid'}
      - name: limit
        in: query
        schema: {type: integer, minimum: 1, maximum: 100, default: 25}
      - name: offset
        in: query
        schema: {type: integer, minimum: 0, default: 0}
      responses:
        '200': {description: Tenant- and branch-authorized production plans, content: {application/json: {schema: {type: array, items: {$ref: '#/components/schemas/ProductionPlan'}}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      operationId: createProductionPlan
      tags: [Production]
      security: [{bearerAuth: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/CreateProductionPlan'}}}}
      responses:
        '201': {description: Draft plan and first operation created, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionPlan'}}}}
        '200': {description: Idempotent replay returns the existing plan, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionPlan'}}}}
        '409': {$ref: '#/components/responses/Conflict'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /api/v1/production/plans/{plan_id}:
    get:
      operationId: getProductionPlan
      tags: [Production]
      security: [{bearerAuth: []}]
      parameters:
      - name: plan_id
        in: path
        required: true
        schema: {$ref: '#/components/schemas/Uuid'}
      responses:
        '200': {description: "Plan, first operation, checks, and durable lifecycle lineage", content: {application/json: {schema: {$ref: '#/components/schemas/ProductionPlanDetail'}}}}
        '404': {$ref: '#/components/responses/NotFound'}
  /api/v1/production/plans/{plan_id}/operations/{operation_id}/records:
    post:
      operationId: recordProductionOperation
      tags: [Production]
      security: [{bearerAuth: []}]
      parameters:
      - name: plan_id
        in: path
        required: true
        schema: {$ref: '#/components/schemas/Uuid'}
      - name: operation_id
        in: path
        required: true
        schema: {$ref: '#/components/schemas/Uuid'}
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/RecordProductionOperation'}}}}
      responses:
        '200': {description: "Output, scrap, downtime, and quality evidence recorded idempotently", content: {application/json: {schema: {$ref: '#/components/schemas/ProductionOperation'}}}}
        '409': {$ref: '#/components/responses/Conflict'}
  /api/v1/production/plans/{plan_id}/release:
    post:
      operationId: releaseProductionPlan
      tags: [Production]
      security: [{bearerAuth: []}]
      parameters:
      - name: plan_id
        in: path
        required: true
        schema: {$ref: '#/components/schemas/Uuid'}
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/ReleaseProductionPlan'}}}}
      responses:
        '200': {description: Plan and first operation released exactly once, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionPlan'}}}}
        '409': {$ref: '#/components/responses/Conflict'}
  /api/v1/production/source-ingress:
    post:
      operationId: ingestProductionSource
      tags: [Production]
      summary: Ingest one idempotent machine-owned production source fact
      security: [{basicAuth: []}]
      description: Machine-only Basic ingress. The client id is a service-principal UUID and the password is a one-time 32-byte secret. Tenant, branch, principal, and source system identity are derived server-side; the source id/version tuple is immutable and idempotent.
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionSourceIngress'}}}}
      responses:
        '200': {description: Existing source-version replay or accepted source fact receipt, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionSourceIngressReceipt'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '422': {$ref: '#/components/responses/ValidationError'}
        '500': {$ref: '#/components/responses/InternalServerError'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/production/source-systems:
    post:
      operationId: registerProductionSourceSystem
      tags: [Production]
      summary: Register a production source system and disclose its first credential once
      security: [{bearerAuth: []}]
      description: RoleManage-only registration of a tenant-scoped non-human source principal. Registration, rotation, and disable actions are audited server-side.
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/RegisterProductionSourceSystem'}}}}
      responses:
        '201': {description: Registered source system with one-time credential disclosure, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionSourceSystemCredential'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {$ref: '#/components/responses/Conflict'}
        '422': {$ref: '#/components/responses/ValidationError'}
        '500': {$ref: '#/components/responses/InternalServerError'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/production/source-systems/{source_system_id}/disable:
    post:
      operationId: disableProductionSourceSystem
      tags: [Production]
      summary: Disable a production source system at the expected generation
      security: [{bearerAuth: []}]
      parameters: [{name: source_system_id, in: path, required: true, schema: {$ref: '#/components/schemas/Uuid'}}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionSourceSystemGenerationRequest'}}}}
      responses:
        '200': {description: Disabled source system receipt, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionSourceSystemReceipt'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '422': {$ref: '#/components/responses/ValidationError'}
        '500': {$ref: '#/components/responses/InternalServerError'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/production/source-systems/{source_system_id}/rotate:
    post:
      operationId: rotateProductionSourceSystem
      tags: [Production]
      summary: Rotate a production source system credential at the expected generation
      security: [{bearerAuth: []}]
      parameters: [{name: source_system_id, in: path, required: true, schema: {$ref: '#/components/schemas/Uuid'}}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionSourceSystemGenerationRequest'}}}}
      responses:
        '200': {description: Rotated source system with one-time replacement credential disclosure, content: {application/json: {schema: {$ref: '#/components/schemas/ProductionSourceSystemCredential'}}}}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
        '422': {$ref: '#/components/responses/ValidationError'}
        '500': {$ref: '#/components/responses/InternalServerError'}
        '503': {$ref: '#/components/responses/ServiceUnavailable'}
  /api/v1/recruiting/applicants/{applicantId}:
    get:
      tags:
      - recruiting
      operationId: getRecruitApplicant
      summary: Applicant detail (audited PII read) with offer versions and the stage timeline
      security:
      - bearerAuth: []
      parameters:
      - name: applicantId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Applicant, offer versions (newest first), and stage-event history (oldest first).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitApplicantDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/recruiting/applicants/{applicantId}/advance:
    post:
      tags:
      - recruiting
      operationId: advanceRecruitApplicant
      summary: Advance one stage (INTERVIEW→OFFER is offer-only and OFFER→HIRED hire-only — both 422)
      security:
      - bearerAuth: []
      parameters:
      - name: applicantId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdvanceRecruitApplicantRequest'
      responses:
        '200':
          description: The advanced applicant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitApplicant'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/applicants/{applicantId}/assess:
    post:
      tags:
      - recruiting
      operationId: assessRecruitApplicant
      summary: Record the interview assessment at the INTERVIEW stage (offer prerequisite)
      security:
      - bearerAuth: []
      parameters:
      - name: applicantId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssessRecruitApplicantRequest'
      responses:
        '200':
          description: The assessed applicant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitApplicant'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/applicants/{applicantId}/hire:
    post:
      tags:
      - recruiting
      operationId: hireRecruitApplicant
      summary: Hire — one transaction through the HR-owned employee core
      description: >-
        Requires recruiting_manage AND employee_directory_manage. Creates the employee through the same idempotent core as
        People & Workforce (idempotency key recruit-hire-{applicantId}), links the applicant (HIRED), and increments the
        posting fill count. The latest offer must be ACCEPTED. POOL_DAILY postings return 422 POOL_REGISTRATION_UNAVAILABLE.
      security:
      - bearerAuth: []
      parameters:
      - name: applicantId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HireRecruitApplicantRequest'
      responses:
        '201':
          description: Employee created and linked (also returned on an idempotent employee-core replay).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HireRecruitApplicantResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: State conflict; an already-hired replay additionally carries the linked employee_id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitHireConflictResponse'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/applicants/{applicantId}/hold:
    post:
      tags:
      - recruiting
      operationId: holdRecruitApplicant
      summary: Set or clear the hold flag
      security:
      - bearerAuth: []
      parameters:
      - name: applicantId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HoldRecruitApplicantRequest'
      responses:
        '200':
          description: The applicant with the new hold state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitApplicant'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/recruiting/applicants/{applicantId}/offer:
    post:
      tags:
      - recruiting
      operationId: extendRecruitOffer
      summary: Extend an offer (assessment-gated; one live offer per applicant)
      security:
      - bearerAuth: []
      parameters:
      - name: applicantId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExtendRecruitOfferRequest'
      responses:
        '201':
          description: The extended offer; the applicant moves to OFFER.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitOffer'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/applicants/{applicantId}/reinstate:
    post:
      tags:
      - recruiting
      operationId: reinstateRecruitApplicant
      summary: Reinstate a rejected applicant to their prior stage (no request body)
      security:
      - bearerAuth: []
      parameters:
      - name: applicantId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The reinstated applicant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitApplicant'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/recruiting/applicants/{applicantId}/reject:
    post:
      tags:
      - recruiting
      operationId: rejectRecruitApplicant
      summary: Reject with an enum reason — archives the applicant into the talent pool
      security:
      - bearerAuth: []
      parameters:
      - name: applicantId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RejectRecruitApplicantRequest'
      responses:
        '200':
          description: The rejected applicant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitApplicant'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/applicants/{applicantId}/request-documents:
    post:
      tags:
      - recruiting
      operationId: requestRecruitApplicantDocuments
      summary: Flag supplementary documents as requested (no request body)
      security:
      - bearerAuth: []
      parameters:
      - name: applicantId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The applicant with doc_requested set.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitApplicant'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/recruiting/offers/{offerId}/adjust:
    post:
      tags:
      - recruiting
      operationId: adjustRecruitOffer
      summary: Adjust a live offer — writes an immutable v+1 row and SUPERSEDEs the prior one
      security:
      - bearerAuth: []
      parameters:
      - name: offerId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdjustRecruitOfferRequest'
      responses:
        '201':
          description: The new offer version.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitOffer'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/offers/{offerId}/record-reply:
    post:
      tags:
      - recruiting
      operationId: recordRecruitOfferReply
      summary: Record the candidate's reply on a live offer (ACCEPTED or DECLINED)
      security:
      - bearerAuth: []
      parameters:
      - name: offerId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordRecruitOfferReplyRequest'
      responses:
        '200':
          description: The resolved offer.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitOffer'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/offers/{offerId}/withdraw:
    post:
      tags:
      - recruiting
      operationId: withdrawRecruitOffer
      summary: Withdraw a live offer; the applicant returns to INTERVIEW
      security:
      - bearerAuth: []
      parameters:
      - name: offerId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WithdrawRecruitOfferRequest'
      responses:
        '200':
          description: The withdrawn offer.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitOffer'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/postings:
    get:
      tags:
      - recruiting
      operationId: listRecruitPostings
      summary: List job postings with per-stage applicant counts (recruiting_read or recruiting_manage)
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/RecruitPostingStatus'
      - name: scope
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/RecruitPostingScope'
      responses:
        '200':
          description: Postings in the caller's organization, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitPostingListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
    post:
      tags:
      - recruiting
      operationId: createRecruitPosting
      summary: Create a draft job posting (JP- code); recruiting_manage only
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRecruitPostingRequest'
      responses:
        '201':
          description: The draft posting.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitPosting'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/postings/{postingId}:
    get:
      tags:
      - recruiting
      operationId: getRecruitPosting
      summary: Posting detail with its non-PII applicant pipeline rows
      security:
      - bearerAuth: []
      parameters:
      - name: postingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Posting and applicant summaries.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitPostingDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags:
      - recruiting
      operationId: updateRecruitPosting
      summary: Edit a DRAFT posting in place (409 for any other status); CAS on expected_updated_at
      security:
      - bearerAuth: []
      parameters:
      - name: postingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRecruitPostingRequest'
      responses:
        '200':
          description: The updated draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitPosting'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/postings/{postingId}/applicants:
    post:
      tags:
      - recruiting
      operationId: createRecruitApplicant
      summary: Recruiter intake — register an applicant (APL- code) on a PUBLISHED posting
      security:
      - bearerAuth: []
      parameters:
      - name: postingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRecruitApplicantRequest'
      responses:
        '201':
          description: The applicant at APPLIED.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitApplicant'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/postings/{postingId}/close:
    post:
      tags:
      - recruiting
      operationId: closeRecruitPosting
      summary: Close a PUBLISHED posting (archive-not-delete); CAS on expected_updated_at
      security:
      - bearerAuth: []
      parameters:
      - name: postingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloseRecruitPostingRequest'
      responses:
        '200':
          description: The closed posting.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitPosting'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/recruiting/postings/{postingId}/preflight:
    post:
      tags:
      - recruiting
      operationId: preflightRecruitPosting
      summary: Read-only publish checklist evaluation for a DRAFT posting (recruiting_manage)
      security:
      - bearerAuth: []
      parameters:
      - name: postingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Check vector and the automatic-check verdict.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitPostingPreflightResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/recruiting/postings/{postingId}/publish:
    post:
      tags:
      - recruiting
      operationId: publishRecruitPosting
      summary: Publish a draft (atomic preflight re-evaluation plus exposure attest; fail-closed)
      security:
      - bearerAuth: []
      parameters:
      - name: postingId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PublishRecruitPostingRequest'
      responses:
        '200':
          description: The published posting.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitPosting'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          description: Request validation failed, or the preflight gate is unmet (code PREFLIGHT_FAILED, which adds the full
            check vector).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitPublishFailedResponse'
  /api/v1/recruiting/talent-pool:
    get:
      tags:
      - recruiting
      operationId: listRecruitTalentPool
      summary: Rejected-applicant archive (talent pool)
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Talent-pool rows, newest rejection first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecruitTalentPoolListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/regions:
    get:
      tags:
        - regions
      operationId: listRegions
      summary: List all regions
      security:
      - bearerAuth: []
      responses:
        '200':
          description: All regions.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/RegionSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    post:
      tags:
        - regions
      operationId: createRegion
      summary: Propose creating a region
      description: Creates a governed DRAFT containing one CREATE_REGION proposal. The live regions table is unchanged
        until the org-change lifecycle is approved and effectuated. Requires an Idempotency-Key; an exact replay returns
        the existing org-change request with 200.
      security:
      - bearerAuth: []
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        schema:
          type: string
          minLength: 16
          maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRegionRequest'
      responses:
        '200':
          description: Idempotent replay of the existing org-change request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '201':
          description: Governed region-create draft created; no region row was applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/regions/{id}:
    delete:
      tags:
        - regions
      operationId: deactivateRegion
      summary: Propose deactivating a region
      description: Creates a governed DRAFT containing one DEACTIVATE_REGION proposal. The live row is unchanged; preflight
        and apply retain the active-branch guard.
      security:
      - bearerAuth: []
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        schema:
          type: string
          minLength: 16
          maxLength: 200
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Idempotent replay of the existing org-change request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '201':
          description: Governed region-deactivation draft created; no region row was applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    patch:
      tags:
        - regions
      operationId: updateRegion
      summary: Rename a region
      description: Creates a governed DRAFT containing one RENAME_REGION proposal; it does not rename the live row.
      security:
      - bearerAuth: []
      parameters:
      - name: Idempotency-Key
        in: header
        required: true
        schema:
          type: string
          minLength: 16
          maxLength: 200
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRegionRequest'
      responses:
        '200':
          description: Idempotent replay of the existing org-change request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '201':
          description: Governed region-rename draft created; no region row was applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgChangeDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/reporting/work-diary:
    get:
      tags:
        - reporting
      operationId: getWorkDiaryDraft
      summary: Get or generate the editable work-diary draft for a date
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ReportDate'
      responses:
        '200':
          description: Work diary draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkDiaryDraft'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
    put:
      tags:
        - reporting
      operationId: updateWorkDiaryDraft
      summary: Update an editable work-diary draft body
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ReportDate'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkDiaryUpdateRequest'
      responses:
        '200':
          description: Updated work diary draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkDiaryDraft'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/reporting/work-diary/confirm:
    post:
      tags:
        - reporting
      operationId: confirmWorkDiaryDraft
      summary: Confirm a work-diary draft before export
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/ReportDate'
      responses:
        '200':
          description: Confirmed work diary draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkDiaryDraft'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/sales/inquiries:
    get:
      tags:
        - sales
      operationId: listInquiries
      summary: List customer inquiries for the admin inbox (#6)
      description: Admin-gated (SalesManage) inquiry inbox. Optional status filter; results are paged.
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/InquiryStatus'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 50
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paged customer inquiries.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerInquiryPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/sales/inquiries/{id}:
    patch:
      tags:
        - sales
      operationId: updateInquiryStatus
      summary: Update a customer inquiry's triage status (#6)
      description: Admin-gated (SalesManage). Transitions an inquiry between NEW/CONTACTED/CLOSED.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateInquiryStatusRequest'
      responses:
        '204':
          description: The inquiry status was updated.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/sales/listings:
    get:
      tags:
        - sales
      operationId: adminListListings
      summary: List all sales listings for the admin console (#6)
      description: Admin catalog read (SalesManage). Unlike the public storefront, returns listings in every status (including
        DRAFT/SOLD/WITHDRAWN). Optional filters narrow by fuel/drive kind and sale/rental type; results are paged.
      security:
      - bearerAuth: []
      parameters:
      - name: kind
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/ListingKind'
      - name: condition
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/ListingCondition'
      - name: listing_type
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/ListingType'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 24
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paged sales listings (all statuses).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalesListingPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags:
        - sales
      operationId: createListing
      summary: Create a sales listing (#6)
      description: Admin-gated (SalesManage). Creates a new catalog listing and returns its server-generated id.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateListingRequest'
      responses:
        '201':
          description: The listing was created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateListingResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/sales/listings/{id}:
    delete:
      tags:
        - sales
      operationId: deleteListing
      summary: Archive a sales listing (#6)
      description: Admin-gated (SalesManage). Soft-archives the listing (status -> WITHDRAWN) and removes it from
        the public catalog; the row and its media are preserved for history/object-graph integrity, never hard-deleted.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '204':
          description: The listing was archived (status set to WITHDRAWN).
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags:
        - sales
      operationId: updateListing
      summary: Update a sales listing (#6)
      description: 'Admin-gated (SalesManage). Partial update: absent keys are left unchanged; nullable keys explicitly set
        to null clear the column. At least one field must be supplied.'
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateListingRequest'
      responses:
        '204':
          description: The listing was updated.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/search:
    get:
      tags:
        - search
      operationId: searchObjects
      summary: Global object + person search (⌘K palette, compose picker, explore, token dropdowns)
      description: 'Searches the object kinds resolveObject serves that carry a human-searchable name/code (work_order,
        equipment, support_ticket, org_unit) plus the person directory (messenger member semantics: active + shared
        branch). Every hit is scoped identically to resolveObject: the WorkOrderReadAll feature gate for
        work_order/equipment (a caller lacking it gets zero hits of those kinds), and the branch-visibility guard for
        every branch-scoped kind — a hit the caller could not resolve never appears. Org-isolated under RLS.
        Deny-by-omission: never a 403 for an out-of-scope kind, just absence.'
      security:
      - bearerAuth: []
      parameters:
      - name: q
        in: query
        required: true
        description: Case-insensitive substring to match against names/codes (≤200 chars; empty returns no hits).
        schema:
          type: string
      - name: limit
        in: query
        required: false
        description: Max hits per object kind, clamped 1-50 (default 20).
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: Caller-visible hits across kinds and the person directory.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/series:
    post:
      tags:
        - series
      operationId: createSeries
      summary: Create an SR- series and attach its first instance
      description: 'Creates an org-scoped series with a canonical SR- code and attaches the first instance. The instance
        must resolve for the caller (deny-by-omission): you cannot found a series on an object you cannot see. Audited.'
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSeriesRequest'
      responses:
        '200':
          description: The created series with its first resolved instance.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SeriesDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/series/by-instance:
    get:
      tags:
        - series
      operationId: getSeriesByInstance
      summary: Find which series an object belongs to
      description: 'Returns the series a given (kind, id) instance belongs to, or null. Membership is revealed only when
        the caller can resolve the instance, so series membership is never an existence oracle for out-of-scope objects.'
      security:
      - bearerAuth: []
      parameters:
      - name: kind
        in: query
        required: true
        description: Instance object kind slug.
        schema:
          type: string
      - name: id
        in: query
        required: true
        description: Instance object id/reference.
        schema:
          type: string
      responses:
        '200':
          description: The series the instance belongs to (series=null when none / not resolvable).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SeriesByInstanceResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/series/{id}:
    get:
      tags:
        - series
      operationId: getSeries
      summary: Read a series and its ordered, caller-visible instances
      description: 'Returns the series head and its instances resolved to ObjectHeads, ordered by attach time. Instances
        that no longer resolve for the caller are omitted (deny-by-omission). Current/next derivation is left to the
        client. An unknown id or another tenant''s series is 404.'
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The series with its ordered, resolved instances.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SeriesDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/series/{id}/instances:
    post:
      tags:
        - series
      operationId: attachSeriesInstance
      summary: Attach an instance to an existing series
      description: 'Attaches a (kind, id) instance to the series. The series must exist for the caller and the instance
        must resolve for the caller (both deny-by-omission -> 404). An object already in a series is rejected with 409
        (the not-yet-in-a-series promotion invariant). Audited.'
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachInstanceRequest'
      responses:
        '200':
          description: The instance was attached.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachInstanceAck'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/settlements/{settlementId}/review:
    post:
      tags:
      - maintenance
      operationId: reviewSettlement
      summary: Four-eyes review of a SUBMITTED settlement; the reviewer must differ from the submitter
      security:
      - bearerAuth: []
      parameters:
      - name: settlementId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReviewSettlementRequest'
      responses:
        '200':
          description: The reviewed settlement, APPROVED or RETURNED back to DRAFT.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SettlementSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/settlements/{settlementId}/submit:
    post:
      tags:
      - maintenance
      operationId: submitSettlement
      summary: Submit a DRAFT settlement for review (creator or completion reviewer)
      security:
      - bearerAuth: []
      parameters:
      - name: settlementId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The submitted settlement.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SettlementSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/settlements/{settlementId}/void:
    post:
      tags:
      - maintenance
      operationId: voidSettlement
      summary: Void a DRAFT or SUBMITTED settlement (completion reviewer holding an admin-tier role; reason required)
      security:
      - bearerAuth: []
      parameters:
      - name: settlementId
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VoidSettlementRequest'
      responses:
        '200':
          description: The voided settlement; VOID frees the one-live-settlement slot.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SettlementSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/sites:
    post:
      tags:
        - sites
      operationId: createSite
      summary: Create a site (현장) under an existing customer
      description: Admin-gated (EquipmentManage). Creates a site under a customer in the caller's authorized branch scope;
        organization-wide callers may use any customer in their organization. A customer outside that scope is a 404, including
        another tenant's customer hidden by RLS. The name is required and bounded;
        optional address / WGS84 coordinates / contact fields follow the same ranges and length bounds as PATCH /api/v1/sites/{id}
        (a one-sided coordinate or an over-long value is a 422). A duplicate site name under the same customer is a 409. The
        created site is returned so it appears in the 고객·현장 list immediately.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSiteRequest'
      responses:
        '201':
          description: Site created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatedSite'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/sites/{id}:
    patch:
      tags:
        - sites
      operationId: updateSite
      summary: Update a site's coordinates and administrative address
      description: 'Admin-gated (EquipmentManage). The only coordinate entry point: a site is pinnable on the dispatch map
        only once an admin writes a valid lat/lon pair here. Latitude/longitude must be updated together and fall within WGS84
        ranges; supplying one without the other is a 422. Only supplied fields are written; nullable fields explicitly set
        to null are cleared.'
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/SiteId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSiteRequest'
      responses:
        '204':
          description: Site updated.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
  /api/v1/storefront/inquiries:
    post:
      tags:
        - storefront
      security: []
      operationId: submitInquiry
      summary: Submit a public customer inquiry (#6)
      description: Public, unauthenticated lead intake. Accepts the customer's contact details and topic and acknowledges
        receipt without echoing any field. Generic validation; a bad payload returns 400.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitInquiryRequest'
      responses:
        '202':
          description: Inquiry accepted for processing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InquiryAck'
        '400':
          $ref: '#/components/responses/ValidationError'
  /api/v1/storefront/listings:
    get:
      tags:
        - storefront
      security: []
      operationId: storefrontListListings
      summary: List published sales listings for the public storefront (#6)
      description: Public, unauthenticated catalog read. Returns only published/reserved listings (the storefront-visible
        set). Carries no bearer token; the store is scoped to the KNL org. Optional filters narrow by fuel/drive kind and
        sale/rental type; results are paged.
      parameters:
      - name: kind
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/ListingKind'
      - name: condition
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/ListingCondition'
      - name: listing_type
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/ListingType'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 24
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paged public sales listings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalesListingPage'
  /api/v1/storefront/listings/{id}:
    get:
      tags:
        - storefront
      security: []
      operationId: storefrontGetListing
      summary: Read one public sales listing (#6)
      description: Public, unauthenticated read of a single storefront-visible listing. Returns 404 if the listing does not
        exist or is not public.
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The sales listing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalesListingView'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/storefront/listings/{id}/media/{media_id}:
    get:
      tags:
        - storefront
      security: []
      operationId: storefrontGetListingMedia
      summary: Serve one public sales-listing photo (#6)
      description: Public, unauthenticated serve of one listing photo's bytes, streamed from the object store. The media must
        belong to the listing and the listing must be storefront-visible (published/reserved); otherwise 404. The response
        body is the raw image with its stored content type.
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: media_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The listing photo bytes.
          content:
            image/*:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/support/intake:
    post:
      tags:
        - support
      security: []
      operationId: submitSupportIntake
      summary: Submit a support request from the unauthenticated customer channel
      description: 'Unauthenticated, rate-limited customer intake. Generic validation errors are returned; the customer contact
        is treated as PII and never echoed or logged.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerIntakeRequest'
      responses:
        '202':
          description: Support request received.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportIntakeAck'
        '400':
          $ref: '#/components/responses/ValidationError'
        '422':
          $ref: '#/components/responses/ValidationError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
  /api/v1/support/tickets:
    get:
      tags:
        - support
      operationId: listSupportTickets
      summary: List branch-scoped support tickets with optional filters
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/SupportTicketStatus'
      - name: priority
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/SupportTicketPriority'
      - name: category
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/SupportTicketCategory'
      - name: origin
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/SupportTicketOrigin'
      - name: assignee_user_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: site_id
        in: query
        required: false
        description: Restrict to tickets linked to one customer site (the field-console queue).
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: include_untriaged
        in: query
        required: false
        schema:
          type: boolean
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 50
      - name: cursor
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: A keyset page of support tickets visible in the principal's branch scope, plus the unpaged total for
            the same filters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportTicketPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    post:
      tags:
        - support
      operationId: createInternalSupportTicket
      summary: Open an internal support ticket as authenticated staff
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateInternalTicketRequest'
      responses:
        '201':
          description: Support ticket created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportTicketSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/support/tickets/{id}:
    get:
      tags:
        - support
      operationId: getSupportTicket
      summary: Get a support ticket with its comments (staff view)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Support ticket detail including internal notes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportTicketDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/support/tickets/{id}/acceptance:
    post:
      tags:
      - support
      operationId: recordSupportTicketAcceptance
      summary: Record the customer's acceptance verdict for a RESOLVED ticket (audited closure evidence)
      description: Requires assignee_manage on the ticket branch and an Idempotency-Key header. CUSTOMER_ACCEPTED drives
        RESOLVED to CLOSED; CUSTOMER_DECLINED requires a note, drives RESOLVED to IN_PROGRESS, and the note becomes a
        customer-visible comment. A replay with the same key and the same request returns the stored acceptance; the same
        key with a different request is a conflict. accepted_by is a business fact and is never logged.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: Idempotency-Key
        in: header
        required: true
        schema:
          type: string
          minLength: 16
          maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordSupportTicketAcceptanceRequest'
      responses:
        '201':
          description: The recorded acceptance, or the stored acceptance on an idempotent replay.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportTicketAcceptance'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/support/tickets/{id}/assign:
    post:
      tags:
        - support
      operationId: assignSupportTicket
      summary: Assign or reassign a support ticket (triages untriaged intake)
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignTicketRequest'
      responses:
        '200':
          description: Support ticket assigned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportTicketSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/support/tickets/{id}/comments:
    post:
      tags:
        - support
      operationId: addSupportTicketComment
      summary: Append a comment or internal note to a support ticket
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCommentRequest'
      responses:
        '201':
          description: Comment added.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportTicketComment'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/support/tickets/{id}/link:
    post:
      tags:
      - support
      operationId: linkSupportTicket
      summary: Bind a support ticket to the field object chain (customer site and/or dispatched work order)
      description: Requires assignee_manage on the ticket branch (untriaged tickets require cross-branch authority). Linking
        a site to an untriaged CUSTOMER ticket also sets its branch, and customer_id is denormalized from the site. A linked
        work order must be dispatched to the ticket's linked site. Each body field distinguishes an absent field (leave the
        link untouched) from an explicit null (clear the link).
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LinkSupportTicketRequest'
      responses:
        '200':
          description: The updated ticket summary carrying the new links.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportTicketSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/support/tickets/{id}/transition:
    post:
      tags:
        - support
      operationId: transitionSupportTicket
      summary: Drive the support ticket status FSM
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransitionTicketRequest'
      responses:
        '200':
          description: Support ticket status changed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SupportTicketSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/sync:
    post:
      tags:
        - sync
      operationId: replayOfflineSyncBatch
      summary: Replay an idempotent mobile offline-operation batch
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/XDeviceId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncBatchRequest'
      responses:
        '200':
          description: Per-operation replay results. Individual failures do not abort the batch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncBatchResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/users:
    get:
      tags:
        - users
      operationId: listUsers
      summary: List users visible within the caller's branch scope
      security:
      - bearerAuth: []
      parameters:
      - name: include_inactive
        in: query
        required: false
        schema:
          type: boolean
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 200
          default: 50
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: A page of users in the principal's branch scope.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    post:
      tags:
        - users
      operationId: createUser
      summary: Create a user with roles and branch memberships
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRequest'
      responses:
        '201':
          description: User created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/users/me:
    get:
      tags:
        - users
      operationId: getCurrentUser
      summary: Get the authenticated user's own profile
      security:
      - bearerAuth: []
      responses:
        '200':
          description: The authenticated user's profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    patch:
      tags:
        - users
      operationId: updateCurrentUser
      summary: Update the authenticated user's own display name and phone
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSelfProfileRequest'
      responses:
        '200':
          description: Updated profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/users/{id}:
    get:
      tags:
        - users
      operationId: getUser
      summary: Get a user by id within the caller's branch scope
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The requested user.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    patch:
      tags:
        - users
      operationId: updateUser
      summary: Update a user's profile, roles, and branch memberships
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserRequest'
      responses:
        '200':
          description: Updated user.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/users/{id}/activate:
    post:
      tags:
        - users
      operationId: activateUser
      summary: Reactivate an archived user
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Reactivated user.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/users/{id}/deactivate:
    post:
      tags:
        - users
      operationId: deactivateUser
      summary: Deactivate (soft-disable) a user
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Deactivated user.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v1/work-orders:
    get:
      tags:
        - work-orders
      operationId: listWorkOrders
      summary: List branch-scoped work orders
      description: Returns branch-scoped work orders sorted by priority and target due date. The server accepts repeated `status`,
        `status[]`, `priority`, and `priority[]` query keys, plus comma-separated values. `branch_id` narrows results
        within the caller's existing branch/RLS scope. `around_work_order_id` searches
        around a seed work order by returning the seed plus branch/RLS-visible work orders sharing customer, site, or equipment.
        The response includes a branch/RLS-scoped object-set lens with aggregates, facets, due-date histogram buckets, and
        customer/site listograms for drill-to-act dashboards.
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        style: form
        explode: true
        schema:
          type: array
          items:
            $ref: '#/components/schemas/WorkOrderStatus'
      - name: priority
        in: query
        required: false
        style: form
        explode: true
        schema:
          type: array
          items:
            $ref: '#/components/schemas/PriorityLevel'
      - name: maintenance_type
        in: query
        required: false
        style: form
        explode: true
        schema:
          type: array
          items:
            $ref: '#/components/schemas/MaintenanceType'
      - name: maintenance_cause
        in: query
        required: false
        style: form
        explode: true
        schema:
          type: array
          items:
            $ref: '#/components/schemas/MaintenanceCause'
      - name: assigned_to
        in: query
        required: false
        schema:
          type: string
        description: Use `me` for the authenticated user or a user UUID.
      - name: customer_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: site_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: equipment_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
        description: Restricts to one asset's orders (asset → work-order history).
      - name: branch_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
        description: Narrows results to one branch already visible to the caller; it never expands branch/RLS visibility.
      - name: around_work_order_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
        description: Search around this seed work order; returns branch/RLS-visible orders sharing the seed's customer, site,
          or equipment, including the seed.
      - name: target_due_from
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: target_due_to
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Timestamp'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
          default: 50
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paginated work-order list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderListPage'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/work-orders/{workOrderId}:
    get:
      tags:
        - work-orders
      operationId: getWorkOrderDetail
      summary: Fetch branch-scoped work-order detail
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      responses:
        '200':
          description: Full work-order detail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/work-orders/{workOrderId}/p1-dispatch:
    post:
      tags:
        - work-orders
      operationId: startP1Dispatch
      summary: Start a P1 emergency dispatch broadcast for a work order
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartP1DispatchRequest'
      responses:
        '200':
          description: Started P1 dispatch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/P1DispatchSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/work-orders/{workOrderId}/reject:
    post:
      tags:
        - work-orders
      operationId: rejectWorkOrder
      summary: Reject a submitted work order with an audit memo
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RejectWorkOrderRequest'
      responses:
        '200':
          description: Rejected work order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/work-orders/{workOrderId}/settlement:
    get:
      tags:
      - maintenance
      operationId: getWorkOrderSettlement
      summary: Fetch the live (non-VOID) cost settlement of a work order
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      responses:
        '200':
          description: The live cost settlement.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SettlementSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags:
      - maintenance
      operationId: createWorkOrderSettlement
      summary: Open a DRAFT cost settlement on a report-submitted, admin-review, or final-completed order
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      - name: Idempotency-Key
        in: header
        required: true
        schema:
          type: string
          minLength: 16
        description: Replaying the same key with an identical body returns the existing settlement without a second audit
          event; the same key with a different body is a 409.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSettlementRequest'
      responses:
        '201':
          description: The draft settlement, also returned on an idempotent replay.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SettlementSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-runs:
    get:
      tags:
        - workflow-runs
      operationId: listWorkflowRunsAdmin
      summary: Org-wide admin run list, incl. dead-letter (workflow-manage)
      description: Lists workflow runs across the org, filterable by status (including FAILED/DEAD_LETTERED for dead-letter
        visibility) and keyset-paginated over (updated_at, id). Requires workflow-manage authority.
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        description: Comma-separated run statuses (e.g. FAILED,DEAD_LETTERED). Omitted returns all statuses.
        schema:
          type: string
      - name: before
        in: query
        required: false
        description: Keyset cursor — the run_id of the last row of the previous page.
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        description: Page size, clamped to [1, 200]. Defaults to 50.
        schema:
          type: integer
          format: int32
      responses:
        '200':
          description: A page of org-wide runs, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminWorkflowRunListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
    post:
      tags:
        - workflow-runs
      operationId: startWorkflowRun
      summary: Start a workflow run (idempotent)
      description: Starts an ACTIVE definition and advances synchronously until the first WAITING task or terminal node.
        idempotency_key maps to workflow_runs.idempotency_key — a replay returns the existing run, and the same key with a
        different definition/object is a 409. Entry-node policy (when declared) is legacy-enforced with an inert Cedar shadow.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartWorkflowRunRequest'
      responses:
        '200':
          description: Run started (or replayed) with its current first WAITING task, if any.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StartWorkflowRunResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-runs/for-object:
    get:
      tags:
        - workflow-studio
      operationId: listWorkflowRunsForObject
      summary: List visible workflow runs for one authorized object
      description: >-
        Read-only exact-pair bridge for work_order and support_ticket subjects.
        The server authorizes the native subject first, then returns only workflow
        runs visible to the same principal. Unknown or invisible cursors fail with
        the same validation code and do not disclose foreign rows.
      security:
      - bearerAuth: []
      parameters:
      - name: object_type
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/WorkflowObjectKind'
      - name: object_id
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 25
      - name: before
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Visible workflow runs bound to the exact subject pair.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowRunsForObjectResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/workflow-runs/mine:
    get:
      tags:
        - workflow-runs
      operationId: listMyWorkflowRuns
      summary: List the runs the caller initiated (submission box)
      description: Returns runs where initiated_by is the caller, optionally filtered by status/object_type. Final-approved but
        not-yet-finalized runs are still WAITING (non-terminal) and remain visible.
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        description: Comma-separated run statuses (e.g. WAITING,SUCCEEDED). Omitted returns all statuses.
        schema:
          type: string
      - name: object_type
        in: query
        required: false
        schema:
          type: string
      - name: q
        in: query
        required: false
        description: Reserved free-text filter (not yet applied).
        schema:
          type: string
      responses:
        '200':
          description: The caller's submission box.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowRunListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-runs/{run_id}:
    get:
      tags:
        - workflow-runs
      operationId: getWorkflowRun
      summary: Read-only run detail — head, waiting tasks, node-step timeline
      description: Visibility mirrors the approval inbox exactly — the run's initiator, a claimer of one of its tasks, or a
        holder of an authority role a task is routed to — plus workflow-manage admins org-wide. Anyone else gets 404
        (deny-by-omission), never a leak of another branch's/org's run.
      security:
      - bearerAuth: []
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: The run's head, current waiting task(s), and its full node-step timeline.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowRunDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow-runs/{run_id}/post-finalization-rejection:
    post:
      tags:
        - workflow-runs
      operationId: createWorkflowPostFinalizationRejection
      summary: Create a compensating post-finalization rejection (사후 반려)
      description: Records a compensating document linked to an already-finalized run and notifies the whole approval line.
        The terminal run is never reopened or mutated; a new POST_FINALIZATION_REJECTION document is created instead.
        Policy-gated (legacy enforce, inert Cedar shadow). Idempotent on idempotency_key.
      security:
      - bearerAuth: []
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PostFinalizationRejectionRequest'
      responses:
        '200':
          description: Compensating document created (replays return the recorded result).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PostFinalizationRejectionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/catalog:
    get:
      tags:
        - workflow-studio
      operationId: getWorkflowStudioCatalog
      summary: List Workflow Studio connector allowlist and templates
      description: 'Returns the server-owned connector/action allowlist and starter workflow templates. This is the safe no-code
        boundary: workflow authors can only compose allowed internal actions, and every request still requires tenant RLS
        plus RoleManage authorization.'
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Workflow Studio catalog.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowStudioCatalogResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/workflow-studio/definitions:
    get:
      tags:
        - workflow-studio
      operationId: listWorkflowDefinitions
      summary: List tenant workflow definitions
      description: Lists tenant-owned workflow definitions with latest version metadata, approval/payment line requirements,
        notification rules, and action allowlist. The backing schema keeps definitions tenant-scoped under RLS and stores
        versions/change events append-only.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Workflow definitions for the caller's tenant.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags:
        - workflow-studio
      operationId: createWorkflowDefinition
      summary: Create a draft workflow definition
      description: Creates a DRAFT workflow definition and version 1. Publishing, pausing, rollback, and clone operations
        are sensitive actions that require a fresh passkey step-up assertion and append immutable history.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWorkflowDefinitionRequest'
      responses:
        '200':
          description: Draft workflow definition created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/by-object-kind/{kind}:
    get:
      tags:
        - workflow-studio
      operationId: listWorkflowDefinitionsByObjectKind
      summary: Automation rules acting on an object kind (dynamics↔ontology)
      description: The explore screen's "작용 자동화" panel source. Returns definitions whose primary object_type is the kind or
        whose declared object_kinds chain touches it, plus the trigger bindings scoped to that kind.
      security:
      - bearerAuth: []
      parameters:
      - name: kind
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Definitions and trigger bindings touching the object kind.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DefinitionsByObjectKindResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/{id}:
    delete:
      tags:
        - workflow-studio
      operationId: archiveWorkflowDefinition
      summary: Delete a draft workflow definition
      description: Soft-deletes a DRAFT workflow definition by moving the mutable pointer to RETIRED while preserving
        append-only versions, change events, and audit evidence. Requires a fresh passkey step-up assertion.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowStepUpRequest'
      responses:
        '200':
          description: Archived workflow definition draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    patch:
      tags:
        - workflow-studio
      operationId: updateWorkflowDefinition
      summary: Update a draft workflow definition
      description: Updates an existing DRAFT workflow definition by appending a new draft version. Workflow key and object
        type remain immutable; archived, active, and paused definitions are rejected.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkflowDefinitionRequest'
      responses:
        '200':
          description: Updated draft workflow definition.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/{id}/clone:
    post:
      tags:
        - workflow-studio
      operationId: cloneWorkflowDefinition
      summary: Clone a workflow definition into a new draft
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloneWorkflowDefinitionRequest'
      responses:
        '200':
          description: Cloned workflow definition draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/{id}/history:
    get:
      tags:
        - workflow-studio
      operationId: listWorkflowDefinitionHistory
      summary: List append-only workflow definition change history
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Append-only Workflow Studio change history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionHistoryResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow-studio/definitions/{id}/pause:
    post:
      tags:
        - workflow-studio
      operationId: pauseWorkflowDefinition
      summary: Pause an active workflow definition
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowStepUpRequest'
      responses:
        '200':
          description: Paused workflow definition.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/{id}/publish:
    post:
      tags:
        - workflow-studio
      operationId: publishWorkflowDefinition
      summary: Publish a workflow definition version (direct for new, staged for active)
      description: Sensitive no-code workflow publication. Requires RoleManage, tenant RLS, a fresh passkey step-up assertion,
        required approval/payment line validation, connector/action allowlist validation, append-only workflow_definition_versions,
        workflow_definition_events, and audit log. A definition that has never been activated is published directly; publishing
        a revision to an ALREADY-ACTIVE definition instead STAGES a pending revision (the active version keeps serving) that a
        second, distinct actor must approve via the revisions/{rev}/approve endpoint. The response's pending_version signals a
        staged (not-yet-applied) revision.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowStepUpRequest'
      responses:
        '200':
          description: Published workflow definition.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/{id}/resume:
    post:
      tags:
        - workflow-studio
      operationId: resumeWorkflowDefinition
      summary: Resume a paused workflow definition
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowStepUpRequest'
      responses:
        '200':
          description: Resumed workflow definition.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/{id}/revisions/{rev}/approve:
    post:
      tags:
        - workflow-studio
      operationId: approveWorkflowDefinitionRevision
      summary: Approve a staged pending revision (four-eyes application)
      description: Applies the revision staged by a publish on an ACTIVE definition. A SECOND, distinct actor must approve
        (the publisher who staged it cannot self-approve unless org-lead/SUPER_ADMIN, recorded as a governance finding). The
        approved DRAFT is appended as a new PUBLISHED version and becomes the active version.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: rev
        in: path
        required: true
        description: The pending revision (version number) being approved.
        schema:
          type: integer
          format: int32
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowStepUpRequest'
      responses:
        '200':
          description: Revision applied; the new version is active.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/{id}/revisions/{rev}/withdraw:
    post:
      tags:
        - workflow-studio
      operationId: withdrawWorkflowDefinitionRevision
      summary: Withdraw (discard) a staged pending revision
      description: Clears the pending-revision pointer; the active version keeps serving and the DRAFT remains in history.
        Any workflow manager may withdraw (it applies nothing, so it is not SoD-gated).
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      - name: rev
        in: path
        required: true
        description: The pending revision (version number) being withdrawn.
        schema:
          type: integer
          format: int32
      responses:
        '200':
          description: Pending revision withdrawn.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/workflow-studio/definitions/{id}/rollback:
    post:
      tags:
        - workflow-studio
      operationId: rollbackWorkflowDefinition
      summary: Roll back by appending a new active version copied from history
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RollbackWorkflowDefinitionRequest'
      responses:
        '200':
          description: Rolled back workflow definition.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinitionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '428':
          description: Fresh passkey step-up is required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/{id}/run:
    post:
      tags:
        - workflow-studio
      operationId: triggerWorkflowRun
      summary: Trigger a workflow definition run
      description: Creates an idempotent MANUAL runtime row for an ACTIVE workflow definition and returns the run-log event used by Workflow Studio.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TriggerWorkflowRunRequest'
      responses:
        '200':
          description: Workflow run trigger accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowRunResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/definitions/{id}/run-log:
    get:
      tags:
        - workflow-studio
      operationId: listWorkflowRunLog
      summary: List workflow automation run-log entries
      description: Returns the most recent durable runtime rows for this workflow definition, scoped by tenant RLS, for the Workflow Studio timeline.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Workflow runtime run log.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowRunLogResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow-studio/definitions/{id}/simulate:
    post:
      tags:
        - workflow-studio
      operationId: simulateWorkflowDefinition
      summary: Simulate publish readiness without writing
      description: Validates the workflow definition, required approval/payment lines, conditional notification rules, and
        connector/action allowlist without mutating state. Use this before passkey-gated publication.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SimulateWorkflowDefinitionRequest'
      responses:
        '200':
          description: Simulation result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowSimulationResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/schedules:
    get:
      tags:
        - workflow-studio
      operationId: listWorkflowSchedules
      summary: List workflow cron schedules
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Workflow schedules.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowScheduleListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags:
        - workflow-studio
      operationId: createWorkflowSchedule
      summary: Create a workflow cron schedule
      description: cron_expr is evaluated in the schedule's IANA timezone (default Asia/Seoul). The background poller starts
        one idempotent run per fire and advances next_run_at.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWorkflowScheduleRequest'
      responses:
        '200':
          description: Created workflow schedule.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowScheduleResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/schedules/preview-next-runs:
    post:
      tags:
        - workflow-studio
      operationId: previewWorkflowScheduleNextRuns
      summary: Preview the next fire times for a cron expression
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PreviewScheduleRequest'
      responses:
        '200':
          description: The next fire times (RFC 3339, UTC) for the expression in its timezone.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreviewScheduleResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/schedules/{id}:
    patch:
      tags:
        - workflow-studio
      operationId: updateWorkflowSchedule
      summary: Update a workflow cron schedule (label, cron, timezone, enabled)
      description: Changing the cron pattern or timezone, or re-enabling, recomputes next_run_at from now. Schedules are
        durable objects — disable instead of delete (run history stays dereferenceable).
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkflowScheduleRequest'
      responses:
        '200':
          description: Updated workflow schedule.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowScheduleResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/schedules/{id}/runs:
    get:
      tags:
        - workflow-studio
      operationId: listWorkflowScheduleRuns
      summary: Run history for a workflow schedule
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Runs started by this schedule, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScheduleRunListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow-studio/submittable-definitions:
    get:
      tags:
        - workflow-studio
      operationId: listSubmittableWorkflowDefinitions
      summary: List workflow definitions the caller may start (기안 template gallery)
      description: 'All-employee (member-gated, not workflow-manage admin) catalog of ACTIVE workflow definitions the
        caller could actually START. Deny-by-omission: a definition is listed only when its start authority admits the
        caller — the identical check the start endpoint (POST /api/v1/workflow-runs) enforces (top-level start_policy,
        else the entry node''s required_policy; absent = self-service all-employee). The catalog never advertises a
        definition the caller would get a 403 starting. Carries only the metadata definitions actually hold (no invented
        icon/desc/tone — those are frontend presentation).'
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Startable ACTIVE definitions for the caller.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmittableDefinitionListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /api/v1/workflow-studio/trigger-bindings:
    get:
      tags:
        - workflow-studio
      operationId: listWorkflowTriggerBindings
      summary: List workflow trigger bindings (domain event → workflow start rules)
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Trigger bindings plus the registered event-key vocabulary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TriggerBindingListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags:
        - workflow-studio
      operationId: createWorkflowTriggerBinding
      summary: Bind a registered domain event to a workflow definition
      description: When the bound event's audited mutation commits, the dispatcher starts one idempotent run of the bound
        definition per event occurrence. trigger_type must be an event-shaped reserved TriggerType and event_key a registered
        domain event.
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTriggerBindingRequest'
      responses:
        '200':
          description: Created trigger binding.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TriggerBindingResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-studio/trigger-bindings/{id}/disable:
    post:
      tags:
        - workflow-studio
      operationId: disableWorkflowTriggerBinding
      summary: Disable a workflow trigger binding
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Updated trigger binding.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TriggerBindingResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow-studio/trigger-bindings/{id}/enable:
    post:
      tags:
        - workflow-studio
      operationId: enableWorkflowTriggerBinding
      summary: Enable a workflow trigger binding
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '200':
          description: Updated trigger binding.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TriggerBindingResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow-tasks:
    get:
      tags:
        - workflow-tasks
      operationId: listWorkflowTasks
      summary: List approval-inbox waiting tasks (group or personal)
      description: Group inbox via role_key, personal inbox via assignee=me. Policy-bearing rows are gated legacy-enforce with an
        inert Cedar shadow and denied rows are OMITTED (deny-by-omission), never returned as a 403. Requires role_key or assignee=me.
      security:
      - bearerAuth: []
      parameters:
      - name: role_key
        in: query
        required: false
        description: Group-inbox filter matched against assignee_role_key.
        schema:
          type: string
      - name: assignee
        in: query
        required: false
        description: Set to "me" for the personal inbox (the caller's CLAIMED tasks plus claimable OPEN ones).
        schema:
          type: string
      - name: status
        in: query
        required: false
        description: Comma-separated task statuses (e.g. OPEN,CLAIMED). Defaults to OPEN.
        schema:
          type: string
      responses:
        '200':
          description: The visible waiting tasks.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowTaskListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-tasks/{task_id}/claim:
    post:
      tags:
        - workflow-tasks
      operationId: claimWorkflowTask
      summary: Claim an OPEN waiting task
      description: Transitions an OPEN task to CLAIMED for the caller. A same-user replay is a 200 no-op; a task claimed by
        another user, or in a terminal/cancelled/expired state, is a 409. Audits workflow_task.claim.
      security:
      - bearerAuth: []
      parameters:
      - name: task_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClaimWorkflowTaskRequest'
      responses:
        '200':
          description: Task claimed (replays return the current claim).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClaimWorkflowTaskResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-tasks/{task_id}/decide:
    post:
      tags:
        - workflow-tasks
      operationId: decideWorkflowTask
      summary: Decide a waiting task (approve/reject/return)
      description: Completes a non-finalize approval task. approve advances the run to the next node (a human task parks WAITING;
        no successor closes the run SUCCEEDED). reject and return require a non-empty comment (422 otherwise) and cancel the run —
        a resubmission is a new run, never a terminal reopen. Idempotent on idempotency_key. Audits workflow_task.decide.
      security:
      - bearerAuth: []
      parameters:
      - name: task_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DecideWorkflowTaskRequest'
      responses:
        '200':
          description: Decision recorded (replays return the recorded result).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DecideWorkflowTaskResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/workflow-tasks/{task_id}/finalize:
    post:
      tags:
        - workflow-tasks
      operationId: finalizeWorkflowTask
      summary: Finalize an approval document (author or delegate)
      description: Completes a finalization waiting task (종결). Author mode requires the initiating author; delegate mode is
        policy-gated (legacy enforce, inert Cedar shadow) and requires a non-empty reason. Finalization is a pre-terminal
        WAITING step, not a terminal reopen — the run reaches SUCCEEDED only when no receipt-confirmation step follows;
        otherwise it stays WAITING and a receipt task opens. Idempotent on idempotency_key.
      security:
      - bearerAuth: []
      parameters:
      - name: task_id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FinalizeWorkflowTaskRequest'
      responses:
        '200':
          description: Finalization recorded (replays return the recorded result).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FinalizeWorkflowTaskResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/v1/ws:
    get:
      tags:
        - ws
      operationId: connectRealtimeWebSocket
      summary: Connect to the realtime WebSocket stream
      description: 'Upgrades to a WebSocket after bearer-token verification. Server frames are JSON realtime events; reconnecting
        clients pass last_message_id to resume from the last processed message cursor.

        '
      security:
      - bearerAuth: []
      parameters:
      - name: last_message_id
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Uuid'
      responses:
        '101':
          description: WebSocket upgrade accepted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: Realtime is unavailable.
  /api/v2/leave/requests:
    get:
      tags:
        - leave
      operationId: listLeaveRequestsV2
      summary: List the branch-scoped leave-request approval queue (연차 결재함)
      description: >-
        Pending-first, then newest. Requires `employee_directory_read`. The
        queue is confined to the caller's branches (resolved from the JWT); an
        out-of-scope request is invisible (deny-by-omission).
      security:
      - bearerAuth: []
      parameters:
      - name: status
        in: query
        required: false
        description: >-
          Filter to one status; omitted returns every status, including the
          read-only historical `returned`/`rejected` rows.
        schema:
          type: string
          enum: [pending, approved, time_change_consult, returned, rejected]
      - name: limit
        in: query
        required: false
        description: Page size for the stable pending-first queue.
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 200
          default: 100
      - name: cursor
        in: query
        required: false
        description: Last request id from the preceding branch-scoped page.
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: A branch-scoped page of leave requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequestV2Page'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: JWT verification is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
    post:
      tags:
        - leave
      operationId: createLeaveRequestV2
      summary: File a self-service 연차/반차 request (본인 연차 신청)
      description: >-
        The caller files a leave request for THEMSELVES. `subject_employee_id`
        and the routing `branch_id` are resolved server-side from the caller's
        own account and employee.home_branch_id — never from input — so a
        caller can only file for their own employee record. No directory feature
        is required (filing one's own leave is a base employee capability); the
        gate is an active account linked to an active employee; an unlinked or
        inactive subject is denied with 403. Missing home-branch authority returns the
        stable 409 `leave_home_branch_review_required`. The request preserves
        date and AM/PM intent in `review_required`; no calendar-day or fixed-half
        quantity is invented. A separate resolver pins authoritative evidence
        before any approval can move the ledger. Modern clients generate one
        `idempotency_key` per user intent and reuse it after an unknown or lost
        response. The same key and canonical client intent return the original
        request without another mutation or audit; a different intent conflicts.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveCreateRequest'
      responses:
        '201':
          description: The created pending leave request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequestV2View'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: The linked active employee has no explicit home branch (`leave_home_branch_review_required`), or the supplied `idempotency_key` is already bound to different client intent (`leave_create.idempotency_conflict`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          description: >-
            Invalid leave type, date range, or partial-day intent — or a body
            carrying the retired `reason` field (근로기준법 §60: 연차에는 사유가
            없다).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: JWT verification or the separately credentialed leave command database is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v2/leave/requests/{id}/alternate-dates:
    post:
      tags:
        - leave
      operationId: proposeLeaveAlternateDatesV2
      summary: Propose worker-chosen alternate 시기 on a §60⑤ consult
      description: >-
        Self-service only: the authenticated caller must be the original
        requester of a `time_change_consult` leave request. No
        `employee_directory_manage` capability is required (or sufficient) —
        managers cannot choose the alternate dates. The body carries a new
        inclusive date span that must differ from the original 시기; leave
        type and partial-day period are preserved from the consult row.
        Returns the updated v2 leave-request view with `alternate_*` fields
        populated.
      security:
        - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveProposeAlternateDatesRequest'
      responses:
        '200':
          description: The consult request with worker-proposed alternate dates.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequestV2View'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: >-
            Request is not in `time_change_consult`, dates are invalid or
            identical to the original 시기, or the body carries unknown fields.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: The separately credentialed leave command database is not configured or unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v2/leave/requests/{id}/charge-resolution:
    post:
      tags:
        - leave
      operationId: resolveLeaveChargeV2
      summary: Record an audited exact leave-charge resolution
      description: >-
        Requires EmployeeDirectoryManage in the request branch. The body carries
        reviewed per-date obligations and source revision references, never a
        client total or digest. The server validates complete date coverage,
        canonicalizes, totals, hashes, and records an immutable resolution.
        `expected_version` is the current mutable request_version; the
        charge_version is assigned independently to the evidence snapshot.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveChargeResolutionRequest'
      responses:
        '200':
          description: Server-computed exact charge and digest with the new request CAS and charge-evidence revisions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveChargeResolutionView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The request is no longer pending or expected_version does not match the current request_version (`leave_concurrent_modification`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          $ref: '#/components/responses/ValidationError'
        '503':
          description: The separately credentialed leave command database is not configured or unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v2/leave/requests/{id}/decide:
    post:
      tags:
        - leave
      operationId: decideLeaveRequestV2
      summary: Approve a pending leave request or open a 시기변경 협의
      description: >-
        Requires `employee_directory_manage` in the request's branch. An
        APPROVE writes the leave ledger (used += exact resolved charge units,
        remaining -= exact resolved charge units) in the same audited transaction.
        Separation of duties — a request cannot be
        decided by its own requester (403). 근로기준법 §60 forbids refusing 연차:
        `return`/`reject` are retired (422) and the only non-approve decision is
        `time_change` (§60⑤ 시기변경 협의), which requires a comment and never
        touches the ledger. Eligibility for `time_change` is system-judged from
        branch coverage evidence (요건 자동 판정); without a coverage shortfall
        the decision fails closed with 422 `leave_decide.time_change_ineligible`.
        A successful consult stamps `time_change_grounds` /
        `time_change_evidence` on the v2 view; the worker then proposes
        alternate dates via POST `/api/v2/leave/requests/{id}/alternate-dates`.
        Approval additionally requires a resolved exact charge and a distinct
        resolver. Clients must carry the current request_version as
        `expected_version`; omission is rejected so every v2 decision is an
        explicit compare-and-swap. charge_version identifies immutable evidence
        and is never a request mutation precondition. A successful decision
        increments request_version only; charge_version remains unchanged. An
        unresolved approval returns 409 `leave_calendar_review_required` plus
        review reasons, audits the blocked attempt, and changes neither request
        nor ledger.
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/Uuid'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveDecideV2Request'
      responses:
        '200':
          description: The decided leave request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequestV2View'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Not pending, stale expected request_version (`leave_concurrent_modification`), or unresolved calendar/policy evidence.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '422':
          description: >-
            Missing mandatory comment, unknown decision, or `time_change`
            without automatic coverage-shortfall eligibility
            (`leave_decide.time_change_ineligible`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
        '503':
          description: The separately credentialed leave command database is not configured or unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBody'
  /api/v2/me/leave:
    get:
      tags:
        - leave
      operationId: getMyLeaveV2
      summary: Read the authenticated employee's own leave balance and history
      description: >-
        Base employee self-service. The server binds both user and linked
        employee identity; no employee-directory feature is required and no
        employee or branch identifier is accepted from the client. The balance
        includes a closed filing state so a missing or inactive home branch is
        visible before submission rather than discovered only after a 409.
      security:
      - bearerAuth: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int64
          minimum: 1
          maximum: 200
          default: 100
      - name: cursor
        in: query
        required: false
        description: Last request id from the preceding stable self-service page.
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Exact self balance plus self-only request history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MyLeaveV2Overview'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/work-orders:
    post:
      tags:
        - work-orders
      operationId: createWorkOrder
      summary: Create a work order from branch-scoped equipment management number
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWorkOrderRequest'
      responses:
        '201':
          description: Work order created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '408':
          $ref: '#/components/responses/RequestTimeout'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/work-orders/{workOrderId}:
    get:
      tags:
        - work-orders
      operationId: getWorkOrder
      summary: Fetch one work order
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      responses:
        '200':
          description: Work order summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags:
        - work-orders
      operationId: updateWorkOrderIntake
      summary: Edit work-order intake narrative fields
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkOrderIntakeRequest'
      responses:
        '200':
          description: Work order updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /api/work-orders/{workOrderId}/approve:
    post:
      tags:
        - work-orders
      operationId: approveWorkOrder
      summary: Approve the next pending non-mechanic approval step
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApproveWorkOrderRequest'
      responses:
        '200':
          description: Updated work-order approval state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/work-orders/{workOrderId}/assignments:
    put:
      tags:
        - work-orders
      operationId: assignWorkOrder
      summary: Assign mechanics and approval-line approvers
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignWorkOrderRequest'
      responses:
        '200':
          description: Assigned work order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
  /api/work-orders/{workOrderId}/outsource-works:
    post:
      tags:
        - work-orders
      operationId: createOutsourceWork
      summary: Create an outsource work request for a work order
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOutsourceWorkRequest'
      responses:
        '201':
          description: Outsource work created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutsourceWorkSummary'
  /api/work-orders/{workOrderId}/priority:
    patch:
      tags:
        - work-orders
      operationId: updateWorkOrderPriority
      summary: Update work-order priority
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - priority
              properties:
                priority:
                  $ref: '#/components/schemas/PriorityLevel'
      responses:
        '200':
          description: Updated work order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
  /api/work-orders/{workOrderId}/report:
    post:
      tags:
        - work-orders
      operationId: submitWorkOrderReport
      summary: Submit a mechanic work report
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitReportRequest'
      responses:
        '200':
          description: Report-submitted work order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
  /api/work-orders/{workOrderId}/start:
    post:
      tags:
        - work-orders
      operationId: startWorkOrder
      summary: Start assigned work
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      responses:
        '200':
          description: In-progress work order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkOrderSummary'
  /api/work-orders/{workOrderId}/target-change-requests:
    post:
      tags:
        - work-orders
      operationId: requestTargetChange
      summary: Request a target due-date change
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/WorkOrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TargetChangeRequest'
      responses:
        '201':
          description: Target change request created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TargetChangeRequestSummary'
  /healthz:
    get:
      tags:
        - system
      security: []
      operationId: healthz
      summary: Health check
      responses:
        '200':
          description: Service is alive.
  /readyz:
    get:
      tags:
        - system
      security: []
      operationId: readyz
      summary: Readiness check
      responses:
        '200':
          description: Service dependencies are ready.
        '503':
          description: A required dependency is unavailable.
components:
  securitySchemes:
    basicAuth:
      type: http
      scheme: basic
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    officeCallbackToken:
      type: apiKey
      in: query
      name: ct
      description: >-
        HS256 token minted by THIS service, carrying the org and document binding for
        an ONLYOFFICE editing session. Verified with no clock-skew grace, so it is
        mandatory and short-lived. Distinct from bearerAuth: it is a query parameter,
        not a user session.
    officeDocumentServerToken:
      type: http
      scheme: bearer
      description: >-
        JWT signed by ONLYOFFICE DocumentServer, NOT by this service's issuer, so it
        is not bearerAuth. Accepted from the request body `token` field or from this
        header. Required in addition to officeCallbackToken, never instead of it.
  parameters:
    DispatchId:
      name: dispatchId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    EquipmentId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    EquipmentIdV2:
      name: equipmentId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    EquipmentSubstitutionId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    EvaluationCycleId:
      name: cycle_id
      in: path
      required: true
      schema: { $ref: '#/components/schemas/Uuid' }
    EvaluationEmployeeId:
      name: employee_id
      in: path
      required: true
      schema: { $ref: '#/components/schemas/Uuid' }
    EvaluationReviewKindPath:
      name: kind
      in: path
      required: true
      description: Lowercase path representation of a review kind.
      schema: { type: string, enum: [self, manager] }
    EvaluationSubjectId:
      name: subject_id
      in: path
      required: true
      schema: { $ref: '#/components/schemas/Uuid' }
    EvidenceId:
      name: evidenceId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    LifecycleObjectId:
      name: objectId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    LifecycleObjectType:
      name: objectType
      in: path
      required: true
      schema:
        type: string
        pattern: '^[a-z][a-z0-9_]{1,63}$'
    PlanId:
      name: planId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    PurchaseRequestId:
      name: purchaseRequestId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    QuoteId:
      name: quoteId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ReportDate:
      name: date
      in: query
      required: true
      schema:
        $ref: '#/components/schemas/Date'
      description: Report date in YYYY-MM-DD format.
    RequestId:
      name: requestId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SiteId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ThreadId:
      name: threadId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    WorkOrderId:
      name: workOrderId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    XDeviceId:
      name: X-Device-Id
      in: header
      required: true
      schema:
        type: string
        minLength: 1
      description: Client-stable device identifier. The server stores only a SHA-256 hash.
  responses:
    BadGateway:
      description: An upstream dependency (e.g. the email relay) failed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorBody"
    BadRequest:
      description: Request syntax or conditional validator is malformed.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    Conflict:
      description: State conflict or illegal transition.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    Forbidden:
      description: Principal lacks role or branch authority.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    InternalServerError:
      description: Unexpected server-side failure.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    MailUnavailable:
      description: Webmail is not configured on this server (the master key CONSOLE_MAIL_MASTER_KEY is absent), or JWT verification
        is not configured. The app is otherwise healthy.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    NotFound:
      description: Resource was not found in branch scope.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    PayloadTooLarge:
      description: Request body exceeded the configured size limit.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    PreconditionFailed:
      description: The submitted ontology key write validator is stale.
      headers:
        ETag:
          description: Current strong tenant/key write validator.
          schema: { type: string }
        Cache-Control:
          schema: { type: string, enum: [no-store] }
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    PreconditionRequired:
      description: A required passkey step-up or other precondition is missing.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    RequestTimeout:
      description: Request exceeded the server timeout budget.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    ServiceUnavailable:
      description: Required storage or platform dependency is not configured.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorBody"
    TooManyRequests:
      description: Rate limit exceeded for this client; retry later.
      headers:
        Retry-After:
          description: Seconds until the client may retry this endpoint.
          schema:
            type: integer
            minimum: 1
          required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    Unauthorized:
      description: Missing or invalid bearer token.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
    ValidationError:
      description: Request failed validation.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorBody'
  schemas:
    AccountDrillEntry:
      type: object
      required:
      - voucher_id
      - voucher_no
      - status
      - line_id
      - account_code
      - side
      - amount_won
      - entry_at
      properties:
        voucher_id:
          $ref: '#/components/schemas/Uuid'
        voucher_no:
          type: string
        status:
          $ref: '#/components/schemas/VoucherStatus'
        line_id:
          $ref: '#/components/schemas/Uuid'
        account_code:
          type: string
        side:
          $ref: '#/components/schemas/DebitCredit'
        amount_won:
          type: integer
          format: int64
        source_object_type:
          type:
          - string
          - 'null'
        source_object_id:
          type:
          - string
          - 'null'
        entry_at:
          $ref: '#/components/schemas/Timestamp'
    AccountStatus:
      type: string
      description: Derived account-setup state for the console roster. ACTIVE only once the user has enrolled a passkey (can
        sign in); PENDING_SETUP when created / OTP-issued but not yet enrolled; ARCHIVED when soft-disabled.
      enum:
      - ACTIVE
      - PENDING_SETUP
      - ARCHIVED
    AcquisitionBasis:
      type: string
      description: Where the acquisition figure that anchors TCO came from.
      enum:
      - EXPLICIT
      - VEHICLE_VALUE_FALLBACK
      - NONE
    ActingRule:
      type: object
      required: [id, label, kind]
      properties:
        id: { type: string, format: uuid }
        label: { type: string }
        kind: { type: string, enum: [automation, policy] }
    ActionInboxItem:
      type: object
      description: >-
        One unified actionable item. Field names mirror the overview prototype's
        items[] shape. Source-partial fields (site/who/due/submitted) are absent
        when the originating source does not carry them.
      required:
      - id
      - kind
      - urg
      - ref
      - title
      - dueTone
      - links
      - done
      properties:
        id:
          type: string
          description: '"{kind}:{uuid}" — source-namespaced so ids never collide.'
        kind:
          type: string
          enum:
          - approval
          - dispatch
          - work
          - support
        urg:
          type: string
          enum:
          - now
          - today
          - wait
        ref:
          type: string
          description: >-
            Source reference (work order request_no, ticket id, or the workflow
            run/object id). Not a canonical AP-/CS- object code.
        title:
          type: string
        site:
          type: string
        who:
          type: string
        due:
          $ref: '#/components/schemas/Timestamp'
        dueTone:
          type: string
          enum:
          - danger
          - warn
          - neutral
        submitted:
          $ref: '#/components/schemas/Timestamp'
        links:
          type: array
          items:
            $ref: '#/components/schemas/ActionInboxLink'
        done:
          type: boolean
    ActionInboxLink:
      type: object
      description: >-
        One server-ordered canonical source-object reference for an action-inbox
        item. Clients resolve only explicitly registered kind/id pairs; current
        browser-linkable kinds are approval_run, work_order, and support_ticket.
        Unknown kinds remain valid for forward compatibility and MUST stay inert
        until a client registry adds support. Clients MUST NOT accept a server URL
        or infer a destination from the enclosing item kind, item id, label, or a
        code prefix.
      required:
      - kind
      - id
      properties:
        kind:
          type: string
          description: >-
            Canonical source-object kind. Current browser-linkable values are
            approval_run, work_order, and support_ticket; other strings are
            forward-compatible but inert.
        id:
          type: string
          description: >-
            Canonical source-object identifier. Clients trim surrounding
            whitespace and reject a blank result before route construction.
        label:
          type: string
    ActionInboxResponse:
      type: object
      required:
      - items
      - total
      - total_is_exact
      - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ActionInboxItem'
        total:
          type: integer
          minimum: 0
          description: Live visible item count across sources admitted by as_of; inspect total_is_exact before treating it as authoritative.
        total_is_exact:
          type: boolean
          description: False only when a bounded authorization-filtered count reached its scan budget.
        next_cursor:
          type:
          - string
          - 'null'
          description: Opaque cursor for the next immutable-keyset page, or null when exhausted.
    AddCommentRequest:
      type: object
      required:
      - body
      properties:
        body:
          type: string
          minLength: 1
        is_internal_note:
          type: boolean
    AddEvaluationSubjectRequest:
      type: object
      additionalProperties: false
      required: [cycle_id, employee_id, manager_user_id]
      properties:
        cycle_id: { $ref: '#/components/schemas/Uuid' }
        employee_id: { $ref: '#/components/schemas/Uuid' }
        manager_user_id: { $ref: '#/components/schemas/Uuid' }
    AdjustRecruitOfferRequest:
      type: object
      description: The amount_period is inherited from the offer being adjusted and cannot be changed.
      required:
      - amount
      properties:
        amount:
          type: string
          description: Canonical NUMERIC(14,2) decimal string.
        reply_deadline:
          type:
          - string
          - 'null'
          format: date
          description: Omit or null to keep the prior offer's reply deadline.
    AdminCredentialResetRequest:
      type: object
      required:
      - user_id
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
    AdminCredentialResetResponse:
      type: object
      required:
      - user_id
      - otp
      - expires_at
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        otp:
          type: string
        expires_at:
          $ref: '#/components/schemas/Timestamp'
    AdminIssueOtpRequest:
      type: object
      required:
      - user_id
      - branch_id
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        ttl_seconds:
          type: integer
          format: int64
          description: Optional code lifetime in seconds; defaults to 86400 (24 hours).
    AdminIssueOtpResponse:
      type: object
      required:
      - user_id
      - otp
      - expires_at
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        otp:
          type: string
        expires_at:
          $ref: '#/components/schemas/Timestamp'
    AdminWorkflowRunListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowRunListItem'
        next_cursor:
          $ref: '#/components/schemas/Uuid'
    AdmissibilityStatus:
      { type: string, enum: [ADMISSIBLE, REVIEW_NEEDED, BLOCKED, INADMISSIBLE] }
    AdvanceRecruitApplicantRequest:
      type: object
      required:
      - expected_updated_at
      properties:
        expected_updated_at:
          $ref: '#/components/schemas/Timestamp'
    AndroidAssetLinkStatement:
      type: object
      description: A single Android Digital Asset Links statement authorizing the native app's signing keys to provide login
        credentials for the RP domain.
      required:
      - relation
      - target
      properties:
        relation:
          type: array
          items:
            type: string
        target:
          type: object
          required:
          - namespace
          - package_name
          - sha256_cert_fingerprints
          properties:
            namespace:
              type: string
            package_name:
              type: string
            sha256_cert_fingerprints:
              type: array
              items:
                type: string
    AppendManualCostLedgerRequest:
      type: object
      required:
      - branch_id
      - amount_won
      - memo
      - config
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          type:
          - string
          - 'null'
          format: uuid
        amount_won:
          type: integer
          format: int64
          minimum: 1
        memo:
          type: string
          minLength: 1
        config:
          $ref: '#/components/schemas/FinancialConfigSnapshot'
    AppleAppSiteAssociation:
      type: object
      description: Apple App Site Association document authorizing the native iOS app's passkeys for the RP domain.
      required:
      - webcredentials
      properties:
        webcredentials:
          type: object
          required:
          - apps
          properties:
            apps:
              type: array
              description: iOS app identifiers (`<TeamID>.<bundle-id>`).
              items:
                type: string
    ApprovalItem:
      type: object
      description: A typed approval task over an ontology object. The source-specific payloads are mutually exclusive; `source`
        selects which payload is populated.
      required:
      - id
      - source
      - source_id
      - branch_id
      - status
      - title
      - summary
      - requested_at
      - due_at
      - href
      - action_href
      - ontology
      - workflow
      - policy
      properties:
        id:
          type: string
          description: Stable federated id in `{source}:{source_id}` form.
        source:
          type: string
          enum:
          - WORK_ORDER
          - DAILY_PLAN
          - TARGET_CHANGE
        source_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
        title:
          type: string
        summary:
          type: string
        requested_at:
          type:
          - string
          - 'null'
          format: date-time
        due_at:
          type:
          - string
          - 'null'
          format: date-time
        href:
          type: string
        action_href:
          type: string
        ontology:
          $ref: '#/components/schemas/ApprovalOntologyContext'
        workflow:
          $ref: '#/components/schemas/ApprovalWorkflowContext'
        policy:
          $ref: '#/components/schemas/ApprovalPolicyContext'
        work_order:
          $ref: '#/components/schemas/WorkOrderListItem'
          description: Present only when `source` is `WORK_ORDER`.
        daily_plan:
          $ref: '#/components/schemas/DailyPlanSummary'
          description: Present only when `source` is `DAILY_PLAN`.
        target_change:
          $ref: '#/components/schemas/TargetChangeRequestSummary'
          description: Present only when `source` is `TARGET_CHANGE`.
    ApprovalItemSource:
      type: object
      required:
      - key
      - label
      - status
      - count
      properties:
        key:
          type: string
          enum:
          - workOrders
          - dailyPlans
          - targetChanges
        label:
          type: string
        status:
          type: string
          enum:
          - ok
        count:
          type: integer
          format: int64
    ApprovalItemsPage:
      type: object
      required:
      - items
      - sources
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ApprovalItem'
        sources:
          type: array
          items:
            $ref: '#/components/schemas/ApprovalItemSource'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
    ApprovalOntologyContext:
      type: object
      description: Tenant/org/branch/object identity for ontology-aware workflow, analytics, audit, and future group/organization
        hierarchy views.
      required:
      - object_type
      - object_id
      - tenant_id
      - branch_id
      properties:
        object_type:
          type: string
          enum:
          - WORK_ORDER
          - DAILY_PLAN
          - TARGET_CHANGE
        object_id:
          $ref: '#/components/schemas/Uuid'
        tenant_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
    ApprovalPolicyContext:
      type: object
      description: Server policy result and requirements. Clients may display it, but must not treat it as authorization proof;
        source mutation endpoints always re-check PBAC/RBAC/ABAC server-side.
      required:
      - decision
      - enforcement
      - required_features
      - scope_kind
      - scope_id
      properties:
        decision:
          type: string
          enum:
          - ALLOWED
        enforcement:
          type: string
          enum:
          - server
        required_features:
          type: array
          items:
            type: string
        scope_kind:
          type: string
          enum:
          - BRANCH
        scope_id:
          $ref: '#/components/schemas/Uuid'
    ApprovalStepSummary:
      type: object
      required:
      - id
      - step_order
      - role
      - approver_id
      - approver_name
      - status
      - requested_at
      - approved_at
      - approved_by_id
      - approved_by_name
      - decision_comment
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        step_order:
          type: integer
          format: int32
        role:
          type: string
        approver_id:
          type:
          - string
          - 'null'
          format: uuid
        approver_name:
          type:
          - string
          - 'null'
        status:
          type: string
        requested_at:
          type:
          - string
          - 'null'
          format: date-time
        approved_at:
          type:
          - string
          - 'null'
          format: date-time
        approved_by_id:
          type:
          - string
          - 'null'
          format: uuid
        approved_by_name:
          type:
          - string
          - 'null'
        decision_comment:
          type:
          - string
          - 'null'
    ApprovalWorkflowContext:
      type: object
      description: Workflow/action identity for a typed approval task.
      required:
      - workflow_key
      - action_key
      properties:
        workflow_key:
          type: string
          examples:
          - work_order.report_completion_review
          - daily_plan.review
          - work_order.target_change_review
        action_key:
          type: string
          examples:
          - approve_work_order
          - review_daily_plan
          - review_target_change
    ApproveWorkOrderRequest:
      type: object
      required:
      - comment
      properties:
        comment:
          type: string
          minLength: 1
    ArrivalEvent:
      type: object
      required:
      - id
      - work_order_id
      - site_id
      - work_order_no
      - site_name
      - customer_name
      - mechanic_name
      - latitude
      - longitude
      - kind
      - occurred_at
      properties:
        id:
          type: string
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        site_id:
          $ref: '#/components/schemas/Uuid'
        work_order_no:
          type: string
        site_name:
          type: string
        customer_name:
          type: string
        mechanic_name:
          type: string
        latitude:
          type:
          - number
          - 'null'
          format: double
        longitude:
          type:
          - number
          - 'null'
          format: double
        kind:
          type: string
          enum:
          - ARRIVAL
          - DEPARTURE
        occurred_at:
          $ref: '#/components/schemas/Timestamp'
    ArrivalEventPage:
      type: object
      required:
      - items
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ArrivalEvent'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
    AssessRecruitApplicantRequest:
      type: object
      required:
      - score
      properties:
        score:
          $ref: '#/components/schemas/RecruitAssessmentScore'
    AssetLifecycleCostSummary:
      type: object
      description: Per-asset lifecycle / total-cost-of-ownership rollup. outsource_unlinked_won is read-only and never summed
        into tco_won.
      required:
      - equipment_id
      - equipment_no
      - status
      - acquisition_source
      - maintenance_total_won
      - manual_total_won
      - purchase_total_won
      - entry_count
      - residual_value_won
      - tco_won
      - timeline
      properties:
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        equipment_no:
          type: string
        status:
          type: string
        acquisition_cost_won:
          type:
          - integer
          - 'null'
          format: int64
        acquisition_date:
          type:
          - string
          - 'null'
          format: date
        acquisition_source:
          $ref: '#/components/schemas/AcquisitionBasis'
        maintenance_total_won:
          type: integer
          format: int64
        manual_total_won:
          type: integer
          format: int64
        purchase_total_won:
          type: integer
          format: int64
        entry_count:
          type: integer
          format: int64
        outsource_unlinked_won:
          type:
          - integer
          - 'null'
          format: int64
        residual_value_won:
          type: integer
          format: int64
        sale_price_won:
          type:
          - integer
          - 'null'
          format: int64
        sold_at:
          type:
          - string
          - 'null'
          format: date
        gross_margin_won:
          type:
          - integer
          - 'null'
          format: int64
        tco_won:
          type: integer
          format: int64
        cost_per_month_won:
          type:
          - integer
          - 'null'
          format: int64
        cost_per_hour_won:
          type:
          - integer
          - 'null'
          format: int64
        timeline:
          type: array
          items:
            $ref: "#/components/schemas/CostLedgerEntrySummary"
    AssignAttendanceSubstituteRequest:
      type: object
      additionalProperties: false
      required:
        - site
        - role
        - cover_date
        - from_minutes
        - to_minutes
        - covered_employee_id
        - reason_kind
        - worker_employee_id
      properties:
        site:
          type: string
        branch_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        role:
          type: string
        cover_date:
          type: string
          format: date
        from_minutes:
          type: integer
          minimum: 0
          maximum: 1439
        to_minutes:
          type: integer
          minimum: 1
          maximum: 1440
        covered_employee_id:
          $ref: '#/components/schemas/Uuid'
        reason_kind:
          type: string
        reason_detail:
          type:
            - string
            - 'null'
        worker_employee_id:
          $ref: '#/components/schemas/Uuid'
        exception_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
    AssignSubstituteRequest:
      type: object
      required:
      - source_equipment_id
      - substitute_equipment_id
      - assignment_location
      properties:
        source_equipment_id:
          type: string
          format: uuid
        substitute_equipment_id:
          type: string
          format: uuid
        assigned_to:
          type:
          - string
          - 'null'
          format: uuid
        assignment_location:
          type: string
          minLength: 1
    AssignTicketRequest:
      type: object
      required:
      - assignee_user_id
      properties:
        assignee_user_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
    AssignWorkOrderRequest:
      type: object
      required:
      - assignments
      properties:
        assignments:
          type: array
          minItems: 1
          items:
            type: object
            required:
            - mechanic_id
            - role
            properties:
              mechanic_id:
                $ref: '#/components/schemas/Uuid'
              role:
                $ref: '#/components/schemas/AssignmentRole'
        admin_approver_id:
          $ref: '#/components/schemas/Uuid'
        executive_approver_id:
          $ref: '#/components/schemas/Uuid'
    AssignmentRole:
      type: string
      enum:
      - PRIMARY
      - SECONDARY
    AssignmentSummary:
      type: object
      required:
      - id
      - mechanic_id
      - mechanic_name
      - role
      - assigned_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        mechanic_id:
          $ref: '#/components/schemas/Uuid'
        mechanic_name:
          type: string
        role:
          $ref: '#/components/schemas/AssignmentRole'
        assigned_at:
          $ref: '#/components/schemas/Timestamp'
    AttachInstanceAck:
      type: object
      required:
      - attached
      properties:
        attached:
          type: boolean
    AttachInstanceRequest:
      type: object
      description: Attach an instance (kind, id) to a series.
      required:
      - kind
      - id
      properties:
        kind:
          type: string
          description: Instance object kind slug.
        id:
          type: string
          maxLength: 200
          description: Instance object id/reference (≤200 chars).
    AttachmentStage:
      type: string
      enum:
      - REQUEST
      - BEFORE
      - DURING
      - AFTER
      - REPORT
      - OUTSOURCE_RESULT
    AttendanceCloseAmendment:
      type: object
      required: [id, reason, actor, created_at]
      properties: { id: { $ref: '#/components/schemas/Uuid' }, reason: { type: string }, actor: { $ref: '#/components/schemas/Uuid' }, created_at: { type: string, format: date-time } }
    AttendanceCloseAmendmentRequest:
      type: object
      additionalProperties: false
      required:
        - reason
        - detail
      properties:
        reason:
          type: string
        detail:
          type: string
        ref:
          type:
            - string
            - 'null'
    AttendanceCloseBoard:
      type: object
      required: [month, items]
      properties: { month: { type: string, pattern: '^\\d{4}-\\d{2}$' }, items: { type: array, items: { $ref: '#/components/schemas/AttendanceMonthCloseItem' } } }
    AttendanceCloseCheck:
      type: object
      required:
        - key
        - ok
      properties:
        key:
          type: string
        ok:
          type: boolean
        warn:
          type: boolean
        note:
          type:
            - string
            - 'null'
    AttendanceClosePreflight:
      type: object
      required: [month, branch_scope, checks, can_close]
      properties: { month: { type: string }, branch_scope: { type: string }, checks: { type: array, items: { $ref: '#/components/schemas/AttendanceCloseCheck' } }, can_close: { type: boolean } }
    AttendanceCloseRequest:
      type: object
      additionalProperties: false
      required:
        - month
      properties:
        month:
          type: string
          pattern: ^\\d{4}-\\d{2}$
        branch_scope:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        attest:
          type:
            - boolean
            - 'null'
    AttendanceException:
      type: object
      required:
        - id
        - code
        - kind
        - status
        - employee_id
        - employee_name
        - work_date
        - occurred_at
        - detail
        - evidence
        - links
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        kind:
          type: string
          enum:
            - LATE
            - NO_SHOW
            - UNAPPROVED_OVERTIME
            - EARLY_LEAVE
        status:
          type: string
          enum:
            - OPEN
            - RESOLVED
        employee_id:
          $ref: '#/components/schemas/Uuid'
        employee_name:
          type: string
        team:
          type:
            - string
            - 'null'
        branch_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        work_date:
          type: string
          format: date
        occurred_at:
          type: string
          format: date-time
        detail:
          type: string
        evidence:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceExceptionEvidence'
        links:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceExceptionLink'
        resolution:
          oneOf:
            - {$ref: '#/components/schemas/AttendanceExceptionResolution'}
            - {type: 'null'}
        created_at:
          type: string
          format: date-time
    AttendanceExceptionEvidence:
      type: object
      required:
        - name
      properties:
        name:
          type: string
        size:
          type:
            - string
            - 'null'
    AttendanceExceptionLink:
      type: object
      required:
        - kind
        - label
      properties:
        kind:
          type: string
        label:
          type: string
        ref:
          type:
            - string
            - 'null'
    AttendanceExceptionPage:
      type: object
      required: [items, total, limit, offset]
      properties: { items: { type: array, items: { $ref: '#/components/schemas/AttendanceException' } }, total: { type: integer, format: int64 }, limit: { type: integer, format: int64 }, offset: { type: integer, format: int64 } }
    AttendanceExceptionResolution:
      type: object
      required:
        - action
        - reason
        - actor
        - resolved_at
      properties:
        action:
          type: string
        reason:
          type: string
        linked_work_ref:
          type:
            - string
            - 'null'
        ot_hours:
          type:
            - number
            - 'null'
        actor:
          $ref: '#/components/schemas/Uuid'
        resolved_at:
          type: string
          format: date-time
    AttendanceImportApplyReport:
      type: object
      required:
      - run_id
      - inserted
      - skipped
      - error_rows
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        inserted:
          type: integer
          minimum: 0
        skipped:
          type: integer
          minimum: 0
        error_rows:
          type: integer
          minimum: 0
    AttendanceImportColumn:
      type: object
      required:
        - source_header
        - normalized_header
        - classification
        - preview_allowed
      properties:
        source_header:
          type: string
        normalized_header:
          type: string
        target:
          oneOf:
            - {type: string, enum: [employee_number, employee_name, branch_name, work_date, check_in_at, check_out_at, minutes_worked]}
            - {type: 'null'}
        classification:
          type: string
          enum:
            - canonical
            - retained
            - restricted
        preview_allowed:
          type: boolean
    AttendanceImportDryRunSummary:
      type: object
      required:
      - run_id
      - input_rows
      - candidate_rows
      - preserved_rows
      - ready_rows
      - error_rows
      - duplicate_rows
      - missing_employee_rows
      - ambiguous_employee_rows
      - row_errors
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        input_rows:
          type: integer
          minimum: 0
        candidate_rows:
          type: integer
          minimum: 0
        preserved_rows:
          type: integer
          minimum: 0
        ready_rows:
          type: integer
          minimum: 0
        error_rows:
          type: integer
          minimum: 0
        duplicate_rows:
          type: integer
          minimum: 0
        missing_employee_rows:
          type: integer
          minimum: 0
        ambiguous_employee_rows:
          type: integer
          minimum: 0
        row_errors:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceImportRowError'
    AttendanceImportPreviewResponse:
      type: object
      required:
      - run_id
      - entity_type
      - source_filename
      - source_sha256
      - input_rows
      - candidate_rows
      - preserved_rows
      - columns
      - sample_rows
      - mapping_profile
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        entity_type:
          type: string
          enum:
          - attendance_direct
        source_filename:
          type: string
        source_sha256:
          type: string
          pattern: ^[a-f0-9]{64}$
        input_rows:
          type: integer
          minimum: 0
        candidate_rows:
          type: integer
          minimum: 0
        preserved_rows:
          type: integer
          minimum: 0
        columns:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceImportColumn'
        sample_rows:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceImportPreviewRow'
        mapping_profile:
          type: object
          additionalProperties: true
    AttendanceImportPreviewRow:
      type: object
      required:
      - source_sheet
      - source_row
      - row_status
      - values
      - validation
      properties:
        source_sheet:
          type: string
        source_row:
          type: integer
          format: int32
          minimum: 1
        row_status:
          type: string
          enum:
          - CANDIDATE
          - PRESERVED
          - ERROR
        values:
          type: object
          additionalProperties: true
        validation:
          type: object
          additionalProperties: true
    AttendanceImportRowError:
      type: object
      required:
      - source_sheet
      - source_row
      - source_key
      - code
      - message
      properties:
        source_sheet:
          type: string
        source_row:
          type: integer
          format: int32
          minimum: 1
        source_key:
          type: string
        code:
          type: string
        message:
          type: string
    AttendanceImportSummaryItem:
      type: object
      required:
        - run_id
        - status
        - source_filename
        - source_format
        - source_sha256
        - input_rows
        - candidate_rows
        - preserved_rows
        - dry_run_summary
        - apply_summary
        - created_at
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
            - PREVIEWED
            - DRY_RUN
            - APPLIED
            - FAILED
        source_filename:
          type: string
        source_format:
          type: string
          enum:
            - xlsx
            - csv
        source_sha256:
          type: string
          pattern: ^[a-f0-9]{64}$
        input_rows:
          type: integer
          format: int32
          minimum: 0
        candidate_rows:
          type: integer
          format: int32
          minimum: 0
        preserved_rows:
          type: integer
          format: int32
          minimum: 0
        dry_run_summary:
          type: object
          additionalProperties: true
        apply_summary:
          type: object
          additionalProperties: true
        created_at:
          $ref: '#/components/schemas/Timestamp'
        applied_at:
          oneOf:
            - {$ref: '#/components/schemas/Timestamp'}
            - {type: 'null'}
    AttendanceImportSummaryPage:
      type: object
      required:
      - items
      - total
      - limit
      - offset
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceImportSummaryItem'
        total:
          type: integer
          format: int64
          minimum: 0
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
    AttendanceMonthClose:
      type: object
      required:
        - id
        - month
        - branch_scope
        - checks
        - attested_by
        - attested_at
        - closed_at
        - amendments
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        month:
          type: string
          pattern: ^\\d{4}-\\d{2}$
        branch_scope:
          type: string
        checks:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceCloseCheck'
        attested_by:
          $ref: '#/components/schemas/Uuid'
        attested_at:
          type: string
          format: date-time
        period_lock_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        closed_at:
          type: string
          format: date-time
        amendments:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceCloseAmendment'
    AttendanceMonthCloseItem:
      type: object
      required:
        - branch_scope
        - closed
        - open_exceptions
        - pending_leave
      properties:
        branch_scope:
          type: string
        closed:
          type: boolean
        close:
          oneOf:
            - {$ref: '#/components/schemas/AttendanceMonthClose'}
            - {type: 'null'}
        open_exceptions:
          type: integer
          format: int64
        pending_leave:
          type: integer
          format: int64
    AttendanceRecordKind:
      type: string
      enum:
      - CLOCK_IN
      - OUT_FOR_WORK
      - BUSINESS_TRIP
      - RETURNED
      - CLOCK_OUT
    AttendanceSubstitution:
      type: object
      required:
        - id
        - site
        - role
        - cover_date
        - from_minutes
        - to_minutes
        - covered_employee_id
        - covered_name
        - reason_kind
        - worker_name
        - worker_type
        - status
        - created_by
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        site:
          type: string
        branch_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        role:
          type: string
        cover_date:
          type: string
          format: date
        from_minutes:
          type: integer
        to_minutes:
          type: integer
        covered_employee_id:
          $ref: '#/components/schemas/Uuid'
        covered_name:
          type: string
        reason_kind:
          type: string
        reason_detail:
          type:
            - string
            - 'null'
        worker_employee_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        worker_name:
          type: string
        worker_type:
          type: string
        worker_rate:
          type:
            - string
            - 'null'
        status:
          type: string
          enum:
            - ASSIGNED
            - CANCELLED
        exception_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        created_by:
          $ref: '#/components/schemas/Uuid'
        created_at:
          type: string
          format: date-time
    AttendanceSubstitutionCandidate:
      type: object
      required: [employee_id, employee_name, branch_id]
      properties: { employee_id: { $ref: '#/components/schemas/Uuid' }, employee_name: { type: string }, branch_id: { $ref: '#/components/schemas/Uuid' } }
    AttendanceSubstitutionCandidatePage:
      type: object
      required: [items, total, limit, offset]
      properties: { items: { type: array, items: { $ref: '#/components/schemas/AttendanceSubstitutionCandidate' } }, total: { type: integer, format: int64 }, limit: { type: integer, format: int64 }, offset: { type: integer, format: int64 } }
    AttendanceSubstitutionPage:
      type: object
      required: [items, total, limit, offset]
      properties: { items: { type: array, items: { $ref: '#/components/schemas/AttendanceSubstitution' } }, total: { type: integer, format: int64 }, limit: { type: integer, format: int64 }, offset: { type: integer, format: int64 } }
    AttendanceSummaryItem:
      type: object
      required:
        - user_id
        - display_name
        - arrivals
        - departures
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type: string
        arrivals:
          type: integer
          format: int64
          minimum: 0
        departures:
          type: integer
          format: int64
          minimum: 0
        last_kind:
          type:
            - string
            - 'null'
        last_event_at:
          oneOf:
            - {$ref: '#/components/schemas/Timestamp'}
            - {type: 'null'}
    AttendanceSummaryPage:
      type: object
      required:
      - items
      - total
      - limit
      - offset
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceSummaryItem'
        total:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
    AttendanceWeek52AckRequest:
      type: object
      additionalProperties: false
      required: [employee_id, week_start]
      properties: { employee_id: { $ref: '#/components/schemas/Uuid' }, week_start: { type: string, format: date } }
    AttendanceWeek52Board:
      type: object
      required: [week_start, items]
      properties: { week_start: { type: string, format: date }, items: { type: array, items: { $ref: '#/components/schemas/AttendanceWeek52Row' } } }
    AttendanceWeek52Row:
      type: object
      required:
        - employee_id
        - name
        - week_start
        - current_hours
        - projected_hours
        - tone
        - acked
      properties:
        employee_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        team:
          type:
            - string
            - 'null'
        week_start:
          type: string
          format: date
        current_hours:
          type: number
        projected_hours:
          type: number
        tone:
          type: string
          enum:
            - OK
            - WARN
            - DANGER
        acked:
          type: boolean
        acked_at:
          type:
            - string
            - 'null'
          format: date-time
    AttestPayrollDisbursementRequest:
      type: object
      required:
      - status
      properties:
        status:
          type: string
          enum: [SCHEDULED, SUBMITTED_TO_BANK, PAID, FAILED]
        reason:
          type: string
          description: Required for FAILED (a blank reason is rejected with 422).
    AuditChainAttestation:
      type: object
      required:
        - org_id
        - ok
        - kind
        - unsealed_tail
      properties:
        org_id:
          type: string
          format: uuid
        ok:
          type: boolean
          description: No tamper detected. Independent of unsealed_tail.
        first_bad_seq:
          type:
            - integer
            - 'null'
          format: int64
          description: The first offending seal's seq, when the failure localizes to one seal.
        kind:
          type: string
          enum:
            - ok
            - seal_hash_mismatch
            - batch_hash_mismatch
            - broken_continuity
            - bad_signature
            - missing_seq
            - coverage_gap
            - corrupt_seal
          description: Tamper classification. `ok` means every seal recomputes, chains, verifies, and covers its range with no gap.
        unsealed_tail:
          type: boolean
          description: >-
            Freshness signal — committed rows older than the seal watermark exist beyond the head seal's cursor (the worker
            fell behind or is stopped). Never forces ok=false on a healthy live chain.
    AuditStreamPage:
      type: object
      required:
      - items
      - limit
      - offset
      - total
      - stream_key
      - read_kind
      - access_audit_id
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditStreamRecord'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
        stream_key:
          type: string
          enum:
          - ceo_covert_audit
        read_kind:
          $ref: '#/components/schemas/AuditStreamReadKind'
        access_audit_id:
          type: string
    AuditStreamReadKind:
      type: string
      enum:
      - events
      - access_events
    AuditStreamRecord:
      type: object
      required:
      - id
      - actor
      - action
      - target_type
      - target_id
      - sensitivity
      - before_snap
      - after_snap
      - trace_id
      - span_id
      - occurred_at
      - created_at
      properties:
        id:
          type: string
        actor:
          type:
          - string
          - 'null'
          format: uuid
        action:
          type: string
        target_type:
          type: string
        target_id:
          type: string
        sensitivity:
          type: string
        before_snap:
          type:
          - object
          - 'null'
          additionalProperties: true
        after_snap:
          type:
          - object
          - 'null'
          additionalProperties: true
        trace_id:
          type: string
        span_id:
          type: string
        occurred_at:
          $ref: '#/components/schemas/Timestamp'
        created_at:
          $ref: '#/components/schemas/Timestamp'
    BenefitCatalogCondition:
      type: object
      additionalProperties: false
      required:
        - condition_kind
        - operator
        - condition_key
        - condition_value
        - display_label
        - display_order
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        benefit_id:
          $ref: '#/components/schemas/Uuid'
        condition_kind:
          type: string
        operator:
          type: string
        condition_key:
          type: string
        condition_value:
          type: object
          additionalProperties: true
        display_label:
          type: string
        cedar_policy_ref:
          type:
            - string
            - 'null'
        display_order:
          type: integer
    BenefitCatalogCreateRequest:
      type: object
      description: Tenant and actor are derived from the verified bearer token; neither can be supplied by callers.
      additionalProperties: false
      required:
        - scope
        - category
        - name
        - coverageLabel
        - costLabel
      properties:
        scope:
          $ref: '#/components/schemas/BenefitCatalogScope'
        category:
          type: string
          enum:
            - legal
            - extra
        name:
          type: string
          minLength: 1
        coverageLabel:
          type: string
          minLength: 1
        coveredCount:
          type:
            - integer
            - 'null'
        costLabel:
          type: string
          minLength: 1
        estimatedAnnualCostWon:
          type:
            - integer
            - 'null'
          format: int64
        employerRateBps:
          type:
            - integer
            - 'null'
        note:
          type:
            - string
            - 'null'
        legalBasis:
          type:
            - string
            - 'null'
        relatedDomain:
          type:
            - string
            - 'null'
        relatedObjectId:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        effectiveOn:
          type:
            - string
            - 'null'
          format: date
        retiresOn:
          type:
            - string
            - 'null'
          format: date
        displayOrder:
          type: integer
          default: 0
        metadata:
          type: object
          additionalProperties: true
        tiers:
          type: array
          items:
            $ref: '#/components/schemas/BenefitCatalogTier'
        conditions:
          type: array
          items:
            $ref: '#/components/schemas/BenefitCatalogCondition'
    BenefitCatalogItem:
      type: object
      required:
        - id
        - benefit_code
        - category
        - name
        - scope
        - coverage_label
        - cost_label
        - tiers
        - conditions
        - lifecycle
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        benefit_code:
          type: string
        category:
          type: string
          enum:
            - legal
            - extra
        name:
          type: string
        scope:
          $ref: '#/components/schemas/BenefitCatalogScope'
        coverage_label:
          type: string
        covered_count:
          type:
            - integer
            - 'null'
        cost_label:
          type: string
        estimated_annual_cost_won:
          type:
            - integer
            - 'null'
          format: int64
        employer_rate_bps:
          type:
            - integer
            - 'null'
        note:
          type:
            - string
            - 'null'
        legal_basis:
          type:
            - string
            - 'null'
        related_domain:
          type:
            - string
            - 'null'
        related_object_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        effective_on:
          type:
            - string
            - 'null'
          format: date
        retires_on:
          type:
            - string
            - 'null'
          format: date
        tiers:
          type: array
          items:
            $ref: '#/components/schemas/BenefitCatalogTier'
        conditions:
          type: array
          items:
            $ref: '#/components/schemas/BenefitCatalogCondition'
        lifecycle:
          $ref: '#/components/schemas/BenefitCatalogLifecycleBinding'
    BenefitCatalogItemPage:
      type: object
      required: [items, limit, offset, total]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/BenefitCatalogItem' } }
        limit: { type: integer }
        offset: { type: integer }
        total: { type: integer }
    BenefitCatalogLifecycleBinding:
      type: object
      additionalProperties: false
      required:
        - object_type
        - object_id
      properties:
        object_type:
          type: string
          enum:
            - benefit_catalog_item
        object_id:
          $ref: '#/components/schemas/Uuid'
        current_state:
          type:
            - string
            - 'null'
        legal_hold:
          type:
            - boolean
            - 'null'
        retention_until:
          type:
            - string
            - 'null'
          format: date-time
    BenefitCatalogReplaceConditionsRequest:
      type: object
      additionalProperties: false
      required: [conditions]
      properties:
        conditions: { type: array, items: { $ref: '#/components/schemas/BenefitCatalogCondition' } }
    BenefitCatalogReplaceTiersRequest:
      type: object
      additionalProperties: false
      required: [tiers]
      properties:
        tiers: { type: array, items: { $ref: '#/components/schemas/BenefitCatalogTier' } }
    BenefitCatalogScope:
      type: object
      additionalProperties: false
      required:
        - scope_type
      properties:
        scope_type:
          type: string
          enum:
            - ORG
            - BRANCH
            - SITE
            - TEAM
            - ROLE
            - EMPLOYEE_SEGMENT
        scope_ref:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        branch_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        site_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
    BenefitCatalogTier:
      type: object
      additionalProperties: false
      required:
        - tier_basis
        - tier_key
        - value_label
        - criteria
        - display_order
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        benefit_id:
          $ref: '#/components/schemas/Uuid'
        tier_basis:
          type: string
        tier_key:
          type: string
        value_label:
          type: string
        amount_won:
          type:
            - integer
            - 'null'
          format: int64
        limit_period:
          type:
            - string
            - 'null'
        criteria:
          type: object
          additionalProperties: true
        display_order:
          type: integer
    BenefitCatalogUpdateRequest:
      type: object
      description: Mutable catalog fields only; tenant, actor, identifiers, and lifecycle state are server-owned.
      additionalProperties: false
      properties:
        category:
          type: string
          enum:
            - legal
            - extra
        name:
          type: string
          minLength: 1
        scope:
          $ref: '#/components/schemas/BenefitCatalogScope'
        coverageLabel:
          type: string
        coveredCount:
          type:
            - integer
            - 'null'
        costLabel:
          type: string
        estimatedAnnualCostWon:
          type:
            - integer
            - 'null'
          format: int64
        employerRateBps:
          type:
            - integer
            - 'null'
        note:
          type:
            - string
            - 'null'
        legalBasis:
          type:
            - string
            - 'null'
        relatedDomain:
          type:
            - string
            - 'null'
        relatedObjectId:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        effectiveOn:
          type:
            - string
            - 'null'
          format: date
        retiresOn:
          type:
            - string
            - 'null'
          format: date
        displayOrder:
          type: integer
        metadata:
          type: object
          additionalProperties: true
    BranchScope:
      type: object
      description: >-
        The set of branches a principal may act within. `all` (SUPER_ADMIN /
        EXECUTIVE rollup) carries no `branches`; `branches` carries the explicit
        set.
      properties:
        kind:
          type: string
          enum:
          - all
          - branches
        branches:
          type: array
          description: Present only when `kind = branches`.
          items:
            $ref: '#/components/schemas/Uuid'
      required:
      - kind
    BranchSummary:
      type: object
      required:
      - id
      - region_id
      - name
      - deactivated_at
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        region_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        deactivated_at:
          type:
          - string
          - 'null'
          format: date-time
          description: Set when the branch has been soft-deleted (deactivated); null for an active branch. Active-only listings
            omit deactivated rows.
        created_at:
          $ref: '#/components/schemas/Timestamp'
    BulkApprovalCapability:
      type: object
      required:
      - decidable
      properties:
        decidable:
          type: boolean
        reason:
          type: string
          enum:
          - CLAIMED_BY_ANOTHER_USER
    BulkApprovalInboxResponse:
      type: object
      required:
      - items
      - has_more
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BulkApprovalTask'
        has_more:
          type: boolean
        next_cursor:
          type: string
    BulkApprovalTask:
      type: object
      required:
      - task_id
      - run_id
      - waiting_key
      - title
      - status
      - bulk_decision
      properties:
        task_id:
          $ref: '#/components/schemas/Uuid'
        run_id:
          $ref: '#/components/schemas/Uuid'
        waiting_key:
          type: string
        title:
          type: string
        assignee_role_key:
          type: string
        status:
          type: string
        claimed_by:
          $ref: '#/components/schemas/Uuid'
        due_at:
          $ref: '#/components/schemas/Timestamp'
        bulk_decision:
          $ref: '#/components/schemas/BulkApprovalCapability'
    BulkAuthorizeBody:
      type: object
      required:
        - subject
        - checks
      properties:
        subject:
          $ref: '#/components/schemas/PolicySimSubject'
        checks:
          type: array
          items:
            type: object
            required: [action, resource]
            properties: {action: {type: string}, resource: {$ref: '#/components/schemas/PolicySimResource'}, purpose: {type: [string, 'null']}, field: {type: [string, 'null']}}
    BulkDecisionResponse:
      type: object
      required: [decisions]
      properties:
        decisions: { type: array, items: { $ref: '#/components/schemas/SimulationOutcome' } }
    CalendarEventListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CalendarEventResponse'
    CalendarEventResponse:
      type: object
      required:
      - id
      - scope_type
      - title
      - description
      - starts_at
      - ends_at
      - all_day
      - status
      - created_at
      - updated_at
      - policy
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        scope_type:
          $ref: '#/components/schemas/CollaborationScopeType'
        scope_ref:
          type:
          - string
          - 'null'
        title:
          type: string
        description:
          type: string
        starts_at:
          $ref: '#/components/schemas/Timestamp'
        ends_at:
          $ref: '#/components/schemas/Timestamp'
        all_day:
          type: boolean
        status:
          $ref: '#/components/schemas/CalendarEventStatus'
        object_type:
          type:
          - string
          - 'null'
        object_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        created_by:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        policy:
          $ref: '#/components/schemas/CollaborationScopePolicy'
    CalendarEventStatus:
      type: string
      enum:
      - ACTIVE
      - CANCELLED
    CalibrateEvaluationSubjectRequest:
      type: object
      additionalProperties: false
      required: [final_grade]
      properties:
        final_grade: { $ref: '#/components/schemas/EvaluationGrade' }
        reason: { type: [string, 'null'], maxLength: 500 }
    CancelAttendanceSubstitutionRequest:
      type: object
      additionalProperties: false
      required: [reason]
      properties: { reason: { type: string, minLength: 1 } }
    CancelOrgChangeRequest:
      type: object
      additionalProperties: false
      required:
      - reason
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 4000
    ClaimWorkflowTaskRequest:
      type: object
      required:
      - idempotency_key
      properties:
        idempotency_key:
          type: string
          minLength: 16
    ClaimWorkflowTaskResponse:
      type: object
      required:
      - task
      properties:
        task:
          $ref: '#/components/schemas/ClaimedWorkflowTask'
    ClaimedWorkflowTask:
      type: object
      required:
      - task_id
      - run_id
      - status
      properties:
        task_id:
          $ref: '#/components/schemas/Uuid'
        run_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
        claimed_by:
          $ref: '#/components/schemas/Uuid'
        claimed_at:
          $ref: '#/components/schemas/Timestamp'
    CloneWorkflowDefinitionRequest:
      type: object
      properties:
        workflow_key:
          type: string
        display_name:
          type: string
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    ClosePayrollAttendanceRequest:
      type: object
      required:
      - attest
      properties:
        attest:
          type: boolean
          description: Must be true; the close is an attestation, and false is rejected with 422.
    CloseRecruitPostingRequest:
      type: object
      required:
      - expected_updated_at
      properties:
        expected_updated_at:
          $ref: '#/components/schemas/Timestamp'
    CollaborationScopePolicy:
      type: object
      required:
      - enforcement
      - scope_type
      - visibility
      properties:
        enforcement:
          type: string
          enum:
          - server
        scope_type:
          $ref: '#/components/schemas/CollaborationScopeType'
        scope_ref:
          type:
          - string
          - 'null'
        visibility:
          type: string
          enum:
          - org_members
          - creator_only
          - department_target
          - team_target
    CollaborationScopeType:
      type: string
      enum:
      - TENANT
      - ORG
      - DEPARTMENT
      - TEAM
      - PERSONAL
    Company:
      type: object
      description: "Canonical Company head for one tenant. The tenant IS the company (`org_id`). GET /api/v1/companies/{id} returns this current head (path `id` is `org_id`). GET /api/v1/companies returns zero or one current head for the armed tenant (the same row `list` already returns). as_of / from / to are omitted: `company_revisions` has no valid-time columns, and using `created_at` as `valid_from` would be a second time model. `legal_name` and `reg_no` are read from the latest `company_revisions.attributes`; they are never copied from provisioning-owned `organizations.name`. Write preflight requires a non-empty string `legal_name`; an omitted key is JSON null on the Head. `founded_on` and `registration_number` are company_conformance fixture keys, not this port. `org_id` is the RLS cell, not a Palantir link. Temporal slices other than revision `version` remain HOLD."
      required:
        - org_id
        - version
        - legal_name
        - reg_no
      properties:
        org_id:
          $ref: "#/components/schemas/Uuid"
        legal_name:
          type:
            - string
            - "null"
          description: Title property. Required non-empty on write; omitted key is null on read.
        reg_no:
          type:
            - string
            - "null"
          description: Optional `reg_no` on the latest revision. Not `registration_number`.
        version:
          type: integer
          format: int64
          minimum: 1
          description: Optimistic concurrency token for this Head. Mutating roster actions send it as OntologyActionRequest.expected_revision (JSON body, not HTTP If-Match).
      links: []
      actions:
        - action_key: company.revise
          object_key: company
          input:
            $ref: "#/components/schemas/CompanyReviseInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: natural_person
          edits:
            - company_revisions
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
      concurrency:
        get_token: version
        write_field: expected_revision
        write_in: body
      permissions:
        - employee_directory_read
    CompanyReviseInput:
      type: object
      additionalProperties: false
      description: Typed params for `company.revise` (`CompanyQuery`). The tenant is `org_id` on the command envelope, not this bag. Preflight requires non-empty `legal_name`.
      required:
        - attributes
      properties:
        attributes:
          type: object
          required:
            - legal_name
          properties:
            legal_name:
              type: string
              minLength: 1
            reg_no:
              type:
                - string
                - "null"
          additionalProperties: true
    CompleteInspectionRoundRequest:
      type: object
      required:
      - outcome
      - findings
      properties:
        outcome:
          $ref: '#/components/schemas/InspectionRoundOutcome'
        completed_at:
          type:
          - string
          - 'null'
          format: date-time
        findings:
          type: string
          minLength: 1
        note:
          type:
          - string
          - 'null'
    CompleteOrgChangeSettlementItemRequest:
      type: object
      additionalProperties: false
      properties:
        memo:
          type: string
          minLength: 1
          maxLength: 2000
    ComplianceControl:
      type: object
      required: [id, framework_id, control_key, title, objective, control_type, status, evidence_requirements, created_by, updated_by, created_at, updated_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        framework_id: { $ref: '#/components/schemas/Uuid' }
        control_key: { type: string }
        title: { type: string }
        objective: { type: string }
        control_type: { $ref: '#/components/schemas/ControlType' }
        cadence: { type: [string, 'null'], enum: [CONTINUOUS, DAILY, WEEKLY, MONTHLY, QUARTERLY, ANNUAL, EVENT_DRIVEN, null] }
        status: { $ref: '#/components/schemas/ControlStatus' }
        evidence_requirements: { type: object, additionalProperties: true }
        owner_user_id: { type: [string, 'null'], format: uuid }
        created_by: { $ref: '#/components/schemas/Uuid' }
        updated_by: { $ref: '#/components/schemas/Uuid' }
        created_at: { $ref: '#/components/schemas/Timestamp' }
        updated_at: { $ref: '#/components/schemas/Timestamp' }
    ComplianceControlPage:
      type: object
      required: [items, limit, offset, total]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/ComplianceControl' } }
        limit: { type: integer, format: int64 }
        offset: { type: integer, format: int64 }
        total: { type: integer, format: int64 }
    ComplianceFramework:
      type: object
      required: [id, code, name, version_label, framework_kind, status, metadata, created_by, updated_by, created_at, updated_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        code: { type: string }
        name: { type: string }
        version_label: { type: string }
        framework_kind: { $ref: '#/components/schemas/FrameworkKind' }
        status: { $ref: '#/components/schemas/FrameworkStatus' }
        owner_user_id: { type: [string, 'null'], format: uuid }
        effective_from: { type: [string, 'null'], format: date }
        effective_to: { type: [string, 'null'], format: date }
        metadata: { type: object, additionalProperties: true }
        created_by: { $ref: '#/components/schemas/Uuid' }
        updated_by: { $ref: '#/components/schemas/Uuid' }
        created_at: { $ref: '#/components/schemas/Timestamp' }
        updated_at: { $ref: '#/components/schemas/Timestamp' }
    ComplianceFrameworkPage:
      type: object
      required: [items, limit, offset, total]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/ComplianceFramework' } }
        limit: { type: integer, format: int64 }
        offset: { type: integer, format: int64 }
        total: { type: integer, format: int64 }
    ComplianceObligation:
      type: object
      required: [id, code, title, description, obligation_type, scope, severity, status, metadata, created_by, updated_by, created_at, updated_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        code: { type: string }
        title: { type: string }
        description: { type: string }
        obligation_type: { $ref: '#/components/schemas/ObligationType' }
        scope: { $ref: '#/components/schemas/ComplianceScope' }
        owner_user_id: { type: [string, 'null'], format: uuid }
        severity: { $ref: '#/components/schemas/ComplianceRiskLevel' }
        status: { $ref: '#/components/schemas/ObligationStatus' }
        effective_from: { type: [string, 'null'], format: date }
        effective_to: { type: [string, 'null'], format: date }
        review_cadence: { type: [string, 'null'], enum: [MONTHLY, QUARTERLY, SEMI_ANNUAL, ANNUAL, EVENT_DRIVEN, null] }
        next_review_on: { type: [string, 'null'], format: date }
        metadata: { type: object, additionalProperties: true }
        created_by: { $ref: '#/components/schemas/Uuid' }
        updated_by: { $ref: '#/components/schemas/Uuid' }
        created_at: { $ref: '#/components/schemas/Timestamp' }
        updated_at: { $ref: '#/components/schemas/Timestamp' }
    ComplianceObligationPage:
      type: object
      required: [items, limit, offset, total]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/ComplianceObligation' } }
        limit: { type: integer, format: int64 }
        offset: { type: integer, format: int64 }
        total: { type: integer, format: int64 }
    ComplianceRiskLevel:
      type: string
      enum: [INFO, LOW, MEDIUM, HIGH, CRITICAL]
    ComplianceScope:
      type: object
      required: [kind]
      properties:
        kind: { $ref: '#/components/schemas/ComplianceScopeKind' }
        scope_ref: { type: [string, 'null'], format: uuid }
        branch_id: { type: [string, 'null'], format: uuid }
        site_id: { type: [string, 'null'], format: uuid }
      description: ORG carries no IDs; BRANCH requires matching branch_id and scope_ref; SITE requires branch_id and matching site_id/scope_ref. TEAM and ROLE are not accepted for writes yet.
    ComplianceScopeKind:
      type: string
      enum: [ORG, BRANCH, SITE, TEAM, ROLE]
    ComputeRentalQuoteRequest:
      type: object
      required:
      - branch_id
      - acquisition_value_won
      - current_residual_value_won
      - cumulative_repair_cost_won
      - config
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        acquisition_value_won:
          type: integer
          format: int64
          minimum: 0
        current_residual_value_won:
          type: integer
          format: int64
        cumulative_repair_cost_won:
          type: integer
          format: int64
          minimum: 0
        config:
          $ref: '#/components/schemas/FinancialConfigSnapshot'
    ComputedRentalQuote:
      type: object
      required:
      - effective_residual_value
      - residual_was_floored
      - lines
      - monthly_total
      properties:
        effective_residual_value:
          type: integer
          format: int64
        residual_was_floored:
          type: boolean
        lines:
          type: array
          items:
            $ref: '#/components/schemas/QuoteLine'
        monthly_total:
          type: integer
          format: int64
          minimum: 0
    ConfigureMailAccountRequest:
      type: object
      description: Configure (create or replace) the mailbox. A password field that is absent or null leaves the stored secret
        unchanged; a present value is re-sealed. A first-time configure requires both passwords.
      required:
      - display_name
      - email_address
      - imap_host
      - imap_port
      - imap_security
      - imap_username
      - smtp_host
      - smtp_port
      - smtp_security
      - smtp_username
      properties:
        display_name:
          type: string
        email_address:
          type: string
        from_name:
          type:
          - string
          - 'null'
        imap_host:
          type: string
        imap_port:
          type: integer
          format: int32
        imap_security:
          $ref: '#/components/schemas/MailSecurity'
        imap_username:
          type: string
        imap_password:
          type:
          - string
          - 'null'
          description: Write-only. Present = (re)seal; absent/null = keep the stored secret.
        smtp_host:
          type: string
        smtp_port:
          type: integer
          format: int32
        smtp_security:
          $ref: '#/components/schemas/MailSecurity'
        smtp_username:
          type: string
        smtp_password:
          type:
          - string
          - 'null'
          description: Write-only. Present = (re)seal; absent/null = keep the stored secret.
    ConsoleRouteSurface:
      type: string
      enum:
      - console
      - legacy
    ConsoleRouteTelemetryAccepted:
      type: object
      required:
      - accepted
      properties:
        accepted:
          type: boolean
    ConsoleRouteTelemetryEventKind:
      type: string
      enum:
      - route_selection
      - rum_error
      - rum_perf
    ConsoleRouteTelemetryRequest:
      type: object
      required:
      - event_kind
      - route_surface
      - route_path
      - release_cycle
      properties:
        event_kind:
          $ref: '#/components/schemas/ConsoleRouteTelemetryEventKind'
        route_surface:
          $ref: '#/components/schemas/ConsoleRouteSurface'
        route_path:
          type: string
          minLength: 1
          maxLength: 120
          pattern: ^/[A-Za-z0-9._:/-]*$
          description: Cardinality-safe route template; never include raw URL parameters, queries, or fragments.
        release_cycle:
          type: string
          minLength: 1
          maxLength: 80
          pattern: ^[A-Za-z0-9._:-]+$
        duration_ms:
          type:
          - integer
          - 'null'
          format: int32
          minimum: 0
          maximum: 600000
        error_name:
          type:
          - string
          - 'null'
          minLength: 1
          maxLength: 80
          pattern: ^[A-Za-z0-9._:-]+$
    ConsultingBenefitObservation:
      { type: object, required: [id, initiative_id, kpi_definition_id, evidence_id, observed_at, note, created_at], properties: { id: { $ref: '#/components/schemas/Uuid' }, initiative_id: { $ref: '#/components/schemas/Uuid' }, kpi_definition_id: { $ref: '#/components/schemas/Uuid' }, evidence_id: { $ref: '#/components/schemas/Uuid' }, observed_at: { type: string, format: date-time }, note: { type: string }, created_at: { type: string, format: date-time } } }
    ConsultingDiagnostic:
      type: object
      required:
        - id
        - summary
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        summary:
          type: string
        document_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        created_at:
          type: string
          format: date-time
    ConsultingDiagnosticCreateRequest:
      type: object
      additionalProperties: false
      required:
        - summary
      properties:
        summary:
          type: string
          minLength: 1
        documentId:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
    ConsultingEngagement:
      type: object
      required:
        - id
        - customer_id
        - title
        - status
        - version
        - created_at
        - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        customer_id:
          $ref: '#/components/schemas/Uuid'
        customer_document_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        ontology_instance_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        title:
          type: string
        status:
          type: string
          enum:
            - DRAFT
            - PROPOSED
            - APPROVED
            - IMPLEMENTED
            - MEASURED
            - SUSTAINED
            - CORRECTIVE
        approval_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        workflow_execution_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        version:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ConsultingEngagementCreateRequest:
      type: object
      additionalProperties: false
      required:
        - customerId
        - title
        - idempotencyKey
      properties:
        customerId:
          $ref: '#/components/schemas/Uuid'
        customerDocumentId:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        ontologyInstanceId:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        title:
          type: string
          minLength: 1
        idempotencyKey:
          type: string
          minLength: 1
    ConsultingEngagementDetail:
      allOf:
      - { $ref: '#/components/schemas/ConsultingEngagement' }
      - type: object
        required: [diagnostics, findings, initiatives, observations]
        properties:
          diagnostics: { type: array, items: { $ref: '#/components/schemas/ConsultingDiagnostic' } }
          findings: { type: array, items: { $ref: '#/components/schemas/ConsultingFinding' } }
          initiatives: { type: array, items: { $ref: '#/components/schemas/ConsultingInitiative' } }
          observations: { type: array, items: { $ref: '#/components/schemas/ConsultingBenefitObservation' } }
    ConsultingEngagementPage:
      type: object
      required: [items, limit, offset, total]
      properties: { items: { type: array, items: { $ref: '#/components/schemas/ConsultingEngagement' } }, limit: { type: integer }, offset: { type: integer }, total: { type: integer } }
    ConsultingFinding:
      type: object
      required:
        - id
        - diagnostic_id
        - statement
        - evidence_id
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        diagnostic_id:
          $ref: '#/components/schemas/Uuid'
        statement:
          type: string
        evidence_id:
          $ref: '#/components/schemas/Uuid'
        document_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        created_at:
          type: string
          format: date-time
    ConsultingFindingCreateRequest:
      type: object
      additionalProperties: false
      required:
        - diagnosticId
        - statement
        - evidenceId
      properties:
        diagnosticId:
          $ref: '#/components/schemas/Uuid'
        statement:
          type: string
          minLength: 1
        evidenceId:
          $ref: '#/components/schemas/Uuid'
        documentId:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
    ConsultingHistoryEntry:
      type: object
      required:
        - id
        - event_type
        - version
        - payload
        - occurred_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        event_type:
          type: string
        from_status:
          type:
            - string
            - 'null'
        to_status:
          type:
            - string
            - 'null'
        version:
          type: integer
          format: int64
        payload:
          type: object
          additionalProperties: true
        occurred_at:
          type: string
          format: date-time
    ConsultingInitiative:
      { type: object, required: [id, finding_id, title, hypothesis, kpi_definition_id, target_direction, created_at], properties: { id: { $ref: '#/components/schemas/Uuid' }, finding_id: { $ref: '#/components/schemas/Uuid' }, title: { type: string }, hypothesis: { type: string }, kpi_definition_id: { $ref: '#/components/schemas/Uuid' }, target_direction: { type: string, enum: [INCREASE, DECREASE] }, created_at: { type: string, format: date-time } } }
    ConsultingInitiativeCreateRequest:
      { type: object, additionalProperties: false, required: [findingId, title, hypothesis, kpiDefinitionId, targetDirection], properties: { findingId: { $ref: '#/components/schemas/Uuid' }, title: { type: string, minLength: 1 }, hypothesis: { type: string, minLength: 1 }, kpiDefinitionId: { $ref: '#/components/schemas/Uuid' }, targetDirection: { type: string, enum: [INCREASE, DECREASE] } } }
    ConsultingObservationCreateRequest:
      { type: object, additionalProperties: false, required: [initiativeId, kpiDefinitionId, evidenceId, observedAt, note], properties: { initiativeId: { $ref: '#/components/schemas/Uuid' }, kpiDefinitionId: { $ref: '#/components/schemas/Uuid' }, evidenceId: { $ref: '#/components/schemas/Uuid' }, observedAt: { type: string, format: date-time }, note: { type: string, minLength: 1 } } }
    ConsultingTransitionRequest:
      type: object
      additionalProperties: false
      required:
        - toStatus
        - expectedVersion
        - reason
      properties:
        toStatus:
          type: string
          enum:
            - PROPOSED
            - APPROVED
            - IMPLEMENTED
            - MEASURED
            - SUSTAINED
            - CORRECTIVE
        expectedVersion:
          type: integer
          format: int64
          minimum: 1
        approvalId:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        reason:
          type: string
          minLength: 1
    ConsumeInventoryItemRequest:
      type: object
      additionalProperties: false
      required:
        - source
        - quantityConsumedMilli
        - idempotencyKey
      properties:
        source:
          $ref: '#/components/schemas/InventoryConsumptionSource'
        quantityConsumedMilli:
          type: integer
          format: int64
          minimum: 1
        occurredAt:
          type:
            - string
            - 'null'
          format: date-time
        memo:
          type:
            - string
            - 'null'
        idempotencyKey:
          type: string
          minLength: 16
          maxLength: 200
    ControlCadence:
      type: string
      enum: [CONTINUOUS, DAILY, WEEKLY, MONTHLY, QUARTERLY, ANNUAL, EVENT_DRIVEN]
    ControlObligationCoverage:
      type: object
      required: [id, control_id, obligation_id, coverage_level, status, created_by, updated_by, created_at, updated_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        control_id: { $ref: '#/components/schemas/Uuid' }
        obligation_id: { $ref: '#/components/schemas/Uuid' }
        coverage_level: { $ref: '#/components/schemas/CoverageLevel' }
        coverage_rationale: { type: [string, 'null'] }
        status: { $ref: '#/components/schemas/CoverageStatus' }
        created_by: { $ref: '#/components/schemas/Uuid' }
        updated_by: { $ref: '#/components/schemas/Uuid' }
        created_at: { $ref: '#/components/schemas/Timestamp' }
        updated_at: { $ref: '#/components/schemas/Timestamp' }
    ControlStatus:
      type: string
      enum: [DRAFT, ACTIVE, RETIRED, ARCHIVED]
    ControlType:
      type: string
      enum: [PREVENTIVE, DETECTIVE, CORRECTIVE, DIRECTIVE, COMPENSATING]
    CopyVerification:
      type: object
      required:
        - copy_id
        - copy_kind
        - recorded_digest_sha256
        - recorded_size_bytes
        - status
      properties:
        copy_id:
          type: string
          format: uuid
        copy_kind:
          $ref: '#/components/schemas/EvidenceCopyKind'
        recorded_digest_sha256:
          type: string
        storage_checksum_sha256:
          type:
            - string
            - 'null'
        recorded_size_bytes:
          type: integer
          format: int64
        storage_size_bytes:
          type:
            - integer
            - 'null'
          format: int64
        status:
          $ref: '#/components/schemas/FixityStatus'
    CostLedgerEntrySummary:
      type: object
      required:
      - id
      - branch_id
      - equipment_id
      - source
      - amount_won
      - memo
      - residual_before_won
      - residual_after_won
      - entry_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          type:
          - string
          - 'null'
          format: uuid
        purchase_request_id:
          type:
          - string
          - 'null'
          format: uuid
        source:
          $ref: '#/components/schemas/CostLedgerSource'
        amount_won:
          type: integer
          format: int64
        memo:
          type: string
        residual_before_won:
          type: integer
          format: int64
        residual_after_won:
          type: integer
          format: int64
        entry_at:
          $ref: '#/components/schemas/Timestamp'
    CostLedgerSource:
      type: string
      enum:
      - MANUAL_ADMIN
      - PURCHASE_EXECUTION
    CoverageLevel:
      type: string
      enum: [PRIMARY, PARTIAL, SUPPORTING, COMPENSATING]
    CoverageStatus:
      type: string
      enum: [ACTIVE, RETIRED]
    CreateBranchRequest:
      type: object
      required:
      - region_id
      - name
      properties:
        region_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          minLength: 1
          maxLength: 120
    CreateCalendarEventRequest:
      type: object
      required:
      - scope_type
      - title
      - starts_at
      - ends_at
      properties:
        scope_type:
          $ref: '#/components/schemas/CollaborationScopeType'
        scope_ref:
          type:
          - string
          - 'null'
        title:
          type: string
          minLength: 1
          maxLength: 160
        description:
          type: string
          maxLength: 2000
        starts_at:
          $ref: '#/components/schemas/Timestamp'
        ends_at:
          $ref: '#/components/schemas/Timestamp'
        all_day:
          type: boolean
          default: false
        object_type:
          type:
          - string
          - 'null'
        object_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
    CreateComplianceControlRequest:
      type: object
      required: [framework_id, control_key, title, objective, control_type]
      properties:
        framework_id: { $ref: '#/components/schemas/Uuid' }
        control_key: { type: string }
        title: { type: string }
        objective: { type: string }
        control_type: { $ref: '#/components/schemas/ControlType' }
        cadence: { type: [string, 'null'], enum: [CONTINUOUS, DAILY, WEEKLY, MONTHLY, QUARTERLY, ANNUAL, EVENT_DRIVEN, null] }
        evidence_requirements: { type: object, additionalProperties: true }
        owner_user_id: { type: [string, 'null'], format: uuid }
    CreateComplianceFrameworkRequest:
      type: object
      required: [name, version_label, framework_kind]
      properties:
        name: { type: string }
        version_label: { type: string }
        framework_kind: { $ref: '#/components/schemas/FrameworkKind' }
        owner_user_id: { type: [string, 'null'], format: uuid }
        effective_from: { type: [string, 'null'], format: date }
        effective_to: { type: [string, 'null'], format: date }
        metadata: { type: object, additionalProperties: true }
    CreateComplianceObligationRequest:
      type: object
      required: [title, description, obligation_type, scope, severity]
      properties:
        title: { type: string }
        description: { type: string }
        obligation_type: { $ref: '#/components/schemas/ObligationType' }
        scope: { $ref: '#/components/schemas/ComplianceScope' }
        owner_user_id: { type: [string, 'null'], format: uuid }
        severity: { $ref: '#/components/schemas/ComplianceRiskLevel' }
        effective_from: { type: [string, 'null'], format: date }
        effective_to: { type: [string, 'null'], format: date }
        review_cadence: { type: [string, 'null'], enum: [MONTHLY, QUARTERLY, SEMI_ANNUAL, ANNUAL, EVENT_DRIVEN, null] }
        next_review_on: { type: [string, 'null'], format: date }
        metadata: { type: object, additionalProperties: true }
        regulation_links: { type: array, items: { $ref: '#/components/schemas/RegulationLinkRequest' }, description: Omit to create no regulation links; the server treats omission as an empty list. }
    CreateCustomerRequest:
      type: object
      required:
      - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
          description: Customer name (고객명). Required, trimmed, ≤ 200 characters.
    CreateDailyPlanRequest:
      type: object
      required:
      - branch_id
      - mechanic_id
      - plan_date
      - items
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        mechanic_id:
          $ref: '#/components/schemas/Uuid'
        plan_date:
          $ref: '#/components/schemas/Date'
        items:
          type: array
          minItems: 1
          items:
            type: object
            required:
            - work_order_id
            - description
            properties:
              work_order_id:
                $ref: '#/components/schemas/Uuid'
              description:
                type: string
    CreateEmployeeAttendanceRecordRequest:
      type: object
      required:
        - kind
        - idempotency_key
      properties:
        kind:
          $ref: '#/components/schemas/AttendanceRecordKind'
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 128
        note:
          type:
            - string
            - 'null'
          maxLength: 500
    CreateEmployeeLifecycleEventRequest:
      type: object
      required:
        - event_type
        - effective_date
        - comment
        - signoffs
      properties:
        event_type:
          type: string
          enum:
            - ONBOARD
            - OFFBOARD
            - TERMINATE
            - TRANSFER
        to_status:
          oneOf:
            - {type: string, enum: [ACTIVE, EXITED, UNKNOWN]}
            - {type: 'null'}
        to_company:
          type:
            - string
            - 'null'
        to_org_unit:
          type:
            - string
            - 'null'
        to_position:
          type:
            - string
            - 'null'
        effective_date:
          type: string
          minLength: 1
        comment:
          type: string
          minLength: 1
        signoffs:
          $ref: '#/components/schemas/EmployeeLifecycleSignoffs'
    CreateEmployeeRequest:
      type: object
      additionalProperties: false
      required:
      - employee_number
      - name
      - company
      - employment_type
      - phone
      - org_unit
      - position
      - site
      - home_branch_id
      - base_pay
      - idempotency_key
      properties:
        employee_number:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        company:
          type: string
          minLength: 1
        employment_type:
          type: string
          enum: [REGULAR, CONTRACT, PART_TIME, INTERN]
        phone:
          type: string
          description: Phone number normalized by the server to E.164.
        org_unit:
          type: string
          minLength: 1
        position:
          type: string
          minLength: 1
        site:
          type: string
          minLength: 1
        home_branch_id:
          $ref: '#/components/schemas/Uuid'
        base_pay:
          type: string
          minLength: 1
          maxLength: 15
          pattern: '^(0|[1-9][0-9]{0,11})(\.[0-9]{1,2})?$'
          description: Canonical non-negative KRW NUMERIC(14,2) decimal; only returned by privileged detail.
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 128
    CreateEquipmentRequest:
      type: object
      required:
      - equipment_no
      - customer_name
      - site_name
      - status
      - specification
      - ton_text
      properties:
        equipment_no:
          type: string
          description: Equipment number in AAANN-NNNN form; prefix derives manufacturer/kind/power codes.
        customer_name:
          type: string
          minLength: 1
        site_name:
          type: string
          minLength: 1
        status:
          $ref: '#/components/schemas/EquipmentStatus'
        specification:
          type: string
          minLength: 1
          description: 규격, e.g. 입식 or 좌식.
        ton_text:
          type: string
          description: 톤수 text, e.g. "2.5T"; numeric milli-tons are derived when the value ends in T.
        management_no:
          type:
          - string
          - 'null'
        power_label:
          type:
          - string
          - 'null'
        manager_name:
          type:
          - string
          - 'null'
        placement_location:
          type:
          - string
          - 'null'
        placement_no:
          type:
          - string
          - 'null'
        operation_shift:
          type:
          - string
          - 'null'
        maker:
          type:
          - string
          - 'null'
        model:
          type:
          - string
          - 'null'
        vin:
          type:
          - string
          - 'null'
        year:
          type:
          - string
          - 'null'
          format: date
        hours:
          type:
          - integer
          - 'null'
          format: int64
        vehicle_registration_no:
          type:
          - string
          - 'null'
        insured:
          type:
          - boolean
          - 'null'
        insurer:
          type:
          - string
          - 'null'
        policy_holder:
          type:
          - string
          - 'null'
        insured_party:
          type:
          - string
          - 'null'
        asset_owner:
          type:
          - string
          - 'null'
        asset_registered_on:
          type:
          - string
          - 'null'
          format: date
        rental_started_on:
          type:
          - string
          - 'null'
          format: date
        rental_fee:
          type:
          - integer
          - 'null'
          format: int64
          description: 임대료 in KRW.
        vehicle_value:
          type:
          - integer
          - 'null'
          format: int64
          description: 취득가액 in KRW.
        residual_value:
          type:
          - integer
          - 'null'
          format: int64
          description: 잔존가액 in KRW.
        note:
          type:
          - string
          - 'null'
    CreateEquipmentResponse:
      type: object
      required:
      - id
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
    CreateEvaluationCycleRequest:
      type: object
      additionalProperties: false
      required: [name, kind, period_label, due_date]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        kind: { $ref: '#/components/schemas/EvaluationCycleKind' }
        period_label: { type: string, minLength: 1, maxLength: 60 }
        due_date: { $ref: '#/components/schemas/Date' }
    CreateEvidenceBindingRequest:
      type: object
      required: [control_id, evidence_target_type, evidence_target_id, confidence]
      properties:
        control_id: { $ref: '#/components/schemas/Uuid' }
        obligation_id: { type: [string, 'null'], format: uuid }
        evidence_target_type: { $ref: '#/components/schemas/EvidenceTargetType' }
        evidence_target_id: { type: string }
        source_audit_event_id: { type: [string, 'null'], format: uuid }
        confidence: { $ref: '#/components/schemas/EvidenceConfidence' }
        collected_at: { type: [string, 'null'], format: date-time }
        collected_by: { type: [string, 'null'], format: uuid }
        valid_from: { type: [string, 'null'], format: date }
        valid_to: { type: [string, 'null'], format: date }
        hash_sha256: { type: [string, 'null'] }
        metadata: { type: object, additionalProperties: true }
    CreateInspectionScheduleRequest:
      type: object
      required:
      - branch_id
      - equipment_id
      - mechanic_id
      - cycle
      - interval_days
      - due_date
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        mechanic_id:
          $ref: '#/components/schemas/Uuid'
        cycle:
          $ref: '#/components/schemas/InspectionCycle'
        interval_days:
          type: integer
          format: int32
          minimum: 1
        due_date:
          $ref: '#/components/schemas/Date'
        note:
          type:
          - string
          - 'null'
    CreateInternalTicketRequest:
      type: object
      required:
      - branch_id
      - category
      - priority
      - title
      - body
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        category:
          $ref: '#/components/schemas/SupportTicketCategory'
        priority:
          $ref: '#/components/schemas/SupportTicketPriority'
        title:
          type: string
          minLength: 1
        body:
          type: string
          minLength: 1
    CreateLeavePromotion:
      type: object
      required:
        - round_no
        - reference_year
      properties:
        round_no:
          type: integer
          enum:
            - 1
            - 2
        reference_year:
          type: integer
        min_remaining_days:
          type:
            - number
            - 'null'
        note:
          type:
            - string
            - 'null'
    CreateLeaveRequest:
      type: object
      required:
        - leave_type
        - reason
        - start_date
        - end_date
      properties:
        leave_type:
          type: string
          enum:
            - ANNUAL
            - HALF_DAY_AM
            - HALF_DAY_PM
            - SICK
            - FAMILY_EVENT
            - PUBLIC_DUTY
            - UNPAID
            - OTHER
        reason:
          type: string
          enum:
            - PERSONAL
            - FAMILY_EVENT
            - MEDICAL
            - BEREAVEMENT
            - CHILDCARE
            - CIVIC_DUTY
            - OTHER
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        note:
          type:
            - string
            - 'null'
    CreateListingRequest:
      type: object
      description: Full editable field set for creating a sales listing.
      required:
      - kind
      - condition
      - model_name
      - listing_type
      - status
      - sort_weight
      properties:
        kind:
          $ref: '#/components/schemas/ListingKind'
        condition:
          $ref: '#/components/schemas/ListingCondition'
        model_name:
          type: string
        capacity_milli:
          type:
          - integer
          - 'null'
          format: int64
        model_year:
          type:
          - integer
          - 'null'
          format: int32
        usage_hours:
          type:
          - integer
          - 'null'
          format: int32
        price_won:
          type:
          - integer
          - 'null'
          format: int64
        badge:
          type:
          - string
          - 'null'
        usage_label:
          type:
          - string
          - 'null'
        condition_label:
          type:
          - string
          - 'null'
        availability:
          type:
          - string
          - 'null'
        location:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        listing_type:
          $ref: '#/components/schemas/ListingType'
        status:
          $ref: '#/components/schemas/ListingStatus'
        sort_weight:
          type: integer
          format: int32
        equipment_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
    CreateListingResponse:
      type: object
      required:
      - id
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
    CreateMessengerThreadRequest:
      type: object
      required:
      - branch_id
      - kind
      - member_ids
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        kind:
          $ref: '#/components/schemas/MessengerThreadKind'
        visibility:
          description: 'Optional taxonomy override; when omitted the thread is always direct (fixed member set) — pass
            visibility=channel explicitly to create a joinable, discoverable team channel. A channel requires a title;
            DM and work-order threads are always direct.'
          oneOf:
          - $ref: '#/components/schemas/MessengerThreadVisibility'
          - type: 'null'
        title:
          type:
          - string
          - 'null'
        work_order_id:
          type:
          - string
          - 'null'
          format: uuid
        member_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
    CreateNoticeDraftRequest:
      type: object
      required:
      - title
      - body
      properties:
        title:
          type: string
          maxLength: 300
        body:
          type: string
          maxLength: 20000
        category:
          $ref: '#/components/schemas/NoticeCategory'
        audience:
          $ref: '#/components/schemas/NoticeAudienceInput'
    CreateObjectLinkRequest:
      type: object
      description: Request to create a directed link between two known objects.
      required:
      - src_kind
      - src_id
      - dst_kind
      - dst_id
      - link_type
      properties:
        src_kind:
          type: string
          description: Source object kind slug (must be a known object type).
        src_id:
          type: string
          description: Source object id/reference (≤200 chars).
        dst_kind:
          type: string
          description: Destination object kind slug (must be a known object type).
        dst_id:
          type: string
          description: Destination object id/reference (≤200 chars).
        link_type:
          type: string
          description: Relationship label slug (e.g. authorized_by, relates_to, blocks).
    CreateObjectTypeDraft:
      type: object
      description: Draft object-type schema (registry backbone) with its property/link/action/analytic children.
      required:
      - stable_key
      - title
      - backing_kind
      properties:
        stable_key:
          type: string
        title:
          type: string
        title_property_key:
          type: string
        backing_kind:
          type: string
          enum:
          - projected
          - instance
        backing_table:
          type: string
        primary_key_property:
          type: string
        properties:
          type: array
          items:
            type: object
            additionalProperties: true
        links:
          type: array
          items:
            type: object
            additionalProperties: true
        actions:
          type: array
          items:
            type: object
            additionalProperties: true
        analytics:
          type: array
          items:
            type: object
            additionalProperties: true
    CreateOrgChangeRequest:
      type: object
      additionalProperties: false
      required:
      - kind
      - target
      - effectiveDate
      - reason
      - proposal
      properties:
        kind:
          $ref: '#/components/schemas/OrgChangeKind'
        target:
          $ref: '#/components/schemas/OrgChangeTarget'
        effectiveDate:
          $ref: '#/components/schemas/Date'
        reason:
          type: string
          minLength: 1
          maxLength: 4000
        proposal:
          type: array
          maxItems: 100
          items:
            $ref: '#/components/schemas/OrgProposalOp'
        supersedesId:
          $ref: '#/components/schemas/Uuid'
    CreateOutsourceWorkRequest:
      type: object
      required:
      - vendor_name
      - reason
      properties:
        vendor_name:
          type: string
        vendor_contact:
          type: string
        reason:
          type: string
    CreateOwnershipTransferRequest:
      type: object
      required:
      - to_owner
      - reason
      properties:
        to_owner:
          type: string
          minLength: 1
          description: New legal-owner label after all signoffs approve.
        reason:
          type: string
          minLength: 1
          maxLength: 1000
          description: Business/legal reason for the transfer.
    CreatePeriodLockRequest:
      type: object
      required:
      - domain
      - periodStart
      - periodEnd
      - reason
      properties:
        domain:
          type: string
          enum: [payroll, accounting]
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        reason:
          type: string
          minLength: 1
    CreatePlatformGroupAccountRequest:
      type: object
      required:
      - org_id
      - display_name
      properties:
        org_id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type: string
          minLength: 1
        phone:
          type: string
        tenant_roles:
          type: array
          items:
            $ref: '#/components/schemas/PlatformTenantRole'
          default:
          - MEMBER
        group_role:
          allOf:
          - $ref: '#/components/schemas/PlatformGroupRole'
          default: GROUP_ADMIN
    CreatePlatformGroupAccountResponse:
      type: object
      required:
      - account
      - otp
      - otp_expires_at
      properties:
        account:
          $ref: '#/components/schemas/PlatformGroupAccount'
        otp:
          type: string
          minLength: 1
          description: One-time setup OTP returned exactly once; deliver out-of-band and never log it.
        otp_expires_at:
          $ref: '#/components/schemas/Timestamp'
    CreatePlatformGroupRequest:
      type: object
      required:
      - slug
      - name
      properties:
        slug:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
    CreatePlatformOrgRequest:
      type: object
      required:
      - slug
      - name
      properties:
        slug:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
    CreatePolicyRoleRequest:
      type: object
      required:
      - role_key
      - display_name
      - permissions
      properties:
        role_key:
          type: string
          pattern: ^[a-z][a-z0-9_]+$
        display_name:
          type: string
          minLength: 1
          maxLength: 80
        description:
          type:
          - string
          - 'null'
          maxLength: 512
        permissions:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/PolicyPermissionResponse'
        conditions:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/PolicyConditionResponse'
    CreatePollRequest:
      type: object
      required:
      - target_scope_type
      - title
      - question
      - options
      properties:
        target_scope_type:
          $ref: '#/components/schemas/CollaborationScopeType'
        target_scope_ref:
          type:
          - string
          - 'null'
        title:
          type: string
          minLength: 1
          maxLength: 160
        question:
          type: string
          minLength: 1
          maxLength: 1000
        status:
          $ref: '#/components/schemas/PollStatus'
        anonymity:
          $ref: '#/components/schemas/PollAnonymity'
        allow_multiple:
          type: boolean
          default: false
        closes_at:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        options:
          type: array
          minItems: 2
          maxItems: 20
          items:
            type: string
            minLength: 1
            maxLength: 240
        object_type:
          type:
          - string
          - 'null'
        object_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
    CreateProductionPlan:
      type: object
      required: [branch_id, customer_demand_id, capacity_slot_id, material_item_id, quantity, due_at, ontology_type_id, idempotency_key]
      properties:
        branch_id: {$ref: '#/components/schemas/Uuid'}
        customer_demand_id: {$ref: '#/components/schemas/Uuid'}
        capacity_slot_id: {$ref: '#/components/schemas/Uuid'}
        material_item_id: {$ref: '#/components/schemas/Uuid'}
        quantity: {type: integer, format: int64, minimum: 1}
        due_at: {type: string, format: date-time}
        ontology_type_id: {$ref: '#/components/schemas/Uuid'}
        idempotency_key: {type: string, minLength: 1, maxLength: 128}
    CreatePurchaseRequest:
      type: object
      required:
        - branch_id
        - purchase_type
        - vendor_name
        - lines
        - quote_attachment_ids
        - memo
        - config
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          type:
            - string
            - "null"
          format: uuid
        work_order_id:
          type:
          - string
          - 'null'
          format: uuid
        statement_evidence_id:
          type:
            - string
            - "null"
          format: uuid
        purchase_type:
          $ref: "#/components/schemas/PurchaseType"
        vendor_name:
          type: string
          minLength: 1
        amount_won:
          type:
            - integer
            - "null"
          format: int64
          minimum: 1
        lines:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/PurchaseRequestLineInput"
        quote_attachment_ids:
          type: array
          items:
            $ref: "#/components/schemas/Uuid"
        memo:
          type: string
          minLength: 1
        config:
          $ref: '#/components/schemas/FinancialConfigSnapshot'
    CreateRecruitApplicantRequest:
      type: object
      required:
      - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        profile_lines:
          type: array
          maxItems: 40
          items:
            type: string
            minLength: 1
            maxLength: 500
        source_document:
          type:
          - string
          - 'null'
          maxLength: 300
    CreateRecruitPostingRequest:
      type: object
      required:
      - role_title
      - company
      - worksite
      - employment_type
      - scope
      - headcount
      properties:
        role_title:
          type: string
          minLength: 1
          maxLength: 200
        company:
          type: string
          minLength: 1
          maxLength: 200
        worksite:
          type: string
          minLength: 1
          maxLength: 200
        employment_type:
          $ref: '#/components/schemas/RecruitEmploymentType'
        scope:
          $ref: '#/components/schemas/RecruitPostingScope'
        headcount:
          type: integer
          format: int32
          minimum: 1
          maximum: 999
        deadline:
          type:
          - string
          - 'null'
          format: date
          description: Omit or null for an open-ended posting (상시).
        requirements:
          type: array
          maxItems: 20
          items:
            type: string
            minLength: 1
            maxLength: 300
        position_ref:
          type:
          - string
          - 'null'
          maxLength: 200
    CreateRegionRequest:
      type: object
      required:
      - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
    CreateRegulationImpactRequest:
      type: object
      required: [title, jurisdiction, citation, impact_area, impact_summary, risk_level]
      properties:
        title: { type: string }
        jurisdiction: { type: string }
        regulator: { type: [string, 'null'] }
        citation: { type: string }
        source_url: { type: [string, 'null'], format: uri }
        impact_area: { type: string }
        impact_summary: { type: string }
        risk_level: { $ref: '#/components/schemas/ComplianceRiskLevel' }
        effective_from: { type: [string, 'null'], format: date }
        effective_to: { type: [string, 'null'], format: date }
        review_due_on: { type: [string, 'null'], format: date }
        owner_user_id: { type: [string, 'null'], format: uuid }
        metadata: { type: object, additionalProperties: true }
    CreateRentalQuoteRequest:
      type: object
      required:
      - branch_id
      - equipment_id
      - config
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        config:
          $ref: '#/components/schemas/FinancialConfigSnapshot'
    CreateSeriesRequest:
      type: object
      description: Create a series and attach its first instance.
      required:
      - label
      - kind
      - id
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 200
          description: Human label for the series (1-200 chars).
        kind:
          type: string
          description: First instance object kind slug.
        id:
          type: string
          maxLength: 200
          description: First instance object id/reference (≤200 chars).
    CreateSettlementRequest:
      type: object
      required:
      - lines
      properties:
        lines:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/SettlementLineRequest'
        note:
          type: string
    CreateSiteRequest:
      type: object
      required:
      - customer_id
      - name
      description: Create a site under an existing customer. Optional location/contact fields follow the same WGS84 ranges
        and length bounds as UpdateSiteRequest; latitude and longitude must be supplied together.
      properties:
        customer_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          minLength: 1
          maxLength: 200
          description: Site name (현장명). Required, trimmed, ≤ 200 characters.
        address:
          type:
          - string
          - 'null'
          maxLength: 500
        province:
          type:
          - string
          - 'null'
          maxLength: 100
        city:
          type:
          - string
          - 'null'
          maxLength: 100
        postal_code:
          type:
          - string
          - 'null'
          maxLength: 20
        latitude:
          type:
          - number
          - 'null'
          format: double
          minimum: -90
          maximum: 90
          description: Optional admin-authored WGS84 latitude in degrees. Must be supplied together with longitude.
        longitude:
          type:
          - number
          - 'null'
          format: double
          minimum: -180
          maximum: 180
          description: Optional admin-authored WGS84 longitude in degrees. Must be supplied together with latitude.
        geofence_radius_m:
          type:
          - number
          - 'null'
          format: double
          minimum: 0
          maximum: 100000
          description: Per-site geofence radius in metres (> 0, ≤ 100000). Null uses the system default (300 m).
        contact_name:
          type:
          - string
          - 'null'
          maxLength: 100
          description: On-site representative contact name (담당자명).
        contact_phone:
          type:
          - string
          - 'null'
          maxLength: 40
          description: On-site contact phone (연락처).
        contact_email:
          type:
          - string
          - 'null'
          maxLength: 320
          description: Optional contact email (이메일).
    CreateTodoRequest:
      type: object
      required:
      - text
      properties:
        text:
          type: string
          description: 1..=500 characters after trimming.
        scopes:
          type: array
          description: Scope chips (person/team/site/entity refs); at most 20.
          items:
            $ref: '#/components/schemas/TodoRef'
        links:
          type: array
          description: Object links (kind+id pairs); at most 20.
          items:
            $ref: '#/components/schemas/TodoRef'
    CreateTriggerBindingRequest:
      type: object
      required: [definition_id, trigger_type, event_key]
      properties:
        definition_id:
          $ref: '#/components/schemas/Uuid'
        trigger_type:
          type: string
          enum: [OBJECT_EVENT, IMPORT_EVENT, MAIL_EVENT, MESSENGER_EVENT, CALENDAR_EVENT, POLL_EVENT]
        event_key:
          type: string
        subject_kind:
          type: string
          description: Optional ontology object kind the rule acts on; must be a registered object_types.kind.
        enabled:
          type: boolean
          default: true
    CreateUserRequest:
      type: object
      required:
      - display_name
      properties:
        display_name:
          type: string
        employee_id:
          type:
          - string
          - 'null'
          format: uuid
        phone:
          type:
          - string
          - 'null'
        team:
          $ref: '#/components/schemas/Team'
        roles:
          type: array
          items:
            type: string
        branch_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
    CreateVoucherRequest:
      type: object
      required:
      - branch_id
      - lines
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        memo:
          type: string
        lines:
          type: array
          items:
            $ref: '#/components/schemas/VoucherLineInput'
    CreateWorkOrderRequest:
      type: object
      required:
      - branch_id
      - management_no
      - symptom
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        management_no:
          type: string
        symptom:
          type: string
        customer_request:
          type: string
        target_due_at:
          $ref: '#/components/schemas/Timestamp'
        maintenance_type:
          $ref: '#/components/schemas/MaintenanceType'
        maintenance_cause:
          $ref: '#/components/schemas/MaintenanceCause'
    CreateWorkflowDefinitionRequest:
      type: object
      required:
      - workflow_key
      - display_name
      - object_type
      properties:
        workflow_key:
          type: string
        display_name:
          type: string
        object_type:
          type: string
        definition:
          type: object
          additionalProperties: true
        approval_line:
          type: array
          items:
            type: object
            additionalProperties: true
        payment_line:
          type: array
          items:
            type: object
            additionalProperties: true
        notification_rules:
          type: array
          items:
            type: object
            additionalProperties: true
        action_allowlist:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowActionAllowlistEntry'
        required_approval_line:
          type: boolean
        required_payment_line:
          type: boolean
    CreateWorkflowScheduleRequest:
      type: object
      required: [label, cron_expr, definition_id]
      properties:
        label:
          type: string
          maxLength: 120
        cron_expr:
          type: string
        timezone:
          type: string
          description: IANA timezone the cron pattern is evaluated in (default Asia/Seoul).
        definition_id:
          $ref: '#/components/schemas/Uuid'
        enabled:
          type: boolean
          default: true
    CreatedCustomer:
      type: object
      required:
      - id
      - branch_id
      - name
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
    CreatedSite:
      type: object
      required:
      - id
      - customer_id
      - branch_id
      - name
      - address
      - province
      - city
      - postal_code
      - latitude
      - longitude
      - geofence_radius_m
      - contact_name
      - contact_phone
      - contact_email
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        customer_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        address:
          type:
          - string
          - 'null'
        province:
          type:
          - string
          - 'null'
        city:
          type:
          - string
          - 'null'
        postal_code:
          type:
          - string
          - 'null'
        latitude:
          type:
          - number
          - 'null'
          format: double
          minimum: -90
          maximum: 90
          description: Admin-authored WGS84 latitude in degrees, or null when unpinned.
        longitude:
          type:
          - number
          - 'null'
          format: double
          minimum: -180
          maximum: 180
          description: Admin-authored WGS84 longitude in degrees, or null when unpinned.
        geofence_radius_m:
          type:
          - number
          - 'null'
          format: double
        contact_name:
          type:
          - string
          - 'null'
        contact_phone:
          type:
          - string
          - 'null'
        contact_email:
          type:
          - string
          - 'null'
    CustodyEventView:
      type: object
      required:
        - id
        - evidence_object_id
        - stage
        - actor_user_id
        - reason
        - event_digest_sha256
        - occurred_at
        - created_at
      properties:
        id:
          type: string
          format: uuid
        evidence_object_id:
          type: string
          format: uuid
        stage:
          $ref: '#/components/schemas/CustodyStage'
        actor_user_id:
          type: string
          format: uuid
        from_custodian:
          type:
            - object
            - 'null'
          additionalProperties: true
        to_custodian:
          type:
            - object
            - 'null'
          additionalProperties: true
        location_label:
          type:
            - string
            - 'null'
        reason:
          type: string
        source_ref:
          oneOf:
            - {$ref: '#/components/schemas/EvidenceSourceRef'}
            - {type: 'null'}
        audit_event_id:
          type:
            - string
            - 'null'
          format: uuid
        previous_event_id:
          type:
            - string
            - 'null'
          format: uuid
        event_digest_sha256:
          type: string
        occurred_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    CustodyStage:
      { type: string, enum: [REGISTERED, HASH_RECORDED, TSA_SUBMITTED, TSA_VERIFIED, WORM_REPLICATED, CUSTODY_TRANSFERRED, UNDER_REVIEW, ADMISSIBILITY_EVALUATED, LEGAL_HOLD_APPLIED, LEGAL_HOLD_RELEASED, EXPORTED, ARCHIVED, DISPOSAL_REQUESTED, DISPOSED] }
    CustomerInquiryPage:
      type: object
      required:
      - items
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CustomerInquiryView'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
    CustomerInquiryView:
      type: object
      required:
      - id
      - name
      - phone
      - topic
      - location
      - message
      - listing_id
      - status
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        phone:
          type: string
        topic:
          $ref: '#/components/schemas/InquiryTopic'
        location:
          type:
          - string
          - 'null'
        message:
          type:
          - string
          - 'null'
        listing_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        status:
          $ref: '#/components/schemas/InquiryStatus'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    CustomerIntakeRequest:
      type: object
      required:
      - category
      - priority
      - title
      - body
      - requester_name
      - requester_contact
      properties:
        category:
          $ref: '#/components/schemas/SupportTicketCategory'
        priority:
          $ref: '#/components/schemas/SupportTicketPriority'
        title:
          type: string
          minLength: 1
        body:
          type: string
          minLength: 1
        requester_name:
          type: string
          minLength: 1
        requester_contact:
          type: string
          minLength: 1
    CycleCount:
      type: object
      required:
        - id
        - ccCode
        - branchId
        - stockLocation
        - status
        - version
        - openedBy
        - lineCount
        - varianceLineCount
        - createdAt
        - updatedAt
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        ccCode:
          type: string
        branchId:
          $ref: '#/components/schemas/Uuid'
        stockLocation:
          $ref: '#/components/schemas/InventoryStockLocationSummary'
        status:
          type: string
          enum:
            - DRAFT
            - SUBMITTED
            - APPROVED
            - REJECTED
            - CANCELLED
        version:
          type: integer
        openedBy:
          $ref: '#/components/schemas/Uuid'
        submittedBy:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        submittedAt:
          type:
            - string
            - 'null'
          format: date-time
        decidedBy:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        decidedAt:
          type:
            - string
            - 'null'
          format: date-time
        decisionMemo:
          type:
            - string
            - 'null'
        lineCount:
          type: integer
          format: int64
        varianceLineCount:
          type: integer
          format: int64
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CycleCountDetail:
      type: object
      required: [count, lines, appliedMovementIds]
      properties: { count: { $ref: '#/components/schemas/CycleCount' }, lines: { type: array, items: { $ref: '#/components/schemas/CycleCountLine' } }, appliedMovementIds: { type: array, items: { $ref: '#/components/schemas/Uuid' } } }
    CycleCountLine:
      type: object
      required:
        - id
        - itemId
        - ivCode
        - displayName
        - unitCode
        - systemQuantityMilli
        - countedQuantityMilli
        - varianceMilli
        - recordedBy
        - recordedAt
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        itemId:
          $ref: '#/components/schemas/Uuid'
        ivCode:
          type: string
        displayName:
          type: string
        unitCode:
          type: string
        systemQuantityMilli:
          type: integer
          format: int64
        countedQuantityMilli:
          type: integer
          format: int64
        varianceMilli:
          type: integer
          format: int64
        reason:
          oneOf:
            - {type: string, enum: [DAMAGE, LOSS, MISCOUNT, FOUND, OTHER]}
            - {type: 'null'}
        note:
          type:
            - string
            - 'null'
        recordedBy:
          $ref: '#/components/schemas/Uuid'
        recordedAt:
          type: string
          format: date-time
    CycleCountPage:
      type: object
      required: [items, limit, offset, total]
      properties: { items: { type: array, items: { $ref: '#/components/schemas/CycleCount' } }, limit: { type: integer, format: int64 }, offset: { type: integer, format: int64 }, total: { type: integer, format: int64 } }
    CycleCountVersionRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion]
      properties: { expectedVersion: { type: integer, minimum: 1 } }
    DailyPlanItemSummary:
      type: object
      required:
        - description
        - sort_order
      properties:
        work_order_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        request_no:
          type:
            - string
            - 'null'
        equipment_no:
          type:
            - string
            - 'null'
        management_no:
          type:
            - string
            - 'null'
        customer_name:
          type:
            - string
            - 'null'
        site_name:
          type:
            - string
            - 'null'
        description:
          type: string
        sort_order:
          type: integer
          format: int32
    DailyPlanListPage:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/DailyPlanSummary'
    DailyPlanStatus:
      type: string
      enum:
      - DRAFT
      - REQUESTED
      - APPROVED
      - REJECTED
      - FINAL_CONFIRMED
    DailyPlanSummary:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        mechanic_id:
          $ref: '#/components/schemas/Uuid'
        plan_date:
          $ref: '#/components/schemas/Date'
        status:
          $ref: '#/components/schemas/DailyPlanStatus'
        items:
          type: array
          items:
            $ref: '#/components/schemas/DailyPlanItemSummary'
    Date:
      type: string
      format: date
    DebitCredit:
      type: string
      enum: [DEBIT, CREDIT]
    DecideCycleCountRequest:
      type: object
      additionalProperties: false
      required:
        - expectedVersion
        - decision
      properties:
        expectedVersion:
          type: integer
          minimum: 1
        decision:
          type: string
          enum:
            - APPROVE
            - REJECT
        memo:
          type:
            - string
            - 'null'
        idempotencyKey:
          type:
            - string
            - 'null'
          minLength: 16
          maxLength: 200
    DecideLeaveRequest:
      type: object
      required:
        - decision
      properties:
        decision:
          type: string
          enum:
            - APPROVE
            - REJECT
        note:
          type:
            - string
            - 'null'
    DecideOwnershipTransferRequest:
      type: object
      required:
      - decision
      - comment
      properties:
        decision:
          type: string
          enum:
          - approve
          - reject
        comment:
          type: string
          minLength: 1
          maxLength: 1000
    DecidePayrollRunRequest:
      type: object
      required:
      - decision
      properties:
        decision:
          type: string
          enum: [APPROVE, REJECT]
        reason:
          type: string
          description: Required for REJECT (a blank reason is rejected with 422).
    DecideWorkflowTaskRequest:
      type: object
      required:
      - decision
      - idempotency_key
      properties:
        decision:
          type: string
          enum:
          - approve
          - reject
          - return
        comment:
          type: string
          description: Required and non-empty when decision is reject or return.
        idempotency_key:
          type: string
          minLength: 16
    DecideWorkflowTaskResponse:
      type: object
      required:
      - task
      - run
      properties:
        task:
          $ref: '#/components/schemas/DecidedWorkflowTask'
        run:
          $ref: '#/components/schemas/FinalizedWorkflowRun'
        next_task:
          $ref: '#/components/schemas/WorkflowTaskSummary'
    DecidedWorkflowTask:
      type: object
      required:
      - task_id
      - run_id
      - status
      - decision_payload
      properties:
        task_id:
          $ref: '#/components/schemas/Uuid'
        run_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
        decision_payload:
          type: object
          additionalProperties: true
    DecisionLogRow:
      type: object
      required:
        - id
        - decided_at
        - subject_ref
        - action
        - resource_type
        - effect
        - determining_policies
        - reason
      properties:
        id:
          type: string
          format: uuid
        decided_at:
          type: string
          format: date-time
        subject_ref:
          type: string
        action:
          type: string
        resource_type:
          type: string
        resource_id:
          type:
            - string
            - 'null'
        effect:
          type: string
          enum:
            - allow
            - deny
        determining_policies:
          type: array
          items:
            type: string
        reason:
          type: string
    DefinitionsByObjectKindResponse:
      type: object
      required: [kind, definitions, bindings]
      properties:
        kind:
          type: string
        definitions:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowDefinitionResponse'
        bindings:
          type: array
          items:
            $ref: '#/components/schemas/TriggerBindingResponse'
    DepreciationMethod:
      type: string
      enum:
      - STRAIGHT_LINE
      - DECLINING_BALANCE
    DeviceLoginApproveRequest:
      type: object
      required:
      - approve_token
      - ceremony_id
      - credential
      properties:
        approve_token:
          type: string
        ceremony_id:
          $ref: '#/components/schemas/Uuid'
        credential:
          type: object
          additionalProperties: true
    DeviceLoginApproveSessionRequest:
      type: object
      required:
      - approve_token
      properties:
        approve_token:
          type: string
    DeviceLoginPollRequest:
      type: object
      required:
      - poll_token
      properties:
        poll_token:
          type: string
    DeviceLoginPollResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          enum:
            - pending
            - approved
            - expired
        access_token:
          type:
            - string
            - 'null'
        refresh_token:
          type:
            - string
            - 'null'
          description: Null in web cookie transport; present in body transport.
        token_type:
          oneOf:
            - {type: string, enum: [Bearer]}
            - {type: 'null'}
        refresh_expires_at:
          type:
            - string
            - 'null'
          format: date-time
        requires_passkey_setup:
          type:
            - boolean
            - 'null'
          description: Approved desktop QR sessions are passkey-authenticated and return false.
    DeviceLoginStartResponse:
      type: object
      required:
      - poll_token
      - approve_url
      - expires_at
      properties:
        poll_token:
          type: string
          description: Desktop-only token used to poll for approval.
        approve_url:
          type: string
          description: Phone QR URL carrying the approve token in the URL fragment.
        expires_at:
          $ref: '#/components/schemas/Timestamp'
    DevicePlatform:
      type: string
      enum:
      - ios
      - android
    DeviceRegistrationRequest:
      type: object
      required:
      - platform
      - app_version
      properties:
        platform:
          $ref: '#/components/schemas/DevicePlatform'
        push_token:
          type:
          - string
          - 'null'
        app_version:
          type: string
          minLength: 1
    DeviceRegistrationResponse:
      type: object
      required:
      - id
      - user_id
      - device_hash
      - platform
      - app_version
      - last_registered_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        user_id:
          $ref: '#/components/schemas/Uuid'
        device_hash:
          type: string
          pattern: ^[a-f0-9]{64}$
        platform:
          $ref: '#/components/schemas/DevicePlatform'
        push_token:
          type:
          - string
          - 'null'
        app_version:
          type: string
        last_registered_at:
          $ref: '#/components/schemas/Timestamp'
    DirectoryPage:
      type: object
      required:
      - items
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/DirectoryPerson'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
    DirectoryPerson:
      type: object
      required:
      - id
      - display_name
      - employee_id
      - employee_name
      - employee_number
      - employee_company
      - employee_org_unit
      - employee_position
      - employee_identity_review_required
      - employee_identity_resolution_confidence
      - employee_link_status
      - team
      - roles
      - branch_ids
      - is_active
      - has_passkey
      - account_status
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type: string
        employee_id:
          type:
          - string
          - 'null'
          format: uuid
        employee_name:
          type:
          - string
          - 'null'
        employee_number:
          type:
          - string
          - 'null'
        employee_company:
          type:
          - string
          - 'null'
        employee_org_unit:
          type:
          - string
          - 'null'
        employee_position:
          type:
          - string
          - 'null'
        employee_identity_review_required:
          type:
          - boolean
          - 'null'
        employee_identity_resolution_confidence:
          type:
          - string
          - 'null'
        employee_link_status:
          type: string
          enum:
          - LINKED
          - UNLINKED
        team:
          $ref: '#/components/schemas/Team'
        roles:
          type: array
          items:
            type: string
        branch_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        is_active:
          type: boolean
        has_passkey:
          type: boolean
          description: Whether the user has at least one enrolled passkey. A user can only sign in once this is true; until
            then the account is pending setup.
        account_status:
          $ref: '#/components/schemas/AccountStatus'
        created_at:
          $ref: '#/components/schemas/Timestamp'
    DispatchCandidatePage:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/DispatchCandidateSummary' } }
    DispatchCandidateSummary:
      type: object
      required: [mechanic_id, score_milli, gps_ranked, workload, score_reason]
      properties:
        mechanic_id: { $ref: '#/components/schemas/Uuid' }
        score_milli: { type: integer, format: int64 }
        gps_ranked: { type: boolean }
        distance_meters: { type: integer, format: int64 }
        location_recorded_at: { $ref: '#/components/schemas/Timestamp' }
        workload: { type: object, additionalProperties: true }
        score_reason: { type: string }
        response: { $ref: '#/components/schemas/DispatchResponseKind' }
        responded_at: { $ref: '#/components/schemas/Timestamp' }
    DispatchQueueDispatch:
      type: object
      required: [id, status, accept_window_ends_at, target_count, accepted_count, declined_count, manual_call_required]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        status: { $ref: '#/components/schemas/DispatchStatus' }
        accept_window_ends_at: { $ref: '#/components/schemas/Timestamp' }
        target_count: { type: integer, format: int64 }
        accepted_count: { type: integer, format: int64 }
        declined_count: { type: integer, format: int64 }
        manual_call_required: { type: boolean }
    DispatchQueueItem:
      type: object
      required: [work_order_id, request_no, branch_id, status, priority, symptom, equipment_id, customer_id, site_id, updated_at]
      properties:
        work_order_id: { $ref: '#/components/schemas/Uuid' }
        request_no: { type: string }
        branch_id: { $ref: '#/components/schemas/Uuid' }
        status: { $ref: '#/components/schemas/WorkOrderStatus' }
        priority: { $ref: '#/components/schemas/PriorityLevel' }
        symptom: { type: string }
        equipment_id: { $ref: '#/components/schemas/Uuid' }
        customer_id: { $ref: '#/components/schemas/Uuid' }
        site_id: { $ref: '#/components/schemas/Uuid' }
        target_due_at: { $ref: '#/components/schemas/Timestamp' }
        assigned_mechanic_id: { $ref: '#/components/schemas/Uuid' }
        dispatch: { $ref: '#/components/schemas/DispatchQueueDispatch' }
        updated_at: { $ref: '#/components/schemas/Timestamp' }
    DispatchQueuePage:
      type: object
      required: [items, stats]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/DispatchQueueItem' } }
        next_after: { type: string }
        stats: { $ref: '#/components/schemas/DispatchQueueStats' }
    DispatchQueueStats:
      type: object
      required: [unassigned_count, sla_due_count]
      properties:
        unassigned_count: { type: integer, format: int64 }
        sla_due_count: { type: integer, format: int64 }
    DispatchQueueStatus:
      type: string
      enum: [RECEIVED, UNASSIGNED, ASSIGNED, IN_PROGRESS, PART_WAITING, DELAYED]
    DispatchResponseKind:
      type: string
      enum:
      - ACCEPT
      - DECLINE
    DispatchStatus:
      type: string
      enum:
      - BROADCASTING
      - AUTO_ASSIGNED
      - MANAGER_FORCE_PENDING
    DocumentVersion:
      type: object
      description: One immutable version of an in-console office document. The storage key is internal and never returned.
      required: [id, documentRef, versionNo, contentHash, fileType, byteSize, createdAt]
      properties:
        id:
          type: string
          format: uuid
        documentRef:
          type: string
          minLength: 1
          maxLength: 200
        versionNo:
          type: integer
          minimum: 1
        contentHash:
          type: string
          pattern: ^[a-f0-9]{64}$
        fileType:
          type: string
          enum: [docx, xlsx, pptx]
        byteSize:
          type: integer
          format: int64
          minimum: 0
        restoredFrom:
          type: integer
          minimum: 1
          description: The version_no this version non-destructively restored, when it is a rollback.
        createdBy:
          type: string
          format: uuid
          description: Actor who produced the version; omitted for system-initiated (force-save callback) versions.
        createdAt:
          type: string
          format: date-time
    Employee:
      type: object
      required:
        - id
        - company
        - name
        - identity_resolution_strategy
        - identity_resolution_confidence
        - identity_review_required
        - identity_name_only_merge
        - home_branch_review_required
        - created_at
        - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        company:
          type: string
        name:
          type: string
        employee_number:
          type:
            - string
            - 'null'
        org_unit:
          type:
            - string
            - 'null'
        worksite_name:
          type:
            - string
            - 'null'
        worksite:
          type:
            - string
            - 'null'
        job:
          type:
            - string
            - 'null'
        position:
          type:
            - string
            - 'null'
        hire_date:
          type:
            - string
            - 'null'
        exit_date:
          type:
            - string
            - 'null'
        status:
          type:
            - string
            - 'null'
        leave_accrued:
          type:
            - string
            - 'null'
        leave_used:
          type:
            - string
            - 'null'
        leave_remaining:
          type:
            - string
            - 'null'
        home_branch_id:
          type:
            - string
            - 'null'
          format: uuid
          description: Active authoritative leave-routing branch; null requires HR review before self-service filing.
        home_branch_name:
          type:
            - string
            - 'null'
        home_branch_review_required:
          type: boolean
          description: True when no active authoritative home branch is configured.
        identity_resolution_strategy:
          type: string
          enum:
            - employee_number
            - legal_identifier_hash
            - birth_hire_fingerprint
            - source_row_fingerprint
          description: Non-PII strategy used to keep same-name employees from being merged by name alone.
        identity_resolution_confidence:
          type: string
          enum:
            - high
            - medium
            - low
          description: Confidence derived from the accepted identity strategy; never from user-supplied labels.
        identity_review_required:
          type: boolean
          description: True when HR must manually verify the employee identity before account linkage or duplicate cleanup.
        identity_name_only_merge:
          type: boolean
          description: Always false; name-only merging is not allowed because different people can share a name.
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    EmployeeAttendanceRecord:
      type: object
      required:
        - id
        - employee_id
        - employee_display_name
        - kind
        - occurred_at
        - work_date
        - state_after
        - payroll_material_ref_id
        - payroll_link_status
        - duplicate
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        employee_id:
          $ref: '#/components/schemas/Uuid'
        employee_display_name:
          type: string
        kind:
          $ref: '#/components/schemas/AttendanceRecordKind'
        occurred_at:
          $ref: '#/components/schemas/Timestamp'
        work_date:
          type: string
          format: date
        state_after:
          type: string
          enum:
            - CLOCKED_IN
            - OUT_FOR_WORK
            - BUSINESS_TRIP
            - OFF_DUTY
        note:
          type:
            - string
            - 'null'
        payroll_material_ref_id:
          $ref: '#/components/schemas/Uuid'
        payroll_link_status:
          type: string
          enum:
            - LINKED
        duplicate:
          type: boolean
    EmployeeAttendanceRecordPage:
      type: object
      required:
      - items
      - total
      - limit
      - offset
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeAttendanceRecord'
        total:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
    EmployeeDetail:
      type: object
      required: [employee, employment]
      properties:
        employee:
          $ref: '#/components/schemas/Employee'
        employment:
          $ref: '#/components/schemas/EmployeeEmploymentDetail'
    EmployeeEmploymentDetail:
      type: object
      required: [employment_type, phone_e164, base_pay, currency]
      properties:
        employment_type:
          type: string
          enum: [REGULAR, CONTRACT, PART_TIME, INTERN]
        phone_e164:
          type: string
        base_pay:
          type: string
          minLength: 4
          maxLength: 15
          pattern: '^(0|[1-9][0-9]{0,11})\.[0-9]{2}$'
        currency:
          type: string
          enum: [KRW]
    EmployeeHomeBranch:
      type: object
      required:
      - employee_id
      - branch_id
      - branch_name
      - updated_at
      properties:
        employee_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        branch_name:
          type: string
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    EmployeeImportColumn:
      type: object
      required:
        - source_header
        - normalized_header
        - classification
        - preview_allowed
      properties:
        source_header:
          type: string
        normalized_header:
          type: string
        target:
          oneOf:
            - {type: string, enum: [name, employee_number, org_unit, job, position, worksite_name, worksite_address, hire_date, exit_date, leave_accrued, leave_used, leave_remaining, company]}
            - {type: 'null'}
        classification:
          type: string
          enum:
            - canonical
            - retained
            - restricted
            - location
        preview_allowed:
          type: boolean
    EmployeeImportCompanySummary:
      type: object
      required:
      - company
      - input_rows
      - inserted
      - updated
      - skipped
      properties:
        company:
          type: string
        input_rows:
          type: integer
          minimum: 0
        inserted:
          type: integer
          minimum: 0
        updated:
          type: integer
          minimum: 0
        skipped:
          type: integer
          minimum: 0
    EmployeeImportDryRunSummary:
      type: object
      required:
      - run_id
      - input_rows
      - candidate_rows
      - preserved_rows
      - insert_candidates
      - update_candidates
      - companies
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        input_rows:
          type: integer
          minimum: 0
        candidate_rows:
          type: integer
          minimum: 0
        preserved_rows:
          type: integer
          minimum: 0
        insert_candidates:
          type: integer
          minimum: 0
        update_candidates:
          type: integer
          minimum: 0
        companies:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeImportCompanySummary'
    EmployeeImportPreviewResponse:
      type: object
      required:
      - run_id
      - entity_type
      - source_filename
      - source_sha256
      - input_rows
      - candidate_rows
      - preserved_rows
      - columns
      - sample_rows
      - mapping_profile
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        entity_type:
          type: string
          enum:
          - employee_hr
        source_filename:
          type: string
        source_sha256:
          type: string
          pattern: ^[a-f0-9]{64}$
        input_rows:
          type: integer
          minimum: 0
        candidate_rows:
          type: integer
          minimum: 0
        preserved_rows:
          type: integer
          minimum: 0
        columns:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeImportColumn'
        sample_rows:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeImportPreviewRow'
        mapping_profile:
          type: object
          additionalProperties: true
    EmployeeImportPreviewRow:
      type: object
      required:
      - source_sheet
      - source_row
      - row_status
      - values
      properties:
        source_sheet:
          type: string
        source_row:
          type: integer
          format: int32
          minimum: 1
        row_status:
          type: string
          enum:
          - CANDIDATE
          - PRESERVED
          - ERROR
        values:
          type: object
          additionalProperties: true
    EmployeeImportReport:
      type: object
      required:
      - input_rows
      - inserted
      - updated
      - skipped
      - companies
      properties:
        input_rows:
          type: integer
          minimum: 0
        inserted:
          type: integer
          minimum: 0
        updated:
          type: integer
          minimum: 0
        skipped:
          type: integer
          minimum: 0
        companies:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeImportCompanySummary'
    EmployeeLifecycleEvent:
      type: object
      required:
        - id
        - employee_id
        - event_type
        - to_status
        - effective_date
        - comment
        - signoffs
        - created_by
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        employee_id:
          $ref: '#/components/schemas/Uuid'
        event_type:
          type: string
          enum:
            - ONBOARD
            - OFFBOARD
            - TERMINATE
            - TRANSFER
        from_status:
          type:
            - string
            - 'null'
        to_status:
          type: string
          enum:
            - ACTIVE
            - EXITED
            - UNKNOWN
        from_company:
          type:
            - string
            - 'null'
        to_company:
          type:
            - string
            - 'null'
        from_org_unit:
          type:
            - string
            - 'null'
        to_org_unit:
          type:
            - string
            - 'null'
        from_position:
          type:
            - string
            - 'null'
        to_position:
          type:
            - string
            - 'null'
        effective_date:
          type: string
        comment:
          type: string
        signoffs:
          $ref: '#/components/schemas/EmployeeLifecycleSignoffs'
        created_by:
          $ref: '#/components/schemas/Uuid'
        created_at:
          $ref: '#/components/schemas/Timestamp'
    EmployeeLifecycleEventPage:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeLifecycleEvent'
    EmployeeLifecycleSignoffs:
      type: object
      required:
      - privacy_notice_ack
      - korean_labor_law_ack
      - payroll_cutoff_ack
      - retirement_settlement_ack
      properties:
        privacy_notice_ack:
          type: boolean
          description: Confirms the employee was notified of personal-data processing for this HR action.
        korean_labor_law_ack:
          type: boolean
          description: Confirms Korean labor-law and employment-rule review.
        payroll_cutoff_ack:
          type: boolean
          description: Confirms payroll cutoff and final wage impact were reviewed.
        retirement_settlement_ack:
          type: boolean
          description: Confirms severance/retirement settlement impact was reviewed.
    EmployeePage:
      type: object
      required:
      - items
      - total
      - limit
      - offset
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Employee'
        total:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
    Employment:
      type: object
      description: Canonical Employment head. Current get/list still return the *open* head (`employment_heads.valid_to IS NULL`). GET /api/v1/employments/{id}?as_of= reconstructs the slice effective at an RFC3339 instant using the same half-open predicate as ontology instance GET (`valid_from <= as_of < coalesce(valid_to, ∞)`); absent as_of = current open head. GET /api/v1/employments?from=&to= lists heads whose `[valid_from, valid_to)` overlaps `[from, to)` with that same algebra (absent both = current open heads). Closed (EXITED) windows are omitted by current get/list and by as_of at or after valid_to. `org_unit_id` / `job_position_id` are canonical UUIDs from the effective revision, not free-text team or title labels. `person_id` is the unique source-binding → person-binding path; ambiguous or unbound identities are null, never invented from `employee_id`. `appointed_on` is the head's opening `valid_from`. `version` is `employment_revisions.version` on that already-joined effective row (MAX(valid_from), not MAX(version)). Write attributes also carry `company` and `employment_status` in {ACTIVE, EXITED, UNKNOWN}; those write fields are not this Head. The Head never includes phone, salary, bank_account, rrn, or base_pay. Corrections vs new slice and delta transmission remain HOLD. EmployeeDetail as_of remains HOLD.
      required:
        - id
        - version
        - appointed_on
        - person_id
        - org_unit_id
        - job_position_id
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        person_id:
          oneOf:
            - $ref: "#/components/schemas/Uuid"
            - type: "null"
        org_unit_id:
          oneOf:
            - $ref: "#/components/schemas/Uuid"
            - type: "null"
        job_position_id:
          oneOf:
            - $ref: "#/components/schemas/Uuid"
            - type: "null"
        appointed_on:
          $ref: "#/components/schemas/Timestamp"
        version:
          type: integer
          format: int64
          minimum: 1
          description: Optimistic concurrency token for this Head. Mutating roster actions send it as OntologyActionRequest.expected_revision (JSON body, not HTTP If-Match).
      links:
        - key: employment_person
          from: Employment
          to: Person
          field: person_id
          cardinality: many-to-one
          option: true
          operationId: getPerson
        - key: employment_org_unit
          from: Employment
          to: OrgUnit
          field: org_unit_id
          cardinality: many-to-one
          option: true
          operationId: getOrgUnit
        - key: employment_job_position
          from: Employment
          to: JobPosition
          field: job_position_id
          cardinality: many-to-one
          option: true
          operationId: getJobPosition
      actions:
        - action_key: hr.appoint
          object_key: employment
          input:
            $ref: "#/components/schemas/HrAppointInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: natural_person
          edits:
            - employment_heads
            - employment_revisions
            - employment_source_bindings
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
        - action_key: hr.promote
          object_key: employment
          input:
            $ref: "#/components/schemas/HrPromoteInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: natural_person
          edits:
            - employment_revisions
            - employees
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
        - action_key: hr.transfer
          object_key: employment
          input:
            $ref: "#/components/schemas/HrTransferInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: natural_person
          edits:
            - employment_revisions
            - employees
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
      concurrency:
        get_token: version
        write_field: expected_revision
        write_in: body
      permissions:
        - employee_directory_read
    EmploymentAttributesInput:
      type: object
      additionalProperties: false
      description: Write bag for `hr.appoint` / `hr.promote` / `hr.transfer` (`EmploymentAttributes`). Not the Employment Head. `company` is a non-blank string; `employment_status` is the 0066 CHECK set. UUID refs must not be nil.
      required:
        - company
        - employment_status
      properties:
        company:
          type: string
          minLength: 1
        org_unit_id:
          oneOf:
            - $ref: "#/components/schemas/Uuid"
            - type: "null"
        job_position_id:
          oneOf:
            - $ref: "#/components/schemas/Uuid"
            - type: "null"
        employment_status:
          type: string
          enum:
            - ACTIVE
            - EXITED
            - UNKNOWN
    EnrollHandoffRequest:
      type: object
      description: Cross-device passkey-enrollment handoff request. Carries NO user/org — those come from the verified access
        token, so a caller can only mint a handoff for itself. `step_up` is REQUIRED only when the caller already has a passkey
        (adding a device); a mid-onboarding caller (zero passkeys) omits it.
      properties:
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    EnrollHandoffResponse:
      type: object
      description: The minted single-use, short-lived passkey-enrollment code (returned once, only its hash is stored) plus
        the ready-to-encode enrollment URL the frontend renders as a QR. The phone opens `enroll_url`, redeems `otp` via the
        first-sign-in path, enrolls a platform passkey, then approves the paired desktop poll token when `desktop_approve`
        is present in the URL fragment.
      required:
      - otp
      - expires_at
      - enroll_url
      - poll_token
      properties:
        otp:
          type: string
          description: The single-use enrollment handoff code.
        expires_at:
          $ref: '#/components/schemas/Timestamp'
        enroll_url:
          type: string
          description: The console enrollment URL carrying the handoff code in the fragment (`{origin}/login#otp=<code>&desktop_approve=<approve-token>`),
            to be rendered as a QR and scanned on a phone.
        poll_token:
          type: string
          description: Desktop-only poll token paired to the phone enrollment QR. The desktop polls the device-login endpoint
            with this token and receives its own session once the phone enrollment approves the handoff.
    Equipment3rCaseDetailView:
      allOf:
        - $ref: '#/components/schemas/Equipment3rCaseView'
        - type: object
          required:
            - inspections
            - createdBy
            - createdAt
            - updatedAt
          properties:
            approval: {type: [object, 'null'], properties: {decision: {type: string, enum: [APPROVED, DECLINED]}, reason: {type: [string, 'null']}, decidedBy: {$ref: '#/components/schemas/Uuid'}, decidedAt: {type: string, format: date-time}}}
            dispatch: {type: [object, 'null'], properties: {carrierName: {type: string}, vehicleReference: {type: string}, dispatchedAt: {type: string, format: date-time}}}
            handover: {type: [object, 'null'], properties: {recipientName: {type: string}, evidenceObjectId: {$ref: '#/components/schemas/Uuid'}, handedOverAt: {type: string, format: date-time}}}
            returnedAt: {type: [string, 'null'], format: date-time}
            assessment: {type: [object, 'null'], properties: {conditionGrade: {type: string, enum: [A, B, C, D]}, findings: {type: string}, disposition: {type: string, enum: [REPAIR, REFURBISH, RESALE, REDEPLOY]}, assessedBy: {$ref: '#/components/schemas/Uuid'}, assessedAt: {type: string, format: date-time}}}
            dispositionId: {oneOf: [{$ref: '#/components/schemas/Uuid'}, {type: 'null'}]}
            inspections: {type: array, items: {$ref: '#/components/schemas/Equipment3rInspectionView'}}
            createdBy: {$ref: '#/components/schemas/Uuid'}
            createdAt: {type: string, format: date-time}
            updatedAt: {type: string, format: date-time}
    Equipment3rCaseView:
      type: object
      required: [id, unitId, status, customerName, siteReference, monthlyRateMinor, durationMonths, currencyCode, branchId]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        unitId: { $ref: '#/components/schemas/Uuid' }
        status: { type: string, enum: [QUOTED, APPROVED, DECLINED, DISPATCHED, HANDED_OVER, RETURNED, CLOSED] }
        customerName: { type: string }
        siteReference: { type: string }
        monthlyRateMinor: { type: integer, format: int64 }
        durationMonths: { type: integer }
        currencyCode: { type: string, enum: [KRW] }
        branchId: { $ref: '#/components/schemas/Uuid' }
        replayed: { type: boolean, description: Present and true only on an idempotent replay }
    Equipment3rDispositionView:
      type: object
      required:
        - id
        - unitId
        - caseId
        - kind
        - status
        - completedBy
        - completedAt
        - financeGlPosting
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        unitId:
          $ref: '#/components/schemas/Uuid'
        caseId:
          $ref: '#/components/schemas/Uuid'
        kind:
          type: string
          enum:
            - REPAIR
            - REFURBISH
            - RESALE
            - REDEPLOY
        status:
          type: string
          enum:
            - OPEN
            - COMPLETED
        costMinor:
          type:
            - integer
            - 'null'
          format: int64
        saleAmountMinor:
          type:
            - integer
            - 'null'
          format: int64
        buyerName:
          type:
            - string
            - 'null'
        completedBy:
          $ref: '#/components/schemas/Uuid'
        completedAt:
          type: string
          format: date-time
        financeGlPosting:
          description: Always null — the pilot records no GL posting
          type: 'null'
    Equipment3rHistoryEntry:
      type: object
      required: [aggregateKind, aggregateId, transition, actorId, occurredAt]
      properties:
        aggregateKind: { type: string, enum: [unit, case, disposition] }
        aggregateId: { $ref: '#/components/schemas/Uuid' }
        transition: { type: string }
        actorId: { $ref: '#/components/schemas/Uuid' }
        occurredAt: { type: string, format: date-time }
    Equipment3rInspectionView:
      type: object
      required:
        - id
        - caseId
        - outcome
        - findings
        - inspectedBy
        - inspectedAt
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        caseId:
          $ref: '#/components/schemas/Uuid'
        outcome:
          type: string
          enum:
            - PASS
            - MAINTENANCE_PERFORMED
        findings:
          type: string
        maintenanceNote:
          type:
            - string
            - 'null'
        inspectedBy:
          $ref: '#/components/schemas/Uuid'
        inspectedAt:
          type: string
          format: date-time
    Equipment3rUnitDetailView:
      allOf:
        - $ref: '#/components/schemas/Equipment3rUnitView'
        - type: object
          required:
            - createdAt
            - updatedAt
          properties:
            activeCaseId: {oneOf: [{$ref: '#/components/schemas/Uuid'}, {type: 'null'}]}
            openDispositionId: {oneOf: [{$ref: '#/components/schemas/Uuid'}, {type: 'null'}]}
            createdAt: {type: string, format: date-time}
            updatedAt: {type: string, format: date-time}
    Equipment3rUnitView:
      type: object
      required: [id, serialNo, modelName, capacityClass, availability, acquisitionCostMinor, branchId]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        serialNo: { type: string }
        modelName: { type: string }
        capacityClass: { type: string }
        availability: { type: string, enum: [AVAILABLE, RESERVED, ON_RENT, IN_ASSESSMENT, IN_REPAIR, IN_REFURBISHMENT, FOR_SALE, SOLD] }
        acquisitionCostMinor: { type: integer, format: int64 }
        branchId: { $ref: '#/components/schemas/Uuid' }
    EquipmentAutocompletePage:
      type: object
      required:
      - items
      - limit
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/EquipmentLookupResponse'
        limit:
          type: integer
          format: int64
    EquipmentByLocationPage:
      type: object
      required:
      - items
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SiteLocationGroup'
        total:
          type: integer
          minimum: 0
    EquipmentGraphEdge:
      type: object
      required:
      - from
      - to
      - kind
      - label
      properties:
        from:
          type: string
        to:
          type: string
        kind:
          type: string
        label:
          type: string
    EquipmentGraphNode:
      type: object
      required:
      - id
      - node_type
      - label
      - current
      properties:
        id:
          type: string
        node_type:
          type: string
        label:
          type: string
        subtitle:
          type:
          - string
          - 'null'
        href:
          type:
          - string
          - 'null'
        current:
          type: boolean
    EquipmentLifecycleEvent:
      type: object
      required:
      - id
      - kind
      - label
      properties:
        id:
          type: string
        kind:
          type: string
        label:
          type: string
        description:
          type:
          - string
          - 'null'
        event_date:
          oneOf:
          - $ref: '#/components/schemas/Date'
          - type: 'null'
        occurred_at:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        href:
          type:
          - string
          - 'null'
    EquipmentListItem:
      type: object
      required:
      - equipment_id
      - branch_id
      - equipment_no
      - status
      - specification
      - ton_text
      - customer_name
      - site_name
      - updated_at
      properties:
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_no:
          type: string
        management_no:
          type:
          - string
          - 'null'
        status:
          $ref: '#/components/schemas/EquipmentStatus'
        model:
          type:
          - string
          - 'null'
        maker:
          type:
          - string
          - 'null'
        specification:
          type: string
        ton_text:
          type: string
        customer_name:
          type: string
        site_name:
          type: string
        asset_owner:
          type:
          - string
          - 'null'
          description: Legal owner recorded on the asset master. Distinct from customer_name/site_name, which describe the
            current operator/site.
        vin:
          type:
          - string
          - 'null'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    EquipmentListPage:
      type: object
      required:
      - items
      - total
      - limit
      - offset
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/EquipmentListItem'
        total:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
    EquipmentLookupResponse:
      type: object
      required:
      - id
      - branch_id
      - equipment_no
      - management_no
      - model
      - status
      - specification
      - ton_text
      - maker
      - vin
      - vehicle_registration_no
      - customer
      - site
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_no:
          type: string
        management_no:
          type:
          - string
          - 'null'
        model:
          type:
          - string
          - 'null'
        status:
          type: string
        specification:
          type: string
        ton_text:
          type: string
        maker:
          type:
          - string
          - 'null'
        vin:
          type:
          - string
          - 'null'
        vehicle_registration_no:
          type:
          - string
          - 'null'
        customer:
          $ref: '#/components/schemas/NamedEntity'
        site:
          $ref: '#/components/schemas/NamedEntity'
    EquipmentRelationshipGraph:
      type: object
      required:
      - nodes
      - edges
      properties:
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/EquipmentGraphNode'
        edges:
          type: array
          items:
            $ref: '#/components/schemas/EquipmentGraphEdge'
    EquipmentRollbackResult:
      type: object
      required:
      - version
      properties:
        version:
          type: integer
          format: int32
    EquipmentSortBy:
      type: string
      enum:
      - equipment_no
      - model
      - customer
      - updated_at
    EquipmentStatus:
      type: string
      enum:
      - rented
      - spare
      - disposed
      - replacement
      - sold
    EquipmentSummary:
      type: object
      required:
      - id
      - equipment_no
      - management_no
      - model
      - status
      - specification
      - ton_text
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        equipment_no:
          type: string
        management_no:
          type:
          - string
          - 'null'
        model:
          type:
          - string
          - 'null'
        status:
          type: string
        specification:
          type: string
        ton_text:
          type: string
    EquipmentTimelineEquipment:
      type: object
      required:
      - equipment_id
      - branch_id
      - equipment_no
      - status
      - customer_id
      - customer_name
      - site_id
      - site_name
      properties:
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_no:
          type: string
        management_no:
          type:
          - string
          - 'null'
        status:
          $ref: '#/components/schemas/EquipmentStatus'
        model:
          type:
          - string
          - 'null'
        maker:
          type:
          - string
          - 'null'
        customer_id:
          $ref: '#/components/schemas/Uuid'
        customer_name:
          type: string
        site_id:
          $ref: '#/components/schemas/Uuid'
        site_name:
          type: string
    EquipmentTimelineGraph:
      type: object
      required:
      - equipment
      - lifecycle_events
      - graph
      - work_order_count
      - cost_ledger_total_won
      properties:
        equipment:
          $ref: '#/components/schemas/EquipmentTimelineEquipment'
        lifecycle_events:
          type: array
          items:
            $ref: '#/components/schemas/EquipmentLifecycleEvent'
        graph:
          $ref: '#/components/schemas/EquipmentRelationshipGraph'
        work_order_count:
          type: integer
          format: int64
        cost_ledger_total_won:
          type: integer
          format: int64
    EquipmentVersion:
      type: object
      required:
      - version
      - status
      - content
      - createdAt
      properties:
        version:
          type: integer
          format: int32
        status:
          type: string
          enum: [CAPTURED, ROLLBACK]
        sourceVersion:
          type: integer
          format: int32
        content:
          type: object
          additionalProperties: true
        createdBy:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
    EquipmentVersionList:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/EquipmentVersion'
    ErrorBody:
      type: object
      required:
      - error
      properties:
        error:
          type: object
          required:
          - code
          - message
          properties:
            code:
              type: string
            message:
              type: string
            reasons:
              type: array
              items:
                type: string
              description: Stable machine-readable reasons for review-required conflicts, when applicable.
            current_key_write_revision:
              type: integer
              format: int64
              minimum: 1
    EvaluationCycleDetail:
      type: object
      additionalProperties: false
      required: [id, name, kind, period_label, due_date, stage, subjects_total, manager_submitted, self_submitted, calibrated, finalized, created_at, opened_at, calibration_started_at, finalized_at, archived_at, created_by, progress_by_unit, subjects]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        name: { type: string }
        kind: { $ref: '#/components/schemas/EvaluationCycleKind' }
        period_label: { type: string }
        due_date: { $ref: '#/components/schemas/Date' }
        stage: { $ref: '#/components/schemas/EvaluationCycleStage' }
        subjects_total: { type: integer, format: int64 }
        manager_submitted: { type: integer, format: int64 }
        self_submitted: { type: integer, format: int64 }
        calibrated: { type: integer, format: int64 }
        finalized: { type: integer, format: int64 }
        created_at: { $ref: '#/components/schemas/Timestamp' }
        opened_at: { oneOf: [{ $ref: '#/components/schemas/Timestamp' }, { type: 'null' }] }
        calibration_started_at: { oneOf: [{ $ref: '#/components/schemas/Timestamp' }, { type: 'null' }] }
        finalized_at: { oneOf: [{ $ref: '#/components/schemas/Timestamp' }, { type: 'null' }] }
        archived_at: { oneOf: [{ $ref: '#/components/schemas/Timestamp' }, { type: 'null' }] }
        created_by: { $ref: '#/components/schemas/Uuid' }
        progress_by_unit: { type: array, items: { $ref: '#/components/schemas/EvaluationUnitProgress' } }
        subjects: { type: array, items: { $ref: '#/components/schemas/EvaluationSubjectSummary' } }
    EvaluationCycleKind:
      type: string
      enum: [REGULAR, PROBATION]
    EvaluationCyclePage:
      type: object
      additionalProperties: false
      required: [items, total]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/EvaluationCycleSummary' } }
        total: { type: integer, format: int64 }
    EvaluationCycleStage:
      type: string
      enum: [DRAFT, OPEN, CALIBRATION, FINALIZED, ARCHIVED]
    EvaluationCycleSummary:
      type: object
      additionalProperties: false
      required: [id, name, kind, period_label, due_date, stage, subjects_total, manager_submitted, self_submitted, calibrated, finalized, created_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        name: { type: string }
        kind: { $ref: '#/components/schemas/EvaluationCycleKind' }
        period_label: { type: string }
        due_date: { $ref: '#/components/schemas/Date' }
        stage: { $ref: '#/components/schemas/EvaluationCycleStage' }
        subjects_total: { type: integer, format: int64 }
        manager_submitted: { type: integer, format: int64 }
        self_submitted: { type: integer, format: int64 }
        calibrated: { type: integer, format: int64 }
        finalized: { type: integer, format: int64 }
        created_at: { $ref: '#/components/schemas/Timestamp' }
    EvaluationCycleTransition:
      type: string
      enum: [open, start_calibration, finalize, archive]
    EvaluationEvidenceKind:
      type: string
      enum: [ATTENDANCE, WORK_ORDER, APPROVAL, KPI, OTHER]
    EvaluationEvidenceLink:
      type: object
      additionalProperties: false
      required: [id, object_kind, object_ref, label, sort_order]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        object_kind: { $ref: '#/components/schemas/EvaluationEvidenceKind' }
        object_ref: { type: string }
        label: { type: string }
        sort_order: { type: integer, format: int32 }
    EvaluationEvidenceLinkInput:
      type: object
      additionalProperties: false
      required: [object_kind, object_ref, label]
      properties:
        object_kind: { $ref: '#/components/schemas/EvaluationEvidenceKind' }
        object_ref: { type: string, minLength: 1, maxLength: 120 }
        label: { type: string, minLength: 1, maxLength: 200 }
    EvaluationGoal:
      type: object
      additionalProperties: false
      required: [id, title, metric_kind, target_label, weight_pct, sort_order]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        title: { type: string }
        metric_kind: { $ref: '#/components/schemas/EvaluationMetricKind' }
        target_label: { type: string }
        weight_pct: { type: integer, format: int16 }
        sort_order: { type: integer, format: int32 }
    EvaluationGoalInput:
      type: object
      additionalProperties: false
      required: [title, metric_kind, target_label, weight_pct]
      properties:
        title: { type: string, minLength: 1, maxLength: 200 }
        metric_kind: { $ref: '#/components/schemas/EvaluationMetricKind' }
        target_label: { type: string, minLength: 1, maxLength: 200 }
        weight_pct: { type: integer, format: int16, minimum: 0, maximum: 100 }
    EvaluationGrade:
      type: string
      enum: [S, A, B, C, D]
    EvaluationLedgerEntry:
      type: object
      additionalProperties: false
      required: [rv_code, cycle_id, cycle_name, period_label, final_grade, finalized_at, subject_id]
      properties:
        rv_code: { type: string }
        cycle_id: { $ref: '#/components/schemas/Uuid' }
        cycle_name: { type: string }
        period_label: { type: string }
        final_grade: { $ref: '#/components/schemas/EvaluationGrade' }
        finalized_at: { $ref: '#/components/schemas/Timestamp' }
        subject_id: { $ref: '#/components/schemas/Uuid' }
    EvaluationLedgerPage:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/EvaluationLedgerEntry' } }
    EvaluationMetricKind:
      type: string
      enum: [KPI, ATTENDANCE, TASK, CUSTOM]
    EvaluationPreflightItem:
      type: object
      additionalProperties: false
      required: [code, message, subject_id]
      properties:
        code: { type: string }
        message: { type: string }
        subject_id: { oneOf: [{ $ref: '#/components/schemas/Uuid' }, { type: 'null' }] }
    EvaluationPreflightReport:
      type: object
      additionalProperties: false
      required: [next_transition, blockers, advisories]
      properties:
        next_transition: { oneOf: [{ $ref: '#/components/schemas/EvaluationCycleTransition' }, { type: 'null' }] }
        blockers: { type: array, items: { $ref: '#/components/schemas/EvaluationPreflightItem' } }
        advisories: { type: array, items: { $ref: '#/components/schemas/EvaluationPreflightItem' } }
    EvaluationReview:
      type: object
      additionalProperties: false
      required: [id, subject_id, kind, status, evaluator_user_id, grade, note, evidence_links, submitted_at, updated_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        subject_id: { $ref: '#/components/schemas/Uuid' }
        kind: { $ref: '#/components/schemas/EvaluationReviewKind' }
        status: { $ref: '#/components/schemas/EvaluationReviewStatus' }
        evaluator_user_id: { $ref: '#/components/schemas/Uuid' }
        grade: { oneOf: [{ $ref: '#/components/schemas/EvaluationGrade' }, { type: 'null' }] }
        note: { type: [string, 'null'] }
        evidence_links: { type: array, items: { $ref: '#/components/schemas/EvaluationEvidenceLink' } }
        submitted_at: { oneOf: [{ $ref: '#/components/schemas/Timestamp' }, { type: 'null' }] }
        updated_at: { $ref: '#/components/schemas/Timestamp' }
    EvaluationReviewKind:
      type: string
      enum: [SELF, MANAGER]
    EvaluationReviewStatus:
      type: string
      enum: [DRAFT, SUBMITTED]
    EvaluationSubjectDetail:
      type: object
      additionalProperties: false
      required: [id, cycle_id, employee_id, employee_name, org_unit, manager_user_id, state, final_grade, rv_code, goals, reviews, calibrated_grade, calibration_reason, calibrated_by, calibrated_at, finalized_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        cycle_id: { $ref: '#/components/schemas/Uuid' }
        employee_id: { $ref: '#/components/schemas/Uuid' }
        employee_name: { type: string }
        org_unit: { type: [string, 'null'] }
        manager_user_id: { $ref: '#/components/schemas/Uuid' }
        state: { $ref: '#/components/schemas/EvaluationSubjectState' }
        final_grade: { oneOf: [{ $ref: '#/components/schemas/EvaluationGrade' }, { type: 'null' }] }
        rv_code: { type: [string, 'null'] }
        goals: { type: array, items: { $ref: '#/components/schemas/EvaluationGoal' } }
        reviews: { type: array, items: { $ref: '#/components/schemas/EvaluationReview' } }
        calibrated_grade: { oneOf: [{ $ref: '#/components/schemas/EvaluationGrade' }, { type: 'null' }] }
        calibration_reason: { type: [string, 'null'] }
        calibrated_by: { oneOf: [{ $ref: '#/components/schemas/Uuid' }, { type: 'null' }] }
        calibrated_at: { oneOf: [{ $ref: '#/components/schemas/Timestamp' }, { type: 'null' }] }
        finalized_at: { oneOf: [{ $ref: '#/components/schemas/Timestamp' }, { type: 'null' }] }
    EvaluationSubjectState:
      type: string
      enum: [ENROLLED, IN_REVIEW, REVIEWED, CALIBRATED, FINALIZED]
    EvaluationSubjectSummary:
      type: object
      additionalProperties: false
      required: [id, cycle_id, employee_id, employee_name, org_unit, manager_user_id, state, final_grade, rv_code]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        cycle_id: { $ref: '#/components/schemas/Uuid' }
        employee_id: { $ref: '#/components/schemas/Uuid' }
        employee_name: { type: string }
        org_unit: { type: [string, 'null'] }
        manager_user_id: { $ref: '#/components/schemas/Uuid' }
        state: { $ref: '#/components/schemas/EvaluationSubjectState' }
        final_grade: { oneOf: [{ $ref: '#/components/schemas/EvaluationGrade' }, { type: 'null' }] }
        rv_code: { type: [string, 'null'] }
    EvaluationTaskItem:
      type: object
      additionalProperties: false
      required: [subject_id, cycle_id, cycle_name, due_date, employee_id, employee_name, kind, review_status]
      properties:
        subject_id: { $ref: '#/components/schemas/Uuid' }
        cycle_id: { $ref: '#/components/schemas/Uuid' }
        cycle_name: { type: string }
        due_date: { $ref: '#/components/schemas/Date' }
        employee_id: { $ref: '#/components/schemas/Uuid' }
        employee_name: { type: string }
        kind: { $ref: '#/components/schemas/EvaluationReviewKind' }
        review_status: { oneOf: [{ $ref: '#/components/schemas/EvaluationReviewStatus' }, { type: 'null' }] }
    EvaluationTaskPage:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/EvaluationTaskItem' } }
    EvaluationUnitProgress:
      type: object
      additionalProperties: false
      required: [org_unit, total, manager_submitted]
      properties:
        org_unit: { type: [string, 'null'] }
        total: { type: integer, format: int64 }
        manager_submitted: { type: integer, format: int64 }
    EvidenceBinding:
      type: object
      required: [id, control_id, evidence_target_type, evidence_target_id, status, confidence, metadata, created_by, updated_by, created_at, updated_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        control_id: { $ref: '#/components/schemas/Uuid' }
        obligation_id: { type: [string, 'null'], format: uuid }
        evidence_target_type: { $ref: '#/components/schemas/EvidenceTargetType' }
        evidence_target_id: { type: string }
        source_audit_event_id: { type: [string, 'null'], format: uuid }
        status: { $ref: '#/components/schemas/EvidenceBindingStatus' }
        confidence: { $ref: '#/components/schemas/EvidenceConfidence' }
        collected_at: { type: [string, 'null'], format: date-time }
        collected_by: { type: [string, 'null'], format: uuid }
        valid_from: { type: [string, 'null'], format: date }
        valid_to: { type: [string, 'null'], format: date }
        hash_sha256: { type: [string, 'null'] }
        metadata: { type: object, additionalProperties: true }
        created_by: { $ref: '#/components/schemas/Uuid' }
        updated_by: { $ref: '#/components/schemas/Uuid' }
        created_at: { $ref: '#/components/schemas/Timestamp' }
        updated_at: { $ref: '#/components/schemas/Timestamp' }
    EvidenceBindingPage:
      type: object
      required: [items, limit, offset, total]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/EvidenceBinding' } }
        limit: { type: integer, format: int64 }
        offset: { type: integer, format: int64 }
        total: { type: integer, format: int64 }
    EvidenceBindingStatus:
      type: string
      enum: [PROPOSED, ACCEPTED, REJECTED, EXPIRED, RETRACTED]
    EvidenceClassification:
      { type: string, enum: [General, Internal, Sensitive, Confidential, Secret] }
    EvidenceConfidence:
      type: string
      enum: [LOW, MEDIUM, HIGH, SYSTEM]
    EvidenceConfirmResponse:
      type: object
      required:
      - id
      - work_order_id
      - stage
      - worm_replica_status
      - retry_count
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        stage:
          $ref: '#/components/schemas/AttachmentStage'
        worm_replica_status:
          $ref: '#/components/schemas/WormReplicaStatus'
        retry_count:
          type: integer
          format: int32
        verified_at:
          $ref: '#/components/schemas/Timestamp'
    EvidenceCopyEvidentiaryStatus:
      type: string
      description: Server-derived classification; verified replicas of derivative copies never become evidence-equivalent originals.
      enum: [VERIFIED_ORIGINAL, ORIGINAL_UNVERIFIED, NON_EVIDENTIARY_DERIVATIVE]
    EvidenceCopyKind:
      { type: string, enum: [ORIGINAL, DERIVATIVE] }
    EvidenceCopyView:
      type: object
      required:
        - id
        - evidence_object_id
        - copy_kind
        - evidentiary_status
        - storage
        - digest_sha256
        - content_type
        - size_bytes
        - worm_status
        - created_by
        - created_at
      properties:
        id:
          type: string
          format: uuid
        evidence_object_id:
          type: string
          format: uuid
        copy_kind:
          $ref: '#/components/schemas/EvidenceCopyKind'
        evidentiary_status:
          $ref: '#/components/schemas/EvidenceCopyEvidentiaryStatus'
        derivative_kind:
          oneOf:
            - {type: string, enum: [REDACTED, THUMBNAIL, TRANSCODED, EXCERPT, EXPORT_MANIFEST, NORMALIZED_TEXT, OTHER]}
            - {type: 'null'}
        parent_copy_id:
          type:
            - string
            - 'null'
          format: uuid
        storage:
          $ref: '#/components/schemas/EvidenceStorageRef'
        source_evidence_media_id:
          type:
            - string
            - 'null'
          format: uuid
        digest_sha256:
          type: string
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        worm_status:
          $ref: '#/components/schemas/WormStorageStatus'
        verified_at:
          type:
            - string
            - 'null'
          format: date-time
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
    EvidenceExportView:
      type: object
      required:
        - id
        - evidence_object_id
        - manifest_digest_sha256
        - signature_algorithm
        - export_reason
        - exported_by
        - exported_at
      properties:
        id:
          type: string
          format: uuid
        evidence_object_id:
          type: string
          format: uuid
        manifest_digest_sha256:
          type: string
        signature_algorithm:
          type: string
        signature_ref:
          type:
            - string
            - 'null'
        export_reason:
          type: string
        exported_by:
          type: string
          format: uuid
        exported_at:
          type: string
          format: date-time
    EvidenceHoldRequest:
      oneOf:
        - type: object
          required: [op, case_ref, basis, reason]
          properties:
            op: { type: string, enum: [apply] }
            case_ref: { type: string }
            basis: { type: string }
            reason: { type: string }
        - type: object
          required: [op, hold_id, reason, four_eyes_request_ref]
          properties:
            op: { type: string, enum: [release] }
            hold_id: { type: string, format: uuid }
            reason: { type: string }
            four_eyes_request_ref: { type: string, format: uuid }
      discriminator: { propertyName: op }
    EvidenceObjectDetail:
      type: object
      required: [object, copies, tsa_proofs, custody_history, legal_holds, exports]
      properties:
        object: { $ref: '#/components/schemas/EvidenceObjectView' }
        copies: { type: array, items: { $ref: '#/components/schemas/EvidenceCopyView' } }
        tsa_proofs: { type: array, items: { $ref: '#/components/schemas/TimestampAuthorityProofView' } }
        custody_history: { type: array, items: { $ref: '#/components/schemas/CustodyEventView' } }
        legal_holds: { type: array, items: { $ref: '#/components/schemas/LegalHoldRecordView' } }
        exports: { type: array, items: { $ref: '#/components/schemas/EvidenceExportView' } }
    EvidenceObjectPage:
      type: object
      required: [items, limit, offset, total, as_of, next_cursor]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/EvidenceObjectView' } }
        limit: { type: integer, format: int64 }
        offset: { type: integer, format: int64 }
        total: { type: integer, format: int64 }
        as_of: { type: integer, format: int64, description: Immutable evidence-register sequence that fences every row in this response. }
        next_cursor:
          type:
          - string
          - 'null'
          description: Opaque unpadded-base64url continuation token, or null at the end of the snapshot.
    EvidenceObjectView:
      type: object
      required:
        - id
        - code
        - title
        - source
        - classification
        - current_custody_stage
        - legal_hold_state
        - admissibility_status
        - admissibility_reasons
        - admissibility_inputs
        - created_by
        - updated_by
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
        title:
          type: string
        description:
          type:
            - string
            - 'null'
        source:
          $ref: '#/components/schemas/EvidenceSourceRef'
        classification:
          $ref: '#/components/schemas/EvidenceClassification'
        record_owner_user_id:
          type:
            - string
            - 'null'
          format: uuid
        current_custody_stage:
          $ref: '#/components/schemas/CustodyStage'
        legal_hold_state:
          $ref: '#/components/schemas/LegalHoldState'
        admissibility_status:
          $ref: '#/components/schemas/AdmissibilityStatus'
        admissibility_reasons:
          type: array
          items:
            type: string
        admissibility_inputs:
          type: object
          additionalProperties: true
        created_by:
          type: string
          format: uuid
        updated_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        disposed_at:
          type:
            - string
            - 'null'
          format: date-time
    EvidencePresignRequest:
      type: object
      required:
      - work_order_id
      - stage
      - content_type
      - size_bytes
      properties:
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        stage:
          $ref: '#/components/schemas/AttachmentStage'
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
          minimum: 0
        checksum_sha256:
          type: string
    EvidencePresignResponse:
      type: object
      required:
      - id
      - work_order_id
      - stage
      - upload
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        stage:
          $ref: '#/components/schemas/AttachmentStage'
        upload:
          $ref: '#/components/schemas/PresignedUpload'
    EvidenceSourceRef:
      type: object
      required:
        - source_type
        - source_id
      properties:
        source_type:
          $ref: '#/components/schemas/EvidenceSourceType'
        source_id:
          type: string
        source_code:
          type:
            - string
            - 'null'
    EvidenceSourceType:
      { type: string, enum: [record_archive, inbox_doc, mail_attachment, ingest_job, work_order_evidence_media, external_document] }
    EvidenceStagingPresignRequest:
      type: object
      required:
      - work_order_id
      - stage
      - content_type
      - size_bytes
      properties:
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        stage:
          $ref: '#/components/schemas/AttachmentStage'
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
          minimum: 0
        checksum_sha256:
          type: string
    EvidenceStagingPresignResponse:
      type: object
      required:
      - id
      - work_order_id
      - stage
      - media_kind
      - processing_status
      - upload
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        stage:
          $ref: '#/components/schemas/AttachmentStage'
        media_kind:
          $ref: '#/components/schemas/MediaKind'
        processing_status:
          $ref: '#/components/schemas/ProcessingStatus'
        upload:
          $ref: '#/components/schemas/PresignedUpload'
    EvidenceStatusResponse:
      type: object
      required:
      - id
      - work_order_id
      - stage
      - processing_status
      - content_type
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        stage:
          $ref: '#/components/schemas/AttachmentStage'
        processing_status:
          $ref: '#/components/schemas/ProcessingStatus'
        content_type:
          type: string
        thumbnail_url:
          type: string
          format: uri
          description: Short-lived presigned GET URL for the generated thumbnail (absent until the row is READY). Replaces
            the previous thumbnail_s3_key, which exposed the internal object key; clients render this URL directly.
        processing_error:
          type: string
        processed_at:
          $ref: '#/components/schemas/Timestamp'
    EvidenceStorageRef:
      type: object
      required:
        - provider
        - object_id
      properties:
        provider:
          type: string
        object_id:
          type: string
        key_ref:
          type:
            - string
            - 'null'
        version_id:
          type:
            - string
            - 'null'
    EvidenceSummary:
      type: object
      required:
      - id
      - stage
      - content_type
      - size_bytes
      - uploaded_by
      - worm_replica_status
      - retry_count
      - verified_at
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        stage:
          $ref: '#/components/schemas/AttachmentStage'
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        uploaded_by:
          $ref: '#/components/schemas/Uuid'
        worm_replica_status:
          $ref: '#/components/schemas/WormReplicaStatus'
        retry_count:
          type: integer
          format: int32
        verified_at:
          type:
          - string
          - 'null'
          format: date-time
        created_at:
          $ref: '#/components/schemas/Timestamp'
    EvidenceTargetType:
      type: string
      enum: [audit_event, evidence_media, workflow_run, workflow_task, object_link, governance_finding, external_document, future_ev_object]
    EvidenceVerifyReport:
      type: object
      required: [evidence_object_id, verified_at, outcome, copies]
      properties:
        evidence_object_id: { type: string, format: uuid }
        verified_at: { type: string, format: date-time }
        outcome: { $ref: '#/components/schemas/VerifyOutcome' }
        copies: { type: array, items: { $ref: '#/components/schemas/CopyVerification' } }
    ExecuteObjectActionRequest:
      type: object
      required:
      - action_id
      - object_type
      - object_id
      - input
      properties:
        action_id:
          type: string
          enum:
          - equipment.update_profile
        object_type:
          type: string
          enum:
          - equipment
        object_id:
          $ref: '#/components/schemas/Uuid'
        input:
          oneOf:
          - $ref: '#/components/schemas/UpdateEquipmentRequest'
          description: Typed action payload. For `equipment.update_profile`, this is UpdateEquipmentRequest.
        idempotency_key:
          type:
          - string
          - 'null'
          maxLength: 128
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    ExportSourceNote:
      type: object
      required:
      - source_domain
      - reason
      properties:
        source_domain:
          type: string
        reason:
          type: string
    ExtendRecruitOfferRequest:
      type: object
      required:
      - amount
      - amount_period
      - reply_deadline
      properties:
        amount:
          type: string
          description: Canonical NUMERIC(14,2) decimal string; at most 12 whole digits and 2 fraction digits, no exponent
            or leading zeros.
        amount_period:
          $ref: '#/components/schemas/RecruitAmountPeriod'
        reply_deadline:
          $ref: '#/components/schemas/Date'
    FacilitiesAcceptanceRequest:
      type: object
      required: [decision]
      properties: { decision: { type: string, enum: [ACCEPTED, REJECTED] }, reason: { type: string, maxLength: 1000 } }
    FacilitiesAssignRequest:
      type: object
      required: [assigneeId]
      properties: { assigneeId: { $ref: '#/components/schemas/Uuid' } }
    FacilitiesCase:
      type: object
      required: [id, branchId, status, responseDueAt, completionDueAt, acceptanceDueAt, totalCostKrw]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        branchId: { $ref: '#/components/schemas/Uuid', description: Persisted case branch used for capability scoping. }
        status: { type: string, enum: [DUE, TRIAGED, SCHEDULED, ASSIGNED, IN_PROGRESS, SUBMITTED, REWORK_REQUIRED, AWAITING_ACCEPTANCE, CLOSED] }
        assigneeId: { type: [string, 'null'], format: uuid }
        responseDueAt: { type: string, format: date-time }
        completionDueAt: { type: string, format: date-time }
        acceptanceDueAt: { type: string, format: date-time }
        energyDeltaKwh: { type: [string, 'null'] }
        totalCostKrw: { type: integer, format: int64 }
    FacilitiesDueCaseRequest:
      type: object
      required: [obligationId, idempotencyKey]
      properties: { obligationId: { $ref: '#/components/schemas/Uuid' }, idempotencyKey: { type: string, minLength: 16 } }
    FacilitiesObservationRequest:
      type: object
      required: [observedAt]
      properties: { preKwh: { type: string, pattern: '^[0-9]+(\\.[0-9]{1,3})?$' }, postKwh: { type: string, pattern: '^[0-9]+(\\.[0-9]{1,3})?$' }, observedAt: { type: string, format: date-time }, costKrw: { type: integer, format: int64, minimum: 0 } }
    FacilitiesSubmitRequest:
      type: object
      required: [safetyChecklistEvidenceId, serviceReportEvidenceId]
      properties: { safetyChecklistEvidenceId: { $ref: '#/components/schemas/Uuid' }, serviceReportEvidenceId: { $ref: '#/components/schemas/Uuid' }, photoEvidenceId: { $ref: '#/components/schemas/Uuid' } }
    FacilitiesTriageRequest:
      type: object
      required: [scheduledFor]
      properties: { scheduledFor: { type: string, format: date-time } }
    FieldAttendanceEvent:
      type: object
      required:
      - user_id
      - user_name
      - work_order_id
      - kind
      - occurred_at
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        user_name:
          type:
          - string
          - 'null'
          description: Same-org display-name lookup; null when the account no longer exists.
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        kind:
          type: string
          description: Compliance attendance vocabulary, read-only here — ARRIVAL or DEPARTURE.
        occurred_at:
          $ref: '#/components/schemas/Timestamp'
    FieldSiteDetail:
      type: object
      required:
      - site
      - sla
      - tickets
      - work_orders
      - attendance
      - acceptances
      properties:
        site:
          $ref: '#/components/schemas/FieldSiteSummary'
        sla:
          $ref: '#/components/schemas/FieldSlaSummary'
        tickets:
          type: array
          items:
            $ref: '#/components/schemas/SupportTicketSummary'
        work_orders:
          type: array
          items:
            $ref: '#/components/schemas/FieldWorkOrderRef'
        attendance:
          type: array
          items:
            $ref: '#/components/schemas/FieldAttendanceEvent'
        acceptances:
          type: array
          items:
            $ref: '#/components/schemas/SupportTicketAcceptance'
    FieldSitePage:
      type: object
      required:
      - items
      - next_cursor
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/FieldSiteRow'
        next_cursor:
          type:
          - string
          - 'null'
          format: uuid
          description: Id to pass as `cursor` for the next page, or null on the last page.
        total:
          type: integer
          format: int64
    FieldSiteRow:
      type: object
      required:
      - site_id
      - site_name
      - branch_id
      - customer_id
      - customer_name
      - address
      - latitude
      - longitude
      - open_ticket_count
      - breached_ticket_count
      - next_due_at
      - active_work_order_count
      - last_arrival_at
      - sla
      properties:
        site_id:
          $ref: '#/components/schemas/Uuid'
        site_name:
          type: string
        branch_id:
          $ref: '#/components/schemas/Uuid'
        customer_id:
          $ref: '#/components/schemas/Uuid'
        customer_name:
          type: string
        address:
          type:
          - string
          - 'null'
        latitude:
          type:
          - number
          - 'null'
          format: double
        longitude:
          type:
          - number
          - 'null'
          format: double
        open_ticket_count:
          type: integer
          format: int64
          description: Tickets in OPEN/IN_PROGRESS/ON_HOLD linked to the site.
        breached_ticket_count:
          type: integer
          format: int64
          description: Open tickets whose SLA due_at has already passed.
        next_due_at:
          type:
          - string
          - 'null'
          format: date-time
        active_work_order_count:
          type: integer
          format: int64
          description: Work orders for the site in a non-terminal status.
        last_arrival_at:
          type:
          - string
          - 'null'
          format: date-time
        sla:
          $ref: '#/components/schemas/FieldSlaState'
    FieldSiteSummary:
      type: object
      required:
      - id
      - name
      - branch_id
      - customer_id
      - customer_name
      - address
      - province
      - city
      - postal_code
      - latitude
      - longitude
      - geofence_radius_m
      - contact_name
      - contact_phone
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        branch_id:
          $ref: '#/components/schemas/Uuid'
        customer_id:
          $ref: '#/components/schemas/Uuid'
        customer_name:
          type: string
        address:
          type:
          - string
          - 'null'
        province:
          type:
          - string
          - 'null'
        city:
          type:
          - string
          - 'null'
        postal_code:
          type:
          - string
          - 'null'
        latitude:
          type:
          - number
          - 'null'
          format: double
        longitude:
          type:
          - number
          - 'null'
          format: double
        geofence_radius_m:
          type:
          - number
          - 'null'
          format: double
        contact_name:
          type:
          - string
          - 'null'
        contact_phone:
          type:
          - string
          - 'null'
    FieldSlaState:
      type: string
      enum:
      - OK
      - AT_RISK
      - BREACHED
      description: Deterministic per-site SLA state over OPEN/IN_PROGRESS/ON_HOLD tickets — BREACHED when any due_at is
        past, else AT_RISK when any due_at falls within 24 hours, else OK.
    FieldSlaSummary:
      type: object
      required:
      - state
      - open
      - breached
      - next_due_at
      - resolved_within_sla_90d
      - resolved_breached_90d
      properties:
        state:
          $ref: '#/components/schemas/FieldSlaState'
        open:
          type: integer
          format: int64
        breached:
          type: integer
          format: int64
        next_due_at:
          type:
          - string
          - 'null'
          format: date-time
        resolved_within_sla_90d:
          type: integer
          format: int64
        resolved_breached_90d:
          type: integer
          format: int64
    FieldWorkOrderRef:
      type: object
      required:
      - id
      - request_no
      - status
      - priority
      - target_due_at
      - report_submitted_at
      - result_type
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        request_no:
          type: string
        status:
          type: string
          description: Workorder-crate status vocabulary, read-only here.
        priority:
          type: string
        target_due_at:
          type:
          - string
          - 'null'
          format: date-time
        report_submitted_at:
          type:
          - string
          - 'null'
          format: date-time
        result_type:
          type: string
        created_at:
          $ref: '#/components/schemas/Timestamp'
    FinalizeWorkflowTaskRequest:
      type: object
      required:
      - mode
      - idempotency_key
      properties:
        mode:
          type: string
          enum:
          - author
          - delegate
          description: author = the initiating author closes; delegate = a policy-authorized delegate closes and must give a reason.
        reason:
          type: string
          description: Required and non-empty when mode is delegate.
        idempotency_key:
          type: string
          minLength: 16
    FinalizeWorkflowTaskResponse:
      type: object
      required:
      - task
      - run
      properties:
        task:
          $ref: '#/components/schemas/FinalizedWorkflowTask'
        run:
          $ref: '#/components/schemas/FinalizedWorkflowRun'
        archive_ref:
          type: object
          additionalProperties: true
    FinalizedWorkflowRun:
      type: object
      required:
      - id
      - status
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
    FinalizedWorkflowTask:
      type: object
      required:
      - id
      - run_id
      - status
      - decision_payload
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        run_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
        completed_by:
          $ref: '#/components/schemas/Uuid'
        decision_payload:
          type: object
          additionalProperties: true
    FinancialConfigSnapshot:
      type: object
      required:
      - depreciation_method
      - useful_life_months
      - residual_rate_bps
      - declining_balance_rate_bps
      - management_fee_rate_bps
      - profit_rate_bps
      - floor_negative_quote_residual
      - executive_approval_threshold_won
      properties:
        depreciation_method:
          $ref: '#/components/schemas/DepreciationMethod'
        useful_life_months:
          type: integer
          format: int32
          minimum: 1
        residual_rate_bps:
          type: integer
          format: int32
          minimum: 0
          maximum: 10000
        declining_balance_rate_bps:
          type: integer
          format: int32
          minimum: 0
          maximum: 10000
        management_fee_rate_bps:
          type: integer
          format: int32
          minimum: 0
          maximum: 10000
        profit_rate_bps:
          type: integer
          format: int32
          minimum: 0
          maximum: 10000
        floor_negative_quote_residual:
          type: boolean
        executive_approval_threshold_won:
          type: integer
          format: int64
          minimum: 0
    FinancialStepUpRequest:
      type: object
      description: Fresh passkey step-up evidence required before sensitive financial state transitions.
      required:
      - step_up
      properties:
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    FindingSeverity:
      type: string
      enum:
      - INFO
      - LOW
      - MEDIUM
      - HIGH
      - CRITICAL
    FindingStatus:
      type: string
      description: Lifecycle of a governance finding. OPEN findings are triaged to one of REVIEWED, DISMISSED, or ESCALATED.
      enum:
      - OPEN
      - REVIEWED
      - DISMISSED
      - ESCALATED
    FixityStatus:
      { type: string, enum: [MATCH, MISMATCH, CHECKSUM_UNAVAILABLE, STORAGE_ERROR] }
    ForceAssignP1DispatchRequest:
      type: object
      required:
      - mechanic_id
      properties:
        mechanic_id:
          $ref: '#/components/schemas/Uuid'
    FrameworkKind:
      type: string
      enum: [LEGAL_BASELINE, INTERNAL_CONTROL, CUSTOMER_CONTROL, SECURITY_STANDARD, SAFETY_STANDARD, AUDIT_PROGRAM]
    FrameworkStatus:
      type: string
      enum: [DRAFT, ACTIVE, RETIRED, ARCHIVED]
    GateChainConfig:
      type: object
      required: [authority, self_checklist, four_eyes, egress_dlp]
      properties:
        authority: { type: boolean }
        self_checklist: { type: boolean }
        four_eyes: { type: boolean }
        egress_dlp: { type: boolean }
    GateChainOutcome:
      type: object
      required: [allow, gates]
      properties:
        allow: { type: boolean }
        gates:
          type: array
          items: { type: object, additionalProperties: true }  # per-gate { gate, status }
    GovernanceConfigureTransitionRequest:
      type: object
      required:
      - object_type_id
      - from_state
      - to_state
      properties:
        object_type_id:
          $ref: '#/components/schemas/Uuid'
        from_state:
          $ref: '#/components/schemas/LifecycleState'
        to_state:
          $ref: '#/components/schemas/LifecycleState'
        requires_reason:
          type: boolean
        requires_four_eyes:
          type: boolean
        requires_checklist:
          type: boolean
    GovernanceDecideApprovalRequest:
      type: object
      required:
      - request_ref
      - kind
      - requested_by
      - decision
      properties:
        request_ref:
          $ref: '#/components/schemas/Uuid'
        kind:
          type: string
        requested_by:
          $ref: '#/components/schemas/Uuid'
          deprecated: true
          description: Deprecated and IGNORED. Still required on the wire — omitting it is a 422 — but the requester is
            read from the open pending approval request inside the decision transaction, so whatever is sent here has
            no effect on the self-approval check or on the recorded approval. Send the open request's requested_by.
        decision:
          type: string
          enum:
          - approved
          - rejected
    GovernanceFinding:
      type: object
      description: A governance finding raised by the integrity engine (#34). Findings are "검토 필요" (review needed) — e.g.
        a self-approval record (detector_id `anomaly.self_approval`) or a price anomaly (`anomaly.price_outlier`) — and are
        NOT accusations of wrongdoing.
      required:
      - id
      - org_id
      - detector_id
      - entity_type
      - entity_id
      - score
      - severity
      - evidence
      - status
      - detected_at
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        org_id:
          $ref: '#/components/schemas/Uuid'
        detector_id:
          type: string
          description: Dot-namespaced detector identity, e.g. `anomaly.self_approval` or `anomaly.price_outlier`.
        entity_type:
          type: string
          description: The domain entity the finding concerns, e.g. `financial_purchase_request`.
        entity_id:
          type: string
          description: Identifier of the entity the finding concerns.
        source_audit_event_id:
          type:
          - string
          - 'null'
          format: uuid
          description: Audit event that triggered the finding (OnWrite detectors); may be null.
        subject_user_id:
          type:
          - string
          - 'null'
          format: uuid
          description: The user whose action was flagged (e.g. the self-approver). Null for non-person findings. A raw id;
            resolve to a display name in the UI.
        score:
          type: number
          format: double
          minimum: 0
          description: Non-negative detector score; the severity enum is the primary triage signal.
        severity:
          $ref: '#/components/schemas/FindingSeverity'
        evidence:
          type: object
          additionalProperties: true
          description: Detector-specific evidence bag. For `anomaly.self_approval` this includes action, requested_by, submitted_by,
            approver, and exemption_reason (org_lead_exempt or super_admin_exempt).
        status:
          $ref: '#/components/schemas/FindingStatus'
        detected_at:
          $ref: '#/components/schemas/Timestamp'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        reviewed_by:
          type:
          - string
          - 'null'
          format: uuid
          description: The reviewer who triaged the finding; null until triaged.
        reviewed_at:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        review_memo:
          type:
          - string
          - 'null'
          description: Reviewer memo recorded at triage; required when dismissing or escalating.
    GovernanceLifecyclePreflightRequest:
      type: object
      required:
      - object_type_id
      - from_state
      - to_state
      properties:
        object_type_id:
          $ref: '#/components/schemas/Uuid'
        from_state:
          $ref: '#/components/schemas/LifecycleState'
        to_state:
          $ref: '#/components/schemas/LifecycleState'
        authority_allow:
          type: boolean
        checklist_all_acknowledged:
          type: boolean
        four_eyes_request_ref:
          $ref: '#/components/schemas/Uuid'
        egress_cleared:
          type: boolean
    GovernanceOpenOverrideRequest:
      type: object
      required:
      - target_type
      - target_id
      - reason
      - before_snapshot
      properties:
        target_type:
          type: string
        target_id:
          $ref: '#/components/schemas/Uuid'
        reason:
          type: string
        before_snapshot:
          type: object
          additionalProperties: true
    HireRecruitApplicantRequest:
      type: object
      required:
      - employee_number
      - phone
      - org_unit
      - position
      - site
      - home_branch_id
      properties:
        employee_number:
          type: string
        phone:
          type: string
        org_unit:
          type: string
        position:
          type: string
        site:
          type: string
        home_branch_id:
          $ref: '#/components/schemas/Uuid'
        base_pay:
          type:
          - string
          - 'null'
          description: Defaults to the accepted offer amount for MONTHLY offers; required explicitly for DAILY offers.
    HireRecruitApplicantResponse:
      type: object
      required:
      - employee_id
      - applicant
      - posting
      properties:
        employee_id:
          $ref: '#/components/schemas/Uuid'
        applicant:
          $ref: '#/components/schemas/RecruitApplicant'
        posting:
          $ref: '#/components/schemas/RecruitPosting'
    HoldRecruitApplicantRequest:
      type: object
      required:
      - hold
      properties:
        hold:
          type: boolean
    HrAppointInput:
      type: object
      additionalProperties: false
      description: "Typed params for `hr.appoint` (`EmploymentQuery::Appoint`). Opens a head at `valid_from`. Four-eyes is natural-person. The legacy employees row is not rewritten by this command."
      required:
        - employee_id
        - valid_from
        - attributes
      properties:
        employee_id:
          $ref: "#/components/schemas/Uuid"
        valid_from:
          $ref: "#/components/schemas/Timestamp"
        attributes:
          $ref: "#/components/schemas/EmploymentAttributesInput"
    HrOrgChartCompany:
      type: object
      required:
      - company
      - total
      - active
      - units
      properties:
        company:
          type: string
        total:
          type: integer
          format: int64
          minimum: 0
        active:
          type: integer
          format: int64
          minimum: 0
        units:
          type: array
          items:
            $ref: '#/components/schemas/HrOrgChartUnit'
    HrOrgChartEmployee:
      type: object
      required:
        - id
        - name
        - status
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        employee_number:
          type:
            - string
            - 'null'
        status:
          type: string
    HrOrgChartPosition:
      type: object
      required:
      - title
      - total
      - employees
      properties:
        title:
          type: string
        total:
          type: integer
          format: int64
          minimum: 0
        employees:
          type: array
          items:
            $ref: '#/components/schemas/HrOrgChartEmployee'
    HrOrgChartResponse:
      type: object
      required:
      - companies
      properties:
        companies:
          type: array
          items:
            $ref: '#/components/schemas/HrOrgChartCompany'
    HrOrgChartUnit:
      type: object
      required:
      - name
      - total
      - positions
      properties:
        name:
          type: string
        total:
          type: integer
          format: int64
          minimum: 0
        positions:
          type: array
          items:
            $ref: '#/components/schemas/HrOrgChartPosition'
    HrPromoteInput:
      type: object
      additionalProperties: false
      description: "Typed params for `hr.promote` (`EmploymentQuery::Promote`). Appends a revision and carries the new state onto the legacy head. Four-eyes is natural-person."
      required:
        - employment_id
        - valid_from
        - attributes
      properties:
        employment_id:
          $ref: "#/components/schemas/Uuid"
        valid_from:
          $ref: "#/components/schemas/Timestamp"
        attributes:
          $ref: "#/components/schemas/EmploymentAttributesInput"
    HrTransferInput:
      type: object
      additionalProperties: false
      description: "Typed params for `hr.transfer` (`EmploymentQuery::Transfer`). Same shape as promote; the receipt records a different dispatch target. Four-eyes is natural-person."
      required:
        - employment_id
        - valid_from
        - attributes
      properties:
        employment_id:
          $ref: "#/components/schemas/Uuid"
        valid_from:
          $ref: "#/components/schemas/Timestamp"
        attributes:
          $ref: "#/components/schemas/EmploymentAttributesInput"
    ImportApplyRequest:
      type: object
      description: >-
        §16 self-checklist evidence for an ingest-commit ("적재") apply. Missing or false ⇒ fail-closed deny,
        nothing written (85 판정).
      properties:
        checklist_all_acknowledged:
          type:
          - boolean
          - 'null'
    InboxDocConfirmReceiptRequest:
      type: object
      description: >-
        The fresh passkey assertion proving present possession of an
        authenticator. Its absence yields 428 (precondition required).
      required:
      - step_up
      properties:
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    InboxDocDetail:
      type: object
      description: >-
        A single inbox document. Extends InboxDocSummary with the body
        `payload`, present only when readable (a payslip, or an already-confirmed
        legal notice) and omitted while a legal notice is locked.
      allOf:
      - $ref: '#/components/schemas/InboxDocSummary'
      - type: object
        properties:
          payload:
            type: object
            additionalProperties: true
            description: Rendered document payload (legal prose paragraphs, or payslip figures).
    InboxDocPage:
      type: object
      required:
      - items
      - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/InboxDocSummary'
        next_cursor:
          type:
          - string
          - 'null'
          format: uuid
    InboxDocSummary:
      type: object
      description: >-
        A list-row / confirmation view of one inbox document. Never carries the
        body `payload` — that is only ever on InboxDocDetail, and only once
        readable.
      required:
      - id
      - recipient_user_id
      - kind
      - title
      - locked
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        recipient_user_id:
          $ref: '#/components/schemas/Uuid'
        kind:
          type: string
          enum: [payslip, legal_notice]
        notice_type:
          type: string
          description: Statutory subtype for a legal notice (근로계약/취업규칙/연차촉진/노무수령거부).
        title:
          type: string
        legal_basis:
          type: string
          description: Statutory basis surfaced in the passkey gate (e.g. 근로기준법 §61).
        source_kind:
          type: string
          description: Producing-object kind (e.g. workflow_run, payroll_run).
        source_id:
          type: string
          description: Producing-object id (e.g. the AP- run code).
        locked:
          type: boolean
          description: >-
            True while a legal notice awaits receipt confirmation — its body is
            withheld until confirmed. Always false for payslips.
        confirmed_by:
          type:
          - string
          - 'null'
          format: uuid
          description: The recipient who confirmed receipt; null until confirmed.
        confirmed_at:
          type:
          - string
          - 'null'
          format: date-time
          description: When receipt was confirmed (the legal receipt timestamp); null until then.
        created_at:
          $ref: '#/components/schemas/Timestamp'
    IncidentLocation:
      type: object
      required:
      - latitude
      - longitude
      properties:
        latitude:
          type: number
          format: double
          minimum: -90
          maximum: 90
        longitude:
          type: number
          format: double
          minimum: -180
          maximum: 180
    InquiryAck:
      type: object
      description: Minimal acknowledgement of a submitted inquiry (no identifiers, no PII echo).
      required:
      - status
      properties:
        status:
          type: string
    InquiryStatus:
      type: string
      description: Triage state of an inbound inquiry in the internal inbox.
      enum:
      - NEW
      - CONTACTED
      - CLOSED
    InquiryTopic:
      type: string
      description: Subject of a customer inquiry.
      enum:
      - RENTAL
      - USED_SALES
      - MAINTENANCE
      - OTHER
    InspectionCycle:
      type: string
      enum:
      - DAILY
      - WEEKLY
      - MONTHLY
      - QUARTERLY
      - YEARLY
      - CUSTOM
    InspectionRoundOutcome:
      type: string
      enum:
      - COMPLETED
      - FOLLOW_UP_REQUIRED
    InspectionRoundSummary:
      type: object
      required:
      - id
      - schedule_id
      - branch_id
      - equipment_id
      - mechanic_id
      - completed_by
      - outcome
      - findings
      - note
      - completed_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        schedule_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        mechanic_id:
          $ref: '#/components/schemas/Uuid'
        completed_by:
          $ref: '#/components/schemas/Uuid'
        outcome:
          $ref: '#/components/schemas/InspectionRoundOutcome'
        findings:
          type: string
        note:
          type:
          - string
          - 'null'
        completed_at:
          $ref: '#/components/schemas/Timestamp'
    InspectionSchedulePage:
      type: object
      required:
      - items
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/InspectionScheduleSummary'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
    InspectionScheduleStatus:
      type: string
      enum:
      - SCHEDULED
      - COMPLETED
      - CANCELLED
    InspectionScheduleSummary:
      type: object
      required:
      - id
      - branch_id
      - equipment_id
      - mechanic_id
      - mechanic_display_name
      - cycle
      - interval_days
      - due_date
      - status
      - completed_at
      - note
      - site_name
      - management_no
      - model
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        mechanic_id:
          $ref: '#/components/schemas/Uuid'
        mechanic_display_name:
          type:
          - string
          - 'null'
          description: Assigned mechanic's display name, resolved via a same-org LEFT JOIN on users. Null when the mechanic
            account no longer exists.
        cycle:
          $ref: '#/components/schemas/InspectionCycle'
        interval_days:
          type: integer
          format: int32
          minimum: 1
        due_date:
          $ref: '#/components/schemas/Date'
        status:
          $ref: '#/components/schemas/InspectionScheduleStatus'
        completed_at:
          type:
          - string
          - 'null'
          format: date-time
        note:
          type:
          - string
          - 'null'
        site_name:
          type: string
        management_no:
          type:
          - string
          - 'null'
        model:
          type:
          - string
          - 'null'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    InstanceHead:
      type: object
      required:
        - id
        - object_type_id
        - title
        - lifecycle_state
      properties:
        id:
          type: string
          format: uuid
        object_type_id:
          type: string
          format: uuid
        title:
          type: string
        current_revision_id:
          type:
            - string
            - 'null'
          format: uuid
        lifecycle_state:
          $ref: '#/components/schemas/InstanceLifecycleState'
    InstanceLifecycleState:
      type: string
      enum: [draft, active, locked, archived, disposed]
    InstanceState:
      type: object
      required: [instance, revision]
      properties:
        instance: { $ref: '#/components/schemas/InstanceHead' }
        revision: { $ref: '#/components/schemas/RevisionSummary' }
    InventoryConsumptionEvent:
      type: object
      required:
        - id
        - item_id
        - iv_code
        - branch_id
        - stock_location_id
        - source
        - quantity_before_milli
        - quantity_consumed_milli
        - quantity_after_milli
        - consumed_by
        - occurred_at
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        item_id:
          $ref: '#/components/schemas/Uuid'
        iv_code:
          type: string
        branch_id:
          $ref: '#/components/schemas/Uuid'
        stock_location_id:
          $ref: '#/components/schemas/Uuid'
        source:
          $ref: '#/components/schemas/InventoryConsumptionSource'
        quantity_before_milli:
          type: integer
          format: int64
        quantity_consumed_milli:
          type: integer
          format: int64
        quantity_after_milli:
          type: integer
          format: int64
        unit_cost_won:
          type:
            - integer
            - 'null'
          format: int64
        cost_won:
          type:
            - integer
            - 'null'
          format: int64
        consumed_by:
          $ref: '#/components/schemas/Uuid'
        occurred_at:
          type: string
          format: date-time
        memo:
          type:
            - string
            - 'null'
        created_at:
          type: string
          format: date-time
    InventoryConsumptionResult:
      type: object
      required: [event, item]
      properties: { event: { $ref: '#/components/schemas/InventoryConsumptionEvent' }, item: { $ref: '#/components/schemas/InventoryItem' } }
    InventoryConsumptionSource:
      oneOf:
        - type: object
          required: [kind, work_order_id]
          properties: { kind: { type: string, enum: [work_order] }, work_order_id: { $ref: '#/components/schemas/Uuid' } }
        - type: object
          required: [kind, dispatch_id]
          properties: { kind: { type: string, enum: [p1_dispatch] }, dispatch_id: { $ref: '#/components/schemas/Uuid' } }
    InventoryItem:
      type: object
      required:
        - id
        - branch_id
        - stock_location
        - iv_code
        - display_name
        - unit_code
        - quantity_on_hand_milli
        - safety_stock_milli
        - low_stock
        - status
        - href
        - created_by
        - created_at
        - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        site_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        stock_location:
          $ref: '#/components/schemas/InventoryStockLocationSummary'
        iv_code:
          type: string
        sku:
          type:
            - string
            - 'null'
        display_name:
          type: string
        description:
          type:
            - string
            - 'null'
        unit_code:
          type: string
        quantity_on_hand_milli:
          type: integer
          format: int64
        safety_stock_milli:
          type: integer
          format: int64
        unit_cost_won:
          type:
            - integer
            - 'null'
          format: int64
        low_stock:
          type: boolean
        status:
          type: string
        href:
          type: string
        created_by:
          $ref: '#/components/schemas/Uuid'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    InventoryItemPage:
      type: object
      required: [items, limit, offset, total]
      properties: { items: { type: array, items: { $ref: '#/components/schemas/InventoryItem' } }, limit: { type: integer, format: int64 }, offset: { type: integer, format: int64 }, total: { type: integer, format: int64 } }
    InventoryMovement:
      type: object
      required:
        - id
        - itemId
        - ivCode
        - kind
        - quantityDeltaMilli
        - quantityBeforeMilli
        - quantityAfterMilli
        - source
        - actor
        - occurredAt
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        itemId:
          $ref: '#/components/schemas/Uuid'
        ivCode:
          type: string
        kind:
          type: string
          enum:
            - ISSUE
            - RECEIPT
            - ADJUSTMENT
        quantityDeltaMilli:
          type: integer
          format: int64
        quantityBeforeMilli:
          type: integer
          format: int64
        quantityAfterMilli:
          type: integer
          format: int64
        source:
          $ref: '#/components/schemas/InventoryMovementSource'
        actor:
          $ref: '#/components/schemas/Uuid'
        occurredAt:
          type: string
          format: date-time
        memo:
          type:
            - string
            - 'null'
    InventoryMovementSource:
      oneOf:
      - $ref: '#/components/schemas/InventoryMovementSourceWorkOrder'
      - $ref: '#/components/schemas/InventoryMovementSourceP1Dispatch'
      - $ref: '#/components/schemas/InventoryMovementSourceCycleCount'
      - $ref: '#/components/schemas/InventoryMovementSourceExternalRef'
      discriminator:
        propertyName: kind
        mapping:
          work_order: '#/components/schemas/InventoryMovementSourceWorkOrder'
          p1_dispatch: '#/components/schemas/InventoryMovementSourceP1Dispatch'
          cycle_count: '#/components/schemas/InventoryMovementSourceCycleCount'
          external_ref: '#/components/schemas/InventoryMovementSourceExternalRef'
    InventoryMovementSourceCycleCount:
      type: object
      additionalProperties: false
      required: [kind, cycleCountId, ccCode]
      properties: { kind: { type: string, enum: [cycle_count] }, cycleCountId: { $ref: '#/components/schemas/Uuid' }, ccCode: { type: string } }
    InventoryMovementSourceExternalRef:
      type: object
      additionalProperties: false
      required:
        - kind
        - sourceRef
      properties:
        kind:
          type: string
          enum:
            - external_ref
        sourceRef:
          type:
            - string
            - 'null'
    InventoryMovementSourceP1Dispatch:
      type: object
      additionalProperties: false
      required: [kind, dispatchId, workOrderId]
      properties: { kind: { type: string, enum: [p1_dispatch] }, dispatchId: { $ref: '#/components/schemas/Uuid' }, workOrderId: { $ref: '#/components/schemas/Uuid' } }
    InventoryMovementSourceWorkOrder:
      type: object
      additionalProperties: false
      required: [kind, workOrderId]
      properties: { kind: { type: string, enum: [work_order] }, workOrderId: { $ref: '#/components/schemas/Uuid' } }
    InventoryMrpLine:
      type: object
      required:
        - itemId
        - ivCode
        - displayName
        - unitCode
        - quantityOnHandMilli
        - safetyStockMilli
        - inboundExpectedMilli
        - reservedOutboundMilli
        - monthlyUsageMilli
        - short
        - proposedOrderMilli
      properties:
        itemId:
          $ref: '#/components/schemas/Uuid'
        ivCode:
          type: string
        displayName:
          type: string
        unitCode:
          type: string
        quantityOnHandMilli:
          type: integer
          format: int64
        safetyStockMilli:
          type: integer
          format: int64
        inboundExpectedMilli:
          type: integer
          format: int64
        reservedOutboundMilli:
          type: integer
          format: int64
        monthlyUsageMilli:
          type: integer
          format: int64
        coverMonthsCenti:
          type:
            - integer
            - 'null'
          format: int64
        short:
          type: boolean
        proposedOrderMilli:
          type: integer
          format: int64
    InventoryReceiptResult:
      type: object
      required: [movement, item]
      properties: { movement: { $ref: '#/components/schemas/InventoryMovement' }, item: { $ref: '#/components/schemas/InventoryItem' } }
    InventoryStockLocationSummary:
      type: object
      required: [id, label]
      properties: { id: { $ref: '#/components/schemas/Uuid' }, label: { type: string } }
    JobPosition:
      type: object
      description: "Canonical JobPosition head. Distinct from recruiting postings and from `employees.position` TEXT. GET /api/v1/job-positions/{id} returns this current head. GET /api/v1/job-positions returns current heads (the same rows `list` already returns). as_of / from / to are omitted: `job_positions` / `job_position_revisions` have no valid-time columns, and using `created_at` as `valid_from` would be a second time model. `org_unit_id` is a foreign key to an existing OrgUnit. `attributes` is the latest revision bag; write preflight requires a non-empty string `title` (catalog JOB_POSITION_TITLE). Other attribute keys are not a published closed set. Temporal slices other than revision `version` remain HOLD."
      required:
        - job_position_id
        - org_unit_id
        - version
        - attributes
      properties:
        job_position_id:
          $ref: "#/components/schemas/Uuid"
        org_unit_id:
          $ref: "#/components/schemas/Uuid"
        version:
          type: integer
          format: int64
          minimum: 1
          description: Optimistic concurrency token for this Head. Mutating roster actions send it as OntologyActionRequest.expected_revision (JSON body, not HTTP If-Match).
        attributes:
          type: object
          additionalProperties: true
          description: Latest revision JSON object. Write requires non-empty `title`.
      links:
        - key: job_position_org_unit
          from: JobPosition
          to: OrgUnit
          field: org_unit_id
          cardinality: many-to-one
          option: false
          operationId: getOrgUnit
      actions:
        - action_key: organization.create_job_position
          object_key: job_position
          input:
            $ref: "#/components/schemas/OrganizationCreateJobPositionInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: account
          edits:
            - job_positions
            - job_position_revisions
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
        - action_key: organization.revise_job_position
          object_key: job_position
          input:
            $ref: "#/components/schemas/OrganizationReviseJobPositionInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: account
          edits:
            - job_positions
            - job_position_revisions
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
      concurrency:
        get_token: version
        write_field: expected_revision
        write_in: body
      permissions:
        - employee_directory_read
    KpiMetric:
      type: string
      enum:
      - completed_count
      - average_response_speed
      - completion_duration_and_due_compliance
      - revisit_rate
      - delay_rate_and_reason_distribution
      - inspection_plan_completion_rate
      - p1_acceptance_rate
    KpiReport:
      type: object
      required:
      - period
      - requested_scope
      - rollups
      - unavailable_metrics
      properties:
        period:
          $ref: '#/components/schemas/Period'
        requested_scope:
          $ref: '#/components/schemas/KpiScope'
        rollups:
          type: array
          items:
            $ref: '#/components/schemas/KpiRollup'
        unavailable_metrics:
          type: array
          items:
            $ref: '#/components/schemas/UnavailableMetric'
    KpiRollup:
      type: object
      required:
      - scope
      - approved_report_count
      - completed_count
      - weighted_completed_points
      - average_response_seconds
      - average_completion_seconds
      - target_due_compliance_bps
      - revisit_rate_bps
      - delay_rate_bps
      - delay_reason_distribution
      - inspection_schedule_due_count
      - inspection_schedule_completed_count
      - inspection_plan_completion_bps
      - p1_dispatch_count
      - p1_accepted_count
      - p1_acceptance_bps
      properties:
        scope:
          $ref: '#/components/schemas/KpiRollupScope'
        scope_display_name:
          type:
          - string
          - 'null'
          description: Human-readable name for the scope (region/branch/mechanic), resolved via a same-org lookup. Null for
            the company-wide scope or a deleted region/branch/user. Optional; absent on the pure-domain shape.
        approved_report_count:
          type: integer
          format: int32
          minimum: 0
        completed_count:
          type: integer
          format: int32
          minimum: 0
        weighted_completed_points:
          type: integer
          format: int32
          minimum: 0
        average_response_seconds:
          type:
          - integer
          - 'null'
          format: int64
          minimum: 0
        average_completion_seconds:
          type:
          - integer
          - 'null'
          format: int64
          minimum: 0
        target_due_compliance_bps:
          type:
          - integer
          - 'null'
          format: int32
          minimum: 0
          maximum: 10000
        revisit_rate_bps:
          type: integer
          format: int32
          minimum: 0
          maximum: 10000
        delay_rate_bps:
          type: integer
          format: int32
          minimum: 0
          maximum: 10000
        delay_reason_distribution:
          type: object
          additionalProperties:
            type: integer
            format: int32
            minimum: 0
        inspection_schedule_due_count:
          type: integer
          format: int32
          minimum: 0
        inspection_schedule_completed_count:
          type: integer
          format: int32
          minimum: 0
        inspection_plan_completion_bps:
          type:
          - integer
          - 'null'
          format: int32
          minimum: 0
          maximum: 10000
        p1_dispatch_count:
          type: integer
          format: int32
          minimum: 0
        p1_accepted_count:
          type: integer
          format: int32
          minimum: 0
        p1_acceptance_bps:
          type:
          - integer
          - 'null'
          format: int32
          minimum: 0
          maximum: 10000
    KpiRollupScope:
      type: object
      required:
      - kind
      properties:
        kind:
          type: string
          enum:
          - company
          - region
          - branch
          - technician
        id:
          $ref: '#/components/schemas/Uuid'
      description: '`id` is present for region, branch, and technician rollups and absent for company.'
    KpiScope:
      type: object
      required:
      - kind
      properties:
        kind:
          type: string
          enum:
          - company
          - region
          - branch
          - technician
        id:
          $ref: '#/components/schemas/Uuid'
      description: '`id` is present for region, branch, and technician scopes and absent for company.'
    LeaveBalanceAmount:
      type: string
      pattern: '^-?(0|[1-9][0-9]{0,9})\.[0-9]{6}$'
      example: '13.000000'
      description: Exact signed fixed-scale ledger days; historical imports may legitimately be negative.
    LeaveBalanceItem:
      type: object
      required:
        - id
        - company
        - name
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        company:
          type: string
        name:
          type: string
        employee_number:
          type:
            - string
            - 'null'
        org_unit:
          type:
            - string
            - 'null'
        position:
          type:
            - string
            - 'null'
        leave_accrued:
          type:
            - string
            - 'null'
        leave_used:
          type:
            - string
            - 'null'
        leave_remaining:
          type:
            - string
            - 'null'
    LeaveBalancePage:
      type: object
      required:
      - items
      - total
      - limit
      - offset
      - summary
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/LeaveBalanceItem'
        total:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        summary:
          $ref: '#/components/schemas/LeaveBalanceSummary'
    LeaveBalanceSummary:
      type: object
      required:
      - accrued
      - used
      - remaining
      properties:
        accrued:
          type: string
        used:
          type: string
        remaining:
          type: string
    LeaveChargeResolutionRequest:
      type: object
      additionalProperties: false
      required:
      - expected_version
      - date_charges
      - calendar_revision_ref
      - policy_revision_ref
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: Expected mutable request_version; charge_version is evidence-only.
        date_charges:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/LeaveDateCharge'}
        calendar_revision_ref: {$ref: '#/components/schemas/LeaveSourceRevisionRef'}
        policy_revision_ref: {$ref: '#/components/schemas/LeaveSourceRevisionRef'}
        supporting_source_refs:
          type: array
          items: {$ref: '#/components/schemas/LeaveSourceRevisionRef'}
          default: []
    LeaveChargeResolutionView:
      type: object
      required:
      - request_id
      - request_version
      - charge_units
      - charge_state
      - charge_version
      - server_digest
      - resolution_origin
      - resolved_by
      properties:
        request_id: {$ref: '#/components/schemas/Uuid'}
        request_version:
          type: integer
          format: int64
          minimum: 1
          description: New mutable request/workflow CAS token after the resolution commits.
        charge_units: {$ref: '#/components/schemas/LeaveUnits'}
        charge_state: {type: string, enum: [resolved]}
        charge_version:
          type: integer
          format: int64
          minimum: 1
          description: Immutable revision of the newly recorded charge-evidence snapshot.
        server_digest: {type: string, pattern: '^[0-9a-f]{64}$'}
        resolution_origin: {type: string, enum: [automated, manual]}
        resolved_by:
          type:
          - string
          - 'null'
          format: uuid
    LeaveChargeReviewReason:
      type: string
      enum:
      - missing_calendar
      - ambiguous_calendar
      - calendar_source_unavailable
      - missing_policy
      - ambiguous_policy
      - policy_source_unavailable
    LeaveCreateRequest:
      type: object
      description: >-
        A self-service leave-request filing. The subject employee and branch are NOT accepted here — they are resolved from
        the authenticated caller. There is deliberately no `reason` field: 근로기준법 §60 grants the worker the 시기지정권, so a 연차/반차
        filing carries no 사유 and a body that still sends one is rejected (422).
      additionalProperties: false
      required:
        - idempotency_key
        - leave_type
        - start_date
        - end_date
      properties:
        idempotency_key:
          type: string
          format: uuid
          description: >-
            Stable client submission id. Reuse it only to retry the same canonical client intent after an unknown or lost
            response.
        leave_type:
          type: string
          enum:
            - annual
            - half_day
          description: Full-day or partial-day intent; quantity is resolved only from evidence.
        partial_day_period:
          oneOf:
            - {type: string, enum: [am, pm]}
            - {type: 'null'}
          description: Required exactly when leave_type is half_day.
        start_date:
          type: string
          format: date
          description: YYYY-MM-DD. A half-day request must use the same start and end date.
        end_date:
          type: string
          format: date
    LeaveDateCharge:
      type: object
      additionalProperties: false
      required: [date, obligation, charge_units]
      properties:
        date: {type: string, format: date}
        obligation:
          oneOf:
          - type: object
            additionalProperties: false
            required: [kind, minutes]
            properties:
              kind: {type: string, enum: [scheduled]}
              minutes: {type: integer, format: int32, minimum: 1}
          - type: object
            additionalProperties: false
            required: [kind, basis]
            properties:
              kind: {type: string, enum: [not_scheduled]}
              basis:
                type: string
                enum: [rest_day, public_holiday, substitute_holiday, contractual_day_off, other]
        charge_units:
          $ref: '#/components/schemas/LeaveUnits'
    LeaveDecideRequest:
      type: object
      required:
      - decision
      properties:
        decision:
          type: string
          enum: [approve, time_change]
          description: >-
            근로기준법 §60: refusal is not representable. `time_change` opens the
            §60⑤ 시기변경 협의; `return`/`reject` are rejected with 422.
        comment:
          type: string
          description: Mandatory for time_change; optional for approve.
    LeaveDecideV2Request:
      type: object
      additionalProperties: false
      required:
      - expected_version
      - decision
      properties:
        expected_version:
          type: integer
          format: int64
          minimum: 1
          description: Required mutable request_version compare-and-swap token; never use charge_version.
        decision:
          type: string
          enum: [approve, time_change]
          description: >-
            근로기준법 §60: refusal is not representable. `time_change` opens the
            §60⑤ 시기변경 협의 only when system coverage evidence shows a
            shortfall; `return`/`reject` are rejected with 422.
        comment:
          type: string
          description: Mandatory for time_change; optional for approve.
    LeavePromotion:
      type: object
      required:
        - id
        - round_no
        - reference_year
        - threshold_days
        - status
        - created_by
        - target_count
        - confirmed_count
      properties:
        id:
          type: string
          format: uuid
        round_no:
          type: integer
        reference_year:
          type: integer
        threshold_days:
          type: string
        status:
          type: string
          enum:
            - DRAFT
            - SENT
            - COMPLETED
        note:
          type:
            - string
            - 'null'
        created_by:
          type: string
          format: uuid
        sent_at:
          type:
            - string
            - 'null'
          format: date-time
        target_count:
          type: integer
        confirmed_count:
          type: integer
    LeavePromotionRequest:
      type: object
      description: A §61 연차 사용 촉진 push to a target employee.
      required:
      - branch_id
      - target_user_id
      - target_employee_id
      - target_name
      - round
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        target_user_id:
          $ref: '#/components/schemas/Uuid'
        target_employee_id:
          $ref: '#/components/schemas/Uuid'
        target_name:
          type: string
        round:
          type: integer
          format: int32
          description: §61 round — 1 (사용 촉구) or 2 (시기 지정).
        unused_days:
          type: number
          format: double
          description: Unused annual-leave days motivating the push.
    LeaveProposeAlternateDatesRequest:
      type: object
      additionalProperties: false
      required:
      - start_date
      - end_date
      properties:
        start_date:
          type: string
          format: date
          description: Inclusive alternate 시기 start (YYYY-MM-DD); must differ from the original span.
        end_date:
          type: string
          format: date
          description: Inclusive alternate 시기 end (YYYY-MM-DD); must differ from the original span.
    LeaveRefusalRequest:
      type: object
      description: A 노무수령거부 notice served after a round-2 promotion.
      required:
      - branch_id
      - target_user_id
      - target_employee_id
      - target_name
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        target_user_id:
          $ref: '#/components/schemas/Uuid'
        target_employee_id:
          $ref: '#/components/schemas/Uuid'
        target_name:
          type: string
        unused_days:
          type: number
          format: double
    LeaveRequest:
      type: object
      required:
        - id
        - employee_id
        - leave_type
        - reason
        - start_date
        - end_date
        - status
        - requested_by
        - requested_at
      properties:
        id:
          type: string
          format: uuid
        employee_id:
          type: string
          format: uuid
        employee_name:
          type:
            - string
            - 'null'
        branch_id:
          type:
            - string
            - 'null'
          format: uuid
        leave_type:
          type: string
        reason:
          type: string
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        note:
          type:
            - string
            - 'null'
        status:
          type: string
          enum:
            - DRAFT
            - SUBMITTED
            - APPROVED
            - REJECTED
        requested_by:
          type: string
          format: uuid
        requested_at:
          type: string
          format: date-time
        decided_by:
          type:
            - string
            - 'null'
          format: uuid
        decided_at:
          type:
            - string
            - 'null'
          format: date-time
        decision_note:
          type:
            - string
            - 'null'
    LeaveRequestPage:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/LeaveRequestView'
    LeaveRequestV2Page:
      type: object
      required:
      - items
      - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/LeaveRequestV2View'
        next_cursor:
          type:
          - string
          - 'null'
          format: uuid
          description: Last request id for the next stable keyset page, or null when exhausted.
    LeaveRequestV2View:
      type: object
      description: One leave request in the approval queue (결재함 leave variant).
      required:
      - id
      - branch_id
      - requester_user_id
      - subject_employee_id
      - leave_type
      - days
      - charge_units
      - charge_state
      - charge_review_reasons
      - request_version
      - charge_version
      - start_date
      - end_date
      - status
      - decided_by
      - decided_at
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        requester_user_id:
          $ref: '#/components/schemas/Uuid'
        subject_employee_id:
          $ref: '#/components/schemas/Uuid'
        leave_type:
          type: string
          enum: [annual, half_day]
        days:
          type: number
          format: double
          deprecated: true
          description: Non-null legacy compatibility projection; never authoritative for new approvals.
        charge_units:
          anyOf:
          - $ref: '#/components/schemas/LeaveUnits'
          - type: 'null'
          description: Exact resolved charge; null while review is required or no charge applies.
        charge_state:
          type: string
          enum: [review_required, resolved, not_required, legacy_unverified]
        charge_review_reasons:
          type: array
          items:
            $ref: '#/components/schemas/LeaveChargeReviewReason'
        request_version:
          type: integer
          format: int64
          minimum: 1
          description: Mutable request/workflow CAS token; submit as expected_version for resolve or decide.
        charge_version:
          type: integer
          format: int64
          minimum: 0
          description: Monotonic immutable charge-evidence revision counter; decisions do not advance it and it is never a request mutation precondition.
        charge_digest:
          type: string
          pattern: '^[0-9a-f]{64}$'
          description: Server digest of the immutable resolution snapshot, when resolved.
        charge_resolved_by:
          $ref: '#/components/schemas/Uuid'
        charge_resolution_origin:
          type: string
          enum: [automated, manual]
          description: Provenance of the current immutable charge snapshot, when one exists.
        partial_day_period:
          type: string
          enum: [am, pm]
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        reason:
          type: string
          description: >-
            Historical rows only (filed before the 근로기준법 §60 guardrail);
            omitted for requests that carry no 사유.
        status:
          type: string
          enum: [pending, approved, time_change_consult, returned, rejected]
          description: >-
            `returned`/`rejected` are historical rows only — no decision can
            produce them since the §60 guardrail. `time_change_consult` is the
            lawful non-approve outcome.
        decided_by:
          type:
          - string
          - 'null'
          format: uuid
        decided_at:
          type:
          - string
          - 'null'
          format: date-time
        decision_comment:
          type: string
          description: Mandatory on time_change; present only when set.
        time_change_grounds:
          $ref: '#/components/schemas/TimeChangeGroundsCode'
          description: >-
            System-judged §60⑤ grounds code when status is `time_change_consult`.
            Omitted until a consult opens; never a free-text manager narrative.
        time_change_evidence:
          $ref: '#/components/schemas/TimeChangeCoverageEvidence'
          description: >-
            Coverage evidence snapshot that justified the grounds. Omitted until
            a consult opens.
        alternate_start_date:
          type: string
          format: date
          description: Worker-chosen alternate 시기 start; omitted until proposed.
        alternate_end_date:
          type: string
          format: date
          description: Worker-chosen alternate 시기 end; omitted until proposed.
        alternate_partial_day_period:
          type: string
          enum: [am, pm]
          description: >-
            Preserved partial-day period from the consult row when the original
            leave_type is half_day; omitted otherwise / until proposed.
        alternate_proposed_at:
          type: string
          format: date-time
          description: When the requester proposed alternate dates; omitted until then.
        ap_run_id:
          type: string
          format: uuid
          description: The engine AP- run, when the submittable definition exists.
        created_at:
          $ref: '#/components/schemas/Timestamp'
    LeaveRequestView:
      type: object
      description: One leave request in the approval queue (결재함 leave variant).
      required:
      - id
      - branch_id
      - requester_user_id
      - subject_employee_id
      - leave_type
      - days
      - start_date
      - end_date
      - reason
      - status
      - decided_by
      - decided_at
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        requester_user_id:
          $ref: '#/components/schemas/Uuid'
        subject_employee_id:
          $ref: '#/components/schemas/Uuid'
        leave_type:
          type: string
          enum: [annual, half_day]
        days:
          type: number
          format: double
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        reason:
          type: string
          description: >-
            Frozen v1 key. Requests filed since the 근로기준법 §60 guardrail carry
            no 사유 and serialize as an empty string; only historical rows still
            hold text.
        status:
          type: string
          enum: [pending, approved, time_change_consult, returned, rejected]
          description: >-
            `returned`/`rejected` are historical rows only — no decision can
            produce them since the §60 guardrail. `time_change_consult` is the
            lawful non-approve outcome.
        decided_by:
          type:
          - string
          - 'null'
          format: uuid
        decided_at:
          type:
          - string
          - 'null'
          format: date-time
        decision_comment:
          type: string
          description: Mandatory on time_change; present only when set.
        ap_run_id:
          type: string
          format: uuid
          description: The engine AP- run, when the submittable definition exists.
        created_at:
          $ref: '#/components/schemas/Timestamp'
    LeaveRosterEntry:
      type: object
      description: One employee's annual-leave balance row (직원별 연차 현황).
      required:
      - employee_id
      - name
      - team
      - grant
      - used
      - left
      - tone
      properties:
        employee_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        team:
          type:
          - string
          - 'null'
        grant:
          type: number
          format: double
        used:
          type: number
          format: double
        left:
          type: number
          format: double
        tone:
          type: string
          description: Bar color / 촉진 bucket — one of ok, promote, low.
          enum: [ok, promote, low]
    LeaveRosterPage:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/LeaveRosterEntry'
    LeaveSourceRevisionRef:
      type: object
      additionalProperties: false
      required: [kind, reference, revision]
      properties:
        kind: {type: string, minLength: 1, maxLength: 64}
        reference: {type: string, minLength: 1, maxLength: 256}
        revision: {type: string, minLength: 1, maxLength: 128}
    LeaveStatutoryPushView:
      type: object
      description: The result of a §61 push — the delivered notice + engine state.
      required:
      - id
      - kind
      - round
      - target_user_id
      - inbox_doc_id
      - ap_submission
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kind:
          type: string
          enum: [promotion, refusal]
        round:
          type: integer
          format: int32
        target_user_id:
          $ref: '#/components/schemas/Uuid'
        inbox_doc_id:
          $ref: '#/components/schemas/Uuid'
        ap_run_id:
          type: string
          format: uuid
          description: The engine AP- run, when the submittable definition exists.
        ap_submission:
          type: string
          description: submitted when a run was started, else pending_engine_definition.
          enum: [submitted, pending_engine_definition]
    LeaveUnits:
      type: string
      pattern: '^(0|[1-9][0-9]{0,9})\.[0-9]{6}$'
      example: '0.500000'
      description: Exact fixed-scale leave days; six fractional digits and no floating-point semantics.
    LegalHoldRecordView:
      type: object
      required:
        - id
        - evidence_object_id
        - status
        - case_ref
        - basis
        - reason
        - applied_by
        - applied_at
      properties:
        id:
          type: string
          format: uuid
        evidence_object_id:
          type: string
          format: uuid
        status:
          $ref: '#/components/schemas/LegalHoldStatus'
        case_ref:
          type: string
        basis:
          type: string
        reason:
          type: string
        applied_by:
          type: string
          format: uuid
        applied_at:
          type: string
          format: date-time
        released_by:
          type:
            - string
            - 'null'
          format: uuid
        released_at:
          type:
            - string
            - 'null'
          format: date-time
        release_reason:
          type:
            - string
            - 'null'
        audit_event_id:
          type:
            - string
            - 'null'
          format: uuid
    LegalHoldState:
      { type: string, enum: [CLEAR, ACTIVE] }
    LegalHoldStatus:
      { type: string, enum: [ACTIVE, RELEASED] }
    LifecycleOutcome:
      type: object
      required: [instance, config, gates]
      properties:
        instance: { $ref: '#/components/schemas/InstanceHead' }
        config: { $ref: '#/components/schemas/GateChainConfig' }
        gates: { $ref: '#/components/schemas/GateChainOutcome' }
    LifecycleRequest:
      type: object
      required:
        - to_state
      properties:
        to_state:
          $ref: '#/components/schemas/InstanceLifecycleState'
        reason:
          type:
            - string
            - 'null'
        checklist_all_acknowledged:
          type:
            - boolean
            - 'null'
        four_eyes_request_ref:
          type:
            - string
            - 'null'
          format: uuid
    LifecycleState:
      type: string
      description: Instance lifecycle state. Dispose is terminal; there is no hard delete.
      enum:
      - DRAFT
      - ACTIVE
      - LOCKED
      - ARCHIVED
      - DISPOSED
    LinkControlObligationRequest:
      type: object
      required: [control_id, obligation_id, coverage_level]
      properties:
        control_id: { $ref: '#/components/schemas/Uuid' }
        obligation_id: { $ref: '#/components/schemas/Uuid' }
        coverage_level: { $ref: '#/components/schemas/CoverageLevel' }
        coverage_rationale: { type: [string, 'null'] }
    LinkObligationRegulationRequest:
      type: object
      required: [obligation_id, regulation_impact_id, relationship]
      properties:
        obligation_id: { $ref: '#/components/schemas/Uuid' }
        regulation_impact_id: { $ref: '#/components/schemas/Uuid' }
        relationship: { $ref: '#/components/schemas/ObligationRegulationRelationship' }
        rationale: { type: [string, 'null'] }
    LinkSupportTicketRequest:
      type: object
      description: At least one of site_id / work_order_id must be present. An absent field leaves that link untouched; an
        explicit null clears it.
      properties:
        site_id:
          type:
          - string
          - 'null'
          format: uuid
        work_order_id:
          type:
          - string
          - 'null'
          format: uuid
    LinkTypeResponse:
      type: object
      description: A registered edge type (object-link relationship label).
      required:
      - link_type
      - description
      - status
      properties:
        link_type:
          type: string
        description:
          type: string
        status:
          type: string
          description: Edge-type lifecycle state (draft, active, archived).
          enum:
          - draft
          - active
          - archived
    ListingCondition:
      type: string
      description: Whether a listed unit is used (중고) or brand-new (신차).
      enum:
      - USED
      - NEW
    ListingKind:
      type: string
      description: Fuel / drive class of a listed forklift.
      enum:
      - ELECTRIC
      - DIESEL
      - LPG
      - REACH
    ListingMediaView:
      type: object
      description: One photo attached to a sales listing.
      required:
      - id
      - url
      - content_type
      - alt_text
      - sort_order
      properties:
        id:
          type: string
        url:
          type: string
        content_type:
          type: string
        alt_text:
          type:
          - string
          - 'null'
        sort_order:
          type: integer
          format: int32
    ListingStatus:
      type: string
      description: Publication lifecycle of a listing.
      enum:
      - DRAFT
      - PUBLISHED
      - RESERVED
      - SOLD
      - WITHDRAWN
    ListingType:
      type: string
      description: Whether a listing is offered for sale, rental, or both.
      enum:
      - SALE
      - RENTAL
      - BOTH
    LocationConsentLedgerEntry:
      type: object
      required:
      - id
      - consent_id
      - user_id
      - branch_id
      - action
      - from_status
      - to_status
      - occurred_at
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        consent_id:
          $ref: '#/components/schemas/Uuid'
        user_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        actor:
          type:
          - string
          - 'null'
          format: uuid
        action:
          type: string
          enum:
          - consent.grant
          - consent.suspend
          - consent.resume
          - consent.withdraw
        from_status:
          $ref: '#/components/schemas/LocationConsentState'
        to_status:
          $ref: '#/components/schemas/LocationConsentState'
        occurred_at:
          $ref: '#/components/schemas/Timestamp'
        created_at:
          $ref: '#/components/schemas/Timestamp'
    LocationConsentLedgerPage:
      type: object
      required:
      - items
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/LocationConsentLedgerEntry'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
    LocationConsentState:
      type: string
      enum:
      - NO_RECORD
      - GRANTED
      - SUSPENDED
      - WITHDRAWN
    LocationConsentStatus:
      type: object
      required:
      - consent_id
      - user_id
      - branch_id
      - state
      - may_collect
      properties:
        consent_id:
          $ref: '#/components/schemas/Uuid'
        user_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        state:
          $ref: '#/components/schemas/LocationConsentState'
        may_collect:
          type: boolean
        granted_at:
          type:
          - string
          - 'null'
          format: date-time
        suspended_at:
          type:
          - string
          - 'null'
          format: date-time
        resumed_at:
          type:
          - string
          - 'null'
          format: date-time
        withdrawn_at:
          type:
          - string
          - 'null'
          format: date-time
        updated_at:
          type:
          - string
          - 'null'
          format: date-time
    LocationConsentTransitionRequest:
      type: object
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
    LocationPingRequest:
      type: object
      required:
      - latitude
      - longitude
      - recorded_at
      - on_duty
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        latitude:
          type: number
          format: double
          minimum: -90
          maximum: 90
        longitude:
          type: number
          format: double
          minimum: -180
          maximum: 180
        accuracy_m:
          type:
          - number
          - 'null'
          format: double
          minimum: 0
        recorded_at:
          $ref: '#/components/schemas/Timestamp'
        on_duty:
          type: boolean
    LogisticsAsnCreated:
      type: object
      required: [id, status, branchId]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        status: { type: string, enum: [EXPECTED] }
        branchId: { $ref: '#/components/schemas/Uuid' }
    LogisticsAsnPutaway:
      type: object
      required: [id, status]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        status: { type: string, enum: [PUTAWAY] }
    LogisticsAsnReceipt:
      type: object
      required: [id, status]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        status: { type: string, enum: [PARTIAL_RECEIVED, RECEIVED] }
        receivedQuantity: { type: integer, format: int64, description: cumulative received total; absent on an idempotent replay }
        replayed: { type: boolean, description: present and true only on an idempotent replay }
    LogisticsFulfillmentPacked:
      type: object
      required: [id, status, pickedQuantity]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        status: { type: string, enum: [PACKED] }
        pickedQuantity: { type: integer, format: int64 }
    LogisticsFulfillmentPicked:
      type: object
      required: [id, status, pickedQuantity]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        status: { type: string, enum: [PICKED, SHORT_PICK] }
        pickedQuantity: { type: integer, format: int64 }
    LogisticsFulfillmentReleased:
      type: object
      required: [id, status, reservedQuantity]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        status: { type: string, enum: [RELEASED] }
        reservedQuantity: { type: integer, format: int64 }
    LogisticsPodVerified:
      type: object
      required: [id, status, recipientConfirmedEvidenceReference, slaAssessment]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        status: { type: string, enum: [DELIVERED] }
        recipientConfirmedEvidenceReference: { type: string }
        slaAssessment: { type: string, enum: [MET, BREACHED] }
    LogisticsShipmentDispatched:
      type: object
      required: [id, fulfillmentId, status]
      properties:
        id: { $ref: '#/components/schemas/Uuid', description: created shipment id }
        fulfillmentId: { $ref: '#/components/schemas/Uuid' }
        status: { type: string, enum: [DISPATCHED] }
    LogisticsShipmentSettlement:
      type: object
      required:
        - id
        - status
        - operationalCost
        - financeGlPosting
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
            - SETTLED
        operationalCost:
          type: object
          required:
            - currency
            - amountMinor
          properties:
            currency: {type: string, enum: [KRW]}
            amountMinor: {type: integer, format: int64}
        financeGlPosting:
          description: Always null by design — no GL/finance edge in the pilot
          type: 'null'
    LogisticsTimeTuple:
      type: array
      minItems: 9
      maxItems: 9
      items: { type: integer }
      description: 'Wire form of the logistics pilot datetime fields (dueAt, confirmedAt, settledAt): the time-crate
        default serde tuple [year, ordinal-day, hour, minute, second, nanosecond, offsetHours, offsetMinutes, offsetSeconds].
        The deployed rest crate deserializes these as plain OffsetDateTime without rfc3339 serde, so an RFC3339 string is
        rejected with 422. Encode in UTC (offsets 0). Divergence recorded in
        docs/evidence/console/CAP-LOGISTICS-PILOT/manifests/openapi-applied-notes.md.'
    LogoutRequest:
      $ref: '#/components/schemas/RefreshTokenRequest'
    MailAccountView:
      type: object
      description: The write-only view of a configured mailbox. It NEVER contains a password; has_smtp_password / has_imap_password
        signal whether a credential is on file.
      required:
      - id
      - display_name
      - email_address
      - imap_host
      - imap_port
      - imap_security
      - imap_username
      - smtp_host
      - smtp_port
      - smtp_security
      - smtp_username
      - has_smtp_password
      - has_imap_password
      - status
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type: string
        email_address:
          type: string
        from_name:
          type:
          - string
          - 'null'
        imap_host:
          type: string
        imap_port:
          type: integer
          format: int32
        imap_security:
          $ref: '#/components/schemas/MailSecurity'
        imap_username:
          type: string
        smtp_host:
          type: string
        smtp_port:
          type: integer
          format: int32
        smtp_security:
          $ref: '#/components/schemas/MailSecurity'
        smtp_username:
          type: string
        has_smtp_password:
          type: boolean
        has_imap_password:
          type: boolean
        status:
          type: string
    MailAddress:
      type: object
      required:
      - address
      properties:
        address:
          type: string
          description: The bare mailbox, e.g. user@example.com.
        name:
          type:
          - string
          - 'null'
          description: Optional display name.
    MailAttachment:
      type: object
      required:
      - filename
      - content_type
      - content_base64
      properties:
        filename:
          type: string
        content_type:
          type: string
        content_base64:
          type: string
          description: Standard-base64 encoded attachment bytes.
    MailAttachmentDownload:
      type: object
      required:
      - url
      properties:
        url:
          type: string
          description: A short-lived presigned GET URL for the attachment bytes.
    MailAttachmentView:
      type: object
      required:
      - id
      - filename
      - content_type
      - size_bytes
      - is_inline
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        filename:
          type: string
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        is_inline:
          type: boolean
    MailFolderView:
      type: object
      required:
      - id
      - role
      - name
      - unread_count
      - total_count
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        role:
          type: string
          description: INBOX | SENT | DRAFTS | ARCHIVE | TRASH | JUNK | CUSTOM.
        name:
          type: string
        unread_count:
          type: integer
          format: int64
        total_count:
          type: integer
          format: int64
    MailMessageView:
      type: object
      required:
      - id
      - thread_id
      - direction
      - from_address
      - to
      - cc
      - subject
      - snippet
      - seen
      - flagged
      - answered
      - has_attachments
      - received_at
      - attachments
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        thread_id:
          $ref: '#/components/schemas/Uuid'
        direction:
          type: string
          description: IN (mirrored from IMAP) or OUT (sent by the tenant).
        message_id:
          type:
          - string
          - 'null'
        in_reply_to:
          type:
          - string
          - 'null'
        from_address:
          type: string
        from_name:
          type:
          - string
          - 'null'
        to:
          type: array
          items:
            $ref: '#/components/schemas/MailAddress'
        cc:
          type: array
          items:
            $ref: '#/components/schemas/MailAddress'
        subject:
          type: string
        snippet:
          type: string
        body_text:
          type:
          - string
          - 'null'
        body_html:
          type:
          - string
          - 'null'
          description: The stored HTML body, returned verbatim. The client MUST sanitize it (DOMPurify-class) before rendering.
        seen:
          type: boolean
        flagged:
          type: boolean
        answered:
          type: boolean
        has_attachments:
          type: boolean
        received_at:
          type: string
          format: date-time
        attachments:
          type: array
          items:
            $ref: '#/components/schemas/MailAttachmentView'
    MailSecurity:
      type: string
      description: Transport security. SSL_TLS is implicit TLS (port 993/465); START_TLS upgrades a plaintext connection (port
        143/587). There is no plaintext option.
      enum:
      - SSL_TLS
      - START_TLS
    MailTestConnectionResult:
      type: object
      required:
      - ok
      properties:
        ok:
          type: boolean
        error_code:
          type:
          - string
          - 'null'
          description: A stable, non-secret token when the connection failed.
    MailThreadDetail:
      type: object
      required:
      - id
      - subject
      - messages
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        subject:
          type: string
        messages:
          type: array
          items:
            $ref: '#/components/schemas/MailMessageView'
    MailThreadReadStateRequest:
      type: object
      required:
      - seen
      properties:
        seen:
          type: boolean
          description: True marks inbound messages in the thread read; false marks them unread.
    MailThreadView:
      type: object
      required:
      - id
      - subject
      - last_message_at
      - message_count
      - unread_count
      - has_attachments
      - is_flagged
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        subject:
          type: string
        last_message_at:
          type: string
          format: date-time
        message_count:
          type: integer
          format: int64
        unread_count:
          type: integer
          format: int64
        has_attachments:
          type: boolean
        is_flagged:
          type: boolean
    MaintenanceCause:
      type: string
      description: Typed maintenance cause (원인).
      enum: [BREAKDOWN, RETURN_PREP, SCHEDULED, INSPECTION_FINDING, OTHER]
    MaintenanceType:
      type: string
      description: Typed maintenance classification (유형).
      enum: [EMERGENCY, CORRECTIVE, PREVENTIVE, INSPECTION]
    MarkMessengerThreadReadRequest:
      type: object
      required:
      - last_read_message_id
      properties:
        last_read_message_id:
          $ref: '#/components/schemas/Uuid'
    MeAuthzCapability:
      type: object
      properties:
        feature:
          type: string
          description: Feature key, matching `/api/v1/policy/features`.
        permission:
          type: string
          description: Effective permission level (`deny` is never emitted).
          enum:
          - request_only
          - limited
          - allow
        branch_scope:
          allOf:
          - $ref: '#/components/schemas/BranchScope'
          description: >-
            The branch subset this `permission` level actually holds over —
            not necessarily the caller's full `branch_scope`. A
            branch-narrowed custom grant only elevates the capability within
            its own branches; the caller must intersect this with the target
            branch before offering the affordance (otherwise the UI can offer
            an action the server's `authorize` call would then reject outside
            this scope).
      required:
      - feature
      - permission
      - branch_scope
    MeAuthzResponse:
      type: object
      description: >-
        NON-AUTHORITATIVE authorization projection for the caller. A rendering
        hint only — the backend matrix is the sole enforcer.
      properties:
        authority:
          type: string
          description: Always `advisory_ui_only`; never an authorization decision.
          enum:
          - advisory_ui_only
        source:
          type: string
          description: >-
            Grant source. `legacy_matrix` today; flips to `cedar` on the
            enforce-promotion with this shape unchanged.
          enum:
          - legacy_matrix
          - cedar
        user_id:
          $ref: '#/components/schemas/Uuid'
        org_id:
          $ref: '#/components/schemas/Uuid'
        roles:
          type: array
          description: Canonical role keys carried as principal attributes.
          items:
            type: string
        branch_scope:
          $ref: '#/components/schemas/BranchScope'
        capabilities:
          type: array
          description: >-
            Capability grants the caller holds (deny-by-omission — a feature the
            caller cannot use is omitted, never present at `deny`).
          items:
            $ref: '#/components/schemas/MeAuthzCapability'
      required:
      - authority
      - source
      - user_id
      - org_id
      - roles
      - branch_scope
      - capabilities
    MediaKind:
      type: string
      enum:
      - IMAGE
      - VIDEO
    MessengerAckSummary:
      type: object
      required:
      - message_id
      - thread_id
      - acked
      - ack_count
      properties:
        message_id:
          $ref: '#/components/schemas/Uuid'
        thread_id:
          $ref: '#/components/schemas/Uuid'
        acked:
          type: boolean
          description: Whether the caller's ack is present after the toggle.
        ack_count:
          type: integer
          format: int64
    MessengerMemberListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MessengerMemberSummary'
    MessengerMemberPresence:
      type: object
      required:
      - user_id
      - display_name
      - last_activity_at
      - status
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type:
          - string
          - 'null'
        last_activity_at:
          type:
          - string
          - 'null'
          format: date-time
        status:
          $ref: '#/components/schemas/MessengerPresenceStatus'
    MessengerMemberPresenceListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MessengerMemberPresence'
    MessengerMemberSummary:
      type: object
      required:
      - id
      - display_name
      - team
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type: string
        team:
          type:
          - string
          - 'null'
    MessengerMessageListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MessengerMessageSummary'
    MessengerMessagePage:
      type: object
      required:
      - items
      - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MessengerMessageSummary'
        next_cursor:
          type:
          - string
          - 'null'
          format: uuid
    MessengerMessageSummary:
      type: object
      required:
      - id
      - thread_id
      - branch_id
      - sender_id
      - sender_name
      - body
      - attachment_evidence_ids
      - read_count
      - read_target_count
      - ack_count
      - acked_by_me
      - quoted_message_id
      - quoted_body
      - quoted_sender_name
      - sent_at
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        thread_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        sender_id:
          $ref: '#/components/schemas/Uuid'
        sender_name:
          type:
          - string
          - 'null'
          description: Sender display name, resolved via a same-org LEFT JOIN on users. Null when the sender account no longer
            exists.
        body:
          type: string
        attachment_evidence_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        read_count:
          type: integer
          format: int64
          description: Non-sender thread members whose read receipt has reached this message.
        read_target_count:
          type: integer
          format: int64
          description: Non-sender thread members expected to read this message.
        ack_count:
          type: integer
          format: int64
          description: Members who have acked ("확인") this message.
        acked_by_me:
          type: boolean
          description: Whether the reading caller has acked this message. Always false on a realtime message-posted event.
        quoted_message_id:
          type:
          - string
          - 'null'
          format: uuid
          description: Reply-quote target, when this message quotes an earlier one in the thread.
        quoted_body:
          type:
          - string
          - 'null'
          description: Short preview of the quoted message; null when nothing is quoted or the quote was deleted.
        quoted_sender_name:
          type:
          - string
          - 'null'
        sent_at:
          $ref: '#/components/schemas/Timestamp'
        created_at:
          $ref: '#/components/schemas/Timestamp'
    MessengerPresence:
      type: object
      required:
      - user_id
      - display_name
      - last_activity_at
      - status
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type:
          - string
          - 'null'
        last_activity_at:
          type:
          - string
          - 'null'
          format: date-time
        status:
          $ref: '#/components/schemas/MessengerPresenceStatus'
    MessengerPresenceListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MessengerPresence'
    MessengerPresenceStatus:
      type: string
      description: Activity-derived presence — online within the freshness window of the member's last action, else away,
        else offline (or never seen).
      enum:
      - online
      - away
      - offline
    MessengerReadReceiptSummary:
      type: object
      required:
      - thread_id
      - user_id
      - last_read_message_id
      - read_at
      - updated_at
      properties:
        thread_id:
          $ref: '#/components/schemas/Uuid'
        user_id:
          $ref: '#/components/schemas/Uuid'
        last_read_message_id:
          $ref: '#/components/schemas/Uuid'
        read_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    MessengerThreadKind:
      type: string
      enum:
      - work_order
      - team
      - dm
      - group
    MessengerThreadListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MessengerThreadSummary'
    MessengerThreadMuteSummary:
      type: object
      required:
      - thread_id
      - muted
      properties:
        thread_id:
          $ref: '#/components/schemas/Uuid'
        muted:
          type: boolean
    MessengerThreadSummary:
      type: object
      required:
      - id
      - kind
      - visibility
      - visibility
      - muted
      - branch_id
      - title
      - work_order_id
      - muted
      - last_message_id
      - last_message_at
      - member_count
      - unread_count
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kind:
          $ref: '#/components/schemas/MessengerThreadKind'
        visibility:
          $ref: '#/components/schemas/MessengerThreadVisibility'
        muted:
          type: boolean
          description: Whether the caller has muted this thread (excluded from the unread badge total; mentions suppressed).
        branch_id:
          $ref: '#/components/schemas/Uuid'
        title:
          type:
          - string
          - 'null'
        work_order_id:
          type:
          - string
          - 'null'
          format: uuid
        last_message_id:
          type:
          - string
          - 'null'
          format: uuid
        last_message_at:
          type:
          - string
          - 'null'
          format: date-time
        member_count:
          type: integer
          format: int64
        unread_count:
          type: integer
          format: int64
          description: Messages newer than the caller's read receipt, excluding messages sent by the caller.
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    MessengerThreadVisibility:
      type: string
      description: 'channel = a named, branch-scoped room any active branch member may join; direct = a fixed member set
        (DMs, work-order threads, groups).'
      enum:
      - channel
      - direct
    MobileApproveWorkOrderRequest:
      type: object
      required:
      - comment
      - step_up
      properties:
        comment:
          type: string
          minLength: 1
        step_up:
          $ref: '#/components/schemas/MobilePasskeyStepUpEnvelope'
    MobilePasskeyStepUpBinding:
      type: object
      required:
      - action_kind
      - object_id
      - reason_key
      - replay_attempt
      properties:
        action_kind:
          $ref: '#/components/schemas/MobileStepUpActionKind'
        object_id:
          $ref: '#/components/schemas/Uuid'
        reason_key:
          type: string
          enum:
          - operations_passkey_approval_decision
          - operations_passkey_poll_vote
        replay_attempt:
          type:
          - integer
          - 'null'
          format: int32
          minimum: 1
          description: Null for an immediate online action; 1-based for queued replay attempts.
    MobilePasskeyStepUpEnvelope:
      type: object
      required:
      - binding
      - assertion
      properties:
        binding:
          $ref: '#/components/schemas/MobilePasskeyStepUpBinding'
        assertion:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    MobilePasskeyStepUpStartRequest:
      type: object
      required:
      - binding
      properties:
        binding:
          $ref: '#/components/schemas/MobilePasskeyStepUpBinding'
    MobilePasskeyStepUpStartResponse:
      type: object
      required:
      - ceremony_id
      - challenge
      - expires_at
      - binding
      properties:
        ceremony_id:
          $ref: '#/components/schemas/Uuid'
        challenge:
          type: object
          additionalProperties: true
        expires_at:
          $ref: '#/components/schemas/Timestamp'
        binding:
          $ref: '#/components/schemas/MobilePasskeyStepUpBinding'
    MobileStepUpActionKind:
      type: string
      enum:
      - APPROVAL_DECISION
      - POLL_VOTE
    MobileVotePollRequest:
      type: object
      required:
      - selected_option_ids
      - step_up
      properties:
        selected_option_ids:
          type: array
          minItems: 1
          maxItems: 20
          items:
            $ref: '#/components/schemas/Uuid'
        step_up:
          $ref: '#/components/schemas/MobilePasskeyStepUpEnvelope'
    MyDispatchOffer:
      type: object
      description: >-
        One pending P1 offer for the signed-in mechanic: a BROADCASTING
        dispatch that fanned out to the caller, still inside its accept
        window, with no response from the caller yet.
      required:
      - dispatch_id
      - work_order_id
      - branch_id
      - request_no
      - accept_window_started_at
      - accept_window_ends_at
      properties:
        dispatch_id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        request_no:
          type: string
        accept_window_started_at:
          $ref: '#/components/schemas/Timestamp'
        accept_window_ends_at:
          $ref: '#/components/schemas/Timestamp'
    MyDispatchOfferPage:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MyDispatchOffer'
    MyLeaveBalance:
      type: object
      required:
        - employee_id
      properties:
        employee_id:
          type: string
          format: uuid
        leave_accrued:
          type:
            - string
            - 'null'
        leave_used:
          type:
            - string
            - 'null'
        leave_remaining:
          type:
            - string
            - 'null'
    MyLeaveOverview:
      type: object
      required: [balance, requests]
      properties:
        balance: { $ref: '#/components/schemas/MyLeaveBalance' }
        requests: { type: array, items: { $ref: '#/components/schemas/LeaveRequest' } }
    MyLeaveV2Overview:
      type: object
      required: [balance, requests]
      properties:
        balance:
          $ref: '#/components/schemas/SelfLeaveBalance'
        requests:
          $ref: '#/components/schemas/LeaveRequestV2Page'
    MyPayrollLine:
      type: object
      required:
      - run_id
      - period_start
      - period_end
      - run_status
      - calculation_status
      - gross_pay_source_present
      - net_pay_source_present
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        period_start:
          type: string
          format: date
        period_end:
          type: string
          format: date
        run_status:
          type: string
          enum: [STAGED, BLOCKED_LEGAL_GATE, READY_FOR_REVIEW, ATTENDANCE_CLOSED, CALCULATING, CALCULATED, SUBMITTED, REJECTED, APPROVED, DISBURSEMENT_SCHEDULED, PAID, ISSUED, VOID]
        calculation_status:
          type: string
          enum: [BLOCKED_LEGAL_GATE, READY_FOR_REVIEW, APPROVED, ISSUED, VOID]
        work_days:
          type:
          - number
          - 'null'
        regular_hours:
          type:
          - number
          - 'null'
        overtime_hours:
          type:
          - number
          - 'null'
        night_hours:
          type:
          - number
          - 'null'
        holiday_hours:
          type:
          - number
          - 'null'
        leave_used:
          type:
          - number
          - 'null'
        leave_remaining:
          type:
          - number
          - 'null'
        gross_pay_source_present:
          type: boolean
        net_pay_source_present:
          type: boolean
    MyPayrollLinePage:
      type: object
      required:
      - items
      - total
      - limit
      - offset
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MyPayrollLine'
        total:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
    MyWorkbenchResponse:
      type: object
      additionalProperties: false
      required:
      - as_of
      - timezone
      - range
      - scope
      - partial
      - action_inbox
      - todos
      - calendar
      properties:
        as_of:
          $ref: '#/components/schemas/Timestamp'
        timezone:
          type: string
          enum:
          - Asia/Seoul
        range:
          $ref: '#/components/schemas/WorkbenchRange'
        scope:
          $ref: '#/components/schemas/WorkbenchEffectiveScope'
        partial:
          type: boolean
        action_inbox:
          $ref: '#/components/schemas/WorkbenchActionSourceEnvelope'
        todos:
          $ref: '#/components/schemas/WorkbenchTodoSourceEnvelope'
        calendar:
          $ref: '#/components/schemas/WorkbenchCalendarSourceEnvelope'
    NamedEntity:
      type: object
      required:
      - id
      - name
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
    NoticeAudienceInput:
      type: object
      required:
      - scope
      properties:
        scope:
          type: string
          enum: [org, branches]
        branch_ids:
          type: array
          description: Non-empty iff scope is branches; must be empty or omitted for org (422 otherwise).
          items:
            $ref: '#/components/schemas/Uuid'
    NoticeCategory:
      type: string
      enum: [general, legal, hr_order, training]
    NoticeMyReceipt:
      type: object
      required:
      - acknowledged_at
      properties:
        acknowledged_at:
          type:
          - string
          - 'null'
          format: date-time
          description: 수령확인 timestamp; null until the caller acknowledges.
    NoticeProgress:
      type: object
      required:
      - total
      - acknowledged
      properties:
        total:
          type: integer
          format: int64
        acknowledged:
          type: integer
          format: int64
    NoticeReceipt:
      type: object
      required:
      - recipient_user_id
      - display_name
      - acknowledged_at
      properties:
        recipient_user_id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type: string
        acknowledged_at:
          type:
          - string
          - 'null'
          format: date-time
          description: 수령확인 timestamp; null while the recipient is outstanding.
    NoticeReceiptPage:
      type: object
      required:
      - items
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/NoticeReceipt'
        total:
          type: integer
          format: int64
          description: Total matching rows before limit/offset.
    NoticeSummary:
      type: object
      required:
      - id
      - code
      - author_user_id
      - title
      - body
      - status
      - published_at
      - created_at
      - category
      - audience_scope
      - audience_branches
      - my_receipt
      - progress
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type:
          - string
          - 'null'
          description: NT- code, set only once published.
        author_user_id:
          $ref: '#/components/schemas/Uuid'
        title:
          type: string
        body:
          type: string
        status:
          type: string
          enum: [draft, published]
        published_at:
          type:
          - string
          - 'null'
          format: date-time
        created_at:
          $ref: '#/components/schemas/Timestamp'
        category:
          $ref: '#/components/schemas/NoticeCategory'
        audience_scope:
          type: string
          enum: [org, branches]
        audience_branches:
          type: array
          description: Audience branches with display names; empty for org-wide notices.
          items:
            $ref: '#/components/schemas/NamedEntity'
        my_receipt:
          oneOf:
          - $ref: '#/components/schemas/NoticeMyReceipt'
          - type: 'null'
          description: The caller's own 수령확인 state; null when the caller is not a snapshotted recipient.
        progress:
          oneOf:
          - $ref: '#/components/schemas/NoticeProgress'
          - type: 'null'
          description: Hydrated only for NoticeManage callers; null otherwise.
    NotificationCategoryCount:
      type: object
      required:
      - category
      - unread
      properties:
        category:
          type: string
        unread:
          type: integer
          format: int64
    NotificationCountsSummary:
      type: object
      required:
      - total_unread
      - by_category
      - muted_unread
      properties:
        total_unread:
          type: integer
          format: int64
          description: Unread rows that want attention; rows suppressed by the caller's mute policies are excluded.
        by_category:
          type: array
          description: Per-category unread breakdown, mute-suppressed rows excluded; a category whose unread is entirely muted is omitted rather than shown as zero.
          items:
            $ref: '#/components/schemas/NotificationCategoryCount'
        muted_unread:
          type: integer
          format: int64
          description: Unread rows suppressed by the caller's mute policies, across all categories.
    NotificationLink:
      description: Deep-link target — an object reference (kind + id) or a bare app screen.
      oneOf:
      - type: object
        required:
        - type
        - kind
        - id
        properties:
          type:
            type: string
            enum:
            - object
          kind:
            type: string
          id:
            type: string
      - type: object
        required:
        - type
        - screen
        properties:
          type:
            type: string
            enum:
            - screen
          screen:
            type: string
    NotificationObjectGroup:
      type: object
      required:
      - link
      - total
      - unread
      - categories
      - latest
      - muted
      properties:
        link:
          $ref: '#/components/schemas/NotificationLink'
        total:
          type: integer
          format: int64
        unread:
          type: integer
          format: int64
          description: Unread rows in the group; annotated, never mute-filtered.
        categories:
          type: array
          description: Per-category unread breakdown within the group; zero-unread categories are omitted.
          items:
            $ref: '#/components/schemas/NotificationCategoryCount'
        latest:
          $ref: '#/components/schemas/NotificationSummary'
        muted:
          type: boolean
          description: True when an all-scope or matching object-scope mute policy covers this object for the caller; category-scope policies never flip the group bell.
    NotificationObjectGroupPage:
      type: object
      required:
      - items
      - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/NotificationObjectGroup'
        next_cursor:
          type:
          - string
          - 'null'
          description: Opaque keyset cursor for the next page; null at the end.
    NotificationPage:
      type: object
      required:
      - items
      - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/NotificationSummary'
        next_cursor:
          type:
          - string
          - 'null'
          format: uuid
    NotificationPolicyList:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/NotificationPolicySummary'
    NotificationPolicySummary:
      type: object
      required:
      - id
      - scope
      - action
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        scope:
          type: string
          enum:
          - all
          - category
          - object
        category:
          type: string
          description: Present only when scope is category.
        link:
          $ref: '#/components/schemas/NotificationLink'
        action:
          type: string
          description: Extensible routing action ("mute" today; e.g. "watch" later).
        created_at:
          $ref: '#/components/schemas/Timestamp'
    NotificationReadAllResponse:
      type: object
      required:
      - marked
      properties:
        marked:
          type: integer
          format: int64
          description: The number of notifications marked read.
    NotificationSummary:
      type: object
      required:
      - id
      - recipient_user_id
      - category
      - kind
      - text
      - link
      - unread
      - created_at
      - read_at
      - resolved_at
      - muted
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        recipient_user_id:
          $ref: '#/components/schemas/Uuid'
        category:
          type: string
          description: Extensible category (결재/멘션/문서/공지/근태/급여 and beyond).
        kind:
          type: string
          description: Extensible producer kind (default "info"); generic detect->assign->resolve chains key on this.
        text:
          type: string
        link:
          $ref: '#/components/schemas/NotificationLink'
        unread:
          type: boolean
        created_at:
          $ref: '#/components/schemas/Timestamp'
        read_at:
          type:
          - string
          - 'null'
          format: date-time
          description: When the notification was first marked read; null while unread.
        resolved_at:
          type:
          - string
          - 'null'
          format: date-time
          description: When a producer's resolve-by-link sweep closed this notification; null while open.
        muted:
          type: boolean
          description: Whether the caller's mute policies suppress this row's attention (badge counts, realtime). Computed per caller at read time, never stored, and never filters the list.
    ObjectActionCatalogResponse:
      type: object
      required:
      - object_type
      - object_id
      - actions
      properties:
        object_type:
          type: string
        object_id:
          $ref: '#/components/schemas/Uuid'
        actions:
          type: array
          items:
            $ref: '#/components/schemas/ObjectActionDescriptor'
    ObjectActionDescriptor:
      type: object
      required:
      - action_id
      - object_type
      - object_id
      - label
      - description
      - submit_label
      - requires_passkey_step_up
      - risk_level
      - fields
      properties:
        action_id:
          type: string
          example: equipment.update_profile
        object_type:
          type: string
        object_id:
          $ref: '#/components/schemas/Uuid'
        label:
          type: string
        description:
          type: string
        submit_label:
          type: string
        requires_passkey_step_up:
          type: boolean
        risk_level:
          type: string
          enum:
          - sensitive_write
        fields:
          type: array
          items:
            $ref: '#/components/schemas/ObjectActionFieldDescriptor'
    ObjectActionExecutionResponse:
      type: object
      required:
      - execution_id
      - action_id
      - object_type
      - object_id
      - status
      - audit_event_id
      - target_href
      - message
      properties:
        execution_id:
          $ref: '#/components/schemas/Uuid'
        action_id:
          type: string
        object_type:
          type: string
        object_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
          enum:
          - succeeded
        audit_event_id:
          $ref: '#/components/schemas/Uuid'
        target_href:
          type: string
        message:
          type: string
    ObjectActionFieldDescriptor:
      type: object
      required:
      - field_key
      - label
      - field_type
      - required
      - options
      properties:
        field_key:
          type: string
        label:
          type: string
        field_type:
          type: string
          enum:
          - text
          - select
        required:
          type: boolean
        current_value:
          type:
          - string
          - 'null'
        options:
          type: array
          items:
            $ref: '#/components/schemas/ObjectActionFieldOption'
    ObjectActionFieldOption:
      type: object
      required:
      - value
      - label
      properties:
        value:
          type: string
        label:
          type: string
    ObjectGraphResponse:
      type: object
      description: The bounded, caller-visible neighborhood of an object, walked over object_links. Unresolvable
        nodes are omitted entirely (never returned as stubs), and edges touching an omitted node are omitted too.
      required:
      - nodes
      - edges
      - truncated
      properties:
        nodes:
          type: array
          description: Every object the caller can resolve, each passed through the same visibility guard as resolveObject.
          items:
            $ref: '#/components/schemas/ObjectHead'
        edges:
          type: array
          description: Links between resolved nodes (the induced subgraph over the visible node set).
          items:
            $ref: '#/components/schemas/ObjectLinkResponse'
        truncated:
          type: boolean
          description: True if the node cap was hit before the walk exhausted the requested depth.
    ObjectHead:
      type: object
      description: >-
        Compact, kind-agnostic head for any object. exists=false means the object is absent OR outside the caller's scope
        (indistinguishable, by design). Carries no route/URL — objectRegistry (frontend) is the sole kind->URL authority.
      required:
        - kind
        - id
        - exists
      properties:
        kind:
          type: string
        id:
          type: string
        code:
          type:
            - string
            - 'null'
          description: Canonical issued code if the kind has one (e.g. work-order request_no); absent otherwise.
        title:
          type:
            - string
            - 'null'
          description: Human display label if available.
        status:
          type:
            - string
            - 'null'
          description: Domain status string if available.
        exists:
          type: boolean
    ObjectLifecycle:
      type: object
      required:
      - objectType
      - objectId
      - currentState
      - legalHold
      - createdAt
      - updatedAt
      - transitions
      properties:
        objectType:
          type: string
        objectId:
          type: string
          format: uuid
        currentState:
          type: string
        legalHold:
          type: boolean
        retentionUntil:
          type: string
          format: date
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        transitions:
          type: array
          items:
            $ref: '#/components/schemas/ObjectLifecycleTransition'
    ObjectLifecycleTransition:
      type: object
      required:
      - fromState
      - toState
      - reason
      - occurredAt
      properties:
        fromState:
          type: string
        toState:
          type: string
        actor:
          type: string
          format: uuid
        reason:
          type: string
        occurredAt:
          type: string
          format: date-time
    ObjectLinkResponse:
      type: object
      required:
        - id
        - src_kind
        - src_id
        - dst_kind
        - dst_id
        - link_type
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        src_kind:
          type: string
        src_id:
          type: string
        dst_kind:
          type: string
        dst_id:
          type: string
        link_type:
          type: string
        created_by:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        created_at:
          $ref: '#/components/schemas/Timestamp'
    ObjectLinksListResponse:
      type: object
      description: Links touching one object, split by direction.
      required:
      - outgoing
      - incoming
      properties:
        outgoing:
          type: array
          description: Links where the queried object is the source.
          items:
            $ref: '#/components/schemas/ObjectLinkResponse'
        incoming:
          type: array
          description: Links where the queried object is the destination.
          items:
            $ref: '#/components/schemas/ObjectLinkResponse'
    ObjectTypeResponse:
      type: object
      description: An object-type registry row plus the caller-visible active instance count.
      required:
        - kind
        - description
        - status
        - active_count
      properties:
        kind:
          type: string
        code_prefix:
          type:
            - string
            - 'null'
          description: Canonical per-kind code prefix (e.g. AP-, CS-); absent for id/name-referenced kinds.
        description:
          type: string
        status:
          type: string
          description: Type lifecycle state (draft, active, archived).
          enum:
            - draft
            - active
            - archived
        active_count:
          type: integer
          format: int64
          description: Instances of this kind visible to the caller (same per-kind visibility as resolveObject).
    ObjectTypeSummary:
      type: object
      required: [id, stable_key, title, backing_kind, schema_version, lifecycle_state, key_write_revision, key_write_etag]
      properties:
        id: { type: string, format: uuid }
        stable_key: { type: string }
        title: { type: string }
        backing_kind: { type: string, enum: [instance, projected] }
        schema_version: { type: integer, format: int64 }
        lifecycle_state: { type: string, enum: [draft, review_pending, published, superseded, retired] }
        key_write_revision: { type: integer, format: int64, minimum: 1 }
        key_write_etag:
          type: string
          pattern: '^"ont-object-type-key:[0-9a-f]{32}:r[1-9][0-9]*"$'
    ObligationRegulationLink:
      type: object
      required: [id, obligation_id, regulation_impact_id, relationship, created_by, created_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        obligation_id: { $ref: '#/components/schemas/Uuid' }
        regulation_impact_id: { $ref: '#/components/schemas/Uuid' }
        relationship: { $ref: '#/components/schemas/ObligationRegulationRelationship' }
        rationale: { type: [string, 'null'] }
        created_by: { $ref: '#/components/schemas/Uuid' }
        created_at: { $ref: '#/components/schemas/Timestamp' }
    ObligationRegulationRelationship:
      type: string
      enum: [DERIVED_FROM, AMENDED_BY, SUPERSEDED_BY, INTERPRETS, EVIDENCES]
    ObligationStatus:
      type: string
      enum: [DRAFT, ACTIVE, WAIVED, SUPERSEDED, ARCHIVED]
    ObligationType:
      type: string
      enum: [LEGAL, REGULATORY, CONTRACTUAL, INTERNAL_POLICY, CONTROL_REQUIREMENT]
    OntologyActionCommandReceipt:
      type: object
      description: Immutable replay receipt for an accepted instance_revision action command.
      required:
      - command_id
      - payload_digest
      - instance
      - gates
      properties:
        command_id:
          $ref: '#/components/schemas/Uuid'
        payload_digest:
          type: string
          pattern: '^[a-f0-9]{64}$'
        instance:
          type: object
          additionalProperties: true
        gates:
          type: object
          additionalProperties: true
    OntologyActionExecuteOutcome:
      type: object
      required:
      - dispatch
      - gates
      properties:
        dispatch:
          type: string
          enum: [instance_revision, projected_usecase]
        gates:
          type: object
          additionalProperties: true
        instance:
          type: object
          additionalProperties: true
        projected:
          type: object
          additionalProperties: true
        receipt:
          $ref: '#/components/schemas/OntologyActionCommandReceipt'
    OntologyActionRequest:
      type: object
      description: An object-action preflight/execute request. Canonical DispatchTarget `params` `$ref` the typed input schemas; generic instance-revision actions keep a free-form object validated against the action's `params_schema` at runtime.
      additionalProperties: false
      required:
      - object_type_id
      properties:
        object_type_id:
          $ref: '#/components/schemas/Uuid'
        instance_id:
          $ref: '#/components/schemas/Uuid'
        title:
          type: string
        params:
          description: >-
            Typed params for the thirteen DispatchTarget action_keys, or a free-form
            object for generic instance-revision actions. The path `action_key` selects
            the codec; a caller-supplied `target` / `action_key` property is refused.
          anyOf:
          - $ref: '#/components/schemas/CompanyReviseInput'
          - $ref: '#/components/schemas/OrganizationCreateOrgUnitInput'
          - $ref: '#/components/schemas/OrganizationReviseOrgUnitInput'
          - $ref: '#/components/schemas/OrganizationCreateJobPositionInput'
          - $ref: '#/components/schemas/OrganizationReviseJobPositionInput'
          - $ref: '#/components/schemas/PeopleCreatePersonInput'
          - $ref: '#/components/schemas/PeopleRevisePersonInput'
          - $ref: '#/components/schemas/HrAppointInput'
          - $ref: '#/components/schemas/HrPromoteInput'
          - $ref: '#/components/schemas/HrTransferInput'
          - $ref: '#/components/schemas/PayrollCreateRunInput'
          - $ref: '#/components/schemas/PayrollSubmitRunInput'
          - $ref: '#/components/schemas/PayrollDecideRunInput'
          - type: object
            additionalProperties: true
            description: >-
              Non-DispatchTarget instance-revision params (`create`, `set_priority`,
              …). Runtime `params_schema` still fail-closes undeclared keys.
        reason:
          type: string
        valid_from:
          $ref: '#/components/schemas/Timestamp'
        checklist_all_acknowledged:
          type: boolean
        four_eyes_request_ref:
          $ref: '#/components/schemas/Uuid'
        command_id:
          $ref: '#/components/schemas/Uuid'
        expected_revision:
          type: integer
          format: int64
          minimum: 1
    OntologyInstanceAggregateBucket:
      type: object
      required:
        - count
      properties:
        key:
          type:
            - string
            - 'null'
          description: Text form of the group value; null when the attribute is absent.
        count:
          type: integer
          format: int64
          minimum: 0
    OpenCycleCountRequest:
      type: object
      additionalProperties: false
      required: [branchId, stockLocationId]
      properties: { branchId: { $ref: '#/components/schemas/Uuid' }, stockLocationId: { $ref: '#/components/schemas/Uuid' } }
    OpsEquipmentStatus:
      type: object
      required:
      - rented
      - spare
      - scrapped
      - replacement
      - sold
      properties:
        rented:
          type: integer
          format: int32
          minimum: 0
        spare:
          type: integer
          format: int32
          minimum: 0
        scrapped:
          type: integer
          format: int32
          minimum: 0
        replacement:
          type: integer
          format: int32
          minimum: 0
        sold:
          type: integer
          format: int32
          minimum: 0
    OpsFunnel:
      type: object
      required:
      - received
      - assigned
      - in_progress
      - completed
      properties:
        received:
          type: integer
          format: int32
          minimum: 0
        assigned:
          type: integer
          format: int32
          minimum: 0
        in_progress:
          type: integer
          format: int32
          minimum: 0
        completed:
          type: integer
          format: int32
          minimum: 0
    OpsMechanicLoad:
      type: object
      required:
      - mechanic_id
      - display_name
      - active_assignments
      properties:
        mechanic_id:
          type: string
          format: uuid
        display_name:
          type: string
        active_assignments:
          type: integer
          format: int32
          minimum: 0
    OpsSummary:
      type: object
      required:
      - funnel
      - aging_hours
      - aging_work_orders
      - sla_breached
      - sla_at_risk
      - mechanic_load
      - equipment_status
      - active_substitutions
      - pending_approvals
      - open_support_tickets
      properties:
        funnel:
          $ref: '#/components/schemas/OpsFunnel'
        aging_hours:
          type: integer
          format: int32
          minimum: 0
        aging_work_orders:
          type: integer
          format: int32
          minimum: 0
        sla_breached:
          type: integer
          format: int32
          minimum: 0
        sla_at_risk:
          type: integer
          format: int32
          minimum: 0
        mechanic_load:
          type: array
          items:
            $ref: '#/components/schemas/OpsMechanicLoad'
        equipment_status:
          $ref: '#/components/schemas/OpsEquipmentStatus'
        active_substitutions:
          type: integer
          format: int32
          minimum: 0
        pending_approvals:
          type: integer
          format: int32
          minimum: 0
        open_support_tickets:
          type: integer
          format: int32
          minimum: 0
    OrgChangeApprovalRoleKey:
      type: string
      enum: [hr, finance, legal, executive]
    OrgChangeApprovalStep:
      type: object
      required:
      - id
      - stepOrder
      - roleKey
      - decision
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        stepOrder:
          type: integer
          format: int32
        roleKey:
          $ref: '#/components/schemas/OrgChangeApprovalRoleKey'
        decision:
          $ref: '#/components/schemas/OrgChangeStepDecision'
        decidedBy:
          $ref: '#/components/schemas/Uuid'
        decidedAt:
          $ref: '#/components/schemas/Timestamp'
        memo:
          type: string
    OrgChangeDecisionRequest:
      type: object
      additionalProperties: false
      required:
      - decision
      properties:
        decision:
          type: string
          enum: [APPROVED, REJECTED]
        memo:
          type: string
          minLength: 1
          maxLength: 2000
    OrgChangeDetail:
      allOf:
      - $ref: '#/components/schemas/OrgChangeSummary'
      - type: object
        required:
        - proposal
        - approvalSteps
        - settlementItems
        - events
        properties:
          proposal:
            type: array
            items:
              $ref: '#/components/schemas/OrgProposalOp'
          preflight:
            $ref: '#/components/schemas/OrgChangePreflightReport'
          approvalSteps:
            type: array
            items:
              $ref: '#/components/schemas/OrgChangeApprovalStep'
          settlementItems:
            type: array
            items:
              $ref: '#/components/schemas/OrgChangeSettlementItem'
          events:
            type: array
            items:
              $ref: '#/components/schemas/OrgChangeEvent'
    OrgChangeEvent:
      type: object
      required:
      - at
      - actor
      - action
      properties:
        at:
          $ref: '#/components/schemas/Timestamp'
        actor:
          $ref: '#/components/schemas/Uuid'
        action:
          type: string
        fromStatus:
          type: string
          description: An OrgChangeStatus wire value.
        toStatus:
          type: string
          description: An OrgChangeStatus wire value.
        reason:
          type: string
    OrgChangeKind:
      type: string
      enum: [NEW, REORG, DISSOLVE]
    OrgChangePage:
      type: object
      required:
      - items
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrgChangeSummary'
        total:
          type: integer
          format: int64
    OrgChangePreflightBlocker:
      type: object
      required:
      - code
      - label
      - dependentKind
      - count
      properties:
        code:
          type: string
        label:
          type: string
        dependentKind:
          type: string
        count:
          type: integer
          format: int64
    OrgChangePreflightReport:
      type: object
      required:
      - computedAt
      - stale
      - blockers
      - warnings
      - headcount
      - dependentsTotal
      properties:
        computedAt:
          $ref: '#/components/schemas/Timestamp'
        stale:
          type: boolean
          description: True when the draft changed after this receipt was computed; submit recomputes and never trusts a
            stale receipt.
        blockers:
          type: array
          items:
            $ref: '#/components/schemas/OrgChangePreflightBlocker'
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/OrgChangePreflightWarning'
        headcount:
          type: integer
          format: int64
        dependentsTotal:
          type: integer
          format: int64
    OrgChangePreflightWarning:
      type: object
      required:
      - code
      - label
      properties:
        code:
          type: string
        label:
          type: string
        dependentKind:
          type: string
        count:
          type: integer
          format: int64
    OrgChangeSettlementItem:
      type: object
      required:
      - id
      - itemKey
      - label
      - done
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        itemKey:
          $ref: '#/components/schemas/OrgChangeSettlementKey'
        label:
          type: string
          description: Server-issued display label.
        done:
          type: boolean
        doneBy:
          $ref: '#/components/schemas/Uuid'
        doneAt:
          $ref: '#/components/schemas/Timestamp'
        memo:
          type: string
    OrgChangeSettlementKey:
      type: string
      enum: [TRANSFER_EMPLOYEES, POSITIONS, COST_CENTERS, CLOSE_OPEN_DOCS, ASSETS, PAYROLL_SOCIAL_FINAL]
    OrgChangeStatus:
      type: string
      description: PRECHECKED is legacy and is never entered by a request created today — preflight is read-only, so
        the lifecycle is DRAFT → IN_APPROVAL at submit. It stays in the enum because rows written before that change
        still carry it on disk and are still accepted by PATCH, preflight, submit and cancel. Do not poll for it.
      enum: [DRAFT, PRECHECKED, IN_APPROVAL, APPROVED, APPLIED, SETTLING, ARCHIVED, REJECTED, CANCELLED]
    OrgChangeStepDecision:
      type: string
      enum: [PENDING, APPROVED, REJECTED]
    OrgChangeSummary:
      type: object
      required:
      - id
      - code
      - kind
      - status
      - target
      - effectiveDate
      - reason
      - headcount
      - siteCount
      - teamCount
      - draftedBy
      - createdAt
      - updatedAt
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          description: Server-issued display code `OC-YYYY-NNNN`.
        kind:
          $ref: '#/components/schemas/OrgChangeKind'
        status:
          $ref: '#/components/schemas/OrgChangeStatus'
        target:
          $ref: '#/components/schemas/OrgChangeTarget'
        effectiveDate:
          $ref: '#/components/schemas/Date'
        reason:
          type: string
        headcount:
          type: integer
          format: int64
        siteCount:
          type: integer
          format: int64
        teamCount:
          type: integer
          format: int64
        draftedBy:
          $ref: '#/components/schemas/Uuid'
        createdAt:
          $ref: '#/components/schemas/Timestamp'
        updatedAt:
          $ref: '#/components/schemas/Timestamp'
        supersedesId:
          $ref: '#/components/schemas/Uuid'
    OrgChangeTarget:
      type: object
      additionalProperties: false
      required:
      - kind
      - ref
      - label
      properties:
        kind:
          $ref: '#/components/schemas/OrgChangeTargetKind'
        ref:
          type: string
          minLength: 1
          maxLength: 200
          description: Must parse as a UUID for REGION, BRANCH, and SITE targets; a free-form key for ENTITY and ORG_UNIT.
        label:
          type: string
          minLength: 1
          maxLength: 200
    OrgChangeTargetKind:
      type: string
      enum: [ENTITY, REGION, BRANCH, SITE, ORG_UNIT]
    OrgEntitySummary:
      type: object
      required:
      - orgId
      - slug
      - name
      - status
      properties:
        orgId:
          $ref: '#/components/schemas/Uuid'
        slug:
          type: string
        name:
          type: string
        status:
          type: string
          description: Tenant status; ACTIVE, SUSPENDED, or ARCHIVED.
    OrgProposalOp:
      description: One typed sandbox-diff operation, replayed in order at apply time. Internally tagged by `op`; unknown
        properties are rejected.
      oneOf:
      - $ref: '#/components/schemas/OrgProposalOpCreateRegion'
      - $ref: '#/components/schemas/OrgProposalOpRenameRegion'
      - $ref: '#/components/schemas/OrgProposalOpDeactivateRegion'
      - $ref: '#/components/schemas/OrgProposalOpCreateBranch'
      - $ref: '#/components/schemas/OrgProposalOpRenameBranch'
      - $ref: '#/components/schemas/OrgProposalOpDeactivateBranch'
      - $ref: '#/components/schemas/OrgProposalOpCreateSite'
      - $ref: '#/components/schemas/OrgProposalOpUpdateSite'
      - $ref: '#/components/schemas/OrgProposalOpReassignOrgUnit'
      discriminator:
        propertyName: op
        mapping:
          CREATE_REGION: '#/components/schemas/OrgProposalOpCreateRegion'
          RENAME_REGION: '#/components/schemas/OrgProposalOpRenameRegion'
          DEACTIVATE_REGION: '#/components/schemas/OrgProposalOpDeactivateRegion'
          CREATE_BRANCH: '#/components/schemas/OrgProposalOpCreateBranch'
          RENAME_BRANCH: '#/components/schemas/OrgProposalOpRenameBranch'
          DEACTIVATE_BRANCH: '#/components/schemas/OrgProposalOpDeactivateBranch'
          CREATE_SITE: '#/components/schemas/OrgProposalOpCreateSite'
          UPDATE_SITE: '#/components/schemas/OrgProposalOpUpdateSite'
          REASSIGN_ORG_UNIT: '#/components/schemas/OrgProposalOpReassignOrgUnit'
    OrgProposalOpCreateBranch:
      type: object
      additionalProperties: false
      required:
      - op
      - regionId
      - name
      properties:
        op:
          type: string
          enum: [CREATE_BRANCH]
        regionId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          minLength: 1
          maxLength: 120
    OrgProposalOpCreateRegion:
      type: object
      additionalProperties: false
      required:
      - op
      - name
      properties:
        op:
          type: string
          enum: [CREATE_REGION]
        name:
          type: string
          minLength: 1
          maxLength: 120
    OrgProposalOpCreateSite:
      type: object
      additionalProperties: false
      required:
      - op
      - customerId
      - name
      properties:
        op:
          type: string
          enum: [CREATE_SITE]
        customerId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          minLength: 1
          maxLength: 120
    OrgProposalOpDeactivateBranch:
      type: object
      additionalProperties: false
      required:
      - op
      - branchId
      properties:
        op:
          type: string
          enum: [DEACTIVATE_BRANCH]
        branchId:
          $ref: '#/components/schemas/Uuid'
    OrgProposalOpDeactivateRegion:
      type: object
      additionalProperties: false
      required:
      - op
      - regionId
      properties:
        op:
          type: string
          enum: [DEACTIVATE_REGION]
        regionId:
          $ref: '#/components/schemas/Uuid'
    OrgProposalOpReassignOrgUnit:
      type: object
      additionalProperties: false
      description: >-
        Reassign employees from one OrgUnit to another via canonical `hr.transfer`
        (one transfer per matched employee). `fromOrgUnit` and `toOrgUnit` are OrgUnit
        UUIDs (not free-text team labels) and must differ.
      required:
      - op
      - fromOrgUnit
      - toOrgUnit
      - scope
      properties:
        op:
          type: string
          enum: [REASSIGN_ORG_UNIT]
        fromOrgUnit:
          $ref: '#/components/schemas/Uuid'
        toOrgUnit:
          $ref: '#/components/schemas/Uuid'
        scope:
          $ref: '#/components/schemas/OrgProposalReassignScope'
    OrgProposalOpRenameBranch:
      type: object
      additionalProperties: false
      description: Rename and/or move a branch across regions; at least one of `name` and `regionId` must be present.
      required:
      - op
      - branchId
      properties:
        op:
          type: string
          enum: [RENAME_BRANCH]
        branchId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          minLength: 1
          maxLength: 120
        regionId:
          $ref: '#/components/schemas/Uuid'
    OrgProposalOpRenameRegion:
      type: object
      additionalProperties: false
      required:
      - op
      - regionId
      - name
      properties:
        op:
          type: string
          enum: [RENAME_REGION]
        regionId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          minLength: 1
          maxLength: 120
    OrgProposalOpUpdateSite:
      type: object
      additionalProperties: false
      required:
      - op
      - siteId
      - name
      properties:
        op:
          type: string
          enum: [UPDATE_SITE]
        siteId:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          minLength: 1
          maxLength: 120
    OrgProposalReassignScope:
      type: object
      additionalProperties: false
      required:
      - company
      properties:
        company:
          type: string
          minLength: 1
          maxLength: 120
    OrgUnit:
      type: object
      description: "Canonical OrgUnit head. GET /api/v1/org-units/{id} returns this current head. GET /api/v1/org-units returns current heads (the same rows `list` already returns). GET /api/v1/org-units/{id}/job-positions returns current JobPosition heads under this unit (the same rows `list_for_org_unit` already returns). as_of / from / to are omitted: `org_units` / `org_unit_revisions` have no valid-time columns, and using `created_at` as `valid_from` would be a second time model. `name` and `parent_id` are parsed from the latest `org_unit_revisions.attributes`; neither is a column on `org_units`. Sites, regions, and branches stay the operational auth spine and are not OrgUnits. OrgUnit kinds Site/Department/Team are write-preflight on revision attributes (`kind` ∈ {site, department, team}) and are not a Head column. Temporal slices other than revision `version` remain HOLD."
      required:
        - id
        - version
        - name
        - parent_id
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        name:
          type:
            - string
            - "null"
          description: Title property. Required non-empty on write; omitted key is null on read.
        parent_id:
          oneOf:
            - $ref: "#/components/schemas/Uuid"
            - type: "null"
          description: Optional parent OrgUnit. Root units send JSON null.
        version:
          type: integer
          format: int64
          minimum: 1
          description: Optimistic concurrency token for this Head. Mutating roster actions send it as OntologyActionRequest.expected_revision (JSON body, not HTTP If-Match).
      links:
        - key: org_unit_parent
          from: OrgUnit
          to: OrgUnit
          field: parent_id
          cardinality: many-to-one
          option: true
          operationId: getOrgUnit
        - key: org_unit_job_positions
          from: OrgUnit
          to: JobPosition
          field: org_unit_id
          cardinality: one-to-many
          option: false
          operationId: listOrgUnitJobPositions
      actions:
        - action_key: organization.create_org_unit
          object_key: org_unit
          input:
            $ref: "#/components/schemas/OrganizationCreateOrgUnitInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: account
          edits:
            - org_units
            - org_unit_revisions
            - org_unit_source_bindings
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
        - action_key: organization.revise_org_unit
          object_key: org_unit
          input:
            $ref: "#/components/schemas/OrganizationReviseOrgUnitInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: account
          edits:
            - org_unit_revisions
            - org_unit_source_bindings
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
      concurrency:
        get_token: version
        write_field: expected_revision
        write_in: body
      permissions:
        - employee_directory_read
    OrgUnitSourceBinding:
      type: object
      additionalProperties: false
      description: Optional legacy source binding on OrgUnit create/revise. `kind` and `id` are TEXT; the closed set of kinds is not yet enumerable and legacy identifiers are not all UUIDs.
      required:
        - kind
        - id
      properties:
        kind:
          type: string
          minLength: 1
        id:
          type: string
          minLength: 1
    OrganizationCreateJobPositionInput:
      type: object
      additionalProperties: false
      description: "Typed params for `organization.create_job_position` (`JobPositionQuery::Create`). The named OrgUnit must already exist. Write preflight requires non-empty `title`."
      required:
        - org_unit_id
        - attributes
      properties:
        org_unit_id:
          $ref: "#/components/schemas/Uuid"
        attributes:
          type: object
          required:
            - title
          properties:
            title:
              type: string
              minLength: 1
          additionalProperties: true
    OrganizationCreateOrgUnitInput:
      type: object
      additionalProperties: false
      description: "Typed params for `organization.create_org_unit` (`OrgUnitQuery::Create`). Write preflight requires non-empty `name` and `kind` ∈ {site, department, team}."
      required:
        - attributes
      properties:
        source:
          oneOf:
            - $ref: "#/components/schemas/OrgUnitSourceBinding"
            - type: "null"
        attributes:
          type: object
          required:
            - name
            - kind
          properties:
            name:
              type: string
              minLength: 1
            kind:
              type: string
              enum:
                - site
                - department
                - team
            parent_id:
              oneOf:
                - $ref: "#/components/schemas/Uuid"
                - type: "null"
          additionalProperties: true
    OrganizationReviseJobPositionInput:
      type: object
      additionalProperties: false
      description: "Typed params for `organization.revise_job_position` (`JobPositionQuery::Revise`). Null `org_unit_id` leaves the position where it is; a UUID moves it."
      required:
        - job_position_id
        - attributes
      properties:
        job_position_id:
          $ref: "#/components/schemas/Uuid"
        org_unit_id:
          oneOf:
            - $ref: "#/components/schemas/Uuid"
            - type: "null"
        attributes:
          type: object
          required:
            - title
          properties:
            title:
              type: string
              minLength: 1
          additionalProperties: true
    OrganizationReviseOrgUnitInput:
      type: object
      additionalProperties: false
      description: "Typed params for `organization.revise_org_unit` (`OrgUnitQuery::Revise`)."
      required:
        - org_unit_id
        - attributes
      properties:
        org_unit_id:
          $ref: "#/components/schemas/Uuid"
        source:
          oneOf:
            - $ref: "#/components/schemas/OrgUnitSourceBinding"
            - type: "null"
        attributes:
          type: object
          required:
            - name
            - kind
          properties:
            name:
              type: string
              minLength: 1
            kind:
              type: string
              enum:
                - site
                - department
                - team
            parent_id:
              oneOf:
                - $ref: "#/components/schemas/Uuid"
                - type: "null"
          additionalProperties: true
    OtpRedeemRequest:
      type: object
      required:
      - otp
      properties:
        otp:
          type: string
          description: The one-time sign-in code (8 characters for admin-issued codes).
    OtpRedeemResponse:
      type: object
      required:
        - access_token
        - token_type
        - refresh_expires_at
        - requires_passkey_setup
      properties:
        access_token:
          type: string
        refresh_token:
          type:
            - string
            - 'null'
          description: >-
            The opaque rotating refresh token in the body transport (mobile). It is null in the cookie transport (web), where
            the token is set as an HttpOnly `console_refresh` cookie and must never reach web JS.
        token_type:
          type: string
        refresh_expires_at:
          $ref: '#/components/schemas/Timestamp'
        requires_passkey_setup:
          type: boolean
          description: True when the signed-in user has no passkey yet and should enroll one in initial settings.
    OutsourceWorkSummary:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        vendor_id:
          $ref: '#/components/schemas/Uuid'
        vendor_name:
          type: string
        status:
          type: string
          enum:
          - REQUESTED
          - ASSIGNED
          - IN_PROGRESS
          - RESULT_SUBMITTED
          - COMPLETED
          - CANCELLED
    OwnAttendanceException:
      type: object
      additionalProperties: false
      required:
        - id
        - code
        - kind
        - status
        - work_date
        - occurred_at
        - detail
        - evidence
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
        kind:
          type: string
          enum:
            - LATE
            - NO_SHOW
            - UNAPPROVED_OVERTIME
            - EARLY_LEAVE
        status:
          type: string
          enum:
            - OPEN
            - RESOLVED
        work_date:
          type: string
          format: date
        occurred_at:
          type: string
          format: date-time
        detail:
          type: string
        evidence:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceExceptionEvidence'
        resolution:
          oneOf:
            - {$ref: '#/components/schemas/OwnAttendanceExceptionResolution'}
            - {type: 'null'}
        created_at:
          type: string
          format: date-time
    OwnAttendanceExceptionPage:
      type: object
      additionalProperties: false
      required: [items, total, limit, offset]
      properties: { items: { type: array, items: { $ref: '#/components/schemas/OwnAttendanceException' } }, total: { type: integer, format: int64 }, limit: { type: integer, format: int64 }, offset: { type: integer, format: int64 } }
    OwnAttendanceExceptionResolution:
      type: object
      additionalProperties: false
      required:
        - action
        - reason
        - resolved_at
      properties:
        action:
          type: string
        reason:
          type: string
        ot_hours:
          type:
            - string
            - 'null'
        resolved_at:
          type: string
          format: date-time
    OwnAttendanceWeek52:
      type: object
      additionalProperties: false
      required:
        - week_start
        - current_hours
        - projected_hours
        - tone
      properties:
        week_start:
          type: string
          format: date
        current_hours:
          type: number
        projected_hours:
          type: number
        tone:
          type: string
          enum:
            - OK
            - WARN
            - DANGER
        acknowledged_at:
          type:
            - string
            - 'null'
          format: date-time
    OwnAttendanceWeek52Response:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status: { type: string, enum: [available, not_available] }
        projection: { $ref: '#/components/schemas/OwnAttendanceWeek52' }
    OwnershipTransfer:
      type: object
      required:
      - id
      - equipment_id
      - branch_id
      - from_owner
      - to_owner
      - reason
      - status
      - current_step
      - approval_line
      - requested_at
      properties:
        id:
          type: string
          format: uuid
        equipment_id:
          type: string
          format: uuid
        branch_id:
          type: string
          format: uuid
        from_owner:
          type: string
        to_owner:
          type: string
        reason:
          type: string
        status:
          type: string
          enum:
          - PENDING
          - APPROVED
          - REJECTED
        current_step:
          type:
          - string
          - 'null'
          enum:
          - sending_org_admin
          - receiving_org_admin
          - legal_signoff
          - accounting_signoff
          - null
        approval_line:
          type: array
          items:
            $ref: '#/components/schemas/OwnershipTransferStep'
        requested_by:
          type:
          - string
          - 'null'
          format: uuid
        requested_at:
          type: string
          format: date-time
        decided_at:
          type:
          - string
          - 'null'
          format: date-time
        completed_at:
          type:
          - string
          - 'null'
          format: date-time
    OwnershipTransferPage:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OwnershipTransfer'
    OwnershipTransferStep:
      type: object
      required:
      - step_key
      - label
      - status
      properties:
        step_key:
          type: string
          enum:
          - sending_org_admin
          - receiving_org_admin
          - legal_signoff
          - accounting_signoff
        label:
          type: string
        status:
          type: string
          enum:
          - WAITING
          - PENDING
          - APPROVED
          - REJECTED
        decided_by:
          type:
          - string
          - 'null'
          format: uuid
        decided_at:
          type:
          - string
          - 'null'
          format: date-time
        comment:
          type:
          - string
          - 'null'
    P1DispatchResponsePage:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/P1DispatchResponseSummary' } }
    P1DispatchResponseSummary:
      type: object
      required: [dispatch_id, user_id, response, responded_at, gps_ranked]
      properties:
        dispatch_id: { $ref: '#/components/schemas/Uuid' }
        user_id: { $ref: '#/components/schemas/Uuid' }
        response: { $ref: '#/components/schemas/DispatchResponseKind' }
        responded_at: { $ref: '#/components/schemas/Timestamp' }
        score_milli: { type: integer, format: int64 }
        gps_ranked: { type: boolean }
        distance_meters: { type: integer, format: int64 }
        score_reason: { type: string }
    P1DispatchSummary:
      type: object
      required:
      - id
      - work_order_id
      - branch_id
      - status
      - accept_window_started_at
      - accept_window_ends_at
      - target_count
      - accepted_count
      - declined_count
      - manual_call_required
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/DispatchStatus'
        incident_location:
          $ref: '#/components/schemas/IncidentLocation'
        accept_window_started_at:
          $ref: '#/components/schemas/Timestamp'
        accept_window_ends_at:
          $ref: '#/components/schemas/Timestamp'
        auto_assigned_mechanic_id:
          $ref: '#/components/schemas/Uuid'
        manager_force_pending_at:
          $ref: '#/components/schemas/Timestamp'
        manual_call_required:
          type: boolean
        manual_call_required_at:
          $ref: '#/components/schemas/Timestamp'
        manual_call_cleared_at:
          $ref: '#/components/schemas/Timestamp'
        target_count:
          type: integer
          format: int64
        accepted_count:
          type: integer
          format: int64
        declined_count:
          type: integer
          format: int64
    PasskeyLoginFinishRequest:
      type: object
      required:
      - ceremony_id
      - credential
      properties:
        ceremony_id:
          $ref: '#/components/schemas/Uuid'
        credential:
          type: object
          additionalProperties: true
    PasskeyLoginStartResponse:
      type: object
      required:
      - ceremony_id
      - challenge
      - expires_at
      properties:
        ceremony_id:
          $ref: '#/components/schemas/Uuid'
        challenge:
          type: object
          additionalProperties: true
        expires_at:
          $ref: '#/components/schemas/Timestamp'
    PasskeyRegisterFinishRequest:
      type: object
      required:
      - ceremony_id
      - credential
      properties:
        ceremony_id:
          $ref: '#/components/schemas/Uuid'
        credential:
          type: object
          additionalProperties: true
    PasskeyRegisterFinishResponse:
      type: object
      required:
      - passkey_id
      - user_id
      - credential_id
      properties:
        passkey_id:
          $ref: '#/components/schemas/Uuid'
        user_id:
          $ref: '#/components/schemas/Uuid'
        credential_id:
          type: string
    PasskeyRegisterStartRequest:
      type: object
      description: Optional overrides for the authenticated session user's passkey registration (username/display_name default
        to the user's stored profile when omitted), plus the step-up assertion required to ADD a passkey when the user already
        has one. A user with zero passkeys (initial enrollment) omits `step_up`; an already-enrolled user MUST supply a fresh
        `step_up` assertion of an existing passkey (user verification required), or register/start returns 401 — so a stolen
        session cannot silently add a credential.
      properties:
        username:
          type: string
        display_name:
          type: string
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    PasskeyRegisterStartResponse:
      type: object
      required:
      - ceremony_id
      - challenge
      - expires_at
      properties:
        ceremony_id:
          $ref: '#/components/schemas/Uuid'
        challenge:
          type: object
          additionalProperties: true
        expires_at:
          $ref: '#/components/schemas/Timestamp'
    PasskeyStepUpAssertion:
      type: object
      description: A fresh assertion of an EXISTING passkey, proving the caller currently possesses an authenticator (not
        just a bearer token). The `ceremony_id` comes from a preceding `POST /api/v1/auth/passkey/login/start`; the `credential`
        is the resulting WebAuthn assertion. Verified with user verification (UV) required and rejected unless the asserted
        credential belongs to the authenticated caller.
      required:
      - ceremony_id
      - credential
      properties:
        ceremony_id:
          $ref: '#/components/schemas/Uuid'
        credential:
          type: object
          additionalProperties: true
    PasskeySummary:
      type: object
      description: A passkey credential summary for the self-service management surface. It deliberately carries no secret
        material (no passkey blob, public key, or raw credential id) — only the opaque row id and the registration / last-use
        timestamps.
      required:
      - id
      - created_at
      - last_used_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        last_used_at:
          type:
          - string
          - 'null'
          format: date-time
    PayRun:
      type: object
      description: Canonical PayRun scaffold over existing `payroll_draft_runs`. Not SAP-complete. Draft calculate is admitted on POST /api/v1/payroll/runs/{id}/calculate (not a DispatchTarget). `payable` is always false. Payment execution, Korea compliance conclusions, and wage-statement legal sign-off remain HOLD. REST list/detail DTOs remain `PayrollRunSummary` / `PayrollRunDetail`. No outgoing Head FK; the run is Company-scoped via RLS `org_id`, which is the cell, not a Palantir link. Temporal slices other than draft status remain HOLD.
      required:
        - id
        - period_start
        - period_end
        - source_label
        - status
        - payable
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        period_start:
          type: string
          format: date
        period_end:
          type: string
          format: date
        source_label:
          type: string
        status:
          type: string
          enum:
            - STAGED
            - BLOCKED_LEGAL_GATE
            - READY_FOR_REVIEW
            - ATTENDANCE_CLOSED
            - CALCULATING
            - CALCULATED
            - SUBMITTED
            - REJECTED
            - APPROVED
            - DISBURSEMENT_SCHEDULED
            - PAID
            - ISSUED
            - VOID
          description: "`payroll_draft_runs.status`. Same closed set as `PayrollRunSummary`."
        payable:
          type: boolean
          const: false
          description: Always false until the 노무사/세무사 release gate flips stored calculation rows. Payment execution remains HOLD. Not a payable payroll object.
      links: []
      actions:
        - action_key: payroll.create_run
          object_key: pay_run
          input:
            $ref: "#/components/schemas/PayrollCreateRunInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: natural_person
          edits:
            - payroll_draft_runs
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
        - action_key: payroll.submit_run
          object_key: pay_run
          input:
            $ref: "#/components/schemas/PayrollSubmitRunInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: natural_person
          edits:
            - payroll_draft_runs
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
        - action_key: payroll.decide_run
          object_key: pay_run
          input:
            $ref: "#/components/schemas/PayrollDecideRunInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: natural_person
          edits:
            - payroll_draft_runs
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
      concurrency:
        get_token: null
        write_field: expected_revision
        write_in: body
    PayrollClosePreflight:
      type: object
      required:
      - checks
      - can_close
      properties:
        checks:
          type: array
          items:
            $ref: '#/components/schemas/PayrollPreflightCheck'
        can_close:
          type: boolean
    PayrollCreateRunInput:
      type: object
      additionalProperties: false
      description: "Typed params for `payroll.create_run` (`PayRunQuery::CreateRun`). Stages a draft under a caller-chosen source label. `payable` is not an input; stored calculations stay false. Payment execution remains HOLD."
      required:
        - run_id
        - period_start
        - period_end
      properties:
        run_id:
          $ref: "#/components/schemas/Uuid"
        period_start:
          type: string
          format: date
        period_end:
          type: string
          format: date
        connector:
          type:
            - string
            - "null"
        job:
          type:
            - string
            - "null"
    PayrollDecideRunInput:
      type: object
      additionalProperties: false
      description: "Typed params for `payroll.decide_run` (`PayRunQuery::DecideRun`). `SUBMITTED → APPROVED|REJECTED`, refused when the decider is the submitter. Distinct from path-bound `DecidePayrollRunRequest` (run_id is in this bag). `payable` stays false. Payment execution remains HOLD."
      required:
        - run_id
        - decision
      properties:
        run_id:
          $ref: "#/components/schemas/Uuid"
        decision:
          type: string
          enum:
            - APPROVE
            - REJECT
        reason:
          type:
            - string
            - "null"
          description: Required for REJECT at the lifecycle writer (blank reason is 422).
    PayrollDisbursement:
      type: object
      required:
      - id
      - run_id
      - scheduled_at
      - status
      - attested_by
      - attested_at
      - reason
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        run_id:
          $ref: '#/components/schemas/Uuid'
        scheduled_at:
          $ref: '#/components/schemas/Timestamp'
        status:
          type: string
          enum: [SCHEDULED, SUBMITTED_TO_BANK, PAID, FAILED]
          description: Operator attestation, never a bank result — no bank API exists.
        attested_by:
          type:
          - string
          - 'null'
          format: uuid
        attested_at:
          type:
          - string
          - 'null'
          format: date-time
        reason:
          type:
          - string
          - 'null'
          description: Retained only while the status is FAILED; cleared on any other attestation.
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    PayrollException:
      type: object
      required:
      - id
      - run_id
      - line_id
      - employee_id
      - employee_display_name
      - kind
      - severity
      - amount_delta_won
      - summary_ko
      - detail
      - linked_refs
      - status
      - resolved_by
      - resolved_at
      - resolved_reason
      - carried_from_run_id
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        run_id:
          $ref: '#/components/schemas/Uuid'
        line_id:
          type:
          - string
          - 'null'
          format: uuid
        employee_id:
          type:
          - string
          - 'null'
          format: uuid
        employee_display_name:
          type: string
        kind:
          type: string
          enum: [OVERTIME_ALLOWANCE, RETRO_ADJUSTMENT, ABSENCE_DEDUCTION, PRORATION, ACCOUNT_VERIFICATION]
        severity:
          type: string
          enum: [info, warn, danger]
        amount_delta_won:
          type:
          - integer
          - 'null'
          format: int64
          description: Whole KRW won. null when the delta is not derivable from a verified source.
        summary_ko:
          type: string
        detail:
          type: object
        linked_refs:
          type: array
          description: Traversal handles to the objects this exception is about. The column is untyped JSON server-side; the
            only writer emits payroll_line and employee refs in this shape.
          items:
            $ref: '#/components/schemas/PayrollLinkedRef'
        status:
          type: string
          enum: [OPEN, CONFIRMED, HELD]
        resolved_by:
          type:
          - string
          - 'null'
          format: uuid
        resolved_at:
          type:
          - string
          - 'null'
          format: date-time
        resolved_reason:
          type:
          - string
          - 'null'
        carried_from_run_id:
          type:
          - string
          - 'null'
          format: uuid
          description: The run this HELD exception was carried forward from.
        created_at:
          $ref: '#/components/schemas/Timestamp'
    PayrollExceptionPage:
      type: object
      required:
      - items
      - total
      - open
      - limit
      - offset
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PayrollException'
        total:
          type: integer
          format: int64
        open:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
    PayrollLineSummary:
      type: object
      required:
      - id
      - employee_display_name
      - employee_company
      - gross_pay_source_present
      - net_pay_source_present
      - nts_tax_row_status
      - calculation_status
      - blockers
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        employee_id:
          type:
          - string
          - 'null'
          format: uuid
        employee_display_name:
          type: string
        employee_company:
          type: string
        work_days:
          type:
          - number
          - 'null'
        regular_hours:
          type:
          - number
          - 'null'
        overtime_hours:
          type:
          - number
          - 'null'
        night_hours:
          type:
          - number
          - 'null'
        holiday_hours:
          type:
          - number
          - 'null'
        leave_used:
          type:
          - number
          - 'null'
        leave_remaining:
          type:
          - number
          - 'null'
        gross_pay_source_present:
          type: boolean
        net_pay_source_present:
          type: boolean
        nts_tax_row_status:
          type: string
          enum: [REQUIRED_NOT_SUPPLIED, SUPPLIED_UNVERIFIED, VERIFIED_SOURCE_ROW]
        calculation_status:
          type: string
          enum: [BLOCKED_LEGAL_GATE, READY_FOR_REVIEW, APPROVED, ISSUED, VOID]
        blockers:
          type: array
          items: {}
    PayrollLinkedRef:
      type: object
      required:
      - kind
      - code
      properties:
        kind:
          type: string
          description: Object family the ref points at — payroll_line or employee today; extensible.
        code:
          type: string
          description: Human-facing display code for the referenced object.
        id:
          type:
          - string
          - 'null'
          format: uuid
    PayrollPayslipDeliveryItem:
      type: object
      required:
      - line_id
      - employee_id
      - inbox_doc_id
      - issued_at
      - acknowledged_at
      properties:
        line_id:
          $ref: '#/components/schemas/Uuid'
        employee_id:
          $ref: '#/components/schemas/Uuid'
        inbox_doc_id:
          $ref: '#/components/schemas/Uuid'
        issued_at:
          $ref: '#/components/schemas/Timestamp'
        acknowledged_at:
          type:
          - string
          - 'null'
          format: date-time
          description: Read back from the inbox doc's confirmed_at; the vault does not yet confirm payslip receipts, so this stays null today.
    PayrollPayslipDeliverySummary:
      type: object
      required:
      - run_id
      - issued
      - acknowledged
      - items
      - total
      - limit
      - offset
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        issued:
          type: integer
          format: int64
        acknowledged:
          type: integer
          format: int64
        items:
          type: array
          items:
            $ref: '#/components/schemas/PayrollPayslipDeliveryItem'
        total:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
    PayrollPayslipDraft:
      type: object
      description: >
        The payslip draft. The first nine properties are always present; the
        computed half is absent when a blocker stopped the draft before the
        arithmetic (no contract wage in force, or an hourly contract).
      required:
      - employee_id
      - employee_name
      - period
      - pay_date
      - contract
      - attendance
      - issuable
      - blockers
      - statutory_citations
      properties:
        employee_id: { $ref: '#/components/schemas/Uuid' }
        employee_name: { type: string }
        period:
          type: object
          required: [start, end]
          properties:
            start: { type: string, format: date }
            end: { type: string, format: date }
        pay_date: { type: string, format: date }
        contract:
          description: The contract wage in force on `pay_date`. Null when none is.
          oneOf:
          - type: object
            required: [id, effective_from, wage_kind, amount_won, monthly_standard_hours, source_note]
            properties:
              id: { $ref: '#/components/schemas/Uuid' }
              effective_from: { type: string, format: date }
              wage_kind: { type: string, enum: [MONTHLY, HOURLY] }
              amount_won: { type: integer, format: int64 }
              monthly_standard_hours: { type: integer }
              source_note: { type: string }
          - type: 'null'
        attendance:
          type: object
          description: >
            The period's real 근태기록 counts. They GATE the draft and drive no
            figure — nothing here is prorated by attendance.
          required: [period_start, period_end, worked_days, clock_in_events, clock_out_events]
          properties:
            period_start: { type: string, format: date }
            period_end: { type: string, format: date }
            worked_days:
              type: integer
              format: int64
              description: Distinct `work_date`s carrying at least one CLOCK_IN.
            clock_in_events: { type: integer, format: int64 }
            clock_out_events: { type: integer, format: int64 }
        issuable:
          type: boolean
          description: >
            False for every employee in this slice, because 원천징수 is not
            computed. A draft that cannot state 차인지급액 is not a 급여명세서.
        blockers:
          type: array
          items: { type: string }
          description: >
            Always carries `WITHHOLDING_NOT_COMPUTED`. Adds
            `ATTENDANCE_INCOMPLETE` (with the counts) when the recorded working
            days fall short of the period's working days or the CLOCK_IN and
            CLOCK_OUT counts differ, `CONTRACT_WAGE_NOT_IN_FORCE`,
            `HOURLY_WAGE_NOT_SUPPORTED_IN_THIS_SLICE`, the 최저임금 findings, and
            the id of any unresolved rounding question.
        statutory_citations:
          type: array
          items: { $ref: '#/components/schemas/PayrollStatutoryCitation' }
        earnings:
          type: array
          items:
            type: object
            required: [code, label_ko, amount_won, note_ko]
            properties:
              code: { type: string }
              label_ko: { type: string }
              amount_won: { type: integer, format: int64 }
              note_ko: { type: string }
        gross_won: { type: integer, format: int64 }
        deductions:
          type: array
          items: { $ref: '#/components/schemas/PayrollPayslipDraftDeduction' }
        not_computed:
          type: array
          description: >
            Deductions the engine REFUSES to compute, each naming the instrument
            that would supply it. Never emitted as a zero line.
          items:
            type: object
            required: [code, label_ko, reason_ko, instrument]
            properties:
              code: { type: string }
              label_ko: { type: string }
              reason_ko: { type: string }
              instrument: { $ref: '#/components/schemas/PayrollStatutoryInstrument' }
        minimum_wage_check:
          type: object
          required: [hourly_floor_won, monthly_209h_floor_won, monthly_standard_hours, effective_hourly_won, passes, instrument]
          properties:
            hourly_floor_won: { type: integer, format: int64 }
            monthly_209h_floor_won: { type: integer, format: int64 }
            monthly_standard_hours: { type: [integer, 'null'] }
            effective_hourly_won: { type: [integer, 'null'], format: int64 }
            passes:
              type: [boolean, 'null']
              description: Null means NOT COMPARABLE — which is not a pass.
            instrument: { $ref: '#/components/schemas/PayrollStatutoryInstrument' }
        total_employee_insurance_won:
          type: [integer, 'null']
          format: int64
          description: Null when any employee component is blocked.
        remainder_after_insurance_won: { type: [integer, 'null'], format: int64 }
        net_pay_won:
          type: 'null'
          description: >
            차인지급액. Always null in this slice: 원천징수 is not computed, and a
            net figure that ignored it would be wrong rather than approximate.
        net_pay_unavailable_reason_ko: { type: string }
        compliance_notice_ko:
          type: string
          description: States that the draft asserts no legal compliance.
    PayrollPayslipDraftDeduction:
      type: object
      description: >
        One 공제 component, carrying its own basis, rate and instrument so the
        payslip is recomputable under 근로기준법 제42조. An amount this engine did
        not compute is null, never 0.
      required:
      - code
      - label_ko
      - basis_kind
      - basis_won
      - rate_num
      - rate_den
      - total_won
      - employee_won
      - employer_only
      - blocked_by
      - total_rounding
      - employee_rounding
      - instrument
      - share_instrument
      - provenance_ko
      properties:
        code:
          type: string
          enum: [NationalPension, HealthInsurance, LongTermCare, EmploymentUnemployment, IndustrialAccident]
        label_ko: { type: string }
        basis_kind:
          type: string
          enum: [MonthlyStandardIncome, MonthlyRemuneration, HealthInsurancePremium, IndustryTariff]
          description: >
            장기요양's basis is HealthInsurancePremium — the COMPUTED 건강보험료액
            under 노인장기요양보험법 제9조제1항, not 보수월액.
        basis_won:
          type: [integer, 'null']
          format: int64
          description: >
            Null for 산재 only: its 사업종류별 요율 and the basis that rate applies
            to are per-employer figures from an un-ingested 별지.
        rate_num:
          type: [integer, 'null']
          format: int64
          description: Integer numerator, never a decimal. Null for 산재, with `rate_den`.
        rate_den: { type: [integer, 'null'], format: int64 }
        total_won:
          type: [integer, 'null']
          format: int64
          description: >
            Null for 산재 (the employer premium is unknown, not zero) and null for
            any component whose rounding unit is unsettled — see `blocked_by`.
        employee_won:
          type: [integer, 'null']
          format: int64
          description: Always null when `employer_only` is true — 산재 has no employee line.
        employer_only:
          type: boolean
          description: >
            True for 산재 alone (징수법 제13조제5항). The absence of an employee
            line is STATED here rather than left silent.
        blocked_by:
          type: [string, 'null']
          description: The unresolved question id that withheld this amount, if any.
        total_rounding: { type: string }
        employee_rounding: { type: string }
        instrument: { $ref: '#/components/schemas/PayrollStatutoryInstrument' }
        share_instrument:
          oneOf: [{ $ref: '#/components/schemas/PayrollStatutoryInstrument' }, { type: 'null' }]
          description: The instrument setting the bearer split, when it is a different one.
        provenance_ko:
          type: string
          description: What is actually established about this row, so a reviewer is never told more than was verified.
    PayrollPreflightCheck:
      type: object
      required:
      - key
      - label_ko
      - ok
      - warn
      - note
      - blocking_refs
      properties:
        key:
          type: string
          enum: [attendance_material, period_lock, pending_leave]
        label_ko:
          type: string
        ok:
          type: boolean
        warn:
          type: boolean
          description: Soft signal only; can_close is the AND of the hard checks (attendance_material, period_lock).
        note:
          type:
          - string
          - 'null'
        blocking_refs:
          type: array
          description: Up to 50 fix-link ids (draft lines missing attendance material, or pending leave requests); the note carries the full count.
          items:
            $ref: '#/components/schemas/Uuid'
    PayrollRunCalcSummary:
      type: object
      required:
      - version
      - calculated_at
      - calculated_lines
      - blocked_lines
      - payable
      - kernel_rate_table
      - total_net_won
      properties:
        version:
          type: integer
          format: int32
        calculated_at:
          $ref: '#/components/schemas/Timestamp'
        calculated_lines:
          type: integer
          format: int64
        blocked_lines:
          type: integer
          format: int64
        payable:
          type: boolean
          description: Always false until the 노무사/세무사 release gate flips the stored rows.
        kernel_rate_table:
          type: string
        total_net_won:
          type:
          - integer
          - 'null'
          format: int64
          description: Whole KRW won. null unless EVERY line of the run calculated — never a partial sum presented as a total.
    PayrollRunDetail:
      type: object
      required:
      - run
      - legal_basis
      - source_summary
      - lines
      - lines_total
      - lines_limit
      - lines_offset
      - exceptions_open
      - exceptions_total
      - calculation
      - disbursement
      - payslip_delivery
      properties:
        run:
          $ref: '#/components/schemas/PayrollRunSummary'
        legal_basis:
          type: object
        source_summary:
          type: object
        lines:
          type: array
          items:
            $ref: '#/components/schemas/PayrollLineSummary'
        lines_total:
          type: integer
          format: int64
        lines_limit:
          type: integer
          format: int64
        lines_offset:
          type: integer
          format: int64
        exceptions_open:
          type: integer
          format: int64
        exceptions_total:
          type: integer
          format: int64
        calculation:
          anyOf:
          - $ref: '#/components/schemas/PayrollRunCalcSummary'
          - type: 'null'
          description: Latest stored calculation version; null until the run has been calculated.
        disbursement:
          anyOf:
          - $ref: '#/components/schemas/PayrollDisbursement'
          - type: 'null'
        payslip_delivery:
          anyOf:
          - $ref: '#/components/schemas/PayrollPayslipDeliverySummary'
          - type: 'null'
          description: Unpaginated delivery summary; null until at least one payslip has been issued.
    PayrollRunPage:
      type: object
      required:
      - items
      - total
      - limit
      - offset
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PayrollRunSummary'
        total:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
    PayrollRunSummary:
      type: object
      required:
      - id
      - period_start
      - period_end
      - source_label
      - status
      - calculation_enabled
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        period_start:
          type: string
          format: date
        period_end:
          type: string
          format: date
        source_label:
          type: string
        status:
          type: string
          enum: [STAGED, BLOCKED_LEGAL_GATE, READY_FOR_REVIEW, ATTENDANCE_CLOSED, CALCULATING, CALCULATED, SUBMITTED, REJECTED, APPROVED, DISBURSEMENT_SCHEDULED, PAID, ISSUED, VOID]
        calculation_enabled:
          type: boolean
        created_by:
          type:
          - string
          - 'null'
          format: uuid
        approved_by:
          type:
          - string
          - 'null'
          format: uuid
        approved_at:
          type:
          - string
          - 'null'
          format: date-time
        close_receipt:
          type:
          - object
          - 'null'
          description: Attestation receipt written by the attendance close — the preflight checks, the attesting user, and the attestation time.
        submitted_by:
          type:
          - string
          - 'null'
          format: uuid
        submitted_at:
          type:
          - string
          - 'null'
          format: date-time
        decided_by:
          type:
          - string
          - 'null'
          format: uuid
          description: Maker-checker counterparty; a DB CHECK forbids it from equalling submitted_by.
        decided_at:
          type:
          - string
          - 'null'
          format: date-time
        decision_reason:
          type:
          - string
          - 'null'
        approval_ref:
          type:
          - string
          - 'null'
          format: uuid
          description: Reserved for the future AP- workflow-engine object; never written today.
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    PayrollStatutoryCitation:
      type: object
      description: One row of `payroll_statutory_rates` in force on the pay date, verbatim.
      required:
      - code
      - effective_from
      - effective_to_exclusive
      - rate_num
      - rate_den
      - floor_won
      - cap_won
      - basis
      - bearer
      - instrument_ko
      - article_ko
      - promulgation_ko
      - enforced_on
      - source_url
      - retrieved_on
      - provenance_ko
      properties:
        code: { type: string }
        effective_from: { type: string, format: date }
        effective_to_exclusive: { type: [string, 'null'], format: date }
        rate_num: { type: [integer, 'null'], format: int64 }
        rate_den: { type: [integer, 'null'], format: int64 }
        floor_won: { type: [integer, 'null'], format: int64 }
        cap_won: { type: [integer, 'null'], format: int64 }
        basis: { type: string }
        bearer: { type: string }
        instrument_ko: { type: string }
        article_ko: { type: string }
        promulgation_ko: { type: string }
        enforced_on:
          type: string
          format: date
          description: Never later than `effective_from` — a row cannot be in force before the document that sets it.
        source_url: { type: string }
        retrieved_on: { type: string, format: date }
        provenance_ko: { type: string }
    PayrollStatutoryInstrument:
      type: object
      description: >
        The document that actually sets a number. The version anchor is 공포번호
        (법령) or 발령번호 (고시) together with `enforced_on` — never a `flSeq`
        file handle, three of which serve byte-identical 별표 2 content.
      required: [name_ko, article_ko, promulgation_ko, enforced_on, url, retrieved_on]
      properties:
        name_ko: { type: string, description: 법령명 or 행정규칙명. }
        article_ko:
          type: string
          description: The article carrying the number, quoted tightly enough to diff.
        promulgation_ko: { type: string, description: 공포번호 (법령) or 발령번호 (고시). }
        enforced_on:
          type: string
          format: date
          description: >
            시행일자 of the text quoted in `article_ko` — not the promulgation
            date, and not the latest promulgated slice.
        url: { type: string }
        retrieved_on: { type: string, format: date }
    PayrollSubmitRunInput:
      type: object
      additionalProperties: false
      description: "Typed params for `payroll.submit_run` (`PayRunQuery::SubmitRun`). `CALCULATED → SUBMITTED`, refused while any exception is open. `payable` stays false."
      required:
        - run_id
      properties:
        run_id:
          $ref: "#/components/schemas/Uuid"
    PeopleCreatePersonInput:
      type: object
      additionalProperties: false
      description: "Typed params for `people.create_person` (`PersonQuery::Create`). When `employee_id` is set (trusted uniquely resolved), the new person's id IS that employee id. Head projection stays four fields; this bag is not a license to publish phone/salary/bank_account/rrn on GET."
      required:
        - attributes
      properties:
        employee_id:
          oneOf:
            - $ref: "#/components/schemas/Uuid"
            - type: "null"
        attributes:
          type: object
          properties:
            legal_name:
              type:
                - string
                - "null"
            display_name:
              type:
                - string
                - "null"
          additionalProperties: true
    PeopleRevisePersonInput:
      type: object
      additionalProperties: false
      description: "Typed params for `people.revise_person` (`PersonQuery::Revise`). Optional `employee_id` binds a further employee record to the same natural person."
      required:
        - person_id
        - attributes
      properties:
        person_id:
          $ref: "#/components/schemas/Uuid"
        employee_id:
          oneOf:
            - $ref: "#/components/schemas/Uuid"
            - type: "null"
        attributes:
          type: object
          properties:
            legal_name:
              type:
                - string
                - "null"
            display_name:
              type:
                - string
                - "null"
          additionalProperties: true
    Period:
      type: object
      required:
      - start
      - end
      properties:
        start:
          $ref: '#/components/schemas/Timestamp'
        end:
          $ref: '#/components/schemas/Timestamp'
    PeriodLock:
      type: object
      required:
      - id
      - domain
      - periodStart
      - periodEnd
      - reason
      - lockedAt
      properties:
        id:
          type: string
          format: uuid
        domain:
          type: string
          enum: [payroll, accounting]
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        reason:
          type: string
        lockedBy:
          type: string
          format: uuid
        lockedAt:
          type: string
          format: date-time
        unlockedBy:
          type: string
          format: uuid
        unlockedAt:
          type: string
          format: date-time
        unlockReason:
          type: string
    PeriodLockList:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PeriodLock'
    Person:
      type: object
      description: "Canonical Person head. Closed four-field projection of the latest `person_revisions` row. GET /api/v1/persons/{id} returns this current head. GET /api/v1/persons returns current heads (the same closed four-field rows `list` already returns). as_of / from / to are omitted: `persons` / `person_revisions` have no valid-time columns, and using `created_at` as `valid_from` would be a second time model. `get`/`list` never copy the attributes object onto the wire, so stored `phone` / `salary` / `bank_account` / `rrn` cannot appear. Trusted directory create mints `person_id = employee_id`. No outgoing Head FK; Employment.person_id is the reverse. Temporal slices other than revision `version` remain HOLD."
      required:
        - id
        - version
        - display_name
        - legal_name
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        display_name:
          type:
            - string
            - "null"
          description: Optional `display_name` attribute. Never invented from `legal_name`.
        legal_name:
          type:
            - string
            - "null"
        version:
          type: integer
          format: int64
          minimum: 1
          description: Optimistic concurrency token for this Head. Mutating roster actions send it as OntologyActionRequest.expected_revision (JSON body, not HTTP If-Match).
      links: []
      actions:
        - action_key: people.create_person
          object_key: person
          input:
            $ref: "#/components/schemas/PeopleCreatePersonInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: account
          edits:
            - persons
            - person_revisions
            - employee_person_bindings
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
        - action_key: people.revise_person
          object_key: person
          input:
            $ref: "#/components/schemas/PeopleRevisePersonInput"
          result:
            $ref: "#/components/schemas/OntologyActionExecuteOutcome"
          permissions:
            - role_manage
          four_eyes: account
          edits:
            - person_revisions
            - employee_person_bindings
          concurrency:
            command_id: tenant_global_idempotency
            expected_revision: optional_cas
      concurrency:
        get_token: version
        write_field: expected_revision
        write_in: body
      permissions:
        - employee_directory_read
    PlatformAccountStatus:
      type: string
      enum:
      - ACTIVE
      - PENDING_SETUP
      - DEACTIVATED
    PlatformExitResponse:
      type: object
      required:
      - ended
      properties:
        ended:
          type: boolean
    PlatformGroup:
      type: object
      required:
      - id
      - slug
      - name
      - status
      - member_count
      - members
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        slug:
          type: string
        name:
          type: string
        status:
          $ref: '#/components/schemas/PlatformOrgStatus'
        member_count:
          type: integer
          format: int64
        members:
          type: array
          items:
            $ref: '#/components/schemas/PlatformGroupMember'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    PlatformGroupAccount:
      type: object
      required:
      - user_id
      - display_name
      - phone
      - tenant_roles
      - is_active
      - has_passkey
      - account_status
      - org_id
      - org_slug
      - org_name
      - group_roles
      - created_at
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type: string
        phone:
          type:
          - string
          - 'null'
        tenant_roles:
          type: array
          items:
            $ref: '#/components/schemas/PlatformTenantRole'
        is_active:
          type: boolean
        has_passkey:
          type: boolean
        account_status:
          $ref: '#/components/schemas/PlatformAccountStatus'
        org_id:
          $ref: '#/components/schemas/Uuid'
        org_slug:
          type: string
        org_name:
          type: string
        group_roles:
          type: array
          items:
            $ref: '#/components/schemas/PlatformGroupRole'
        created_at:
          $ref: '#/components/schemas/Timestamp'
    PlatformGroupMember:
      type: object
      required:
      - id
      - slug
      - name
      - status
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        slug:
          type: string
        name:
          type: string
        status:
          $ref: '#/components/schemas/PlatformOrgStatus'
    PlatformGroupRole:
      type: string
      enum:
      - GROUP_ADMIN
      - GROUP_VIEWER
      - GROUP_FINANCE
    PlatformOpsResponse:
      type: object
      required:
      - tenants
      properties:
        tenants:
          type: array
          items:
            $ref: '#/components/schemas/PlatformTenantHealth'
    PlatformOrg:
      type: object
      required:
      - id
      - slug
      - name
      - status
      - group_id
      - group_slug
      - group_name
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        slug:
          type: string
        name:
          type: string
        status:
          $ref: '#/components/schemas/PlatformOrgStatus'
        group_id:
          type:
          - string
          - 'null'
          format: uuid
        group_slug:
          type:
          - string
          - 'null'
        group_name:
          type:
          - string
          - 'null'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    PlatformOrgOnboardingResponse:
      type: object
      required:
      - org
      - admin_user_id
      - otp
      - admin_otp_expires_at
      properties:
        org:
          $ref: '#/components/schemas/PlatformOrg'
        admin_user_id:
          $ref: '#/components/schemas/Uuid'
        otp:
          type: string
          minLength: 1
          description: One-time bootstrap OTP returned exactly once; deliver out-of-band and never log it.
        admin_otp_expires_at:
          $ref: '#/components/schemas/Timestamp'
    PlatformOrgStatus:
      type: string
      enum:
      - ACTIVE
      - SUSPENDED
      - ARCHIVED
    PlatformTenantContextStartRequest:
      type: object
      required:
      - org_id
      properties:
        org_id:
          $ref: '#/components/schemas/Uuid'
    PlatformTenantContextStartResponse:
      type: object
      required:
      - access_token
      - token_type
      - acting_org_id
      - acting_org_name
      - acting_role
      - expires_at
      properties:
        access_token:
          type: string
          minLength: 1
        token_type:
          type: string
          enum:
          - Bearer
        acting_org_id:
          $ref: '#/components/schemas/Uuid'
        acting_org_name:
          type: string
        acting_role:
          type: string
          enum:
          - SUPER_ADMIN
        expires_at:
          $ref: '#/components/schemas/Timestamp'
    PlatformTenantHealth:
      type: object
      required:
      - id
      - slug
      - name
      - status
      - group_id
      - group_slug
      - group_name
      - user_count
      - active_user_count
      - active_work_orders
      - open_work_orders
      - last_activity_at
      - route_adoption
      - zero_legacy_release_cycles
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        slug:
          type: string
        name:
          type: string
        status:
          $ref: '#/components/schemas/PlatformOrgStatus'
        group_id:
          type:
          - string
          - 'null'
          format: uuid
        group_slug:
          type:
          - string
          - 'null'
        group_name:
          type:
          - string
          - 'null'
        user_count:
          type: integer
          format: int64
        active_user_count:
          type: integer
          format: int64
        active_work_orders:
          type: integer
          format: int64
        open_work_orders:
          type: integer
          format: int64
        last_activity_at:
          type:
          - string
          - 'null'
          format: date-time
        route_adoption:
          type: array
          description: Per-release console-vs-legacy route adoption and RUM rollup for ramp decisions.
          items:
            $ref: '#/components/schemas/RouteAdoptionMetric'
        zero_legacy_release_cycles:
          type: integer
          format: int64
          description: Count of release cycles with console route traffic and zero legacy route-selection events.
    PlatformTenantRole:
      type: string
      enum:
      - SUPER_ADMIN
      - ADMIN
      - MECHANIC
      - RECEPTIONIST
      - EXECUTIVE
      - MEMBER
    PlatformViewAsStartRequest:
      type: object
      required:
      - org_id
      - role
      properties:
        org_id:
          $ref: '#/components/schemas/Uuid'
        role:
          $ref: '#/components/schemas/PlatformTenantRole'
    PlatformViewAsStartResponse:
      type: object
      required:
      - access_token
      - token_type
      - acting_org_id
      - acting_org_name
      - acting_role
      - expires_at
      properties:
        access_token:
          type: string
          minLength: 1
        token_type:
          type: string
          enum:
          - Bearer
        acting_org_id:
          $ref: '#/components/schemas/Uuid'
        acting_org_name:
          type: string
        acting_role:
          $ref: '#/components/schemas/PlatformTenantRole'
        expires_at:
          $ref: '#/components/schemas/Timestamp'
    PolicyAssignmentPreviewResponse:
      type: object
      required:
      - user_id
      - preview_receipt_id
      - preview_receipt_expires_at
      - effective
      - system_roles
      - current_system_roles
      - requested_system_roles
      - current_branch_ids
      - requested_branch_ids
      - current_role_ids
      - requested_role_ids
      - delta
      - custom_roles
      - feature_grants
      - warnings
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        preview_receipt_id:
          $ref: '#/components/schemas/Uuid'
        preview_receipt_expires_at:
          $ref: '#/components/schemas/Timestamp'
        effective:
          type: boolean
          description: True when the requested saved assignment set contains at least one ACTIVE custom role that will produce
            runtime-effective feature grants for the target user's live branch scope. DRAFT/RETIRED roles, unsupported conditions,
            invalid branch values, and reserved elevated/scope-widening features remain fail-closed.
        system_roles:
          type: array
          items:
            type: string
        current_system_roles:
          type: array
          description: System-role set currently persisted on the target before this preview.
          items:
            type: string
        requested_system_roles:
          type: array
          description: Replacement system-role set covered by the preview receipt. Defaults to the current system roles when omitted.
          items:
            type: string
        current_branch_ids:
          type: array
          description: Branch membership set currently persisted on the target before this preview.
          items:
            $ref: '#/components/schemas/Uuid'
        requested_branch_ids:
          type: array
          description: Replacement branch membership set covered by the preview receipt. Defaults to the current branch ids when omitted.
          items:
            $ref: '#/components/schemas/Uuid'
        current_role_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        requested_role_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        delta:
          $ref: '#/components/schemas/PolicyRoleAssignmentDeltaResponse'
        custom_roles:
          type: array
          items:
            $ref: '#/components/schemas/PolicyRoleImpactResponse'
        feature_grants:
          type: array
          description: Effective runtime grant preview. Custom-role rows are included only for runtime_effective custom roles;
            DRAFT roles, unsupported conditions, branch/team mismatches, and unsupported permissions are reported through
            custom_roles[].runtime_warnings instead of being shown as grants.
          items:
            $ref: '#/components/schemas/PolicyFeatureGrantPreviewResponse'
        warnings:
          type: array
          description: Preview-level warnings. Includes deterministic custom-role runtime warning codes when any requested
            role is planning/audit-only.
          items:
            type: string
    PolicyAuditEventResponse:
      type: object
      description: >-
        Append-only Policy Studio audit evidence. Console UI should render human-safe summaries and avoid exposing raw target
        identifiers by default; the identifiers remain in the API for export and traceability.
      required:
        - id
        - action
        - target_type
        - target_id
        - trace_id
        - span_id
        - occurred_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        actor:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        action:
          type: string
          pattern: ^[a-z0-9_]+(\.[a-z0-9_]+)+$
        target_type:
          type: string
          enum:
            - policy_role
            - policy_role_assignment
        target_id:
          type: string
        before_snapshot:
          type:
            - object
            - 'null'
          additionalProperties: true
        after_snapshot:
          type:
            - object
            - 'null'
          additionalProperties: true
        trace_id:
          type: string
          minLength: 32
          maxLength: 32
        span_id:
          type: string
          minLength: 16
          maxLength: 16
        occurred_at:
          $ref: '#/components/schemas/Timestamp'
    PolicyAuthorizeRequest:
      type: object
      required:
      - request
      properties:
        request:
          $ref: '#/components/schemas/PolicySimRequest'
        object_type_id:
          $ref: '#/components/schemas/Uuid'
        property_def_id:
          $ref: '#/components/schemas/Uuid'
    PolicyConditionResponse:
      type: object
      description: ABAC/PBAC condition metadata attached to a custom policy role. These constraints are persisted for audit
        and preview. Runtime evaluation supports data-backed branch narrowing and team matching; unsupported ABAC/PBAC attributes
        remain visible but fail closed until implemented.
      required:
      - condition_key
      - attribute
      - operator
      - values
      properties:
        condition_key:
          type: string
          pattern: ^[a-z][a-z0-9_]{1,63}$
        attribute:
          type: string
          enum:
          - group
          - tenant
          - organization
          - org
          - department
          - team
          - position
          - employment_status
          - assignment
          - location
          - site
          - branch
          - device_posture
          - purpose
          - action
          - resource
          - sensitive_action
        operator:
          type: string
          enum:
          - equals
          - not_equals
          - in
        values:
          type: array
          minItems: 1
          maxItems: 20
          items:
            type: string
            minLength: 1
            maxLength: 120
    PolicyCreateDraftRequest:
      type: object
      required:
      - draft_key
      - title
      - blocks
      properties:
        draft_key:
          type: string
        title:
          type: string
        author_note:
          type: string
        blocks:
          $ref: '#/components/schemas/PolicyNoCodeBlocks'
    PolicyDefaultPermissionResponse:
      type: object
      required:
      - role_key
      - permission_level
      properties:
        role_key:
          type: string
        permission_level:
          type: string
          enum:
          - deny
          - request_only
          - limited
          - allow
    PolicyFeatureGrantPreviewResponse:
      type: object
      required:
      - feature_key
      - permission_level
      - source_type
      - source_key
      - source_label
      properties:
        feature_key:
          type: string
        permission_level:
          type: string
          enum:
          - request_only
          - limited
          - allow
        source_type:
          type: string
          enum:
          - system_role
          - custom_role
        source_key:
          type: string
        source_label:
          type: string
    PolicyFeatureResponse:
      type: object
      required:
      - feature_key
      - elevated
      - default_permissions
      properties:
        feature_key:
          type: string
        elevated:
          type: boolean
        default_permissions:
          type: array
          items:
            $ref: '#/components/schemas/PolicyDefaultPermissionResponse'
    PolicyNoCodeBlocks:
      type: object
      description: One authored policy - one effect, one action, one resource type, and a set of AND-ed conditions.
      required:
      - effect
      - action
      - resource_type
      properties:
        effect:
          type: string
          enum:
          - permit
          - forbid
        action:
          type: string
        resource_type:
          type: string
        conditions:
          type: array
          items:
            $ref: '#/components/schemas/PolicyNoCodeCondition'
    PolicyNoCodeCondition:
      type: object
      required:
      - attr
      - op
      - value
      properties:
        attr:
          type: string
        op:
          type: string
          enum:
          - eq
          - ne
          - contains
        value:
          type: object
          description: 'Tagged right-hand side: {kind: literal|subject_attr|bool, value: ...}.'
          additionalProperties: true
    PolicyPermissionResponse:
      type: object
      required:
      - feature_key
      - permission_level
      properties:
        feature_key:
          type: string
        permission_level:
          type: string
          enum:
          - deny
          - request_only
          - limited
          - allow
    PolicyReviewRequest:
      type: object
      required:
      - decision
      properties:
        decision:
          type: string
          enum:
          - approve
          - reject
        note:
          type: string
    PolicyRoleAssignmentDeltaResponse:
      type: object
      required:
      - added_role_ids
      - removed_role_ids
      - unchanged_role_ids
      properties:
        added_role_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        removed_role_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        unchanged_role_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
    PolicyRoleAssignmentResponse:
      type: object
      required:
      - user_id
      - role_id
      - role_key
      - display_name
      - status
      - assigned_by
      - created_at
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        role_id:
          $ref: '#/components/schemas/Uuid'
        role_key:
          type: string
        display_name:
          type: string
        status:
          type: string
          enum:
          - DRAFT
          - ACTIVE
          - RETIRED
        assigned_by:
          type:
          - string
          - 'null'
          format: uuid
        created_at:
          $ref: '#/components/schemas/Timestamp'
    PolicyRoleCatalogResponse:
      type: object
      required:
      - policy_version
      - system_roles
      - custom_roles
      properties:
        policy_version:
          $ref: '#/components/schemas/PolicyVersionResponse'
        system_roles:
          type: array
          items:
            $ref: '#/components/schemas/SystemPolicyRoleResponse'
        custom_roles:
          type: array
          items:
            $ref: '#/components/schemas/PolicyRoleResponse'
    PolicyRoleImpactResponse:
      type: object
      description: 'Per-custom-role impact row for assignment preview. Runtime fields are computed by the same fail-closed
        evaluator used for the top-level preview: only ACTIVE roles whose supported conditions (branch and team) match the
        target user''s live attributes/scope and whose permissions are supported can expose runtime grants.'
      required:
      - role_id
      - role_key
      - display_name
      - status
      - runtime_effective
      - runtime_warnings
      - conditions
      properties:
        role_id:
          $ref: '#/components/schemas/Uuid'
        role_key:
          type: string
        display_name:
          type: string
        status:
          type: string
          enum:
          - DRAFT
          - ACTIVE
          - RETIRED
        runtime_effective:
          type: boolean
          description: True only when this custom role will produce supported runtime feature grants for the target user after
            the assignment is saved.
        runtime_warnings:
          type: array
          description: Deterministic fail-closed reasons explaining why this role is planning/audit-only for the target user.
          items:
            type: string
            enum:
            - custom_role_status_not_active
            - custom_role_condition_unsupported_by_runtime_evaluator
            - custom_role_condition_invalid_branch_value
            - custom_role_condition_outside_target_branch_scope
            - custom_role_condition_outside_target_attributes
            - custom_role_no_runtime_allowed_permissions
        conditions:
          type: array
          items:
            $ref: '#/components/schemas/PolicyConditionResponse'
    PolicyRoleResponse:
      type: object
      required:
      - id
      - role_key
      - display_name
      - description
      - status
      - is_system
      - permissions
      - conditions
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        role_key:
          type: string
        display_name:
          type: string
        description:
          type:
          - string
          - 'null'
        status:
          type: string
          description: Requested lifecycle status. Allowed transitions are DRAFT→ACTIVE, ACTIVE→DRAFT, and ACTIVE→RETIRED;
            RETIRED is terminal.
          enum:
          - DRAFT
          - ACTIVE
          - RETIRED
        is_system:
          type: boolean
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/PolicyPermissionResponse'
        conditions:
          type: array
          items:
            $ref: '#/components/schemas/PolicyConditionResponse'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    PolicyRoleStatusPreviewRequest:
      type: object
      required:
      - status
      properties:
        status:
          type: string
          enum:
          - DRAFT
          - ACTIVE
          - RETIRED
    PolicyRoleStatusPreviewResponse:
      type: object
      required:
      - role_id
      - role_key
      - display_name
      - current_status
      - requested_status
      - permission_count
      - condition_count
      - planned_assignment_count
      - requires_passkey_step_up
      - effective_runtime_change
      - warnings
      properties:
        role_id:
          $ref: '#/components/schemas/Uuid'
        role_key:
          type: string
          pattern: ^[a-z][a-z0-9_]+$
        display_name:
          type: string
        current_status:
          type: string
          enum:
          - DRAFT
          - ACTIVE
          - RETIRED
        requested_status:
          type: string
          enum:
          - DRAFT
          - ACTIVE
          - RETIRED
        permission_count:
          type: integer
          format: int64
          minimum: 0
        condition_count:
          type: integer
          format: int64
          minimum: 0
        planned_assignment_count:
          type: integer
          format: int64
          minimum: 0
        requires_passkey_step_up:
          type: boolean
        effective_runtime_change:
          type: boolean
          description: True when changing this role status would make existing assignments gain or lose runtime-effective
            custom-role grants on the next request.
        warnings:
          type: array
          items:
            type: string
            enum:
            - passkey_step_up_required
            - no_status_change
            - assigned_users_may_gain_or_lose_runtime_permissions
            - rollback_disables_assigned_custom_role_runtime_grants
            - retire_disables_assigned_custom_role_runtime_grants
            - publish_enables_assigned_custom_role_runtime_grants
    PolicyRoleTemplateResponse:
      type: object
      required:
      - template_key
      - role_key
      - display_name
      - category
      - description
      - permissions
      properties:
        template_key:
          type: string
          pattern: ^[a-z][a-z0-9_]+$
        role_key:
          type: string
          pattern: ^[a-z][a-z0-9_]+$
          description: Suggested custom role key to copy into CreatePolicyRoleRequest.
        display_name:
          type: string
          minLength: 1
          maxLength: 80
        category:
          type: string
          description: Stable grouping key for UI sections.
        description:
          type: string
          maxLength: 512
        permissions:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/PolicyPermissionResponse'
    PolicySimRequest:
      type: object
      required:
      - subject
      - action
      - resource
      properties:
        subject:
          $ref: '#/components/schemas/PolicySimSubject'
        action:
          type: string
        resource:
          $ref: '#/components/schemas/PolicySimResource'
        purpose:
          type: string
        field:
          type: string
    PolicySimResource:
      type: object
      required:
      - org
      - resource_type
      properties:
        org:
          $ref: '#/components/schemas/Uuid'
        resource_type:
          type: string
        resource_id:
          type: string
        owner:
          type: string
        branch:
          type: string
        legal_hold:
          type: boolean
    PolicySimSubject:
      type: object
      required:
      - org
      - user_id
      properties:
        org:
          $ref: '#/components/schemas/Uuid'
        user_id:
          type: string
        roles:
          type: array
          items:
            type: string
        clearance_keys:
          type: array
          items:
            type: string
    PolicySimulateRequest:
      type: object
      required:
      - request
      properties:
        request:
          $ref: '#/components/schemas/PolicySimRequest'
        include_draft_id:
          $ref: '#/components/schemas/Uuid'
    PolicyUpdateDraftRequest:
      type: object
      required:
      - blocks
      properties:
        title:
          type: string
        author_note:
          type: string
        blocks:
          $ref: '#/components/schemas/PolicyNoCodeBlocks'
    PolicyVersionResponse:
      type: object
      description: >-
        Monotonic tenant policy revision. Version 0 means no custom policy write has occurred yet; every role or custom-role
        assignment write bumps the stored version for future effective-policy cache invalidation.
      required:
        - version
      properties:
        version:
          type: integer
          format: int64
          minimum: 0
        updated_at:
          type:
            - string
            - 'null'
          format: date-time
    PollAnonymity:
      type: string
      enum:
      - NAMED
      - ANONYMOUS
    PollListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PollResponse'
    PollMyVote:
      type: object
      required:
      - submitted
      properties:
        submitted:
          type: boolean
        selected_option_ids:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/Uuid'
    PollOptionResponse:
      type: object
      required:
      - id
      - label
      - position
      - vote_count
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        label:
          type: string
        position:
          type: integer
          format: int32
        vote_count:
          type: integer
          format: int64
    PollResponse:
      type: object
      required:
      - id
      - target_scope_type
      - title
      - question
      - status
      - anonymity
      - allow_multiple
      - options
      - vote_count
      - my_vote
      - created_at
      - updated_at
      - policy
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        target_scope_type:
          $ref: '#/components/schemas/CollaborationScopeType'
        target_scope_ref:
          type:
          - string
          - 'null'
        title:
          type: string
        question:
          type: string
        status:
          $ref: '#/components/schemas/PollStatus'
        anonymity:
          $ref: '#/components/schemas/PollAnonymity'
        allow_multiple:
          type: boolean
        closes_at:
          oneOf:
          - $ref: '#/components/schemas/Timestamp'
          - type: 'null'
        object_type:
          type:
          - string
          - 'null'
        object_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        options:
          type: array
          items:
            $ref: '#/components/schemas/PollOptionResponse'
        vote_count:
          type: integer
          format: int64
        my_vote:
          $ref: '#/components/schemas/PollMyVote'
        created_by:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        policy:
          $ref: '#/components/schemas/CollaborationScopePolicy'
    PollStatus:
      type: string
      enum:
      - DRAFT
      - OPEN
      - CLOSED
      - ARCHIVED
    PostFinalizationRejectionDocument:
      type: object
      required:
      - id
      - original_run_id
      - reason
      - created_by
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        original_run_id:
          $ref: '#/components/schemas/Uuid'
        reason:
          type: string
        created_by:
          $ref: '#/components/schemas/Uuid'
    PostFinalizationRejectionRequest:
      type: object
      required:
      - reason
      - idempotency_key
      properties:
        reason:
          type: string
          minLength: 1
        idempotency_key:
          type: string
          minLength: 16
    PostFinalizationRejectionResponse:
      type: object
      required:
      - compensation
      - run
      properties:
        compensation:
          $ref: '#/components/schemas/PostFinalizationRejectionDocument'
        run:
          $ref: '#/components/schemas/FinalizedWorkflowRun'
    PrepareExpenditureRequest:
      type: object
      required:
      - expenditure_no
      - step_up
      properties:
        expenditure_no:
          type: string
          minLength: 1
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    PresignedUpload:
      type: object
      required:
      - method
      - url
      - headers
      - expires_in_secs
      properties:
        method:
          type: string
          enum:
          - PUT
        url:
          type: string
          format: uri
        headers:
          type: array
          items:
            type: array
            prefixItems:
            - type: string
            - type: string
            minItems: 2
            maxItems: 2
        expires_in_secs:
          type: integer
          format: int64
          minimum: 1
    PreviewScheduleRequest:
      type: object
      required: [cron_expr]
      properties:
        cron_expr:
          type: string
        timezone:
          type: string
          description: IANA timezone (default Asia/Seoul).
    PreviewScheduleResponse:
      type: object
      required: [cron_expr, timezone, fire_times]
      properties:
        cron_expr:
          type: string
        timezone:
          type: string
        fire_times:
          type: array
          description: The next fire instants, RFC 3339 in UTC.
          items:
            type: string
            format: date-time
    PriorityLevel:
      type: string
      enum:
      - P1
      - P2
      - P3
      - OUTSOURCE
      - UNSET
    PrivacyConsentAcceptRequest:
      type: object
      description: Required initial-login Korean privacy collection/use and service-terms acknowledgement. These required
        agreements are explicit booleans so the client cannot bundle them into one generic "agree all" flag. Optional marketing
        consent and GPS/location consent are not collected by this request.
      required:
      - policy_version
      - privacy_collection
      - terms_of_service
      properties:
        policy_version:
          type: string
          description: Required privacy/terms notice version being accepted.
        privacy_collection:
          type: boolean
          description: Required acknowledgement of collection/use purpose, items, retention period, refusal right, and refusal
            consequence.
        terms_of_service:
          type: boolean
          description: Required acknowledgement of service terms, security controls, and audit-log processing needed to operate
            the console.
    PrivacyConsentStatusResponse:
      type: object
      description: Current required first-login privacy/terms consent status.
      required:
        - policy_version
        - accepted
      properties:
        policy_version:
          type: string
          description: Current required privacy/terms notice version.
        accepted:
          type: boolean
          description: True when the authenticated user has accepted the current version.
        accepted_at:
          type:
            - string
            - 'null'
          format: date-time
          description: UTC timestamp of the latest acceptance for the current version.
    ProcessingStatus:
      type: string
      enum:
      - PROCESSING
      - READY
      - FAILED
    ProductionCapacityIngress:
      type: object
      required: [kind, id, site_id, capacity_date, available_quantity, source_id, source_version]
      properties:
        kind: {type: string, enum: [capacity]}
        id: {$ref: '#/components/schemas/Uuid'}
        site_id: {$ref: '#/components/schemas/Uuid'}
        capacity_date: {type: string, format: date}
        available_quantity: {type: integer, format: int64, minimum: 1}
        source_id: {type: string, minLength: 1, maxLength: 160}
        source_version: {type: string, minLength: 1, maxLength: 160}
    ProductionCapacitySlot:
      type: object
      required: [id, branch_id, site_id, capacity_date, available_quantity, reserved_quantity, version, source_ref, evaluated_at]
      properties:
        id: {$ref: '#/components/schemas/Uuid'}
        branch_id: {$ref: '#/components/schemas/Uuid'}
        site_id: {$ref: '#/components/schemas/Uuid'}
        capacity_date: {type: string, format: date}
        available_quantity: {type: integer, format: int64}
        reserved_quantity: {type: integer, format: int64}
        version: {type: integer}
        source_ref: {type: string}
        evaluated_at: {type: string, format: date-time}
    ProductionDemandIngress:
      type: object
      required: [kind, id, inquiry_id, product_code, quantity, due_at, source_id, source_version]
      properties:
        kind: {type: string, enum: [demand]}
        id: {$ref: '#/components/schemas/Uuid'}
        inquiry_id: {$ref: '#/components/schemas/Uuid'}
        product_code: {type: string, minLength: 1}
        quantity: {type: integer, format: int64, minimum: 1}
        due_at: {type: string, format: date-time}
        source_id: {type: string, minLength: 1, maxLength: 160}
        source_version: {type: string, minLength: 1, maxLength: 160}
    ProductionMaterialIngress:
      type: object
      required: [kind, material_item_id, quantity_on_hand_milli, safety_stock_milli, source_id, source_version]
      properties:
        kind: {type: string, enum: [material]}
        material_item_id: {$ref: '#/components/schemas/Uuid'}
        quantity_on_hand_milli: {type: integer, format: int64, minimum: 0}
        safety_stock_milli: {type: integer, format: int64, minimum: 0}
        source_id: {type: string, minLength: 1, maxLength: 160}
        source_version: {type: string, minLength: 1, maxLength: 160}
    ProductionOperation:
      type: object
      required: [id, sequence, status, output_quantity, scrap_quantity, downtime_minutes, version]
      properties:
        id: {$ref: '#/components/schemas/Uuid'}
        sequence: {type: integer}
        status: {type: string, enum: [PENDING, RELEASED, RECORDED]}
        output_quantity: {type: integer, format: int64}
        scrap_quantity: {type: integer, format: int64}
        downtime_minutes: {type: integer}
        quality_evidence_ref: {type: [string, 'null']}
        quality_passed: {type: [boolean, 'null']}
        version: {type: integer}
    ProductionPlan:
      type: object
      required: [id, branch_id, customer_demand_id, product_code, quantity, status, version, first_operation_id, created_at, due_at, plan_digest]
      properties:
        id: {$ref: '#/components/schemas/Uuid'}
        branch_id: {$ref: '#/components/schemas/Uuid'}
        customer_demand_id: {$ref: '#/components/schemas/Uuid'}
        product_code: {type: string}
        quantity: {type: integer, format: int64}
        status: {type: string, enum: [DRAFT, RELEASED]}
        version: {type: integer}
        first_operation_id: {$ref: '#/components/schemas/Uuid'}
        created_at: {type: string, format: date-time}
        due_at: {type: string, format: date-time}
        plan_digest: {type: string, pattern: '^[0-9a-f]{64}$', description: Immutable server-computed digest required in the matching release approval kind.}
    ProductionPlanDetail:
      allOf:
      - $ref: '#/components/schemas/ProductionPlan'
      - type: object
        required: [checks, events, operation]
        properties:
          checks: {type: object, additionalProperties: true}
          events: {type: array, items: {type: object, required: [id, event_type, actor_id, payload, occurred_at], properties: {id: {$ref: '#/components/schemas/Uuid'}, event_type: {type: string}, actor_id: {$ref: '#/components/schemas/Uuid'}, payload: {type: object, additionalProperties: true}, occurred_at: {type: string, format: date-time}}}}
          operation: {$ref: '#/components/schemas/ProductionOperation'}
    ProductionSourceIngress:
      oneOf:
      - $ref: '#/components/schemas/ProductionDemandIngress'
      - $ref: '#/components/schemas/ProductionCapacityIngress'
      - $ref: '#/components/schemas/ProductionMaterialIngress'
      discriminator:
        propertyName: kind
        mapping:
          demand: '#/components/schemas/ProductionDemandIngress'
          capacity: '#/components/schemas/ProductionCapacityIngress'
          material: '#/components/schemas/ProductionMaterialIngress'
    ProductionSourceIngressReceipt:
      type: object
      required: [kind, id, source_version]
      properties:
        kind: {type: string, enum: [demand, capacity, material]}
        id: {$ref: '#/components/schemas/Uuid'}
        source_version: {type: string}
    ProductionSourceSystemCredential:
      type: object
      required: [id, source_system, enabled, credential_generation, secret]
      properties:
        id: {$ref: '#/components/schemas/Uuid'}
        source_system: {type: string}
        enabled: {type: boolean, description: Always true for a newly registered or rotated source system.}
        credential_generation: {type: integer, format: int32, minimum: 1}
        secret: {type: string, readOnly: true, description: One-time standard-base64 disclosure of exactly 32 server-generated bytes.}
    ProductionSourceSystemGenerationRequest:
      type: object
      required: [expected_generation]
      properties:
        expected_generation: {type: integer, format: int32, minimum: 1}
    ProductionSourceSystemReceipt:
      type: object
      required: [id, enabled, credential_generation]
      properties:
        id: {$ref: '#/components/schemas/Uuid'}
        enabled: {type: boolean, description: Always false after a source system is disabled.}
        credential_generation: {type: integer, format: int32, minimum: 1}
    ProjectionAssumptions:
      type: object
      required: [ewma_volatility, student_t_nu, drift, simulations, seed]
      properties:
        ewma_volatility:
          type: number
          format: double
          description: Final EWMA volatility σ.
        student_t_nu:
          type: number
          format: double
          description: Student-t degrees of freedom (4).
        drift:
          type: number
          format: double
          description: Estimated per-step drift μ.
        simulations:
          type: integer
          format: int32
          description: Monte-Carlo path count.
        seed:
          type: integer
          format: int64
          description: RNG seed (echoed to prove determinism).
    ProjectionRequest:
      type: object
      required: [series, horizon, kind]
      properties:
        series:
          type: array
          description: Ordered historical values, oldest first (min 3).
          items:
            type: number
            format: double
          minItems: 3
        horizon:
          type: integer
          format: int32
          minimum: 1
          maximum: 3650
          description: Number of forward steps to project.
        kind:
          type: string
          enum: [money, percent]
          description: >
            Composition rule. `money` = multiplicative (ratio returns, floored
            at 0); `percent` = additive (arithmetic differences).
    ProjectionResult:
      type: object
      required: [point_estimate, ci95_low, ci95_high, cvar95, assumptions]
      properties:
        point_estimate:
          type: number
          format: double
          description: Deterministic central projection.
        ci95_low:
          type: number
          format: double
          description: 2.5th percentile of terminal outcomes.
        ci95_high:
          type: number
          format: double
          description: 97.5th percentile of terminal outcomes.
        cvar95:
          type: number
          format: double
          description: EVT-refined expected shortfall of the worst 5% of outcomes.
        assumptions:
          $ref: '#/components/schemas/ProjectionAssumptions'
    PublishRecruitPostingRequest:
      type: object
      required:
      - attest_exposure_scope
      - expected_updated_at
      properties:
        attest_exposure_scope:
          type: boolean
          description: The publisher's exposure-scope attestation; false fails the preflight gate.
        expected_updated_at:
          $ref: '#/components/schemas/Timestamp'
    PurchaseAttachmentDownloadResponse:
      type: object
      required:
      - url
      properties:
        url:
          type: string
    PurchaseAttachmentPresignRequest:
      type: object
      required:
      - branch_id
      - file_name
      - content_type
      - size_bytes
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        file_name:
          type: string
          minLength: 1
        content_type:
          type: string
          minLength: 1
        size_bytes:
          type: integer
          format: int64
          minimum: 1
        checksum_sha256:
          type:
          - string
          - 'null'
        role:
          type:
          - string
          - 'null'
    PurchaseAttachmentPresignResponse:
      type: object
      required:
      - attachment_id
      - upload
      - file_name
      - content_type
      - size_bytes
      - role
      - upload_state
      properties:
        attachment_id:
          $ref: '#/components/schemas/Uuid'
        upload:
          $ref: '#/components/schemas/PresignedUpload'
        file_name:
          type: string
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        role:
          type: string
        upload_state:
          type: string
    PurchaseAttachmentSummary:
      type: object
      required:
      - id
      - file_name
      - content_type
      - size_bytes
      - role
      - download_url
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        file_name:
          type: string
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        role:
          type: string
        download_url:
          type: string
        created_at:
          $ref: '#/components/schemas/Timestamp'
    PurchaseAttachmentUploadRecord:
      type: object
      required:
      - id
      - branch_id
      - file_name
      - content_type
      - size_bytes
      - role
      - upload_state
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        file_name:
          type: string
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        role:
          type: string
        upload_state:
          type: string
        created_at:
          $ref: '#/components/schemas/Timestamp'
    PurchaseFeaturePreferences:
      type: object
      required:
      - feature_key
      - schema_version
      - preferences
      properties:
        feature_key:
          type: string
        schema_version:
          type: integer
          format: int32
        preferences:
          type: object
          additionalProperties: true
    PurchasePolicySummary:
      type: object
      required:
      - equipment_required
      - statement_evidence_required
      - price_anomaly
      - quote_update_required
      - submit_blocked
      - messages
      properties:
        equipment_required:
          type: boolean
        statement_evidence_required:
          type: boolean
        price_anomaly:
          type: boolean
        quote_update_required:
          type: boolean
        submit_blocked:
          type: boolean
        messages:
          type: array
          items:
            type: string
    PurchaseRequestLineInput:
      type: object
      required:
      - item
      - quantity
      - unit_supply_price_won
      properties:
        item:
          type: string
          minLength: 1
        quantity:
          type: integer
          format: int32
          minimum: 1
        unit_supply_price_won:
          type: integer
          format: int64
          minimum: 0
        vat_won:
          type:
          - integer
          - 'null'
          format: int64
          minimum: 0
    PurchaseRequestLineSummary:
      type: object
      required:
      - id
      - line_no
      - item
      - quantity
      - unit_supply_price_won
      - vat_won
      - vat_overridden
      - line_total_won
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        line_no:
          type: integer
          format: int32
          minimum: 1
        item:
          type: string
        quantity:
          type: integer
          format: int32
        unit_supply_price_won:
          type: integer
          format: int64
        vat_won:
          type: integer
          format: int64
        vat_overridden:
          type: boolean
        line_total_won:
          type: integer
          format: int64
    PurchaseRequestPage:
      type: object
      description: Stable offset page for the branch-scoped purchase-request queue.
      required:
      - items
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseRequestSummary'
        limit:
          type: integer
          format: int64
          minimum: 1
          maximum: 100
        offset:
          type: integer
          format: int64
          minimum: 0
        total:
          type: integer
          format: int64
          minimum: 0
    PurchaseRequestSummary:
      type: object
      required:
        - id
        - branch_id
        - equipment_id
        - work_order_id
        - statement_evidence_id
        - purchase_type
        - vendor_name
        - amount_won
        - status
        - requester
        - lines
        - quote_attachments
        - policy
        - expenditure_no
        - rejection_memo
        - created_at
        - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          type:
            - string
            - "null"
          format: uuid
        work_order_id:
          type:
          - string
          - 'null'
          format: uuid
        statement_evidence_id:
          type:
            - string
            - "null"
          format: uuid
        purchase_type:
          $ref: "#/components/schemas/PurchaseType"
        vendor_name:
          type: string
        amount_won:
          type: integer
          format: int64
        status:
          $ref: "#/components/schemas/PurchaseStatus"
        requester:
          $ref: "#/components/schemas/PurchaseRequesterSummary"
        lines:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseRequestLineSummary"
        quote_attachments:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseAttachmentSummary"
        policy:
          $ref: "#/components/schemas/PurchasePolicySummary"
        expenditure_no:
          type:
          - string
          - 'null'
        rejection_memo:
          type:
          - string
          - 'null'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: "#/components/schemas/Timestamp"
    PurchaseRequesterSummary:
      type: object
      required:
      - user_id
      - display_name
      properties:
        user_id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type: string
    PurchaseStatus:
      type: string
      enum:
      - STATEMENT_ATTACHED
      - REQUEST_SUBMITTED
      - ADMIN_APPROVED
      - EXECUTIVE_PENDING
      - READY_TO_EXECUTE
      - EXECUTED
      - REJECTED
    PurchaseType:
      type: string
      enum:
      - REGULAR
      - ONE_OFF
      - OTHER
      - LEGACY_MANUAL
    QuoteLine:
      type: object
      required:
      - code
      - label
      - amount
      properties:
        code:
          type: string
        label:
          type: string
        amount:
          type: integer
          format: int64
          minimum: 0
    RaiseAttendanceExceptionRequest:
      type: object
      additionalProperties: false
      required:
        - kind
        - employee_id
        - work_date
        - detail
      properties:
        kind:
          type: string
          enum:
            - LATE
            - NO_SHOW
            - UNAPPROVED_OVERTIME
            - EARLY_LEAVE
        employee_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          oneOf:
            - {$ref: '#/components/schemas/Uuid'}
            - {type: 'null'}
        work_date:
          type: string
          format: date
        detail:
          type: string
        evidence:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [name]
            properties: {name: {type: string}, size: {type: [string, 'null']}}
    RecordInventoryReceiptRequest:
      type: object
      additionalProperties: false
      required:
        - quantityReceivedMilli
        - idempotencyKey
      properties:
        quantityReceivedMilli:
          type: integer
          format: int64
          minimum: 1
        sourceRef:
          type:
            - string
            - 'null'
        memo:
          type:
            - string
            - 'null'
        idempotencyKey:
          type: string
          minLength: 16
          maxLength: 200
    RecordProductionOperation:
      type: object
      required: [expected_version, idempotency_key, output_quantity, scrap_quantity, downtime_minutes, quality_evidence_ref, quality_passed, note]
      properties:
        expected_version: {type: integer, minimum: 1}
        idempotency_key: {type: string, minLength: 1, maxLength: 128}
        output_quantity: {type: integer, format: int64, minimum: 0}
        scrap_quantity: {type: integer, format: int64, minimum: 0}
        downtime_minutes: {type: integer, minimum: 0}
        quality_evidence_ref: {type: string, minLength: 1}
        quality_passed: {type: boolean}
        note: {type: string, maxLength: 500}
    RecordRecruitOfferReplyRequest:
      type: object
      required:
      - decision
      properties:
        decision:
          type: string
          enum: [ACCEPTED, DECLINED]
    RecordSupportTicketAcceptanceRequest:
      type: object
      required:
      - kind
      - channel
      - accepted_by
      properties:
        kind:
          $ref: '#/components/schemas/SupportTicketAcceptanceKind'
        channel:
          $ref: '#/components/schemas/SupportTicketAcceptanceChannel'
        accepted_by:
          type: string
          minLength: 1
          maxLength: 200
          description: Customer-side acknowledger name (business fact, never logged).
        note:
          type:
          - string
          - 'null'
          maxLength: 2000
          description: Required when kind is CUSTOMER_DECLINED; it becomes the customer-visible comment on the reopen.
    RecruitAmountPeriod:
      type: string
      enum: [MONTHLY, DAILY]
    RecruitApplicant:
      type: object
      required:
      - id
      - posting_id
      - applicant_no
      - name
      - profile_lines
      - source_document
      - stage
      - hold
      - doc_requested
      - rejected_at
      - reject_reason
      - reject_note
      - assessment
      - hired_employee_id
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        posting_id:
          $ref: '#/components/schemas/Uuid'
        applicant_no:
          type: string
        name:
          type: string
        profile_lines:
          type: array
          items:
            type: string
        source_document:
          type:
          - string
          - 'null'
          description: Provenance filename only.
        stage:
          $ref: '#/components/schemas/RecruitApplicantStage'
        hold:
          type: boolean
        doc_requested:
          type: boolean
        rejected_at:
          type:
          - string
          - 'null'
          format: date-time
        reject_reason:
          oneOf:
          - $ref: '#/components/schemas/RecruitRejectReason'
          - type: 'null'
        reject_note:
          type:
          - string
          - 'null'
        assessment:
          oneOf:
          - $ref: '#/components/schemas/RecruitAssessment'
          - type: 'null'
        hired_employee_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    RecruitApplicantDetailResponse:
      type: object
      required:
      - applicant
      - offers
      - events
      properties:
        applicant:
          $ref: '#/components/schemas/RecruitApplicant'
        offers:
          type: array
          items:
            $ref: '#/components/schemas/RecruitOffer'
        events:
          type: array
          items:
            $ref: '#/components/schemas/RecruitStageEvent'
    RecruitApplicantStage:
      type: string
      enum: [APPLIED, SCREENING, INTERVIEW, OFFER, HIRED]
    RecruitApplicantSummary:
      type: object
      description: Non-PII pipeline projection for posting detail. Profile lines, source document, reject note, and the assessment
        signature are served only by the audited applicant detail read.
      required:
      - id
      - posting_id
      - applicant_no
      - name
      - stage
      - hold
      - doc_requested
      - rejected_at
      - reject_reason
      - assessed
      - hired_employee_id
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        posting_id:
          $ref: '#/components/schemas/Uuid'
        applicant_no:
          type: string
          description: Per-organization display code, APL-0001.
        name:
          type: string
        stage:
          $ref: '#/components/schemas/RecruitApplicantStage'
        hold:
          type: boolean
        doc_requested:
          type: boolean
        rejected_at:
          type:
          - string
          - 'null'
          format: date-time
        reject_reason:
          oneOf:
          - $ref: '#/components/schemas/RecruitRejectReason'
          - type: 'null'
        assessed:
          type: boolean
          description: Existence flag only; the score stays behind the audited applicant detail read.
        hired_employee_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    RecruitAssessment:
      type: object
      required:
      - score
      - by
      - at
      properties:
        score:
          $ref: '#/components/schemas/RecruitAssessmentScore'
        by:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        at:
          type:
          - string
          - 'null'
          format: date-time
    RecruitAssessmentScore:
      type: string
      enum: [SUITABLE, NEUTRAL, UNSUITABLE]
    RecruitEmploymentType:
      type: string
      enum: [REGULAR, RESIDENT_SHIFT, PART_TIME, POOL_DAILY]
    RecruitHireConflictResponse:
      allOf:
      - $ref: '#/components/schemas/ErrorBody'
      - type: object
        description: employee_id is present only on an already-hired replay, naming the employee the applicant is linked
          to; every other conflict carries the error envelope alone.
        properties:
          employee_id:
            $ref: '#/components/schemas/Uuid'
    RecruitOffer:
      type: object
      required:
      - id
      - applicant_id
      - version
      - amount
      - amount_period
      - currency
      - reply_deadline
      - status
      - withdraw_reason
      - extended_by
      - extended_at
      - resolved_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        applicant_id:
          $ref: '#/components/schemas/Uuid'
        version:
          type: integer
          format: int32
          minimum: 1
        amount:
          type: string
          description: Canonical NUMERIC(14,2) decimal string.
        amount_period:
          $ref: '#/components/schemas/RecruitAmountPeriod'
        currency:
          type: string
          enum: [KRW]
        reply_deadline:
          $ref: '#/components/schemas/Date'
        status:
          $ref: '#/components/schemas/RecruitOfferStatus'
        withdraw_reason:
          type:
          - string
          - 'null'
        extended_by:
          $ref: '#/components/schemas/Uuid'
        extended_at:
          $ref: '#/components/schemas/Timestamp'
        resolved_at:
          type:
          - string
          - 'null'
          format: date-time
    RecruitOfferStatus:
      type: string
      enum: [EXTENDED, SUPERSEDED, WITHDRAWN, ACCEPTED, DECLINED]
    RecruitPosting:
      type: object
      required:
      - id
      - posting_no
      - role_title
      - company
      - worksite
      - employment_type
      - scope
      - headcount
      - hired_count
      - deadline
      - requirements
      - position_ref
      - status
      - published_at
      - closed_at
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        posting_no:
          type: string
          description: Per-organization display code, JP-0001.
        role_title:
          type: string
        company:
          type: string
        worksite:
          type: string
        employment_type:
          $ref: '#/components/schemas/RecruitEmploymentType'
        scope:
          $ref: '#/components/schemas/RecruitPostingScope'
        headcount:
          type: integer
          format: int32
        hired_count:
          type: integer
          format: int32
        deadline:
          type:
          - string
          - 'null'
          format: date
          description: Null = open-ended (상시).
        requirements:
          type: array
          items:
            type: string
        position_ref:
          type:
          - string
          - 'null'
          description: Optional ontology position instance reference.
        status:
          $ref: '#/components/schemas/RecruitPostingStatus'
        published_at:
          type:
          - string
          - 'null'
          format: date-time
        closed_at:
          type:
          - string
          - 'null'
          format: date-time
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    RecruitPostingDetailResponse:
      type: object
      required:
      - posting
      - applicants
      properties:
        posting:
          $ref: '#/components/schemas/RecruitPosting'
        applicants:
          type: array
          items:
            $ref: '#/components/schemas/RecruitApplicantSummary'
    RecruitPostingListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/RecruitPostingSummary'
    RecruitPostingPreflightResponse:
      type: object
      required:
      - checks
      - publishable
      properties:
        checks:
          type: array
          items:
            $ref: '#/components/schemas/RecruitPreflightCheck'
        publishable:
          type: boolean
          description: Verdict over the automatic checks only; the exposure attest is collected at publish.
    RecruitPostingScope:
      type: string
      enum: [INTERNAL, EXTERNAL]
    RecruitPostingStatus:
      type: string
      enum: [DRAFT, PUBLISHED, CLOSED]
    RecruitPostingSummary:
      allOf:
      - $ref: '#/components/schemas/RecruitPosting'
      - type: object
        required:
        - stage_counts
        properties:
          stage_counts:
            $ref: '#/components/schemas/RecruitStageCounts'
    RecruitPreflightCheck:
      type: object
      required:
      - key
      - ok
      - note
      properties:
        key:
          type: string
          enum: [role_defined, quota_defined, no_duplicate_open, exposure_attested]
        ok:
          type: boolean
        note:
          type: string
    RecruitPublishFailedResponse:
      allOf:
      - $ref: '#/components/schemas/ErrorBody'
      - type: object
        description: A plain validation failure carries the error envelope only; the PREFLIGHT_FAILED gate additionally carries
          the full check vector with publishable false.
        properties:
          checks:
            type: array
            items:
              $ref: '#/components/schemas/RecruitPreflightCheck'
          publishable:
            type: boolean
    RecruitRejectReason:
      type: string
      enum: [CAREER_SHORTFALL, ROLE_MISMATCH, COMP_MISMATCH, ACCEPTED_ELSEWHERE, OTHER]
    RecruitStageCounts:
      type: object
      description: Non-rejected applicant counts per pipeline stage; HIRED is not counted.
      required:
      - applied
      - screening
      - interview
      - offer
      properties:
        applied:
          type: integer
          format: int64
        screening:
          type: integer
          format: int64
        interview:
          type: integer
          format: int64
        offer:
          type: integer
          format: int64
    RecruitStageEvent:
      type: object
      required:
      - id
      - action
      - from_stage
      - to_stage
      - reason
      - actor
      - occurred_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        action:
          $ref: '#/components/schemas/RecruitStageEventAction'
        from_stage:
          oneOf:
          - $ref: '#/components/schemas/RecruitApplicantStage'
          - type: 'null'
        to_stage:
          oneOf:
          - $ref: '#/components/schemas/RecruitApplicantStage'
          - type: 'null'
        reason:
          type:
          - string
          - 'null'
          description: Free-text or enum payload for the action — reject reason, assessment score, offer decision, or withdraw
            reason.
        actor:
          $ref: '#/components/schemas/Uuid'
        occurred_at:
          $ref: '#/components/schemas/Timestamp'
    RecruitStageEventAction:
      type: string
      enum: [APPLY, ADVANCE, ASSESS, HOLD, UNHOLD, REQUEST_DOCUMENTS, OFFER_EXTEND, OFFER_ADJUST, OFFER_WITHDRAW, OFFER_REPLY,
        REJECT, REINSTATE, HIRE]
    RecruitTalentPoolEntry:
      type: object
      required:
      - applicant_id
      - applicant_no
      - name
      - role_title
      - reason
      - rejected_at
      properties:
        applicant_id:
          $ref: '#/components/schemas/Uuid'
        applicant_no:
          type: string
        name:
          type: string
        role_title:
          type: string
          description: Role title of the posting the applicant was rejected from.
        reason:
          oneOf:
          - $ref: '#/components/schemas/RecruitRejectReason'
          - type: 'null'
        rejected_at:
          $ref: '#/components/schemas/Timestamp'
    RecruitTalentPoolListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/RecruitTalentPoolEntry'
    RefreshTokenRequest:
      type: object
      description: >-
        Refresh/logout request body. `refresh_token` is OPTIONAL because the web transport carries the token in the HttpOnly
        `console_refresh` cookie (sent automatically by the browser) and the body is empty; mobile clients send the token
        here.
      properties:
        refresh_token:
          type:
            - string
            - 'null'
    RegionSummary:
      type: object
      required:
      - id
      - name
      - deactivated_at
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        deactivated_at:
          type:
          - string
          - 'null'
          format: date-time
          description: Set when the region has been soft-deleted (deactivated); null for an active region. Active-only listings
            omit deactivated rows.
        created_at:
          $ref: '#/components/schemas/Timestamp'
    RegisterProductionSourceSystem:
      type: object
      required: [branch_id, source_system]
      properties:
        branch_id: {$ref: '#/components/schemas/Uuid'}
        source_system: {type: string, minLength: 1, maxLength: 80}
    RegistryImportReport:
      type: object
      required:
      - input_rows
      - equipment_count
      - added
      - updated
      - unchanged
      - orphaned
      - errors
      properties:
        input_rows:
          type: integer
          minimum: 0
        equipment_count:
          type: integer
          minimum: 0
        added:
          type: integer
          minimum: 0
        updated:
          type: integer
          minimum: 0
        unchanged:
          type: integer
          minimum: 0
        orphaned:
          type: integer
          minimum: 0
        errors:
          type: array
          items:
            $ref: '#/components/schemas/RegistryRowError'
    RegistryRowError:
      type: object
      required:
      - sheet
      - row
      - message
      properties:
        sheet:
          type: string
        row:
          type: integer
          format: int64
          minimum: 0
        message:
          type: string
    RegulationImpact:
      type: object
      required: [id, code, title, jurisdiction, citation, impact_area, impact_summary, risk_level, status, metadata, created_by, updated_by, created_at, updated_at]
      properties:
        id: { $ref: '#/components/schemas/Uuid' }
        code: { type: string }
        title: { type: string }
        jurisdiction: { type: string }
        regulator: { type: [string, 'null'] }
        citation: { type: string }
        source_url: { type: [string, 'null'], format: uri }
        impact_area: { type: string }
        impact_summary: { type: string }
        risk_level: { $ref: '#/components/schemas/ComplianceRiskLevel' }
        status: { $ref: '#/components/schemas/RegulationImpactStatus' }
        effective_from: { type: [string, 'null'], format: date }
        effective_to: { type: [string, 'null'], format: date }
        review_due_on: { type: [string, 'null'], format: date }
        owner_user_id: { type: [string, 'null'], format: uuid }
        metadata: { type: object, additionalProperties: true }
        created_by: { $ref: '#/components/schemas/Uuid' }
        updated_by: { $ref: '#/components/schemas/Uuid' }
        created_at: { $ref: '#/components/schemas/Timestamp' }
        updated_at: { $ref: '#/components/schemas/Timestamp' }
    RegulationImpactPage:
      type: object
      required: [items, limit, offset, total]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/RegulationImpact' } }
        limit: { type: integer, format: int64 }
        offset: { type: integer, format: int64 }
        total: { type: integer, format: int64 }
    RegulationImpactStatus:
      type: string
      enum: [DRAFT, ACTIVE, SUPERSEDED, ARCHIVED]
    RegulationLinkRequest:
      type: object
      required: [regulation_impact_id, relationship]
      properties:
        regulation_impact_id: { $ref: '#/components/schemas/Uuid' }
        relationship: { $ref: '#/components/schemas/ObligationRegulationRelationship' }
        rationale: { type: [string, 'null'] }
    RejectPurchaseRequest:
      type: object
      required:
      - memo
      - step_up
      properties:
        memo:
          type: string
          minLength: 1
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    RejectRecruitApplicantRequest:
      type: object
      required:
      - reason
      properties:
        reason:
          $ref: '#/components/schemas/RecruitRejectReason'
        note:
          type:
          - string
          - 'null'
          maxLength: 500
    RejectWorkOrderRequest:
      type: object
      required:
      - memo
      properties:
        memo:
          type: string
          minLength: 1
    ReleaseProductionPlan:
      type: object
      required: [expected_version, approval_ref, idempotency_key]
      properties:
        expected_version: {type: integer, minimum: 1}
        approval_ref: {$ref: '#/components/schemas/Uuid', description: 'Approved production_plan_release:v{expected_version} decision bound to this persisted plan. It is consumed exactly once by the current approver.'}
        idempotency_key: {type: string, minLength: 1, maxLength: 128}
    RentalQuoteSummary:
      type: object
      required:
      - id
      - branch_id
      - equipment_id
      - acquisition_value
      - current_residual_value
      - effective_residual_value
      - residual_was_floored
      - cumulative_repair_cost
      - monthly_total
      - lines
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        acquisition_value:
          type: integer
          format: int64
        current_residual_value:
          type: integer
          format: int64
        effective_residual_value:
          type: integer
          format: int64
        residual_was_floored:
          type: boolean
        cumulative_repair_cost:
          type: integer
          format: int64
        monthly_total:
          type: integer
          format: int64
        lines:
          type: array
          items:
            $ref: '#/components/schemas/QuoteLine'
        created_at:
          $ref: '#/components/schemas/Timestamp'
    ReplaceEvaluationGoalsRequest:
      type: object
      additionalProperties: false
      required: [goals]
      properties:
        goals:
          type: array
          maxItems: 20
          items: { $ref: '#/components/schemas/EvaluationGoalInput' }
    ReplacePolicyRoleAssignmentsRequest:
      type: object
      description: 'Policy assignment/account-scope preview and custom-role assignment replacement. The preview endpoint
        accepts `role_ids` plus optional `system_roles` and `branch_ids` for account/person mutation previews. The mutating
        custom-role replacement endpoint also requires `preview_acknowledged: true`, the server-issued `preview_receipt_id`
        from a current impact preview for the same actor/user/role set, plus a fresh passkey `step_up` assertion server-side
        so the audit trail binds the sensitive policy write to the human actor. ACTIVE custom-role assignments become runtime-effective
        on the next request when their grants and conditions pass the fail-closed runtime evaluator.'
      properties:
        role_ids:
          type: array
          description: Custom-role replacement set. For account/person role-or-scope previews, omit this field to preserve
            the target's current custom-role assignments; send an explicit empty array to preview removing all custom roles.
          items:
            $ref: '#/components/schemas/Uuid'
        system_roles:
          type: array
          description: Optional replacement system-role set for account/person mutation previews. Ignored by the custom-role assignment save endpoint.
          items:
            type: string
        branch_ids:
          type: array
          description: Optional replacement branch membership set for account/person mutation previews. Ignored by the custom-role assignment save endpoint.
          items:
            $ref: '#/components/schemas/Uuid'
        preview_acknowledged:
          type: boolean
          description: Must be true for the mutating replacement endpoint after the caller reviewed the assignment impact
            preview. The preview endpoint ignores this field. Omitted values are treated as false by the server.
        preview_receipt_id:
          $ref: '#/components/schemas/Uuid'
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    ResolveAttendanceExceptionRequest:
      type: object
      additionalProperties: false
      required:
        - action
        - reason
      properties:
        action:
          type: string
        reason:
          type: string
        linked_work_ref:
          type:
            - string
            - 'null'
        ot_hours:
          type:
            - number
            - 'null'
    ResolvePayrollExceptionRequest:
      type: object
      required:
      - action
      properties:
        action:
          type: string
          enum: [CONFIRM, HOLD]
        reason:
          type: string
          description: Required for HOLD (a blank reason is rejected with 422).
    ResolvedInstance:
      type: object
      required: [id, type, title]
      properties:
        id: { type: string, format: uuid }
        type: { type: string }   # object-type stable_key
        title: { type: string }
    RespondP1DispatchRequest:
      type: object
      required:
      - response
      properties:
        response:
          $ref: '#/components/schemas/DispatchResponseKind'
    RestartPurchaseRequest:
      type: object
      required:
        - lines
        - quote_attachment_ids
        - memo
      properties:
        statement_evidence_id:
          type:
            - string
            - "null"
          format: uuid
        amount_won:
          type:
            - integer
            - "null"
          format: int64
          minimum: 1
        lines:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/PurchaseRequestLineInput"
        quote_attachment_ids:
          type: array
          items:
            $ref: "#/components/schemas/Uuid"
        memo:
          type: string
          minLength: 1
    ReturnSubstituteRequest:
      type: object
      properties:
        return_note:
          type:
          - string
          - 'null'
    ReverseVoucherRequest:
      type: object
      properties:
        memo:
          type: string
    ReviewCadence:
      type: string
      enum: [MONTHLY, QUARTERLY, SEMI_ANNUAL, ANNUAL, EVENT_DRIVEN]
    ReviewDailyPlanRequest:
      type: object
      required:
      - decision
      properties:
        decision:
          $ref: '#/components/schemas/DailyPlanStatus'
        memo:
          type: string
    ReviewSettlementRequest:
      type: object
      required:
      - decision
      properties:
        decision:
          type: string
          enum: [APPROVED, RETURNED]
        comment:
          type: string
          description: Required (non-empty after trim) when decision is RETURNED.
    ReviewTargetChangeRequest:
      type: object
      required:
      - decision
      properties:
        decision:
          $ref: '#/components/schemas/TargetChangeDecision'
        memo:
          type: string
    RevisionSummary:
      type: object
      required:
        - id
        - instance_id
        - version
        - attributes
        - valid_from
        - prev_hash
        - row_hash
      properties:
        id:
          type: string
          format: uuid
        instance_id:
          type: string
          format: uuid
        version:
          type: integer
          format: int64
        attributes:
          type: object
          additionalProperties: true
        valid_from:
          type: string
          format: date-time
        valid_to:
          type:
            - string
            - 'null'
          format: date-time
        action_type_id:
          type:
            - string
            - 'null'
          format: uuid
        actor:
          type:
            - string
            - 'null'
          format: uuid
        reason:
          type:
            - string
            - 'null'
        prev_hash:
          type: string
        row_hash:
          type: string
    RollbackWorkflowDefinitionRequest:
      type: object
      required:
      - target_version
      properties:
        target_version:
          type: integer
          format: int32
          minimum: 1
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    RouteAdoptionMetric:
      type: object
      required:
      - release_cycle
      - console_route_events
      - legacy_route_events
      - rum_error_events
      - rum_perf_p95_ms
      - last_event_at
      properties:
        release_cycle:
          type: string
        console_route_events:
          type: integer
          format: int64
        legacy_route_events:
          type: integer
          format: int64
        rum_error_events:
          type: integer
          format: int64
        rum_perf_p95_ms:
          type:
          - integer
          - 'null'
          format: int64
        last_event_at:
          type: string
          format: date-time
    SalesListingPage:
      type: object
      required:
      - items
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SalesListingView'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
    SalesListingView:
      type: object
      description: A sales listing as read by the storefront or the admin console.
      required:
      - id
      - equipment_id
      - kind
      - condition
      - model_name
      - capacity_milli
      - model_year
      - usage_hours
      - price_won
      - badge
      - usage_label
      - condition_label
      - availability
      - location
      - description
      - listing_type
      - status
      - sort_weight
      - created_at
      - updated_at
      - media
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
        kind:
          $ref: '#/components/schemas/ListingKind'
        condition:
          $ref: '#/components/schemas/ListingCondition'
        model_name:
          type: string
        capacity_milli:
          type:
          - integer
          - 'null'
          format: int64
        model_year:
          type:
          - integer
          - 'null'
          format: int32
        usage_hours:
          type:
          - integer
          - 'null'
          format: int32
        price_won:
          type:
          - integer
          - 'null'
          format: int64
        badge:
          type:
          - string
          - 'null'
        usage_label:
          type:
          - string
          - 'null'
        condition_label:
          type:
          - string
          - 'null'
        availability:
          type:
          - string
          - 'null'
        location:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        listing_type:
          $ref: '#/components/schemas/ListingType'
        status:
          $ref: '#/components/schemas/ListingStatus'
        sort_weight:
          type: integer
          format: int32
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        media:
          type: array
          items:
            $ref: '#/components/schemas/ListingMediaView'
    SaveEvaluationReviewRequest:
      type: object
      additionalProperties: false
      properties:
        grade: { oneOf: [{ $ref: '#/components/schemas/EvaluationGrade' }, { type: 'null' }] }
        note: { type: [string, 'null'], maxLength: 2000 }
        evidence_links:
          type: array
          maxItems: 10
          default: []
          items: { $ref: '#/components/schemas/EvaluationEvidenceLinkInput' }
    SavePurchasePreferencesRequest:
      type: object
      required:
      - schema_version
      - preferences
      properties:
        schema_version:
          type: integer
          format: int32
          minimum: 1
        preferences:
          type: object
          additionalProperties: true
    SchedulePayrollDisbursementRequest:
      type: object
      required:
      - scheduled_at
      properties:
        scheduled_at:
          type: string
          format: date-time
          description: RFC 3339; must be in the future.
    ScheduleRunItem:
      type: object
      required:
        - run_id
        - status
        - definition_id
        - definition_version
        - started_at
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
        definition_id:
          $ref: '#/components/schemas/Uuid'
        definition_version:
          type: integer
        started_at:
          type: string
          format: date-time
        completed_at:
          type:
            - string
            - 'null'
          format: date-time
        failed_at:
          type:
            - string
            - 'null'
          format: date-time
    ScheduleRunListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ScheduleRunItem'
    SearchResponse:
      type: object
      description: Global search hits, each an ObjectHead scoped identically to resolveObject (a hit the caller could
        not resolve never appears). Grouped by kind; exists is always true for a hit.
      required:
      - results
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/ObjectHead'
    SelfLeaveBalance:
      type: object
      required:
        - employee_id
        - name
        - accrued_units
        - used_units
        - remaining_units
        - filing_state
        - home_branch_id
      properties:
        employee_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        accrued_units:
          oneOf:
            - {$ref: '#/components/schemas/LeaveBalanceAmount'}
            - {type: 'null'}
        used_units:
          oneOf:
            - {$ref: '#/components/schemas/LeaveBalanceAmount'}
            - {type: 'null'}
        remaining_units:
          oneOf:
            - {$ref: '#/components/schemas/LeaveBalanceAmount'}
            - {type: 'null'}
        filing_state:
          type: string
          enum:
            - ready
            - home_branch_required
          description: Whether self-service filing currently has an active authoritative home branch.
        home_branch_id:
          type:
            - string
            - 'null'
          format: uuid
          description: Active authoritative routing branch; null exactly when filing_state is home_branch_required.
    SendMailRequest:
      type: object
      description: Compose a message. For reply/forward, in_reply_to is required and references carries the accumulated chain.
        The From is constrained to the configured account address (it cannot be set here).
      required:
      - to
      - subject
      - body_text
      properties:
        to:
          type: array
          items:
            $ref: '#/components/schemas/MailAddress'
        cc:
          type: array
          items:
            $ref: '#/components/schemas/MailAddress'
        bcc:
          type: array
          items:
            $ref: '#/components/schemas/MailAddress'
        subject:
          type: string
        body_text:
          type: string
        attachments:
          type: array
          items:
            $ref: '#/components/schemas/MailAttachment'
        in_reply_to:
          type:
          - string
          - 'null'
          description: Required for reply/forward — the Message-ID being responded to.
        references:
          type: array
          items:
            type: string
    SendMailResult:
      type: object
      required:
      - message_id
      - rfc_message_id
      properties:
        message_id:
          $ref: '#/components/schemas/Uuid'
        rfc_message_id:
          type: string
          description: The RFC 5322 Message-ID stamped on the sent message.
    SendMessengerMessageRequest:
      type: object
      required:
      - body
      properties:
        body:
          type: string
          minLength: 1
          maxLength: 4000
        attachment_evidence_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        quoted_message_id:
          description: Optional reply-quote target; must be a message in the same thread.
          type:
          - string
          - 'null'
          format: uuid
    SeriesByInstanceResponse:
      type: object
      description: The series an instance belongs to, or null.
      required:
      - series
      properties:
        series:
          oneOf:
          - $ref: '#/components/schemas/SeriesHead'
          - type: 'null'
    SeriesDetailResponse:
      type: object
      description: A series head plus its instances resolved to ObjectHeads, ordered by attach time. Instances not
        resolvable for the caller are omitted (deny-by-omission).
      required:
      - id
      - code
      - label
      - created_at
      - instances
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          description: Canonical SR- code.
        label:
          type: string
        created_at:
          $ref: '#/components/schemas/Timestamp'
        instances:
          type: array
          items:
            $ref: '#/components/schemas/ObjectHead'
    SeriesHead:
      type: object
      required:
      - id
      - code
      - label
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          description: Canonical SR- code.
        label:
          type: string
        created_at:
          $ref: '#/components/schemas/Timestamp'
    SetEmployeeHomeBranchRequest:
      type: object
      additionalProperties: false
      required:
      - branch_id
      - expected_updated_at
      properties:
        branch_id:
          $ref: '#/components/schemas/Uuid'
        expected_updated_at:
          $ref: '#/components/schemas/Timestamp'
    SetLifecycleHoldRequest:
      type: object
      required:
      - legalHold
      properties:
        legalHold:
          type: boolean
        retentionUntil:
          type: string
          format: date
          description: ISO date; omit or null to clear the retention deadline.
    SetMessengerThreadMuteRequest:
      type: object
      required:
      - muted
      properties:
        muted:
          type: boolean
    SetTodoDoneRequest:
      type: object
      required:
      - done
      properties:
        done:
          type: boolean
          description: Explicit target state (true = done, false = undo).
    SettlementLineKind:
      type: string
      enum: [LABOR, PART, OUTSOURCE, OTHER]
    SettlementLineRequest:
      type: object
      required:
      - kind
      - label
      - amount_krw
      properties:
        kind:
          $ref: '#/components/schemas/SettlementLineKind'
        label:
          type: string
          minLength: 1
        amount_krw:
          type: integer
          format: int64
          minimum: 0
        source_ref:
          type: string
          description: PO/IV/outsource code when known.
        sort_order:
          type: integer
          format: int32
          description: Defaults to the line's position in the array.
    SettlementLineSummary:
      type: object
      required:
      - id
      - kind
      - label
      - amount_krw
      - source_ref
      - sort_order
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        kind:
          $ref: '#/components/schemas/SettlementLineKind'
        label:
          type: string
        amount_krw:
          type: integer
          format: int64
        source_ref:
          type:
          - string
          - 'null'
        sort_order:
          type: integer
          format: int32
    SettlementStatus:
      type: string
      description: Cost-settlement lifecycle (정산 → 전표). VOID is terminal and frees the one-live-settlement slot.
      enum: [DRAFT, SUBMITTED, APPROVED, VOID]
    SettlementSummary:
      type: object
      required:
      - id
      - work_order_id
      - branch_id
      - status
      - total_amount_krw
      - voucher_ref
      - note
      - lines
      - created_by
      - submitted_by
      - submitted_at
      - approved_by
      - approved_at
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/SettlementStatus'
        total_amount_krw:
          type: integer
          format: int64
        voucher_ref:
          type:
          - string
          - 'null'
          description: Finance journal/voucher code once posted (finance-gl lane).
        note:
          type:
          - string
          - 'null'
        lines:
          type: array
          items:
            $ref: '#/components/schemas/SettlementLineSummary'
        created_by:
          $ref: '#/components/schemas/Uuid'
        submitted_by:
          type:
          - string
          - 'null'
          format: uuid
        submitted_at:
          type:
          - string
          - 'null'
          format: date-time
        approved_by:
          type:
          - string
          - 'null'
          format: uuid
        approved_at:
          type:
          - string
          - 'null'
          format: date-time
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    SignupRequest:
      type: object
      required:
      - email
      properties:
        email:
          type: string
          format: email
          description: The email address to register and deliver the one-time code to.
    SignupResponse:
      type: object
      required:
      - accepted
      properties:
        accepted:
          type: boolean
          description: Always true. The signup was accepted and a one-time code was sent; the response intentionally carries
            no account-existence or token information.
    SimulateWorkflowDefinitionRequest:
      type: object
      properties:
        definition:
          type: object
          additionalProperties: true
        approval_line:
          type: array
          items:
            type: object
            additionalProperties: true
        payment_line:
          type: array
          items:
            type: object
            additionalProperties: true
        notification_rules:
          type: array
          items:
            type: object
            additionalProperties: true
        action_allowlist:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowActionAllowlistEntry'
        sample_context:
          type: object
          additionalProperties: true
          description: Sample run context to exercise condition/branch nodes against (defaults to {}).
    SimulationOutcome:
      type: object
      required: [effect, determining_policies, errors, reason]
      properties:
        effect: { type: string, enum: [allow, deny] }
        determining_policies: { type: array, items: { type: string } }
        errors: { type: array, items: { type: string } }
        reason: { type: string }
    SiteContact:
      type: object
      required:
      - name
      - phone
      - email
      properties:
        name:
          type:
          - string
          - 'null'
        phone:
          type:
          - string
          - 'null'
        email:
          type:
          - string
          - 'null'
    SiteLocationGroup:
      type: object
      required:
      - site_id
      - site_name
      - customer_id
      - customer_name
      - branch_id
      - address
      - postal_code
      - province
      - city
      - latitude
      - longitude
      - geofence_radius_m
      - contact_name
      - contact_phone
      - contact_email
      - equipment_count
      - rented_count
      - spare_count
      - substitution_active_count
      properties:
        site_id:
          $ref: '#/components/schemas/Uuid'
        site_name:
          type: string
        customer_id:
          $ref: '#/components/schemas/Uuid'
        customer_name:
          type: string
        branch_id:
          $ref: '#/components/schemas/Uuid'
        address:
          type:
          - string
          - 'null'
        postal_code:
          type:
          - string
          - 'null'
        province:
          type:
          - string
          - 'null'
        city:
          type:
          - string
          - 'null'
        latitude:
          type:
          - number
          - 'null'
          format: double
          minimum: -90
          maximum: 90
          description: Admin-entered WGS84 latitude in degrees; null means the site is not pinned.
        longitude:
          type:
          - number
          - 'null'
          format: double
          minimum: -180
          maximum: 180
          description: Admin-entered WGS84 longitude in degrees; null means the site is not pinned.
        geofence_radius_m:
          type:
          - number
          - 'null'
          format: double
          description: Per-site geofence radius in metres for arrival/departure detection; null uses the system default (300
            m).
        contact_name:
          type:
          - string
          - 'null'
          description: On-site representative contact name (담당자명).
        contact_phone:
          type:
          - string
          - 'null'
          description: On-site contact phone (연락처).
        contact_email:
          type:
          - string
          - 'null'
          description: Optional contact email (이메일).
        equipment_count:
          type: integer
          format: int64
        rented_count:
          type: integer
          format: int64
        spare_count:
          type: integer
          format: int64
        substitution_active_count:
          type: integer
          format: int64
    StartP1DispatchRequest:
      type: object
      properties:
        incident_location:
          $ref: '#/components/schemas/IncidentLocation'
        include_region:
          type: boolean
          default: false
    StartWorkflowRunRequest:
      type: object
      required:
      - definition_id
      - trigger_type
      - idempotency_key
      properties:
        definition_id:
          $ref: '#/components/schemas/Uuid'
        definition_version:
          type: integer
          format: int32
          description: Pin a specific version; omitted uses the definition's active version.
        object_type:
          type: string
          description: Must be provided together with object_id.
        object_id:
          $ref: '#/components/schemas/Uuid'
        trigger_type:
          type: string
          enum:
          - MANUAL
          - SCHEDULE
          - OBJECT_EVENT
          - IMPORT_EVENT
          - MAIL_EVENT
          - MESSENGER_EVENT
          - CALENDAR_EVENT
          - POLL_EVENT
          - API
        idempotency_key:
          type: string
          minLength: 16
        correlation_id:
          type: string
          minLength: 8
        input_payload:
          type: object
          additionalProperties: true
        context_payload:
          type: object
          additionalProperties: true
    StartWorkflowRunResponse:
      type: object
      required:
      - run
      properties:
        run:
          $ref: '#/components/schemas/WorkflowRunSummary'
        next_task:
          $ref: '#/components/schemas/WorkflowTaskSummary'
    StatusHistorySummary:
      type: object
      required:
      - id
      - actor
      - action
      - from_status
      - to_status
      - occurred_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        actor:
          type:
          - string
          - 'null'
          format: uuid
        action:
          type: string
        from_status:
          type:
          - string
          - 'null'
        to_status:
          $ref: '#/components/schemas/WorkOrderStatus'
        occurred_at:
          $ref: '#/components/schemas/Timestamp'
    SubmitInquiryRequest:
      type: object
      description: Public lead intake payload. `name` and `phone` are required; `location`, `message`, and `listing_id` are
        optional. The server never echoes any field back.
      required:
      - name
      - phone
      - topic
      properties:
        name:
          type: string
        phone:
          type: string
        topic:
          $ref: '#/components/schemas/InquiryTopic'
        location:
          type:
          - string
          - 'null'
        message:
          type:
          - string
          - 'null'
        listing_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
    SubmitReportRequest:
      type: object
      required:
      - result_type
      - diagnosis
      - action_taken
      properties:
        result_type:
          $ref: '#/components/schemas/WorkResultType'
        diagnosis:
          type: string
        action_taken:
          type: string
    SubmittableDefinitionListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SubmittableDefinitionResponse'
    SubmittableDefinitionResponse:
      type: object
      description: An ACTIVE workflow definition the caller may start from the 기안 template gallery. Carries only the
        metadata definitions actually hold; active_version is the version a start binds to.
      required:
      - id
      - workflow_key
      - display_name
      - object_type
      - active_version
      - required_approval_line
      - required_payment_line
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        workflow_key:
          type: string
        display_name:
          type: string
        object_type:
          type: string
        active_version:
          type: integer
          format: int32
        required_approval_line:
          type: boolean
        required_payment_line:
          type: boolean
    SubstituteAssignment:
      type: object
      required:
      - id
      - branch_id
      - source_equipment_id
      - substitute_equipment_id
      - assigned_by
      - assignment_location
      - assigned_at
      properties:
        id:
          type: string
          format: uuid
        branch_id:
          type: string
          format: uuid
        source_equipment_id:
          type: string
          format: uuid
        substitute_equipment_id:
          type: string
          format: uuid
        assigned_by:
          type: string
          format: uuid
        assigned_to:
          type:
          - string
          - 'null'
          format: uuid
        assignment_location:
          type: string
        assigned_at:
          type: string
          format: date-time
        returned_by:
          type:
          - string
          - 'null'
          format: uuid
        returned_at:
          type:
          - string
          - 'null'
          format: date-time
        return_note:
          type:
          - string
          - 'null'
    SubstituteCandidate:
      type: object
      required:
      - equipment_id
      - branch_id
      - equipment_no
      - management_no
      - model
      - status
      - specification
      - ton_text
      - ton_milli
      - power_code
      - power_label
      - customer_name
      - site_name
      - placement_location
      - match_kind
      - ton_delta_milli
      properties:
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_no:
          type: string
        management_no:
          type:
          - string
          - 'null'
        model:
          type:
          - string
          - 'null'
        status:
          $ref: '#/components/schemas/EquipmentStatus'
        specification:
          type: string
        ton_text:
          type: string
        ton_milli:
          type:
          - integer
          - 'null'
          format: int32
        power_code:
          type: string
        power_label:
          type:
          - string
          - 'null'
        customer_name:
          type: string
        site_name:
          type: string
        placement_location:
          type:
          - string
          - 'null'
        match_kind:
          $ref: '#/components/schemas/SubstituteMatchKind'
        ton_delta_milli:
          type:
          - integer
          - 'null'
          format: int32
    SubstituteCandidatePage:
      type: object
      required:
      - items
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SubstituteCandidate'
        total:
          type: integer
          minimum: 0
    SubstituteMatchKind:
      type: string
      enum:
      - exact_ton
      - nearest_above
      - unknown_ton_exact_text
    SupportIntakeAck:
      type: object
      required:
      - status
      properties:
        status:
          type: string
    SupportTicketAcceptance:
      type: object
      required:
      - id
      - ticket_id
      - kind
      - channel
      - accepted_by
      - note
      - recorded_by_user_id
      - recorded_by_name
      - occurred_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        ticket_id:
          $ref: '#/components/schemas/Uuid'
        kind:
          $ref: '#/components/schemas/SupportTicketAcceptanceKind'
        channel:
          $ref: '#/components/schemas/SupportTicketAcceptanceChannel'
        accepted_by:
          type: string
        note:
          type:
          - string
          - 'null'
        recorded_by_user_id:
          $ref: '#/components/schemas/Uuid'
        recorded_by_name:
          type:
          - string
          - 'null'
          description: Recorder display name via a same-org lookup; null when the account no longer exists.
        occurred_at:
          $ref: '#/components/schemas/Timestamp'
    SupportTicketAcceptanceChannel:
      type: string
      enum:
      - IN_PERSON
      - PHONE
      - EMAIL
      - MESSENGER
    SupportTicketAcceptanceKind:
      type: string
      enum:
      - CUSTOMER_ACCEPTED
      - CUSTOMER_DECLINED
    SupportTicketCategory:
      type: string
      enum:
      - SYSTEM_BUG
      - ACCESS_REQUEST
      - OPERATIONAL
      - EQUIPMENT_INQUIRY
      - COMPLAINT
      - OTHER
    SupportTicketComment:
      type: object
      required:
      - id
      - ticket_id
      - author_user_id
      - author_name
      - body
      - is_internal_note
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        ticket_id:
          $ref: '#/components/schemas/Uuid'
        author_user_id:
          $ref: '#/components/schemas/Uuid'
        author_name:
          type:
          - string
          - 'null'
          description: Comment author display name, resolved via a same-org LEFT JOIN on users. Null for an authorless comment
            or a deleted author.
        body:
          type: string
        is_internal_note:
          type: boolean
        created_at:
          $ref: '#/components/schemas/Timestamp'
    SupportTicketDetail:
      type: object
      required:
      - ticket
      - comments
      properties:
        ticket:
          $ref: '#/components/schemas/SupportTicketSummary'
        comments:
          type: array
          items:
            $ref: '#/components/schemas/SupportTicketComment'
    SupportTicketOrigin:
      type: string
      enum:
      - INTERNAL
      - CUSTOMER
    SupportTicketPage:
      type: object
      required:
      - items
      - next_cursor
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SupportTicketSummary'
        next_cursor:
          type:
          - string
          - 'null'
          format: uuid
          description: Id to pass as `cursor` for the next page, or null on the last page.
        total:
          type: integer
          format: int64
    SupportTicketPriority:
      type: string
      enum:
      - LOW
      - MEDIUM
      - HIGH
      - URGENT
    SupportTicketStatus:
      type: string
      enum:
      - OPEN
      - IN_PROGRESS
      - ON_HOLD
      - RESOLVED
      - CLOSED
    SupportTicketSummary:
      type: object
      required:
      - id
      - branch_id
      - origin
      - category
      - priority
      - status
      - title
      - requester_user_id
      - requester_name
      - assignee_user_id
      - assignee_name
      - due_at
      - created_at
      - updated_at
      - resolved_at
      - closed_at
      - site_id
      - site_name
      - customer_id
      - customer_name
      - work_order_id
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        origin:
          $ref: '#/components/schemas/SupportTicketOrigin'
        category:
          $ref: '#/components/schemas/SupportTicketCategory'
        priority:
          $ref: '#/components/schemas/SupportTicketPriority'
        status:
          $ref: '#/components/schemas/SupportTicketStatus'
        title:
          type: string
        requester_user_id:
          $ref: '#/components/schemas/Uuid'
        requester_name:
          type:
          - string
          - 'null'
        assignee_user_id:
          $ref: '#/components/schemas/Uuid'
        assignee_name:
          type:
          - string
          - 'null'
          description: Assignee display name, resolved via a same-org LEFT JOIN on users. Null when unassigned or the assignee
            account no longer exists.
        due_at:
          type:
          - string
          - 'null'
          format: date-time
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        resolved_at:
          type:
          - string
          - 'null'
          format: date-time
        closed_at:
          type:
          - string
          - 'null'
          format: date-time
        site_id:
          type:
          - string
          - 'null'
          format: uuid
          description: Linked customer site (the field object chain); null until the ticket is linked.
        site_name:
          type:
          - string
          - 'null'
        customer_id:
          type:
          - string
          - 'null'
          format: uuid
          description: Denormalized from the linked site on link; null until the ticket is linked.
        customer_name:
          type:
          - string
          - 'null'
        work_order_id:
          type:
          - string
          - 'null'
          format: uuid
          description: Work order dispatched for the visit; null until the ticket is linked.
    SyncBatchRequest:
      type: object
      required:
      - sync_id
      - operations
      properties:
        sync_id:
          type: string
          minLength: 1
        operations:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/SyncOperationRequest'
    SyncBatchResponse:
      type: object
      required:
      - sync_id
      - results
      properties:
        sync_id:
          type: string
        results:
          type: array
          items:
            $ref: '#/components/schemas/SyncOperationResult'
    SyncError:
      type: object
      required:
      - code
      - message
      properties:
        code:
          type: string
        message:
          type: string
    SyncOperationKind:
      type: string
      enum:
      - WORK_ORDER_START
      - WORK_ORDER_REPORT
    SyncOperationRequest:
      type: object
      required:
      - request_id
      - operation
      - created_at
      - payload
      properties:
        request_id:
          type: string
          minLength: 1
        operation:
          $ref: '#/components/schemas/SyncOperationKind'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        payload:
          oneOf:
          - $ref: '#/components/schemas/SyncWorkOrderStartPayload'
          - $ref: '#/components/schemas/SyncWorkOrderReportPayload'
    SyncOperationResult:
      type: object
      required:
      - request_id
      - operation
      - status
      - http_status
      - replayed
      properties:
        request_id:
          type: string
        operation:
          $ref: '#/components/schemas/SyncOperationKind'
        status:
          $ref: '#/components/schemas/SyncOperationStatus'
        http_status:
          type: integer
          format: int32
          minimum: 100
          maximum: 599
        result:
          $ref: '#/components/schemas/WorkOrderSummary'
        error:
          $ref: '#/components/schemas/SyncError'
        replayed:
          type: boolean
    SyncOperationStatus:
      type: string
      enum:
      - APPLIED
      - FAILED
    SyncWorkOrderReportPayload:
      type: object
      required:
      - work_order_id
      - result_type
      - diagnosis
      - action_taken
      properties:
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        result_type:
          $ref: '#/components/schemas/WorkResultType'
        diagnosis:
          type: string
        action_taken:
          type: string
    SyncWorkOrderStartPayload:
      type: object
      required:
      - work_order_id
      properties:
        work_order_id:
          $ref: '#/components/schemas/Uuid'
    SystemPolicyRoleResponse:
      type: object
      required:
      - role_key
      - display_name
      - permissions
      - status
      - is_system
      - permissions
      properties:
        role_key:
          type: string
        display_name:
          type: string
        status:
          type: string
        is_system:
          type: boolean
        permissions:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/PolicyPermissionResponse'
    TargetChangeDecision:
      type: string
      enum:
      - APPROVED
      - REJECTED
    TargetChangeRequest:
      type: object
      required:
      - requested_target_due_at
      - reason
      properties:
        requested_target_due_at:
          $ref: '#/components/schemas/Timestamp'
        reason:
          type: string
    TargetChangeRequestSummary:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        work_order_id:
          $ref: '#/components/schemas/Uuid'
        branch_id:
          $ref: '#/components/schemas/Uuid'
        requested_target_due_at:
          $ref: '#/components/schemas/Timestamp'
        status:
          type: string
          enum:
          - REQUESTED
          - APPROVED
          - REJECTED
    Team:
      type: string
      enum:
      - MAINTENANCE
      - PREVENTION
      - MANAGEMENT
      - RECEPTION
    TimeChangeCoverageEvidence:
      type: object
      additionalProperties: false
      required:
      - headcount
      - already_out
      - minimum_on_duty
      - projected_available
      properties:
        headcount:
          type: integer
          format: int32
          minimum: 0
          description: Home-branch employees counted for coverage at decide time.
        already_out:
          type: integer
          format: int32
          minimum: 0
          description: >-
            Distinct other subjects with approved leave overlapping the
            requested span on that branch.
        minimum_on_duty:
          type: integer
          format: int32
          minimum: 1
          description: Smallest non-vacuous on-duty floor used by the automatic gate.
        projected_available:
          type: integer
          format: int64
          description: >-
            headcount − already_out − 1 (the requester). Negative or below
            minimum_on_duty is what makes `time_change` eligible.
    TimeChangeGroundsCode:
      type: string
      enum:
      - branch_coverage_shortfall
      description: >-
        Closed set of system-judged §60⑤ grounds. Free-text manager narrative is
        never a grounds code.
    Timestamp:
      type: string
      format: date-time
    TimestampAuthorityProofView:
      type: object
      description: Nullable/absent until an RFC-3161 TSA lane exists (FUTURE — not faked).
      required:
        - id
        - copy_id
        - status
        - provider
        - hash_algorithm
        - created_by
        - created_at
      properties:
        id:
          type: string
          format: uuid
        copy_id:
          type: string
          format: uuid
        status:
          $ref: '#/components/schemas/TsaProofStatus'
        provider:
          type: string
        policy_oid:
          type:
            - string
            - 'null'
        serial_number:
          type:
            - string
            - 'null'
        hash_algorithm:
          type: string
        message_imprint_sha256:
          type:
            - string
            - 'null'
        generated_at:
          type:
            - string
            - 'null'
          format: date-time
        accuracy_millis:
          type:
            - integer
            - 'null'
          format: int64
        ordering:
          type:
            - boolean
            - 'null'
        tsa_cert_fingerprint_sha256:
          type:
            - string
            - 'null'
        token_digest_sha256:
          type:
            - string
            - 'null'
        token_storage:
          oneOf:
            - {$ref: '#/components/schemas/EvidenceStorageRef'}
            - {type: 'null'}
        verified_at:
          type:
            - string
            - 'null'
          format: date-time
        failure_reason:
          type:
            - string
            - 'null'
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
    TodoPage:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TodoSummary'
    TodoRef:
      type: object
      description: >-
        One scope chip or object link: a reference to a domain object by
        kind + id with an optional display-label snapshot. `kind` is an
        extensible free-form string (frontend object-registry kinds), not an
        enum.
      required:
      - kind
      - id
      properties:
        kind:
          type: string
        id:
          type: string
        label:
          type: string
    TodoSummary:
      type: object
      required:
      - id
      - owner_user_id
      - text
      - scopes
      - links
      - done
      - created_at
      - updated_at
      - done_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        owner_user_id:
          $ref: '#/components/schemas/Uuid'
        text:
          type: string
        scopes:
          type: array
          items:
            $ref: '#/components/schemas/TodoRef'
        links:
          type: array
          items:
            $ref: '#/components/schemas/TodoRef'
        done:
          type: boolean
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        done_at:
          type:
          - string
          - 'null'
          format: date-time
          description: When the todo was first marked done; null while open.
    TokenPairResponse:
      type: object
      required:
        - access_token
        - token_type
        - refresh_expires_at
        - requires_passkey_setup
      properties:
        access_token:
          type: string
        refresh_token:
          type:
            - string
            - 'null'
          description: >-
            The opaque rotating refresh token in the body transport (mobile). It is null in the cookie transport (web), where
            the token is set as an HttpOnly `console_refresh` cookie and must never reach web JS.
        token_type:
          type: string
          enum:
            - Bearer
        refresh_expires_at:
          $ref: '#/components/schemas/Timestamp'
        requires_passkey_setup:
          type: boolean
          description: >-
            True when the refreshed session belongs to an OTP-signed-in user who still has no passkey. Web clients must keep
            routing this session to initial passkey enrollment after a reload; passkey-authenticated sessions return false.
    TransitionLifecycleRequest:
      type: object
      required:
      - toState
      - reason
      properties:
        toState:
          type: string
          minLength: 1
        reason:
          type: string
          minLength: 1
    TransitionTicketRequest:
      type: object
      required:
      - to_status
      properties:
        to_status:
          $ref: '#/components/schemas/SupportTicketStatus'
    TraversalEdge:
      type: object
      required: [id, link_type_id, from_instance_id, to_instance_id]
      properties:
        id: { type: string, format: uuid }
        link_type_id: { type: string, format: uuid }
        from_instance_id: { type: string, format: uuid }
        to_instance_id: { type: string, format: uuid }
    TraversalGraph:
      type: object
      required: [root, nodes, edges]
      properties:
        root: { type: string, format: uuid }
        nodes: { type: array, items: { $ref: '#/components/schemas/TraversalNode' } }
        edges: { type: array, items: { $ref: '#/components/schemas/TraversalEdge' } }
    TraversalNode:
      type: object
      required: [instance_id, object_type_id, title, lifecycle_state, depth]
      properties:
        instance_id: { type: string, format: uuid }
        object_type_id: { type: string, format: uuid }
        title: { type: string }
        lifecycle_state: { $ref: '#/components/schemas/InstanceLifecycleState' }
        depth: { type: integer, format: int32, minimum: 0 }
    TriageFindingRequest:
      type: object
      description: Triage an OPEN finding. A memo is required when status is DISMISSED or ESCALATED, optional for REVIEWED.
        Max 2000 characters.
      required:
      - status
      properties:
        status:
          type: string
          description: Target triage status. OPEN is not a valid target.
          enum:
          - REVIEWED
          - DISMISSED
          - ESCALATED
        memo:
          type:
          - string
          - 'null'
          maxLength: 2000
          description: Reviewer memo. Required when dismissing or escalating.
    TriggerBindingListResponse:
      type: object
      required: [items, registered_event_keys]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TriggerBindingResponse'
        registered_event_keys:
          type: array
          items:
            type: string
    TriggerBindingResponse:
      type: object
      required: [id, definition_id, trigger_type, event_key, subject_kind, enabled, created_at, updated_at]
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        definition_id:
          $ref: '#/components/schemas/Uuid'
        trigger_type:
          type: string
          enum: [OBJECT_EVENT, IMPORT_EVENT, MAIL_EVENT, MESSENGER_EVENT, CALENDAR_EVENT, POLL_EVENT]
        event_key:
          type: string
        subject_kind:
          type:
          - string
          - 'null'
          description: The ontology object kind this rule acts on (dynamics↔ontology); null when unscoped.
        enabled:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    TriggerWorkflowRunRequest:
      type: object
      properties:
        trigger_type:
          type: string
          enum:
          - MANUAL
          - SCHEDULE
          - WEBHOOK
          - SYSTEM
          default: MANUAL
        idempotency_key:
          type:
          - string
          - 'null'
          maxLength: 160
        four_eyes_request_ref:
          type:
          - string
          - 'null'
          format: uuid
          description: >-
            §16 org-scope automation gate (85 판정); ignored for a personal-scope (§3.9.0-①) definition.
    TsaProofStatus:
      { type: string, enum: [MISSING, PENDING, VERIFIED, FAILED, REVOKED, EXPIRED_CA] }
    UnavailableMetric:
      type: object
      required:
      - metric
      - source_domain
      - reason
      properties:
        metric:
          $ref: '#/components/schemas/KpiMetric'
        source_domain:
          type: string
        reason:
          type: string
    UnlockPeriodLockRequest:
      type: object
      required:
      - reason
      properties:
        reason:
          type: string
          minLength: 1
    UnreadNotificationCountResponse:
      type: object
      required:
      - unread
      properties:
        unread:
          type: integer
          format: int64
          description: The number of the caller's unread notifications.
    UpdateBranchRequest:
      type: object
      minProperties: 1
      properties:
        region_id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
          minLength: 1
          maxLength: 120
    UpdateEquipmentRequest:
      type: object
      description: Partial update. Absent keys are left unchanged; nullable keys set to null clear the column.
      properties:
        customer_name:
          type: string
          minLength: 1
        site_name:
          type: string
          minLength: 1
        status:
          $ref: '#/components/schemas/EquipmentStatus'
        specification:
          type: string
          minLength: 1
        ton_text:
          type: string
        management_no:
          type:
          - string
          - 'null'
        power_label:
          type:
          - string
          - 'null'
        manager_name:
          type:
          - string
          - 'null'
        placement_location:
          type:
          - string
          - 'null'
        placement_no:
          type:
          - string
          - 'null'
        operation_shift:
          type:
          - string
          - 'null'
        maker:
          type:
          - string
          - 'null'
        model:
          type:
          - string
          - 'null'
        vin:
          type:
          - string
          - 'null'
        year:
          type:
          - string
          - 'null'
          format: date
        hours:
          type:
          - integer
          - 'null'
          format: int64
        vehicle_registration_no:
          type:
          - string
          - 'null'
        insured:
          type:
          - boolean
          - 'null'
        insurer:
          type:
          - string
          - 'null'
        policy_holder:
          type:
          - string
          - 'null'
        insured_party:
          type:
          - string
          - 'null'
        asset_owner:
          type:
          - string
          - 'null'
        asset_registered_on:
          type:
          - string
          - 'null'
          format: date
        rental_started_on:
          type:
          - string
          - 'null'
          format: date
        rental_fee:
          type:
          - integer
          - 'null'
          format: int64
        vehicle_value:
          type:
          - integer
          - 'null'
          format: int64
        residual_value:
          type:
          - integer
          - 'null'
          format: int64
        acquisition_cost_won:
          description: Acquisition cost in KRW. A distinct accounting fact, never the depreciation base.
          type:
          - integer
          - 'null'
          format: int64
          minimum: 0
        acquisition_date:
          type:
          - string
          - 'null'
          format: date
        note:
          type:
          - string
          - 'null'
    UpdateInquiryStatusRequest:
      type: object
      required:
      - status
      properties:
        status:
          $ref: '#/components/schemas/InquiryStatus'
    UpdateListingRequest:
      type: object
      description: Partial update. Absent keys are left unchanged; nullable keys explicitly set to null clear the column.
        At least one field must be supplied.
      properties:
        kind:
          $ref: '#/components/schemas/ListingKind'
        condition:
          $ref: '#/components/schemas/ListingCondition'
        model_name:
          type: string
        capacity_milli:
          type:
          - integer
          - 'null'
          format: int64
        model_year:
          type:
          - integer
          - 'null'
          format: int32
        usage_hours:
          type:
          - integer
          - 'null'
          format: int32
        price_won:
          type:
          - integer
          - 'null'
          format: int64
        badge:
          type:
          - string
          - 'null'
        usage_label:
          type:
          - string
          - 'null'
        condition_label:
          type:
          - string
          - 'null'
        availability:
          type:
          - string
          - 'null'
        location:
          type:
          - string
          - 'null'
        description:
          type:
          - string
          - 'null'
        listing_type:
          $ref: '#/components/schemas/ListingType'
        status:
          $ref: '#/components/schemas/ListingStatus'
        sort_weight:
          type: integer
          format: int32
        equipment_id:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
    UpdateNoticeDraftRequest:
      type: object
      properties:
        title:
          type: string
          maxLength: 300
        body:
          type: string
          maxLength: 20000
        category:
          $ref: '#/components/schemas/NoticeCategory'
        audience:
          $ref: '#/components/schemas/NoticeAudienceInput'
    UpdateOrgChangeDraftRequest:
      type: object
      additionalProperties: false
      description: Every field is optional; an omitted field is left unchanged.
      properties:
        kind:
          $ref: '#/components/schemas/OrgChangeKind'
        effectiveDate:
          $ref: '#/components/schemas/Date'
        reason:
          type: string
          minLength: 1
          maxLength: 4000
        proposal:
          type: array
          maxItems: 100
          items:
            $ref: '#/components/schemas/OrgProposalOp'
    UpdatePlatformGroupRequest:
      type: object
      properties:
        slug:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        status:
          $ref: '#/components/schemas/PlatformOrgStatus'
    UpdatePlatformOrgRequest:
      type: object
      required:
      - status
      properties:
        status:
          $ref: '#/components/schemas/PlatformOrgStatus'
    UpdatePolicyRoleRequest:
      type: object
      required:
      - display_name
      - permissions
      - step_up
      properties:
        display_name:
          type: string
          minLength: 1
          maxLength: 80
        description:
          type:
          - string
          - 'null'
          maxLength: 512
        permissions:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/PolicyPermissionResponse'
        conditions:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/PolicyConditionResponse'
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    UpdatePolicyRoleStatusRequest:
      type: object
      required:
      - status
      - step_up
      properties:
        status:
          type: string
          description: Requested lifecycle status for read-only impact preview. Allowed transitions are DRAFT→ACTIVE, ACTIVE→DRAFT,
            and ACTIVE→RETIRED; RETIRED is terminal.
          enum:
          - DRAFT
          - ACTIVE
          - RETIRED
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
    UpdateRecruitPostingRequest:
      allOf:
      - $ref: '#/components/schemas/CreateRecruitPostingRequest'
      - type: object
        description: Optimistic concurrency — expected_updated_at is the RFC 3339 updated_at read from the posting; a mismatch
          is 409.
        required:
        - expected_updated_at
        properties:
          expected_updated_at:
            $ref: '#/components/schemas/Timestamp'
    UpdateRegionRequest:
      type: object
      required:
      - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
    UpdateSelfProfileRequest:
      type: object
      properties:
        display_name:
          type: string
        phone:
          type:
          - string
          - 'null'
    UpdateSiteRequest:
      type: object
      description: Partial site update. Absent keys are left unchanged; nullable keys set to null clear the column. Latitude
        and longitude must be supplied together and within WGS84 ranges.
      properties:
        address:
          type:
          - string
          - 'null'
        province:
          type:
          - string
          - 'null'
        city:
          type:
          - string
          - 'null'
        postal_code:
          type:
          - string
          - 'null'
        latitude:
          type:
          - number
          - 'null'
          format: double
          minimum: -90
          maximum: 90
          description: Nullable admin-authored WGS84 latitude in degrees. Include with longitude to set or clear the pair.
        longitude:
          type:
          - number
          - 'null'
          format: double
          minimum: -180
          maximum: 180
          description: Nullable admin-authored WGS84 longitude in degrees. Include with latitude to set or clear the pair.
        geofence_radius_m:
          type:
          - number
          - 'null'
          format: double
          minimum: 0
          maximum: 100000
          description: Per-site geofence radius in metres (> 0, ≤ 100000). Null clears the override so the system default
            (300 m) applies.
        contact_name:
          type:
          - string
          - 'null'
          maxLength: 100
          description: On-site representative contact name (담당자명). Null clears it.
        contact_phone:
          type:
          - string
          - 'null'
          maxLength: 40
          description: On-site contact phone (연락처). Null clears it.
        contact_email:
          type:
          - string
          - 'null'
          maxLength: 320
          description: Optional contact email (이메일). Null clears it.
    UpdateUserRequest:
      type: object
      properties:
        display_name:
          type: string
        employee_id:
          type:
          - string
          - 'null'
          format: uuid
        phone:
          type:
          - string
          - 'null'
        team:
          $ref: '#/components/schemas/Team'
        roles:
          type: array
          items:
            type: string
        branch_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        preview_acknowledged:
          type: boolean
          description: Required when `roles` or `branch_ids` is present; confirms the caller reviewed the policy assignment/account-scope preview.
        preview_receipt_id:
          $ref: '#/components/schemas/Uuid'
    UpdateWorkOrderIntakeRequest:
      type: object
      minProperties: 1
      properties:
        symptom:
          type: string
        customer_request:
          type: string
        maintenance_type:
          $ref: '#/components/schemas/MaintenanceType'
        maintenance_cause:
          $ref: '#/components/schemas/MaintenanceCause'
    UpdateWorkflowDefinitionRequest:
      type: object
      description: Partial update for a DRAFT workflow definition. Workflow key and object type are immutable.
      minProperties: 1
      properties:
        display_name:
          type: string
        definition:
          type: object
          additionalProperties: true
        approval_line:
          type: array
          items:
            type: object
            additionalProperties: true
        payment_line:
          type: array
          items:
            type: object
            additionalProperties: true
        notification_rules:
          type: array
          items:
            type: object
            additionalProperties: true
        action_allowlist:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowActionAllowlistEntry'
        required_approval_line:
          type: boolean
        required_payment_line:
          type: boolean
    UpdateWorkflowScheduleRequest:
      type: object
      properties:
        label:
          type: string
          maxLength: 120
        cron_expr:
          type: string
        timezone:
          type: string
        enabled:
          type: boolean
    UpsertCycleCountLineRequest:
      type: object
      additionalProperties: false
      required:
        - expectedVersion
        - itemId
        - countedQuantityMilli
      properties:
        expectedVersion:
          type: integer
          minimum: 1
        itemId:
          $ref: '#/components/schemas/Uuid'
        countedQuantityMilli:
          type: integer
          format: int64
          minimum: 0
        reason:
          oneOf:
            - {type: string, enum: [DAMAGE, LOSS, MISCOUNT, FOUND, OTHER]}
            - {type: 'null'}
        note:
          type:
            - string
            - 'null'
    UpsertNotificationPolicyRequest:
      type: object
      required:
      - scope
      properties:
        scope:
          type: string
          enum:
          - all
          - category
          - object
        category:
          type: string
          description: Required when scope is category; must be absent otherwise.
        link:
          $ref: '#/components/schemas/NotificationLink'
    UserPage:
      type: object
      required:
      - items
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/UserSummary'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
    UserSummary:
      type: object
      required:
      - id
      - display_name
      - employee_id
      - employee_name
      - employee_number
      - employee_company
      - employee_org_unit
      - employee_position
      - employee_identity_review_required
      - employee_identity_resolution_confidence
      - employee_link_status
      - phone
      - team
      - roles
      - branch_ids
      - is_active
      - has_passkey
      - account_status
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        display_name:
          type: string
        employee_id:
          type:
          - string
          - 'null'
          format: uuid
        employee_name:
          type:
          - string
          - 'null'
        employee_number:
          type:
          - string
          - 'null'
        employee_company:
          type:
          - string
          - 'null'
        employee_org_unit:
          type:
          - string
          - 'null'
        employee_position:
          type:
          - string
          - 'null'
        employee_identity_review_required:
          type:
          - boolean
          - 'null'
        employee_identity_resolution_confidence:
          type:
          - string
          - 'null'
        employee_link_status:
          type: string
          enum:
          - LINKED
          - UNLINKED
        phone:
          type:
          - string
          - 'null'
        team:
          $ref: '#/components/schemas/Team'
        roles:
          type: array
          items:
            type: string
        branch_ids:
          type: array
          items:
            $ref: '#/components/schemas/Uuid'
        is_active:
          type: boolean
        has_passkey:
          type: boolean
          description: Whether the user has at least one enrolled passkey. A user can only sign in once this is true; until
            then the account is pending setup.
        account_status:
          $ref: '#/components/schemas/AccountStatus'
        created_at:
          $ref: '#/components/schemas/Timestamp'
    Uuid:
      type: string
      format: uuid
    VerifyOutcome:
      { type: string, enum: [VERIFIED, MISMATCH, INDETERMINATE] }
    VoidSettlementRequest:
      type: object
      required:
      - reason
      properties:
        reason:
          type: string
          minLength: 1
    VotePollRequest:
      type: object
      required:
      - selected_option_ids
      properties:
        selected_option_ids:
          type: array
          minItems: 1
          maxItems: 20
          items:
            $ref: '#/components/schemas/Uuid'
    VoucherLineInput:
      type: object
      required:
      - account_code
      - side
      - amount_won
      properties:
        account_code:
          type: string
        side:
          $ref: '#/components/schemas/DebitCredit'
        amount_won:
          type: integer
          format: int64
          minimum: 1
        memo:
          type: string
    VoucherLineSummary:
      type: object
      required:
      - id
      - line_no
      - account_code
      - side
      - amount_won
      - memo
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        line_no:
          type: integer
          format: int32
        account_code:
          type: string
        side:
          $ref: '#/components/schemas/DebitCredit'
        amount_won:
          type: integer
          format: int64
        memo:
          type: string
    VoucherStatus:
      type: string
      enum: [DRAFT, BALANCE_CHECKED, APPROVED, POSTED, REVERSED]
    VoucherSummary:
      type: object
      required:
      - id
      - voucher_no
      - branch_id
      - status
      - memo
      - debit_total_won
      - credit_total_won
      - lines
      - created_by
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        voucher_no:
          type: string
        branch_id:
          $ref: '#/components/schemas/Uuid'
        branch_name:
          type:
          - string
          - 'null'
        status:
          $ref: '#/components/schemas/VoucherStatus'
        memo:
          type: string
        source_object_type:
          type:
          - string
          - 'null'
        source_object_id:
          type:
          - string
          - 'null'
        reversal_of_voucher_id:
          type:
          - string
          - 'null'
          format: uuid
        reversed_by_voucher_id:
          type:
          - string
          - 'null'
          format: uuid
        debit_total_won:
          type: integer
          format: int64
        credit_total_won:
          type: integer
          format: int64
        lines:
          type: array
          items:
            $ref: '#/components/schemas/VoucherLineSummary'
        created_by:
          $ref: '#/components/schemas/Uuid'
        created_by_name:
          type:
          - string
          - 'null'
        approved_by:
          type:
          - string
          - 'null'
          format: uuid
        approved_by_name:
          type:
          - string
          - 'null'
        posted_at:
          type:
          - string
          - 'null'
          format: date-time
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    WithdrawRecruitOfferRequest:
      type: object
      required:
      - reason
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 300
    WorkDiaryActionEntry:
      type: object
      required:
      - site_name
      - management_no
      - diagnosis
      - action_taken
      properties:
        site_name:
          type: string
        management_no:
          type: string
        diagnosis:
          type: string
        action_taken:
          type: string
    WorkDiaryBody:
      type: object
      required:
      - previous_results
      - today_plans
      - urgent_actions
      - source_notes
      properties:
        previous_results:
          type: string
        today_plans:
          type: string
        urgent_actions:
          type: array
          items:
            $ref: '#/components/schemas/WorkDiaryActionEntry'
        source_notes:
          type: array
          items:
            $ref: '#/components/schemas/ExportSourceNote'
    WorkDiaryDraft:
      type: object
      required:
      - id
      - date
      - status
      - body
      - confirmed_by
      - confirmed_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        date:
          $ref: '#/components/schemas/Date'
        status:
          $ref: '#/components/schemas/WorkDiaryStatus'
        body:
          $ref: '#/components/schemas/WorkDiaryBody'
        confirmed_by:
          type:
          - string
          - 'null'
          format: uuid
        confirmed_at:
          type:
          - string
          - 'null'
          format: date-time
    WorkDiaryStatus:
      type: string
      enum:
      - DRAFT
      - CONFIRMED
    WorkDiaryUpdateRequest:
      type: object
      required:
      - body
      properties:
        body:
          $ref: '#/components/schemas/WorkDiaryBody'
    WorkOrderDetail:
      allOf:
      - $ref: '#/components/schemas/WorkOrderListItem'
      - type: object
        required:
        - symptom
        - customer_request
        - delay_reason
        - delay_note
        - diagnosis
        - action_taken
        - report_submitted_by
        - report_submitted_at
        - kpi_excluded
        - evidence_verified
        - approval_line
        - status_history
        - evidence
        - settlement
        properties:
          symptom:
            type: string
          customer_request:
            type:
            - string
            - 'null'
          delay_reason:
            type:
            - string
            - 'null'
          delay_note:
            type:
            - string
            - 'null'
          diagnosis:
            type:
            - string
            - 'null'
          action_taken:
            type:
            - string
            - 'null'
          report_submitted_by:
            type:
            - string
            - 'null'
            format: uuid
          report_submitted_at:
            type:
            - string
            - 'null'
            format: date-time
          kpi_excluded:
            type: boolean
          evidence_verified:
            type: boolean
          approval_line:
            type: array
            items:
              $ref: '#/components/schemas/ApprovalStepSummary'
          status_history:
            type: array
            items:
              $ref: '#/components/schemas/StatusHistorySummary'
          evidence:
            type: array
            items:
              $ref: '#/components/schemas/EvidenceSummary'
          settlement:
            description: The live (non-VOID) cost settlement of this order, if one exists.
            oneOf:
            - type: 'null'
            - $ref: '#/components/schemas/SettlementSummary'
    WorkOrderFacetBucket:
      type: object
      required:
      - value
      - count
      - filters
      properties:
        value:
          type: string
        count:
          type: integer
          format: int64
        filters:
          type: object
          additionalProperties:
            type: string
    WorkOrderHistogramBucket:
      type: object
      required:
      - bucket
      - count
      - filters
      properties:
        bucket:
          type: string
          format: date
        count:
          type: integer
          format: int64
        filters:
          type: object
          additionalProperties:
            type: string
    WorkOrderLensAggregates:
      type: object
      required:
      - total_count
      - p1_count
      - overdue_open_count
      - unassigned_count
      - preventive_on_time_rate
      - mttr_minutes
      properties:
        total_count:
          type: integer
          format: int64
        p1_count:
          type: integer
          format: int64
        overdue_open_count:
          type: integer
          format: int64
        unassigned_count:
          type: integer
          format: int64
        preventive_on_time_rate:
          type:
          - number
          - 'null'
          format: double
          description: Share (0..1) of closed preventive orders that reached FINAL_COMPLETED no later than their target due
            date; null when the filtered set has no basis.
        mttr_minutes:
          type:
          - number
          - 'null'
          format: double
          description: Mean minutes from the first IN_PROGRESS transition to the first REPORT_SUBMITTED transition (OT-13
            MTTR); null when no order has completed that span.
    WorkOrderLensFacets:
      type: object
      required:
      - status
      - priority
      - maintenance_type
      - maintenance_cause
      properties:
        status:
          type: array
          items:
            $ref: '#/components/schemas/WorkOrderFacetBucket'
        priority:
          type: array
          items:
            $ref: '#/components/schemas/WorkOrderFacetBucket'
        maintenance_type:
          type: array
          items:
            $ref: '#/components/schemas/WorkOrderFacetBucket'
        maintenance_cause:
          type: array
          items:
            $ref: '#/components/schemas/WorkOrderFacetBucket'
    WorkOrderLensHistograms:
      type: object
      required:
      - target_due_date
      properties:
        target_due_date:
          type: array
          items:
            $ref: '#/components/schemas/WorkOrderHistogramBucket'
    WorkOrderLensListograms:
      type: object
      required:
      - customers
      - sites
      properties:
        customers:
          type: array
          items:
            $ref: '#/components/schemas/WorkOrderNamedBucket'
        sites:
          type: array
          items:
            $ref: '#/components/schemas/WorkOrderNamedBucket'
    WorkOrderListItem:
      type: object
      required:
      - id
      - request_no
      - branch_id
      - status
      - priority
      - result_type
      - maintenance_type
      - maintenance_cause
      - target_due_at
      - created_at
      - updated_at
      - equipment
      - customer
      - site
      - site_contact
      - assignments
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        request_no:
          type: string
          pattern: ^[0-9]{8}-[0-9]{3}$
        branch_id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/WorkOrderStatus'
        priority:
          $ref: '#/components/schemas/PriorityLevel'
        result_type:
          $ref: '#/components/schemas/WorkResultType'
        maintenance_type:
          oneOf:
          - type: 'null'
          - $ref: '#/components/schemas/MaintenanceType'
        maintenance_cause:
          oneOf:
          - type: 'null'
          - $ref: '#/components/schemas/MaintenanceCause'
        target_due_at:
          type:
          - string
          - 'null'
          format: date-time
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        equipment:
          $ref: '#/components/schemas/EquipmentSummary'
        customer:
          $ref: '#/components/schemas/NamedEntity'
        site:
          $ref: '#/components/schemas/NamedEntity'
        site_contact:
          description: 사업장 대표 담당자 연락처 (#13). 등록된 연락처가 없으면 null.
          oneOf:
          - type: 'null'
          - $ref: '#/components/schemas/SiteContact'
        assignments:
          type: array
          items:
            $ref: '#/components/schemas/AssignmentSummary'
    WorkOrderListPage:
      type: object
      required:
      - items
      - limit
      - offset
      - total
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkOrderListItem'
        limit:
          type: integer
          format: int64
        offset:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
        lens:
          $ref: '#/components/schemas/WorkOrderObjectSetLens'
    WorkOrderNamedBucket:
      type: object
      required:
      - id
      - name
      - count
      - filters
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        name:
          type: string
        count:
          type: integer
          format: int64
        filters:
          type: object
          additionalProperties:
            type: string
    WorkOrderObjectSetLens:
      type: object
      required:
      - object_type
      - aggregates
      - facets
      - histograms
      - listograms
      properties:
        object_type:
          type: string
          enum:
          - work_order
        aggregates:
          $ref: '#/components/schemas/WorkOrderLensAggregates'
        facets:
          $ref: '#/components/schemas/WorkOrderLensFacets'
        histograms:
          $ref: '#/components/schemas/WorkOrderLensHistograms'
        listograms:
          $ref: '#/components/schemas/WorkOrderLensListograms'
    WorkOrderStatus:
      type: string
      enum:
      - RECEIVED
      - UNASSIGNED
      - ASSIGNED
      - IN_PROGRESS
      - REPORT_SUBMITTED
      - ADMIN_REVIEW
      - FINAL_COMPLETED
      - REJECTED
      - ON_HOLD
      - DELAYED
      - TEMPORARY_ACTION
      - PART_WAITING
      - EQUIPMENT_IN_USE
      - REVISIT_REQUIRED
      - ARCHIVED
      - CANCELLED
    WorkOrderSummary:
      type: object
      required:
      - id
      - request_no
      - branch_id
      - equipment_id
      - customer_id
      - site_id
      - status
      - priority
      - result_type
      - maintenance_type
      - maintenance_cause
      - evidence_verified
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        request_no:
          type: string
          pattern: ^[0-9]{8}-[0-9]{3}$
        branch_id:
          $ref: '#/components/schemas/Uuid'
        equipment_id:
          $ref: '#/components/schemas/Uuid'
        customer_id:
          $ref: '#/components/schemas/Uuid'
        site_id:
          $ref: '#/components/schemas/Uuid'
        status:
          $ref: '#/components/schemas/WorkOrderStatus'
        priority:
          $ref: '#/components/schemas/PriorityLevel'
        result_type:
          $ref: '#/components/schemas/WorkResultType'
        maintenance_type:
          oneOf:
          - type: 'null'
          - $ref: '#/components/schemas/MaintenanceType'
        maintenance_cause:
          oneOf:
          - type: 'null'
          - $ref: '#/components/schemas/MaintenanceCause'
        evidence_verified:
          type: boolean
    WorkResultType:
      type: string
      enum:
      - COMPLETED
      - TEMPORARY_ACTION
      - INCOMPLETE
      - REVISIT_REQUIRED
      - UNKNOWN
    WorkbenchActionInboxItem:
      type: object
      additionalProperties: false
      required:
      - id
      - urgency
      - title
      - source
      - target
      properties:
        id:
          type: string
        urgency:
          $ref: '#/components/schemas/WorkbenchUrgency'
        title:
          type: string
        due_at:
          $ref: '#/components/schemas/Timestamp'
        source:
          $ref: '#/components/schemas/WorkbenchSourceRef'
        target:
          $ref: '#/components/schemas/WorkbenchTarget'
    WorkbenchActionSourceEnvelope:
      oneOf:
      - $ref: '#/components/schemas/WorkbenchActionSourceOk'
      - $ref: '#/components/schemas/WorkbenchDeniedSourceEnvelope'
      - $ref: '#/components/schemas/WorkbenchUnavailableSourceEnvelope'
      discriminator:
        propertyName: status
        mapping:
          ok: '#/components/schemas/WorkbenchActionSourceOk'
          denied: '#/components/schemas/WorkbenchDeniedSourceEnvelope'
          unavailable: '#/components/schemas/WorkbenchUnavailableSourceEnvelope'
    WorkbenchActionSourceOk:
      type: object
      description: >-
        Exact, complete action-inbox snapshot admitted at the request ceiling.
        The server follows the public immutable cursor contract in pages of at
        most 200 items and fails this source closed rather than returning a
        priority-ranked prefix when the exact set exceeds its 1000-item budget,
        drifts, repeats a cursor, or repeats an item id.
      additionalProperties: false
      required:
      - status
      - as_of
      - items
      - total
      - truncated
      properties:
        status:
          type: string
          enum:
          - ok
        as_of:
          $ref: '#/components/schemas/Timestamp'
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkbenchActionInboxItem'
        total:
          type: integer
          minimum: 0
        truncated:
          type: boolean
    WorkbenchCalendarItem:
      type: object
      additionalProperties: false
      required:
      - id
      - title
      - starts_at
      - ends_at
      - target
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        title:
          type: string
        starts_at:
          $ref: '#/components/schemas/Timestamp'
        ends_at:
          $ref: '#/components/schemas/Timestamp'
        target:
          $ref: '#/components/schemas/WorkbenchTarget'
    WorkbenchCalendarSourceEnvelope:
      oneOf:
      - $ref: '#/components/schemas/WorkbenchCalendarSourceOk'
      - $ref: '#/components/schemas/WorkbenchDeniedSourceEnvelope'
      - $ref: '#/components/schemas/WorkbenchUnavailableSourceEnvelope'
      discriminator:
        propertyName: status
        mapping:
          ok: '#/components/schemas/WorkbenchCalendarSourceOk'
          denied: '#/components/schemas/WorkbenchDeniedSourceEnvelope'
          unavailable: '#/components/schemas/WorkbenchUnavailableSourceEnvelope'
    WorkbenchCalendarSourceOk:
      type: object
      additionalProperties: false
      required:
      - status
      - as_of
      - items
      - total
      - truncated
      properties:
        status:
          type: string
          enum:
          - ok
        as_of:
          $ref: '#/components/schemas/Timestamp'
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkbenchCalendarItem'
        total:
          type: integer
          minimum: 0
        truncated:
          type: boolean
    WorkbenchDeniedSourceEnvelope:
      type: object
      additionalProperties: false
      required:
      - status
      - code
      properties:
        status:
          type: string
          enum:
          - denied
        code:
          type: string
    WorkbenchEffectiveScope:
      oneOf:
      - $ref: '#/components/schemas/WorkbenchScopeAll'
      - $ref: '#/components/schemas/WorkbenchScopeBranches'
      discriminator:
        propertyName: kind
        mapping:
          all: '#/components/schemas/WorkbenchScopeAll'
          branches: '#/components/schemas/WorkbenchScopeBranches'
    WorkbenchRange:
      type: object
      additionalProperties: false
      required:
      - from
      - to
      properties:
        from:
          $ref: '#/components/schemas/Timestamp'
        to:
          $ref: '#/components/schemas/Timestamp'
    WorkbenchScopeAll:
      type: object
      additionalProperties: false
      required:
      - kind
      properties:
        kind:
          type: string
          enum:
          - all
        selected_branch_id:
          $ref: '#/components/schemas/Uuid'
    WorkbenchScopeBranches:
      type: object
      additionalProperties: false
      required:
      - kind
      - branch_ids
      properties:
        kind:
          type: string
          enum:
          - branches
        branch_ids:
          type: array
          uniqueItems: true
          items:
            $ref: '#/components/schemas/Uuid'
        selected_branch_id:
          $ref: '#/components/schemas/Uuid'
    WorkbenchSourceRef:
      type: object
      additionalProperties: false
      required:
      - kind
      - id
      properties:
        kind:
          type: string
        id:
          $ref: '#/components/schemas/Uuid'
    WorkbenchTarget:
      type: object
      additionalProperties: false
      description: Server-issued bounded module target; never an arbitrary URL.
      required:
      - module
      - id
      properties:
        module:
          type: string
        id:
          type: string
    WorkbenchTodoItem:
      type: object
      additionalProperties: false
      required:
      - id
      - text
      - done
      - source_order
      - target
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        text:
          type: string
        done:
          type: boolean
        source_order:
          type: integer
          format: int64
          minimum: 0
        target:
          $ref: '#/components/schemas/WorkbenchTarget'
    WorkbenchTodoSourceEnvelope:
      oneOf:
      - $ref: '#/components/schemas/WorkbenchTodoSourceOk'
      - $ref: '#/components/schemas/WorkbenchDeniedSourceEnvelope'
      - $ref: '#/components/schemas/WorkbenchUnavailableSourceEnvelope'
      discriminator:
        propertyName: status
        mapping:
          ok: '#/components/schemas/WorkbenchTodoSourceOk'
          denied: '#/components/schemas/WorkbenchDeniedSourceEnvelope'
          unavailable: '#/components/schemas/WorkbenchUnavailableSourceEnvelope'
    WorkbenchTodoSourceOk:
      type: object
      additionalProperties: false
      required:
      - status
      - as_of
      - items
      - total
      - truncated
      properties:
        status:
          type: string
          enum:
          - ok
        as_of:
          $ref: '#/components/schemas/Timestamp'
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkbenchTodoItem'
        total:
          type: integer
          minimum: 0
        truncated:
          type: boolean
    WorkbenchUnavailableSourceEnvelope:
      type: object
      additionalProperties: false
      required:
      - status
      - code
      properties:
        status:
          type: string
          enum:
          - unavailable
        code:
          type: string
    WorkbenchUrgency:
      type: string
      enum:
      - now
      - today
      - wait
    WorkflowActionAllowlistEntry:
      type: object
      required:
      - connector_key
      - action_key
      properties:
        connector_key:
          type: string
        action_key:
          type: string
    WorkflowConnectorDescriptor:
      type: object
      required:
      - connector_key
      - display_name
      - action_keys
      properties:
        connector_key:
          type: string
        display_name:
          type: string
        action_keys:
          type: array
          items:
            type: string
    WorkflowDefinitionEventResponse:
      type: object
      required:
      - id
      - definition_id
      - version
      - status
      - action
      - actor_display_name
      - summary
      - created_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        definition_id:
          $ref: '#/components/schemas/Uuid'
        version:
          type:
          - integer
          - 'null'
          format: int32
        status:
          type: string
        action:
          type: string
        actor_display_name:
          type:
          - string
          - 'null'
        summary:
          type: string
        created_at:
          type: string
          format: date-time
    WorkflowDefinitionHistoryResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowDefinitionEventResponse'
    WorkflowDefinitionListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowDefinitionResponse'
    WorkflowDefinitionResponse:
      type: object
      required:
      - id
      - workflow_key
      - display_name
      - object_type
      - status
      - latest_version
      - active_version
      - definition
      - approval_line
      - payment_line
      - notification_rules
      - action_allowlist
      - required_approval_line
      - required_payment_line
      - pending_version
      - pending_staged_by
      - object_kinds
      - pending_version
      - pending_staged_by
      - created_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        workflow_key:
          type: string
          example: work_order.completion_review
        display_name:
          type: string
        object_type:
          type: string
        status:
          type: string
          enum:
          - DRAFT
          - ACTIVE
          - PAUSED
          - RETIRED
        latest_version:
          type: integer
          format: int32
        active_version:
          type:
          - integer
          - 'null'
          format: int32
        definition:
          type: object
          additionalProperties: true
        approval_line:
          type: array
          items:
            type: object
            additionalProperties: true
        payment_line:
          type: array
          items:
            type: object
            additionalProperties: true
        notification_rules:
          type: array
          items:
            type: object
            additionalProperties: true
        action_allowlist:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowActionAllowlistEntry'
        required_approval_line:
          type: boolean
        required_payment_line:
          type: boolean
        object_kinds:
          type: array
          description: Ontology object kinds this definition's nodes touch (dynamics↔ontology).
          items:
            type: string
        pending_version:
          type:
          - integer
          - 'null'
          format: int32
          description: A staged revision (version) awaiting four-eyes approval; null when none is pending.
        pending_staged_by:
          oneOf:
          - $ref: '#/components/schemas/Uuid'
          - type: 'null'
          description: Who staged the pending revision (the actor barred from self-approving it).
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    WorkflowObjectKind:
      type: string
      enum:
      - work_order
      - support_ticket
    WorkflowObjectSubject:
      type: object
      additionalProperties: false
      required:
      - object_type
      - object_id
      properties:
        object_type:
          $ref: '#/components/schemas/WorkflowObjectKind'
        object_id:
          $ref: '#/components/schemas/Uuid'
    WorkflowRunDetailResponse:
      type: object
      required:
      - run
      - waiting_tasks
      - timeline
      properties:
        run:
          $ref: '#/components/schemas/WorkflowRunDetailRun'
        waiting_tasks:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowTaskSummary'
        timeline:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowRunTimelineStep'
    WorkflowRunDetailRun:
      type: object
      required:
      - id
      - status
      - definition_id
      - definition_version
      - trigger_type
      - started_at
      - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
        definition_id:
          $ref: '#/components/schemas/Uuid'
        definition_version:
          type: integer
          format: int32
        trigger_type:
          type: string
        object_type:
          type: string
        object_id:
          $ref: '#/components/schemas/Uuid'
        initiated_by:
          $ref: '#/components/schemas/Uuid'
        error_payload:
          type: object
          additionalProperties: true
          description: Failure reason for FAILED/DEAD_LETTERED runs (dead-letter visibility).
        started_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        completed_at:
          $ref: '#/components/schemas/Timestamp'
        failed_at:
          $ref: '#/components/schemas/Timestamp'
    WorkflowRunDetailTarget:
      type: object
      additionalProperties: false
      required:
      - kind
      - run_id
      properties:
        kind:
          type: string
          enum:
          - workflow_run_detail
        run_id:
          $ref: '#/components/schemas/Uuid'
    WorkflowRunForObjectSummary:
      type: object
      additionalProperties: false
      required:
      - run_id
      - definition_id
      - definition_version
      - status
      - trigger_type
      - object_type
      - object_id
      - started_at
      - updated_at
      - detail_target
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        definition_id:
          $ref: '#/components/schemas/Uuid'
        definition_version:
          type: integer
          format: int32
        status:
          type: string
        trigger_type:
          type: string
        object_type:
          $ref: '#/components/schemas/WorkflowObjectKind'
        object_id:
          $ref: '#/components/schemas/Uuid'
        started_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        completed_at:
          $ref: '#/components/schemas/Timestamp'
        detail_target:
          $ref: '#/components/schemas/WorkflowRunDetailTarget'
    WorkflowRunListItem:
      type: object
      required:
      - run_id
      - status
      - definition_id
      - definition_version
      - started_at
      - updated_at
      properties:
        run_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
        definition_id:
          $ref: '#/components/schemas/Uuid'
        definition_version:
          type: integer
          format: int32
        object_type:
          type: string
        object_id:
          $ref: '#/components/schemas/Uuid'
        initiated_by:
          $ref: '#/components/schemas/Uuid'
        started_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
    WorkflowRunListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowRunListItem'
    WorkflowRunLogResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowRunResponse'
    WorkflowRunResponse:
      type: object
      required:
      - id
      - code
      - definition_id
      - definition_version
      - trigger_type
      - status
      - actor_display_name
      - summary
      - error_message
      - generated_objects
      - started_at
      - updated_at
      - completed_at
      - failed_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        code:
          type: string
          example: RUN-9AF31C
        definition_id:
          $ref: '#/components/schemas/Uuid'
        definition_version:
          type: integer
          format: int32
        trigger_type:
          type: string
          enum:
          - MANUAL
          - SCHEDULE
          - WEBHOOK
          - SYSTEM
        status:
          type: string
          enum:
          - STARTING
          - RUNNING
          - WAITING
          - SUCCEEDED
          - FAILED
          - CANCELLED
        actor_display_name:
          type:
          - string
          - 'null'
        summary:
          type: string
        error_message:
          type:
          - string
          - 'null'
        generated_objects:
          type: array
          items:
            type: string
        started_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        completed_at:
          type:
          - string
          - 'null'
          format: date-time
        failed_at:
          type:
          - string
          - 'null'
          format: date-time
    WorkflowRunSummary:
      type: object
      required:
      - id
      - status
      - definition_id
      - definition_version
      - started_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
        definition_id:
          $ref: '#/components/schemas/Uuid'
        definition_version:
          type: integer
          format: int32
        object_type:
          type: string
        object_id:
          $ref: '#/components/schemas/Uuid'
        initiated_by:
          $ref: '#/components/schemas/Uuid'
        started_at:
          $ref: '#/components/schemas/Timestamp'
    WorkflowRunTimelineStep:
      type: object
      required:
      - node_key
      - node_type
      - status
      - attempt
      properties:
        node_key:
          type: string
        node_type:
          type: string
        status:
          type: string
        attempt:
          type: integer
          format: int32
        started_at:
          $ref: '#/components/schemas/Timestamp'
        finished_at:
          $ref: '#/components/schemas/Timestamp'
        actor:
          $ref: '#/components/schemas/Uuid'
          description: The user who decided this node (decision nodes only).
        outcome:
          type: object
          additionalProperties: true
          description: The decision payload recorded on the node's waiting task, if any.
        error:
          type: object
          additionalProperties: true
          description: The node's error payload for a FAILED node step.
    WorkflowRunsForObjectResponse:
      type: object
      additionalProperties: false
      required:
      - subject
      - as_of
      - items
      properties:
        subject:
          $ref: '#/components/schemas/WorkflowObjectSubject'
        as_of:
          $ref: '#/components/schemas/Timestamp'
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowRunForObjectSummary'
        next_before:
          $ref: '#/components/schemas/Uuid'
    WorkflowScheduleListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowScheduleResponse'
    WorkflowScheduleResponse:
      type: object
      required:
        - id
        - label
        - cron_expr
        - timezone
        - definition_id
        - enabled
        - created_at
        - updated_at
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        label:
          type: string
        cron_expr:
          type: string
        timezone:
          type: string
        definition_id:
          $ref: '#/components/schemas/Uuid'
        enabled:
          type: boolean
        next_run_at:
          type:
            - string
            - 'null'
          format: date-time
        last_run_at:
          type:
            - string
            - 'null'
          format: date-time
        last_status:
          oneOf:
            - {type: string, enum: [STARTED, SKIPPED, FAILED]}
            - {type: 'null'}
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    WorkflowSimulationFinding:
      type: object
      required:
      - severity
      - code
      - message
      properties:
        severity:
          type: string
          enum:
          - info
          - warning
          - blocker
        code:
          type: string
        message:
          type: string
    WorkflowSimulationResponse:
      type: object
      required:
      - decision
      - findings
      properties:
        decision:
          type: string
          enum:
          - ready
          - blocked
        findings:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowSimulationFinding'
        simulated_path:
          type: array
          description: For a wf.exec.v1 definition, the ordered node keys that would execute for the sample context
            (the branch actually taken), stopping at the first human task or terminal node. Absent for non-executable definitions.
          items:
            type: string
    WorkflowStepUpRequest:
      type: object
      description: Fresh passkey step-up assertion for a sensitive Workflow Studio mutation.
      required:
      - step_up
      properties:
        step_up:
          $ref: '#/components/schemas/PasskeyStepUpAssertion'
        four_eyes_request_ref:
          type:
          - string
          - 'null'
          format: uuid
          description: >-
            §16 org-scope automation gate (85 판정). Required to publish (direct-activate) an org-scope definition;
            a personal-scope definition (§3.9.0-①) ignores this and publishes directly. References a gov_approvals
            request_ref recorded by a distinct approver.
    WorkflowStudioCatalogResponse:
      type: object
      required:
      - connectors
      - templates
      properties:
        connectors:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowConnectorDescriptor'
        templates:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowTemplateDescriptor'
    WorkflowTaskListResponse:
      type: object
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowTaskSummary'
    WorkflowTaskSummary:
      type: object
      required:
      - task_id
      - run_id
      - waiting_key
      - title
      - status
      - form_payload
      properties:
        task_id:
          $ref: '#/components/schemas/Uuid'
        run_id:
          $ref: '#/components/schemas/Uuid'
        waiting_key:
          type: string
        title:
          type: string
        assignee_role_key:
          type: string
        required_policy:
          type: string
        object_type:
          type: string
        object_id:
          $ref: '#/components/schemas/Uuid'
        status:
          type: string
        claimed_by:
          $ref: '#/components/schemas/Uuid'
        due_at:
          $ref: '#/components/schemas/Timestamp'
        form_payload:
          type: object
          additionalProperties: true
    WorkflowTemplateDescriptor:
      type: object
      required:
      - template_key
      - display_name
      - object_type
      - required_approval_line
      - required_payment_line
      properties:
        template_key:
          type: string
        display_name:
          type: string
        object_type:
          type: string
        required_approval_line:
          type: boolean
        required_payment_line:
          type: boolean
    WorkspaceResponse:
      type: object
      properties:
        layout:
          type: object
          additionalProperties: true
          description: >-
            Opaque, frontend-owned console layout object. Stored and returned
            verbatim; empty default is `{}`.
      required:
      - layout
    WorkspaceUpsertRequest:
      type: object
      properties:
        layout:
          type: object
          additionalProperties: true
          description: Opaque, frontend-owned console layout object to store verbatim.
      required:
      - layout
    WormReplicaStatus:
      type: string
      enum:
      - PENDING
      - VERIFIED
      - FAILED
    WormStorageStatus:
      { type: string, enum: [PENDING, VERIFIED, FAILED] }
