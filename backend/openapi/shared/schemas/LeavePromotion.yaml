type: object
required:
  - id
  - round_no
  - reference_year
  - threshold_days
  - status
  - created_by
  - target_count
  - confirmed_count
properties:
  id:
    type: string
    format: uuid
  round_no:
    type: integer
  reference_year:
    type: integer
  threshold_days:
    type: string
  status:
    type: string
    enum:
      - DRAFT
      - SENT
      - COMPLETED
  note:
    type:
      - string
      - 'null'
  created_by:
    type: string
    format: uuid
  sent_at:
    type:
      - string
      - 'null'
    format: date-time
  target_count:
    type: integer
  confirmed_count:
    type: integer
