      type: object
      required: [instance, revision]
      properties:
        instance: { $ref: '#/components/schemas/InstanceHead' }
        revision: { $ref: '#/components/schemas/RevisionSummary' }
