      type: object
      required: [employee_id]
      properties:
        employee_id: { type: string, format: uuid }
        leave_accrued: { type: string, nullable: true }
        leave_used: { type: string, nullable: true }
        leave_remaining: { type: string, nullable: true }
