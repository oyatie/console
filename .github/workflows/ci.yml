name: CI

on:
  push:
    branches:
      - main
    tags:
      - "v*"
    # GitHub Actions does not evaluate `paths` for tag pushes; release tags
    # therefore run the full ADR-0009/0012 CI gate regardless of changed files.
    paths:
      - "backend/**"
      - "backend/openapi/**"
      - "clients/**"
      - "web/**"
      - "e2e/**"
      - "playwright.config.ts"
      - "android/**"
      - "ios/**"
      - "deploy/**"
      - "docs/decisions/**"
      - "docs/program/**"
      - "docs/CI-GATES.md"
      - "docs/benchmarks/**"
      - "docs/specs/**"
      - "docs/ENTERPRISE-READINESS.md"
      - "docs/GO-LIVE-CHECKLIST.md"
      - "docs/i18n.md"
      - "docs/parity-checklist.md"
      - "docs/release/**"
      - "ops/**"
      - "scripts/**"
      - "tools/buck/**"
      - "tools/buck2"
      - "toolchains/**"
      - "third-party/rust/**"
      - "BUCK"
      - ".buckconfig"
      - "fastlane/**"
      - "Gemfile"
      - "Gemfile.lock"
      - "package.json"
      - "package-lock.json"
      - "openapitools.json"
      - "tsconfig.json"
      - ".github/workflows/*.yml"
  pull_request:
    paths:
      - "backend/**"
      - "backend/openapi/**"
      - "clients/**"
      - "web/**"
      - "e2e/**"
      - "playwright.config.ts"
      - "android/**"
      - "ios/**"
      - "deploy/**"
      - "docs/decisions/**"
      - "docs/program/**"
      - "docs/CI-GATES.md"
      - "docs/benchmarks/**"
      - "docs/specs/**"
      - "docs/ENTERPRISE-READINESS.md"
      - "docs/GO-LIVE-CHECKLIST.md"
      - "docs/i18n.md"
      - "docs/parity-checklist.md"
      - "docs/release/**"
      - "ops/**"
      - "scripts/**"
      - "tools/buck/**"
      - "tools/buck2"
      - "toolchains/**"
      - "third-party/rust/**"
      - "BUCK"
      - ".buckconfig"
      - "fastlane/**"
      - "Gemfile"
      - "Gemfile.lock"
      - "package.json"
      - "package-lock.json"
      - "openapitools.json"
      - "tsconfig.json"
      - ".github/workflows/*.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  # Cancel superseded runs only for PRs. On main, every push must run CI to
  # completion: image-release is triggered by this completed successful CI run
  # for the exact SHA, so a rapid merge train must not cancel main CI.
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  preflight:
    name: CI preflight — foundation and lockfile integrity
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Derive exact console C/T/M train
        if: ${{ github.event_name == 'pull_request' }}
        shell: bash
        run: |
          set -euo pipefail
          CONSOLE_SYNTHETIC_MERGE_SHA="$(git rev-parse "$GITHUB_SHA^{commit}")"
          test "$(git rev-parse HEAD)" = "$CONSOLE_SYNTHETIC_MERGE_SHA"
          CONSOLE_AUTHORITY_TIP_SHA="$(git rev-parse "$CONSOLE_SYNTHETIC_MERGE_SHA^2")"
          CONSOLE_CANDIDATE_SHA="$(git rev-parse "$CONSOLE_AUTHORITY_TIP_SHA^")"
          {
            printf 'CONSOLE_CANDIDATE_SHA=%s\n' "$CONSOLE_CANDIDATE_SHA"
            printf 'CONSOLE_AUTHORITY_TIP_SHA=%s\n' "$CONSOLE_AUTHORITY_TIP_SHA"
            printf 'CONSOLE_SYNTHETIC_MERGE_SHA=%s\n' "$CONSOLE_SYNTHETIC_MERGE_SHA"
          } >> "$GITHUB_ENV"

      - name: Install pinned DotSlash runtime
        run: tools/buck/install_dotslash.sh

      - name: Install Rust toolchain for Cargo.lock consistency
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: "1.97.1"

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24.16.0"
          cache: npm

      - name: Install workspace dependencies
        run: npm ci

      - name: Cheap Buck2 generated-face admission
        run: tools/buck/preflight.sh

      - name: Foundation gate contract
        run: npm run check:foundation-gates

      - name: Console truth-ledger exact-M admission
        if: ${{ github.event_name == 'pull_request' }}
        run: npm run check:console-truth-ledger

      - name: Console fanout planner exact-M admission
        if: ${{ github.event_name == 'pull_request' }}
        run: node scripts/console/plan-fanout.mjs --candidate "$CONSOLE_CANDIDATE_SHA" --authority-tip "$CONSOLE_AUTHORITY_TIP_SHA" --synthetic-merge "$CONSOLE_SYNTHETIC_MERGE_SHA"

      - name: CI preflight contract tests
        run: node --test scripts/check-ci-preflight.test.mjs

      - name: Console route inventory regression
        run: node --test scripts/console/route-inventory.test.mjs

      - name: Console authority-train regression
        run: node --test scripts/console/verify-console-authority-train.test.mjs

      - name: Console truth-ledger validator exact-M regression
        run: node --test scripts/console/validate-console-truth-ledger.test.mjs

      - name: Console fanout planner exact-M regression
        run: node --test scripts/console/plan-fanout.test.mjs

      - name: Buck PostgreSQL environment wrapper regression
        run: tools/buck/run_test_with_postgres_env.test.sh

      - name: Buck disposable PostgreSQL harness regression
        run: tools/buck/test_needs_postgres.test.sh

      - name: CI preflight contract
        run: npm run check:ci-preflight

      - name: Root npm workspace integrity
        run: npm run check:root-workspaces

      - name: Root npm workspace integrity tests
        run: npm run test:root-workspaces

      - name: Canonical npm lockfile
        run: npm run check:package-lock

      - name: Cargo.lock consistency
        run: cargo metadata --manifest-path backend/Cargo.toml --locked --format-version=1 >/dev/null

  support-domain-unit:
    name: Support domain — Buck2 unit reachability
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Install pinned DotSlash runtime
        run: tools/buck/install_dotslash.sh

      - name: Free runner disk for support Buck2 unit
        uses: ./.github/actions/free-runner-disk

      - name: Install Rust toolchain (pinned via rust-toolchain.toml)
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: "1.97.1"

      - name: Cache Rust dependencies + build artifacts
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: backend

      - name: Support domain unit target
        run: tools/buck2 test //backend/crates/support/domain:mnt-support-domain-unit

  postgres-domain-reachability:
    name: Dispatch and attendance — disposable PostgreSQL reachability
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Install pinned DotSlash runtime
        run: tools/buck/install_dotslash.sh

      - name: Free runner disk for PostgreSQL Buck2 tests
        uses: ./.github/actions/free-runner-disk

      - name: Install Rust toolchain (pinned via rust-toolchain.toml)
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: "1.97.1"

      - name: Cache Rust dependencies + build artifacts
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: backend

      - name: Serialized disposable PostgreSQL integration targets
        run: |
          tools/buck/test_needs_postgres.sh --num-threads=1 \
            //tools/buck:dispatch-p1-postgres \
            //tools/buck:attendance-cancel-substitution-postgres \
            //tools/buck:attendance-concurrency-postgres

  generated-face-authority:
    # This status is the complete generated-face closure required by policy.
    # It becomes merge-enforced only after the live GitHub ruleset admits this
    # exact status; cheap `preflight` intentionally defers expensive faces.
    name: Generated faces — full required drift authority
    runs-on: macos-latest
    needs: preflight
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Install pinned DotSlash runtime
        run: tools/buck/install_dotslash.sh

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24.16.0"
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5.4.0
        with:
          distribution: temurin
          java-version: "21"

      - name: Install Rust toolchain (pinned via rust-toolchain.toml)
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: "1.97.1"

      - name: Install lock-pinned Reindeer Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          # shellcheck source=third-party/rust/reindeer/upstream.lock
          source third-party/rust/reindeer/upstream.lock
          rustup toolchain install "$REINDEER_TOOLCHAIN" --profile minimal

      - name: Install workspace dependencies
        run: npm ci

      - name: Full generated-face closure
        run: tools/buck/preflight.sh --full-generated-faces

  backend:
    name: Backend — fmt / clippy / test / gates
    runs-on: ubuntu-latest
    needs: preflight
    # Serial integration execution avoids cluster-global role mutation races.
    #
    # NOTE: the "full workspace" this number was originally sized for no longer
    # runs here. Migration 0196 admits only `mnt_buck_admin` (with
    # `mnt.sqlx_test_bootstrap` armed) for applying migrations, so the workspace
    # run was dropped rather than re-identified. The topology step below now
    # provisions that role, so restoring the sweep is a matter of pointing it at
    # $MNT_BUCK_ADMIN_DATABASE_URL -- see H-8 in
    # docs/program/false-green-gate-holes.md. Deliberately not done here: it
    # will surface a genuine backlog and belongs in its own change.
    #
    # 90 rather than 45 because the harness performs 13 per-invocation Docker
    # bring-ups (2 Apalis + 11 guarded regressions), each with container start
    # and topology reconcile, and this branch carries considerably more code
    # than main.
    timeout-minutes: 90
    services:
      postgres:
        image: postgres:18.4
        env:
          POSTGRES_DB: mnt_ci
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - "5432:5432"
        options: >-
          --health-cmd "pg_isready -U postgres -d mnt_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/mnt_ci
      # Job-level so every step (including the gate binaries, which didn't
      # previously set it themselves) compiles query! macros against the
      # committed .sqlx cache instead of an unmigrated DB. Cheap insurance;
      # steps below still set it explicitly too, which is a harmless no-op.
      SQLX_OFFLINE: "true"
      # Backend CI compiles the full workspace, tests, and custom gates on the
      # standard GitHub runner. Keep artifacts lean so unrelated UI-only PRs do
      # not fail from runner disk exhaustion while preserving the same checks.
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Install pinned DotSlash runtime
        run: ../tools/buck/install_dotslash.sh

      - name: Free runner disk for Rust backend
        uses: ./.github/actions/free-runner-disk

      - name: Install Rust toolchain (pinned via rust-toolchain.toml)
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          # rust-toolchain.toml drives the exact version; this step just
          # ensures rustup is available and honours the file.
          toolchain: "1.97.1"
          components: rustfmt, clippy

      - name: Cache Rust dependencies + build artifacts
        # rust-cache keys on Cargo.lock + rustc + job, caches registry/git deps
        # and prunes stale crates from target/ (so the cache does not grow
        # unbounded on every Cargo.lock change the way a raw target/ cache does).
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: backend

      - name: rustfmt check
        run: cargo fmt --all -- --check

      - name: clippy -D warnings
        run: SQLX_OFFLINE=true cargo clippy --all-targets -- -D warnings
      - name: Layer-boundary gate
        run: cargo run -p mnt-gate-layer-boundary

      - name: Audit-coverage gate
        run: cargo run -p mnt-gate-audit-coverage

      - name: Migration-safety gate
        run: cargo run -p mnt-gate-migration-safety

      - name: Tenant-isolation gate
        run: cargo run -p mnt-gate-tenant-isolation

      - name: PII-no-logs gate
        run: cargo run -p mnt-gate-pii-no-logs

      - name: RLS-arming gate
        run: cargo run -p mnt-gate-rls-arming

      - name: Dev-auth-absence gate
        run: cargo run -p mnt-gate-dev-auth-absence

      - name: IaC tier-discipline gate
        run: cargo run -p mnt-gate-iac-tier
      # Migration 0165 fails closed unless the portable seven-role topology
      # already exists. Reconcile it before any database test applies the
      # migration, using the distinct service administrator only for this
      # cluster-scoped bootstrap.
      - name: PR 473 migration operational contract tests
        working-directory: .
        run: python3 scripts/check-pr473-migration-operational.test.py -v

      - name: Reconcile portable PostgreSQL role topology
        run: |
          APP_PASSWORD="$(openssl rand -hex 32)"
          RT_PASSWORD="$(openssl rand -hex 32)"
          LEAVE_COMMAND_PASSWORD="$(openssl rand -hex 32)"
          ONTOLOGY_COMMAND_PASSWORD="$(openssl rand -hex 32)"
          PLATFORM_FORCE_COMMAND_PASSWORD="$(openssl rand -hex 32)"
          docker run --rm --network host \
            -v "$GITHUB_WORKSPACE/ops/postgres-reconcile-topology.sh:/usr/local/bin/postgres-reconcile-topology:ro" \
            -e POSTGRES_HOST=127.0.0.1 -e POSTGRES_DB=mnt_ci \
            -e POSTGRES_ADMIN_USER=postgres -e POSTGRES_ADMIN_PASSWORD=postgres \
            -e MNT_APP_POSTGRES_PASSWORD="$APP_PASSWORD" \
            -e MNT_RT_POSTGRES_PASSWORD="$RT_PASSWORD" \
            -e MNT_LEAVE_COMMAND_POSTGRES_PASSWORD="$LEAVE_COMMAND_PASSWORD" \
            -e MNT_ONTOLOGY_COMMAND_POSTGRES_PASSWORD="$ONTOLOGY_COMMAND_PASSWORD" \
            -e MNT_PLATFORM_FORCE_COMMAND_POSTGRES_PASSWORD="$PLATFORM_FORCE_COMMAND_PASSWORD" \
            --entrypoint bash postgres:18.4@sha256:4aabea78cf39b90e834caf3af7d602a18565f6fe2508705c8d01aa63245c2e20 \
            /usr/local/bin/postgres-reconcile-topology

          # The Apalis adapter contract intentionally mutates only its own
          # dedicated database while proving direct mnt_app/mnt_rt logins. The
          # job-level DATABASE_URL stays on the cluster administrator for the
          # boot smoke; it can no longer serve migration-applying tests, which
          # migration 0196 restricts to the mnt_buck_admin harness identity.
          docker run --rm --network host \
            -e PGPASSWORD=postgres \
            --entrypoint psql \
            postgres:18.4@sha256:4aabea78cf39b90e834caf3af7d602a18565f6fe2508705c8d01aa63245c2e20 \
            -h 127.0.0.1 -U postgres -d postgres -v ON_ERROR_STOP=1 \
            -c "DROP DATABASE IF EXISTS mnt_apalis_contract WITH (FORCE)" \
            -c "CREATE DATABASE mnt_apalis_contract OWNER mnt_app"

          # Migration 0196 restricts migration application to a superuser named
          # exactly `mnt_buck_admin` with `mnt.sqlx_test_bootstrap` armed (and a
          # test database it owns). The `postgres` service account therefore
          # cannot apply a single migration, which silently breaks every direct
          # `cargo test` that relies on `#[sqlx::test]`. Provision that identity
          # here so those suites can run; the password crosses through a
          # mode-0600 file, never argv.
          BUCK_ADMIN_PASSWORD="$(openssl rand -hex 32)"
          umask 077
          printf "CREATE ROLE mnt_buck_admin SUPERUSER LOGIN PASSWORD '%s';\n" \
            "$BUCK_ADMIN_PASSWORD" > "$RUNNER_TEMP/buck-admin.sql"
          docker run --rm --network host \
            -e PGPASSWORD=postgres \
            -v "$RUNNER_TEMP/buck-admin.sql:/buck-admin.sql:ro" \
            --entrypoint psql \
            postgres:18.4@sha256:4aabea78cf39b90e834caf3af7d602a18565f6fe2508705c8d01aa63245c2e20 \
            -h 127.0.0.1 -U postgres -d postgres -v ON_ERROR_STOP=1 -f /buck-admin.sql
          rm -f "$RUNNER_TEMP/buck-admin.sql"

          echo "::add-mask::$APP_PASSWORD"
          echo "::add-mask::$RT_PASSWORD"
          echo "::add-mask::$BUCK_ADMIN_PASSWORD"
          {
            echo "MNT_BUCK_ADMIN_DATABASE_URL=postgres://mnt_buck_admin:${BUCK_ADMIN_PASSWORD}@localhost:5432/mnt_ci?options%5Bmnt.sqlx_test_bootstrap%5D=buck-sqlx-superuser-v1"
            echo "MNT_APALIS_OWNER_DATABASE_URL=postgres://mnt_app:${APP_PASSWORD}@localhost:5432/mnt_apalis_contract"
            echo "MNT_APALIS_RUNTIME_DATABASE_URL=postgres://mnt_rt:${RT_PASSWORD}@localhost:5432/mnt_apalis_contract"
            echo "MNT_APALIS_ADMIN_DATABASE_URL=postgres://postgres:postgres@localhost:5432/mnt_apalis_contract"
          } >> "$GITHUB_ENV"

      - name: PR 473 migration operational gate
        working-directory: .
        # SQLX_OFFLINE=true so the query! macros compile against the committed
        # .sqlx cache (the documented contract — see crates/platform/db/src/lib.rs)
        # rather than the unmigrated CI service DB. The #[sqlx::test] runtime
        # tests still use DATABASE_URL to create their own per-test databases.
        # Without this, a Cargo.lock change busts the build cache and the macros
        # re-check online against a schema-less DB ("audit_events does not exist").
        # The wrapper isolates the 11 migration regressions for exact one-test
        # runtime-result validation. It does NOT preserve a full workspace run --
        # migration 0196 restricts migration application to the mnt_buck_admin
        # harness identity, so the service DB cannot host one (H-8 in
        # docs/program/false-green-gate-holes.md). Every test binary remains
        # serial because topology upgrade tests mutate cluster-global roles.
        run: npm run check:pr473-migration-operational

      # Boot smoke: the real mnt-app binary, migrate then serve mode, against
      # this job's fresh service DB — catches route-panic-at-boot regressions
      # (e.g. a bad router/state wiring) that unit/integration tests never
      # exercise because they never call `serve()`. Mirrors the minimal env
      # image-release.yml's release-probe job uses to boot the shipped image;
      # a throwaway keypair is fine, this only proves the app comes up.
      - name: Boot smoke — migrate + serve + /readyz
        run: |
          set -e
          openssl ecparam -name prime256v1 -genkey -noout | openssl pkcs8 -topk8 -nocrypt -out /tmp/smoke-priv.pem
          openssl ec -in /tmp/smoke-priv.pem -pubout -out /tmp/smoke-pub.pem

          APP_PASSWORD="$(openssl rand -hex 32)"
          RT_PASSWORD="$(openssl rand -hex 32)"
          LEAVE_COMMAND_PASSWORD="$(openssl rand -hex 32)"
          ONTOLOGY_COMMAND_PASSWORD="$(openssl rand -hex 32)"
          PLATFORM_FORCE_COMMAND_PASSWORD="$(openssl rand -hex 32)"
          docker run --rm --network host \
            -v "$GITHUB_WORKSPACE/ops/postgres-reconcile-topology.sh:/usr/local/bin/postgres-reconcile-topology:ro" \
            -e POSTGRES_HOST=127.0.0.1 -e POSTGRES_DB=mnt_ci \
            -e POSTGRES_ADMIN_USER=postgres -e POSTGRES_ADMIN_PASSWORD=postgres \
            -e MNT_APP_POSTGRES_PASSWORD="$APP_PASSWORD" \
            -e MNT_RT_POSTGRES_PASSWORD="$RT_PASSWORD" \
            -e MNT_LEAVE_COMMAND_POSTGRES_PASSWORD="$LEAVE_COMMAND_PASSWORD" \
            -e MNT_ONTOLOGY_COMMAND_POSTGRES_PASSWORD="$ONTOLOGY_COMMAND_PASSWORD" \
            -e MNT_PLATFORM_FORCE_COMMAND_POSTGRES_PASSWORD="$PLATFORM_FORCE_COMMAND_PASSWORD" \
            --entrypoint bash postgres:18.4 /usr/local/bin/postgres-reconcile-topology

          OWNER_DATABASE_URL="postgres://mnt_app:${APP_PASSWORD}@localhost:5432/mnt_ci"
          RUNTIME_DATABASE_URL="postgres://mnt_rt:${RT_PASSWORD}@localhost:5432/mnt_ci"
          LEAVE_COMMAND_DATABASE_URL="postgres://mnt_leave_cmd:${LEAVE_COMMAND_PASSWORD}@localhost:5432/mnt_ci"
          ONTOLOGY_COMMAND_DATABASE_URL="postgres://mnt_ontology_cmd:${ONTOLOGY_COMMAND_PASSWORD}@localhost:5432/mnt_ci"
          PLATFORM_FORCE_COMMAND_DATABASE_URL="postgres://mnt_platform_force_cmd:${PLATFORM_FORCE_COMMAND_PASSWORD}@localhost:5432/mnt_ci"
          export DATABASE_URL="$OWNER_DATABASE_URL"

          MNT_APP_ROLE=migrate SQLX_OFFLINE=true cargo run -q -p mnt-app
          SQLX_OFFLINE=true cargo test --locked \
            -p mnt-gate-tenant-isolation \
            --test owner_only_acl_postgres18 -- \
            --include-ignored --test-threads=1

          MNT_APP_ROLE=api \
          DATABASE_URL="$RUNTIME_DATABASE_URL" \
          LEAVE_COMMAND_DATABASE_URL="$LEAVE_COMMAND_DATABASE_URL" \
          ONTOLOGY_COMMAND_DATABASE_URL="$ONTOLOGY_COMMAND_DATABASE_URL" \
          PLATFORM_FORCE_COMMAND_DATABASE_URL="$PLATFORM_FORCE_COMMAND_DATABASE_URL" \
          MNT_HTTP_ADDR=127.0.0.1:8092 \
          MNT_JWT_ISSUER=ci-boot-smoke \
          MNT_JWT_AUDIENCE=ci-boot-smoke \
          MNT_JWT_PRIVATE_KEY_PEM="$(cat /tmp/smoke-priv.pem)" \
          MNT_JWT_PUBLIC_KEY_PEM="$(cat /tmp/smoke-pub.pem)" \
          MNT_WEBAUTHN_RP_ID=localhost \
          MNT_WEBAUTHN_RP_ORIGIN=http://localhost \
          MNT_COOKIE_SECURE=false \
          SQLX_OFFLINE=true \
          cargo run -q -p mnt-app &
          BOOT_PID=$!

          ready=0
          for _ in $(seq 1 60); do
            if curl -sf http://127.0.0.1:8092/readyz; then
              ready=1
              break
            fi
            sleep 2
          done

          kill "$BOOT_PID" 2>/dev/null || true
          wait "$BOOT_PID" 2>/dev/null || true

          if [ "$ready" != "1" ]; then
            echo "::error::mnt-app never became ready in boot smoke"
            exit 1
          fi

      - name: Buck2 dev-auth feature PostgreSQL suites
        working-directory: .
        # `dev-auth` is NOT in default features (proven above). These exact
        # feature-sensitive auth-rest and provisioning binaries run through
        # generator-owned Buck targets and the disposable role-topology harness;
        # direct Cargo would bypass that contract.
        # That harness uses the migration-0196-authorized `mnt_buck_admin`
        # identity with `mnt.sqlx_test_bootstrap` armed, so the suites reach
        # their assertions instead of failing during migration application.
        run: |
          tools/buck/test_needs_postgres.sh --num-threads=1 \
            //tools/buck:auth-rest-dev-auth-inline-postgres \
            //tools/buck:auth-rest-dev-auth-session-postgres \
            //tools/buck:auth-rest-dev-auth-group-admin-postgres \
            //tools/buck:provisioning-dev-principal-upsert-race-postgres

      - name: Buck2 mnt-app unit suite
        working-directory: .
        run: env -u DATABASE_URL tools/buck2 test //backend/app:mnt-app-unit

      - name: Buck2 mnt-app inline PostgreSQL suites
        working-directory: .
        run: |
          tools/buck/test_needs_postgres.sh --num-threads=1 \
            //tools/buck:app-inline-postgres \
            //tools/buck:app-dev-auth-persona-guard-postgres

  dev-up-smoke:
    name: dev-up.mjs smoke — compose deps + migrate + /readyz + dev-auth e2e
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 30
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: dev-up compose contract unit test
        run: node --test scripts/dev-up-compose.test.mjs

      - name: Install pinned DotSlash runtime
        run: tools/buck/install_dotslash.sh

      - name: Free runner disk for Rust backend
        uses: ./.github/actions/free-runner-disk

      - name: Install Rust toolchain (pinned via rust-toolchain.toml)
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: "1.97.1"

      - name: Cache Rust dependencies + build artifacts
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: backend

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24"
          cache: npm

      - name: PostgreSQL topology integration regression
        run: ops/postgres-topology.integration.test.sh

      - name: dev-up bootstrap (compose deps + migrate + backend readyz)
        # macOS/Windows GH-hosted runners cannot run Linux containers reliably,
        # so this smoke test is ubuntu-only; the other OSes' acceptance is the
        # `doctor` self-check (documented deviation from the plan's "macos
        # runner" wording, per W2.5 reviewer note).
        run: node scripts/dev-up.mjs bootstrap

      - name: Confirm /readyz reachable
        run: curl -fsS "http://127.0.0.1:${MNT_DEV_HTTP_PORT:-8090}/readyz"

      - name: dev-up down
        if: always()
        run: node scripts/dev-up.mjs down

      - name: Install workspace dependencies (for the dev-auth e2e project)
        run: npm ci

      - name: Install Playwright Chromium + OS deps
        run: npx playwright install --with-deps chromium

      - name: dev-up bootstrap --features dev-auth (backend + Vite dev server)
        env:
          MNT_DEV_AUTH_E2E: "1"
        run: node scripts/dev-up.mjs bootstrap

      - name: Dev-mode e2e — role switcher against the real dev-auth stack
        # This spec CANNOT run in the "Browser E2E" job above: that suite tests
        # a production build (no dev-auth feature), and the switcher's
        # DEV-only predicate must never be weakened just to make it render
        # there (see playwright.config.ts). Both tiers are already running
        # from the previous step.
        env:
          MNT_DEV_AUTH_E2E: "1"
        run: npx playwright test --project=dev-auth

      - name: Upload dev-auth e2e report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dev-auth-e2e-report
          path: |
            e2e/.artifacts/html-report
            e2e/.artifacts/test-results
          retention-days: 7

      - name: dev-up down (dev-auth stack)
        if: always()
        run: node scripts/dev-up.mjs down

  api-clients:
    name: API clients — TypeScript / Kotlin generation and compile
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24"
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5.4.0
        with:
          distribution: temurin
          java-version: "21"

      - name: Install client tooling
        run: npm ci

      - name: Regenerate TypeScript and Kotlin clients
        run: npm run gen:api:portable

      - name: Generated client drift gate
        run: git diff --exit-code -- clients/ts clients/kotlin

      - name: TypeScript compile
        if: ${{ !cancelled() }}
        run: npm run check:ts

      - name: TypeScript generated contract tests
        if: ${{ !cancelled() }}
        run: npm run test:api-client-contract:ts

      - name: Kotlin compile and generated contract tests
        if: ${{ !cancelled() }}
        run: npm run check:kotlin

  web:
    name: Web console — lint / test / build
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24.16.0"
          cache: npm

      - name: Install workspace dependencies
        run: npm ci

      - name: ADR governance tests
        if: ${{ !cancelled() }}
        run: npm run test:adrs

      - name: ADR governance gate
        if: ${{ !cancelled() }}
        run: npm run check:adrs

      - name: Foundation gate contract
        if: ${{ !cancelled() }}
        run: npm run check:foundation-gates

      - name: Root npm workspace integrity
        if: ${{ !cancelled() }}
        run: npm run check:root-workspaces

      - name: Root npm workspace integrity tests
        if: ${{ !cancelled() }}
        run: npm run test:root-workspaces

      - name: Canonical npm lockfile
        if: ${{ !cancelled() }}
        run: npm run check:package-lock

      - name: Shared text gate unit tests
        if: ${{ !cancelled() }}
        run: npm run test:text-gate

      - name: Enterprise UX parity / no-text-wall gate
        if: ${{ !cancelled() }}
        run: npm run check:enterprise-ux-parity

      - name: Browser persona E2E matrix gate
        if: ${{ !cancelled() }}
        run: npm run check:browser-persona-matrix

      - name: iOS UI-test fail-closed workflow guard
        if: ${{ !cancelled() }}
        run: npm run check:ios-ui-test-fail-closed

      - name: Android E2E fail-closed workflow guard
        if: ${{ !cancelled() }}
        run: npm run check:android-e2e-fail-closed

      - name: G004 identity group org people policy foundation gate
        if: ${{ !cancelled() }}
        run: npm run check:g004-identity-foundation

      - name: G005 workflow approval Work Hub lifecycle gate
        if: ${{ !cancelled() }}
        run: npm run check:g005-workflow-lifecycle

      - name: Workflow runtime spine gate
        if: ${{ !cancelled() }}
        run: npm run check:workflow-runtime-spine

      - name: Workflow runtime M2 strangler dark-landing gate
        if: ${{ !cancelled() }}
        run: npm run check:workflow-runtime-m2-strangler

      - name: Workflow runtime M2 Cedar-guard observe-and-record gate
        if: ${{ !cancelled() }}
        run: npm run check:workflow-runtime-m2-cedar-guards

      - name: Workflow runtime M2 flag-ON runtime gate
        if: ${{ !cancelled() }}
        run: npm run check:workflow-runtime-m2-runtime

      - name: Workflow runtime M2 outbox-drainer transactional-idempotency gate
        if: ${{ !cancelled() }}
        run: npm run check:workflow-runtime-m2-drainer

      - name: G006 asset equipment dispatch lifecycle gate
        if: ${{ !cancelled() }}
        run: npm run check:g006-asset-dispatch-lifecycle

      - name: G007 collaboration mail calendar poll mobile lifecycle gate
        if: ${{ !cancelled() }}
        run: npm run check:g007-collaboration-mobile-lifecycle

      - name: G008 import HR payroll readiness gate
        if: ${{ !cancelled() }}
        run: npm run check:g008-payroll-readiness

      - name: People HR lifecycle maturity gate
        if: ${{ !cancelled() }}
        run: npm run check:people-hr-maturity

      - name: Payroll release-gate contract
        if: ${{ !cancelled() }}
        run: npm run check:payroll-release-gate

      - name: Financial maturity gate
        if: ${{ !cancelled() }}
        run: npm run check:financial-maturity

      - name: CX reporting BI maturity gate
        if: ${{ !cancelled() }}
        run: npm run check:cx-reporting-maturity

      - name: Operations intelligence maturity gate
        if: ${{ !cancelled() }}
        run: npm run check:operations-intelligence-maturity

      - name: Lint (eslint + UI-string gate)
        if: ${{ !cancelled() }}
        run: npm run lint --workspace @console/web

      - name: Unit tests
        if: ${{ !cancelled() }}
        run: npm run test --workspace @console/web

      - name: Production dev-auth artifact entrypoint assertions
        if: ${{ !cancelled() }}
        run: npm run test:production-dev-auth-guards --workspace @console/web

      - name: Production build
        if: ${{ !cancelled() }}
        run: npm run check:production-dev-auth-absence --workspace @console/web

  api-contract:
    name: API contract — app OpenAPI and generated TS round-trip
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:18.4
        env:
          POSTGRES_DB: mnt_contract
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - "5432:5432"
        options: >-
          --health-cmd "pg_isready -U postgres -d mnt_contract"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      CONTRACT_DATABASE_URL: postgres://postgres:postgres@localhost:5432/mnt_contract
      # This job builds mnt-app before comparing the app-served OpenAPI and
      # running the generated-client round-trip. Match the backend job's lean
      # Cargo artifacts so the standard runner does not exhaust disk on cache
      # misses.
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Install pinned DotSlash runtime
        run: tools/buck/install_dotslash.sh

      - name: Free runner disk for API contract
        uses: ./.github/actions/free-runner-disk

      - name: Install Rust toolchain (pinned via rust-toolchain.toml)
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: "1.97.1"

      - name: Cache Rust dependencies + build artifacts
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: backend

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24"
          cache: npm

      - name: Install client tooling
        run: npm ci

      - name: OpenAPI app-served drift gate
        if: ${{ !cancelled() }}
        run: npm run check:openapi-app

      - name: Capture Buck2-built app for contract test
        if: ${{ !cancelled() }}
        shell: bash
        run: |
          set -euo pipefail
          # check:openapi-app is the sole Buck2 producer for this handoff.
          mnt_app_bin="${GITHUB_WORKSPACE}/.tmp/buck2/api-contract/mnt-app"
          test -x "${mnt_app_bin}"
          printf 'MNT_APP_BIN=%s\n' "${mnt_app_bin}" >> "${GITHUB_ENV}"

      - name: Employee import replay contract
        if: ${{ !cancelled() }}
        run: npm run test:employee-import-contract

      - name: Ontology write precondition contract
        if: ${{ !cancelled() }}
        run: npm run test:ontology-write-precondition

      - name: Generated TypeScript client round-trip
        if: ${{ !cancelled() }}
        run: npm run test:contract

  kubernetes-manifests:
    name: Kubernetes manifests — render / hardening / NetworkPolicy preflight
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0

      - name: Install kubectl (for kustomize renderer)
        env:
          KUBECTL_VERSION: "v1.36.2"
        run: |
          curl -fsSLO "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl"
          curl -fsSLO "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl.sha256"
          echo "$(cat kubectl.sha256)  kubectl" | sha256sum --check
          sudo install -m 0755 kubectl /usr/local/bin/kubectl
          kubectl version --client=true

      - name: Governed command-database DARK wiring regression
        run: node --test scripts/check-command-database-wiring.test.mjs

      - name: Render manifests and NetworkPolicy enforcement preflight
        if: ${{ !cancelled() }}
        # Generic CI has no production kubeconfig, so the live readback runs in
        # warning mode here. Real deployment validation must set
        # MNT_NETWORKPOLICY_PREFLIGHT=require against the target cluster.
        env:
          MNT_NETWORKPOLICY_PREFLIGHT: warn
        run: npm run check:k8s

      - name: Production hardening contract
        if: ${{ !cancelled() }}
        run: npm run check:production-hardening

      - name: Production hardening regression tests
        if: ${{ !cancelled() }}
        run: npm run test:production-hardening

  swift-client:
    name: API client — Swift generation and build
    runs-on: macos-latest
    needs: preflight
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24"
          cache: npm

      - name: Install client tooling
        run: npm ci

      - name: Verify Swift toolchain
        run: swift --version

      - name: Regenerate Swift client
        run: npm run gen:api:swift

      - name: Generated Swift client drift gate
        run: git diff --exit-code -- clients/swift

      - name: Swift build
        if: ${{ !cancelled() }}
        run: npm run check:swift

      - name: Swift generated contract tests
        if: ${{ !cancelled() }}
        run: npm run test:api-client-contract:swift

  mobile-parity:
    name: Mobile parity — checklist and strings
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Check hardcoded UI strings across clients
        if: ${{ !cancelled() }}
        run: node scripts/check-i18n.mjs

  android-app:
    name: Android app — Gradle build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [preflight, mobile-parity]

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Set up Java
        uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5.4.0
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4.0.1

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null
          sdkmanager --install "platform-tools" "platforms;android-36" "build-tools;36.1.0"

      - name: Gradle build
        working-directory: android
        # Exclude unit-test tasks from the build step: testDebugUnitTest is run in the
        # dedicated step below, and testReleaseUnitTest cannot be run here because
        # ui-test-manifest (debugImplementation) provides ComponentActivity only to the
        # debug unit-test manifest — not the release one. AGP does not support merging
        # additional AAR manifests into the release JVM unit-test binary via any test
        # source-set scope. The debug run provides equivalent coverage; the release APK
        # is still assembled and lint-checked here.
        run: ./gradlew build -x testReleaseUnitTest -x testDebugUnitTest

      - name: Robolectric Compose UI + accessibility tests
        if: ${{ !cancelled() }}
        working-directory: android
        run: ./gradlew testDebugUnitTest

      - name: Roborazzi screenshot verification
        if: ${{ !cancelled() }}
        working-directory: android
        # Goldens are recorded and committed under android/app/src/test/snapshots.
        # verifyRoborazziDebug is the regression gate; it fails on any pixel diff.
        run: ./gradlew verifyRoborazziDebug

      - name: Upload test + Roborazzi reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: android-test-reports
          path: |
            android/app/build/reports/tests/**
            android/app/build/test-results/**
            android/app/build/outputs/roborazzi/**
          if-no-files-found: ignore
          retention-days: 7

  android-instrumented:
    name: Android app — instrumented post-login E2E (emulator)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs: [preflight, android-app]
    services:
      postgres:
        image: postgres:18.4@sha256:4aabea78cf39b90e834caf3af7d602a18565f6fe2508705c8d01aa63245c2e20
        env:
          POSTGRES_USER: postgres
          POSTGRES_DB: postgres
          POSTGRES_PASSWORD: android-${{ github.run_id }}-${{ github.run_attempt }}-admin
        ports:
          - "5432:5432"
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      CI: "true"
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"
      E2E_PG_SUPERUSER: postgres
      E2E_PG_SUPERUSER_PASSWORD: android-${{ github.run_id }}-${{ github.run_attempt }}-admin
      E2E_PG_HOST: localhost
      E2E_PG_PORT: "5432"
      E2E_DB_NAME: mnt_android_${{ github.run_id }}_${{ github.run_attempt }}
      E2E_HTTP_ADDR: 127.0.0.1:8080

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Verify exact candidate SHA before build
        run: |
          set -euo pipefail
          actual_sha="$(git rev-parse HEAD)"
          if [ "$actual_sha" != "$GITHUB_SHA" ]; then
            echo "::error title=Android E2E candidate mismatch::Checked-out HEAD $actual_sha does not match GITHUB_SHA $GITHUB_SHA."
            exit 1
          fi

      - name: Free runner disk for Rust backend and emulator
        uses: ./.github/actions/free-runner-disk

      - name: Enable KVM for hardware-accelerated emulator
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Install Rust toolchain (pinned via rust-toolchain.toml)
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: "1.97.1"

      - name: Cache Rust dependencies + build artifacts
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: backend

      - name: Set up Java
        uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5.4.0
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4.0.1
        env:
          # free-runner-disk removes the preinstalled root-owned SDK tree. Keep
          # this job's replacement SDK writable, isolated, and runner-local.
          ANDROID_HOME: ${{ runner.temp }}/android-sdk
          ANDROID_SDK_ROOT: ${{ runner.temp }}/android-sdk

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null
          sdkmanager --install "platform-tools" "platforms;android-36" "build-tools;36.1.0"

      - name: Install PostgreSQL client for the hermetic harness
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends postgresql-client

      - name: Build exact candidate backend
        working-directory: backend
        run: SQLX_OFFLINE=true cargo build --locked -q -p mnt-app

      - name: Run hermetic post-login E2E on Gradle Managed Device
        # Candidate source, PostgreSQL, database fixtures, backend, OTP, session,
        # emulator, and assertions all live in this job. No external backend or
        # long-lived refresh-token secret can drift from the tested SHA.
        run: |
          set -euo pipefail
          session_assets_dir="${RUNNER_TEMP}/android-e2e-session-assets"
          session_file="${session_assets_dir}/field-e2e-session.properties"
          export E2E_AUTH_DIR="${RUNNER_TEMP}/android-e2e-auth"
          export MNT_APP_BIN="${GITHUB_WORKSPACE}/backend/target/debug/mnt-app"

          cleanup_android_e2e() {
            if [ -s "${E2E_AUTH_DIR}/backend.pid" ]; then
              backend_pid="$(cat "${E2E_AUTH_DIR}/backend.pid")"
              kill "$backend_pid" 2>/dev/null || true
            fi
            rm -rf "$session_assets_dir" "$E2E_AUTH_DIR"
            find android/app/build -type f -name 'field-e2e-session.properties' -delete 2>/dev/null || true
            find android/app/build -type f -name '*androidTest*.apk' -delete 2>/dev/null || true
          }
          trap cleanup_android_e2e EXIT
          cleanup_android_e2e

          printf '::add-mask::%s\n' "$E2E_PG_SUPERUSER_PASSWORD"
          for role_password_var in \
            E2E_MNT_APP_POSTGRES_PASSWORD \
            E2E_MNT_RT_POSTGRES_PASSWORD \
            E2E_MNT_LEAVE_COMMAND_POSTGRES_PASSWORD \
            E2E_MNT_ONTOLOGY_COMMAND_POSTGRES_PASSWORD \
            E2E_MNT_PLATFORM_FORCE_COMMAND_POSTGRES_PASSWORD
          do
            role_password="$(openssl rand -hex 32)"
            printf '::add-mask::%s\n' "$role_password"
            export "$role_password_var=$role_password"
          done
          unset role_password

          bootstrap_otp="$(openssl rand -hex 24)"
          printf '::add-mask::%s\n' "$bootstrap_otp"
          otp_hash="$(printf '%s' "$bootstrap_otp" | sha256sum | awk '{print $1}')"

          bash e2e/harness/db.sh
          PGPASSWORD="$E2E_MNT_APP_POSTGRES_PASSWORD" psql \
            --host "$E2E_PG_HOST" \
            --port "$E2E_PG_PORT" \
            --username mnt_app \
            --dbname "$E2E_DB_NAME" \
            --set ON_ERROR_STOP=1 \
            --set "otp_hash=$otp_hash" \
            --set "fixture_profile=full" \
            --file e2e/harness/seed-mobile-ci.sql

          bash e2e/harness/boot-backend.sh
          backend_url="http://127.0.0.1:8080"
          if ! resp="$(printf '%s' "$bootstrap_otp" | jq -Rsc '{otp:.}' | curl -fsS -X POST "$backend_url/api/v1/auth/otp/redeem" \
            -H 'Content-Type: application/json' \
            --data-binary @-)"; then
            echo "::error title=Android E2E session mint failed::The job-local OTP exchange failed."
            exit 1
          fi
          if ! access_token=$(printf '%s' "$resp" | jq -er '.access_token'); then
            echo "::error title=Android E2E session mint failed::The backend response omitted access_token."
            exit 1
          fi
          if ! refresh_token=$(printf '%s' "$resp" | jq -er '.refresh_token'); then
            echo "::error title=Android E2E session mint failed::The backend response omitted refresh_token."
            exit 1
          fi
          printf '::add-mask::%s\n' "$access_token"
          printf '::add-mask::%s\n' "$refresh_token"
          unset bootstrap_otp otp_hash resp

          install -d -m 700 "$session_assets_dir"
          umask 077
          {
            printf 'FIELD_E2E_ACCESS_TOKEN=%s\n' "$access_token"
            printf 'FIELD_E2E_REFRESH_TOKEN=%s\n' "$refresh_token"
          } > "$session_file"
          chmod 600 "$session_file"
          export FIELD_E2E_SESSION_ASSETS_DIR="$session_assets_dir"

          (
            cd android
            ./gradlew fieldApi34DebugAndroidTest
          )

          # JUnit evidence is mandatory: WorkOrderFlowTest must be present and
          # must have no skipped, failed, or errored test cases.
          python3 - <<'PY'
          import pathlib
          import sys
          import xml.etree.ElementTree as ET

          result_files = pathlib.Path("android/app/build").rglob("TEST-*.xml")
          cases = []
          for result_file in result_files:
              try:
                  root = ET.parse(result_file).getroot()
              except ET.ParseError as error:
                  raise SystemExit(f"invalid Android JUnit XML {result_file}: {error}")
              for case in root.iter("testcase"):
                  if case.attrib.get("classname", "").endswith(".WorkOrderFlowTest"):
                      cases.append((result_file, case))

          if not cases:
              raise SystemExit("WorkOrderFlowTest is missing from Android JUnit test-results")
          for result_file, case in cases:
              if case.find("skipped") is not None:
                  raise SystemExit(f"WorkOrderFlowTest was skipped in {result_file}")
              failure = case.find("failure")
              error = case.find("error")
              if failure is not None or error is not None:
                  raise SystemExit(f"WorkOrderFlowTest failed or errored in {result_file}")
          print(f"verified {len(cases)} WorkOrderFlowTest JUnit case(s): executed, zero skipped/failures/errors")
          PY

          {
            echo "### Android instrumented post-login E2E"
            echo "- Candidate SHA: \`$GITHUB_SHA\`"
            echo "- Backend: checked-out candidate on job-local loopback"
            echo "- Database: job-local PostgreSQL 18.4"
            echo "- Result: WorkOrderFlowTest executed with zero skips/failures/errors"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload hermetic Android instrumented reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: android-instrumented-test-reports
          path: |
            android/app/build/outputs/androidTest-results/**
            android/app/build/reports/androidTests/**
          if-no-files-found: ignore
          retention-days: 7

      - name: Remove hermetic Android session and backend
        if: always()
        run: |
          auth_dir="${RUNNER_TEMP}/android-e2e-auth"
          if [ -s "${auth_dir}/backend.pid" ]; then
            kill "$(cat "${auth_dir}/backend.pid")" 2>/dev/null || true
          fi
          rm -rf "${RUNNER_TEMP}/android-e2e-session-assets" "$auth_dir"
          find android/app/build -type f -name 'field-e2e-session.properties' -delete 2>/dev/null || true
          find android/app/build -type f -name '*androidTest*.apk' -delete 2>/dev/null || true

  ios-app:
    name: iOS app — Swift build and behavior tests
    runs-on: macos-latest
    timeout-minutes: 30
    needs: [preflight, mobile-parity]

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Verify Swift toolchain
        run: swift --version

      - name: Swift build
        working-directory: ios
        run: swift build

      - name: Swift package tests
        if: ${{ !cancelled() }}
        working-directory: ios
        run: swift test

      - name: Behavior runner
        if: ${{ !cancelled() }}
        working-directory: ios
        run: swift run MaintenanceFieldCoreBehaviorTests

  browser-e2e:
    name: Browser E2E — Playwright (all user stories)
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:18.4
        env:
          POSTGRES_USER: postgres
          POSTGRES_DB: postgres
          # The topology bootstrap requires a real administrator credential;
          # keep the E2E service and harness on the same password-authenticated
          # contract instead of declaring a password that trust auth ignores.
          POSTGRES_PASSWORD: postgres
        ports:
          - "5432:5432"
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      CI: "true"
      # Keep the E2E Rust build lean on the standard GitHub runner; this job
      # only needs the mnt-app binary, not debug symbols.
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"
      # Point the harness (e2e/harness/*.sh) at the service as the postgres superuser.
      E2E_PG_SUPERUSER: postgres
      E2E_PG_SUPERUSER_PASSWORD: postgres
      # Playwright fixtures invoke psql after db.sh exits, so retain the same
      # libpq credential in the parent job environment for their passwordless URLs.
      PGPASSWORD: postgres
      E2E_PG_HOST: localhost
      E2E_PG_PORT: "5432"

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Free runner disk for Rust backend
        uses: ./.github/actions/free-runner-disk

      - name: Install Rust toolchain (pinned via rust-toolchain.toml)
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: "1.97.1"

      - name: Cache Rust dependencies + build artifacts
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: backend

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24"
          cache: npm

      - name: Install workspace dependencies
        run: npm ci

      - name: Install PostgreSQL client (psql for the harness seeding)
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends postgresql-client

      - name: Install Python E2E helpers
        run: |
          python3 -m venv .venv-e2e
          .venv-e2e/bin/python -m pip install --upgrade pip
          .venv-e2e/bin/python -m pip install -r e2e/requirements.txt
          echo "$PWD/.venv-e2e/bin" >> "$GITHUB_PATH"

      - name: Build backend binary (mnt-app) for the harness
        working-directory: backend
        # SQLX_OFFLINE so the query! macros compile against the committed .sqlx cache
        # (same contract as the backend job). The harness then runs target/debug/mnt-app
        # for the migrate + api roles.
        run: SQLX_OFFLINE=true cargo build -q -p mnt-app

      - name: Install Playwright Chromium + OS deps
        run: npx playwright install --with-deps chromium

      - name: Run browser E2E suite
        # e2e/run.sh: gen-keys -> db (drop+migrate+seed mnt_e2e) -> boot backend
        # (api role, /readyz) -> playwright (builds + previews web on :5173, proxies
        # /api) -> teardown. Every user-story spec across all roles, real WebAuthn
        # via the CDP virtual authenticator.
        env:
          MNT_APP_BIN: ${{ github.workspace }}/backend/target/debug/mnt-app
        run: bash e2e/run.sh

      - name: Upload Playwright report + traces
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: browser-e2e-report
          path: |
            e2e/.artifacts/html-report
            e2e/.artifacts/test-results
          retention-days: 7
