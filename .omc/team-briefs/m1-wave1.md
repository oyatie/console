# M1 Wave 1 — worker briefs

Same Hard Rules as `/Users/jasonlee/Developer/maintenance/.omc/team-briefs/m0-wave1.md` (read them). Plan: `.omc/plans/fsm-maintenance-plan.md` §M1 + §2. Spec: `.omc/specs/deep-interview-fsm-maintenance.md`.

### 1. T1.1 — registry domain + adapters + master-list importer (`backend/crates/registry/`)
- Crates per layering: `mnt-registry-domain`, `mnt-registry-application`, `mnt-registry-adapter-postgres`. Add `"crates/registry/*"` to workspace members.
- Domain: Equipment entity (장비No unique key + 호기(management no), model, VIN(차대번호), ton(톤수), 규격(좌식/입식), 동력(전동B/디젤D/LPG… — derive from 장비No prefix per the real file's MID() formulas: pos1=제조, pos2=종류, pos3=동력), 상태(임대/예비/폐기/대체 등 — survey actual values in the file), hours(가동시간), 차량가액, 잔존가(**CAN BE NEGATIVE** — i64/decimal, not unsigned), 임대료, 년식, 보험 fields, 자산처/자산등록일/임대시작일), Customer(계약처)/Site(사업장) entities, branch FK (kernel BranchId; importer assigns a default HQ branch — create one branch row in the importer if roster hasn't; document).
- Migration **0007** (registry tables: equipment, customers, sites; FKs; unique on 장비No; partial indexes for 상태).
- Importer: parses the REAL `docs/reference/master-list_251120.xlsx` (4 sheets — primary `K&L 지게차 Master list` header row 3 data rows 4..447, sheet 2 `예비 및 여유차량` header row 4 with 잔존가 col U incl. negatives; sheets 3-4 are pivots — DO NOT import, they're derived). Reader crate: verify live — `calamine` (read-oriented) or umya-spreadsheet (already a dep); pick and justify in commit.
- Idempotent upsert keyed on 장비No (호기 as secondary); reconciliation report struct (added/updated/unchanged/orphaned counts + row-level errors with sheet/row refs); dirty rows (missing 장비No, malformed dates) collected as errors, never panic, never partial-write a failed row.
- Tests (#[sqlx::test] + unit): import of the REAL file yields ≥440 equipment rows (assert exact count discovered from the file and pin it); re-import = no-op (0 added/0 updated); a modified copy produces correct reconciliation diff; 호기→model lookup query works (the 접수 use case: input #290 → GTS25DE-style answer); negative 잔존가 row roundtrips; 장비No prefix decomposition matches the file's own MID()-formula columns for all rows (self-consistency check against columns B-E).
- Full verification suite per Hard Rules before commit.

### 2. T1.2 — workorder domain (`backend/crates/workorder/domain` → `mnt-workorder-domain`)
- Pure domain crate (NO sqlx/axum/tokio — layer gate enforces). Study `mnt-compliance-domain` for the established FSM-on-kernel-Transition pattern.
- 16-state `WorkOrderStatus` FSM exactly matching the prior project (verified): RECEIVED, UNASSIGNED, ASSIGNED, IN_PROGRESS, REPORT_SUBMITTED, ADMIN_REVIEW, FINAL_COMPLETED, REJECTED, ON_HOLD, DELAYED, TEMPORARY_ACTION, PART_WAITING, EQUIPMENT_IN_USE, REVISIT_REQUIRED, ARCHIVED, CANCELLED — with an EXPLICIT transition table (const data, not match-arm sprawl) including guards: transition logic from prior project (RECEIVED/UNASSIGNED→ASSIGNED on assignment; ASSIGNED/RECEIVED/UNASSIGNED→IN_PROGRESS on start; IN_PROGRESS→REPORT_SUBMITTED; REPORT_SUBMITTED→ADMIN_REVIEW; ADMIN_REVIEW→FINAL_COMPLETED|TEMPORARY_ACTION; any-active→REJECTED; any→ARCHIVED admin-only; plus DELAYED/ON_HOLD/PART_WAITING/EQUIPMENT_IN_USE/REVISIT_REQUIRED transitions — derive the full legal set from the plan + prior schema semantics, document each edge with a one-line rationale in the table).
- `PriorityLevel` (P1/P2/P3/OUTSOURCE/UNSET), `DelayReason` (8: PART_WAITING, CUSTOMER_ABSENT, EQUIPMENT_IN_USE, MECHANIC_OVERLOADED, OUTSOURCE_DELAY, ADDITIONAL_FAULT_FOUND, SAFETY_ISSUE, OTHER), `WorkResultType` (COMPLETED, TEMPORARY_ACTION, INCOMPLETE, REVISIT_REQUIRED, UNKNOWN), `AttachmentStage` (REQUEST/BEFORE/DURING/AFTER/REPORT/OUTSOURCE_RESULT).
- Approval-line model: ordered 3-step MECHANIC→ADMIN→EXECUTIVE with unlock rules (mechanic auto-approved on report; admin unlocks after mechanic; executive after admin; status advances when all non-MECHANIC steps approved) — as typed domain structs, not JSON.
- Multi-assignment value objects (2인작업: 주/부 roles, ≥1 assignee).
- WorkOrder aggregate with invariants (e.g., cannot FINAL_COMPLETED without approval line complete — the WORM evidence interlock hook point is a domain trait/port, defined here, implemented later in T1.4).
- NOTE: P1 broadcast states are NOT in this FSM (separate dispatch domain, M2 — plan §2.2 FSM clarification).
- Tests: EVERY cell of the 16×16 transition matrix asserted (legal accepted / illegal rejected — table-driven, 256 cases); approval unlock sequence tests; multi-assignment invariants; serde roundtrips for all enums (snake_case or SCREAMING_SNAKE — match prior project's wire format: SCREAMING_SNAKE_CASE).

### 3. T1.3 — workorder application + adapter + REST + OpenAPI (`backend/crates/workorder/{application,adapter-postgres,rest}`)
- Build on the MERGED mnt-workorder-domain (16-state FSM, approval line, guards) — READ it first; do not modify domain semantics.
- Application use-cases (ports per layering): 접수 intake (호기→equipment lookup via registry application), priority/triage set, assignment (2인작업 주/부, ≥1 assignee), start-work, submit-report (resultType + diagnosis/action), approval gates (계획 daily-plan approval, 완료 completion ADMIN_REVIEW→FINAL_COMPLETED honoring the evidence-interlock port — wire a Postgres-backed implementation that consults an `evidence_verified` flag field for now owned by T1.4's pipeline, default FALSE means interlock blocks; document), 외주 transition + vendor fields, target-change request/review flow, daily-work-plan DRAFT→REQUESTED→APPROVED→REJECTED→FINAL_CONFIRMED.
- EVERY state mutation through mnt-platform-db with_audit (action codes: work_order.create/assign/start/report/approve/reject/target_change.request/target_change.review/daily_plan.request/daily_plan.review/daily_plan.confirm…).
- Migration **0008**: work_orders (requestNo unique auto YYYYMMDD-NNN, branch_id non-null FK, equipment/customer/site FKs, status, priority, delay fields, result fields, kpi_excluded, approval-line table not JSON: work_order_approval_steps), work_order_assignments (N:M with role 주/부), work_order_status_history, target_change_requests, daily_work_plans + items, outsource_vendors + outsource_works (status enum from prior project), evidence_verified bool default false on work_orders.
- REST (mnt-workorder-rest): axum routers with utoipa annotations (verify utoipa current version live; plan said 5.x + utoipa-axum); JWT auth (mnt-platform-auth verifier) + mnt-platform-authz feature checks per route (matrix semantics: 가능/제한/요청); branch scoping default-deny on every query (registry+WO rows filtered by principal scope).
- OpenAPI: emit to /openapi/openapi.yaml via a binary or test that writes the utoipa-generated spec; COMMIT the yaml; a test fails if committed yaml drifts from generated (the T1.9 backbone).
- Wire routers into mnt-app (mount under /api/v1) — mnt-app stays thin composition.
- Tests: #[sqlx::test] per use-case asserting state change AND audit row in same tx (reuse with_audit test patterns); authz: MECHANIC cannot approve (403-kind), cross-branch WO invisible; requestNo generation race-safe (two concurrent creates same day → distinct sequential numbers); full intake→assign→start→report→approve E2E test through application layer; interlock: approve-completion with evidence_verified=false → Conflict.
- Full verification suite per Hard Rules (sqlx prepare --all-targets, fmt, clippy, tests, all 4 gates).
