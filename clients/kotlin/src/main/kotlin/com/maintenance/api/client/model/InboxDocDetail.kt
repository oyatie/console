/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package com.maintenance.api.client.model


import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual

/**
 * A single inbox document. Extends InboxDocSummary with the body `payload`, present only when readable (a payslip, or an already-confirmed legal notice) and omitted while a legal notice is locked.
 *
 * @param id
 * @param recipientUserId
 * @param kind
 * @param title
 * @param locked True while a legal notice awaits receipt confirmation — its body is withheld until confirmed. Always false for payslips.
 * @param createdAt
 * @param noticeType Statutory subtype for a legal notice (근로계약/취업규칙/연차촉진/노무수령거부).
 * @param legalBasis Statutory basis surfaced in the passkey gate (e.g. 근로기준법 §61).
 * @param sourceKind Producing-object kind (e.g. workflow_run, payroll_run).
 * @param sourceId Producing-object id (e.g. the AP- run code).
 * @param confirmedBy The recipient who confirmed receipt; null until confirmed.
 * @param confirmedAt When receipt was confirmed (the legal receipt timestamp); null until then.
 * @param payload Rendered document payload (legal prose paragraphs, or payslip figures).
 */
@Serializable

data class InboxDocDetail (

    @Contextual @SerialName(value = "id")
    val id: java.util.UUID,

    @Contextual @SerialName(value = "recipient_user_id")
    val recipientUserId: java.util.UUID,

    @SerialName(value = "kind")
    val kind: InboxDocDetail.Kind,

    @SerialName(value = "title")
    val title: kotlin.String,

    /* True while a legal notice awaits receipt confirmation — its body is withheld until confirmed. Always false for payslips. */
    @SerialName(value = "locked")
    val locked: kotlin.Boolean,

    @Contextual @SerialName(value = "created_at")
    val createdAt: java.time.OffsetDateTime,

    /* Statutory subtype for a legal notice (근로계약/취업규칙/연차촉진/노무수령거부). */
    @SerialName(value = "notice_type")
    val noticeType: kotlin.String? = null,

    /* Statutory basis surfaced in the passkey gate (e.g. 근로기준법 §61). */
    @SerialName(value = "legal_basis")
    val legalBasis: kotlin.String? = null,

    /* Producing-object kind (e.g. workflow_run, payroll_run). */
    @SerialName(value = "source_kind")
    val sourceKind: kotlin.String? = null,

    /* Producing-object id (e.g. the AP- run code). */
    @SerialName(value = "source_id")
    val sourceId: kotlin.String? = null,

    /* The recipient who confirmed receipt; null until confirmed. */
    @Contextual @SerialName(value = "confirmed_by")
    val confirmedBy: java.util.UUID? = null,

    /* When receipt was confirmed (the legal receipt timestamp); null until then. */
    @Contextual @SerialName(value = "confirmed_at")
    val confirmedAt: java.time.OffsetDateTime? = null,

    /* Rendered document payload (legal prose paragraphs, or payslip figures). */
    @Contextual @SerialName(value = "payload")
    val payload: kotlin.collections.Map<kotlin.String, kotlinx.serialization.json.JsonElement>? = null

) {

    /**
     *
     *
     * Values: PAYSLIP,LEGAL_NOTICE
     */
    @Serializable
    enum class Kind(val value: kotlin.String) {
        @SerialName(value = "payslip") PAYSLIP("payslip"),
        @SerialName(value = "legal_notice") LEGAL_NOTICE("legal_notice");
    }

}
